; Modular ERB: OMOGATARI/TUTORIAL/Tutorial Expansion.ERB

@TUTORIAL_NAS
#DIM チュート行数保管
#DIM FIRST_LINE
#DIM ML_COUNT
#DIM DYNAMIC PAGE = 0
#DIM CONST TU_PADDING = 38
#DIM TU_PAD_FULL
#DIM TU_SEL_LINES
#DIMS DYNAMIC HTML
CLEARLINE 1
PRINTL 
CALL 画像表示("立ち絵_服_通常_109", "right", 0, 130, 110)
CALL 画像描画終了
PRINTL 
PRINTL 
PRINTL 
SETBGCOLOR 0, 0, 102
PRINTFORML ............
PRINTFORML .........
PRINTFORML ......
PRINTFORML 정신을 차리자, 당신은 이상한 방에 있는 자신을 발견한다.
PRINTFORML 당신 앞에는 깊고 푸른 머리와 붉은 수면모를 쓴 소녀가 앉아 있다.
PRINTW 
SETCOLOR 0xE6E6FA
CALL DQPRINT("「왼쪽도 오른쪽도, 위도 아래도, 안도 밖도, 그리고 그때와 지금도 없는 꿈의 세계에 오신 것을 환영합니다.」")
SELECTCASE FLAG:592
	CASE 0
		IF CFLAG:[[ドレミー]]:面識
			CALL DQPRINT("「아라, 나를 아는구나?」")
			CALL DQPRINT("「네가 알고 있는 나랑 '나'라는 존재가 같은 건지... 한번 알아내 보는 게 어때?」")
			PRINTFORMDW 소녀는 유혹적으로 미소 짓는다...
		ELSE
			CALL DQPRINT("「나는 이 장소의 안내자 역할을 한다.」")
			CALL DQPRINT("「나를 꿈의 관리자라고 생각해 주렴.」")
			PRINTFORMDW 소녀가 공손하게 자신을 소개하고, 당신도 그에 맞춰 응답한다.
		ENDIF
	CASE 1
		CALL DQPRINT("「또 여기로 왔구나.」")
		CALL DQPRINT("「자, 그럼 이번 기회에 무엇을 듣고 싶으신가요?」")
		PRINTFORMDW 소녀는 침착한 표정으로 당신에게 인사한다.
	CASE 2
		CALL DQPRINT("「아, 다시 만나서 기쁘군.」")
		CALL DQPRINT("「내가 하는 말을 좀 더 들어줘, 단 잠을 너무 많이 자지는 않도록 조심해.」")
		PRINTFORMDW 소녀는 당신을 초대하며 미소 짓는다...
	CASE 3
		CALL DQPRINT("「반복적인 성원에 감사드립니다.」")
		CALL DQPRINT("「많은 환대를 해드릴 수는 없지만 계속 찾아와 주십시오.」")
		PRINTFORMDW 소녀가 부드러운 눈빛으로 당신을 안으로 안내한다...
	CASEELSE
		SELECTCASE RAND:5
			CASE 0
				CALL DQPRINT("「또 여기로 왔구나.」")
				CALL DQPRINT("「자, 이번 기회에 무엇을 듣고 싶으신가요?」")
				PRINTFORMDW 소녀는 침착한 표정으로 당신에게 인사했다.
			CASE 1
				CALL DQPRINT("「아, 다시 만나서 기쁘군요.」")
				CALL DQPRINT("「내가 하는 말을 좀 더 잘 들어줘, 다만 너무 깊이 잠들지는 않도록 해.」")
				PRINTFORMDW 소녀는 당신을 초대하면서 미소 짓는다...
			CASE 2
				CALL DQPRINT("「반복적인 성원에 감사드립니다.」")
				CALL DQPRINT("「많은 환대는 해드릴 수 없지만, 계속 방문해 주세요.」")
				PRINTFORMDW 소녀가 부드러운 눈빛으로 당신을 안내한다...
			CASE 3
				IF CFLAG:[[ドレミー]]:面識
					CALL DQPRINT("「현실 세계에서 만났다고 말하는 거야?」")
					CALL DQPRINT("「내가 아는 나와 『나』라는 내가 같은 건지... 한번 알아내 보면 어떨까?」")
					PRINTFORMDW 소녀는 유혹적으로 미소 짓는다...
				ELSE
					CALL DQPRINT("「그냥 『이야기』를 듣는 건 좀 지루하다고 말하는 거야?」")
					CALL DQPRINT("「후후...... 희망을 품었나 보군? 뭐, 어쩌면 언젠가는 말이야.」")
					PRINTFORMDW 소녀는 유혹적으로 미소 짓는다...
				ENDIF
			CASEELSE
				CALL DQPRINT("「내가 네게 준 베개는 어떠니?」")
				CALL DQPRINT("「아, 그래, 알겠다. 결국 내 걸로 만든 작품 중 하나였군♪」")
				PRINTFORMDW 소녀는 자만심에 미소 짓는다...
		ENDSELECT
ENDSELECT
PRINTL 
CALL DQPRINT("「자. 이제 네가 여기 왔으니 궁금한 점이 있을 거라 확신해.」")
CALL DQPRINT("「...무엇을 알고 싶으신가요?」",2)
DRAWLINE
PRINTL 
$LOOP01
FIRST_LINE = LINECOUNT
TU_PAD_FULL = TU_PADDING+8
DO
CLEARLINE LINECOUNT - FIRST_LINE
チュート行数保管 = 0
VARSET ML_COUNT
VARSET HTML
RESETCOLOR
FOR LOCAL,0,1000
VARSET TUTORIAL_ENTRY_NAME
VARSET TUTORIAL_ENTRY_TYPE
VARSET TUTORIAL_ENTRY_SHOW
TRYCCALLFORM TUTORIAL_ENTRY_INFO_{LOCAL}
CATCH
CONTINUE
ENDCATCH
SIF TUTORIAL_ENTRY_TYPE != TUTORIAL_TYPES:PAGE
	CONTINUE
HTML += @"<button value='{LOCAL}'>[{LOCAL,3}] - %TUTORIAL_ENTRY_NAME,TU_PADDING,LEFT%</button>"
ML_COUNT++
IF ML_COUNT % (MAXWIDTH()/(TU_PAD_FULL)) == 0
	HTML_PRINT HTML
	VARSET ML_COUNT
	VARSET HTML
ENDIF
NEXT
IF ML_COUNT % (MAXWIDTH()/(TU_PAD_FULL))
	HTML_PRINT HTML
ENDIF
DRAWLINE
FOR LOCAL,0,VARSIZE("TUTORIAL_TYPES")
SIF PAGE == LOCAL
	SETCOLOR C_AQUA
PRINTBUTTON @"%TUTORIAL_TYPES:LOCAL%%" "*5%", LOCAL
RESETCOLOR
NEXT
PRINTL
DRAWLINE
IF FLAG:チュートログ == 0;FLAG:594
	PRINTL [996] 방금 네가 한 말을 하나 잊어버렸다 (메시지 로그 유지)
ELSE
	PRINTL [996] 지금 당신이 하는 모든 말에 집중하고 싶다 (모든 줄을 지운다)
ENDIF
PRINTL 
PRINTL [997] - 더 빠르게 말해라
PRINTL [998] - 그건 그렇고...
PRINTL [999] - 딱히 떠오르는 건 없다
INPUT
SELECTCASE RESULT
	CASE 999
		$TUTORIALEND
		CALL DQPRINT("「어라? 돌아가는 거야?」")
		SELECTCASE RAND:2
			CASE 0
				CALL DQPRINT("「...잘 자, 좋은 하루 보내렴. 아, 잠깐, 좋은 하루를 말한 거야.」", 1)
			CASE 1
				CALL DQPRINT("「그럼... 잘 지내요. 좋은 각성이 되길 바랄게요.」", 1)
		ENDSELECT
		PRINTL 
		RESETCOLOR
		RESETBGCOLOR
		;消去行数は今のところ固定（最初～））
		CLEARLINE LINECOUNT - gLineCount
		PRINTL 
		RETURN
	CASE 998
		SELECTCASE RAND:7
			CASE 0
				CALL DQPRINT("「꽤 졸려 보이네.」", FLAG:594)
				CALL DQPRINT("「아아... 내 이야기는 좀 길어서 어쩔 수 없겠지.」", FLAG:594)
				CALL DQPRINT("「하지만 꿈속에서 어떻게 잠들 수 있겠어?」", FLAG:594)
				チュート行数保管 ++
			CASE 1
				CALL DQPRINT("「재확인하고 재확인하는 것이 근본이다.」", FLAG:594)
				CALL DQPRINT("「가능한 모든 곳에서 어떤 종류의 정보라도 찾아보라.」", FLAG:594)
			CASE 2
				CALL DQPRINT("「아주 천천히 말하고 있다고 말하는 건가?」", FLAG:594)
				CALL DQPRINT("「하지만 그 책은 그것이 『진정한 공연』의 일부라고 말한다.」", FLAG:594)
				CALL DQPRINT("「하지만 그것으로도 충분하지 않다면, 내가 어떤 소리를 내길 원해? 비프 비프처럼?」", FLAG:594)
				チュート行数保管 ++
			CASE 3
				CALL DQPRINT("「자장가를 불러줄까?」", FLAG:594)
				CALL DQPRINT("「...더 이상 머리에 넣을 게 없다는 거야? 알겠어.」", FLAG:594)
			CASE 4
				CALL DQPRINT("「어? 잠잘 때 아름다운 여자가 옆에 있다는 게 네가 생각했던 것처럼 좋다고 생각해?」", FLAG:594)
				CALL DQPRINT("「...악몽과 혼동하지 않도록 해라.」", FLAG:594)
				CALL DQPRINT("「나는 존경받는 악몽 먹는 자이기 때문이다.」", FLAG:594)
				チュート行数保管 ++
			CASE 5
				CALL DQPRINT("「왜 매일 아침 제 시간에 일어난다고 생각해요?」", FLAG:594)
				CALL DQPRINT("「...후후, 상상에 맡길게.」", FLAG:594)
			CASEELSE
				CALL DQPRINT("「...흠, 왜 이런 일을 하고 있는 거지?」", FLAG:594)
				CALL DQPRINT("「꿈속이니까 그래. 그래서 메타적인 말을 하는 건 여기선 괜찮아.」", FLAG:594)
		ENDSELECT
		チュート行数保管 += 2
	CASE 997
		SIF FLAG:チュート文速度 > -6
			FLAG:チュート文速度 -= 2
		$LOOP_SpeedTest
		CALL DQPRINT("「아... 그럼 이걸 해보는 건 어때?」")
		CALL ASK_M("조금 더 빠르게",FLAG:チュート文速度 > -8,"조금 더 천천히",FLAG:チュート文速度 < 6,"그대로 두어도 괜찮다.",1)
		SELECTCASE RESULT
			CASE 0
				CLEARLINE 4
				IF FLAG:チュート文速度 == -7
					CALL DQPRINT("「나는 내가 낼 수 있는 한 최대한 빨리 말하고 있다고 확신해. 더 빠르게 하는 건 내 능력 밖이야...」")
					CALL DQPRINT("「에? 내 태도가 거만해 보이니?」")
					CALL DQPRINT("「실례지만, 그건 설계상 그런 거예요. 아, 네.」")
				ELSE
					FLAG:チュート文速度 = MAX(FLAG:チュート文速度 - 2, -7)
					GOTO LOOP_SpeedTest
				ENDIF
			CASE 1
				CLEARLINE 4
				FLAG:チュート文速度 += 2
				GOTO LOOP_SpeedTest
			CASE 2
				CLEARLINE 4
				CALL DQPRINT("「참 까다로운 사람이구나.」")
		ENDSELECT
	CASE 996
		IF FLAG:チュートログ == 0
			CALL DQPRINT("「음, 정말 많은 정보네.」")
			FLAG:チュートログ = 2
			CALL DQPRINT("「...이건 어때? 뭐라고? 꿈에 대한 내 힘을 적용한 것일 뿐이야.」", FLAG:チュートログ)
			CLEARLINE 1
		ELSE
			CALL DQPRINT("「호호, 당신은 간단한 것을 선호하는군요.」", FLAG:チュートログ)
			CLEARLINE 1
			FLAG:チュートログ = 0
			;astro fighter sunred reference...?
			;nah, it's an old saying for things being very easy/smooth, ちょちょいのちょい variation is used much more commonly nowadays
			CALL DQPRINT("「나에겐 그저 졸음일 뿐이야. 후후, 그럼 이걸 어때?」")
		ENDIF
	CASE IS >= 100
		TU_SEL_LINES = LINECOUNT
		TRYCALLFORM TUTORIAL_ENTRY_{RESULT}
		チュート行数保管 = LINECOuNT - TU_SEL_LINES
		SIF FLAG:チュートログ != 0
			CLEARLINE チュート行数保管
		FLAG:チュート文累計 += チュート行数保管
		CALL TUTORIAL_BONUS()
	CASE 0 TO VARSIZE("TUTORIAL_TYPES")
		PAGE = RESULT
ENDSELECT
LOOP 1

@TUTORIAL_TALENT_MENU
#DIMS html
#DIMS htmlButton
#DIM itemCount
#DIM nInputResult
#DIM lLineCount
#DIM CONST lineWidth = 35
gLinecount = LINECOUNT
DO
$Loop
CLEARLINE LINECOUNT - gLinecount
VARSET html
VARSET itemCount
PRINTFORML 【재능】은 대략 여기서 설명한다.
DRAWLINE
FOR LOCAL,0,VARSIZE("TALENT")
;blacklist
SIF GROUPMATCH(LOCAL,132,165,224,239,243,144,27,297)
	CONTINUE
IF STRLENS(TALENT_TR(LOCAL))
	html += @"%HTML_BUTTON(@"%TALENT_TR(LOCAL),lineWidth-6,LEFT%",LOCAL,3)%"
	itemCount ++
	SIF itemCount % (MAXWIDTH()/lineWidth) == 0
		html += "<br>"
ENDIF
NEXT
HTML_PRINT html
PRINTFORMLC [1000] 리턴
INPUT
nInputResult = RESULT
SELECTCASE nInputResult
	CASE 1000
		RETURN
	CASE 0 TO VARSIZE("TALENT")
		PRINTFORML 【%TALENT_TR(nInputResult)%】
		TRYCCALLFORM DICTIONARY_TALENT_{nInputResult}
		CATCH
		;multi-value talents
		IF (STRLENS(GET_TALENTNAME_TR(nInputResult,-1,MASTER)) && GET_TALENTNAME_TR(nInputResult,1,MASTER) != GET_TALENTNAME_TR(nInputResult,-1,MASTER)) || (STRLENS(GET_TALENTNAME_TR(nInputResult,2,MASTER)) && GET_TALENTNAME_TR(nInputResult,1,MASTER) != GET_TALENTNAME_TR(nInputResult,2,MASTER))
			lLineCount = LINECOUNT
			DO
			CLEARLINE LINECOUNT - lLineCount
			TRYCCALLFORM DICTIONARY_TALENT_{nInputResult}_SELECTION
			CATCH
			VARSET html
			FOR LOCAL,-20, 20
			VARSET htmlButton
			IF STRLENS(GET_TALENTNAME_TR(nInputResult,LOCAL,MASTER))
				htmlButton '= HTML_BUTTON(GET_TALENTNAME_TR(nInputResult,LOCAL,MASTER), LOCAL, 3)
				SIF TALENT_VALUE(nInputResult,MASTER,LOCAL) && STRLENS(COLOR_FROM_VALUE(TALENT_VALUE(nInputResult,MASTER,LOCAL)))
					htmlButton '= HTML_COLOR(htmlButton,COLOR_FROM_VALUE(TALENT_VALUE(nInputResult,MASTER,LOCAL)))
				html += @"%htmlButton%<br>"
			ENDIF
			NEXT
			HTML_PRINT html
			ENDCATCH
			PRINTFORMLC [1000] 리턴
			INPUT
			SELECTCASE RESULT
				CASE 1000
					GOTO Loop
				CASEELSE
					PRINTFORML 【%GET_TALENTNAME_TR(nInputResult,RESULT,MASTER)%】
					IF RESULT < 0
						LOCALS = DICTIONARY_TALENT_{nInputResult}_NEG_{ABS(RESULT)}
					ELSE
						LOCALS = DICTIONARY_TALENT_{nInputResult}_{RESULT}
					ENDIF
					TRYCCALLFORM %LOCALS%
					CATCH
					PRINTFORML %TALENT_INFO(nInputResult,RESULT,MASTER)%
					ENDCATCH
					WAIT
			ENDSELECT
			LOOP 1
		ELSE
			;sngle line variant
			PRINTFORML %TALENT_INFO(nInputResult,1,MASTER)%
		ENDIF
		ENDCATCH
		WAIT
ENDSELECT
LOOP 1

@TUTORIAL_YOUJUTSU_MENU
#DIMS html
#DIMS htmlButton
#DIM itemCount
#DIM nInputResult
#DIM lLineCount
#DIM CONST lineWidth = 35
gLinecount = LINECOUNT
DO
$Loop
CLEARLINE LINECOUNT - gLinecount
VARSET html
VARSET itemCount
PRINTFORML 「주술 능력」은 대략 여기서 설명된다.
DRAWLINE
FOR LOCAL,0,64
;check
VARSET NAS_ESP_CanDo
TRYCALLFORMF NAS_ESP_{LOCAL}
SIF !NAS_ESP_CanDo
	CONTINUE
html += @"%HTML_BUTTON(@"%EsperAbilitySTR(LOCAL),lineWidth-6,LEFT%",LOCAL,3)%"
itemCount ++
SIF itemCount % (MAXWIDTH()/lineWidth) == 0
	html += "<br>"
NEXT
HTML_PRINT html
PRINTFORMLC [1000] 리턴
INPUT
nInputResult = RESULT
SELECTCASE nInputResult
	CASE 1000
		RETURN
	CASE 0 TO 63
		PRINTFORML 【%EsperAbilitySTR(nInputResult)%】
		lLineCount = LINECOUNT
		DO
		CLEARLINE LINECOUNT - lLineCount
		TRYCCALLFORM DICTIONARY_YOUJUTSU_{nInputResult}_SELECTION
		CATCH
		VARSET html
		FOR LOCAL,0, 100
		VARSET htmlButton
		;check
		VARSET NAS_ESP_NAME
		TRYCALLFORM NAS_ESP_{nInputResult}_{LOCAL}
		IF STRLENS(NAS_ESP_NAME)
			htmlButton '= HTML_BUTTON(NAS_ESP_NAME, LOCAL, 3)
			html += @"%htmlButton%<br>"
		ENDIF
		NEXT
		HTML_PRINT html
		ENDCATCH
		PRINTFORMLC [1000] 리턴
		INPUT
		SELECTCASE RESULT
			CASE 1000
				GOTO Loop
			CASEELSE
				LOCAL:8 = RESULT
				TRYCALLFORM NAS_ESP_{nInputResult}_{LOCAL:8}
				PRINTFORML 【%NAS_ESP_NAME%】
				PRINTFORML MP 필요량: {NAS_ESP_COST}
				LOCALS = DICTIONARY_YOUJUTSU_{nInputResult}_{LOCAL:8}
				TRYCCALLFORM %LOCALS%
				CATCH
				PRINTFORML %NAS_ESP_DESC%
				ENDCATCH
				WAIT
		ENDSELECT
		LOOP 1
ENDSELECT
LOOP 1
