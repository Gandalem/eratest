; Modular ERB: OMOGATARI/Lily.ERB
;Yuri stuff (the NPC vs NPC kind of thing)

@ONANISM_NAS
#DIM 欲求不満度
#DIM 満足度
#DIM 嫉妬度
#DIM 珠入手倍率
#DIM 部位指定
#DIM 追加Ｖ自慰
#DIM 追加Ａ自慰
#DIM 追加Ｂ自慰
#DIM LocalMStim
#DIM LocalUStim
#DIM 自慰タイプ
#DIM 自慰発生閾値
#DIM CONST 文字サイズ = 15
#DIMS 自慰画像
#DIM CHARA
#DIM CharaVStim
#DIM CharaAStim
#DIM CharaBStim
#DIM CharaMStim
#DIM CharaUStim
#DIM CharaYuri
VARSET LOCAL
FOR LOCAL,0,CHARANUM
SIF CFLAG:LOCAL:出禁
	CONTINUE
SIF IsDead:LOCAL:0
	CONTINUE
部位指定 = 0
自慰タイプ = 0
追加Ｖ自慰 = 0
追加Ａ自慰 = 0
追加Ｂ自慰 = 0
LocalMStim = 0
LocalUStim = 0
CharaYuri = 0
CHARA = -1
自慰発生閾値 = LIMIT(750 - 200 * TALENT:LOCAL:自慰しやすい - 100 * TALENT:LOCAL:快感応答 - 100 * TALENT:LOCAL:自己愛 + 200 * TALENT:LOCAL:自制心,500,999)
自慰発生閾値 -= 300 * TALENT:LOCAL:自慰狂い
IF GET_INT(LOCAL, "Religion", TALENT:LOCAL:Religion, "Masturbation") > 0
	GOTO ForceFap
ELSEIF GET_INT(LOCAL, "Religion", TALENT:LOCAL:Religion, "Masturbation") > -3
	自慰発生閾値 += (100*-(GET_INT(LOCAL, "Religion", TALENT:LOCAL:Religion, "Masturbation")))
ENDIF
IF CFLAG:LOCAL:オナバレ
	PRINTFORML %CALLNAME:LOCAL% %HIS_HER(LOCAL)%의 얼굴을 베개에 묻고 %HIM_HER(LOCAL)% 자신을 위로했다.
ELSEIF CFLAG:LOCAL:溜まってる度 > 自慰発生閾値
	$ForceFap
	LOCAL:1 = 0
	IF TALENT:LOCAL:愛欲
		LOCAL:1 = 4
	ELSEIF TALENT:LOCAL:恋慕
		LOCAL:1 = 3
	ELSEIF TALENT:LOCAL:セフレ
		;愛人もここ
		LOCAL:1 = 2
	ELSEIF TALENT:LOCAL:思慕
		LOCAL:1 = 1
	ENDIF
	IF TALENT:LOCAL:自制心 && LOCAL:1 < 5
		LOCAL:1 += 1
	ENDIF
	SIF !RAND:(8 - LOCAL:1) && GETBIT(nNPCSex,0)
		CharaYuri = 1
	SIF CharaYuri && LOCAL != MASTER
		GOTO MultiFapNewChara
	SIF !nNPCMasturb && LOCAL != MASTER
		CONTINUE
	SIF !FLAG:自慰画像非表示
		CALL PrintOnanismImage(LOCAL)
	IF LOCAL == MASTER
		TRYCALLFORM DAILY_CONDITION_EV1000
	ELSEIF CFLAG:LOCAL:既成事実 & 合意_うふふ && !RAND:(8 - LOCAL:1)
		;custom breakeng, this sentence gets too long
		PRINTFORML %BREAKENG(@"견디기 힘든 욕망에 온몸이 계속 욱신거리지만, %조사처리(CALLNAME:LOCAL,"는")% 머지않아 당신과 몸을 섞을 날을 떠올리며 마음을 달래고 억지로 잠을 청한다...")%
		自慰タイプ = 7
		;CALL KOJO_MESSAGE_SEND("EVENT",22,LOCAL,7)
		CFLAG:LOCAL:溜まってる度 = LIMIT(CFLAG:LOCAL:溜まってる度 + 100, 0 ,1000)
		CALL CharacterSinned(LOCAL,"Masturbation",@"자위를 참았다.", 1, 1)
		SIF !CFLAG:LOCAL:神社在住
			CFLAG:LOCAL:翌日来訪 = 1
		PRINTL
	ELSEIF ApplicablePrecept(LOCAL, "Masturbation") <= -3
		;religion
		PRINTFORML %BREAKENG(@"비록 %CALLNAME:LOCAL%의 몸이 참을 수 없는 욕정으로 계속 아파하고 있지만, %HE_SHE(LOCAL)%는 %HIMSELF_HERSELF(LOCAL)%를 잠들게 했다. 왜냐하면 %HIS_HER(LOCAL)% 종교가 %HIM_HER(LOCAL)%가 자위하는 것을 막고 있기 때문이다...")%
		自慰タイプ = 7
		;CALL KOJO_MESSAGE_SEND("EVENT",22,LOCAL,7)
		CFLAG:LOCAL:溜まってる度 = LIMIT(CFLAG:LOCAL:溜まってる度 - 200, 0 ,1000)
		CALL CharacterSinned(LOCAL,"Masturbation",@"자위를 참았다.", 1, 1)
		PRINTL
	ELSEIF GETBIT(IN_CHASTITY(LOCAL),0)
		;chastity
		PRINTFORML %BREAKENG(@"%CALLNAME:LOCAL%의 몸이 참을 수 없는 욕정으로 계속 아파하고 있음에도 불구하고, %HIS_HER(LOCAL)% 순결 시스템은 %HIMSELF_HERSELF(LOCAL)%에게 그 욕정을 견디도록 강요한다. 왜냐하면 그것이 %HIM_HER(LOCAL)%가 자위하는 것을 막기 때문이다...")%
		自慰タイプ = 7
		;CALL KOJO_MESSAGE_SEND("EVENT",22,LOCAL,7)
		CFLAG:LOCAL:溜まってる度 = LIMIT(CFLAG:LOCAL:溜まってる度 - 200, 0 ,1000)
		CALL CharacterSinned(LOCAL,"Masturbation",@"자위를 참았다.", 1, 1)
		PRINTL
	ELSEIF CFLAG:MASTER:現在位置 == CFLAG:LOCAL:初期位置
		PRINTFORML 한밤중, 무언가 들리는 소리에 잠에서 깨어난다.
		PRINTFORML ... %CALLNAME:LOCAL%은(는) 자위를 하면서 %HIS_HER(LOCAL)%의 목소리를 억누르고 당신의 손을 잡으려고 애쓴다...
		自慰タイプ = 2
	ELSEIF CharaYuri && LOCAL != MASTER
		;reverse sleep rape but with another person
		;like mesko's
		; - Mr Pops Alot
		$MultiFapNewChara
		SIF !FLAG:自慰画像非表示 && !nNPCMasturb
			CALL PrintOnanismImage(LOCAL)
		CALL FISHER_YATES_SHAFFLE(CHARANUM)
		FOR LOCAL:2, 1, CHARANUM
		CHARA = SHAFFLE_ARRAY:(LOCAL:2)
		CharaVStim = 0
		CharaAStim = 0
		CharaBStim = 0
		CharaMStim = 0
		CharaUStim = 0
		;対象があなた
		SIF CHARA == MASTER
			CONTINUE
		SIF CHARA == LOCAL
			CONTINUE
		;でてこない
		SIF CFLAG:CHARA:出禁
			CONTINUE
		SIF IsDead:CHARA:0
			CONTINUE
		;wouldn't seek someone one they have a low relation for
		;but can still seek 2hus with a low relation to them
		SIF (RELATION:LOCAL:CHARA < 100 && RELATION:LOCAL:CHARA)
			CONTINUE
		自慰発生閾値 = LIMIT(750 - 200 * TALENT:CHARA:自慰しやすい - 100 * TALENT:CHARA:快感応答 - 100 * TALENT:CHARA:自己愛 + 200 * TALENT:CHARA:自制心,500,999)
		自慰発生閾値 -= 300 * TALENT:CHARA:自慰狂い
		;needs max horniness
		SIF CFLAG:CHARA:溜まってる度 < 自慰発生閾値
			CONTINUE
		BREAK
		NEXT
		SIF !FLAG:自慰画像非表示
			CALL PrintOnanismImage(CHARA)
		IF ABL:LOCAL:指 >= ABL:CHARA:指
			PRINTFORM %CALLNAME:LOCAL%이/가 %CALLNAME:CHARA%의 방에 들어와서 %HIS_HER(CHARA)%에게 ~해달라고 부탁한다
		ELSE
			PRINTFORML %CALLNAME:LOCAL%은(는) %CALLNAME:CHARA%의 문을 두드리지만 열 수 없다. %HE_SHE(LOCAL)%이 그것을 열지 못했다는 것에 좌절하여, %HE_SHE(LOCAL)%은(는) 바로 %HIS_HER(CHARA)%의 문 앞에서 자위하기 시작한다.
			自慰タイプ = 1
			GOTO OnaniEnd
		ENDIF
		IF TALENT:LOCAL:SexKnow == -1
			PRINTFORML %HIS_HER(LOCAL)%의 아픈 사타구니에 대해 무언가를 한다.
		ELSE
			PRINTFORML 서로 성관계를 가진다.
		ENDIF
		IF (RELATION:CHARA:LOCAL < 100 && RELATION:CHARA:LOCAL)
			PRINTFORML %CALLNAME:CHARA%가 %CALLNAME:LOCAL%에게 사라지라고 말한다.
			PRINTFORMW %CALLNAME:LOCAL%는 부끄러운 듯 도망쳐 다른 방으로 향한다...
			GOTO MultiFapNewChara
		ELSEIF TALENT:CHARA:SexKnow == -1 && (ABL:LOCAL:教養 >= UniversalRank("B") || TALENT:LOCAL:SexKnow >= 3)
			PRINTFORML %CALLNAME:CHARA%은 %CALLNAME:LOCAL%가 원하는 것에 놀란 표정을 짓는다.
			PRINTFORML %CALLNAME:LOCAL%는 %CALLNAME:CHARA%에게 섹스의 기초와 %HE_SHE(CHARA)%가 사용할 수 있는 몇 가지 기교를 가르친다.
			PRINTFORMW %HE_SHE(CHARA,1)%은 이제 진실을 안다.
			PRINTFORMW %HE_SHE(CHARA,1)%은 %HIS_HER(CHARA)%의 무지를 잃는다...
			TALENT:CHARA:SexKnow = 0
			CALL QoL_Info_Write(CHARA, Qol_Dates_SexKnow) ;qol custom code
		ELSEIF TALENT:LOCAL:SexKnow == -1 && (ABL:CHARA:教養 >= UniversalRank("B") || TALENT:CHARA:SexKnow >= 3)
			PRINTFORML %CALLNAME:CHARA%은(는) %CALLNAME:LOCAL%에게 성의 기초와 사용할 수 있는 몇 가지 기교를 가르친다.
			PRINTFORMW %HE_SHE(LOCAL,1)%는 이제 진실을 안다.
			PRINTFORMW %HE_SHE(LOCAL,1)%는 무지(%HIS_HER(LOCAL)%)를 잃는다...
			TALENT:LOCAL:SexKnow = 0
			CALL QoL_Info_Write(CHARA, Qol_Dates_SexKnow) ;qol custom code
		ELSEIF TALENT:CHARA:SexKnow == -1 || TALENT:LOCAL:SexKnow == -1
			PRINTFORML %CALLNAME:CHARA%은(는) %CALLNAME:LOCAL%가 원하는 것에 놀란다.
			PRINTFORMW %CALLNAME:LOCAL%는(는) %HE_SHE(LOCAL)%이 하려던 행동에 당황하여 도망치고, %HIS_HER(LOCAL)%를 통제되지 않은 욕정과 함께 잠든다...
			CONTINUE
		ELSE
			PRINTFORML %HE_SHE(CHARA,1)%은 복종한다. 둘 다 섹스가 하고 싶기 때문이다...
		ENDIF
		$YuriLoop
		LOCAL:2 = RAND:7
		SELECTCASE LOCAL:2
			CASE 0
				;vag sex
				IF (!HAS_PENIS(LOCAL) && !HAS_PENIS(CHARA)) || (!HAS_VAGINA(LOCAL) && !HAS_VAGINA(CHARA))
					;needs to be hetero or futa
					GOTO YuriLoop
				ELSE
					$VSexLoop
					LOCAL:5 = RAND:2
					SELECTCASE LOCAL:5
						CASE 0
							IF HAS_PENIS(LOCAL) && HAS_VAGINA(CHARA)
								LOCAL:3 = LOCAL
								LOCAL:4 = CHARA
								CharaVStim = 2
							ELSE
								GOTO VSexLoop
							ENDIF
						CASE 1
							IF HAS_PENIS(CHARA) && HAS_VAGINA(LOCAL)
								LOCAL:3 = CHARA
								LOCAL:4 = LOCAL
								追加Ｖ自慰 = 2
							ELSE
								GOTO VSexLoop
							ENDIF
					ENDSELECT
					PRINTFORML %CALLNAME:(LOCAL:3)%은/는 %CALLNAME:(LOCAL:4)%의 팬티를 벗기고 %HIS_HER(LOCAL:4)+" "+TINKO(LOCAL:3)%을/를 %HIS_HER(LOCAL:4)%의 구멍에 밀어 넣는다.
					PRINTFORML %CALLNAME:(LOCAL:3)%은/는 반복해서 사정하며 %CALLNAME:(LOCAL:4)%의 자궁을 채운다.
				ENDIF
			CASE 1
				;a na ru
				IF (!HAS_PENIS(LOCAL) && !HAS_PENIS(CHARA))
					;one needs to have penis
					GOTO YuriLoop
				ELSE
					$ASexLoop
					LOCAL:5 = RAND:2
					SELECTCASE LOCAL:5
						CASE 0
							IF HAS_PENIS(LOCAL)
								LOCAL:3 = LOCAL
								LOCAL:4 = CHARA
								CharaAStim = 2
							ELSE
								GOTO ASexLoop
							ENDIF
						CASE 1
							IF HAS_PENIS(CHARA)
								LOCAL:3 = CHARA
								LOCAL:4 = LOCAL
								追加Ａ自慰 = 2
							ELSE
								GOTO ASexLoop
							ENDIF
					ENDSELECT
					PRINTFORML %CALLNAME:(LOCAL:3)%은(는) %CALLNAME:(LOCAL:4)%의 팬티를 벗기고 %HIS_HER(LOCAL:3)+" "+TINKO(LOCAL:3)%을/를 %HIS_HER(LOCAL:4)% 항문에 밀어 넣는다.
					PRINTFORML %CALLNAME:(LOCAL:3)%은(는) 계속해서 사정하여 %CALLNAME:(LOCAL:4)%의 장을 가득 채운다.
				ENDIF
			CASE 2
				;urethra play (segs)
				IF (!HAS_PENIS(LOCAL) && !HAS_PENIS(CHARA)) || (TALENT:LOCAL:SexKnow < 5 || !TALENT:CHARA:SexKnow < 5) || !nPee
					;one needs to have penis and forbidden knowledge
					GOTO YuriLoop
				ELSE
					$USexLoop
					LOCAL:5 = RAND:2
					SELECTCASE LOCAL:5
						CASE 0
							IF HAS_PENIS(LOCAL)
								LOCAL:3 = LOCAL
								LOCAL:4 = CHARA
								CharaUStim = 2
							ELSE
								GOTO USexLoop
							ENDIF
						CASE 1
							IF HAS_PENIS(CHARA)
								LOCAL:3 = CHARA
								LOCAL:4 = LOCAL
								LocalUStim = 2
							ELSE
								GOTO USexLoop
							ENDIF
					ENDSELECT
					PRINTFORML %CALLNAME:(LOCAL:3)%이/가 %CALLNAME:(LOCAL:4)%의 팬티를 벗기고 %HIS_HER(LOCAL:3)+" "+TINKO(LOCAL:3)%을/를 %HIS_HER(LOCAL:4)%의 요도에 밀어넣는다.
					PRINTFORML %CALLNAME:(LOCAL:3)%은 반복해서 사정하며 %CALLNAME:(LOCAL:4)%의 방광을 가득 채운다.
				ENDIF
			CASE 3
				; cunni/fella
				LocalMStim = 1
				CharaMStim = 1
				PRINTFORML 그들은 %CALLNAME:LOCAL%이 %CALLNAME:CHARA%의 %PRINT_CLIT(CHARA)%를 핥기 시작하면서 팬티를 벗는다.
				PRINTFORML %CALLNAME:LOCAL%은(는) %HIS_HER(CHARA)+" "+PRINT_CLIT(CHARA)%의 윗부분을 반복해서 핥으며, 매번 %HIM_HER(CHARA)%를 절정 직전까지 몰고 간다.
				PRINTFORML 결국 %CALLNAME:LOCAL%은(는) %CALLNAME:CHARA%의 %PRINT_CLIT(CHARA)%를 빨기 시작하며 %HIM_HER(CHARA)%를 오르가즘으로 이끈다.
				PRINTFORML 두 사람은 서로 만족할 때까지 역할을 바꾼다.
			CASE 4
				; fingering
				SIF (!HAS_VAGINA(LOCAL) && !HAS_VAGINA(CHARA))
					GOTO YuriLoop
				CharaVStim = 1
				追加Ｖ自慰 = 1
				PRINTFORML 그들은 서로의 질을 손가락으로 만지다가 만족할 때까지 팬티를 벗는다.
			CASE 5
				; anal tease
				CharaAStim = 1
				追加Ａ自慰 = 1
				PRINTFORML 그들은 만족할 때까지 서로의 항문을 손가락으로 만지며 팬티를 벗는다.
			CASE 6
				; tribbing
				SIF (!HAS_VAGINA(LOCAL) || !HAS_VAGINA(CHARA))
					GOTO YuriLoop
				PRINTFORML 그들은 %CALLNAME:LOCAL%이 %HIS_HER(LOCAL)%의 다리를 벌리고 %CALLNAME:CHARA%가 뒤따르자 팬티를 벗어던진다.
				PRINTFORML 서로의 대음순을 문지르며 만족할 때까지, 그들은 함께 여러 번 절정에 이른다.
		ENDSELECT
		自慰タイプ = 90
	ELSEIF ABL:LOCAL:精液中毒 > 2 && EX:LOCAL:膣内精液 && !RAND:4
		PRINTFORML %CALLNAME:LOCAL% 더 이상 버틸 수 없었고, %HE_SHE(LOCAL)%은 %HIS_HER(LOCAL)%의 보지에 당신의 정액을 문지르며 자위했다.
		自慰タイプ = 3
	ELSEIF TALENT:LOCAL:SexKnow == -1
		LOCAL:1 = 0
		LOCAL:1 = EXP:LOCAL:自慰経験 - EXP:LOCAL:調教自慰経験
		IF LOCAL:1 >= 120 - (MIN(ABL:LOCAL:親密,6) * 10) && !FLAG:無知自動喪失禁止
			PRINTFORMW %CALLNAME:LOCAL%가 마침내 %HIS_HER(LOCAL)%의 보호자들에게 그것에 대해 이야기한다...
			PRINTFORMW %HE_SHE(LOCAL,1)%는 이제 진실을 안다.
			PRINTFORMW %HE_SHE(LOCAL,1)%는 %HIS_HER(LOCAL)%의 무지를 잃는다...
			TALENT:LOCAL:SexKnow = 0
			CALL QoL_Info_Write(LOCAL, Qol_Dates_SexKnow) ;qol custom code
		ELSEIF LOCAL:1 >= 80 - (MIN(ABL:LOCAL:親密,4) * 10)
			PRINTFORML %CALLNAME:LOCAL%은(는) %HE_SHE(LOCAL)%가 뭘 하고 있는지 모르고 자위에 몰두한다...
		ELSEIF LOCAL:1 >= 50 - (MIN(ABL:LOCAL:親密,3) * 10)
			PRINTFORML %CALLNAME:LOCAL%은(는) %HIM_HER(LOCAL)%이 자극을 위해 책상 모서리에서 자위하는 것을 발견한다...
		ELSEIF LOCAL:1 >= 30 - (MIN(ABL:LOCAL:親密,2) * 10)
			PRINTFORML %CALLNAME:LOCAL%은(는) 몰래 책을 %HIS_HER(LOCAL)%의 사타구니에 비빈다...
		ELSEIF LOCAL:1 >= 15 - (MIN(ABL:LOCAL:親密,1) * 10)
			PRINTFORML 공부하는 동안, %CALLNAME:LOCAL%은(는) %HIS_HER(LOCAL)%의 브러시로 %HIS_HER(LOCAL)%의 보지를 문지른다...
		ELSE
			PRINTFORML %CALLNAME:LOCAL%은 침대에 누워 %HIS_HER(LOCAL)%의 허벅지를 비비며 고통스럽게 한숨을 내쉰다...
		ENDIF
		自慰タイプ = 4
	ELSEIF ABL:LOCAL:自慰中毒 >= 3 && (TALENT:LOCAL:思慕 || IS_LOVER(LOCAL) || TALENT:LOCAL:恋人 || TALENT:LOCAL:セフレ) && !RAND:3
		PRINTFORML %CALLNAME:LOCAL%이 더 이상 참을 수 없을 때, 그녀는 당신의의 향이 묻은 옷을 맡으며 격렬하게 자위를 한다...
		LOCAL:2 = RAND:5
		IF (TALENT:LOCAL:Ｃ感度 == 1 || ABL:LOCAL:Ｃ感覚 >= 10) && LOCAL:2 == 4
			PRINTFORM 클리토스를 미친 듯이 비비며, 그녀는 계속해서 절정에 도달하며 분출한다.
			部位指定 = 1
		ELSEIF (TALENT:LOCAL:Ｖ感度 == 1 || ABL:LOCAL:Ｖ感覚 >= 10) && LOCAL:2 == 3
			PRINTFORM 스스로 깊숙이 손가락을 넣고 분비물로 손이 젖을 때까지 자위하며 계속해서 절정에 이른다.
			部位指定 = 2
			追加Ｖ自慰 = 1
		ELSEIF (TALENT:LOCAL:Ａ感度 == 1 || ABL:LOCAL:Ａ感覚 >= 10) && LOCAL:2 == 2
			PRINTFORM 그녀의 항문과 보지를 모두 손가락으로 만지며, 그녀는 계속해서 절정에 이른다.
			部位指定 = 3
			追加Ａ自慰 = 1
		ELSEIF (TALENT:LOCAL:Ｂ感度 == 1 || ABL:LOCAL:Ｂ感覚 >= 10) && LOCAL:2 == 1
			IF TALENT:LOCAL:バストサイズ >= 1
				PRINTFORM 그녀의 유두를 입으로 가져가며,
			ELSEIF TALENT:LOCAL:バストサイズ <= -1
				PRINTFORM 그녀의 평평한 가슴을 만지며,
			ELSE
				PRINTFORM 그녀의 부드러운 가슴을 움켜쥐며,
			ENDIF
			PRINTFORM 그녀는 계속해서 절정에 이른다.
			部位指定 = 4
			追加Ｂ自慰 = 0
		ELSE
			;PRINTFORML しかし、どんなに快感を得ようとも%CALLNAME:MASTER%への想いは治まることはなく、%CALLNAME:LOCAL%は朝まで悶々とした時間を過ごした……
			PRINTFORM 그녀의 몸이 쾌감으로 떨린다.
			部位指定 = 5
		ENDIF
		IF TALENT:LOCAL:思慕 || TALENT:LOCAL:恋慕 || TALENT:LOCAL:恋人
			PRINTFORML 그녀에 대한 당신의 생각이 계속 머물러 있었다...
		ELSE
			PRINTFORML 그녀의 뜨거운 열정은 결코 사그라지지 않았다...
		ENDIF
		追加Ｖ自慰 = 1
		自慰タイプ = 5
	ELSEIF ABL:LOCAL:自慰中毒 >= 3 && !RAND:2
		PRINTFORML 손가락만으로는 더 이상 만족할 수 없게 된 %CALLNAME:LOCAL%는 카파에게서 산 장난감을 사용한다.
		PRINTFORM 그녀는 자위를 즐긴다.
		IF ABL:LOCAL:Ａ感覚 >= 5
			IF TALENT:LOCAL:貞操 == -1 && TALENT:LOCAL:処女 == 0
				PRINTFORM 그것을 그녀의 항문과 보지에 깊숙이 넣고 박아댄다.
				追加Ｖ自慰 = 1
			ELSEIF TALENT:LOCAL:貞操 == 1
				PRINTFORM 부드럽고 조심스럽게 그것을 그녀의 엉덩이와 보지에 대고 문지르고 눌렀다.
			ELSE
				PRINTFORM 그녀의 엉덩이와 보지를 가지고 놀며, 그들을 희롱한다.
			ENDIF
			部位指定 = 3
			追加Ａ自慰 = 1
		ELSE
			IF TALENT:LOCAL:貞操 == -1 && TALENT:LOCAL:処女 == 0
				PRINTFORM 그것을 그녀의 보지 깊숙이 넣고 박아댄다.
				追加Ｖ自慰 = 1
			ELSEIF TALENT:LOCAL:貞操 == 1
				PRINTFORM 부드럽고 조심스럽게 그것을 그녀의 보지에 대고 문지르며 눌렀다.
			ELSE
				PRINTFORM 그녀의 보지를 가지고 놀며, 그것을 희롱한다.
			ENDIF
			部位指定 = 1
		ENDIF
		PRINTFORML ...
		自慰タイプ = 6
	ELSEIF CFLAG:LOCAL:溜まってる度 < 自慰発生閾値
		;masturbation from religion
		PRINTFORML %CALLNAME:LOCAL%이 참을 수 없을 정도로 흥분하지는 않았지만, %HE_SHE(LOCAL)%는 %HIS_HER(LOCAL)% 종교에서 요구하기 때문에 여전히 자위를 한다...
		自慰タイプ = 1
	ELSE
		PRINTFORML %CALLNAME:LOCAL%은 더 이상 %HIS_HER(LOCAL)%의 아픈 몸을 제어할 수 없게 되자, %HE_SHE(LOCAL)%는 %HIS_HER(LOCAL)%의 손가락으로 놀기 시작했다.
		自慰タイプ = 1
	ENDIF
	$OnaniEnd
	IF LOCAL != MASTER
		IF 自慰タイプ != 90
			CALL KOJO_MESSAGE_SEND("EVENT",22,LOCAL,自慰タイプ,部位指定)
			SIF 自慰タイプ == 7
				CONTINUE
			EXP:LOCAL:自慰経験 += ABL:LOCAL:自慰中毒 * 2 + 1
			CALL COLORMESSAGE(@"자위경험 플러스 {ABL:LOCAL:自慰中毒 * 2 + 1}",C_PINK,1)
		ENDIF
		SELECTCASE ABL:LOCAL:自慰中毒
			CASE 0 TO 2
				珠入手倍率 = 5
			CASE 3 TO 5
				珠入手倍率 = 3
			CASEELSE
				珠入手倍率 = 1
		ENDSELECT
		LOCAL:8 = 0
		IF CHARA > -1
			SIF HAS_PENIS(CHARA)
				LOCAL:8 = 1
		ENDIF
		CALL FUCK(LOCAL, @" C, \@ 追加Ｖ自慰 ? V, #  \@ \@ 追加Ａ自慰 ? A, #  \@ \@ 追加Ｂ自慰 ? B, #  \@ \@ LocalMStim ? M, #  \@ \@ LocalUStim ? U, #  \@ desire, \@ HAS_PENIS(LOCAL) || LOCAL:8 ? cumlove, #  \@ ejac, \@ 追加Ｖ自慰 == 2 ? Vrecept, #  \@ \@ PeeHold:LOCAL:2 <= -2 ? piss, #  \@ \@ PooHold:LOCAL:2 <= -2 ? shit, #  \@ 노출벽", @"\@ 追加Ｖ自慰 == 2 ? Vcreamp, Vvloss, #\@ \@ 追加Ｖ自慰 == 2 ? Pvloss, #\@", 0, CHARA, CHARA, CHARA, @"\@ CHARA > 0 ? 그것을 %CALLNAME:CHARA%에게 주어 %HIS_HER(LOCAL)%의 참을 수 없는 욕정을 달래주었다. #  \@", "ペニス", "膣", "口")
		IF CHARA > -1
			CALL FUCK(CHARA, @" C, \@ CharaVStim ? V, #  \@ \@ CharaAStim ? A, #  \@ \@ CharaBStim ? B, #  \@ \@ CharaMStim ? M, #  \@ \@ CharaUStim ? U, #  \@ 욕정, \@ HAS_PENIS(LOCAL) || HAS_PENIS(CHARA) ? cumlove, #  \@ ejac, \@ CharaVStim == 2 ? Vrecept, #  \@ \@ PeeHold:CHARA:0 <= -2 ? piss, #  \@ \@ PooHold:CHARA:0 <= -2 ? shit, #  \@ 노출", @"\@ CharaVStim == 2 ? Vcreamp, Vvloss, #\@", 0, LOCAL, LOCAL, LOCAL, @"그것을 %CALLNAME:LOCAL%에게 주어 %HIS_HER(CHARA)%의 참을 수 없는 욕정을 달래도록 했다.", "ペニス", "膣", "口")
			CALL CheckSinsForSex(LOCAL, CHARA)
			CFLAG:CHARA:溜まってる度 /= 4
		ELSE
			CALL CharacterSinned(LOCAL,"Masturbation",@"자위했다", -3,-1)
		ENDIF
	ENDIF
	CFLAG:LOCAL:溜まってる度 /= 4
	PRINTW
ENDIF
;まず欲望で基礎値を決める
SELECTCASE ABL:LOCAL:欲望
	CASE 0 TO 2
		欲求不満度 = 10
	CASE 3 TO 5
		欲求不満度 = 20
	CASE 6 TO 10
		欲求不満度 = 30
	CASE 10 TO 15
		欲求不満度 = 50
	CASEELSE
		欲求不満度 = 80
ENDSELECT
欲求不満度 += ABL:LOCAL:自慰中毒 * 7 + (ABL:LOCAL:膣射中毒 + ABL:LOCAL:肛射中毒) * 3
欲求不満度 = 欲求不満素質補正(LOCAL,欲求不満度)
SELECTCASE 欲求不満度
	CASE IS > 400
		欲求不満度 = (欲求不満度 - 400) / 10 + 400
	CASE IS > 200
		欲求不満度 = (欲求不満度 - 200) / 4 + 200
	CASE IS > 100
		欲求不満度 = (欲求不満度 - 100) / 2 + 100
ENDSELECT
SIF 自慰タイプ
	欲求不満度 = 0
SIF TCVAR:LOCAL:今日会ったか
	欲求不満度 /= 2
SIF TALENT:LOCAL:妊娠
	欲求不満度 /= 5
SIF TALENT:LOCAL:育児中
	欲求不満度 /= 2
;溜まってる度は0~1000の間に
CFLAG:LOCAL:溜まってる度 = LIMIT(CFLAG:LOCAL:溜まってる度 + 欲求不満度, 0, 1000)
DEBUGPRINTFORML %CALLNAME:LOCAL%의 만족도: {満足度} 욕구 불만 상승치: {欲求不満度} 쌓여있는 정도: {CFLAG:LOCAL:溜まってる度}
NEXT
;made universal
;-Mr Pops Alot

@LOST_GYAKURE_NAS(ARG,LOSER,ARG:1)
#DIM LOSER
SIF !ARG:1 ;bug fix
	CALL NAS_LOST_GYAKURE(ARG,LOSER) ;addition custom code
GOTO SKIP
BASE:LOSER:体力 = 0
BASE:LOSER:気力 = 0
BASE:LOSER:精力 = 0
;custom code
CFLAG:ARG:溜まってる度 = 0
$SKIP
Qol_Info_Count_Array:LOSER:Qol_Count_EncountMolest ++ ;qol custom code
IF LOSER == MASTER
	;note - exception, not sure if PC name can be translated with the function if the name changes
	SIF !ARG:1 ;bug fix
		CSTR:ARG:モブ子プロフィール２ = Has ravaged you in the past after defeating you.
	CALL SET_SLEEP(1,MASTER,0)
	CFLAG:MASTER:就寝時間 = DAY * 1440 + TIME + 120 + RAND:120
	TFLAG:106 = 0
	BEGIN AFTERTRAIN
ENDIF
;made it universal for lethal combats - Mr Pops Alot

@NAS_LOST_GYAKURE(ARG,LOSER)
#DIM LOSER
#DIM nSexCount
;SIF ARG <= 0 || !HAS_PENIS(MASTER)
;	RETURN 0
;sex count
CFLAG:ARG:溜まってる度 = LIMIT(CFLAG:ARG:溜まってる度 + RAND:200, 500, 1000)
nSexCount = MAX(MIN(ABL:ARG:欲望 / 2, 5) + CFLAG:ARG:溜まってる度 / 200,RAND(5,16))
;style
IF IS_HERM(ARG) || IS_MALE(ARG) ;futa/gay (gayyyyyyyyyyyyyyy) (males are not generated as of now, but just in case)
	;use FUCK() instead
	IF HAS_VAGINA(LOSER)
		CALL FUCK(LOSER, @"V는 욕정, cumlove, ejac, Vrecept, maso, \@ PeeHold:ARG:2 <= -2 ? 오줌을 누고 #  \@ \@ PooHold:ARG:2 <= -2 ? 똥을 싸며 #  \@, 노출", "Vcreamp, Vvloss", 0, ARG, ARG, ARG, @"%CALLNAME:ARG%에게 승부에서 진 뒤 일방적인 강간을 당하며 순결을 빼앗겼다\@ TALENT:ARG:行きずり ? %" "%우연히 마주친 상대였다 #\@.", "ペニス", "膣", "口")
		CALL FUCK(ARG, "C, desire, cumlove, ejac, Vinsert, sadism, exposure", "Pvloss", 0, LOSER, LOSER, LOSER, @"승리한 후, %CALLNAME:LOSER%의 몸을 차지하고 잠시 동안의 격렬한 욕정으로 그것을 버렸다.", "ペニス", "膣", "口")
	ELSE
		CALL FUCK(LOSER, "A, desire, cumlove, ejac, Vrecept, maso, piss, exposure", "Acreamp, Avloss", 0, ARG, ARG, ARG, @"%CALLNAME:ARG%에게 승부에서 진 뒤 일방적인 강간을 당하며 순결을 빼앗겼다\@ TALENT:ARG:行きずり ? %" "%우연히 마주친 상대였다 #\@.", "ペニス", "アナル", "口")
		CALL FUCK(ARG, "C, desire, cumlove, ejac, Vinsert, sadism, exposure", "Pvloss", 0, LOSER, LOSER, LOSER, @"승리한 후, %CALLNAME:LOSER%의 몸을 차지하고 잠시 동안의 격렬한 욕정으로 그것을 버렸다.", "ペニス", "アナル", "口")
	ENDIF
ELSEIF IS_FEMALE(LOSER) && IS_SAMESEX(LOSER, ARG) ;lez
	CALL FUCK(ARG, "C, desire, cumlove, ejac, sadism, exposure", "", 0, LOSER, LOSER, LOSER, "", "ペニス", "膣", "口")
	CALL FUCK(LOSER, "C, desire, cumlove, ejac, maso, piss, exposure", "", 0, ARG, ARG, ARG, "", "ペニス", "膣", "口")
ELSE
	CALL FUCK(ARG, "V, desire, cumlove, ejac, Vrecept, sadism, exposure", "Vcreamp, Vvloss", 0, LOSER, LOSER, LOSER, @"승리한 후, %CALLNAME:LOSER%의 몸을 차지하고 잠시 동안의 격렬한 욕정으로 그것을 버렸다.", "ペニス", "膣", "口")
	CALL FUCK(LOSER, "C, desire, cumlove, ejac, Vinsert, maso, piss, exposure", "Pvloss", 0, ARG, ARG, ARG, @"%CALLNAME:ARG%에게 승부에서 진 뒤 일방적인 강간을 당하며 순결을 빼앗겼다\@ TALENT:ARG:行きずり ? %" "%우연히 마주친 상대였다 #\@.", "ペニス", "膣", "口")
ENDIF

@NAS_LOST_GYAKURE_EX(ARG, HORNY, LOSER)
#DIM HORNY
#DIM LOSER
IF LOSER == MASTER
	IF IS_HERM(ARG) || IS_MALE(ARG) ;futa/gay (gayyyyyyyyyyyyyyy) (males are not generated as of now, but just in case)
		PRINTFORMW 정신을 차렸을 때에는 이미 단단한 무언가가 당신의 \@ IS_MALE(MASTER) ? %FSYN("anus")% # %FSYN("slit")% \@에 닿아 있었다.
		PRINTFORMW 오 이런... 설마 그럴 리가?
		PRINTFORMW 미처 반응하기도 전에 날카로운 통증이 온몸을 관통한다. 뜨겁게 달아오른 쇠막대에 꿰뚫린 듯한 감각이다.
		PRINTFORMW 두려움에 아래를 내려다보자, 활짝 웃는 %CALLNAME:ARG%의 %FSYN("penis")%가 당신의 \@ !IS_MALE(MASTER) ? 소중한 곳 # 항문 \@ 깊숙이 뿌리까지 박혀 있다.
		PRINTFORMW \@ HORNY ? %HIS_HER(ARG)%의 절정에 흥분한 %CALLNAME:ARG%는 # 발정은 보름달에 절정에 달하고 %CALLNAME:ARG% \@은 당신를 탐닉하고 고통스럽게 하며 범함으로써 갈증을 해소한다.
		PRINTFORMW .........
		PRINTFORMW ......
		PRINTFORMW ...
		PRINTFORMW %CALLNAME:ARG%만족할 때까지 계속 당신를 탐닉한 후, 그저 바닥에 지치고 탈진한 채 당신을 내버리고 간다.
		RETURN 1
	ELSEIF IS_FEMALE(MASTER) && IS_SAMESEX(MASTER, ARG) ;lez
		PRINTFORMW 다음 순간, 따뜻하고 부드러운 무언가가 당신의 성기를 짓누르는 감각이 전해진다.
		PRINTFORMW 조심스럽게 아래를 내려다보자 %조사처리(CALLNAME:ARG,"는")% 활짝 웃으며 자기 음부를 당신의 것에 세차게 비벼, 두 사람의 음핵을 함께 자극한다.
		PRINTFORMW \@ HORNY ? %HIS_HER(ARG)%의 절정에 흥분한 %CALLNAME:ARG%는 # 발정기가 보름달에 절정에 달하고, %CALLNAME:ARG%은 \@은 갈증을 해소하기 위해 당신를 난폭하게 몰아붙이고, 괴롭히고, 더럽힌다.
		PRINTFORMW .........
		PRINTFORMW ......
		PRINTFORMW ...
		PRINTFORMW %CALLNAME:ARG%만족할 때까지 계속 당신를 탐닉한 후, 그저 바닥에 지치고 탈진한 채 당신을 내버리고 간다.
		RETURN 1
	ENDIF
ELSE
	IF IS_HERM(ARG) || IS_MALE(ARG) ;futa/gay (gayyyyyyyyyyyyyyy) (males are not generated as of now, but just in case)
		PRINTFORMW %조사처리(CALLNAME:ARG,"는")% %TINKO(ARG)%를 %조사처리(CALLNAME:LOSER,"의")% \@ IS_MALE(LOSER) ? %FSYN("anus")% # %FSYN("slit")% \@에 밀어 넣는다.
		PRINTFORMW 아이고... 설마 그럴 리가?
		PRINTFORMW %CALLNAME:LOSER%이 반응할 틈도 없이 날카로운 통증이 온몸을 뒤흔든다. %조사처리(CALLNAME:ARG,"의")% %TINKO(ARG)%가 %조사처리(CALLNAME:LOSER,"의")% \@ IS_MALE(LOSER) ? %FSYN("anus")% # %FSYN("vagina")% \@ 안으로 완전히 들어간다.
		PRINTFORMW 두려움에 아래를 내려다보자 %조사처리(CALLNAME:ARG,"는")% 활짝 웃고 있다. 그 %FSYN("penis")%는 %조사처리(CALLNAME:LOSER,"의")% \@ !IS_MALE(LOSER) ? 소중한 곳 # 항문 \@ 깊숙이 뿌리까지 박혀 있다.
		PRINTFORMW \@ HORNY ? 흥분하여 %HIS_HER(ARG)% 정점에 도달하여 %CALLNAME:ARG% # 발정은 보름달에 절정에 달하고 %CALLNAME:ARG% \@은 갈증을 해소하기 위해 %CALLNAME:LOSER%를 난폭하게 몰아붙이고 고문하며 침범한다.
		PRINTFORMW .........
		PRINTFORMW ......
		PRINTFORMW ...
		PRINTFORMW %CALLNAME:ARG%만족할 때까지 계속해서 %CALLNAME:LOSER%를 탐닉한 후, 그저 %HIS_HER(LOSER)%를 지쳐 쓰러진 채 바닥에 내버려 두고 떠난다.
		RETURN 1
	ELSEIF IS_FEMALE(LOSER) && IS_SAMESEX(LOSER, ARG) ;lez
		PRINTFORMW %CALLNAME:ARG%은 %HIS_HER(ARG)+" "+SHOW_BOTTOM(ARG)%를 벗어 던지고 %HIS_HER(ARG)%의 다리를 %CALLNAME:LOSER%와 연결한다.
		PRINTFORMW 조심스럽게 아래를 내려다보자 %조사처리(CALLNAME:ARG,"는")% 활짝 웃으며 자기 음부를 %조사처리(CALLNAME:LOSER,"의")% 것에 세차게 비벼 두 사람의 음핵을 함께 자극한다.
		PRINTFORMW \@ HORNY ? 흥분한 %HIS_HER(ARG)% 정점에 도달하여 %CALLNAME:ARG% # 발정은 보름달에 최고조에 달하고 %CALLNAME:ARG% \@은 갈증을 해소하기 위해 %CALLNAME:LOSER%를 난폭하게 몰아붙이고, 괴롭히고, 폭행한다.
		PRINTFORMW .........
		PRINTFORMW ......
		PRINTFORMW ...
		PRINTFORMW %CALLNAME:ARG%만족할 때까지 계속해서 %CALLNAME:LOSER%를 탐닉한 후, 그저 %HIS_HER(LOSER)%를 지쳐 쓰러진 채 바닥에 내버려 두고 떠난다.
		RETURN 1
	ENDIF
ENDIF
;for rika
; PRINTFORMDL %CALLNAME:47%'s breasts can be changed to [Flat Chest] to better fit her dialogue.
; PRINTFORMDL Change it now?
; PRINTL
; CALL ASK_M(@"Change to 【%GET_TALENTNAME_TR(105,VAR_OR_DEFAULT("NAS_SAVE_VER") ? -3 # -2)%】", 1, "", 1, @"Stay at 【\@ STRLENS(GET_TALENTNAME_TR(105,TALENT:47:105)) ? %GET_TALENTNAME_TR(105,TALENT:47:105)% # 【Average Breasts\@】", 1)
; SELECTCASE RESULT
; CASE 0
; PRINTFORMDW %CALLNAME:47% is now a proud owner of a【%GET_TALENTNAME_TR(105,VAR_OR_DEFAULT("NAS_SAVE_VER") ? -3 # -2)%】.
; TALENT:47:105 = -2
; CASE 1
; PRINTFORMDW %CALLNAME:47% is now a proud owner of 【%GET_TALENTNAME_TR(105,TALENT:47:105)%】.
; CASE 2
; PRINTFORMDW %CALLNAME:47%'s chest size will remain the same.
; ENDIF
