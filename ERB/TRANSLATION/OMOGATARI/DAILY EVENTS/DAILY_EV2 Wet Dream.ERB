; Modular ERB: OMOGATARI/DAILY EVENTS/DAILY_EV2 Wet Dream.ERB
;-------------------------------------------------
; Wet Dream
; Allows you to have a lucid dream
; Pee dreams can't happen this way.
;-------------------------------------------------

@DAILY_CONDITION_EV2_NAS
IF !RAND:10 && DreamDropTaken != -1 && !DAILY_Lucid_Stats:0; make lucid dreams occur if one hasn't occurred already
	CALL DAILY_EV2_NAS()
	RETURN 1
ELSEIF DreamDropTaken == 1
	CALL DAILY_EV2_NAS()
	RETURN 1
ELSE
ENDIF
RETURN -1

@DAILY_EV2_NAS
; - NAS: Revamped Lucid Dreams
;===============================
CALL LucidDream("", RAND:1000 > BedWetThreshold(MASTER, 1, 1) && GETBIT(nPee,1), RAND:1000 > BedWetThreshold(MASTER, 2, 1) && GETBIT(nScat,2))
;CALL LucidDreamEjac()
