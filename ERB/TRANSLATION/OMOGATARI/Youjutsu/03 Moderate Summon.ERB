; Modular ERB: OMOGATARI/Youjutsu/03 Moderate Summon.ERB
;///////////////////////////////////////
;3: Moderate Summon
;///////////////////////////////////////

@NAS_ESP_3
#FUNCTION
NAS_ESP_NAME '= "Moderate Summon" ;type name
NAS_ESP_DESC '= "Moderate summoning ability, allowing spawning in bigger items such as luxury items." ;description
NAS_ESP_NAME_SHORT '= "M. Summon" ;abriviation in menu
NAS_ESP_NAME_VSHORT '= "MSUMM"
NAS_ESP_CanDo = 1 ;conditions

@NAS_ESP_3_BOOK
#FUNCTION
NAS_ESP_BOOK_NAME '= "『MaBouru Gold』 Youjutsu Reconstruction Guide"
NAS_ESP_BOOK_AUTHOR '= "Keiga Enterprises"
NAS_ESP_BOOK_DESC '= "Reconstruction guide on how to summon a MaBouru enabler with pure magic power."
NAS_ESP_BOOK_PUBLIC = 0

@YoujutsuSelfStudyPuzzle_3(ARG)
;CALL GUESS_THE_ITEM_PUZZLES(ARG,3,150000,99999999999)
;CALL GUESS_THE_ITEM_PUZZLES(ARG,3,0,150000)
CALL GUESS_THE_ITEM_PUZZLES(ARG,3,0,__INT_MAX__)
;;when page is shown

@EsperMenuPage_3(ARG)
CALL EsperMenuPage_Universal(ARG,TOINT(SUBSTRING(GETDOINGFUNCTION(),14)))
;spells
; @NAS_ESP_3_0
; NAS_ESP_HasEffect = 0
; NAS_ESP_COST = 30
; NAS_ESP_BASE = 7
; NAS_ESP_RADIUS = 0
; NAS_ESP_CAN_MASTER = 1
; NAS_ESP_TSP = 1
; NAS_ESP_LevelNeed = 0
; NAS_ESP_NAME '= @"Dia"
; NAS_ESP_DESC '= @"Slightly restores one's HP."
; NAS_ESP_Type '= @"Combat"

@NAS_ESP_3_30
NAS_ESP_HasEffect = 0
NAS_ESP_COST = 500
NAS_ESP_BASE = 7
NAS_ESP_RADIUS = 0
NAS_ESP_RADIUS_COMBAT = 0
NAS_ESP_RANGE_COMBAT = 0
NAS_ESP_SINGLE_VARIANT = -1
NAS_ESP_CAN_MASTER = 1
NAS_ESP_TSP = 1
NAS_ESP_LevelNeed = 0
NAS_ESP_CharacterUnique = 113
NAS_ESP_NAME '= @"「지구, 달 그리고 다른 세계」"
NAS_ESP_DESC '= @"전투나 섹스에 참여할 수 있는 자신과 동일한 복사본 두 개를 만든다."
NAS_ESP_Type '= @"일일/전투/"

@NAS_ESP_3_31
NAS_ESP_HasEffect = 0
NAS_ESP_COST = 500
NAS_ESP_BASE = 7
NAS_ESP_RADIUS = 0
NAS_ESP_RADIUS_COMBAT = 0
NAS_ESP_RANGE_COMBAT = 0
NAS_ESP_SINGLE_VARIANT = -1
NAS_ESP_CAN_MASTER = 1
NAS_ESP_TSP = 1
NAS_ESP_LevelNeed = 0
NAS_ESP_CharacterUnique = 50
NAS_ESP_NAME '= @"금기 「포 오브 어 카인드」"
NAS_ESP_DESC '= @"전투에서 자신과 동일한 복제본 세 개를 생성한다."
NAS_ESP_Type '= @"일일/전투/"
;///////////////////////////////////////
;Spell Results
;///////////////////////////////////////

@EsperMenuResult_3(PYC_CASTER,PYC_TARGET,nInput)
#DIM nInput
#DIM PYC_TARGET
#DIM PYC_CASTER
#DIM nIncreaseAmount
#DIM EMR
#DIM EMR3_COUNT
EMR = TOINT(SUBSTRING(GETDOINGFUNCTION(),16))
TRYCALLFORM NAS_ESP_{EMR}_{nInput}
SELECTCASE nInput
	CASE 0
	CASE 1
	CASE 30
		FOR EMR3_COUNT,0,2
		SELECTCASE K113_BODY(PYC_CASTER)
			CASE 1
				LOCAL:0 = 2
				LOCALS:0 = 달
				LOCAL:1 = 3
				LOCALS:1 = 지구
			CASE 2
				LOCAL:0 = 3
				LOCALS:0 = 지구
				LOCAL:1 = 1
				LOCALS:1 = 이세계
			CASE 3
				LOCAL:0 = 1
				LOCALS:0 = 이세계
				LOCAL:1 = 2
				LOCALS:1 = 달
		ENDSELECT
		CALL Add_Duplicate_Character(PYC_CASTER, @"%CALLNAME:PYC_CASTER% (%LOCALS:EMR3_COUNT%)")
		SIF RESULT < 0 ;cannot create duplicate
			BREAK
		PYC_TARGET = RESULT
		;override color
		CFLAG:PYC_TARGET:1101 = LOCAL:(EMR3_COUNT)
		CFLAG:PYC_TARGET:好感度 -= (EMR3_COUNT) + 1;lower Favor to keep them nicely in order, at least at the start
		;set affinity for other hecatias
		RELATION:PYC_TARGET:PYC_CASTER = 500
		RELATION:PYC_CASTER:PYC_TARGET = 500
		IF EMR3_COUNT == 1;set to other one as well
			RELATION:PYC_TARGET:(LOCAL:11) = 500
			RELATION:(LOCAL:11):PYC_TARGET = 500
		ENDIF
		SIF CFLAG:PYC_TARGET:衣装一時変更 == 55 ;no bunny girl outfits for alternate Hecatias
			CFLAG:PYC_TARGET:衣装一時変更 = 0
		LOCAL:(11+EMR3_COUNT) = PYC_TARGET ;save newly created ids
		;give them different panties
		CFLAG:PYC_TARGET:205 = 0
		CFLAG:PYC_TARGET:206 = 0
		CFLAG:PYC_TARGET:207 = 0
		CFLAG:PYC_TARGET:208 = 0
		CALL SET_UNDERWEAR(PYC_TARGET, "今日の衣装")
		EQUIP:PYC_TARGET:下半身下着２ = CFLAG:PYC_TARGET:きょうのぱんつ
		CALLNAME:PYC_TARGET '= @"%CALLNAME:PYC_CASTER% (%LOCALS:EMR3_COUNT%)"
		CFLAG:PYC_TARGET:MARKED_FOR_DELETION = 1
		IF DT_EXIST("TGC_Map")
			CALL AddCombatantWithMessage(PYC_TARGET,DT_CELL_GET("lcCombatants", GetCombatant(PYC_CASTER), "side", 1))
			CALL TGC_MoveChara(PYC_TARGET,TOINT(SPLIT_SINGLE(DT_CELL_GETS("lcCombatants", GetCombatant(PYC_CASTER), "positionID", 1), 0, ","))+RAND(-1,2),TOINT(SPLIT_SINGLE(DT_CELL_GETS("lcCombatants", GetCombatant(PYC_CASTER), "positionID", 1), 1, ","))+RAND(-1,2))
		ELSEIF DT_EXIST("lcCombatants")
			CALL AddCombatantWithMessage(PYC_TARGET,DT_CELL_GET("lcCombatants", GetCombatant(PYC_CASTER), "side", 1))
		ELSE
			PRINTFORMDL %CALLNAME:PYC_TARGET%이(가) 도착한다.
		ENDIF
		NEXT
		IF RESULT < 0
			;error message
			DEBUGPRINTFORML 복사본을 만들려고 시도하는 동안 오류가 발생했다.
			PRINTFORML 「이런, 지금 제대로 작동하지 않는 것 같네요.」
			PRINTFORML 「오늘은 그냥 %PARSE("You'll",5,,PYC_CASTER)%로 만족해야겠어요.%CALLNAME:PYC_CASTER%는 안 되니까.」
		ELSEIF DT_EXIST("lcCombatants")
			;set colors for each
			CALL SET_KOJO_COLOR(PYC_TARGET)
			PRINTFORML 「나 세 명이 다 여기 있으니, 이건 절대 잊을 수 없는 경험이 될 거야.」
			SETCOLOR K113_BODYCOLOR(LOCAL:0)
			PRINTFORML 「우리가 항복하라고 애원해도, 너희 모두가 완전히 파괴될 때까지 놓아주지 않을 것이다.」
			SETCOLOR K113_BODYCOLOR(LOCAL:1)
			PRINTFORML 「이제, %TOLOWER(TNAME(PYC_CASTER))%의 궁극적인 전투를 준비해라!」
			RESETCOLOR
		ENDIF
	CASE 31
		;flandre clone
		FOR EMR3_COUNT,0,3
		LOCAL:0 = 1
		LOCALS:0 = 첫 번째 노예
		LOCAL:1 = 2
		LOCALS:1 = 두 번째 노예
		LOCAL:2 = 3
		LOCALS:2 = 세 번째 노예
		CALL Add_Duplicate_Character(PYC_CASTER, @"%CALLNAME:PYC_CASTER% (%LOCALS:EMR3_COUNT%)")
		SIF RESULT < 0 ;cannot create duplicate
			BREAK
		PYC_TARGET = RESULT
		;override color
		CFLAG:PYC_TARGET:1101 = LOCAL:(EMR3_COUNT)
		CFLAG:PYC_TARGET:好感度 -= (EMR3_COUNT) + 1;lower Favor to keep them nicely in order, at least at the start
		;set affinity for other hecatias
		RELATION:PYC_TARGET:PYC_CASTER = 500
		RELATION:PYC_CASTER:PYC_TARGET = 500
		IF EMR3_COUNT == 1;set to other one as well
			RELATION:PYC_TARGET:(LOCAL:11) = 500
			RELATION:(LOCAL:11):PYC_TARGET = 500
		ENDIF
		SIF CFLAG:PYC_TARGET:衣装一時変更 == 55 ;no bunny girl outfits for alternate Hecatias
			CFLAG:PYC_TARGET:衣装一時変更 = 0
		LOCAL:(11+EMR3_COUNT) = PYC_TARGET ;save newly created ids
		;give them different panties
		CFLAG:PYC_TARGET:205 = 0
		CFLAG:PYC_TARGET:206 = 0
		CFLAG:PYC_TARGET:207 = 0
		CFLAG:PYC_TARGET:208 = 0
		CALL SET_UNDERWEAR(PYC_TARGET, "今日の衣装")
		EQUIP:PYC_TARGET:下半身下着２ = CFLAG:PYC_TARGET:きょうのぱんつ
		CALLNAME:PYC_TARGET '= @"%CALLNAME:PYC_CASTER% (%LOCALS:EMR3_COUNT%)"
		CFLAG:PYC_TARGET:MARKED_FOR_DELETION = 1
		IF DT_EXIST("TGC_Map")
			CALL AddCombatantWithMessage(PYC_TARGET,DT_CELL_GET("lcCombatants", GetCombatant(PYC_CASTER), "side", 1))
			CALL TGC_MoveChara(PYC_TARGET,TOINT(SPLIT_SINGLE(DT_CELL_GETS("lcCombatants", GetCombatant(PYC_CASTER), "positionID", 1), 0, ","))+RAND(-1,2),TOINT(SPLIT_SINGLE(DT_CELL_GETS("lcCombatants", GetCombatant(PYC_CASTER), "positionID", 1), 1, ","))+RAND(-1,2))
		ELSEIF DT_EXIST("lcCombatants")
			CALL AddCombatantWithMessage(PYC_TARGET,DT_CELL_GET("lcCombatants", GetCombatant(PYC_CASTER), "side", 1))
		ELSE
			PRINTFORMDL %CALLNAME:PYC_TARGET%이(가) 도착했다.
		ENDIF
		NEXT
		PRINTFORMDL %PARSE("You",,,PYC_CASTER)% %PARSE("clone",,,PYC_CASTER)% %PARSE("yourself",,,PYC_CASTER)%이 네 개의 복사본 속으로 들어간다.
		IF RESULT < 0
			;error message
			DEBUGPRINTFORML 복사본을 만들려고 시도하는 동안 오류가 발생했다.
			PRINTFORML 「이런, 지금 제대로 작동하지 않는 것 같네요.」
			PRINTFORML 「오늘은 그냥 %PARSE("You'll",5,,PYC_CASTER)%로 만족해야겠어요.%CALLNAME:PYC_CASTER%는 안 되니까.」
		ENDIF
ENDSELECT
WAIT
