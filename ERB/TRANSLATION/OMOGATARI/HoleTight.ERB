; Modular ERB: OMOGATARI/HoleTight.ERB

@Add_COM_ORDER_HOLE(ARG, nInserter, nHOLE)
#DIMS DYNAMIC nHole
#DIM DYNAMIC nInserter
SIF GROUPMATCH(SELECTCOM,79) ;inchling onahole
	RETURN 0
;custom code: Show a message instead of hiding
CALL MESSAGE_TIGHTNESS_TooTIGHT(@"strict%nHole%", ARG, nInserter)
IF RESULT
	CALL QOL_COM_ORDER_ELEMENT(ARG, "지나치게 조임", -10000)
	RETURN 1
ELSE
	CALL MESSAGE_TIGHTNESS_TooTIGHT(@"reg%nHole%", ARG, nInserter)
	IF RESULT
		CALL QOL_COM_ORDER_ELEMENT(ARG, "도저히 들어가지 않음", -10000)
		RETURN 1
	ENDIF
ENDIF

@MESSAGE_TIGHTNESS_TooTIGHT(ARGS, ARG,nInserter)
#DIM nInserter
IF Add_Size_Check(ARGS, ARG, nInserter)
	PRINTFORM %조사처리(CALLNAME:ARG,"의")%
	SELECTCASE ARGS
		CASE "strictA", "regA", "anal"
			LOCALS = %FSYN("anus",,ARG)%
			LOCAL:1 = 2
			LOCAL:2 = 1
		CASE "regU", "strictU"
			LOCALS = %FSYN("urethra",,ARG)%
			LOCAL:1 = 30
			LOCAL:2 = 1
		CASEELSE
			LOCALS = %FSYN("vagina",,ARG)%
			LOCAL:1 = 1
			LOCAL:2 = 0
	ENDSELECT
	PRINTFORM %LOCALS%
	PRINTFORM 은 너무 좁다.
	PRINTL
	IF Add_HoleSizeCheck(ARG,nInserter,LOCAL:2,0) >= 3
		PRINTFORML %TW_PORT_STR("요술 망치",0,1)%을 사용하면 당신과 %CALLNAME:ARG% 사이의 크기 차이를 줄일 수 있다.
		;small penis advice
		SIF TALENT:nInserter:250 != 1
			PRINTFORML 대안으로 더 작은 성기를 사용한다.
		WAIT
	ELSEIF Add_HoleSizeCheck(ARG,nInserter,LOCAL:2,0) >= 2
		IF TALENT:ARG:SexKnow >= 5 || TALENT:nInserter:SexKnow >= 5
			PRINTFORML 비록 %CALLNAME:nInserter%나 %CALLNAME:ARG%이 \@ EXISTVAR("NAS_SAVE_VER") ? 「금지된」성적 지식 # %" ["+GET_TALENTNAME_TR(144,1)%] \@을 가지고 있다 하더라도, 크기 차이가 너무 커서 상쇄할 수 없다.
			PRINTFORML 삽입하려면 먼저 %CALLNAME:ARG%의 %LOCALS%를 단련해 %EXP_TR(LOCAL:1)% 경험을 50까지 올려야 한다(현재 {EXP:ARG:(LOCAL:1)}).
		ELSEIF EXP:ARG:(LOCAL:1) > 50
			PRINTFORML %CALLNAME:ARG%의 %LOCALS%는 충분히 단련됐지만, %CALLNAME:nInserter%와 %CALLNAME:ARG% 모두 제대로 삽입하는 데 필요한 \@ EXISTVAR("NAS_SAVE_VER") ? 【금지된 성 지식】 # %" ["+GET_TALENTNAME_TR(144,1)%] \@이 없다.
		ELSE
			PRINTFORML \@ EXISTVAR("NAS_SAVE_VER") ? 【%TRAINNAME_TR(3)%】 # %" ["+TRAINNAME_TR(3)%] \@ 명령으로 %CALLNAME:ARG%의 %LOCALS%를 풀어 %EXP_TR(LOCAL:1)% 경험을 50까지 올려야 한다(현재 {EXP:ARG:(LOCAL:1)}).
			PRINTFORML 크기 차이도 너무 크므로 %CALLNAME:nInserter%나 %CALLNAME:ARG% 중 한 명은 \@ EXISTVAR("NAS_SAVE_VER") ? 【금지된 성 지식】 # %" ["+GET_TALENTNAME_TR(144,1)%] \@을 익혀야 한다.
		ENDIF
		WAIT
	ELSE
		PRINTFORML \@ EXISTVAR("NAS_SAVE_VER") ? 【%TRAINNAME_TR(3)%】 # %" ["+TRAINNAME_TR(3)%] \@ 명령으로 %CALLNAME:ARG%의 %LOCALS%를 풀어 %EXP_TR(LOCAL:1)% 경험을 50까지 올려 보자(현재 {EXP:ARG:(LOCAL:1)}).
		PRINTFORML 또는 \@ EXISTVAR("NAS_SAVE_VER") ? 【금지된 성 지식】 # %" ["+GET_TALENTNAME_TR(144,1)%] \@을 이용하면 무리해서라도 삽입할 수 있다.
		WAIT
	ENDIF
	RETURN 1
ENDIF
RETURN 0

@Add_Size_Check(ARGS, ARG = -1, nInserter = -1)
#DIM nInserter
#FUNCTION
SIF ARG == -1
	ARG = TARGET
SIF nInserter == -1
	nInserter = MASTER
;for normal actions
SELECTCASE ARGS
	;custom code
	CASE "strictA", "regA", "anal"
		RETURNF Add_HoleSizeCheck(ARG,nInserter,1,1)
	CASE "regU", "strictU"
		RETURNF Add_HoleSizeCheck(ARG,nInserter,2,1)
	CASEELSE
		RETURNF Add_HoleSizeCheck(ARG,nInserter,0,1)
ENDSELECT

@Add_HoleSizeCheck(ARG = -1, nInserter = -1, nHole = 0, canDo = 1)
;-2 = ガバガバで気持ちよくない -1 = 刺激が弱い 1= 経験が必要 2= 経験と知識が必要 3= 小人オナホ
#FUNCTION
#DIM InserterSize
#DIM SizeDiff
;k stretching system
;0 = vagina, 1 = anus, 2 = urethra, 3 = mouth, -1 = general
#DIM DYNAMIC nHole
#DIM DYNAMIC nOffset
#DIM nInserter
#DIM canDo
#DIM nVal
VARSET nOffset
;disabled
; IF ARG == MASTER && nInserter == MASTER
; 	RETURNF 0
; ENDIF
SIF ARG == -1
	ARG = TARGET
SIF nInserter == -1
	nInserter = MASTER
SELECTCASE TALENT:nInserter:形状
	CASE 0,3,4
		InserterSize = 0
	CASE 1
		InserterSize = -2
	CASE 2
		InserterSize = -1
	CASE 6
		InserterSize = 1
	CASE 5,10
		InserterSize = 2
ENDSELECT
IF TALENT:nInserter:体型 >= TALENT:ARG:体型
	SizeDiff = (NUM_EXPONENT(TALENT:nInserter:体型 - TALENT:ARG:体型,2) + InserterSize) * 100
ELSE
	SizeDiff = (NUM_EXPONENT(TALENT:nInserter:体型 - TALENT:ARG:体型,2) + InserterSize) * -100
ENDIF
SizeDiff += (HoleStretch:ARG:nHole-600)
;after effects
SIF HasBionicPart(nInserter,33)
	SizeDiff -= 300
SIF HasBionicPart(nInserter,53)
	SizeDiff = 0
nVal = SizePointsToValue(SizeDiff)
SELECTCASE canDo
	CASE 0
		RETURNF nVal
	CASE 1
		;replacement for Com_Size_Check and the jap command.
		;0 = can fit
		;1 = can't
		SELECTCASE nHole
			CASE 0
				LOCAL:1 = 1
			CASE 1
				LOCAL:1 = 2
			CASE 2
				LOCAL:1 = 30
		ENDSELECT
		SELECTCASE nHole
			;CASE 2
			;urethral
			CASEELSE
				SELECTCASE nVal
					CASE 3
						;too big
						RETURNF 1
					CASE 2
						;need both lube or forbidden knowledge if difference is too big
						SIF (TALENT:nInserter:SexKnow >= 5 || TALENT:ARG:SexKnow >= 5) && EXP:ARG:(LOCAL:1) >= 50
							RETURNF 0
					CASE 1
						;at this stage, need either high lube or advanced knowledge
						SIF (TALENT:nInserter:SexKnow >= 2 || TALENT:ARG:SexKnow >= 2) || EXP:ARG:(LOCAL:1) >= 50
							RETURNF 0
					CASE IS <= 0
						;can fit
						RETURNF 0
				ENDSELECT
		ENDSELECT
		RETURNF 1
	CASE -1
		RETURNF SizeDiff
ENDSELECT

@SizePointsToValue(ARG)
#FUNCTION
SELECTCASE ARG
	CASE IS > 1000
		LOCAL = 3
	CASE IS > 600
		LOCAL = 2
	CASE IS > 300
		LOCAL = 1
	CASE IS < -300
		LOCAL = -1
	CASE IS < -600
		LOCAL = -2
	CASEELSE
		RETURNF 0
ENDSELECT
RETURNF LOCAL

@NAS_TIGHTNESS(ARG)
#FUNCTIONS
#DIMS nText
SELECTCASE SizePointsToValue(ARG)
	CASE 5
		nText '= @"아니, %CALLNAME:MASTER%아. 너는 그걸 맞출 수 없을 거야."
	CASE 4
		nText '= "대체 뭘 하려는 거야"
	CASE 3
		nText '= "너무 조임"
	CASE 2
		nText '= "극도로 조임"
	CASE 1
		nText '= "매우 조임"
	CASE 0
		nText '= "들어맞음"
	CASE -1
		nText '= "매우 헐거움"
	CASE -2
		nText '= "극도로 헐거움"
	CASE -3
		nText '= "터무니없이 헐거움"
	CASE -4
		nText '= "성기에 간신히 닿는 정도"
	CASE -5
		nText '= "이걸 구멍이라고 할 수 있나"
ENDSELECT
nText += @" ({ARG}pt 차이)"
RETURNF nText
