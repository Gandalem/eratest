;-------------------------------------------------
;Drink Urine
;Hard training command level: 2
;Flags required: Urination
;-------------------------------------------------
@ADD_CUSTOM_COM153
TIME++
CALL Com_DrinkPee ;com custom code
RETURN RESULT

@ADD_CUSTOM_COM_ABLE153
;浣腸実行判定
SIF !TFLAG:100
	RETURN 0
;一括管理
SIF GLOBAL_COMABLE(153)
	RETURN RESULT
;'nuff said
SIF !GETBIT(nPee, 2)
	RETURN 0
;'nuff said
SIF TFLAG:PissCup <= 0
	RETURN 0
RETURN 1

@ADD_CUSTOM_MESSAGE_COM153
#DIM DYNAMIC nContainer
IF TFLAG:194 == MASTER ;you drink
LOCAL:15 = MASTER
	SELECTCASE ABL:MASTER:PeeDrinkAddict
	CASE 0
		PRINTFORML You brace yourself and drink what little you had left.
		PRINTFORML Even then, it tastes terrible, the musky odor and vile taste almost making you gag...
		PRINTFORML You start regretting your decision.
	CASE 1
		PRINTFORML The taste is as terrible as you'd expect, but somehow, you don't feel like vomiting all over yourself this time.
	CASE 2
		PRINTFORML Not being able to handle urine too well, you spill some out...
	CASE 3
		PRINTFORML Something is odd. You didn't have to waste time mentally preparing as much.
		PRINTFORML Perhaps after doing it so many times, it's starting to affect you.
		PRINTFORML It feels scary, but at the same time... exciting.
	CASE 4
		PRINTFORML You can get used to anything, and this is the proof.
		PRINTFORML As a result of swallowing and smelling %FSYN("pee:n")% as much as you did, drinking it didn't seem like such a big deal anymore.
		PRINTFORML You almost... start to enjoy the taste somewhat.
	CASEELSE
		PRINTFORML What you're doing right now may seem disgusting or odd for others, but you care not for their opinion.
		PRINTFORML It's an acquired taste, and those people are missing out, you're sure of it.
		PRINTFORML You drink all of the %FSYN("pee:n")% like it's nothing, almost wishing for seconds...
	ENDSELECT
	IF SHIRAHU(TARGET)
		IF ABL:サドっ気 >= 3
			PRINTFORML %CALLNAME:TARGET% watches you with a %SPLIT_G(@"glint in %HIS_HER(TARGET)% eyes:sadistic glee")%, enjoying the show you're putting up for %HIM_HER(TARGET)%...
		ELSE
			SELECTCASE ABL:PeeDrinkAddict
			CASE 0
				PRINTFORML %CALLNAME:TARGET% %SPLIT_G("watches you with horror in her eyes:with disgust written all over her face:almost gags from imagining herself in your position")%, turning %HIS_HER(TARGET)% face away...
			CASE 1
				PRINTFORML %CALLNAME:TARGET% seems speechless and would rather prefer that you didn't do it in front of %HIM_HER(TARGET)%...
			CASE 2
				PRINTFORML %CALLNAME:TARGET% seems strangely attracted to it, unable to avert %HIS_HER(TARGET)% gaze...
			CASE 3
				PRINTFORML %CALLNAME:TARGET% gasps and almost reaches out for it, but stops at the last second...
			CASE 4
				PRINTFORML Something tells you that %CALLNAME:TARGET% is wishing she were in your position right now...
			CASEELSE
				PRINTFORML %CALLNAME:TARGET% watches sadly as %HIS_HER(TARGET)% beloved %FSYN("pee:n")% disappears in your mouth, asking you to share with a pout...
			ENDSELECT
		ENDIF
	ENDIF
;part of the original
ELSEIF FLAG:70
	PRINTFORM %PARSE("You put")% the full cup between %CALLNAME:TARGET%'s teeth and %PARSE("pull")% it out, depositing \@ TFLAG:PissCup > 500 ? a large serving of%" "% # \@%FSYN("pee:n")% into %HIS_HER(TARGET)% mouth.<br>
	PRINTFORM In order to make %TNAME(TARGET)% remember the taste well, %PARSE("you dip",1)% your finger with a bit of pee and %PARSE("rub")% it all over %HIS_HER(TARGET)% gums, making sure every part of %HIS_HER(TARGET)% mouth tastes %PARSE("your",1)% %FSYN("pee:n")%...
	CALL PRINT_BREAK
ELSEIF CFLAG:MASTER:イタズラ == 2
	PRINTFORM %PARSE("You put")% the full cup between the sleeping %CALLNAME:TARGET%'s teeth and %PARSE("pull")% it out, depositing \@ TFLAG:PissCup > 500 ? a large serving of%" "% # \@%FSYN("pee:n")% into %HIS_HER(TARGET)% mouth.<br>
	PRINTFORM In order to make %TNAME(TARGET)% remember the taste well, %PARSE("you dip",1)% your finger with a bit of pee and %PARSE("rub")% it all over %HIS_HER(TARGET)% gums, making sure every part of %HIS_HER(TARGET)% mouth tastes %PARSE("your",1)% %FSYN("pee:n")%...
	CALL PRINT_BREAK
ELSE
LOCAL:15 = MASTER
;added from sqn/akuma
	LOCAL = 3
	PRINTFORML What kind of container would you like to use?
	CALL ASK_M("Just straight out of the cup", 1, "Masu Wooden Box", 1, "Wine Glass", 1, "Tea Cup", 1, "Beer Mug", 1, "Bowl", 1, "Plastic Bottle", 1)
	SELECTCASE RESULT
		CASE 0 ;condom
		CASE 1
			RESULTS = a Japanese square measuring wooden box called masu, which still gives off the fresh fragrance of Japanese hinoki cypress
		CASE 2
			RESULTS = a wine glass
		CASE 3
			RESULTS = a small tea cup
		CASE 4
			RESULTS = a big beer mug
		CASE 5
			RESULTS = a moderately sized bowl
		CASE 6
			RESULTS = a plastic bottle
	ENDSELECT
	SELECTCASE RESULT
	CASE 0
		;condom original part
		PRINTFORM %PARSE("You push")% the 
		SELECTCASE TFLAG:PissCup
			CASE IS > 100
				PRINTFORM cup filled to the brim with %FSYN("pee:n")%
			CASE IS > 250
				PRINTFORM cup full of %FSYN("pee:n")%
			CASEELSE
				PRINTFORM used cup
		ENDSELECT
		PRINTFORML  against %CALLNAME:TARGET%'s cheek...
		SIF ABL:PeeDrinkAddict
			PRINTFORM After taking the cup from your hands, %CALLNAME:TARGET% 
		IF ABL:PeeDrinkAddict >= 7
			PRINTFORM opens %HIS_HER(TARGET)% mouth ecstatically as %HE_SHE(TARGET)% pours the cup on %HIS_HER(TARGET)% tongue, holding it in %HIS_HER(TARGET)% mouth and swishing it around as if savoring it
		ELSEIF ABL:PeeDrinkAddict >= 5 && RAND:2
			PRINTFORM turns it inside-out and eagerly accepts the thin liquid spilling from it with %SPLIT_G(@"a stretched out tongue:with %HIS_HER(TARGET)% mouth open wide")%
		ELSEIF ABL:PeeDrinkAddict >= 3
			PRINTFORM puts the opening in %HIS_HER(TARGET)% mouth and %FSYN("start:s")% %SPLIT_G("vigorously ::")%%SPLIT_G("slurping:sucking")% the so desired liquid contained within
		ELSEIF ABL:PeeDrinkAddict
			PRINTFORM shows some hesitation at first, but then sticks out %HIS_HER(TARGET)% tongue as the %FSYN("pee:n")% slowly drips on %HIS_HER(TARGET)% lips
		ELSE
			PRINTFORM Timidly opening %HIS_HER(TARGET)% mouth, %TNAME(TARGET)% shudders in revulsion as %PARSE("you pour")% the %FSYN("pee:n")% onto %HIS_HER(TARGET)% tongue
		ENDIF
		PRINT .<br>

		PRINTFORM %CALLNAME:TARGET% 
		SELECTCASE ABL:PeeDrinkAddict
		CASE IS < 3
			PRINTFORM %SPLIT_G(@"winces from:cringes at:frowns from:scowls from:seems like %HE_SHE(TARGET)%'s not used to")% %SPLIT_G(@"the musky smell of urine:the act itself:the fact that %HE_SHE(TARGET)%'s slurping down a cupful of pee")%, 
			IF ABL:PeeDrinkAddict && RAND:4
				PRINTFORM however %SPLIT_G(@"the desire of %HIS_HER(TARGET)% own body and:%HE_SHE(TARGET)%'s actually not all that concerned by it, and:despite looking a little irritated by doing something so outrageous, %HE_SHE(TARGET)%:%HE_SHE(TARGET)% only looks mildly inconvenienced by it, as %HE_SHE(TARGET)%")% swallows it down %HIS_HER(TARGET)% throat
			ELSE
				PRINTFORM trying to use lots of %SPLIT_G("drool:saliva")% to force it down %HIS_HER(TARGET)% throat as fast as possible
			ENDIF
		CASEELSE
			LOCAL = 0
			IF RAND:2
				LOCALS = the %SPLIT_G("fresh:still warm:freshly ejaculated")% %FSYN("pee:n")% coming from 
			ELSE
				LOCALS = the %FSYN("pee:n")% inside
				LOCAL = 1
			ENDIF
			
			IF ABL:PeeDrinkAddict >= 5 && RAND:2
				PRINTFORM %SPLIT_G(@"looks like it's %SPLIT_G("something out of this world:totally delicious")%:%SPLIT_G("relishes:savors")% it deliciously:%SPLIT_G("licks:smacks")% %HIS_HER(TARGET)% lips")% 
				PRINTFORM while swallowing every drop of %LOCALS%
			ELSE
				PRINTFORM %SPLIT_G(@"casually drinks %LOCALS%:swallows %LOCALS% nonchalantly:gulps %LOCALS% down:drinks %LOCALS% while blushing")%
			ENDIF
			SIF LOCAL
				PRINTFORM %SPLIT_G(" without being bothered by its pungent smell in the slightest:, seemingly eager to clean up the full cup: as if not bothered by its pee smell whatsoever")%
		ENDSELECT
		SIF ABL:PeeDrinkAddict >= 7
			PRINTFORM .<br>After drinking the %FSYN("pee:n")%, %CALLNAME:TARGET% continues licking on the now empty cup, only parting with it reluctantly
		PRINT ...
	CASEELSE
		PRINTFORM You pour out 
		SELECTCASE TFLAG:PissCup
			CASE IS > 1000
				PRINTFORM %SPLIT_G("an obscene:an overflowing:a ridiculous")% amount of 
			CASE IS > 500
				PRINTFORM a %SPLIT_G("big:large:sizable")% portion of 
			CASEELSE
				PRINTFORM some 
		ENDSELECT
		PRINTFORM %FSYN("pee:n")% into %RESULTS% and then hand it over to %CALLNAME:TARGET%.<br><wait>
		PRINTFORM %CALLNAME:TARGET% 
		SELECTCASE ABL:PeeDrinkAddict
		CASE 0
			PRINTFORM seems to be quite %SPLIT_G("disgusted:repulsed:creeped out")% by such request, 
			IF TALENT:性別嗜好 == 1
				PRINTFORM not wanting to drink %FSYN("pee:n")%
			ELSEIF ABL:PeeAddict && RAND:2
				PRINTFORM saying that it's %SPLIT_G(@"better off in %HIS_HER(TARGET)%'s pants:something that pee holders don't do:disgusting even for someone who likes having an accident")%
			ELSE
				PRINTFORM complaining about the %SPLIT_G("taste:smell:whole thing")%
			ENDIF
			PRINT .<br>
			PRINTFORM Nevertheless, after some convincing, %HE_SHE(TARGET)% somehow manages to %SPLIT_G("swallow:drink:gulp")% all of the %FSYN("pee:n")%, albeit %SPLIT_G("timidly:reluctantly:hesitantly")%, using lots of %SPLIT_G("drool:saliva")% %SPLIT_G(@"to overcome its terrible taste:so %HE_SHE(TARGET)% could swallow it more easily")%
		CASE 1, 2
			PRINTFORM pours the %FSYN("pee:n")% into %HIS_HER(TARGET)% mouth at once, %SPLIT_G("chugging it down:swallowing it:gulping it down")%%SPLIT_G(@", trying to ignore its taste as much as possible:, trying to get it out of %HIS_HER(TARGET)% throat as much as possible: while taking a sip of water and gargling to make it easier:::")%
		CASE 3
			PRINTFORM takes %HIS_HER(TARGET)% time carefully licking the %FSYN("pee:n")% out, tasting it while savoring %HIS_HER(TARGET)% lips deliciously%SPLIT_G(@" without any complaints: as if %HE_SHE(TARGET)%'s already used to it: in an experienced fashion::")%
		CASE IS < 5
			PRINTFORM %SPLIT_G("gulps:sucks:slurps:guzzles:chugs")% it down\@ RAND:3 ? %SPLIT_G(" casually: nonchalantly")% # with a resounding gulp\@, %SPLIT_G(@"barely holding %HIMSELF_HERSELF(TARGET)% from asking for seconds:making it look very tasty:almost looking like %HE_SHE(TARGET)%'d love to have some more")%
		CASEELSE
			PRINTFORM sniffs it very deeply at first, enjoying its rich fragrance as %HE_SHE(TARGET)% sticks out %HIS_HER(TARGET)% tongue and %FSYN("start:s")% slurping the %FSYN("pee:n")% very carefully as to not spill a single drop 
			IF RAND:4
				PRINTFORM as though it's some sort of ambrosia for %HIM_HER(TARGET)%
			ELSEIF RAND:3
				PRINTFORM as if it's the %SPLIT_G("most delicious:tastiest")% thing in the world
			ELSE
				PRINTFORM while %SPLIT_G("tasting:savoring:enjoying")% it thoroughly
			ENDIF
			PRINTFORM , looking really sad when it's over
		ENDSELECT
		IF ABL:PeeDrinkAddict < 2
			SELECTCASE MARK:反発刻印
				CASE 0
					IF MARK:不埒刻印 < 2
						PRINTFORM  with a %SPLIT_G("pale:disgusted")% %SPLIT_G("expression:face:look")%
					ELSEIF MARK:不埒刻印 >= 3
						PRINTFORM , forcing %HIMSELF_HERSELF(TARGET)% to make an ecstatic %SPLIT_G("expression:face:look")%
					ENDIF
				CASE 2
					PRINTFORM  \@ RAND:2 ? while barely holding %HIMSELF_HERSELF(TARGET)% from %SPLIT_G("gagging:vomiting")% # with %ARTICLE(SPLIT_G("uneasy:troubled:anxious"))% %SPLIT_G("expression:face:look")%\@
				CASE 3
					PRINTFORM  while %SPLIT_G("glaring:scowling")% at you
			ENDSELECT
		ENDIF
		PRINTFORM ...<br>
		SIF ABL:PeeDrinkAddict > 1 && RAND:3
			PRINTFORM After the deed is done, %CALLNAME:TARGET% sticks %HIS_HER(TARGET)% tongue out, showing the proof that she did indeed just swallow everything properly...
	ENDSELECT
	CALL PRINT_BREAK
ENDIF
CALL DRINK(TFLAG:194, TFLAG:PissCup,0)
TFLAG:PissCup = 0
TFLAG:PissCupConcentration = 0
