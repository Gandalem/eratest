;-------------------------------------------------
;Vaginal Electrode
;Pleasures the target's vagina with electrodes. Takes virginity
;Hard training command level: 3
;-------------------------------------------------
@ADD_CUSTOM_COM165
;着脱の処理
CALL EStimChannelCheck

IF TFLAG:EStim >= FLAG:EStimBox+1 && !TEQUIP:EStimVag
	PRINTFORML Your control box can only support {FLAG:EStimBox+1} %PLURALIZER("channel",FLAG:EStimBox+1)%. 
	PRINTFORMW Please either remove an electrode or upgrade your box.
	RETURN -1
ELSEIF !ITEM:EStimBox
	PRINTFORMW %PARSE("You don't")% have a way to power this.
	FONTITALIC
	PRINTFORMW Buy a %ITEMNAME_TR(145)% to use this.
	FONTREGULAR
	RETURN -1
ELSE
TEQUIP:EStimVag = !TEQUIP:EStimVag
CALL EStimChannelCheck
ENDIF
;-------------------------------------------------
;COM別FLAGの設定
;-------------------------------------------------
;行為者
TCVAR:116 = PLAYER
;奉仕快楽フラグ
TCVAR:PLAYER:108 = 1

CSTR:TARGET:10 += @"{PLAYER}/"
;TCVAR:2 射精箇所フラグ（-1=コンドーム 1=膣内 2=アナル 3=手淫 4=口淫 5=パイズリ 6=素股 7=足コキ 8=体表 9=アナル奉仕 10=道具 11=触手 12=Ａ責め 13=Ｖ責め）
TCVAR:TARGET:2 = 23
;強度
TCVAR:TARGET:4 = 1500

;破瓜抑制
;TFLAG:2 = 1

;-------------------------------------------------
;ソース追加処理
;-------------------------------------------------
;消費体力、気力
IF FLAG:70 == 1
	BASE:MASTER:TSP -= 100
	SOURCE:快Ｖ = 600
	SOURCE:苦痛 = 25

ELSE

	DOWNBASE:体力 += 30
	DOWNBASE:気力 += 100

	SOURCE:快Ｖ = 600
	SOURCE:苦痛 = 50
	SOURCE:露出 = 20
	SOURCE:逸脱 = 40
	SOURCE:反感 = 300


	;-------------------------------------------------
	;特殊ソース処理
	;-------------------------------------------------
	;抑圧、抵抗持ちには、抑鬱ソース追加
	SIF TALENT:自己愛 < 0 || TALENT:抵抗
		SOURCE:鬱屈 += 40

	;処女だった場合は、苦痛のソースと反発のソースを追加する
	IF TALENT:処女 == 1
		SOURCE:苦痛 += 2000
		SOURCE:反感 += 3000
	ENDIF

	;再生処女だった場合は、苦痛のソースと反発のソースを追加する
	IF TALENT:処女 == 2
		SOURCE:苦痛 += 500
		SOURCE:反感 += 1000
	ENDIF		

	;上のほうで計算した汚れデータ
	SOURCE:不潔 = Y*10 + 60
	CALL 特殊ソース処理		
ENDIF
;Vへの苦痛は先に処理しておく
CALL PAIN_CHECK_V(TARGET)

;-------------------------------------------------
;経験の処理
;-------------------------------------------------
;give exp
EXP:Ｖ経験 ++
IF FLAG:70 == 1
	IF HETEROSEX(TARGET,PLAYER)
		EXP:PLAYER:(50 + HETEROSEX(TARGET,PLAYER)) += 1
	ENDIF
ELSE
	;同性愛　レズなら戻り値1ゲイなら戻り値2
	IF HETEROSEX(TARGET,PLAYER)
		EXP:(50 + HETEROSEX(TARGET,PLAYER)) += 1
		EXP:PLAYER:(50 + HETEROSEX(TARGET,PLAYER)) += 1
	ENDIF

	;異常経験
	IF TALENT:処女 == 1 && !GETBIT(CFLAG:3,3)
		EXP:異常経験 ++
		SETBIT CFLAG:3,3
	ENDIF
ENDIF
EXP:PLAYER:道具使用経験 ++
TIME ++
RETURN 1

;-------------------------------------------------
;装着継続の処理
;-------------------------------------------------
@ADD_CUSTOM_EQUIP_COM165(ARG)
;insert plug
PRINTL ＜Vaginal Electrode＞

CSTR:TARGET:10 += @"{PLAYER}/"
TCVAR:TARGET:2 = 23
TCVAR:TARGET:4 = 450

IF FLAG:70 == 1
BASE:MASTER:TSP -= 50
SOURCE:ARG:快Ｖ += 250 + (100*TFLAG:VibePower)

ELSE


DOWNBASE:ARG:体力 += 10
DOWNBASE:ARG:気力 += 50

SOURCE:ARG:快Ｖ += 250
SOURCE:ARG:露出 += 10
SOURCE:ARG:逸脱 += 20

;-------------------------------------------------
;特殊ソース処理
;-------------------------------------------------
;抑圧、抵抗持ちには、抑鬱ソース追加
SIF TALENT:ARG:自己愛 < 0 || TALENT:ARG:抵抗
	SOURCE:ARG:鬱屈 += 20

;Vへの苦痛は先に処理しておく
;EXP:Ｖ経験をみる（処女はありえない）
LOCAL = 25
LOCAL:1 = 20
IF EXP:ARG:Ｖ経験 < EXPLV:2
	TIMES LOCAL , 0.80
	TIMES LOCAL:1 , 1.20
ELSEIF EXP:ARG:Ｖ経験 < EXPLV:3
	TIMES LOCAL , 0.40
ELSEIF EXP:ARG:Ｖ経験 < EXPLV:4
	TIMES LOCAL , 0.10
ELSEIF EXP:ARG:Ｖ経験 < EXPLV:5
	TIMES LOCAL , 0.00
ELSEIF EXP:ARG:Ｖ経験 >= EXPLV:5
	TIMES LOCAL , 0.00
ENDIF

;PALAM:潤滑をみる
IF PALAM:ARG:潤滑 < PALAMLV:1
	TIMES LOCAL , 1.20
	TIMES LOCAL:1 , 1.50
ELSEIF PALAM:ARG:潤滑 < PALAMLV:2
	TIMES LOCAL , 0.70
	TIMES LOCAL:1 , 1.20
ELSEIF PALAM:ARG:潤滑 < PALAMLV:3
	TIMES LOCAL , 0.40
ELSEIF PALAM:ARG:潤滑 < PALAMLV:4
	TIMES LOCAL , 0.10
ELSEIF PALAM:ARG:潤滑 >= PALAMLV:4
	TIMES LOCAL , 0.10
ENDIF
SOURCE:ARG:苦痛 += LOCAL
SOURCE:ARG:反感 += LOCAL:1

;-------------------------------------------------
;経験の処理
;-------------------------------------------------
;自慰実行時は経験＋１
SIF EXP_UP(22,TARGET)
	EXP:ARG:Ｖ経験 += 1
ENDIF
RETURN 1

@ADD_CUSTOM_MESSAGE_COM165
IF FLAG:70 == 1
	IF !TEQUIP:EStimVag
	ELSE
	ENDIF
ELSE
	IF !TEQUIP:EStimVag
		PRINTFORML %PARSE("You turn")% off the control box and slowly %PARSE("pull")% out the electrode from %CALLNAME:TARGET%'s pussy.
		;Ｖ経験200以上＋恋慕か親愛
		IF EXP:Ｖ経験 > 200 && TALENT:恋慕
			PRINTFORM %TNAME(TARGET,1)% drools and tears roll down her cheek, unable to bear the suddenly empty sensation
		;Ｖ経験150以上＋恋慕か親愛
		ELSEIF EXP:Ｖ経験 > 150 && TALENT:恋慕
			PRINTFORM A small pool of liquid forms as %PARSE("you pull",1)% it out of %TNAME(TARGET)%'s drenched insides
		;Ｖ経験120以上＋恋慕か親愛
		ELSEIF EXP:Ｖ経験 > 120 && TALENT:恋慕
			PRINTFORM %TNAME(TARGET,1)%'s folds drip with love juice as it gets pulled out
		;Ｖ経験70以上＋恋慕か親愛
		ELSEIF EXP:Ｖ経験 > 70 && TALENT:恋慕
			PRINTFORM %TNAME(TARGET,1)% rubs her thighs together while staring at %PARSE("you",5)%
		;Ｖ経験40以上
		ELSEIF EXP:Ｖ経験 > 40 && TALENT:恋慕
			PRINTFORM %TNAME(TARGET,1)% lets out hot sighs
		;Ｖ経験20以上
		ELSEIF EXP:Ｖ経験 > 20
			PRINTFORM %TNAME(TARGET,1)%'s opening twitches, a bit ajar
		;それ以外
		ELSE
			PRINTFORM %TNAME(TARGET,1)% seems relieved
		ENDIF
		PRINTL ...	
	ELSE
	PRINTFORML You insert a bipolar electrode in %CALLNAME:TARGET%'s %FSYN("vagina")%
		PRINTFORM %PARSE("You")% 
		IF HAS_PANTY_MOVEMENT()
			PRINTFORM %PRINT_PANTY_MOVEMENT(TARGET, MASTER)% and %PARSE("push")% a bipolar electrode against her 
		ELSE
			PRINTFORM %PARSE("push")% a bipolar electrode against %CALLNAME:TARGET%'s 
		ENDIF
		PRINTFORM 
		;潤滑LV5以上でさらに追加
		SIF PALAM:潤滑 >= PALAMLV:5
			PRINT dripping 
		;潤滑LV3以上でさらに追加
		SIF PALAM:潤滑 >= PALAMLV:3
			PRINT wet 
		;小柄体型
		IF TALENT:体型 < 0
			PRINT young pussy
		ELSE
			PRINT vagina
		ENDIF
		SIF ABL:Ｖ感覚 >= 4 && TALENT:淫乱
			PRINTFORM , she shakes her hips on her own, begging %PARSE("you",5)% to insert it
		PRINTL ...
		PRINTFORM %TNAME(TARGET,1)%
		;Custom code, normally it checks for a trait, but TW doesn't have it
		;bug fix since vigrinity is lost when you use the vibe command so it uses the haka cvar instead.
		;処女＋淫乱か娼婦
		IF TCVAR:破瓜 == 1 && TALENT:淫乱 && RAND:2
			PRINTFORM  smiles in a way that makes %PARSE("you")% forget she's a virgin
		;処女＋恋慕か親愛
		ELSEIF (TCVAR:破瓜 == 1 || TCVAR:破瓜 == -1) && TALENT:恋慕
			PRINTFORM  stares up at %PARSE("you")% with a lonely look
		;処女＋淫乱
		ELSEIF TCVAR:破瓜 == 1 && TALENT:淫乱
			PRINTFORM  doesn't care she's a virgin, shaking her hips obscenely
		;処女＋一線越えない
		ELSEIF TCVAR:破瓜 == 1 || (TCVAR:破瓜 == -1) && FuckReserved(TARGET)
			PRINTFORM  cries, tears flowing down her cheeks with a look of resignation on her face
		;処女
		ELSEIF (TCVAR:破瓜 == 1 || TCVAR:破瓜 == -1)
			PRINTFORM  trembles heavily, scared to lose her virginity this way
		;Ｖ経験200以上＋淫乱か娼婦
		ELSEIF EXP:Ｖ経験 > 200 && TALENT:淫乱
			PRINTFORM 's body twitches and trembles, wanting %PARSE("you")% to put it in
		;Ｖ経験150以上＋淫乱か娼婦
		ELSEIF EXP:Ｖ経験 > 150 && TALENT:淫乱
			PRINTFORM  stares up at %PARSE("you")% with wet eyes as %PARSE("you rub")% against her labia
		;Ｖ経験120以上＋淫乱か娼婦
		ELSEIF EXP:Ｖ経験 > 120 && TALENT:淫乱
			PRINTFORM  shakes her hips, wanting to experience ecstasy
		;Ｖ経験70以上＋淫乱か娼婦
		ELSEIF EXP:Ｖ経験 > 70 && TALENT:淫乱
			PRINTFORM  stares up at %PARSE("you")%, longing for %PARSE("you",5)% instead
		;Ｖ経験40以上
		ELSEIF EXP:Ｖ経験 > 40
			PRINTFORM  sighs drunk on pleasure as %PARSE("you rub")% against her lips
		;Ｖ経験20以上
		ELSEIF EXP:Ｖ経験 > 20
			PRINTFORM 's lips leak love juice as %PARSE("you rub")% against them
		;それ以外
		ELSE
			PRINTFORM 's lips twitch as %PARSE("you rub")% against them
		ENDIF
		PRINTL ...
		PRINTFORM %PARSE("You insert")% the electrode in %CALLNAME:TARGET%'s 
		;潤滑LV4以上
		SIF PALAM:潤滑 >= PALAMLV:4
			PRINT dripping wet 
		PRINTFORML slit, before turning on the control box...	
	ENDIF
ENDIF


@ADD_CUSTOM_COM_ABLE165
;バイブ実行判定
SIF !TFLAG:100
	RETURN 0
	
SIF !ITEM:VagElectrode
	RETURN 0	

SIF !CFLAG:MASTER:うふふ && !TFLAG:PublicToyPlay
	RETURN 0			

RETURN 1