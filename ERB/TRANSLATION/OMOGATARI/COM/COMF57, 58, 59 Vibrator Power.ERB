;-------------------------------------------------
;Set Vibe Power
;Give an option to set the vibrator power
;Normal training command level: 1
;-------------------------------------------------
@ADD_CUSTOM_COM59

PRINTFORML Select which level you want to increase the vibrator's intensity to. (Currently level {TFLAG:VibePower})
FOR LOCAL, 0, 6
PRINTFORML [{LOCAL}] - Level {LOCAL}
NEXT
PRINTFORML [999] - Get me out of here! 
INPUT
SIF RESULT != 999
TFLAG:VibePower = RESULT

;着脱の処理
;-------------------------------------------------
;COM別FLAGの設定
;-------------------------------------------------
;行為者
TCVAR:116 = PLAYER
;奉仕快楽フラグ
TCVAR:PLAYER:108 = 1

TCVAR:TARGET:4 = 900

	DOWNBASE:体力 += 30
	DOWNBASE:気力 += 50
	
IF FLAG:70 == 1
	IF HETEROSEX(TARGET,PLAYER)
		EXP:PLAYER:(50 + HETEROSEX(TARGET,PLAYER)) += 1
	ENDIF
ELSE
	;同性愛　レズなら戻り値1ゲイなら戻り値2
	IF HETEROSEX(TARGET,PLAYER)
		EXP:(50 + HETEROSEX(TARGET,PLAYER)) += 1
		EXP:PLAYER:(50 + HETEROSEX(TARGET,PLAYER)) += 1
	ENDIF
ENDIF
EXP:PLAYER:道具使用経験 ++

RETURN 1


@ADD_CUSTOM_MESSAGE_COM59
SELECTCASE TFLAG:VibePower
CASE 0
	PRINTFORML %CALLNAME:TARGET% looks bored as %HE_SHE(TARGET)% can barely feel the vibrations.
CASE 1
	PRINTFORML A small moan slips from %CALLNAME:TARGET%.
CASE 2
CASE 3
CASE 4
CASEELSE
	PRINTFORML %CALLNAME:TARGET% leaks sultry moans while also having a potty dance, trying to resist climax.  
ENDSELECT

@ADD_CUSTOM_COM_ABLE59
;バイブ実行判定
SIF !TFLAG:100
	RETURN 0

RETURN COM_ABLE_VIBES()

@COM_ABLE_VIBES
#FUNCTION
	
SIF !TEQUIP:バイブ && !TEQUIP:TARGET:アナルバイブ && !TEQUIP:TARGET:クリキャップ && !TEQUIP:TARGET:EStimClit && !TEQUIP:TARGET:EStimVag && !TEQUIP:TARGET:アナル電極 && !TEQUIP:TARGET:EStimNipples && !TEQUIP:TARGET:EStimMouth && !TEQUIP:TARGET:EStimUrethra
	RETURNF 0

RETURNF 1