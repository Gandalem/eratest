; Modular ERB: OMOGATARI/COM/COMF163 Urethral Plug.ERB
;-------------------------------------------------
;Urethral Sound
;Pleasures the target's urethra by inserting a plug
;Hard training command level: 3
;Flags required: Urination
;-------------------------------------------------

@ADD_CUSTOM_COM163
;着脱の処理
TEQUIP:UrethralSound = !TEQUIP:UrethralSound
;-------------------------------------------------
;COM別FLAGの設定
;-------------------------------------------------
;行為者
TCVAR:116 = PLAYER
;奉仕快楽フラグ
TCVAR:PLAYER:108 = 1
CSTR:TARGET:10 += @"{PLAYER}/"
;TCVAR:2 射精箇所フラグ（-1=コンドーム 1=膣内 2=アナル 3=手淫 4=口淫 5=パイズリ 6=素股 7=足コキ 8=体表 9=アナル奉仕 10=道具 11=触手 12=Ａ責め 13=Ｖ責め）
TCVAR:TARGET:2 = 22
;強度
TCVAR:TARGET:4 = 900
TCVAR:LostVirginU = 1
;-------------------------------------------------
;ソース追加処理
;-------------------------------------------------
;消費体力、気力
IF FLAG:70 == 1
	BASE:MASTER:TSP -= 100
	SOURCE:UPleas = 250
	SOURCE:苦痛 = 25
ELSE
	DOWNBASE:体力 += 30
	DOWNBASE:気力 += 100
	SOURCE:UPleas = 250
	SOURCE:苦痛 = 50
	SOURCE:露出 = 20
	SOURCE:逸脱 = 40
	SOURCE:反感 = 40
	;-------------------------------------------------
	;特殊ソース処理
	;-------------------------------------------------
	;抑圧、抵抗持ちには、抑鬱ソース追加
	SIF TALENT:自己愛 < 0 || TALENT:抵抗
		SOURCE:鬱屈 += 40
	;give pain and hostility if still urethral virgin
	IF !CFLAG:TARGET:LostVirginUDay
		SOURCE:苦痛 += 2000
		SOURCE:反感 += 3000
	ENDIF
ENDIF
;call pain check
CALL PAIN_CHECK_U(TARGET)
;-------------------------------------------------
;経験の処理
;-------------------------------------------------
;give exp
EXP:UExp ++
EXP:USex ++
IF FLAG:70 == 1
	IF HETEROSEX(TARGET,PLAYER)
		EXP:PLAYER:(50 + HETEROSEX(TARGET,PLAYER)) += 1
	ENDIF
ELSE
	;異常経験
	IF !CFLAG:TARGET:LostVirginUDay && !GETBIT(CFLAG:3,3)
		EXP:異常経験 ++
		SETBIT CFLAG:3,3
	ENDIF
	;同性愛　レズなら戻り値1ゲイなら戻り値2
	IF HETEROSEX(TARGET,PLAYER)
		EXP:(50 + HETEROSEX(TARGET,PLAYER)) += 1
		EXP:PLAYER:(50 + HETEROSEX(TARGET,PLAYER)) += 1
	ENDIF
ENDIF
EXP:PLAYER:道具使用経験 ++
;bugfix
TIME ++
RETURN 1
;-------------------------------------------------
;装着継続の処理
;-------------------------------------------------

@ADD_CUSTOM_EQUIP_COM163(ARG)
;insert plug
PRINTL 요도음
CSTR:TARGET:10 += @"{PLAYER}/"
TCVAR:TARGET:2 = 22
TCVAR:TARGET:4 = 450
IF FLAG:70 == 1
	BASE:MASTER:TSP -= 50
	SOURCE:ARG:UPleas += 125
ELSE
	DOWNBASE:ARG:体力 += 10
	DOWNBASE:ARG:気力 += 50
	SOURCE:ARG:UPleas += 125
	SOURCE:ARG:露出 += 10
	SOURCE:ARG:逸脱 += 20
	PeeHoldExp:ARG:0 += 1
	EXP:UExp ++
	EXP:USex ++
	;-------------------------------------------------
	;特殊ソース処理
	;-------------------------------------------------
	;抑圧、抵抗持ちには、抑鬱ソース追加
	SIF TALENT:ARG:自己愛 < 0 || TALENT:ARG:抵抗
		SOURCE:ARG:鬱屈 += 20
	;Vへの苦痛は先に処理しておく
	;EXP:Ｖ経験をみる（処女はありえない）
	LOCAL = 25
	LOCAL:1 = 20
	IF EXP:ARG:UExp < EXPLV:2
		TIMES LOCAL , 0.80
		TIMES LOCAL:1 , 1.20
	ELSEIF EXP:ARG:UExp < EXPLV:3
		TIMES LOCAL , 0.40
	ELSEIF EXP:ARG:UExp < EXPLV:4
		TIMES LOCAL , 0.10
	ELSEIF EXP:ARG:UExp < EXPLV:5
		TIMES LOCAL , 0.00
	ELSEIF EXP:ARG:UExp >= EXPLV:5
		TIMES LOCAL , 0.00
	ENDIF
	;PALAM:潤滑をみる
	IF PALAM:ARG:潤滑 < PALAMLV:1
		TIMES LOCAL , 1.20
		TIMES LOCAL:1 , 1.50
	ELSEIF PALAM:ARG:潤滑 < PALAMLV:2
		TIMES LOCAL , 0.70
		TIMES LOCAL:1 , 1.20
	ELSEIF PALAM:ARG:潤滑 < PALAMLV:3
		TIMES LOCAL , 0.40
	ELSEIF PALAM:ARG:潤滑 < PALAMLV:4
		TIMES LOCAL , 0.10
	ELSEIF PALAM:ARG:潤滑 >= PALAMLV:4
		TIMES LOCAL , 0.10
	ENDIF
	SOURCE:ARG:苦痛 += LOCAL
	SOURCE:ARG:反感 += LOCAL:1
	;-------------------------------------------------
	;経験の処理
	;-------------------------------------------------
	;自慰実行時は経験＋１
	SIF EXP_UP(68,TARGET)
		EXP:ARG:UExp += 1
ENDIF
RETURN 1

@ADD_CUSTOM_MESSAGE_COM163
IF FLAG:70 == 1
	IF !TEQUIP:UrethralSound
		PRINTFORML 시간이 멈춘 동안, %CALLNAME:TARGET%의 %FSYN("urethra")%에서 요도 플러그를 천천히 빼냈다.
		SELECTCASE EXP:UExp
			CASE IS > 180
				PRINTFORML 플러그를 빼내자 %TNAME(TARGET,1)%의 요도가 탐욕스럽게 경련하며 오줌이 흘러나온다...
			CASE 20 TO 179
				PRINTFORML 플러그를 빼낸 뒤에도 %TNAME(TARGET,1)%의 %FSYN("urethra")%는 완전히 닫히지 않는다...
			CASEELSE
				PRINTFORML 플러그를 빼내자 %TNAME(TARGET,1)%의 오줌 구멍이 고통스럽게 부풀어 오른다...
		ENDSELECT
	ELSE
		LOCALS:0 =
		IF HAS_PANTY_MOVEMENT()
			LOCALS:0 '= @"%PRINT_PANTY_MOVEMENT(TARGET, MASTER)%요도 플러그를 %CALLNAME:TARGET%의 %FSYN("urethra")%에 밀어 넣었다"
		ELSE
			LOCALS:0 '= @"플러그를 %CALLNAME:TARGET%의 %FSYN("urethra")%에 밀어 넣었다"
		ENDIF
		PRINTFORML 시간이 멈춰 있는 동안 %LOCALS:0%...
		PRINTFORM 시간 속에 붙들린 %TNAME(TARGET)%은
		;Ａ経験180以上
		IF EXP:UExp > 180
			PRINT 잘 발달된 구멍이 그것을 탐욕스럽게 삼킨다.
			;Ａ経験20以上
		ELSEIF EXP:UExp > 20
			PRINT 발달된 요도가 기대하며 움찔거린다.
			;それ以外
		ELSE
			PRINTFORM  아직 익숙하지 않은 요도는 플러그를 거부하듯 움찔거린다.
		ENDIF
		PRINTL ...
		PRINTFORM 플러그를 %CALLNAME:TARGET%의
		;潤滑LV4以上
		SIF PALAM:潤滑 >= PALAMLV:4
			PRINT 축축한
		PRINTFORML  %FSYN("urethra")%...
	ENDIF
ELSEIF CFLAG:MASTER:イタズラ == 2
	IF !TEQUIP:UrethralSound
		PRINTFORML 잠든 %CALLNAME:TARGET%의 %FSYN("urethra")%에서 요도 플러그를 천천히 빼냈다.
		SELECTCASE EXP:Ａ経験
			CASE IS > 180
				PRINTFORML 플러그를 빼내자 %TNAME(TARGET,1)%의 요도가 탐욕스럽게 경련하며 오줌이 흘러나온다...
			CASE 20 TO 179
				PRINTFORML 플러그를 빼낸 뒤에도 %TNAME(TARGET,1)%의 %FSYN("urethra")%는 완전히 닫히지 않는다...
			CASEELSE
				PRINTFORML 플러그를 빼내자 %TNAME(TARGET,1)%의 오줌 구멍이 고통스럽게 부풀어 오른다...
		ENDSELECT
	ELSE
		LOCALS:1 =
		IF TEQUIP:0 & 2
			LOCALS:1 '= @"%PRINT_PANTY_MOVEMENT(TARGET, MASTER)%요도 플러그를 %CALLNAME:TARGET%의 %FSYN("urethra")%에 밀어 넣었다"
		ELSE
			LOCALS:1 '= @"잠든 %CALLNAME:TARGET%의 %FSYN("urethra")%에 플러그를 밀어 넣었다"
		ENDIF
		PRINTFORML %LOCALS:1%...
		PRINTFORM %TNAME(TARGET)%은 깊이 잠들어 있다.
		;180 over
		IF EXP:UExp > 180
			PRINT 잘 발달된 구멍이 그것을 탐욕스럽게 삼킨다.
			;over 20
		ELSEIF EXP:UExp > 20
			PRINT 발달된 요도가 기대하며 움찔거린다.
			;それ以外
		ELSE
			PRINTFORM  아직 익숙하지 않은 요도는 플러그를 거부하듯 움찔거린다.
		ENDIF
		PRINTL ...
		PRINTFORM 플러그를 %CALLNAME:TARGET%의
		;潤滑LV4以上
		SIF PALAM:潤滑 >= PALAMLV:4
			PRINT 축축한
		PRINTFORML  %FSYN("urethra")%...
	ENDIF
ELSE
	;解除時
	IF !TEQUIP:UrethralSound
		PRINTFORML %CALLNAME:TARGET%의 %FSYN("urethra")%에서 요도 플러그를 빼냈다.
		PRINTFORM %TNAME(TARGET,1)%
		;Ａ経験180以上＋淫乱か娼婦
		IF EXP:UExp > 180 && TALENT:淫乱
			PRINTFORM  입술 양쪽으로 액체가 흘러내리고, 벌어진 %FSYN("urethra")%는 좀처럼 닫히지 않는다.
			;Ａ経験140以上＋淫乱か娼婦
		ELSEIF EXP:UExp > 140 && TALENT:淫乱
			PRINTFORM  눈물 맺힌 눈으로 당신을 바라보며 %FSYN("urethra")%를 가늘게 떤다.
			;Ａ経験100以上＋淫乱か娼婦
		ELSEIF EXP:UExp > 100 && TALENT:淫乱
			PRINTFORM 그의 %FSYN("urethra")%는 항의하면서 만족스러워 보이지 않았다
			;Ａ経験70以上＋淫乱か娼婦
		ELSEIF EXP:UExp > 70 && TALENT:淫乱
			PRINTFORM %FSYN("urethra")%가 약간 실망한 듯 움찔한다.
			;Ａ経験40以上
		ELSEIF EXP:UExp > 40
			PRINTFORM %FSYN("urethra")%가 움찔하지만 %HE_SHE(TARGET)%는 안도의 한숨을 내쉰다.
			;Ａ経験20以上
		ELSEIF EXP:UExp > 20
			PRINTFORM  강제 배출에 얼굴을 붉힌다.
			;それ以外
		ELSE
			PRINTFORM  %HE_SHE(TARGET)%이 고통을 참자 움츠러든다.
		ENDIF
		PRINTFORML ...
	ELSE
		LOCALS:2 =
		IF TEQUIP:0 & 2
			LOCALS:2 '= @"%PRINT_PANTY_MOVEMENT(TARGET, MASTER)%요도 플러그를 %CALLNAME:TARGET%의 %FSYN("urethra")%에 밀어 넣었다"
		ELSE
			LOCALS:2 '= @"플러그를 %CALLNAME:TARGET%의 %FSYN("urethra")%에 밀어 넣었다"
		ENDIF
		PRINTFORML %LOCALS:2%...
		PRINTFORM %TNAME(TARGET,1)%
		;Ａ経験180以上＋淫乱か娼婦
		IF EXP:UExp > 180 && TALENT:淫乱
			PRINTFORM 얼굴이 쾌락에 녹아내리고, 스스로 다리를 벌린 채 %HIP(TARGET)%을 내민다...
			;Ａ経験140以上＋淫乱か娼婦
		ELSEIF EXP:UExp > 140 && TALENT:淫乱
			PRINTFORM 삽입을 갈망하듯 골반을 흔든다.
			;Ａ経験100以上＋淫乱か娼婦
		ELSEIF EXP:UExp > 100 && TALENT:淫乱
			PRINTFORM 홍조를 띤 채 기대감에 떨며 %HIP(TARGET)%을 흔든다.
			;Ａ経験70以上＋淫乱か娼婦
		ELSEIF EXP:UExp > 70 && TALENT:淫乱
			PRINTFORM 몸이 떨린다, 플러그가 가까워지자.
			;Ａ経験40以上
		ELSEIF EXP:UExp > 40
			PRINTFORM 그의 얼굴은 흥분과 역겨움이 뒤섞여 있었다.
			;Ａ経験20以上
		ELSEIF EXP:UExp > 20
			PRINTFORM  %HE_SHE(TARGET)%이/가 누우자 얼굴을 새빨갛게 물들였다.
			;それ以外
		ELSE
			PRINTFORM  자신의 민감한 구멍이 농락당한다는 수치심에 얼굴이 새빨개진다.
		ENDIF
		PRINTL ...
		PRINTFORM 요도 플러그를 %CALLNAME:TARGET%의
		;潤滑LV4以上
		SIF PALAM:潤滑 >= PALAMLV:4
			PRINT 축축한
		PRINTFORML  %FSYN("urethra")%...
	ENDIF
ENDIF

@ADD_CUSTOM_COM_ABLE163
;バイブ実行判定
SIF !TFLAG:100
	RETURN 0
SIF !ITEM:313
	RETURN 0
SIF TALENT:MASTER:SexKnow < 5
	RETURN 0
SIF !nPee
	RETURN 0
SIF !CFLAG:MASTER:うふふ && !CFLAG:MASTER:今忙しくて手が離せない
	RETURN 0
SIF CFLAG:MASTER:うふふ == 2
	RETURN 0
RETURN 1
