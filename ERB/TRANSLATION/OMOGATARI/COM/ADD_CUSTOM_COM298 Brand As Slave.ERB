@ADD_CUSTOM_COM298
IF FLAG:70
BASE:MASTER:TSP -= 500
ELSE
TIME += 5
SOURCE:反感 = 10000
ENDIF
DOWNBASE:0 += 800
DOWNBASE:1 += 1000
SOURCE:苦痛 += 8000
SOURCE:Terror = 60000
RETURN 1

@ADD_CUSTOM_MESSAGE_COM298
PRINTFORMW You grab %CALLNAME:TARGET% with your arm, turning %HIM_HER(TARGET)% on %HIS_HER(TARGET)% back.
PRINTFORMW You lift up %HIS_HER(TARGET)% clothes, allowing %HIS_HER(TARGET)% bare back to be exposed to the world.
PRINTFORMW Keeping %CALLNAME:TARGET% restrained, you pull out the slave brander and turn it on, letting it heat up.
PRINTL
PRINTFORMW As the slave brander is fully heated, you move it to %CALLNAME:TARGET% and sear the brander on with force.
PRINTFORMW %CALLNAME:TARGET% screams in agony, tears pooling as the mark is burned into %HIS_HER(TARGET)% back.
PRINTFORMW The smell of burnt flesh and cries from %CALLNAME:TARGET% fill the air as the brand sets in.
PRINTL
PRINTFORMW When you lift the tool up, you find these markings burned into %HIS_HER(TARGET)% back.
PRINTFORMW 「SLAVE」 in big wide letters.
PRINTFORMW And「Property of %NAME:MASTER%」 underneath.
PRINTFORMW You quickly pull down %HIS_HER(TARGET)% clothes and let %HIM_HER(TARGET)% go.
PRINTFORMW %HE_SHE(TARGET,1)% doesn't know that %HES_SHES(TARGET)% now branded a slave, but the agonizing experience will definitely take a toll on %HIS_HER(TARGET)% psyche.
CALLF ChangeEndurance(TARGET,"charatrauma",600)
CALLF AddBionic(TARGET,81,140,0)






@ADD_CUSTOM_COM_ABLE298
SIF !TFLAG:100
	RETURN 0
SIF !ITEM:SlaveBrander ; Must have the slave brander
    RETURN 0
SIF TARGET == MASTER ; doesn't make sense to brand yourself
    RETURN 0
SIF CFLAG:うふふ != 1 ; must be in sex, specificly MC controlled.
    RETURN 0
SIF isDead:TARGET:0 ; must be alive
	RETURN 0
SIF inTrain == 3 ;lucid dream
	RETURN 0
IF FLAG:70
	SIF BASE:MASTER:TSP < 500 ;hit min TSP requirement
		RETURN 0
ELSE
    SIF !TCVAR:TARGET:無理矢理 && SHIRAHU(TARGET) ; must be raping
        RETURN 0
ENDIF
SIF DT_SELECT(@"Hediff", @"chara = {TARGET} and type = 240") ; already branded
    RETURN 0
RETURN 1