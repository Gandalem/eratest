; Modular ERB: OMOGATARI/COM/ADD_CUSTOM_COM298 Brand As Slave.ERB

@ADD_CUSTOM_COM298
IF FLAG:70
	BASE:MASTER:TSP -= 500
ELSE
	TIME += 5
	SOURCE:反感 = 10000
ENDIF
DOWNBASE:0 += 800
DOWNBASE:1 += 1000
SOURCE:苦痛 += 8000
SOURCE:Terror = 60000
RETURN 1

@ADD_CUSTOM_MESSAGE_COM298
PRINTFORMW 당신은 팔로 %조사처리(CALLNAME:TARGET,"를")% 붙잡고 %HIM_HER(TARGET)%를 %HIS_HER(TARGET)%에게 돌린다.
PRINTFORMW 당신은 %HIS_HER(TARGET)%의 옷을 들어 올려, %HIS_HER(TARGET)%의 벌거벗은 등이 세상에 노출되게 한다.
PRINTFORMW %조사처리(CALLNAME:TARGET,"를")% 제지한 채, 당신은 노예 낙인 도구를 꺼내 작동시키고 뜨겁게 달아오르게 한다.
PRINTL
PRINTFORMW 노예 낙인 도장이 완전히 달궈지면, 그것을 %조사처리(CALLNAME:TARGET,"에게")% 옮기고 힘으로 도장을 새긴다.
PRINTFORMW %조사처리(CALLNAME:TARGET,"는")%(는) 비명을 지르며 고통 속에 눈물을 흘리는데, 표식이 %HIS_HER(TARGET)%의 등에 새겨진다.
PRINTFORMW 불에 탄 살과 %조사처리(CALLNAME:TARGET,"의")% 울음소리가 공기를 가득 채우고 도장이 각인된다.
PRINTL
PRINTFORMW 도구를 들어 올리면, 당신은 %HIS_HER(TARGET)%의 등에 이 표식들이 새겨져 있는 것을 발견한다.
PRINTFORMW 크고 넓은 글씨로 「SLAVE」라고 적혀 있다.
PRINTFORMW 그리고 그 아래에「Property of %NAME:MASTER%」이라고 쓰여 있다.
PRINTFORMW 당신은 재빨리 %HIS_HER(TARGET)%의 옷을 벗기고 %HIM_HER(TARGET)%를 풀어준다.
PRINTFORMW %HE_SHE(TARGET,1)%는 %HES_SHES(TARGET)%가 이제 노예로 낙인찍혔다는 것을 모르지만, 그 고통스러운 경험은 분명히 %HIS_HER(TARGET)%의 정신에 영향을 미칠 것이다.
CALLF ChangeEndurance(TARGET,"charatrauma",600)
CALLF AddBionic(TARGET,81,140,0)

@ADD_CUSTOM_COM_ABLE298
SIF !TFLAG:100
	RETURN 0
SIF !ITEM:SlaveBrander ; Must have the slave brander
	RETURN 0
SIF TARGET == MASTER ; doesn't make sense to brand yourself
	RETURN 0
SIF CFLAG:うふふ != 1 ; must be in sex, specificly MC controlled.
	RETURN 0
SIF isDead:TARGET:0 ; must be alive
	RETURN 0
SIF inTrain == 3 ;lucid dream
	RETURN 0
IF FLAG:70
	SIF BASE:MASTER:TSP < 500 ;hit min TSP requirement
		RETURN 0
ELSE
	SIF !TCVAR:TARGET:無理矢理 && SHIRAHU(TARGET) ; must be raping
		RETURN 0
ENDIF
SIF DT_SELECT(@"Hediff", @"chara = {TARGET} and type = 240") ; already branded
	RETURN 0
RETURN 1
