;-------------------------------------------------
;オナニー
;日常系コマンド、レベル1
;-------------------------------------------------
@COM451_TR
#DIM C_ID
#DIM AMT
#DIM TOOL
#DIM DIV
#DIMS MasturbFuck
SIF ISSKIP()
	RETURN -1

IF TARGET != MASTER && ABL:TARGET:露出癖 + TALENT:TARGET:羞恥心 < 3
	CALL Com_Mast ;com custom code
	SIF RESULT
		RETURN 1
	FOR LOCAL,1,GET_TARGETNUM()+1
		IF !FLAG:70 || MAXBASE:(TARGET:LOCAL):TSP
		;Kodoku no Gourmet reference
		PRINTFORML When we masturbate, we want to indulge ourselves freely, without interruption. We want to feel relaxed.
		PRINTFORMW We want to be alone somewhere quiet and comfortable...
		RETURN -1
		ENDIF
	NEXT
ENDIF

;NAS:
;fucking what? there's masturbation commands?
;use that instead.
CALL ASK_M("Interactive Masturbation", 1, "Quick Masturbation", 1, "Cancel", 1)
SELECTCASE RESULT
CASE 0
	CFLAG:MASTER:今忙しくて手が離せない = 1
	SIF !FLAG:時間停止
		TIME += 5
	TARGET = MASTER
	RETURN
CASE 2
	RETURN
ENDSELECT
;====================================================

;custom code for different toys
PRINTFORML Select which toy you are going to use.
FOR LOCAL,0,920
    IF GROUPMATCH(LOCAL,0,1,3,4,12)
	SIF !ITEM:(LOCAL)
		CONTINUE
	PRINTFORML [{LOCAL}] - %ITEMNAME_TR(LOCAL)% 
    ENDIF
NEXT
SIF HAS_PENIS(MASTER)
	PRINTFORML [989] - Hand (Penis)
IF HAS_VAGINA(MASTER)	
	IF !OUTROOF(CFLAG:MASTER:現在位置)
	; PRINTFORML [996] - Table
	; PRINTFORML [997] - Door Handle
	ENDIF
	PRINTFORML [998] - Hand (Vagina)
ENDIF
PRINTFORML [999] - Back off
INPUT
TOOL = RESULT
SIF TOOL == 999
	RETURN

PRINTFORML Who comes to %PARSE("your")% mind?
DRAWLINE
FOR LOCAL,1,CHARANUM
	IF !IS_NAMED(LOCAL) || !CFLAG:LOCAL:面識
		CONTINUE
	ENDIF
	PRINTFORMLC [{LOCAL, 3}] %CALLNAME:LOCAL%
NEXT
PRINTL
DRAWLINE
PRINTL [999] No need to fantasize about anyone! [1000] Cancel
INPUT
C_ID = RESULT
SELECTCASE C_ID
	CASE 1 TO CHARANUM
		CALL PRINT_FACE, C_ID, "発情", "裸", "", "照れ" ;custom code tweak - doesn't show nude version
		PRINTFORML %PARSE("You take")% care of %PARSE("your",1)% sexual urges while thinking about %CALLNAME:C_ID%.
	CASE 999
		{
		PRINTFORML %PARSE("You deal")% with %PARSE("your",1)% sexual urges while contemplating %SPLIT_G("
the movements of the celestial bodies:
the secrets of the cosmos:
the nature:
the mountains, rivers, trees and grasses:
the travails of life:
the issues of birth, old age, illness and dying:
the purpose of life:
current events:
what to do next:
the world as a simulation:
what living in space would feel like:
		")%.
		}
	CASEELSE
		RETURN -1
ENDSELECT
AMT = 50 + RAND:50
DIV = 2
VARSET MasturbFuck
PRINTFORML
PRINTFORMW You pull down your %SHOW_BOTTOM(MASTER)% as you prepare to %FSYN("hj")% yourself.
PRINTFORML
SELECTCASE TOOL
CASE 0
	DIV = 4
	PRINTFORMW You place a rotor on your %PRINT_CLIT(MASTER)% and turn it on.
	PRINTFORML An immediate wave of pleasure rolls through your body as you leak out a few moans.	
	PRINTFORML
	PRINTFORML However, in this state, the vibrator isn't enough to get you to climax.
	PRINTFORMW You turn up the vibrator to a higher speed and get another wave of pleasure.
	PRINTFORML
	PRINTFORML It's too much for your body, and you start to moan continuously 
	PRINTFORMW You whimper and squirm as you let a tiny sex toy take over your body.
	MasturbFuck += "C, "
CASE 1
	AMT = 100 + RAND:100
	DIV = 16
	PRINTFORML %PARSE("You press")% the vibrator against %PARSE("your")% \@ HAS_VAGINA() ? pussy # penis\@\@ TEQUIP:0 > 1 ? %" "%through your %SHOW_BOTTOM(MASTER)% # \@.
	PRINTFORM %PARSE("Your")% 
	IF GETBIT(TALENT:性別,1)
		PRINTFORM %TINKO(MASTER)% 
	ELSE
		PRINTFORM %FSYN("clit")% 
	ENDIF
	PRINTFORM twitches
	;Ｃ感覚LV3未満＋欲情のPALAMLV3未満
	IF ABL:MASTER:Ｃ感覚 < 3
		PRINTFORM  and jumps as if it's being tickled
	;恋慕か親愛＋Ｃ感覚LV4以上＋欲情のPALAMLV3以上＋欲情のPALAMLV5未満
	ELSEIF ABL:MASTER:Ｃ感覚 >= 3 && INRANGE(C_ID,0,CHARANUM-1)
		PRINTFORM  as you drool, calling out %CALLNAME:C_ID%'s name
	ENDIF
	SIF ABL:MASTER:Ｃ感覚 >= 3
		PRINTFORM , about to cum any second
	PRINTL ...
	MasturbFuck += "C, "
CASE 3
	DIV = 8
	IF HAS_PENIS(MASTER)
	AMT = 100 + RAND:100
	PRINTFORMW You grab the onahole, pour a bit of lubricant at the opening, and insert your %TINKO(MASTER)% inside.
	PRINTFORML The %FSYN("onahole")% engulfs the entirety of your %TINKO_TR()% as you begin to stroke it.
	PRINTFORML 
	;placeholder texts
	PRINTFORML Using the onahole, %PARSE("you enhance")% the experience quite a bit.
	ELSE
	PRINTFORML Despite not having a penis to use the %FSYN("onahole")% with, you decide to pull it out anyway.
	PRINTFORMW You improvise by turning the onahole inside out, allowing you to grind at it.
	PRINTFORML
	PRINTFORML Slowly grinding at the 
	ENDIF
	MasturbFuck += "C, "
CASE 4
	DIV = 8
	IF TALENT:MASTER:処女 == 1
		SETCOLOR C_RED
		PRINTFORML By continuing, you will lose your virginity.
		RESETCOLOR
		PRINTFORML
		PRINTFORML [  0] - That's fine.
		PRINTFORML [999] - Get me out of here!
		INPUT
		SELECTCASE RESULT
		CASE 0
		SETCOLOR 247,171,166
		PRINTFORMW 【Virginity Loss】
		CALL SET_HISTORY_LOST_V(MASTER, MASTER, "バイブ")
		RESETCOLOR
		PRINTFORMW ...
		PRINTFORMW ..
		PRINTFORMW .		
		PRINTFORMW Blood drips from your %FSYN("vagina")% as you push it in all the way.		
		CASEELSE
		RETURN -1
		ENDSELECT
	ENDIF
	MasturbFuck += "V, "
CASE 12
	PRINTFORMW
	IF TALENT:MASTER:処女 == 1
		SETCOLOR C_RED
		PRINTFORML By continuing, you will lose your virginity.
		RESETCOLOR
		PRINTFORML
		PRINTFORML [  0] - That's fine.
		PRINTFORML [999] - Get me out of here!
		INPUT
		SELECTCASE RESULT
		CASE 0
		SETCOLOR 247,171,166
		PRINTFORMW 【Virginity Loss】
		CALL SET_HISTORY_LOST_V(MASTER, MASTER, "dildo")
		RESETCOLOR
		PRINTFORMW ...
		PRINTFORMW ..
		PRINTFORMW .		
		PRINTFORMW Blood drips from your %FSYN("vagina")% as you push it in all the way.		
		CASEELSE
		RETURN -1
		ENDSELECT
	ENDIF
	MasturbFuck += "V, "
CASE 989
	PRINTFORML You sit down on the floor and spread your legs, gripping your %TINKO()% with your right hand.
	PRINTFORMW %PARSE("You")% slowly %PARSE("begin")% to increase %PARSE("your")% pace as %PARSE("you edge")% closer and closer to orgasm.
	MasturbFuck += "C, "
CASE 997
CASE 998
	PRINTFORML You sit down on the floor and spread your legs, spreading your %FSYN("slit")% with your left hand.
	PRINTFORMW With your right hand, you start rubbing your clitoris to pleasure yourself.
	PRINTFORML You start moaning as your mind goes into autopilot.
	PRINTFORMW Eventually, you start dripping pussy juice and immediately stop rubbing your clit.
	PRINTFORML
	PRINTFORML With your %FSYN("vagina")% lubricated enough, you place two fingers inside your %FSYN("vagina")% as you start stimulating it.
	PRINTFORMW Your %FSYN("vagina")% gladly accepts your fingers...
	;small chance of accidentally losing your virginity
	IF TALENT:MASTER:処女 == 1 && !RAND:20
		SETCOLOR 247,171,166
		PRINTFORMW 【Virginity Loss】
		CALL SET_HISTORY_LOST_V(MASTER, MASTER, "指")
		RESETCOLOR
		PRINTFORMW ...
		PRINTFORMW ..
		PRINTFORMW .
		IF TALENT:MASTER:SexKnow == -1
			PRINTFORMW You don't know what to do...
			PRINTFORMW It kills the mood...
			RETURN -1
		ELSE
			PRINTFORMW You accidentally pushed your fingers in a bit too far and tore your hymen...
			PRINTFORMW Blood drips from your %FSYN("vagina")% as you continue to stimulate it.
		ENDIF
	ENDIF
	PRINTFORML You stimulate your vaginal walls as you start extracting pleasure from them.
	PRINTFORMW You eventually find your G-spot and start hammering it as hard as you can.
	PRINTFORML
	MasturbFuck += "V, "
	IF !RAND:2
	PRINTFORML It feels good, but you need that clitoral stimulation to get off.
	PRINTFORML Your right hand starts to rub your clitoris while the fingers from your left continue to hammer at your %FSYN("vagina")%.
	PRINTFORML You moan uncontrollably as the pleasure from your %FSYN("vagina")% and clit takes over.
	PRINTFORML You continue to %FSYN("hj")% yourself silly as you enjoy every moment of it.
	MasturbFuck += "C, "
	ELSE
	PRINTFORML Your poor body can't endure the sensation from stimulating your G-spot as it starts to %FSYN("twitch")%.
	PRINTFORML You push your fingers as hard and fast as you can, trying to get the final push.
	ENDIF
CASEELSE
	CALL NAS_Error_Show(@"@COM451 TAKES NO SUCH ITEM AS {TOOL} \"%ITEMNAME_TR(TOOL)%\"")
	RETURN
ENDSELECT

PRINTFORML As your start to feel something coming out, you:
CALL ASK_YN("Take your hand off...","Give in and climax...")
LOCAL = RESULT
WAIT
PRINTFORML

SELECTCASE LOCAL
CASE 0
	IF HAS_PENIS(MASTER) && TALENT:MASTER:120 != -1 && BASE:MASTER:6 == MAXBASE:MASTER:6
	;custom code for color
	PRINTFORMW ... %PARSE("Your")% semen builds up in you, giving you 【Thick Semen】.
	RESETCOLOR
	TALENT:MASTER:120 = 1
	ENDIF	
	DOWNBASE:MASTER:気力 = 100
	;custom code for exp and message
	EXP:MASTER:自慰経験 += 1
	OrgasmDenyExp:MASTER:0 += 5	
	CFLAG:MASTER:溜まってる度 = LIMIT(CFLAG:MASTER:溜まってる度+(5*DIV),0,1000)
	PRINTFORMW You ache in discomfort for a bit before you get up and consider what to do next.	
CASE 1
	IF HAS_PENIS(MASTER)
	;custom code for color
	SETCOLOR C_CREAM
	IF TALENT:MASTER:濃厚精液
		AMT *= 2
		;CALL CUMSOUNDS("m")
		PRINTFORMW ... %PARSE("You release")% {AMT}ml of %PARSE("your",1)% extra thick stuff.
	ELSE
		;CALL CUMSOUNDS("l")
		PRINTFORMW ... %PARSE("You release")% {AMT}ml.
	ENDIF
	PRINTFORMW %PARSE("You finish")% with a gasp as %PARSE("your")% %TINKO()% spurts several ropes of %SEMEN()% on the floor.  	
	RESETCOLOR
	MasturbFuck += "cumlove, ejac, "
	ENDIF

PRINTFORML Your mind blanks as your back curves in orgasm, pleasure filling your entire body as you cum. 

IF PeeHold:MASTER:2 <= -2
;CALL PEESOUNDS("h")
	PRINTL
	PRINTFORML ... A %PeeColor(MASTER)% spray soaks into the floor during your orgasm.
	MasturbFuck += "piss, "
ENDIF

IF PooHold:MASTER:2 <= -2
;CALL PEESOUNDS("h")
	PRINTFORML ... You release a bit of %SCAT(MASTER)% onto the floor.
	MasturbFuck += "shit, "
ENDIF

EXP:MASTER:自慰経験 += ABL:MASTER:自慰中毒 * 2 + 1
CALL COLORMESSAGE(@"Masturbation Exp＋{ABL:MASTER:自慰中毒 * 2 + 1}",C_PINK,1)
SELECTCASE ABL:MASTER:自慰中毒
	CASE 0 TO 2
		LOCAL = 5
	CASE 3 TO 5
		LOCAL = 3
	CASEELSE
		LOCAL = 1
ENDSELECT
MasturbFuck += "exposure, "
CALL FUCK(MASTER, MasturbFuck, @"", 0, TARGET, TARGET, TARGET, @"", "ペニス", "膣", "口")
CALL CharacterSinned(MASTER,"Masturbation",@"masturbated", -3,-1)

BASE:MASTER:精力 -= AMT
DOWNBASE:MASTER:気力 = 100
;custom code for exp and message
EXP:MASTER:自慰経験 += 1
OrgasmDenyExp:MASTER:0 -= 1
SIF TALENT:MASTER:濃厚精液 || CFLAG:MASTER:溜まってる度 >= 900
	PRINTFORML %PARSE("You feel")% relieved.
CFLAG:MASTER:溜まってる度 /= DIV
;-----------
PRINTFORMW You soak in the afterglow for a bit before you get up and consider what to do next.
ENDSELECT

nSexDone -- ;addition custom code

IF FLAG:70
BASE:MASTER:TSP -= 60
ELSE
TIME += 20
ENDIF
RETURN 1