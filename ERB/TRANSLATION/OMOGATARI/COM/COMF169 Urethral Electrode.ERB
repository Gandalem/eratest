;-------------------------------------------------
;Urethral Electrode
;Pleasures the target's urethra by inserting a plug (but electrical)
;Hard training command level: 4
;Flags required: Urination
;-------------------------------------------------
@ADD_CUSTOM_COM169
;着脱の処理
CALL EStimChannelCheck

IF TFLAG:EStim >= FLAG:EStimBox+1 && !TEQUIP:EStimUrethra
	PRINTFORML Your control box can only support {FLAG:EStimBox+1} %PLURALIZER("channel",FLAG:EStimBox+1)%. 
	PRINTFORMW Please either remove an electrode or upgrade your box.
	RETURN -1
ELSEIF !ITEM:EStimBox
	PRINTFORMW %PARSE("You don't")% have a way to power this.
	FONTITALIC
	PRINTFORMW Buy a %ITEMNAME_TR(145)% to use this.
	FONTREGULAR
	RETURN -1	
ELSE
TEQUIP:EStimUrethra = !TEQUIP:EStimUrethra
CALL EStimChannelCheck
ENDIF
;-------------------------------------------------
;COM別FLAGの設定
;-------------------------------------------------
;行為者
TCVAR:116 = PLAYER
;奉仕快楽フラグ
TCVAR:PLAYER:108 = 1

CSTR:TARGET:10 += @"{PLAYER}/"
;TCVAR:2 射精箇所フラグ（-1=コンドーム 1=膣内 2=アナル 3=手淫 4=口淫 5=パイズリ 6=素股 7=足コキ 8=体表 9=アナル奉仕 10=道具 11=触手 12=Ａ責め 13=Ｖ責め）
TCVAR:TARGET:2 = 23
;強度
TCVAR:TARGET:4 = 900
TCVAR:LostVirginU = 1

;-------------------------------------------------
;ソース追加処理
;-------------------------------------------------
;消費体力、気力
IF FLAG:70 == 1
	BASE:MASTER:TSP -= 100
	SOURCE:UPleas = 1000
	SOURCE:苦痛 = 2600

ELSE
	DOWNBASE:体力 += 30
	DOWNBASE:気力 += 100

	SOURCE:UPleas = 1000
	SOURCE:苦痛 = 4000
	SOURCE:露出 = 300
	SOURCE:屈従 = 800
	SOURCE:逸脱 = 1200
	SOURCE:反感 = 1200
ENDIF

;-------------------------------------------------
;特殊ソース処理
;-------------------------------------------------
;抑圧、抵抗持ちには、抑鬱ソース追加
SIF TALENT:自己愛 < 0 || TALENT:抵抗
	SOURCE:鬱屈 += 40

;give pain and hostility if still urethral virgin
IF !CFLAG:TARGET:LostVirginUDay
	SOURCE:苦痛 += 2000
	SIF !FLAG:70
	SOURCE:反感 += 3000
ENDIF

;call pain check
CALL PAIN_CHECK_U(TARGET)

;-------------------------------------------------
;経験の処理
;-------------------------------------------------
;give exp
EXP:UExp ++
EXP:USex ++
IF FLAG:70 == 1
	IF HETEROSEX(TARGET,PLAYER)
		EXP:PLAYER:(50 + HETEROSEX(TARGET,PLAYER)) += 1
	ENDIF
ELSE
	;異常経験
	IF !CFLAG:TARGET:LostVirginUDay && !GETBIT(CFLAG:3,3)
		EXP:異常経験 ++
		SETBIT CFLAG:3,3
	ENDIF

	;同性愛　レズなら戻り値1ゲイなら戻り値2
	IF HETEROSEX(TARGET,PLAYER)
		EXP:(50 + HETEROSEX(TARGET,PLAYER)) += 1
		EXP:PLAYER:(50 + HETEROSEX(TARGET,PLAYER)) += 1
	ENDIF
ENDIF
EXP:PLAYER:道具使用経験 ++

RETURN 1

;-------------------------------------------------
;装着継続の処理
;-------------------------------------------------
@ADD_CUSTOM_EQUIP_COM169(ARG)
;insert plug
PRINTL ＜Urethral Electrode＞

CSTR:TARGET:10 += @"{PLAYER}/"
TCVAR:TARGET:2 = 22
TCVAR:TARGET:4 = 450

IF FLAG:70 == 1
BASE:MASTER:TSP -= 50
SOURCE:ARG:UPleas += 800 + (100*TFLAG:VibePower)

ELSE


DOWNBASE:ARG:体力 += 10
DOWNBASE:ARG:気力 += 50

SOURCE:ARG:UPleas += 800
SOURCE:ARG:露出 += 10
SOURCE:ARG:逸脱 += 20
PeeHoldExp:ARG:0 += 1
EXP:UExp ++
EXP:USex ++

;-------------------------------------------------
;特殊ソース処理
;-------------------------------------------------
;抑圧、抵抗持ちには、抑鬱ソース追加
SIF TALENT:ARG:自己愛 < 0 || TALENT:ARG:抵抗
	SOURCE:ARG:鬱屈 += 20

;Vへの苦痛は先に処理しておく
;EXP:Ｖ経験をみる（処女はありえない）
LOCAL = 25
LOCAL:1 = 20
IF EXP:ARG:UExp < EXPLV:2
	TIMES LOCAL , 0.80
	TIMES LOCAL:1 , 1.20
ELSEIF EXP:ARG:UExp < EXPLV:3
	TIMES LOCAL , 0.40
ELSEIF EXP:ARG:UExp < EXPLV:4
	TIMES LOCAL , 0.10
ELSEIF EXP:ARG:UExp < EXPLV:5
	TIMES LOCAL , 0.00
ELSEIF EXP:ARG:UExp >= EXPLV:5
	TIMES LOCAL , 0.00
ENDIF

;PALAM:潤滑をみる
IF PALAM:ARG:潤滑 < PALAMLV:1
	TIMES LOCAL , 1.20
	TIMES LOCAL:1 , 1.50
ELSEIF PALAM:ARG:潤滑 < PALAMLV:2
	TIMES LOCAL , 0.70
	TIMES LOCAL:1 , 1.20
ELSEIF PALAM:ARG:潤滑 < PALAMLV:3
	TIMES LOCAL , 0.40
ELSEIF PALAM:ARG:潤滑 < PALAMLV:4
	TIMES LOCAL , 0.10
ELSEIF PALAM:ARG:潤滑 >= PALAMLV:4
	TIMES LOCAL , 0.10
ENDIF
SOURCE:ARG:苦痛 += LOCAL
SOURCE:ARG:反感 += LOCAL:1

;-------------------------------------------------
;経験の処理
;-------------------------------------------------
;自慰実行時は経験＋１
SIF EXP_UP(68,TARGET)
	EXP:ARG:UExp += 1
ENDIF
RETURN 1

@ADD_CUSTOM_MESSAGE_COM169
IF FLAG:70 == 1
	IF !TEQUIP:EStimUrethra
		PRINTFORML %PARSE("You")% slowly %PARSE("pull")% the urethral electrode out of %CALLNAME:TARGET%'s %FSYN("urethra")% while time is stopped.
		SELECTCASE EXP:UExp
			CASE IS > 180
				PRINTFORML %TNAME(TARGET,1)%'s twitches greedily as %PARSE("you pull",1)% it out...
			CASE 20 TO 179
				PRINTFORML %TNAME(TARGET,1)%'s %FSYN("urethra")% doesn't close completely after %PARSE("you pull",1)% it out...
			CASEELSE
				PRINTFORML %TNAME(TARGET,1)%'s piss hole swells painfully once %PARSE("you've",1)% pulled it out...
		ENDSELECT
	ELSE
		PRINTFORM %PARSE("You")% 
		IF HAS_PANTY_MOVEMENT()
			PRINTFORM %PRINT_PANTY_MOVEMENT(TARGET, MASTER)% and %PARSE("press")% the urethral electrode against %HIS_HER(TARGET)% %FSYN("urethra")%
		ELSE
			PRINTFORM %PARSE("press")% the plug against %CALLNAME:TARGET%'s %FSYN("urethra")%
		ENDIF
		PRINTFORML  while time is stopped...
		PRINTFORM Frozen in time, %TNAME(TARGET)%
		;Ａ経験180以上
		IF EXP:UExp > 180
			PRINT 's well developed pisshole swallows it greedily
		;Ａ経験20以上
		ELSEIF EXP:UExp > 20
			PRINT 's developed urethra twitches expectantly
		;それ以外
		ELSE
			PRINTFORM  is unable to resist, completely at %PARSE("your")% mercy as %PARSE("you play",1)% with %HIS_HER(TARGET)% %FSYN("urethra")%
		ENDIF
		PRINTL ...
		PRINTFORM %PARSE("You insert")% the plug in %CALLNAME:TARGET%'s 
		;潤滑LV4以上
		SIF PALAM:潤滑 >= PALAMLV:4
			PRINT wet 
		PRINTFORML %FSYN("urethra")%...
	ENDIF
ELSEIF CFLAG:MASTER:イタズラ == 2
	IF !TEQUIP:EStimUrethra
		PRINTFORML %PARSE("You")% slowly %PARSE("pull")% out the urethral electrode from the sleeping %CALLNAME:TARGET%'s %FSYN("urethra")%.
		SELECTCASE EXP:Ａ経験
			CASE IS > 180
				PRINTFORML %TNAME(TARGET,1)%'s twitches greedily as %PARSE("you pull",1)% it out...
			CASE 20 TO 179
				PRINTFORML %TNAME(TARGET,1)%'s %FSYN("urethra")% doesn't close completely after %PARSE("you pull",1)% it out...
			CASEELSE
				PRINTFORML %TNAME(TARGET,1)%'s %FSYN("urethra")% swells painfully once %PARSE("you've",1)% pulled it out...
		ENDSELECT
	ELSE
		PRINTFORM %PARSE("You")% 
		IF HAS_PANTY_MOVEMENT()
			PRINTFORM %PARSE("shift")% aside the sleeping %CALLNAME:TARGET%'s %SHOW_BOTTOM(TARGET,2)% and %PARSE("press")% the urethral plug against %HIS_HER(TARGET)% %FSYN("urethra")%
		ELSE
			PRINTFORM %PARSE("press")% the plug against the sleeping %CALLNAME:TARGET%'s %FSYN("urethra")%
		ENDIF
		PRINTFORML ...
		PRINTFORM Fast asleep, %TNAME(TARGET)%
		;180 over
		IF EXP:UExp > 180
			PRINT 's well developed pisshole swallows it greedily
		;over 20
		ELSEIF EXP:UExp > 20
			PRINT 's developed urethra twitches expectantly
		;それ以外
		ELSE
			PRINTFORM  is unable to resist, completely at %PARSE("your")% mercy as %PARSE("you play",1)% with %HIS_HER(TARGET)% %FSYN("urethra")%
		ENDIF
		PRINTL ...
		PRINTFORM %PARSE("You insert")% the electrode in %CALLNAME:TARGET%'s 
		;潤滑LV4以上
		SIF PALAM:潤滑 >= PALAMLV:4
			PRINT wet 
		PRINTFORML %FSYN("urethra")%...
	ENDIF
ELSE
	;解除時
	IF !TEQUIP:EStimUrethra
		PRINTFORML %PARSE("You turn")% off the control box and %PARSE("start")% to slowly %PARSE("pull")% out the electrode from %CALLNAME:TARGET%'s %FSYN("urethra")%.
		PRINTFORM %TNAME(TARGET,1)%
		;Ａ経験180以上＋淫乱か娼婦
		IF EXP:UExp > 180 && TALENT:淫乱
			PRINTFORM  drools from both %HIS_HER(TARGET)% lips, %HIS_HER(TARGET)% %FSYN("urethra")% still spread wide
		;Ａ経験140以上＋淫乱か娼婦
		ELSEIF EXP:UExp > 140 && TALENT:淫乱
			PRINTFORM  stares at %PARSE("you",5)% with tear-filled eyes as %HE_SHE(TARGET)% shakes %HIS_HER(TARGET)% %FSYN("urethra")%
		;Ａ経験100以上＋淫乱か娼婦
		ELSEIF EXP:UExp > 100 && TALENT:淫乱
			PRINTFORM 's %FSYN("urethra")% doesn't seem content as it protests
		;Ａ経験70以上＋淫乱か娼婦
		ELSEIF EXP:UExp > 70 && TALENT:淫乱
			PRINTFORM 's %FSYN("urethra")% twitches a bit disappointed
		;Ａ経験40以上
		ELSEIF EXP:UExp > 40
			PRINTFORM 's %FSYN("urethra")% twitches, but %HE_SHE(TARGET)% lets out a sigh of relief
		;Ａ経験20以上
		ELSEIF EXP:UExp > 20
			PRINTFORM  blushes at the forced excretion
		;それ以外
		ELSE
			PRINTFORM  winces as %HE_SHE(TARGET)% bears the pain
		ENDIF
		PRINTFORML ...
	ELSE
		PRINTFORM %PARSE("You")% 
		IF HAS_PANTY_MOVEMENT()
			PRINTFORM %PRINT_PANTY_MOVEMENT(TARGET, MASTER)% and %PARSE("press")% the urethral electrode against %HIS_HER(TARGET)% %FSYN("urethra")%
		ELSE
			PRINTFORM %PARSE("press")% the electrode against %CALLNAME:TARGET%'s %FSYN("urethra")%
		ENDIF
		PRINTFORML ...
		PRINTFORM %TNAME(TARGET,1)%
		;Ａ経験180以上＋淫乱か娼婦
		IF EXP:UExp > 180 && TALENT:淫乱
			PRINTFORM 's face has melted, %HIS_HER(TARGET)% %HIP(TARGET)% spreading on its own...
		;Ａ経験140以上＋淫乱か娼婦
		ELSEIF EXP:UExp > 140 && TALENT:淫乱
			PRINTFORM  shakes %HIS_HER(TARGET)% hips, longing for the insertion
		;Ａ経験100以上＋淫乱か娼婦
		ELSEIF EXP:UExp > 100 && TALENT:淫乱
			PRINTFORM  blushes, %HIS_HER(TARGET)% %HIP(TARGET)% quivering in anticipation
		;Ａ経験70以上＋淫乱か娼婦
		ELSEIF EXP:UExp > 70 && TALENT:淫乱
			PRINTFORM 's body quivers as the plug comes close
		;Ａ経験40以上
		ELSEIF EXP:UExp > 40
			PRINTFORM 's face is a mix of excitement and revulsion
		;Ａ経験20以上
		ELSEIF EXP:UExp > 20
			PRINTFORM  blushes profusely as %HE_SHE(TARGET)% lays down
		;それ以外
		ELSE
			PRINTFORM  blushes profusely at the humiliation as %PARSE("you violate",1)% %HIS_HER(TARGET)% dirty hole
		ENDIF
		PRINTL ...
		PRINTFORM %PARSE("You insert")% the urethral electrode in %CALLNAME:TARGET%'s 
		;潤滑LV4以上
		SIF PALAM:潤滑 >= PALAMLV:4
			PRINT wet 
		PRINTFORML %FSYN("urethra")%, before turning on the control box...
	ENDIF
ENDIF


@ADD_CUSTOM_COM_ABLE169
;バイブ実行判定
SIF !TFLAG:100
	RETURN 0

SIF !ITEM:149
	RETURN 0
	
SIF TALENT:MASTER:SexKnow < 5
	RETURN 0	

SIF !nPee
	RETURN 0

SIF !CFLAG:TARGET:うふふ
	RETURN 0

SIF PANTIES_LOCKED(TARGET)
	RETURN 0
RETURN 1