;-------------------------------------------------
;告白する
;日常系コマンド、レベル2
;-------------------------------------------------


;i have a vindetta against sex friend agreement
;it's getting put into the confess menu because fuck you
;- Mr Pops Alot
@COM352_NAS
#DIM proposeType
#DIM 理性判定値
#DIM 欲望判定値
#DIM 酒酔い
#DIM SF_DO
LOCAL = 0
SF_DO = 0

;Custom Code
IF FLAG:甲斐性無 && !FLAG:LUSTMODE
	DRAWLINE
	PRINTFORMW You don't have enough courage to do that.
	RETURN -1
ENDIF
;告白対象確認
{
CALL ASK_M(
@"Confess\@ TALENT:恋人 ? %@" (Already【%GET_TALENTNAME_TR(7,1)%】)"% #\@\@ GETBIT(TFLAG:一日一回, 12) ? %@" (Already tried to confess)"% #\@\@ GETBIT(TFLAG:一日一回, 12) ? %@" (Too many lovers: /{FLAG:追加恋人枠})"% #\@", !TALENT:恋人 && !GETBIT(TFLAG:一日一回, 12) && CAN_MAKE_恋人(),
@"Propose\@ !TALENT:恋人 ? %@" (%CALLNAME:TARGET% needs to be【%GET_TALENTNAME_TR(7,1)%】)"% #\@", TALENT:恋人 && !TALENT:225,
"Sex Friend Agreement", !TALENT:セフレ && !TALENT:愛欲 && !TALENT:恋慕 && !TALENT:恋人,
@"\@ PushDownFilter:TARGET:0 ? Give Consent # Revoke Consent\@\@ !GETBIT(CFLAG:TARGET:既成事実 , 1) ? %@" (%CALLNAME:TARGET% hasn't given you consent for sex)"% #\@", GETBIT(CFLAG:TARGET:既成事実 , 1),
@"\@ GETBIT(nRape,0) ? TSP Rape Configuration # ????? \@", GETBIT(nRape,0),
@"\@ nHypno ? Hypnosis Configuration # ????? \@", nHypno,
@"Ask for a divorce\@ !TALENT:恋人 ? %@" (%CALLNAME:TARGET% needs to be【%GET_TALENTNAME_TR(7,1)%】)"% #\@", TALENT:恋人,
"Get me out of here!", 1
)
}
proposeType = RESULT

;show extra info if you're not dum dum
IF ABL:MASTER:教養 >= UniversalRank("B") && GROUPMATCH(proposeType,0,1)
	PRINTFORML Agreeability Level: {CFLAG:合意判定}
	PRINTFORML Love EXP: {EXP:愛情経験}
	PRINTFORML
	CALL COLORMESSAGE("Prediction: ",C_YELLOW,0)

	SELECTCASE proposeType
	CASE 0
		IF  CFLAG:合意判定 < 350 + FuckReserved(TARGET) * 100
			CALL COLORMESSAGE("Fail! Not enough agreeability rating",C_RED,1)
		ELSEIF TFLAG:192 < 0
			CALL COLORMESSAGE("Fail! Can't confess at this moment",C_RED,1)
		ELSE
			CALL COLORMESSAGE("Pass!",C_GREEN,1)
		ENDIF
	CASE 1
		IF  EXP:愛情経験 > 250 + FuckReserved(TARGET) * 100
			CALL COLORMESSAGE("Pass!",C_GREEN,1)		
		ELSE
			CALL COLORMESSAGE("Fail! Not enough love exp",C_RED,1)
		ENDIF
	CASE 2
		$SF_Prop
		欲望判定値 = MIN(ABL:TARGET:欲望, 10) * 50
		CALL SF_DESIRE_MOD(@"%ABL_TR(GETNUM(ABL,  "欲望"))% {ABL:TARGET:欲望}", 欲望判定値)

		欲望判定値 += MIN(ABL:TARGET:従順, 10) * 20
		CALL SF_DESIRE_MOD(@"%ABL_TR(GETNUM(ABL,  "従順"))% {ABL:TARGET:従順}", 欲望判定値)

		欲望判定値 += MIN(EXP:TARGET:絶頂経験, 5000) / 10
		CALL SF_DESIRE_MOD(@"%EXP_TR(GETNUM(EXP,  "絶頂経験"))% {EXP:TARGET:絶頂経験}", 欲望判定値)

		欲望判定値 += GETPALAMLV(PALAM:TARGET:欲情,15) * (30 + TALENT:TARGET:快感応答 * 10)
		CALL SF_DESIRE_MOD(@"%PALAM_TR("欲情")%", 欲望判定値)

		欲望判定値 += MAX(MARK:TARGET:快楽刻印, MARK:TARGET:時姦刻印) * 200
		CALL SF_DESIRE_MOD(@"%MARK_TR(GETNUM(MARK,  "快楽刻印"))%/%MARK_TR(GETNUM(MARK,  "時姦刻印"))%", 欲望判定値)

		IF TCVAR:TARGET:発情 || TCVAR:TARGET:媚薬
			欲望判定値 += 300
			CALL SF_DESIRE_MOD(@"In Heat/Aphrodisiac", 欲望判定値)
		ENDIF
		;WAIT

		理性判定値 = 250 + BASE:TARGET:理性 / 2
		CALL SF_REASON_MOD(@"Reason：{BASE:TARGET:理性}", 理性判定値)

		;酒酔い
		酒酔い = BASE:酒気 * 100 / MAXBASE:酒気
		IF 酒酔い
			SIF 酒酔い > 70
				酒酔い *= 2
			理性判定値 -= 酒酔い
			CALL SF_REASON_MOD(@"Drunkenness", 理性判定値)
		ENDIF
		IF !CFLAG:TARGET:既成事実 & 合意_うふふ
			理性判定値 += 300
			CALL SF_REASON_MOD(@"【Lack of Consent】", 理性判定値)
		ENDIF

		IF TALENT:TARGET:SexKnow == -1 || TALENT:TARGET:SexKnow >= 5
			理性判定値 -= 500
			CALL SF_REASON_MOD(@"【%TALENT_TR(GETNUM(TALENT,  "SexKnow"))%】", 理性判定値)
		ENDIF
		IF TALENT:TARGET:プライド
			理性判定値 += TALENT:TARGET:プライド * 150
			CALL SF_REASON_MOD(@"【%TALENT_TR(GETNUM(TALENT,  "プライド"))%】", 理性判定値)
		ENDIF
		IF TALENT:TARGET:貞操 > 0
			理性判定値 += 200
			CALL SF_REASON_MOD(@"【%GET_TALENTNAME_TR(GETNUM(TALENT,  "貞操"), 1)%】", 理性判定値)
		ENDIF
		IF TALENT:TARGET:貞操 < 0
				理性判定値 -= 200
			CALL SF_REASON_MOD(@"【%GET_TALENTNAME_TR(GETNUM(TALENT,  "貞操"), -1)%】", 理性判定値)
		ENDIF
		IF MARK:TARGET:反発刻印
				理性判定値 += MARK:TARGET:反発刻印 * 100
			CALL SF_REASON_MOD(@"【%MARK_TR(GETNUM(MARK,  "反発刻印"))%】", 理性判定値)
		ENDIF
		IF TALENT:TARGET:機嫌 < 0
			理性判定値 += 300
			CALL SF_REASON_MOD(@"【%GET_TALENTNAME_TR(GETNUM(TALENT,  "機嫌"), -1)%】", 理性判定値)
		ENDIF
		IF TALENT:TARGET:処女
			理性判定値 += 300
			CALL SF_REASON_MOD(@"【%TALENT_TR(GETNUM(TALENT,  "処女"))%】", 理性判定値)
		ENDIF
		IF TALENT:TARGET:倒錯的
			理性判定値 -= 200
			CALL SF_REASON_MOD(@"【%TALENT_TR(GETNUM(TALENT,  "倒錯的"))%】", 理性判定値)
		ENDIF
		IF TALENT:MASTER:謎の魅力
			理性判定値 -= 150
			CALL SF_REASON_MOD(@"Your【%TALENT_TR(GETNUM(TALENT,  "謎の魅力"))%】", 理性判定値)
		ENDIF
		IF TALENT:MASTER:魅力
			理性判定値 -= 150
			CALL SF_REASON_MOD(@"Your【%TALENT_TR(GETNUM(TALENT,  "魅力"))%】", 理性判定値)
		ENDIF
		IF FuckReserved(TARGET)
			理性判定値 += 200
			CALL SF_REASON_MOD(@"【%TALENT_TR(GETNUM(TALENT,  "一線越えない"))%】", 理性判定値)
		ELSEIF HasBionicPart(MASTER,67) && TALENT:TARGET:一線越えない
			理性判定値 -= 200
			CALL SF_REASON_MOD(@"Your【Prude Harmonizer】", 理性判定値)
		ENDIF
		IF OTOKOGIRAI(TARGET)
			理性判定値 += 300
			CALL SF_REASON_MOD(@"Sexual Orientation", 理性判定値)
		ENDIF
		IF CFLAG:TARGET:体目当て
			理性判定値 -= 400
			CALL SF_REASON_MOD(@"The Millennium Stone of Mirada that Monopolizes Lust starts shining...!", 理性判定値)
		ENDIF
		IF HasBionicPart(MASTER,82)
			理性判定値 = 理性判定値 / 2
			CALL SF_REASON_MOD(@"The Trojan Soul activates...!", 理性判定値)
		ENDIF


		SIF SF_DO
			GOTO SF_RESULT
	ENDSELECT
ENDIF

SELECTCASE proposeType
CASE 0
	;harem custom code
	PRINTFORML Confessing to %CALLNAME:TARGET%.\@ FLAG:追加恋人枠 >= 150 ? %@" You can have as many lovers as you want."% # %@" You can only have "%%PRINT_PLUR("lover", FLAG:追加恋人枠 + 1)%! )\@
	PRINTL Confirm?
	CALL ASK_YN("No","Confess")
	IF !RESULT
		TFLAG:192 = 0
		TFLAG:193 = 0
		RETURN -1
	ENDIF
	$CONFESS

	;パラメータにより成功基礎値判定
	SETBIT TFLAG:一日一回, 12

	SELECTCASE TFLAG:192
		;通常
		CASE 0
		IF TALENT:恋人
			IF  EXP:愛情経験 > 250 + FuckReserved(TARGET) * 100
				TFLAG:193 = 1
			ELSE
				TFLAG:193 = 0
			ENDIF
		ELSE
			IF  CFLAG:合意判定 > 350 + FuckReserved(TARGET) * 100
				TFLAG:193 = 1
			ELSE
				TFLAG:193 = 0
			ENDIF
		ENDIF
		;強制成功or大成功
		CASE 1
			TFLAG:193 = 1
		;強制失敗
		CASE -1
			TFLAG:193 = 0
		;コマンド終了
		CASE -2
			TFLAG:193 = 0
			RETURN 0
	ENDSELECT

	IF TFLAG:193 == 1
		SOURCE:歓楽 = 1000
		SOURCE:受動 = 500 + 100 * ABL:従順
		SOURCE:征服 = 500 + 100 * ABL:サドっ気
	ENDIF
CASE 1
	PRINTFORML Proposing to %CALLNAME:TARGET%. %PARSE("You")% can only have {FLAG:追加恋人枠 + 1} \@FLAG:追加恋人枠?wives#wife\@.
	PRINTL Confirm?
	CALL ASK_YN("No","Propose")
	IF !RESULT
		RETURN -1
	ENDIF
	GOTO CONFESS
CASE 2
	PRINTFORML Offering %CALLNAME:TARGET% to become your Sex Friend.
	PRINTFORML You can only attempt it once a day.
	PRINTW Confirm?
	CALL ASK_YN("Cancel","Make proposition")
	IF !RESULT
		RETURN -1
	ENDIF
	RESULT = 0
	SF_DO = 1
	GOTO SF_Prop

	$SF_RESULT
	TRYCALLFORM KOJO_SF_CONTRACT_{TARGET}("補正")
		IF RESULT
			理性判定値 = MAX(理性判定値 + RESULT, 0)
			CALL SHOW_REASONWALL(理性判定値)
		ENDIF
	PRINTFORMW 
		IF 欲望判定値 > 理性判定値
			TALENT:TARGET:セフレ = 1
			TRYCALLFORM KOJO_SF_CONTRACT_{TARGET}("成功")
			CALL COLORMESSAGE(@"%CALLNAME:TARGET% ends up agreeing to become your Sex Friend...",C_PINK,1,1)
			CALL ASK_DIARY(@"Offered %CALLNAME:TARGET% a sex friend agreement and got %HIM_HER(TARGET)% to accept.")
		ELSE
			TRYCALLFORM KOJO_SF_CONTRACT_{TARGET}("失敗")
			PRINTFORMW You get turned down...
			SOURCE:反感 += 1000
		ENDIF
CASE 3
	PushDownFilter:TARGET:0 = !PushDownFilter:TARGET:0
	PRINTFORMDW Random push downs from %CALLNAME:TARGET% are now \@ PushDownFilter:TARGET:0 ? prohibited # allowed\@. 
	RETURN -1
CASE 4
	{
		CALL ASK_M
		(
			@"\@ PushDownFilter:TARGET:2 ? Revoke TSP Rape Consent # Give TSP Rape Consent \@\@ !GETBIT(CFLAG:TARGET:既成事実 , 1) ? %@" (%CALLNAME:TARGET% hasn't given you consent for sex)"% #\@", GETBIT(CFLAG:TARGET:既成事実 , 1) && MAXBASE:MASTER:TSP,
			@"\@ PushDownFilter:TARGET:1 ? Revoke Reverse TSP Rape Consent # Give Reverse TSP Rape Consent \@\@ !GETBIT(CFLAG:TARGET:既成事実 , 1) ? %@" (%CALLNAME:TARGET% hasn't given you consent for sex)"% #\@\@ !MAXBASE:TARGET:TSP ? %@" (%CALLNAME:TARGET% cannot stop time)"% #\@", MAXBASE:TARGET:TSP && GETBIT(CFLAG:TARGET:既成事実 , 1),
			"Leave", 1
		)
	}
	SELECTCASE RESULT
	CASE 0
	PushDownFilter:TARGET:1 = !PushDownFilter:TARGET:1
	PRINTFORMDW TSP violations to %CALLNAME:TARGET% are now \@ PushDownFilter:TARGET:1 ? allowed # prohibited\@. 
	CASE 1
	PushDownFilter:TARGET:1 = !PushDownFilter:TARGET:1
	PRINTFORMDW TSP violations from %CALLNAME:TARGET% are now \@ PushDownFilter:TARGET:1 ? allowed # prohibited\@. 
	ENDSELECT
	RETURN -1
CASE 5
	{
		CALL ASK_M
		(
			@"\@ PushDownFilter:TARGET:3 ? Revoke Hypnosis Consent # Give Hypnosis Consent \@\@ !GETBIT(CFLAG:TARGET:既成事実 , 1) ? %@" (%CALLNAME:TARGET% hasn't given you consent for sex)"% #\@", GETBIT(CFLAG:TARGET:既成事実 , 1) && GETBIT(TALENT:MASTER:ESP,19),
			@"\@ PushDownFilter:TARGET:4 ? Revoke Being Hypnotized Consent # Give Being Hypnotized Consent \@\@ !GETBIT(CFLAG:TARGET:既成事実 , 1) ? %@" (%CALLNAME:TARGET% hasn't given you consent for sex)"% #\@\@ !GETBIT(TALENT:TARGET:ESP,19) ? %@" (%CALLNAME:TARGET% cannot use hypnosis)"% #\@", GETBIT(TALENT:TARGET:ESP,19) && GETBIT(CFLAG:TARGET:既成事実 , 1),
			"Leave", 1
		)
	}
	SELECTCASE RESULT
	CASE 0
	PushDownFilter:TARGET:3 = !PushDownFilter:TARGET:3
	PRINTFORMDW Hypnosis to %CALLNAME:TARGET% is now \@ PushDownFilter:TARGET:3 ? allowed # prohibited\@. 
	CASE 1
	PushDownFilter:TARGET:4 = !PushDownFilter:TARGET:4
	PRINTFORMDW Hypnosis from %CALLNAME:TARGET% is now \@ PushDownFilter:TARGET:4 ? allowed # prohibited\@. 
	ENDSELECT
	TFLAG:192 = 0
	TFLAG:193 = 0
	RETURN -1
CASE 6
	SETCOLOR C_RED
	PRINTFORML Are you sure you want to have a divorce with %CALLNAME:TARGET%?
	PRINTFORML You will lose【%GET_TALENTNAME_TR(7,1,TARGET)%】,【%GET_TALENTNAME_TR(225,1,TARGET)%】, and will instead gain 【%GET_TALENTNAME_TR(225,TALENT:225 ? -1 # -2,TARGET)%】
	RESETCOLOR
	CALL ASK_YN()
	SELECTCASE RESULT
	CASE 0
		SETCOLOR C_RED
		PRINTFORML Are you really sure?
		RESETCOLOR
		CALL ASK_YN()
		SELECTCASE RESULT
		CASE 0
		PRINTFORMW You ask %CALLNAME:TARGET% for a divorce.
		TALENT:7 = 0
		TALENT:225 = TALENT:225 ? -1 # -2
		PRINTFORMW %CALLNAME:TARGET% is now your【%GET_TALENTNAME_TR(225,TALENT:225 ? -2 # -1,TARGET)%】...
		ENDSELECT
	ENDSELECT
CASEELSE
	TFLAG:192 = 0
	TFLAG:193 = 0
	RETURN -1
ENDSELECT

;bug fix, to display description
TIME += 10
RETURN 1

;-------------------------------------------------
;実行判定
;-------------------------------------------------
@COM_ABLE352_NAS
;停止中は不可
SIF FLAG:70 && !MAXBASE:TARGET:TSP
	RETURN 0
;妄想中は告白できない
SIF TFLAG:妄想中 == 1
	RETURN 0
;告白する実行判定
SIF !TFLAG:100
	RETURN 0
;一括管理
SIF GLOBAL_COMABLE(352)
	RETURN RESULT
;二人きりでないとだめ
SIF GET_TARGETNUM() > 1
	RETURN 0
; ;馴れ合い強度2
; NAS: Useless limitation
; SIF TCVAR:MASTER:馴れ合い < 2
; 	RETURN 0
;;すでに恋人の場合はだめ
;SIF TALENT:恋人 && TALENT:225
;	RETURN 0
;NAS: Removed
; SIF GETBIT(TFLAG:一日一回, 12)
; 	RETURN 0
; ;恋人の数が上限に達していてはだめ
; SIF !CAN_MAKE_恋人()
; 	RETURN 0
SIF TALENT:TARGET:行きずり
	RETURN 0
;睡眠中
SIF CFLAG:睡眠
	RETURN 0
;添い寝中
SIF CFLAG:添い寝中
	RETURN 0
;気力0
SIF BASE:MASTER:気力 <= 0
	RETURN 0
RETURN 1

@SF_REASON_MOD(ARGS, ARG)
CALL COLORMESSAGE(@"%ARGS%",C_YELLOW,1,1)
CALL SHOW_REASONWALL(ARG)

@SF_DESIRE_MOD(ARGS, ARG)
CALL COLORMESSAGE(@"%ARGS%",C_PINK,1,1)
CALL SHOW_LOVEPOWER(ARG, 100, "Carnality")