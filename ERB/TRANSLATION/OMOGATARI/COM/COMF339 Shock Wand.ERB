;-------------------------------------------------
;Shock Wand
;Externally gives pleasure without being sexually conspicuous
;-------------------------------------------------
@ADD_CUSTOM_COM339
;Custom Code
IF FLAG:甲斐性無 && !FLAG:LUSTMODE
	DRAWLINE
	PRINTFORMW %PARSE("You don't")% have enough courage to do that.
	RETURN -1
ENDIF

IF !ITEM:EStimBox
	DRAWLINE
	PRINTFORMW %PARSE("You don't")% have a way to power this.
	FONTITALIC
	PRINTFORMW Buy a %ITEMNAME_TR(145)% to use this.
	FONTREGULAR
	RETURN -1
ENDIF

SIF ISSKIP()
	RETURN -1

PRINTFORMDL Where are you going to use the wand?
CALL ASK_M(@"%CAPITALIZE(PRINT_CLIT(TARGET))%",1,"Vagina",1,"Anus",1,"Breasts",1,"Mouth/Head",1,"Urethra",1,"Get me out of here!",1)

TFLAG:144 = RESULT
;行為者
TCVAR:116 = PLAYER
;奉仕快楽フラグ
TCVAR:PLAYER:108 = 1

CSTR:TARGET:10 += @"{PLAYER}/"
;TCVAR:2 射精箇所フラグ（-1=コンドーム 1=膣内 2=アナル 3=手淫 4=口淫 5=パイズリ 6=素股 7=足コキ 8=体表 9=アナル奉仕 10=道具 11=触手 12=Ａ責め 13=Ｖ責め）
TCVAR:TARGET:2 = 23
;強度
TCVAR:TARGET:4 = 900


SELECTCASE TFLAG:144
CASE 0
SOURCE:快Ｃ += 200
SOURCE:欲情 += 1000
SOURCE:屈従 += 2000
SOURCE:恭順 += 500
SOURCE:反感 += 1000
SOURCE:苦痛 += 2000
CASE 1
TFLAG:2 = 1
SOURCE:快Ｖ += 1000
SOURCE:欲情 += 1000
SOURCE:屈従 += 2000
SOURCE:恭順 += 500
SOURCE:反感 += 1000
SOURCE:苦痛 += 2000
CASE 2
SOURCE:快Ａ += 100
SOURCE:欲情 += 1000
SOURCE:屈従 += 2000
SOURCE:恭順 += 500
SOURCE:反感 += 1000
SOURCE:苦痛 += 2000
CASE 3
SOURCE:快Ｂ += 100
SOURCE:欲情 += 1000
SOURCE:屈従 += 2000
SOURCE:恭順 += 500
SOURCE:反感 += 1000
SOURCE:苦痛 += 1000
CASE 4
LOCAL = 120+(10*ABL:MASTER:教養)+(30*ABL:MASTER:話術技能)-(10*ABL:教養)-(30*ABL:話術技能)+(10*ABL:HypnoAddict)
BASE:Hypno = LIMIT(BASE:Hypno+LOCAL, 0, MAXBASE:Hypno)

SOURCE:快Ｍ += 100
SOURCE:欲情 += 1000
SOURCE:屈従 += 2000
SOURCE:恭順 += 500
SOURCE:反感 += 1000
SOURCE:苦痛 += 1000
EXP:HypnoExp ++
OrgasmDenyExp:TARGET:0 --
CASE 5
SOURCE:UPleas += 600
SOURCE:欲情 += 1000
SOURCE:屈従 += 2000
SOURCE:恭順 += 500
SOURCE:反感 += 1000
SOURCE:苦痛 += 3000
CASEELSE
RETURN -1
ENDSELECT

;好感度をみる
SELECTCASE CFLAG:2
	CASE 1000 TO 3000
		SOURCE:情愛 += CFLAG:2 / 100
		SOURCE:反感 -= (ABL:親密 * 10)
	CASE 3001 TO 5000
		SOURCE:情愛 += 100 + (CFLAG:2 - 500) / 5
		SOURCE:歓楽 += 100 + (CFLAG:2 - 500) / 4
		SOURCE:欲情 += (ABL:親密 * 10)
		SOURCE:露出 += (ABL:親密 * 15)
		SOURCE:反感 -= (ABL:親密 * 15)
	CASE IS > 5000
		SOURCE:情愛 += 500 + (CFLAG:2 - 5000) / 7
		SOURCE:歓楽 += 500 + (CFLAG:2 - 5000) / 4
		IF !TALENT:恋慕 && !TALENT:セフレ ;com stuff custom code
			SOURCE:欲情 += (ABL:親密 * 15)
			SOURCE:露出 += (ABL:親密 * 20)
			SOURCE:反感 -= (ABL:親密 * 25)
		ELSE
			SOURCE:欲情 += (ABL:親密 * 20)
			SOURCE:露出 += (ABL:親密 * 30)
			SOURCE:反感 -= (ABL:親密 * 40)
		ENDIF
ENDSELECT
SIF SOURCE:反感 < 0
	SOURCE:反感 = 0
SIF SOURCE:情愛 < 0
	SOURCE:情愛 = 0

	SOURCE:受動 = 100 + 150 * ABL:従順
	SOURCE:征服 = 100 + 150 * ABL:サドっ気

;二人きりだと効果アップ、そうでないと露出以外ダウン=============
FOR LOCAL,1,CHARANUM
	SIF CFLAG:LOCAL:同室
		LOCAL:2 ++
NEXT
IF LOCAL:2 == 1
	TIMES SOURCE:欲情 , 1.20
	TIMES SOURCE:露出 , 0.80
	LOCAL:3 = 1
ELSE
	TIMES SOURCE:欲情 , 0.50
	TIMES SOURCE:露出 , 2.00
	TIMES SOURCE:反感 , 1.50
ENDIF
;ムードの影響=====================================================
IF BASE:ムード == MAXBASE:ムード
	TIMES SOURCE:欲情 , 1.20
	TIMES SOURCE:露出 , 1.20
	TIMES SOURCE:反感 , 0.50
ELSEIF BASE:ムード < 600 && BASE:ムード
	TIMES SOURCE:欲情 , 1.00
	TIMES SOURCE:露出 , 1.00
	TIMES SOURCE:反感 , 1.00
ELSEIF BASE:ムード <= 200
	TIMES SOURCE:欲情 , 0.80
	TIMES SOURCE:露出 , 0.80
	TIMES SOURCE:反感 , 1.50
ENDIF

IF CHK_DATENOW(CFLAG:TARGET:デート中)
	EXP:MASTER:デート経験 ++
	EXP:TARGET:デート経験 ++
ENDIF	

TIME += 1
RETURN 1

@ADD_CUSTOM_MESSAGE_COM339
#DIM DYNAMIC HasPiss
#DIM DYNAMIC HasScat

SELECTCASE PeeHold:TARGET:2
CASE IS >= 6
CASE IS >= 2
	SIF PeeBarStage(TARGET) >= 5
		HasPiss = 1
CASE 0, 1
	IF PeeBarStage(TARGET) >= 5
		HasPiss = 2
	ELSEIF PeeBarStage(TARGET) >= 4
		HasPiss = 1
	ENDIF
CASE -1
	IF PeeBarStage(TARGET) >= 4
		HasPiss = 2
	ELSEIF PeeBarStage(TARGET) >= 3
		HasPiss = 1
	ENDIF
CASE IS <= -2
	SIF PeeBarStage(TARGET) >= 2
		HasPiss = 2
ENDSELECT

SELECTCASE TFLAG:144
CASE 0
PRINTFORMDL You give %CALLNAME:TARGET%'s %PRINT_CLIT(TARGET)% a little shock.
PRINTFORMDL %HIS_HER(TARGET,1)+" "+PRINT_CLIT(TARGET)% throbs with pleasure. 
CASE 1
PRINTFORMDL You %PRINT_PANTY_MOVEMENT(TARGET, MASTER)% and gently place the wand inside her vagina. 
PRINTFORMDL %HIS_HER(TARGET,1)+" "+FSYN("vagina")% shakes as electricity runs through %HIS_HER(TARGET)% pussy. 
CASE 2
PRINTFORMDL You %PRINT_PANTY_MOVEMENT(TARGET, MASTER)% and gently place the wand on her rectum. 
PRINTFORMDL %HIS_HER(TARGET,1)+" "+FSYN("anus")% shakes as electricity runs through %HIS_HER(TARGET)% intestines. 
CASE 3
PRINTFORMDL You give %CALLNAME:TARGET%'s %FSYN("chest")% a little poke with your wand
PRINTFORMDL %HIS_HER(TARGET,1)+" nipples"% shake as electricity runs through %HIS_HER(TARGET)% breasts. 
CASE 4
PRINTFORMDL You give %CALLNAME:TARGET% a taste of electricity.
PRINTFORMDL %CALLNAME:TARGET% falls back, but not before the energy makes %HIM_HER(TARGET)% a bit dizzy.
CASE 5
PRINTFORMDL You aim the wand at %CALLNAME:TARGET%'s bladder, redirecting the electricity to %HIS_HER(TARGET)% urethra.
PRINTFORMDL %CALLNAME:TARGET% blushes as %PARSE("you torture")% %HIS_HER(TARGET)% dirty hole.
SIF PeeBarStage(TARGET) >= 3
	HasPiss = 2
ENDSELECT

SELECTCASE HasPiss
CASE 2
	CALL UFUFU_OMORASHI(TARGET,HasPiss)
CASE 1
	BASE:TARGET:4 -= 75
	CALLF MessUndies(TARGET, "liquid", 0, 75)
	PRINTFORML %URINE(TARGET,1)% trickles from %CALLNAME:TARGET% urethra\@ DiapeCharges:TARGET:0 <= 0 ? , forming a puddle # , causing the padding to swell\@...
	PRINTFORML A considerable stain forms on %CALLNAME:TARGET%'s %PeeProtectType(TARGET)%
ENDSELECT



@ADD_CUSTOM_COM_ABLE339
SIF GLOBAL_COMABLE(339)
	RETURN RESULT
SIF !TFLAG:100
	RETURN 0
SIF CFLAG:睡眠
	RETURN 0    
SIF TARGET == MASTER ; doesn't make sense to kick yourself out
    RETURN 0
SIF !ITEM:ShockWand
	RETURN 0
RETURN 1