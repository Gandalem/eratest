; Modular ERB: OMOGATARI/ITEM/Armory/300 Firearms/500 Shotguns.ERB

@Weapon500(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "Attorney")
		ELSE
			CALLF MAKE_STR(V_NAME, "pump-action shotgun")
		ENDIF
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "Kappa Armory Pump-Action Shotgun「Attorney」")
		ELSE
			CALLF MAKE_STR(V_NAME, "pump-action shotgun")
		ENDIF
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "YKMA 변호사는 진정한 브라우튼 S1000 시스템을 기반으로 요괴 산 카파 무기고에서 제조한 펌프 액션 산탄총이다. 이는 대중에게 스포츠 사격, 사냥 및 자위 목적으로 널리 사용되며 환상의 세계 내 군 조직에 의해 사용된다.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Tech Level"
		LOCAL = 6
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grade"
		LOCAL = 2
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Price"
		LOCAL = 204000
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Magazine Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Body Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Stock Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 80)
	CASE "WeaponBarrelBody/Default"
		CALLF MAKE_INT(V_NAME, 40)
	CASE "WeaponStock/Default"
		CALLF MAKE_INT(V_NAME, 30)
	CASE "WeaponMag/Default"
		CALLF MAKE_INT(V_NAME, 11)
	CASE "Two-Handed"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "Gunsmith/")
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Firearm")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Shotgun")
	CASE "Platform"
		CALLF MAKE_STR(V_NAME, "R870/")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, "kappa-arm/closed-bolt/")
		;旧装備処理との照会用、Weaponの1
ENDSELECT

@EXIST_Weapon501
#LOCALSIZE 1
#LOCALSSIZE 1
;オブジェクト本体

@Weapon501(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "Sweet Sixteen")
		ELSE
			CALLF MAKE_STR(V_NAME, "volley rifle")
		ENDIF
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "Rika's Arms Volley Rifle「Sweet Sixteen」")
		ELSE
			CALLF MAKE_STR(V_NAME, "volley rifle")
		ENDIF
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "RaD에 의해 만들어진 특수 산탄총. 엄밀히 말해 이 고유한 무기는 여러 개의 총열에서 개별 투사체를 동시에 발사하여 광역 공격이 활성화되기 때문에 진정한 산탄총은 아니다.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Grade"
		LOCAL = 2
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tech Level"
		LOCAL = 7
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "Gunsmith/")
	CASE "Price"
		LOCAL = 210000
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Foregrip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 3
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Stock Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 135)
	CASE "WeaponBarrelBody/Default"
		CALLF MAKE_INT(V_NAME, 115)
	CASE "WeaponStock/Default"
		CALLF MAKE_INT(V_NAME, 8)
	CASE "WeaponGrip/Default"
		CALLF MAKE_INT(V_NAME, 3)
	CASE "Two-Handed"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Firearm")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Shotgun")
	CASE "Fire Sound"
		CALLF MAKE_STR(V_NAME, "firearms/S1000.m4a")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"rikaarms/military/modern/native/shotgun/")
		;旧装備処理との照会用、Weaponの1
ENDSELECT

@Weapon502(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "GH512")
		ELSE
			CALLF MAKE_STR(V_NAME, "semi-auto shotgun")
		ENDIF
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "Gabe Hubert Semi-Automatic Shotgun「GH512」")
		ELSE
			CALLF MAKE_STR(V_NAME, "semi-automatic shotgun")
		ENDIF
	CASE "描写"
		CALLF MAKE_STR(V_NAME,"현대의 마에스트로 스톰 샷건 시리즈의 전신으로서, 모델 512(별칭 GH512)는 마에스트로 아미가 설계하고 독일 출신의 게이브 휴버트가 조립한 세미 오토매틱 샷건이다. 이 총기는 처음 서독의 엘리트 GSG-9 대테러 경찰 부대에서 사용하기 위해 제조되었다. GH 모델 512 샷건에는 '맨 커터' 조절 차징이 전면에 장착되어 있었는데, 이는 탄환을 길쭉한 타원형 패턴으로 발사하여 사람을 반으로 가르거나 (수직으로 돌렸을 때) 모든 총알을 표적의 주 몸체에 집중시키는 역할을 했다. ")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Tech Level"
		LOCAL = 7
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grade"
		LOCAL = 3
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Price"
		LOCAL = 204000
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Magazine Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Body Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Stock Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 56)
	CASE "WeaponBarrelBody/Default"
		CALLF MAKE_INT(V_NAME, 40)
	CASE "WeaponStock/Default"
		CALLF MAKE_INT(V_NAME, 30)
	CASE "WeaponMag/Default"
		CALLF MAKE_INT(V_NAME, 12)
	CASE "Two-Handed"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "BlowbackOperation/")
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Firearm")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Shotgun")
	CASE "Platform"
		CALLF MAKE_STR(V_NAME, "HK512/")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, "gabe-hubert/closed-bolt//civilian/military/cold war/modern/western/shotgun/")
		;旧装備処理との照会用、Weaponの1
ENDSELECT

@Weapon503(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "VS-23")
		ELSE
			CALLF MAKE_STR(V_NAME, "high-bore shotgun")
		ENDIF
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "Klimovsk High-Bore Shotgun「VS-23」")
		ELSE
			CALLF MAKE_STR(V_NAME, "high-bore shotgun")
		ENDIF
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "The VS-23 (Special Rifle - 23mm) is a Russian shotgun, although because it uses a rifled barrel it is officially designated by the Russian military as a rifle. VS stands for Vintovka Spetsialniy, \"Special Rifle\". It is renowned for its large caliber, firing a 23 mm round, equating to 6.27 gauge using the British and American standards of shotgun gauges and approximately 4 gauge using the current European standards (based on the metric CIP tables), making it the largest-bore shotgun in use today.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Grade"
		LOCAL = 3
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tech Level"
		LOCAL = 8
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "PrecisionRifling/")
	CASE "Price"
		LOCAL = 3500000
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Foregrip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Magazine Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 3
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Stock Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 136)
	CASE "WeaponBarrelBody/Default"
		CALLF MAKE_INT(V_NAME, 116)
	CASE "WeaponStock/Default"
		CALLF MAKE_INT(V_NAME, 67)
	CASE "WeaponMag/Default"
		CALLF MAKE_INT(V_NAME, 150)
	CASE "Two-Handed"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Firearm")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Shotgun")
	CASE "Platform"
		CALLF MAKE_STR(V_NAME, "ks23/")
	CASE "Fire Sound"
		CALLF MAKE_STR(V_NAME, "firearms/S1000.m4a")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"klimovsk/closed-bolt/civilian/military/soviet/modern/shotgun")
		;旧装備処理との照会用、Weaponの1
ENDSELECT
;オブジェクト本体

@Weapon504(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "Raiken")
		ELSE
			CALLF MAKE_STR(V_NAME, "shotgun musket")
		ENDIF
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "Akatsuki Shotgun Musket「Raiken」")
		ELSE
			CALLF MAKE_STR(V_NAME, "shotgun musket")
		ENDIF
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "The Raiken (Japanese for \"Thunder Pipe\") is a firearm with a short, large caliber barrel which is flared at the muzzle and frequently throughout the entire bore, and used with shot and other projectiles of relevant quantity or caliber. The Raiken is commonly considered to be an early predecessor of the modern shotgun, with similar military and defensive use. It was effective only at short range, lacking accuracy at long distances. A shotgun musket in handgun form was called a dragon, and it is from this that the term dragoon evolved.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Tech Level"
		LOCAL = 4
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Stock Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "Gunpowder/")
	CASE "Grade"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Price"
		LOCAL = 25000
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 137)
	CASE "WeaponBarrelBody/Default"
		CALLF MAKE_INT(V_NAME, 117)
	CASE "WeaponStock/Default"
		CALLF MAKE_INT(V_NAME, 63)
	CASE "Two-Handed"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Firearm")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Shotgun")
	CASE "Platform"
		CALLF MAKE_STR(V_NAME, "tanegashima/")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"akatsuki/civilian/military/medieval/shotgun")
		;旧装備処理との照会用、Weaponの1
ENDSELECT
;SKK-83

@Weapon505(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "Lawyer")
		ELSE
			CALLF MAKE_STR(V_NAME, "auto shotgun")
		ENDIF
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "Kappa Armory Automatic Shotgun「Lawyer」")
		ELSE
			CALLF MAKE_STR(V_NAME, "automatic shotgun")
		ENDIF
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "폴딩 스톡이 달린 완전 자동 12게이지 산탄총. Renov Karbarga-83 (SKK-83) 반자동 산탄총의 개조 복제품이다. 근접전 중 제압 사격과 빠른 방 정리용으로 의도되었다. 짧은 430mm 총열을 특징으로 한다.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Grade"
		LOCAL = 4
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tech Level"
		LOCAL = 8
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "GasOperation/")
	CASE "Price"
		LOCAL = 4461310
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Foregrip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Magazine Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 3
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Stock Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 138)
	CASE "WeaponBarrelBody/Default"
		CALLF MAKE_INT(V_NAME, 118)
	CASE "WeaponStock/Default"
		CALLF MAKE_INT(V_NAME, 17)
	CASE "WeaponGrip/Default"
		CALLF MAKE_INT(V_NAME, 10)
	CASE "WeaponMag/Default"
		CALLF MAKE_INT(V_NAME, 151)
	CASE "Two-Handed"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Firearm")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Shotgun")
	CASE "Platform"
		CALLF MAKE_STR(V_NAME, "AK/")
	CASE "Fire Sound"
		CALLF MAKE_STR(V_NAME, "firearms/Kabarga.m4a")
	CASE "Image"
		CALLF MAKE_STR(V_NAME, "OMOGATARI/Weapons/Kabarga83.png")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"kappa-arm/military/cold war/modern/soviet/shotgun")
		;旧装備処理との照会用、Weaponの1
ENDSELECT

@Weapon506(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "OSP-81")
		ELSE
			CALLF MAKE_STR(V_NAME, "flare gun")
		ENDIF
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "Nizhny Novgorod Illumination and Signal Pistol「OSP-81」")
		ELSE
			CALLF MAKE_STR(V_NAME, "flare gun")
		ENDIF
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "The OSP-81 (\"Signal pistol model 1981\") is a Soviet/Russian flare gun, developed in 1980 and adopted in 1981, produced by the Nizhny Novgorod Arms Plant. Created as a replacement for the SP-44, it actually represents its more compact version. It is also worth noting that the SP-81 was one of the few flare guns that had a combat version. In 1994, by special permission of the MAI, for truckers transporting especially valuable cargo, a version with an elongated heavy barrel, adapted for VS-23 charges, was released.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Tech Level"
		LOCAL = 6
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grade"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Price"
		LOCAL = 154000
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Body Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 102)
	CASE "WeaponBarrelBody/Default"
		CALLF MAKE_INT(V_NAME, 84)
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "Gunsmith/")
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Firearm")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Shotgun")
	CASE "Platform"
		CALLF MAKE_STR(V_NAME, "sp81/")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"nnoz/civilian/world-war/cold war/modern/western/shotgun/")
		;旧装備処理との照会用、Weaponの1
ENDSELECT

@Weapon507(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "Youjyuu-S")
		ELSE
			CALLF MAKE_STR(V_NAME, "recharger shotgun")
		ENDIF
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "Keiga Youjutsu Series Shotgun「Youjyuu-S」")
		ELSE
			CALLF MAKE_STR(V_NAME, "recharger shotgun")
		ENDIF
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "유쥬우-S는 마에스트로 스톰샷건 같은 무기에 해당하는 유유술이다. 발사하기 쉽고 재장전이 필요 없다. 또한 사용자가 유유술 에센스를 가지고 있어야 사용할 수 있다.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Grade"
		LOCAL = 4
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Price"
		LOCAL = 770000
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Foregrip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 3
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Stock Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "MP Cost"
		LOCAL = 5
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tech Level"
		LOCAL = 8
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Damage Type"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 139)
	CASE "WeaponGrip/Default"
		CALLF MAKE_INT(V_NAME, 4)
	CASE "WeaponStock/Default"
		CALLF MAKE_INT(V_NAME, 4)
	CASE "WeaponBarrelBody/Default"
		CALLF MAKE_INT(V_NAME, 119)
	CASE "WeaponSight/Default"
		CALLF MAKE_INT(V_NAME, 15)
	CASE "Two-Handed"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Anti-Theft Strength"
		;Strength of the anti-theft system in Knowledge ranks
		CALLF MAKE_INT(V_NAME, 2)
	CASE "Anti-Theft Message"
		;flavor text function
		CALLF MAKE_STR(V_NAME, "WeaponAntiTheftYomihari")
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "FireMagic/ProfessionalMagicWeapons/")
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Youjutsu")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Shotgun")
	CASE "Ammo"
		CALLF MAKE_STR(V_NAME, "MP")
	CASE "Platform"
		CALLF MAKE_STR(V_NAME, "YoujyuuS/")
	CASE "Fire Sound"
		CALLF MAKE_STR(V_NAME, "firearms/AT MR-92.wav")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"keiga/military/modern/western/shotgun/")
		;旧装備処理との照会用、Weaponの1
ENDSELECT

@Weapon508(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "Defender")
		ELSE
			CALLF MAKE_STR(V_NAME, "single shotgun")
		ENDIF
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "YMKA Single Shotgun「Defender」")
		ELSE
			CALLF MAKE_STR(V_NAME, "single shotgun")
		ENDIF
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "YKMA 디펜더는 요괴 산 카파 무기고에서 제조한 브레이크 액션 샷건으로, 윈더거 B7 시스템을 기반으로 한다. 이 단발식 브리치 로딩 샷건은 1880년대 미국의 사냥에 중요한 무기였다. 가볍고 저렴한 가격(다른 총기에 비해) 덕분에 조류나 다른 가벼운 사냥감 사냥에 매우 인기 있었다. 12게이지 단발 브리치 로딩 샷건은 윈더거, 브라우트온, GKO622, 슈퍼 포인트 및 기타 제조사들에 의해 만들어졌다.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Tech Level"
		LOCAL = 6
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grade"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Price"
		LOCAL = 204000
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Body Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Stock Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 140)
	CASE "WeaponBarrelBody/Default"
		CALLF MAKE_INT(V_NAME, 120)
	CASE "WeaponStock/Default"
		CALLF MAKE_INT(V_NAME, 24)
	CASE "Two-Handed"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "Gunsmith/")
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Firearm")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Shotgun")
	CASE "Platform"
		CALLF MAKE_STR(V_NAME, "topper/")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, "kappa-arm/civilian/shotgun/world war/")
		;旧装備処理との照会用、Weaponの1
ENDSELECT

@Weapon509(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "Grey")
		ELSE
			CALLF MAKE_STR(V_NAME, "smart shotgun")
		ENDIF
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "AImthropic Smart Shotgun「Grey」")
		ELSE
			CALLF MAKE_STR(V_NAME, "smart shotgun")
		ENDIF
	CASE "描写"
		CALLF MAKE_STR(V_NAME,"그레이는 사격이 너무 쉬워서 걸음마 아기도 할 수 있게 만든다. 스마트 타겟팅 시스템과 산탄총의 넓은 탄막을 결합하여, 그레이는 마치 한 번에 5개의 서머스 총을 쏘는 듯한 느낌을 준다.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Tech Level"
		LOCAL = 9
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grade"
		LOCAL = 4
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Price"
		LOCAL = 1350000
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Magazine Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Body Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 3
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Stock Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 141)
	CASE "WeaponBarrelBody/Default"
		CALLF MAKE_INT(V_NAME, 125)
	CASE "WeaponMag/Default"
		CALLF MAKE_INT(V_NAME, 153)
	CASE "WeaponStock/Default"
		CALLF MAKE_INT(V_NAME, 60)
	CASE "WeaponGrip/Default"
		CALLF MAKE_INT(V_NAME, 39)
	CASE "Two-Handed"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Anti-Theft Strength"
		;Strength of the anti-theft system in Knowledge ranks
		CALLF MAKE_INT(V_NAME, 5)
	CASE "Anti-Theft Message"
		;flavor text function
		CALLF MAKE_STR(V_NAME, "WeaponAntiTheftSAGS")
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "AdvancedGuidingBullets/")
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Firearm")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Shotgun")
	CASE "Platform"
		CALLF MAKE_STR(V_NAME, "aimgrey/")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"aimthropic/smartgun/closed-bolt/military/spacer/shotgun/")
		;旧装備処理との照会用、Weaponの1
ENDSELECT
;オブジェクト本体

@Weapon510(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "LAC9")
		ELSE
			CALLF MAKE_STR(V_NAME, "laser shotgun")
		ENDIF
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "LUNINCO Laser Shotgun「LAC9」")
		ELSE
			CALLF MAKE_STR(V_NAME, "laser shotgun")
		ENDIF
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "열에너지를 통해 피해를 입히기 위해 16kw의 광선 빔을 사용하며, 이 빔은 여덟 개의 2kW 빔으로 나뉜다. 막대한 에너지가 필요하기 때문에 배터리를 교체해야 할 정도로 많이 발사할 수 없다.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Tech Level"
		LOCAL = 9
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "ChargedShot/")
	CASE "Grade"
		LOCAL = 4
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Price"
		LOCAL = 4248800
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Prerequisite"
		LOCAL = 84
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Foregrip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Magazine Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Body Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 3
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Stock Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 142)
	CASE "WeaponBarrelBody/Default"
		CALLF MAKE_INT(V_NAME, 126)
	CASE "WeaponMag/Default"
		CALLF MAKE_INT(V_NAME, 154)
	CASE "WeaponStock/Default"
		CALLF MAKE_INT(V_NAME, 60)
	CASE "WeaponGrip/Default"
		CALLF MAKE_INT(V_NAME, 39)
	CASE "Two-Handed"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Anti-Theft Strength"
		;Strength of the anti-theft system in Knowledge ranks
		CALLF MAKE_INT(V_NAME, 5)
	CASE "Anti-Theft Message"
		;flavor text function
		CALLF MAKE_STR(V_NAME, "WeaponAntiTheftLACTite")
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Firearm")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Shotgun")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, "lunarian/civilian/military/spacer/shotgun")
		;旧装備処理との照会用、Weaponの1
ENDSELECT

@Weapon511(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "RAMSET")
		ELSE
			CALLF MAKE_STR(V_NAME, "energy shotgun")
		ENDIF
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "Cactus Company Energy Shotgun「RAMSET」")
		ELSE
			CALLF MAKE_STR(V_NAME, "energy shotgun")
		ENDIF
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "The RAMSET, or the \"Rapid Accelerated Mass Shotgun using Electricity Transfer\" is a energy shotgun using a combination of electricity from a battery in the cartridge and cactus energy to rapidly push multiple pellets at over 2500 m/s. It is both devastating and easy to fire.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Tech Level"
		LOCAL = 9
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "ChargedShot/")
	CASE "Grade"
		LOCAL = 4
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Price"
		LOCAL = 3548800
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Foregrip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Magazine Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 3
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Stock Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 143)
	CASE "WeaponBarrelBody/Default"
		CALLF MAKE_INT(V_NAME, 127)
	CASE "WeaponMag/Default"
		CALLF MAKE_INT(V_NAME, 155)
	CASE "WeaponStock/Default"
		CALLF MAKE_INT(V_NAME, 61)
	CASE "WeaponGrip/Default"
		CALLF MAKE_INT(V_NAME, 4)
	CASE "Two-Handed"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Anti-Theft Strength"
		;Strength of the anti-theft system in Knowledge ranks
		CALLF MAKE_INT(V_NAME, 5)
	CASE "Anti-Theft Message"
		;flavor text function
		CALLF MAKE_STR(V_NAME, "WeaponAntiTheftSAGS")
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Firearm")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Shotgun")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, "cactus-comp/civilian/military/spacer/shotgun")
		;旧装備処理との照会用、Weaponの1
ENDSELECT

@Weapon512(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, "CAWS")
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "Gabe Hubert Automatic Shotgun「CAWS」")
		ELSE
			CALLF MAKE_STR(V_NAME, "close assault weapons system")
		ENDIF
	CASE "描写"
		CALLF MAKE_STR(V_NAME,"게이브 허베르트 CAWS는 특수한 고압 사격탄, 즉 텅스텐 산탄이나 플레셰트가 장전된 탄환을 발사하도록 설계된 선택 사격용 강선 없는 화기이다. GH312는 가변 총열과 추가적인 자체 조절 가스 보조 시스템(저화력 탄약 사용 시 약실 블록이 총열에서 풀리는 것을 돕기 위해 사용됨)을 갖춘 특허받은 반동 작동 방식을 활용했다. 이 시스템의 주된 목표는 체감되는 반동을 적절한 수준으로 낮추는 것이었다.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Tech Level"
		LOCAL = 8
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grade"
		LOCAL = 4
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Price"
		LOCAL = 204000
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Magazine Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Body Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Stock Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 70)
	CASE "WeaponBarrelBody/Default"
		CALLF MAKE_INT(V_NAME, 40)
	CASE "WeaponMag/Default"
		CALLF MAKE_INT(V_NAME, 85)
	CASE "WeaponGrip/Default"
		CALLF MAKE_INT(V_NAME, 4)
	CASE "Two-Handed"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "GasOperation/")
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Firearm")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Shotgun")
	CASE "Platform"
		CALLF MAKE_STR(V_NAME, "CAWS/")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, "gabe-hubert/closed bolt//military/modern/western/shotgun/")
		;旧装備処理との照会用、Weaponの1
ENDSELECT
