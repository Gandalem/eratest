; Modular ERB: OMOGATARI/ITEM/Armory/300 Firearms/500 Shotguns.ERB

@Weapon500(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "Attorney")
		ELSE
			CALLF MAKE_STR(V_NAME, "pump-action shotgun")
		ENDIF
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "캇파 아머리 펌프액션 산탄총「변호사」")
		ELSE
			CALLF MAKE_STR(V_NAME, "펌프액션 산탄총")
		ENDIF
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "YKMA 변호사는 진정한 브라우튼 S1000 시스템을 기반으로 요괴 산 카파 무기고에서 제조한 펌프 액션 산탄총이다. 이는 대중에게 스포츠 사격, 사냥 및 자위 목적으로 널리 사용되며 환상의 세계 내 군 조직에 의해 사용된다.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Tech Level"
		LOCAL = 6
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grade"
		LOCAL = 2
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Price"
		LOCAL = 204000
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Magazine Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Body Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Stock Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 80)
	CASE "WeaponBarrelBody/Default"
		CALLF MAKE_INT(V_NAME, 40)
	CASE "WeaponStock/Default"
		CALLF MAKE_INT(V_NAME, 30)
	CASE "WeaponMag/Default"
		CALLF MAKE_INT(V_NAME, 11)
	CASE "Two-Handed"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "Gunsmith/")
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Firearm")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Shotgun")
	CASE "Platform"
		CALLF MAKE_STR(V_NAME, "R870/")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, "kappa-arm/closed-bolt/")
		;旧装備処理との照会用、Weaponの1
ENDSELECT

@EXIST_Weapon501
#LOCALSIZE 1
#LOCALSSIZE 1
;オブジェクト本体

@Weapon501(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "Sweet Sixteen")
		ELSE
			CALLF MAKE_STR(V_NAME, "volley rifle")
		ENDIF
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "리카 암즈 일제사격 소총「스위트 식스틴」")
		ELSE
			CALLF MAKE_STR(V_NAME, "일제사격 소총")
		ENDIF
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "RaD에 의해 만들어진 특수 산탄총. 엄밀히 말해 이 고유한 무기는 여러 개의 총열에서 개별 투사체를 동시에 발사하여 광역 공격이 활성화되기 때문에 진정한 산탄총은 아니다.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Grade"
		LOCAL = 2
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tech Level"
		LOCAL = 7
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "Gunsmith/")
	CASE "Price"
		LOCAL = 210000
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Foregrip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 3
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Stock Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 135)
	CASE "WeaponBarrelBody/Default"
		CALLF MAKE_INT(V_NAME, 115)
	CASE "WeaponStock/Default"
		CALLF MAKE_INT(V_NAME, 8)
	CASE "WeaponGrip/Default"
		CALLF MAKE_INT(V_NAME, 3)
	CASE "Two-Handed"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Firearm")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Shotgun")
	CASE "Fire Sound"
		CALLF MAKE_STR(V_NAME, "firearms/S1000.m4a")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"rikaarms/military/modern/native/shotgun/")
		;旧装備処理との照会用、Weaponの1
ENDSELECT

@Weapon502(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "GH512")
		ELSE
			CALLF MAKE_STR(V_NAME, "semi-auto shotgun")
		ENDIF
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "게이브 휴버트 반자동 산탄총「GH512」")
		ELSE
			CALLF MAKE_STR(V_NAME, "반자동 산탄총")
		ENDIF
	CASE "描写"
		CALLF MAKE_STR(V_NAME,"현대의 마에스트로 스톰 샷건 시리즈의 전신으로서, 모델 512(별칭 GH512)는 마에스트로 아미가 설계하고 독일 출신의 게이브 휴버트가 조립한 세미 오토매틱 샷건이다. 이 총기는 처음 서독의 엘리트 GSG-9 대테러 경찰 부대에서 사용하기 위해 제조되었다. GH 모델 512 샷건에는 '맨 커터' 조절 차징이 전면에 장착되어 있었는데, 이는 탄환을 길쭉한 타원형 패턴으로 발사하여 사람을 반으로 가르거나 (수직으로 돌렸을 때) 모든 총알을 표적의 주 몸체에 집중시키는 역할을 했다. ")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Tech Level"
		LOCAL = 7
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grade"
		LOCAL = 3
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Price"
		LOCAL = 204000
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Magazine Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Body Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Stock Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 56)
	CASE "WeaponBarrelBody/Default"
		CALLF MAKE_INT(V_NAME, 40)
	CASE "WeaponStock/Default"
		CALLF MAKE_INT(V_NAME, 30)
	CASE "WeaponMag/Default"
		CALLF MAKE_INT(V_NAME, 12)
	CASE "Two-Handed"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "BlowbackOperation/")
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Firearm")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Shotgun")
	CASE "Platform"
		CALLF MAKE_STR(V_NAME, "HK512/")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, "gabe-hubert/closed-bolt//civilian/military/cold war/modern/western/shotgun/")
		;旧装備処理との照会用、Weaponの1
ENDSELECT

@Weapon503(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "VS-23")
		ELSE
			CALLF MAKE_STR(V_NAME, "high-bore shotgun")
		ENDIF
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "클리모프스크 대구경 산탄총「VS-23」")
		ELSE
			CALLF MAKE_STR(V_NAME, "대구경 산탄총")
		ENDIF
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "VS-23(특수 소총 - 23mm)은 러시아제 산탄총이지만 강선 총열을 사용하기 때문에 러시아군에서는 공식적으로 소총으로 분류한다. VS는 Vintovka Spetsialniy, 즉 \"특수 소총\"의 약자다. 23mm 탄을 사용하는 대구경 무기로 유명하며, 영미식 산탄총 규격으로는 약 6.27게이지, 현행 유럽식 기준으로는 약 4게이지에 해당한다. 현재 운용되는 산탄총 가운데 가장 큰 구경에 속한다.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Grade"
		LOCAL = 3
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tech Level"
		LOCAL = 8
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "PrecisionRifling/")
	CASE "Price"
		LOCAL = 3500000
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Foregrip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Magazine Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 3
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Stock Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 136)
	CASE "WeaponBarrelBody/Default"
		CALLF MAKE_INT(V_NAME, 116)
	CASE "WeaponStock/Default"
		CALLF MAKE_INT(V_NAME, 67)
	CASE "WeaponMag/Default"
		CALLF MAKE_INT(V_NAME, 150)
	CASE "Two-Handed"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Firearm")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Shotgun")
	CASE "Platform"
		CALLF MAKE_STR(V_NAME, "ks23/")
	CASE "Fire Sound"
		CALLF MAKE_STR(V_NAME, "firearms/S1000.m4a")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"klimovsk/closed-bolt/civilian/military/soviet/modern/shotgun")
		;旧装備処理との照会用、Weaponの1
ENDSELECT
;オブジェクト本体

@Weapon504(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "Raiken")
		ELSE
			CALLF MAKE_STR(V_NAME, "shotgun musket")
		ENDIF
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "아카츠키 산탄 머스킷「라이켄」")
		ELSE
			CALLF MAKE_STR(V_NAME, "산탄 머스킷")
		ENDIF
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "Raiken은 일본어로 \"천둥 관\"이라는 뜻의 화기로, 짧고 큰 구경의 총열과 넓게 벌어진 총구를 갖고 산탄이나 여러 형태의 투사체를 발사한다. 현대 산탄총의 초기 선조에 해당하는 무기로 여겨지며 군용과 방어용으로 비슷한 역할을 했다. 근거리에서는 효과적이지만 장거리 명중률은 낮다. 권총 형태의 산탄 머스킷은 드래곤이라 불렸으며, 여기에서 드라군이라는 명칭이 유래했다.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Tech Level"
		LOCAL = 4
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Stock Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "Gunpowder/")
	CASE "Grade"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Price"
		LOCAL = 25000
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 137)
	CASE "WeaponBarrelBody/Default"
		CALLF MAKE_INT(V_NAME, 117)
	CASE "WeaponStock/Default"
		CALLF MAKE_INT(V_NAME, 63)
	CASE "Two-Handed"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Firearm")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Shotgun")
	CASE "Platform"
		CALLF MAKE_STR(V_NAME, "tanegashima/")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"akatsuki/civilian/military/medieval/shotgun")
		;旧装備処理との照会用、Weaponの1
ENDSELECT
;SKK-83

@Weapon505(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "Lawyer")
		ELSE
			CALLF MAKE_STR(V_NAME, "auto shotgun")
		ENDIF
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "캇파 아머리 자동 산탄총「변론가」")
		ELSE
			CALLF MAKE_STR(V_NAME, "자동 산탄총")
		ENDIF
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "폴딩 스톡이 달린 완전 자동 12게이지 산탄총. Renov Karbarga-83 (SKK-83) 반자동 산탄총의 개조 복제품이다. 근접전 중 제압 사격과 빠른 방 정리용으로 의도되었다. 짧은 430mm 총열을 특징으로 한다.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Grade"
		LOCAL = 4
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tech Level"
		LOCAL = 8
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "GasOperation/")
	CASE "Price"
		LOCAL = 4461310
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Foregrip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Magazine Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 3
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Stock Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 138)
	CASE "WeaponBarrelBody/Default"
		CALLF MAKE_INT(V_NAME, 118)
	CASE "WeaponStock/Default"
		CALLF MAKE_INT(V_NAME, 17)
	CASE "WeaponGrip/Default"
		CALLF MAKE_INT(V_NAME, 10)
	CASE "WeaponMag/Default"
		CALLF MAKE_INT(V_NAME, 151)
	CASE "Two-Handed"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Firearm")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Shotgun")
	CASE "Platform"
		CALLF MAKE_STR(V_NAME, "AK/")
	CASE "Fire Sound"
		CALLF MAKE_STR(V_NAME, "firearms/Kabarga.m4a")
	CASE "Image"
		CALLF MAKE_STR(V_NAME, "OMOGATARI/Weapons/Kabarga83.png")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"kappa-arm/military/cold war/modern/soviet/shotgun")
		;旧装備処理との照会用、Weaponの1
ENDSELECT

@Weapon506(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "OSP-81")
		ELSE
			CALLF MAKE_STR(V_NAME, "flare gun")
		ENDIF
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "니즈니 노브고로드 조명·신호 권총「OSP-81」")
		ELSE
			CALLF MAKE_STR(V_NAME, "신호탄 권총")
		ENDIF
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "OSP-81(\"1981년형 신호 권총\")은 1980년에 개발되어 1981년에 채택된 소련/러시아제 신호탄 권총으로, Nizhny Novgorod 조병창에서 생산되었다. SP-44를 대체하기 위해 만들어졌으며 보다 작고 간결한 형태로 설계되었다. 전투용 파생형이 존재했던 몇 안 되는 신호탄 권총이기도 하다. 1994년에는 귀중품을 운송하는 운전자들을 위해 MAI의 특별 허가 아래, VS-23 탄약을 사용할 수 있도록 길고 무거운 총열을 단 버전도 출시되었다.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Tech Level"
		LOCAL = 6
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grade"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Price"
		LOCAL = 154000
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Body Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 102)
	CASE "WeaponBarrelBody/Default"
		CALLF MAKE_INT(V_NAME, 84)
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "Gunsmith/")
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Firearm")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Shotgun")
	CASE "Platform"
		CALLF MAKE_STR(V_NAME, "sp81/")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"nnoz/civilian/world-war/cold war/modern/western/shotgun/")
		;旧装備処理との照会用、Weaponの1
ENDSELECT

@Weapon507(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "Youjyuu-S")
		ELSE
			CALLF MAKE_STR(V_NAME, "recharger shotgun")
		ENDIF
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "케이가 요술 시리즈 산탄총「요쥬-S」")
		ELSE
			CALLF MAKE_STR(V_NAME, "재충전식 산탄총")
		ENDIF
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "유쥬우-S는 마에스트로 스톰샷건 같은 무기에 해당하는 유유술이다. 발사하기 쉽고 재장전이 필요 없다. 또한 사용자가 유유술 에센스를 가지고 있어야 사용할 수 있다.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Grade"
		LOCAL = 4
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Price"
		LOCAL = 770000
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Foregrip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 3
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Stock Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "MP Cost"
		LOCAL = 5
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tech Level"
		LOCAL = 8
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Damage Type"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 139)
	CASE "WeaponGrip/Default"
		CALLF MAKE_INT(V_NAME, 4)
	CASE "WeaponStock/Default"
		CALLF MAKE_INT(V_NAME, 4)
	CASE "WeaponBarrelBody/Default"
		CALLF MAKE_INT(V_NAME, 119)
	CASE "WeaponSight/Default"
		CALLF MAKE_INT(V_NAME, 15)
	CASE "Two-Handed"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Anti-Theft Strength"
		;Strength of the anti-theft system in Knowledge ranks
		CALLF MAKE_INT(V_NAME, 2)
	CASE "Anti-Theft Message"
		;flavor text function
		CALLF MAKE_STR(V_NAME, "WeaponAntiTheftYomihari")
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "FireMagic/ProfessionalMagicWeapons/")
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Youjutsu")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Shotgun")
	CASE "Ammo"
		CALLF MAKE_STR(V_NAME, "MP")
	CASE "Platform"
		CALLF MAKE_STR(V_NAME, "YoujyuuS/")
	CASE "Fire Sound"
		CALLF MAKE_STR(V_NAME, "firearms/AT MR-92.wav")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"keiga/military/modern/western/shotgun/")
		;旧装備処理との照会用、Weaponの1
ENDSELECT

@Weapon508(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "Defender")
		ELSE
			CALLF MAKE_STR(V_NAME, "single shotgun")
		ENDIF
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "YMKA 단발 산탄총「디펜더」")
		ELSE
			CALLF MAKE_STR(V_NAME, "단발 산탄총")
		ENDIF
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "YKMA 디펜더는 요괴 산 카파 무기고에서 제조한 브레이크 액션 샷건으로, 윈더거 B7 시스템을 기반으로 한다. 이 단발식 브리치 로딩 샷건은 1880년대 미국의 사냥에 중요한 무기였다. 가볍고 저렴한 가격(다른 총기에 비해) 덕분에 조류나 다른 가벼운 사냥감 사냥에 매우 인기 있었다. 12게이지 단발 브리치 로딩 샷건은 윈더거, 브라우트온, GKO622, 슈퍼 포인트 및 기타 제조사들에 의해 만들어졌다.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Tech Level"
		LOCAL = 6
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grade"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Price"
		LOCAL = 204000
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Body Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Stock Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 140)
	CASE "WeaponBarrelBody/Default"
		CALLF MAKE_INT(V_NAME, 120)
	CASE "WeaponStock/Default"
		CALLF MAKE_INT(V_NAME, 24)
	CASE "Two-Handed"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "Gunsmith/")
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Firearm")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Shotgun")
	CASE "Platform"
		CALLF MAKE_STR(V_NAME, "topper/")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, "kappa-arm/civilian/shotgun/world war/")
		;旧装備処理との照会用、Weaponの1
ENDSELECT

@Weapon509(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "Grey")
		ELSE
			CALLF MAKE_STR(V_NAME, "smart shotgun")
		ENDIF
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "AI믄트로픽 스마트 산탄총「그레이」")
		ELSE
			CALLF MAKE_STR(V_NAME, "스마트 산탄총")
		ENDIF
	CASE "描写"
		CALLF MAKE_STR(V_NAME,"그레이는 사격이 너무 쉬워서 걸음마 아기도 할 수 있게 만든다. 스마트 타겟팅 시스템과 산탄총의 넓은 탄막을 결합하여, 그레이는 마치 한 번에 5개의 서머스 총을 쏘는 듯한 느낌을 준다.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Tech Level"
		LOCAL = 9
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grade"
		LOCAL = 4
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Price"
		LOCAL = 1350000
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Magazine Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Body Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 3
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Stock Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 141)
	CASE "WeaponBarrelBody/Default"
		CALLF MAKE_INT(V_NAME, 125)
	CASE "WeaponMag/Default"
		CALLF MAKE_INT(V_NAME, 153)
	CASE "WeaponStock/Default"
		CALLF MAKE_INT(V_NAME, 60)
	CASE "WeaponGrip/Default"
		CALLF MAKE_INT(V_NAME, 39)
	CASE "Two-Handed"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Anti-Theft Strength"
		;Strength of the anti-theft system in Knowledge ranks
		CALLF MAKE_INT(V_NAME, 5)
	CASE "Anti-Theft Message"
		;flavor text function
		CALLF MAKE_STR(V_NAME, "WeaponAntiTheftSAGS")
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "AdvancedGuidingBullets/")
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Firearm")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Shotgun")
	CASE "Platform"
		CALLF MAKE_STR(V_NAME, "aimgrey/")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"aimthropic/smartgun/closed-bolt/military/spacer/shotgun/")
		;旧装備処理との照会用、Weaponの1
ENDSELECT
;オブジェクト本体

@Weapon510(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "LAC9")
		ELSE
			CALLF MAKE_STR(V_NAME, "laser shotgun")
		ENDIF
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "LUNINCO 레이저 산탄총「LAC9」")
		ELSE
			CALLF MAKE_STR(V_NAME, "레이저 산탄총")
		ENDIF
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "열에너지를 통해 피해를 입히기 위해 16kw의 광선 빔을 사용하며, 이 빔은 여덟 개의 2kW 빔으로 나뉜다. 막대한 에너지가 필요하기 때문에 배터리를 교체해야 할 정도로 많이 발사할 수 없다.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Tech Level"
		LOCAL = 9
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "ChargedShot/")
	CASE "Grade"
		LOCAL = 4
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Price"
		LOCAL = 4248800
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Prerequisite"
		LOCAL = 84
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Foregrip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Magazine Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Body Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 3
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Stock Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 142)
	CASE "WeaponBarrelBody/Default"
		CALLF MAKE_INT(V_NAME, 126)
	CASE "WeaponMag/Default"
		CALLF MAKE_INT(V_NAME, 154)
	CASE "WeaponStock/Default"
		CALLF MAKE_INT(V_NAME, 60)
	CASE "WeaponGrip/Default"
		CALLF MAKE_INT(V_NAME, 39)
	CASE "Two-Handed"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Anti-Theft Strength"
		;Strength of the anti-theft system in Knowledge ranks
		CALLF MAKE_INT(V_NAME, 5)
	CASE "Anti-Theft Message"
		;flavor text function
		CALLF MAKE_STR(V_NAME, "WeaponAntiTheftLACTite")
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Firearm")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Shotgun")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, "lunarian/civilian/military/spacer/shotgun")
		;旧装備処理との照会用、Weaponの1
ENDSELECT

@Weapon511(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "RAMSET")
		ELSE
			CALLF MAKE_STR(V_NAME, "energy shotgun")
		ENDIF
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "캑터스 컴퍼니 에너지 산탄총「RAMSET」")
		ELSE
			CALLF MAKE_STR(V_NAME, "에너지 산탄총")
		ENDIF
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "The RAMSET, or the \"Rapid Accelerated Mass Shotgun using Electricity Transfer\" is a energy shotgun using a combination of electricity from a battery in the cartridge and cactus energy to rapidly push multiple pellets at over 2500 m/s. It is both devastating and easy to fire.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Tech Level"
		LOCAL = 9
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "ChargedShot/")
	CASE "Grade"
		LOCAL = 4
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Price"
		LOCAL = 3548800
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Foregrip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Magazine Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 3
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Stock Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 143)
	CASE "WeaponBarrelBody/Default"
		CALLF MAKE_INT(V_NAME, 127)
	CASE "WeaponMag/Default"
		CALLF MAKE_INT(V_NAME, 155)
	CASE "WeaponStock/Default"
		CALLF MAKE_INT(V_NAME, 61)
	CASE "WeaponGrip/Default"
		CALLF MAKE_INT(V_NAME, 4)
	CASE "Two-Handed"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Anti-Theft Strength"
		;Strength of the anti-theft system in Knowledge ranks
		CALLF MAKE_INT(V_NAME, 5)
	CASE "Anti-Theft Message"
		;flavor text function
		CALLF MAKE_STR(V_NAME, "WeaponAntiTheftSAGS")
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Firearm")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Shotgun")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, "cactus-comp/civilian/military/spacer/shotgun")
		;旧装備処理との照会用、Weaponの1
ENDSELECT

@Weapon512(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, "CAWS")
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "게이브 휴버트 자동 산탄총「CAWS」")
		ELSE
			CALLF MAKE_STR(V_NAME, "근접 돌격 무기 체계")
		ENDIF
	CASE "描写"
		CALLF MAKE_STR(V_NAME,"게이브 허베르트 CAWS는 특수한 고압 사격탄, 즉 텅스텐 산탄이나 플레셰트가 장전된 탄환을 발사하도록 설계된 선택 사격용 강선 없는 화기이다. GH312는 가변 총열과 추가적인 자체 조절 가스 보조 시스템(저화력 탄약 사용 시 약실 블록이 총열에서 풀리는 것을 돕기 위해 사용됨)을 갖춘 특허받은 반동 작동 방식을 활용했다. 이 시스템의 주된 목표는 체감되는 반동을 적절한 수준으로 낮추는 것이었다.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Tech Level"
		LOCAL = 8
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grade"
		LOCAL = 4
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Price"
		LOCAL = 204000
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Magazine Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Body Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Stock Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 70)
	CASE "WeaponBarrelBody/Default"
		CALLF MAKE_INT(V_NAME, 40)
	CASE "WeaponMag/Default"
		CALLF MAKE_INT(V_NAME, 85)
	CASE "WeaponGrip/Default"
		CALLF MAKE_INT(V_NAME, 4)
	CASE "Two-Handed"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "GasOperation/")
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Firearm")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Shotgun")
	CASE "Platform"
		CALLF MAKE_STR(V_NAME, "CAWS/")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, "gabe-hubert/closed bolt//military/modern/western/shotgun/")
		;旧装備処理との照会用、Weaponの1
ENDSELECT
