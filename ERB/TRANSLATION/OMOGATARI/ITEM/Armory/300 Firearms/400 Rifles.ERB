; Modular ERB: OMOGATARI/ITEM/Armory/300 Firearms/400 Rifles.ERB

@Weapon72(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "VR-ICKX BIS")
		ELSE
			CALLF MAKE_STR(V_NAME, "bolt-action railgun")
		ENDIF
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "Gryzov Group 연발 레일건「VR-ICKX BIS」")
		ELSE
			CALLF MAKE_STR(V_NAME, "볼트액션 레일건")
		ENDIF
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "장애물을 관통하는 레일에서 사보트를 발사하는 반복식 볼트액션 저격 소총이다. 극도로 긴 유효 사거리와 매우 높은 정지력 및 관통력을 자랑한다. 자동 조준 컴퓨터, 홀로사이트, 그리고 레일 충전기가 완비되어 있다. Renov-ICKX를 기반으로 Gryzov Group이 개조한 것이다.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Grade"
		LOCAL = 4
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tech Level"
		LOCAL = 9
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "ChargedShot/")
	CASE "Grip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Foregrip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 3
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Stock Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Magazine Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Quest Only"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Price"
		LOCAL = 35000000
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 129)
	CASE "WeaponSight/Default"
		CALLF MAKE_INT(V_NAME, 31)
	CASE "WeaponStock/Default"
		CALLF MAKE_INT(V_NAME, 65)
	CASE "WeaponMag/Default"
		CALLF MAKE_INT(V_NAME, 144)
	CASE "Two-Handed"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Anti-Theft Strength"
		;Strength of the anti-theft system in Knowledge ranks
		CALLF MAKE_INT(V_NAME, 5)
	CASE "Anti-Theft Message"
		;flavor text function
		CALLF MAKE_STR(V_NAME, "WeaponAntiTheftSAGS")
	CASE "Platform"
		CALLF MAKE_STR(V_NAME, "mosin/")
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Firearm")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Rifle")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"gryzov/military/western/spacer/anti tank/")
ENDSELECT

@Weapon400(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "Bastard")
		ELSE
			CALLF MAKE_STR(V_NAME, "handmade rifle")
		ENDIF
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "급조 자동소총「Bastard」")
		ELSE
			CALLF MAKE_STR(V_NAME, "수제 소총")
		ENDIF
	CASE "描写"
		; [AUTO-NEUTRALIZED] CALLF MAKE_STR(V_NAME, "\"It's inaccurate and overheats like hell; that's why we call it a Bastard!\"\nFed with 30-round stripper clips and has a high rate of fire for an improvised weapon- or even a properly manufactured one. Modified from its Metro spec to feed the more common 5.56x45mm instead of 5.45x39mm")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Tech Level"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grade"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Price"
		LOCAL = 204000
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Magazine Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Body Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 3
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Stock Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 32)
	CASE "WeaponBarrelBody/Default"
		CALLF MAKE_INT(V_NAME, 17)
	CASE "WeaponMag/Default"
		CALLF MAKE_INT(V_NAME, 50)
	CASE "WeaponGrip/Default"
		CALLF MAKE_INT(V_NAME, 9)
	CASE "WeaponStock/Default"
		CALLF MAKE_INT(V_NAME, 23)
	CASE "Two-Handed"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "BlowbackOperation/PrecisionRifling/")
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Firearm")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Rifle")
	CASE "Platform"
		CALLF MAKE_STR(V_NAME, "makeshift-bastard/")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, "military/modern/native/assault rifle/makeshift/")
		;旧装備処理との照会用、Weaponの1
ENDSELECT

@Weapon401(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "GH31")
		ELSE
			CALLF MAKE_STR(V_NAME, "outsider BR")
		ENDIF
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "Gabe Hubert 전투소총「GH31」")
		ELSE
			CALLF MAKE_STR(V_NAME, "외부 세계 전투소총")
		ENDIF
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "GH31은 새로 개발된 CETME 플랫폼을 바탕으로 7.62x51 NATO 탄을 사용하는 고위력형으로 제작되었다. 정식 명칭은 GH FAG(Fully Automatic Rifle)이지만, 독일 연방군에서는 \"G3\"라는 이름을 붙였다. 환상향에서는 드문 물건이지만 독일 연방군이 GH233과 GH333을 주력 소총으로 채택하면서 조금씩 유입되기 시작했다. 이 형식은 목재 가구 부품을 장착한 버전이다.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Tech Level"
		LOCAL = 7
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grade"
		LOCAL = 2
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Price"
		LOCAL = 380000
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Magazine Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Body Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 3
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Stock Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 17)
	CASE "WeaponBarrelBody/Default"
		CALLF MAKE_INT(V_NAME, 96)
	CASE "WeaponMag/Default"
		CALLF MAKE_INT(V_NAME, 16)
	CASE "WeaponGrip/Default"
		CALLF MAKE_INT(V_NAME, 2)
	CASE "WeaponStock/Default"
		CALLF MAKE_INT(V_NAME, 12)
	CASE "Two-Handed"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "BlowbackOperation/PrecisionRifling/")
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Firearm")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Rifle")
	CASE "Platform"
		CALLF MAKE_STR(V_NAME, "CETME/")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"\@ GETBIT(nVerboseItems,3) ? heckler&koch # gabe-hubert\@/civilian/military/cold war/modern/western/battle rifle/")
		;旧装備処理との照会用、Weaponの1
ENDSELECT

@Weapon402(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "GH21")
		ELSE
			CALLF MAKE_STR(V_NAME, "MMG")
		ENDIF
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "Gabe Hubert 중형 기관총「GH21」")
		ELSE
			CALLF MAKE_STR(V_NAME, "중형 기관총")
		ENDIF
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "개조된 GH31 리시버를 사용하여, GH21은 제압 사격이나 대공과 같은 일반 목적 기관총(GPMG) 역할을 수행할 수 있다. 100발 탄띠로 장전한다.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Tech Level"
		LOCAL = 7
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grade"
		LOCAL = 2
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Price"
		LOCAL = 380000
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Magazine Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Body Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 3
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Stock Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 17)
	CASE "WeaponBarrelBody/Default"
		CALLF MAKE_INT(V_NAME, 97)
	CASE "WeaponMag/Default"
		CALLF MAKE_INT(V_NAME, 135)
	CASE "WeaponGrip/Default"
		CALLF MAKE_INT(V_NAME, 2)
	CASE "WeaponStock/Default"
		CALLF MAKE_INT(V_NAME, 55)
	CASE "Two-Handed"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "BlowbackOperation/PrecisionRifling/")
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Firearm")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Medium Machine Gun")
	CASE "Platform"
		CALLF MAKE_STR(V_NAME, "CETME/")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"\@ GETBIT(nVerboseItems,3) ? heckler&koch # gabe-hubert\@/civilian/military/cold war/modern/western/machine gun/")
		;旧装備処理との照会用、Weaponの1
ENDSELECT
;S324 Devolver
; The devolver is much like a regular Windurger S324 revolver, but the barrel is curved to face the shooter. You know what'll happen after.
;DO NOT USE IF YOUR NAME IS NOT SEIJA KIJIN AND YOU DON'T DRAIN GUNSHOTS!

@Weapon403(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "Vandal")
		ELSE
			CALLF MAKE_STR(V_NAME, "assault rifle")
		ENDIF
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "YMKA 돌격소총「Vandal」")
		ELSE
			CALLF MAKE_STR(V_NAME, "돌격소총")
		ENDIF
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "반달은 5.56x45mm 암텍 SPA(AR-18), 하이 그라운드 HGC(FNC), 그리고 하루 타입 89 돌격소총의 합체물로, 요괴 산의 카파들이 환상향에서 제조했다. 완전한 제작 시설 부족으로 일부 희생은 있지만, 대부분 일반 돌격소총처럼 다루기 쉽다. 암텍 SPA 내부 부품 및 브라운드 M602 시리즈 총열, 그립, 개머리판과 완벽하게 호환된다.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Tech Level"
		LOCAL = 8
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grade"
		LOCAL = 2
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Price"
		LOCAL = 380000
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Magazine Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Body Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 3
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Stock Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 22)
	CASE "WeaponBarrelBody/Default"
		CALLF MAKE_INT(V_NAME, 99)
	CASE "WeaponMag/Default"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "WeaponGrip/Default"
		CALLF MAKE_INT(V_NAME, 38)
	CASE "WeaponStock/Default"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Two-Handed"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "GasOperation/PrecisionRifling/")
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Firearm")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Rifle")
	CASE "Platform"
		CALLF MAKE_STR(V_NAME, "AR-18/")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"kappa-arm/military/cold war/modern/assault rifle/native/")
		;旧装備処理との照会用、Weaponの1
ENDSELECT

@Weapon404(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "Youjyuu-R")
		ELSE
			CALLF MAKE_STR(V_NAME, "recharger rifle")
		ENDIF
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "Keiga 요술 시리즈 소총「Youjyuu-R」")
		ELSE
			CALLF MAKE_STR(V_NAME, "충전식 소총")
		ENDIF
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "유주우-R은 브라운드 M602 시리즈, 암텍 SPA, 그리조프스와 같은 무기와 동등한 수준의 유술이다. 마법 강화 플라스틱 구조 덕분에 탄환은 날카롭지만 정확도가 낮아, 부피당 정확도가 필요하다. 또한 사용자가 유술 정수를 가지고 있어야 사용할 수 있다.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Grade"
		LOCAL = 3
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Price"
		LOCAL = 770000
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Foregrip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Magazine Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Body Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 3
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Stock Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tech Level"
		LOCAL = 8
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Damage Type"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 118)
	CASE "WeaponGrip/Default"
		CALLF MAKE_INT(V_NAME, 4)
	CASE "WeaponStock/Default"
		CALLF MAKE_INT(V_NAME, 4)
	CASE "WeaponBarrelBody/Default"
		CALLF MAKE_INT(V_NAME, 130)
	CASE "WeaponSight/Default"
		CALLF MAKE_INT(V_NAME, 15)
	CASE "WeaponSight/Default"
		CALLF MAKE_INT(V_NAME, 15)
	CASE "Two-Handed"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Anti-Theft Strength"
		;Strength of the anti-theft system in Knowledge ranks
		CALLF MAKE_INT(V_NAME, 2)
	CASE "Anti-Theft Message"
		;flavor text function
		CALLF MAKE_STR(V_NAME, "WeaponAntiTheftYomihari")
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "FireMagic/ProfessionalMagicWeapons/")
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Youjutsu")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Rifle")
	CASE "Fire Sound"
		CALLF MAKE_STR(V_NAME, "firearms/AT MR-92.wav")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"keiga/military/modern/native/rifle/assault rifle/")
		;旧装備処理との照会用、Weaponの1
ENDSELECT

@Weapon405(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "Stinger")
		ELSE
			CALLF MAKE_STR(V_NAME, "submachine gun")
		ENDIF
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "YMKA 소구경 기관총「Stinger」")
		ELSE
			CALLF MAKE_STR(V_NAME, "기관단총")
		ENDIF
	CASE "描写"
		CALLF MAKE_STR(V_NAME,"소구경 탄약을 사용하는 경량 자동소총인 스팅어는 근접전에서 주로 제압 사격용으로 사용된다. 레노프 PPG-9를 기반으로 하여 그립조프의 디자인과 많은 유사성을 공유한다. 또한 요정 택티컬 기관단총에 비해 좋은 개선점이다.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Tech Level"
		LOCAL = 7
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grade"
		LOCAL = 2
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Price"
		LOCAL = 380000
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Magazine Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Body Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 3
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Stock Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 119)
	CASE "WeaponBarrelBody/Default"
		CALLF MAKE_INT(V_NAME, 101)
	CASE "WeaponMag/Default"
		CALLF MAKE_INT(V_NAME, 66)
	CASE "WeaponStock/Default"
		CALLF MAKE_INT(V_NAME, 58)
	CASE "WeaponGrip/Default"
		CALLF MAKE_INT(V_NAME, 38)
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "BlowbackOperation/PrecisionRifling/")
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Firearm")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Rifle")
	CASE "Platform"
		CALLF MAKE_STR(V_NAME, "stinger/")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"kappa-arm/military/cold war/modern/native/carbine/")
		;旧装備処理との照会用、Weaponの1
ENDSELECT

@Weapon406(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "Shaman")
		ELSE
			CALLF MAKE_STR(V_NAME, "smart SMG")
		ENDIF
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "AImthropic 스마트 기관단총「Shaman」")
		ELSE
			CALLF MAKE_STR(V_NAME, "스마트 기관단총")
		ENDIF
	CASE "描写"
		CALLF MAKE_STR(V_NAME,"샤먼은 서머보다 더 까다로운 전투 시나리오를 위해 설계된 스마트 기관단총이다. 이 총은 깔끔한 형태로 30발의 스마트 8.3x30mm 탄약을 장전하며, 분당 800발을 발사한다. 서머처럼 샤먼 역시 AI 기반 조준 기능을 갖추고 있어 EVA 전투에서도 유효한 무기이다.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Tech Level"
		LOCAL = 9
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grade"
		LOCAL = 3
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Price"
		LOCAL = 790000
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Magazine Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Body Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 3
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Stock Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 120)
	CASE "WeaponBarrelBody/Default"
		CALLF MAKE_INT(V_NAME, 102)
	CASE "WeaponMag/Default"
		CALLF MAKE_INT(V_NAME, 137)
	CASE "WeaponStock/Default"
		CALLF MAKE_INT(V_NAME, 59)
	CASE "WeaponGrip/Default"
		CALLF MAKE_INT(V_NAME, 39)
	CASE "Anti-Theft Strength"
		;Strength of the anti-theft system in Knowledge ranks
		CALLF MAKE_INT(V_NAME, 5)
	CASE "Anti-Theft Message"
		;flavor text function
		CALLF MAKE_STR(V_NAME, "WeaponAntiTheftSAGS")
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "AdvancedGuidingBullets/")
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Firearm")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Rifle")
	CASE "Platform"
		CALLF MAKE_STR(V_NAME, "aimshaman/")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"aimthropic/smartgun/closed-bolt/military/spacer/carbine/")
		;旧装備処理との照会用、Weaponの1
ENDSELECT

@Weapon407(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "Xavier")
		ELSE
			CALLF MAKE_STR(V_NAME, "smart rifle")
		ENDIF
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "AImthropic 스마트 자동소총「Xavier」")
		ELSE
			CALLF MAKE_STR(V_NAME, "스마트 소총")
		ENDIF
	CASE "描写"
		CALLF MAKE_STR(V_NAME,"샤먼과 서머가 함내 전투용으로 만들어진 것과 달리, 자이로스는 세이호 세계의 여러 행성 같은 지상전용으로 만들어졌다. 이 설계 덕분에 고출력 5.8x62mm 탄약을 사용하여 아르텍 AC-15ARS와 같은 보통 수준의 방어구를 관통할 수 있다.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Tech Level"
		LOCAL = 9
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grade"
		LOCAL = 3
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Price"
		LOCAL = 1350000
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Magazine Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Body Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 3
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Stock Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 121)
	CASE "WeaponBarrelBody/Default"
		CALLF MAKE_INT(V_NAME, 103)
	CASE "WeaponMag/Default"
		CALLF MAKE_INT(V_NAME, 138)
	CASE "WeaponStock/Default"
		CALLF MAKE_INT(V_NAME, 60)
	CASE "WeaponGrip/Default"
		CALLF MAKE_INT(V_NAME, 39)
	CASE "Two-Handed"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Anti-Theft Strength"
		;Strength of the anti-theft system in Knowledge ranks
		CALLF MAKE_INT(V_NAME, 5)
	CASE "Anti-Theft Message"
		;flavor text function
		CALLF MAKE_STR(V_NAME, "WeaponAntiTheftSAGS")
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "AdvancedGuidingBullets/")
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Firearm")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Rifle")
	CASE "Platform"
		CALLF MAKE_STR(V_NAME, "aimxavier/")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"aimthropic/smartgun/closed-bolt/military/spacer/carbine/")
		;旧装備処理との照会用、Weaponの1
ENDSELECT

@Weapon408(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "LILAC")
		ELSE
			CALLF MAKE_STR(V_NAME, "energy rifle")
		ENDIF
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "Cactus Company 에너지 소총「LILAC」")
		ELSE
			CALLF MAKE_STR(V_NAME, "에너지 소총")
		ENDIF
	CASE "描写"
		;CALLF MAKE_STR(V_NAME, "As a secondary weapon for VIVIT-based androids, the SideSTRAP, aka the Side Supercharged Thermal Rapidly Accelerated Projectile, is a pistol designed for extravehicular activities and ground-based combat. .")
		CALLF MAKE_STR(V_NAME,"LILAC은 \"Light Infantry Loaded Accelerator Carbine\"의 약자로, Cactus Company 보안 병력의 표준 돌격소총이다. VIVIT 안드로이드의 LILAC-ARM 팔 대포를 비롯해 세이호 세계의 여러 첨단 군대에서도 기반 무장으로 사용된다. 탄환은 탄창 내부 배터리로 자동 충전되며, 카투스 에너지 분말과 점화용 프라이머를 사용해 일반 화약보다 빠르게 탄체를 가속한다. 자사의 Scout 파워 아머 같은 첨단 방어구도 관통할 수 있다.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Tech Level"
		LOCAL = 9
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grade"
		LOCAL = 3
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Price"
		LOCAL = 1350000
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Magazine Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Body Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 3
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Stock Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 122)
	CASE "WeaponBarrelBody/Default"
		CALLF MAKE_INT(V_NAME, 104)
	CASE "WeaponMag/Default"
		CALLF MAKE_INT(V_NAME, 139)
	CASE "WeaponStock/Default"
		CALLF MAKE_INT(V_NAME, 61)
	CASE "WeaponGrip/Default"
		CALLF MAKE_INT(V_NAME, 4)
	CASE "Two-Handed"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Anti-Theft Strength"
		;Strength of the anti-theft system in Knowledge ranks
		CALLF MAKE_INT(V_NAME, 5)
	CASE "Anti-Theft Message"
		;flavor text function
		CALLF MAKE_STR(V_NAME, "WeaponAntiTheftSAGS")
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "ChargedShot/")
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Firearm")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Rifle")
	CASE "Platform"
		CALLF MAKE_STR(V_NAME, "cactuslilac/")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"cactus-comp/closed-bolt/military/spacer/assault rifle/")
		;旧装備処理との照会用、Weaponの1
ENDSELECT

@Weapon409(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "MARLin")
		ELSE
			CALLF MAKE_STR(V_NAME, "ultratech rifle")
		ENDIF
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "Gensou-chan 경량 돌격소총「MARLin」")
		ELSE
			CALLF MAKE_STR(V_NAME, "울트라테크 돌격소총")
		ENDIF
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "MARLin은 마카이의 분노가 치미는 정신을 소모시키는 라이플이다. 호프만 택티컬의 오르카를 기반으로 제작되었으며, 낮은 MP 소환 비용과 겐소키안 민간인들의 접근성을 높이기 위해 3D 프린팅이 가능하다. 이는 U-볼트 뱅가드 하위 버전에서 진화한 형태로 조립이 더 쉽고 강력하며 다양한 M602 부품과 호환된다. 또한 HUG 그립 시리즈와 ALSA 에임 어시스트 같은 여러 울트라테크 장신구를 포함하고 있다. 총열은 탄환을 여러 부분으로 쪼개어 무리에게 극도로 치명적일 수 있다. 어떤 이들은 MARLin 자체가 츠쿠모가미라고 말한다.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Tech Level"
		LOCAL = 10
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grade"
		LOCAL = 6
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Price"
		LOCAL = 1350000
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Magazine Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Body Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 3
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Stock Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 21)
	CASE "WeaponBarrelBody/Default"
		CALLF MAKE_INT(V_NAME, 14)
	CASE "WeaponMag/Default"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "WeaponStock/Default"
		CALLF MAKE_INT(V_NAME, 19)
	CASE "WeaponGrip/Default"
		CALLF MAKE_INT(V_NAME, 5)
	CASE "WeaponForegrip/Default"
		CALLF MAKE_INT(V_NAME, 10)
	CASE "WeaponTactical/Default"
		CALLF MAKE_INT(V_NAME, 14)
	CASE "WeaponAmmo/Default"
		CALLF MAKE_INT(V_NAME, 78)
	CASE "Two-Handed"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Anti-Theft Strength"
		;Strength of the anti-theft system in Knowledge ranks
		CALLF MAKE_INT(V_NAME, 8)
	CASE "Anti-Theft Message"
		;flavor text function
		CALLF MAKE_STR(V_NAME, "WeaponAntiTheftSWH")
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "MARlinproject/")
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Firearm")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Rifle")
	CASE "Platform"
		CALLF MAKE_STR(V_NAME, "AR-15/")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"gensou-chan-gun/closed-bolt/military/assault rifle/native/")
		;旧装備処理との照会用、Weaponの1
ENDSELECT

@Weapon410(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "LAC13")
		ELSE
			CALLF MAKE_STR(V_NAME, "rifle")
		ENDIF
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "LUNINCO 후장식 소총「LAC13」")
		ELSE
			CALLF MAKE_STR(V_NAME, "소총")
		ENDIF
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "최초의 현대 일본산 국산 장총인 제13형은 11x60mmR 탄환을 사용하는 단발 후장식 소총이다. 제13형 소총은 일본 제국군이 청일전쟁과 의화단 운동 중에 사용했으며, 1898년 이후에는 보다 진보된 제국소총으로 대체되었다. LAC13은 일반 7.62x51mm 탄약을 사용하는 제13형 소총의 현대적 복각품이다.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Tech Level"
		LOCAL = 6
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grade"
		LOCAL = 0
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Price"
		LOCAL = 90000
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Body Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 3
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Stock Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 123)
	CASE "WeaponBarrelBody/Default"
		CALLF MAKE_INT(V_NAME, 105)
	CASE "WeaponStock/Default"
		CALLF MAKE_INT(V_NAME, 43)
	CASE "Two-Handed"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "Gunsmith/")
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Firearm")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Rifle")
	CASE "Platform"
		CALLF MAKE_STR(V_NAME, "murata/")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"lunarian/civilian/military/world war/native/sniper rifle/")
		;旧装備処理との照会用、Weaponの1
ENDSELECT

@Weapon411(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "LAC17")
		ELSE
			CALLF MAKE_STR(V_NAME, "bolt-action rifle")
		ENDIF
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "LUNINCO 볼트액션 소총「LAC17」")
		ELSE
			CALLF MAKE_STR(V_NAME, "볼트액션 소총")
		ENDIF
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "제2차 세계 대전 당시 일본 제국 육군의 표준 소총 중 하나였던 하루 타입 99 임페리얼의 복제품이다. 스트라이퍼 클립으로 장전되는 5발짜리 내부 탄창을 가진 클래식 볼트액션 소총이다. 이 복제품은 호환성을 높이기 위해 7.7x58mm 하루 대신 일반 .308 윈더거로 제작되었다.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Tech Level"
		LOCAL = 6
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grade"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Price"
		LOCAL = 60000
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Body Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 3
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Stock Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 87)
	CASE "WeaponBarrelBody/Default"
		CALLF MAKE_INT(V_NAME, 69)
	CASE "WeaponStock/Default"
		CALLF MAKE_INT(V_NAME, 43)
	CASE "WeaponMag/Default"
		CALLF MAKE_INT(V_NAME, 107)
	CASE "Two-Handed"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "PrecisionRifling/")
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Firearm")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Rifle")
	CASE "Platform"
		CALLF MAKE_STR(V_NAME, "Arisaka/")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"lunarian/civilian/military/world war/cold war/modern/native/sniper rifle/")
		;旧装備処理との照会用、Weaponの1
ENDSELECT

@Weapon412(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "Operator")
		ELSE
			CALLF MAKE_STR(V_NAME, "anti material rifle")
		ENDIF
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "YMKA Anti-Material Rifle「Operator」")
		ELSE
			CALLF MAKE_STR(V_NAME, "anti material rifle")
		ENDIF
	CASE "描写"
		CALLF MAKE_STR(V_NAME,"거인, 차량 엔진, 그리고 엄폐물 안팎의 보병을 제압하는 데 적합한 강력하고 무거운 소총이다. 오퍼레이터는 개조된 브레넌 LRS-46 설계를 따르며 장전하기 쉽고, 격발할 수 있다.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Tech Level"
		LOCAL = 8
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grade"
		LOCAL = 2
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Price"
		LOCAL = 1400000
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Magazine Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Body Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 3
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Stock Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 124)
	CASE "WeaponBarrelBody/Default"
		CALLF MAKE_INT(V_NAME, 106)
	CASE "WeaponMag/Default"
		CALLF MAKE_INT(V_NAME, 140)
	CASE "WeaponStock/Default"
		CALLF MAKE_INT(V_NAME, 62)
	CASE "WeaponSight/Default"
		CALLF MAKE_INT(V_NAME, 25)
	CASE "Two-Handed"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "PrecisionRifling/")
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Firearm")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Rifle")
	CASE "Platform"
		CALLF MAKE_STR(V_NAME, "brennan/")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"kappa-arm/military/modern/native/sniper rifle/anti tank/")
		;旧装備処理との照会用、Weaponの1
ENDSELECT

@Weapon413(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "LAC33")
		ELSE
			CALLF MAKE_STR(V_NAME, "self loading rifle")
		ENDIF
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "LUNINCO Semi-Automatic Rifle「LAC33」")
		ELSE
			CALLF MAKE_STR(V_NAME, "self loading rifle")
		ENDIF
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "구형 제국 소총에 사용되던 6.5x50mmSR 대신 표준 7.62x51mm NATO를 채택한, 폐기된 하루 타입 C 자동장전 소총의 개조 복각품이다. 반자동이며 10발 탄창으로 급탄한다.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Tech Level"
		LOCAL = 7
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grade"
		LOCAL = 2
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Price"
		LOCAL = 60000
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Body Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 3
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Magazine 	Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Stock Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 125)
	CASE "WeaponBarrelBody/Default"
		CALLF MAKE_INT(V_NAME, 107)
	CASE "WeaponStock/Default"
		CALLF MAKE_INT(V_NAME, 44)
	CASE "WeaponMag/Default"
		CALLF MAKE_INT(V_NAME, 141)
	CASE "Two-Handed"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "PrecisionRifling/")
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Firearm")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Rifle")
	CASE "Platform"
		CALLF MAKE_STR(V_NAME, "typeHei/")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"lunarian/civilian/military/world war/cold war/modern/native/")
		;旧装備処理との照会用、Weaponの1
ENDSELECT

@Weapon414(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "Banhiken")
		ELSE
			CALLF MAKE_STR(V_NAME, "tanegashima")
		ENDIF
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "Akatsuki Tanegashima「Banhiken」")
		ELSE
			CALLF MAKE_STR(V_NAME, "tanegashima")
		ENDIF
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "전국 시대에 대규모 창병, 궁수, 그리고 결국 조총 부대 형성과 함께 전쟁이 기하급수적으로 변화하면서, 봉건 일본 군대의 철포군(화력 부대)을 장비하기 위해 많은 양의 화기가 필요했고 생산되었다. 이 화기는 주로 아시가루들이 사용했기 때문에 품질이 낮았으며, 다시 사용할 수 있는 곳인 병기고에 보관되었다.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Tech Level"
		LOCAL = 5
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grade"
		LOCAL = 0
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Price"
		LOCAL = 22000
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Body Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Stock Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 127)
	CASE "WeaponBarrelBody/Default"
		CALLF MAKE_INT(V_NAME, 108)
	CASE "WeaponStock/Default"
		CALLF MAKE_INT(V_NAME, 64)
	CASE "Two-Handed"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "Gunpowder/")
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Firearm")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Rifle")
	CASE "Platform"
		CALLF MAKE_STR(V_NAME, "tanegashima/")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"akatsuki/civilian/military/medieval/musket/")
		;旧装備処理との照会用、Weaponの1
ENDSELECT

@Weapon415(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "Eihei")
		ELSE
			CALLF MAKE_STR(V_NAME, "flintlock repeater")
		ENDIF
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "Akatsuki Revolving Flintlock Rifle「Eihei」")
		ELSE
			CALLF MAKE_STR(V_NAME, "flintlock repeater")
		ENDIF
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "암살의 길게 늘인 버전인 에이헤이나 '센티넬'은 플린트락 리볼버를 근거로 하는 겐소키안 복제품이다. 최초의 생산된 회전식 화기를 기반으로, 에이헤이는 플린트락 발사 메커니즘을 사용하며 사용자가 5발짜리 실린더를 수동으로 인덱싱하도록 요구한다.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Tech Level"
		LOCAL = 4
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grade"
		LOCAL = 0
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Price"
		LOCAL = 60000
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Body Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Stock Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Magazine Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 111)
	CASE "WeaponBarrelBody/Default"
		CALLF MAKE_INT(V_NAME, 109)
	CASE "WeaponStock/Default"
		CALLF MAKE_INT(V_NAME, 64)
	CASE "WeaponMag/Default"
		CALLF MAKE_INT(V_NAME, 129)
	CASE "Two-Handed"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "Gunpowder/")
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Firearm")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Rifle")
	CASE "Platform"
		CALLF MAKE_STR(V_NAME, "collier/")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"akatsuki/civilian/military/medieval/musket/")
		;旧装備処理との照会用、Weaponの1
ENDSELECT

@Weapon416(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "Youjyuu-C")
		ELSE
			CALLF MAKE_STR(V_NAME, "Recharger charge rifle")
		ENDIF
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "Keiga Youjutsu Series Charge Rifle「Youjyuu-C」")
		ELSE
			CALLF MAKE_STR(V_NAME, "Recharger charge rifle")
		ENDIF
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "유주-C는 브레넌 LRS 시리즈, 게이브 허버트 GH91/SG1, 그리고 밤베토프와 같은 무기에 상응하는 유술이다. 사용자의 유술 정수를 사용하여 빠르고 정확한 탄환 몇 발을 가속시킨다.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Grade"
		LOCAL = 3
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Price"
		LOCAL = 770000
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Foregrip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Magazine Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Body Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 3
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Stock Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tech Level"
		LOCAL = 8
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Damage Type"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 128)
	CASE "WeaponGrip/Default"
		CALLF MAKE_INT(V_NAME, 4)
	CASE "WeaponBarrelBody/Default"
		CALLF MAKE_INT(V_NAME, 100)
	CASE "WeaponSight/Default"
		CALLF MAKE_INT(V_NAME, 20)
	CASE "Two-Handed"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Anti-Theft Strength"
		;Strength of the anti-theft system in Knowledge ranks
		CALLF MAKE_INT(V_NAME, 2)
	CASE "Anti-Theft Message"
		;flavor text function
		CALLF MAKE_STR(V_NAME, "WeaponAntiTheftYomihari")
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "FireMagic/ProfessionalMagicWeapons/")
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Youjutsu")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Rifle")
	CASE "Fire Sound"
		CALLF MAKE_STR(V_NAME, "firearms/AT MR-92.wav")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"keiga/military/modern/native/rifle/assault rifle/")
		;旧装備処理との照会用、Weaponの1
ENDSELECT

@Weapon417(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "LAC141")
		ELSE
			CALLF MAKE_STR(V_NAME, "charge lance")
		ENDIF
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "LUNINCO Charge Lance「LAC141」")
		ELSE
			CALLF MAKE_STR(V_NAME, "charge lance")
		ENDIF
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "Designed as a personal \"anti-spaceship\" weapon, the LAC141 fires a supercharged beam of light that can fire and hit from kilometers away. Infamous for shooting down many Seihou craft during its still active tenure. As an anti-personal weapon, it is devistating against those with and without armor.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Tech Level"
		LOCAL = 9
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grade"
		LOCAL = 5
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Price"
		LOCAL = 1630000
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Body Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 3
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Magazine Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Stock Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 131)
	CASE "WeaponBarrelBody/Default"
		CALLF MAKE_INT(V_NAME, 111)
	CASE "WeaponStock/Default"
		CALLF MAKE_INT(V_NAME, 4)
	CASE "WeaponGrip/Default"
		CALLF MAKE_INT(V_NAME, 4)
	CASE "WeaponMag/Default"
		CALLF MAKE_INT(V_NAME, 146)
	CASE "Two-Handed"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Anti-Theft Strength"
		;Strength of the anti-theft system in Knowledge ranks
		CALLF MAKE_INT(V_NAME, 5)
	CASE "Anti-Theft Message"
		;flavor text function
		CALLF MAKE_STR(V_NAME, "WeaponAntiTheftLACTite")
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "ChargedShot/")
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Firearm")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Rifle")
	CASE "Platform"
		CALLF MAKE_STR(V_NAME, "lac141/")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"lunarian/millitary/spacer/native/anti-tank/")
		;旧装備処理との照会用、Weaponの1
ENDSELECT
;オブジェクト本体

@Weapon418(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "LLR")
		ELSE
			CALLF MAKE_STR(V_NAME, "less-lethal launcher")
		ENDIF
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "High-Ground Less Lethal Rifle「LLR」")
		ELSE
			CALLF MAKE_STR(V_NAME, "less-lethal launcher")
		ENDIF
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "LLR은 하이그라운드 암즈(High Ground Arms)에서 설계 및 제조한 준자동 비치사 진압용 총기이다. LLR은 압축 공기를 사용하여 15발짜 드럼 탄창에서 투사체를 발사한다. 이 총기는 치명적인 부상을 입히지 않고 타겟을 둔기로 무력화시키도록 설계되었으며, 치명적인 무기가 피되어야 하는 진압 통제 및 기타 종류의 전투에 가장 널리 사용된다.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Damage"
		; default damage in a normal hit
		LOCAL = 1190
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Accuracy"
		;	chance to HIT, 0-100%
		LOCAL = 50
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Burst"
		LOCAL = 2
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Pellets"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Loudness"
		LOCAL = 4
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Mag Size"
		LOCAL = 15
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Penetration"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Range"
		LOCAL = 7
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grade"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tech Level"
		LOCAL = 8
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "Airgun/")
	CASE "Non Lethal"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Price"
		LOCAL = 600000
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 3
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Two-Handed"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Firearm")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Rifle")
	CASE "Ammo"
		CALLF MAKE_STR(V_NAME, "17.3mm")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"hga/civilian/military/western/modern/assault rifle/non lethal/airgun/")
		;旧装備処理との照会用、Weaponの1
ENDSELECT

@Weapon419(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "RAIL-IT")
		ELSE
			CALLF MAKE_STR(V_NAME, "railgun")
		ENDIF
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "Cactus Company Railgun「RAIL-IT」")
		ELSE
			CALLF MAKE_STR(V_NAME, "railgun")
		ENDIF
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "The Rail Assisted Item Launcher (the IT part doesn't mean anything other than its the sniper model) launches 20mm spikes from a rail using electromagnetic force. It is breach loaded and requires the operator to open the breach, drop the spike in, and close the breech to load the weapon. Commonly referred to as a \"railgun\", the RAIL-IT can drive its 20mm projectile at over 3 km/s accurately. Combined with its cheap to manufacture ammunition and it only requiring Midworld-era electronics to build-resulting in a low cost for a Seihou weapon, the RAIL-IT is one of the main sniper rifles used throughout the Seihou World.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Grade"
		LOCAL = 5
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tech Level"
		LOCAL = 9
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "ChargedShot/")
	CASE "Grip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Foregrip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Body Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 3
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Stock Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Quest Only"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Price"
		LOCAL = 35000000
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 133)
	CASE "WeaponBarrelBody/Default"
		CALLF MAKE_INT(V_NAME, 113)
	CASE "WeaponSight/Default"
		CALLF MAKE_INT(V_NAME, 27)
	CASE "WeaponStock/Default"
		CALLF MAKE_INT(V_NAME, 61)
	CASE "WeaponGrip/Default"
		CALLF MAKE_INT(V_NAME, 4)
	CASE "Two-Handed"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Anti-Theft Strength"
		;Strength of the anti-theft system in Knowledge ranks
		CALLF MAKE_INT(V_NAME, 5)
	CASE "Anti-Theft Message"
		;flavor text function
		CALLF MAKE_STR(V_NAME, "WeaponAntiTheftSAGS")
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Firearm")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Rifle")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"cactus-comp/military/western/spacer/anti tank/")
ENDSELECT

@Weapon420(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "M281")
		ELSE
			CALLF MAKE_STR(V_NAME, "Chicago typewriter")
		ENDIF
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "Broughton Chicago Typewriter「M281」")
		ELSE
			CALLF MAKE_STR(V_NAME, "Chicago typewriter")
		ENDIF
	CASE "描写"
		CALLF MAKE_STR(V_NAME,"The M280 series of sub-machine guns, also known as the \"Chicago typewriter\", are legendary sub-machine guns used in the innerwar period. The M281 is a millitary contracted version of the M210 with a slight amount of simplfications for broader military use such as the lack of vetical grip. A further simplified version of the M281 would be used as the basis for the M1 sub-machine gun. The M281 fires at a steady 600 rounds of .45 ABP per minute, providing sufficient stopping power and trench clearing ability. Ironicly marketed as \"The gun that safeguards the innocent!\", a fully automatic weapon with a good amount of recoil isn't exactly the safest way to ensure no innocent bystanders are hurt.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Tech Level"
		LOCAL = 7
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grade"
		LOCAL = 2
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Price"
		LOCAL = 380000
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Magazine Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Body Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 3
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Stock Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 132)
	CASE "WeaponBarrelBody/Default"
		CALLF MAKE_INT(V_NAME, 112)
	CASE "WeaponMag/Default"
		CALLF MAKE_INT(V_NAME, 147)
	CASE "WeaponStock/Default"
		CALLF MAKE_INT(V_NAME, 66)
	CASE "WeaponGrip/Default"
		CALLF MAKE_INT(V_NAME, 40)
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "BlowbackOperation/PrecisionRifling/")
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Firearm")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Rifle")
	CASE "Platform"
		CALLF MAKE_STR(V_NAME, "thompson/")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"broughton/military/world war/cold war/modern/western/carbine/")
		;旧装備処理との照会用、Weaponの1
ENDSELECT

@Weapon421(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "Ender")
		ELSE
			CALLF MAKE_STR(V_NAME, "ultratech railgun")
		ENDIF
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "Gensou-chan Railgun「Ender」")
		ELSE
			CALLF MAKE_STR(V_NAME, "ultratech railgun")
		ENDIF
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "The \"Ender\" is a nickname to Gensou-chan Global's ultratech railgun. A Youjutsu-ification of the Cactus Company RAIL system, the Ender uses Youjutsu energy to drive spikes down kilometers away, penetrating any and every kind of armor avaliable. The NAIVE targeting computer and ALSA aim assist are also installed on the weapon, allowing for accurate shots from very far away. Anyone who gets on the wrong side of an Ender is most likely going to end up as a pin cushion.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Grade"
		LOCAL = 6
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tech Level"
		LOCAL = 10
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "ChargedShot/")
	CASE "Grip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Foregrip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Body Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 3
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Stock Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Price"
		LOCAL = 35000000
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 134)
	CASE "WeaponBarrelBody/Default"
		CALLF MAKE_INT(V_NAME, 114)
	CASE "WeaponSight/Default"
		CALLF MAKE_INT(V_NAME, 29)
	CASE "WeaponTactical/Default"
		CALLF MAKE_INT(V_NAME, 14)
	CASE "WeaponStock/Default"
		CALLF MAKE_INT(V_NAME, 19)
	CASE "WeaponGrip/Default"
		CALLF MAKE_INT(V_NAME, 5)
	CASE "WeaponAmmo/Default"
		CALLF MAKE_INT(V_NAME, 92)
	CASE "Two-Handed"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Anti-Theft Strength"
		;Strength of the anti-theft system in Knowledge ranks
		CALLF MAKE_INT(V_NAME, 8)
	CASE "Anti-Theft Message"
		;flavor text function
		CALLF MAKE_STR(V_NAME, "WeaponAntiTheftSWH")
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Firearm")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Rifle")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"gensou-chan-gun/military/western/anti tank/")
ENDSELECT

@Weapon422(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "LAC1")
		ELSE
			CALLF MAKE_STR(V_NAME, "laser rifle")
		ENDIF
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "LUNINCO Laser Rifle「LAC1」")
		ELSE
			CALLF MAKE_STR(V_NAME, "laser rifle")
		ENDIF
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "모든 임페리얼 소총에 사용할 수 있는 드롭인 리시버, 플래시 튜브 및 배터리 키트이다. 국영 기업인 LUNINCO에서 생산하는 LUNINCO Lumina 모델 코드 LAC1은 가열된 열 빔을 사용하여 방탄 장비를 관통하는 완전 자동 소총이다. 대기가 극도로 희박한 달에서는 효과적이지만, 지구와 마카이에서는 좋은 탄도 결과를 제공하지 못한다.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Tech Level"
		LOCAL = 9
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grade"
		LOCAL = 3
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Price"
		LOCAL = 1630000
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Body Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 3
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Magazine Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Stock Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 86)
	CASE "WeaponBarrelBody/Default"
		CALLF MAKE_INT(V_NAME, 68)
	CASE "WeaponStock/Default"
		CALLF MAKE_INT(V_NAME, 44)
	CASE "WeaponMag/Default"
		CALLF MAKE_INT(V_NAME, 106)
	CASE "Two-Handed"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Anti-Theft Strength"
		;Strength of the anti-theft system in Knowledge ranks
		CALLF MAKE_INT(V_NAME, 5)
	CASE "Anti-Theft Message"
		;flavor text function
		CALLF MAKE_STR(V_NAME, "WeaponAntiTheftLACTite")
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "ChargedShot/")
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Firearm")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Rifle")
	CASE "Platform"
		CALLF MAKE_STR(V_NAME, "Arisaka/lac1/")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"lunarian/millitary/spacer/native/assault rifle/")
		;旧装備処理との照会用、Weaponの1
ENDSELECT

@Weapon423(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "LILAC-ARM")
		ELSE
			CALLF MAKE_STR(V_NAME, "arm-mounted energy rifle")
		ENDIF
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "Cactus Company Arm-Mounted Energy Rifle「LILAC-ARM」")
		ELSE
			CALLF MAKE_STR(V_NAME, "arm-mounted energy rifle")
		ENDIF
	CASE "描写"
		;CALLF MAKE_STR(V_NAME, "As a secondary weapon for VIVIT-based androids, the SideSTRAP, aka the Side Supercharged Thermal Rapidly Accelerated Projectile, is a pistol designed for extravehicular activities and ground-based combat. .")
		CALLF MAKE_STR(V_NAME,"The LILAC-ARM, an abbreviation of \"Light Infantry Loaded Accelerator Carbine - Arm Rail Mount\", is an assault rifle mounted and concealed on the arms of VIVIT androids. Shots are automatically charged by a battery in the magazine and use cactus energy powder and primers that are punctured to release and propel the projectile faster than regular gunpowder. It is capable of piercing even advanced armor systems such as their own Scout power armor.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Tech Level"
		LOCAL = 9
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grade"
		LOCAL = 4
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Price"
		LOCAL = 1350000
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Magazine Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Body Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 3
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Stock Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 122)
	CASE "WeaponBarrelBody/Default"
		CALLF MAKE_INT(V_NAME, 104)
	CASE "WeaponMag/Default"
		CALLF MAKE_INT(V_NAME, 139)
	CASE "WeaponStock/Default"
		CALLF MAKE_INT(V_NAME, 61)
	CASE "WeaponGrip/Default"
		CALLF MAKE_INT(V_NAME, 4)
	CASE "Concealed Weapon"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Quest Only"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "ChargedShot/")
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Firearm")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Rifle")
	CASE "Platform"
		CALLF MAKE_STR(V_NAME, "cactuslilac/")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"cactus-comp/closed-bolt/military/spacer/assault rifle/")
		;旧装備処理との照会用、Weaponの1
ENDSELECT

@Weapon424(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "AG-15 BIS")
		ELSE
			CALLF MAKE_STR(V_NAME, "auto railgun")
		ENDIF
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "Gryzov Group Assault Railgun「AG-15 BIS」")
		ELSE
			CALLF MAKE_STR(V_NAME, "auto railgun")
		ENDIF
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "장애물을 관통하는 레일에서 사보트를 발사하는 돌격소총이다. 극도로 긴 유효 사거리와 매우 높은 정지력 및 관통력을 자랑한다. 자동 조준 컴퓨터, 홀로사이트, 그리고 레일 충전기가 완비되어 있다. 그랴조프 그룹의 AG-15를 기반으로 스페이서용으로 개조한 것이다.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Grade"
		LOCAL = 8
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tech Level"
		LOCAL = 9
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "ChargedShot/")
	CASE "Grip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Foregrip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 3
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Stock Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Magazine Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Quest Only"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Price"
		LOCAL = 35000000
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 130)
	CASE "WeaponSight/Default"
		CALLF MAKE_INT(V_NAME, 31)
	CASE "WeaponStock/Default"
		CALLF MAKE_INT(V_NAME, 18)
	CASE "WeaponGrip/Default"
		CALLF MAKE_INT(V_NAME, 10)
	CASE "WeaponMag/Default"
		CALLF MAKE_INT(V_NAME, 145)
	CASE "Two-Handed"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Anti-Theft Strength"
		;Strength of the anti-theft system in Knowledge ranks
		CALLF MAKE_INT(V_NAME, 5)
	CASE "Anti-Theft Message"
		;flavor text function
		CALLF MAKE_STR(V_NAME, "WeaponAntiTheftSAGS")
	CASE "Platform"
		CALLF MAKE_STR(V_NAME, "AK-12/AK/")
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Firearm")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Rifle")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"gryzov/military/western/spacer/")
ENDSELECT
;Handgonne

@Weapon425(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, "handgonne")
	CASE "FullName"
		CALLF MAKE_STR(V_NAME, "handgonne")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "휴대 가능한 화기 중 가장 오래되고 단순한 형태일 수 있는 핸드건은 안전한 작동을 위해 막대에 올려놓고 사용하는 총구 장전식 화기이다. 발사 메커니즘 없이 터치 홀을 통해 직접 손으로 외부 점화가 필요하다.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Tech Level"
		LOCAL = 4
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grade"
		LOCAL = 0
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Price"
		LOCAL = 8000
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Body Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Grip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 156)
	CASE "WeaponGrip/Default"
		CALLF MAKE_INT(V_NAME, 32)
	CASE "Two-Handed"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "Gunpowder/")
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Firearm")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Rifle")
	CASE "Platform"
		CALLF MAKE_STR(V_NAME, "Polearm/")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"civilian/military/medieval/musket/")
		;旧装備処理との照会用、Weaponの1
ENDSELECT

@Weapon426(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "Ender+")
		ELSE
			CALLF MAKE_STR(V_NAME, "machine railgun")
		ENDIF
	CASE "FullName"
		IF GETBIT(nVerboseItems,1)
			CALLF MAKE_STR(V_NAME, "Gensou-chan Machine Railgun「Ender+」")
		ELSE
			CALLF MAKE_STR(V_NAME, "machine railgun")
		ENDIF
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "엔더+는 센소우짱의 기존 무기 개발의 정점이다. 이미 악명 높은 엔더 레일건의 영적 능력 기반 기관총 변종으로, 거의 기술 없이도 무엇이든 제압한다. 여기서는 장전할 필요가 없으며, 영적 에센스를 사용해 발사하기만 하면 된다. 이 총은 모든 소총이 갖춰야 할 모든 것을 수행한다: 모든 것에 대한 관통력, 가벼운 무게, 고폭탄을 포함한 탄약 교체의 용이성, 그리고 목표가 회피하지 않는 한 빗나갈 수 없게 만드는 스마트 타겟팅 기능이다.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「Weapon」")
	CASE "Grade"
		LOCAL = 6
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tech Level"
		LOCAL = 11
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Research Required"
		CALLF MAKE_STR(V_NAME, "ChargedShot/")
	CASE "Grip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Foregrip Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Receiver Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Barrel Body Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Tactical Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Sight Slot"
		LOCAL = 3
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Stock Slot"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Quest Only"
		LOCAL = 1
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "Price"
		LOCAL = 35000000
		CALLF MAKE_INT(V_NAME, LOCAL)
	CASE "WeaponReceiver/Default"
		CALLF MAKE_INT(V_NAME, 179)
	CASE "WeaponBarrelBody/Default"
		CALLF MAKE_INT(V_NAME, 145)
	CASE "WeaponSight/Default"
		CALLF MAKE_INT(V_NAME, 29)
	CASE "WeaponTactical/Default"
		CALLF MAKE_INT(V_NAME, 14)
	CASE "WeaponStock/Default"
		CALLF MAKE_INT(V_NAME, 19)
	CASE "WeaponGrip/Default"
		CALLF MAKE_INT(V_NAME, 5)
	CASE "WeaponAmmo/Default"
		CALLF MAKE_INT(V_NAME, 92)
	CASE "Two-Handed"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Anti-Theft Strength"
		;Strength of the anti-theft system in Knowledge ranks
		CALLF MAKE_INT(V_NAME, 10)
	CASE "Anti-Theft Message"
		;flavor text function
		CALLF MAKE_STR(V_NAME, "WeaponAntiTheftSWH")
	CASE "Type"
		CALLF MAKE_STR(V_NAME, "Firearm")
	CASE "Class"
		CALLF MAKE_STR(V_NAME, "Rifle")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"gensou-chan-gun/military/western/anti tank/")
ENDSELECT
; @WeaponPreset69(ARG, O_DATA, V_NAME)
; #FUNCTION
; #LOCALSIZE 1
; #LOCALSSIZE 1
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "Name"
; 	IF GETBIT(nVerboseItems,3)
; 		CALLF MAKE_STR(V_NAME, "AR-18")
; 	ELSEIF GETBIT(nVerboseItems,1)
; 		CALLF MAKE_STR(V_NAME, "SPA")
; 	ELSE
; 		CALLF MAKE_STR(V_NAME, "assault rifle")
; 	ENDIF
; CASE "FullName"
; 	IF GETBIT(nVerboseItems,3)
; 	    CALLF MAKE_STR(V_NAME, "ArmaLite AR-18 Automatic Rifle")
; 	ELSEIF GETBIT(nVerboseItems,1)
; 		CALLF MAKE_STR(V_NAME, "ArmTeck \"SPA\" Short-Piston-Automatic Rifle")
; 	ELSE
; 		CALLF MAKE_STR(V_NAME, "assault rifle")
; 	ENDIF
; CASE "Description"
; 	CALLF MAKE_STR(V_NAME,"Another pioneer of the Midworld automatic rifle, the SPA is a gas-operated automatic rifle using the aformentioned short-piston system. Hoping to capture the third-world market as an alternative to the more expensive Broughton M602-series assault rifles, ArmTeck was set for a disappointment when the U.S. Government commissioned, bought, and then gave away hundreds of thousands of Broughton M900s during the course of the 1960s and 1970s (to stem the tide of Communists arming \"their side\" with free weapons). Although never adopted officially by any armed forces, it did somewhat serve as a testbed for weapons such as the SA80, MRX, GH233, Type 89 etc.")
; CASE "装備部位"
; 	CALLF MAKE_STR(V_NAME, "「WeaponPreset」")
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, @"military/cold war/modern/western/assault rifle/")
; CASE "Platform"
; 	CALLF MAKE_STR(V_NAME, "AR-18/")
; CASE "Manufacturer"
; 	CALLF MAKE_STR(V_NAME, @"\@ GETBIT(nVerboseItems,3) ? armalite # armteck \@")
; CASE "Base Weapon"
; 	CALLF MAKE_INT(V_NAME, 401)
; CASE "WeaponReceiver/Required"
; 	CALLF MAKE_INT(V_NAME, 51)
; CASE "WeaponBarrelBody/Required"
; 	CALLF MAKE_INT(V_NAME, 36)
; CASE "WeaponMag/Default"
; 	CALLF MAKE_INT(V_NAME, 1)
; ENDSELECT
;The Lawyer is the most powerful of the three shotguns offered by the Youkai Mountain Kappa Armory (YMKA), Fed by regular 12 gauge shells in an 8-round magazine, its full-auto capabilities mean business in close-quarter battles. Definitely not based off the Kabarga-83 from the Renov Manufacturing Plant
;@EXIST_Weapon127
; #LOCALSIZE 1
; #LOCALSSIZE 1
; ;オブジェクト本体
; @Weapon127(ARG, O_DATA, V_NAME)
; #FUNCTION
; #LOCALSIZE 1
; #LOCALSSIZE 1
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	IF GETBIT(nVerboseItems,1)
; 		CALLF MAKE_STR(V_NAME, "GC Pro Spray")
; 	ELSE
; 		CALLF MAKE_STR(V_NAME, "Pressure Washer")
; 	ENDIF
; CASE "FullName"
; 	IF GETBIT(nVerboseItems,1)
; 		CALLF MAKE_STR(V_NAME, "Gensou-chan Pro Spray Water Gun")
; 	ELSE
; 		CALLF MAKE_STR(V_NAME, "Pressure Washer")
; 	ENDIF
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "A innocent water gun, uses a high-pressure air system to spray water against a target")
; CASE "装備部位"
; 	CALLF MAKE_STR(V_NAME, "「Weapon」")
; CASE "Damage"
; 	; default damage in a normal hit
; 	LOCAL = 100
; 	CALLF MAKE_INT(V_NAME, LOCAL)
; CASE "Accuracy"
; 	;	chance to HIT, 0-100%
; 	LOCAL = 10
; 	CALLF MAKE_INT(V_NAME, LOCAL)
; CASE "Burst"
; 	LOCAL = 30
; 	CALLF MAKE_INT(V_NAME, LOCAL)
; CASE "Pellets"
; 	LOCAL = 1
; 	CALLF MAKE_INT(V_NAME, LOCAL)
; CASE "Loudness"
; 	LOCAL = 0
; 	CALLF MAKE_INT(V_NAME, LOCAL)
; CASE "Mag Size"
; 	LOCAL = 240
; 	CALLF MAKE_INT(V_NAME, LOCAL)
; CASE "Range"
; 	LOCAL = 1
; 	CALLF MAKE_INT(V_NAME, LOCAL)
; CASE "Penetration"
; 	LOCAL = 1
; 	CALLF MAKE_INT(V_NAME, LOCAL)
; CASE "Grade"
; 	LOCAL = 0
; 	CALLF MAKE_INT(V_NAME, LOCAL)
; CASE "Tech Level"
; 	LOCAL = 8
; 	CALLF MAKE_INT(V_NAME, LOCAL)
; CASE "Research Required"
; 	CALLF MAKE_STR(V_NAME, "Electricity/")
; CASE "Price"
; 	LOCAL = 50000
; 	CALLF MAKE_INT(V_NAME, LOCAL)
; CASE "Grip Slot"
; 	LOCAL = 1
; 	CALLF MAKE_INT(V_NAME, LOCAL)
; CASE "Foregrip Slot"
; 	LOCAL = 1
; 	CALLF MAKE_INT(V_NAME, LOCAL)
; CASE "Receiver Slot"
; 	LOCAL = 1
; 	CALLF MAKE_INT(V_NAME, LOCAL)
; CASE "Tactical Slot"
; 	LOCAL = 1
; 	CALLF MAKE_INT(V_NAME, LOCAL)
; CASE "Magazine Slot"
; 	LOCAL = 1
; 	CALLF MAKE_INT(V_NAME, LOCAL)
; CASE "Sight Slot"
; 	LOCAL = 1
; 	CALLF MAKE_INT(V_NAME, LOCAL)
; CASE "Non Lethal"
; 	LOCAL = 1
; 	CALLF MAKE_INT(V_NAME, LOCAL)
; CASE "Type"
; 	CALLF MAKE_STR(V_NAME, "Firearm")
; CASE "Class"
; 	CALLF MAKE_STR(V_NAME, "Rifle")
; CASE "Ammo"
; 	CALLF MAKE_STR(V_NAME, "Liquid")
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "gensou-chan/civilian/soviet/modern/machine gun/joke")
; ;旧装備処理との照会用、Weaponの1
; ENDSELECT
