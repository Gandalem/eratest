;=============================================================================================================
; Outerwear
;=============================================================================================================
; @EXIST_上半身上着_はだけ不可20
; #LOCALSIZE 1
; #LOCALSSIZE 1

; @上半身上着_はだけ不可20(ARG, O_DATA, V_NAME)
; #FUNCTION
; #LOCALSIZE 1
; #LOCALSSIZE 1
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	IF GETBIT(nVerboseItems,2)
; 		CALLF MAKE_STR(V_NAME, "CeleTechΔ operator coat")
; 	ELSE
; 		CALLF MAKE_STR(V_NAME, "spacer operator coat")
; 	ENDIF
; CASE "FullName"
; 	IF GETBIT(nVerboseItems,2)
; 		CALLF MAKE_STR(V_NAME, "CeleTechΔ operator coat")
; 	ELSE
; 		CALLF MAKE_STR(V_NAME, "spacer operator coat")
; 	ENDIF
; CASE "FullDesc"
; 	CALLF MAKE_STR(V_NAME, @"A combat coat designed for CMC Operators, it has in-built optical camouflage device and a nano-shield to maximize wearer's survivability.")			
; CASE "装備部位"
; 	CALLF MAKE_STR(V_NAME, "「上半身上着_はだけ不可」")
; CASE "Armor"
; 	CALLF MAKE_INT(V_NAME, 12)
; CASE "Armor Health"
; 	CALLF MAKE_INT(V_NAME, 12000)
; CASE "Armor Coverage"
; 	CALLF MAKE_STR(V_NAME, "40/53/61/20/30/81/91")	
; CASE "Price"
; 	CALLF MAKE_INT(V_NAME, 1230000)
; CASE "Tags"
;     CALLF MAKE_STR(V_NAME, "cmc/")
; ENDSELECT
