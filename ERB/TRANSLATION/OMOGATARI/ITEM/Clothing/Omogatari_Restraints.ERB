;=============================================================================================================
; Restraints
;=============================================================================================================

;============================================
; Crotch Restraints
;============================================
@EXIST_RestraintCrotch_ID
#LOCALSIZE 1
#LOCALSSIZE 1

;GET_INT(C_ID, "Weapon", P_ID, "Accuracy")

;要素名→ID変換用関数
;必要なら各オブジェクトで同様に作成のこと
@RestraintCrotch_ID(ARG:0, ARGS:0, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS V_NAME                  ;作成する変数名
#DIMS C_NAME, OBJ_ID_LAST + 1 ;データ保持
#DIM  O_ID                    ;参照用
IF ARGS:0 == "RESET"
	VARSET C_NAME
	FOR O_ID, 1, OBJ_ID_LAST + 1
		C_NAME:O_ID = %GET_STR(, "RestraintCrotch", O_ID, "名前")%
	NEXT
ELSE
	O_ID = FINDELEMENT(C_NAME, ESCAPE(ARGS:0), , , 1)
	CALLF MAKE_INT(V_NAME, O_ID)
ENDIF

@RestrainCrotch1(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "chastity cage")
CASE "FullDesc"
	CALLF MAKE_STR(V_NAME, @"What's more lovely that a little peepee stuck in a lovely, restrictive but smooth, lightweight and breathable cage, aiming downward%COND_STR(@" forcing the user to sit to pee",nPee)%, and leaving absolutely no bulge in one's favorite underwear!")		
CASE "装備部位"
	CALLF MAKE_STR(V_NAME, "「RestraintCrotch」")
CASE "Chastity"
	CALLF MAKE_INT(V_NAME, 1)
CASE "Tags"
	CALLF MAKE_STR(V_NAME, "safe virtue/")
ENDSELECT


;https://lockthecock.com/products/steel-female-chastity-belt
@RestrainCrotch2(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "chastity belt")
CASE "FullDesc"
	CALLF MAKE_STR(V_NAME, @"Keeps curious ones out of touching their penis and/or vagina. Fits easily and has a hole for uninterrupted toilet use.")		
CASE "装備部位"
	CALLF MAKE_STR(V_NAME, "「RestraintCrotch」")
CASE "Chastity"
	CALLF MAKE_INT(V_NAME, 39)
CASE "Tags"
	CALLF MAKE_STR(V_NAME, "safe virtue/")
ENDSELECT

;============================================
; Mouth Restraints
;============================================
@EXIST_RestrainMouth_ID
#LOCALSIZE 1
#LOCALSSIZE 1

;GET_INT(C_ID, "Weapon", P_ID, "Accuracy")

;要素名→ID変換用関数
;必要なら各オブジェクトで同様に作成のこと
@RestrainMouth_ID(ARG:0, ARGS:0, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS V_NAME                  ;作成する変数名
#DIMS C_NAME, OBJ_ID_LAST + 1 ;データ保持
#DIM  O_ID                    ;参照用
IF ARGS:0 == "RESET"
	VARSET C_NAME
	FOR O_ID, 1, OBJ_ID_LAST + 1
		C_NAME:O_ID = %GET_STR(, "RestrainMouth", O_ID, "名前")%
	NEXT
ELSE
	O_ID = FINDELEMENT(C_NAME, ESCAPE(ARGS:0), , , 1)
	CALLF MAKE_INT(V_NAME, O_ID)
ENDIF

@RestrainMouth1(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "pacifier")
CASE "FullDesc"
	CALLF MAKE_STR(V_NAME, @"A stuffable nipple substitute given to an baby, toddler, or even adult to suckle to satisfy the wearer's need to suck.\nIt functions to silence the wearer from speaking and casting Youjutsu spells, as well as humiliating them. Often used during punishments to prevent escape via Youjutsu.")

	;CALLF MAKE_STR(V_NAME, @"A stuffable nipple substitute given to an baby, toddler, or even adult to suckle to satisfy the wearer's need to suck.\nIt functions to silence the wearer from speaking, humiliating them, and reminds the wearer of their slave/bab status and psychologically suppresses them and calms them down, reducing the chance of a slave rebellion and/or mental break.")		
CASE "Chastity"
	CALLF MAKE_INT(V_NAME, 16)
CASE "装備部位"
	CALLF MAKE_STR(V_NAME, "「RestrainMouth」")
CASE "Tags"
	CALLF MAKE_STR(V_NAME, "re:dl/")
ENDSELECT

@RestrainMouth2(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "ball gag")
CASE "FullDesc"
	CALLF MAKE_STR(V_NAME, @"A rubber gag that blocks the victim from speaking. This also blocks Youjutsu casting.")
CASE "Chastity"
	CALLF MAKE_INT(V_NAME, 16)
CASE "装備部位"
	CALLF MAKE_STR(V_NAME, "「RestrainMouth」")
CASE "Tags"
	CALLF MAKE_STR(V_NAME, "safe virtue/")
ENDSELECT

;============================================
; Arm Restraints
;============================================
@EXIST_RestrainArm_ID
#LOCALSIZE 1
#LOCALSSIZE 1

;GET_INT(C_ID, "Weapon", P_ID, "Accuracy")

;要素名→ID変換用関数
;必要なら各オブジェクトで同様に作成のこと
@RestrainArm_ID(ARG:0, ARGS:0, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS V_NAME                  ;作成する変数名
#DIMS C_NAME, OBJ_ID_LAST + 1 ;データ保持
#DIM  O_ID                    ;参照用
IF ARGS:0 == "RESET"
	VARSET C_NAME
	FOR O_ID, 1, OBJ_ID_LAST + 1
		C_NAME:O_ID = %GET_STR(, "RestrainArm", O_ID, "名前")%
	NEXT
ELSE
	O_ID = FINDELEMENT(C_NAME, ESCAPE(ARGS:0), , , 1)
	CALLF MAKE_INT(V_NAME, O_ID)
ENDIF

@RestrainArm1(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "handcuffs")
CASE "FullDesc"
	CALLF MAKE_STR(V_NAME, @"Restraint devices designed to secure an individual's wrists in proximity to each other. A handcuffed person cannot move their wrists more than a few centimetres or inches apart, making many tasks difficult or impossible. ")
CASE "Chastity"
	CALLF MAKE_INT(V_NAME, 64)
CASE "装備部位"
	CALLF MAKE_STR(V_NAME, "「RestrainMouth」")
CASE "Tags"
	CALLF MAKE_STR(V_NAME, "safe virtue/")
ENDSELECT

@RestrainArm2(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "rope bind")
CASE "FullDesc"
	CALLF MAKE_STR(V_NAME, @"A crude hand bind using rope. Tight and gets the job done.")
CASE "Chastity"
	CALLF MAKE_INT(V_NAME, 64)
CASE "装備部位"
	CALLF MAKE_STR(V_NAME, "「RestrainMouth」")
CASE "Tags"
	CALLF MAKE_STR(V_NAME, "")
ENDSELECT


;============================================
; Leg Restraints
;============================================
@EXIST_RestraintLeg_ID
#LOCALSIZE 1
#LOCALSSIZE 1

;GET_INT(C_ID, "Weapon", P_ID, "Accuracy")

;要素名→ID変換用関数
;必要なら各オブジェクトで同様に作成のこと
@RestraintLeg_ID(ARG:0, ARGS:0, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS V_NAME                  ;作成する変数名
#DIMS C_NAME, OBJ_ID_LAST + 1 ;データ保持
#DIM  O_ID                    ;参照用
IF ARGS:0 == "RESET"
	VARSET C_NAME
	FOR O_ID, 1, OBJ_ID_LAST + 1
		C_NAME:O_ID = %GET_STR(, "RestrainLeg", O_ID, "名前")%
	NEXT
ELSE
	O_ID = FINDELEMENT(C_NAME, ESCAPE(ARGS:0), , , 1)
	CALLF MAKE_INT(V_NAME, O_ID)
ENDIF


@RestrainLeg1(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "spreader bar")
CASE "FullDesc"
	CALLF MAKE_STR(V_NAME, @"A bar with straps on each side, meant to keep the wearer's legs spread apart.")
CASE "Chastity"
	CALLF MAKE_INT(V_NAME, 128)
CASE "装備部位"
	CALLF MAKE_STR(V_NAME, "「RestrainCrotch」")
CASE "Tags"
	CALLF MAKE_STR(V_NAME, "beedee/")
ENDSELECT