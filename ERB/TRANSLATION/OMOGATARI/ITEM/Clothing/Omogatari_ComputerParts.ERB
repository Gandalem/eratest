; Modular ERB: OMOGATARI/ITEM/Clothing/Omogatari_ComputerParts.ERB
; @EXIST_ComputerPart1
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @ComputerPart1(ARG, O_DATA, V_NAME)
; #FUNCTION
; #LOCALSIZE 1
; #LOCALSSIZE 1
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "Name"
; 	IF GETBIT(nVerboseItems,3)
; 		CALLF MAKE_STR(V_NAME, "MOS 6502")
;     ELSE
;         CALLF MAKE_STR(V_NAME, "ITC 5081")
;     ENDIF
; CASE "FullDesc"
; 	CALLF MAKE_STR(V_NAME, @"A widely successful and inexpensive processor used in microcomputers and video game systems. It runs at 1 to 3 MHz and has a 8-bit data bus.")
; CASE "Year"
; 	CALLF MAKE_INT(V_NAME, 1975)
; CASE "Research Speed"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Price"
; 	CALLF MAKE_INT(V_NAME, 400)
; CASE "Part Type"
; 	CALLF MAKE_STR(V_NAME, "CPU")
; CASE "Tech Level"
; 	CALLF MAKE_INT(V_NAME, 5)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "itc/cpu/8-bit/")
; ENDSELECT
; @EXIST_ComputerPart2
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @ComputerPart2(ARG, O_DATA, V_NAME)
; #FUNCTION
; #LOCALSIZE 1
; #LOCALSSIZE 1
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "Name"
; 	IF GETBIT(nVerboseItems,3)
; 		CALLF MAKE_STR(V_NAME, "Intel 8086")
;     ELSE
;         CALLF MAKE_STR(V_NAME, "Letmein C7816")
;     ENDIF
; CASE "FullDesc"
; 	CALLF MAKE_STR(V_NAME, @"The first commercially successful 16-bit processor. Mainly used in the widely adopted International Technology Corporation (ITC) Microcomputer")
; CASE "Year"
; 	CALLF MAKE_INT(V_NAME, 1978)
; CASE "Research Speed"
; 	CALLF MAKE_INT(V_NAME, 3)
; CASE "Price"
; 	CALLF MAKE_INT(V_NAME, 4000)
; CASE "Part Type"
; 	CALLF MAKE_STR(V_NAME, "CPU")
; CASE "Tech Level"
; 	CALLF MAKE_INT(V_NAME, 5)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "letmein/cpu/16-bit/")
; ENDSELECT
; @EXIST_ComputerPart3
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @ComputerPart3(ARG, O_DATA, V_NAME)
; #FUNCTION
; #LOCALSIZE 1
; #LOCALSSIZE 1
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "Name"
; 	IF GETBIT(nVerboseItems,3)
; 		CALLF MAKE_STR(V_NAME, "Intel Core Ultra 285K")
;     ELSE
;         CALLF MAKE_STR(V_NAME, "Letmein C7816")
;     ENDIF
; CASE "FullDesc"
; 	CALLF MAKE_STR(V_NAME, @"A new CPU with improved performance and efficiency cores.")
; CASE "Year"
; 	CALLF MAKE_INT(V_NAME, 1978)
; CASE "Research Speed"
; 	CALLF MAKE_INT(V_NAME, 3)
; CASE "Price"
; 	CALLF MAKE_INT(V_NAME, 4000)
; CASE "Part Type"
; 	CALLF MAKE_STR(V_NAME, "CPU")
; CASE "Tech Level"
; 	CALLF MAKE_INT(V_NAME, 5)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "letmein/cpu/16-bit/")
; ENDSELECT
; @EXIST_ComputerPart2
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @ComputerPart2(ARG, O_DATA, V_NAME)
; #FUNCTION
; #LOCALSIZE 1
; #LOCALSSIZE 1
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "Name"
; 	IF GETBIT(nVerboseItems,3)
; 		CALLF MAKE_STR(V_NAME, "Intel Core 2 Duo E8500")
;     ELSE
;         CALLF MAKE_STR(V_NAME, "Letmein Yadro Duplet D8500")
;     ENDIF
; CASE "FullDesc"
; 	CALLF MAKE_STR(V_NAME, @"The first commercially successful 16-bit processor. Mainly used in the widely adopted International Technology Corporation (ITC) Microcomputer")
; CASE "Year"
; 	CALLF MAKE_INT(V_NAME, 2008)
; CASE "Research Speed"
; 	CALLF MAKE_INT(V_NAME, 18)
; CASE "Price"
; 	CALLF MAKE_INT(V_NAME, 4000)
; CASE "Part Type"
; 	CALLF MAKE_STR(V_NAME, "CPU")
; CASE "Socket"
; 	CALLF MAKE_STR(V_NAME, "LGA 775")
; CASE "Tech Level"
; 	CALLF MAKE_INT(V_NAME, 5)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "letmein/cpu/64-bit/")
; ENDSELECT
; @EXIST_ComputerPart3
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @ComputerPart3(ARG, O_DATA, V_NAME)
; #FUNCTION
; #LOCALSIZE 1
; #LOCALSSIZE 1
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "Name"
; 	IF GETBIT(nVerboseItems,3)
; 		CALLF MAKE_STR(V_NAME, "ATI Radeon HD 4670")
;     ELSE
;         CALLF MAKE_STR(V_NAME, "SAND Raidon RV730 XT")
;     ENDIF
; CASE "FullDesc"
; 	CALLF MAKE_STR(V_NAME, @"A mid range refresh of the RV630, the RV730 XT boasts 320 unified shading cores, 16 TMUs, and 16 ROPs paired with 512MB of GDDR3 memory in a 128 bit bus.")
; CASE "Year"
; 	CALLF MAKE_INT(V_NAME, 2008)
; CASE "Research Speed"
; 	CALLF MAKE_INT(V_NAME, 125)
; CASE "Price"
; 	CALLF MAKE_INT(V_NAME, 4000)
; CASE "Part Type"
; 	CALLF MAKE_STR(V_NAME, "Video Card")
; CASE "Tech Level"
; 	CALLF MAKE_INT(V_NAME, 8)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "sand/video card/gpu/pcie/")
; ENDSELECT
; @ComputerPart3(ARG, O_DATA, V_NAME)
; #FUNCTION
; #LOCALSIZE 1
; #LOCALSSIZE 1
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "Name"
;     CALLF MAKE_STR(V_NAME, "Yottagram VOREOUS G45 Pro")
; CASE "FullDesc"
; 	CALLF MAKE_STR(V_NAME, @"High end board with all of the bells and whistles for socket 775 systems.")
; CASE "Year"
; 	CALLF MAKE_INT(V_NAME, 2008)
; CASE "Price"
; 	CALLF MAKE_INT(V_NAME, 40000)
; CASE "Part Type"
; 	CALLF MAKE_STR(V_NAME, "Motherboard")
; CASE "Socket"
; 	CALLF MAKE_STR(V_NAME, "LGA 775")
; CASE "Tech Level"
; 	CALLF MAKE_INT(V_NAME, 8)
; CASE "Expansion Slots"
; 	CALLF MAKE_INT(V_NAME, 7)
; CASE "Expansion Slot Config"
; 	CALLF MAKE_STR(V_NAME, "agp x8/pcie x16/pci/pcie x16/pcie x1/pci/pci/")
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "yottagram/motherboard/acpi/")
; ENDSELECT
; @EXIST_ComputerPart10
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @ComputerPart10(ARG, O_DATA, V_NAME)
; #FUNCTION
; #LOCALSIZE 1
; #LOCALSSIZE 1
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "Name"
; 	IF GETBIT(nVerboseItems,3)
; 		CALLF MAKE_STR(V_NAME, "ATI VGA Wonder")
;     ELSE
;         CALLF MAKE_STR(V_NAME, "Discover VGA")
;     ENDIF
; CASE "FullDesc"
; 	CALLF MAKE_STR(V_NAME, @"An advanced for-its-time video card compatible with ITA (International Technology Architecture) expansion slots, being able to display up to 256 colors.")
; CASE "Year"
; 	CALLF MAKE_INT(V_NAME, 1988)
; CASE "Research Speed"
; 	CALLF MAKE_INT(V_NAME, 3)
; CASE "Price"
; 	CALLF MAKE_INT(V_NAME, 4000)
; CASE "Part Type"
; 	CALLF MAKE_STR(V_NAME, "Video Card")
; CASE "Tech Level"
; 	CALLF MAKE_INT(V_NAME, 5)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "discover/video card/gpu/isa/")
; ENDSELECT
; @EXIST_ComputerPart11
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @ComputerPart11(ARG, O_DATA, V_NAME)
; #FUNCTION
; #LOCALSIZE 1
; #LOCALSSIZE 1
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "Name"
; 	IF GETBIT(nVerboseItems,3)
; 		CALLF MAKE_STR(V_NAME, "ATI Graphics Ultra Pro")
;     ELSE
;         CALLF MAKE_STR(V_NAME, "Discover Graphics Pro")
;     ENDIF
; CASE "FullDesc"
; 	CALLF MAKE_STR(V_NAME, @"Discover Video Solutions created a 32-bit GUI accelerator as a response to graphical operation systems such as Freax. It uses the extended bandwidth from EEB (ITC Extended Expansion Bus) to give smooth operation on graphical applications.")
; CASE "Year"
; 	CALLF MAKE_INT(V_NAME, 1992)
; CASE "Research Speed"
; 	CALLF MAKE_INT(V_NAME, 4)
; CASE "Price"
; 	CALLF MAKE_INT(V_NAME, 4000)
; CASE "Part Type"
; 	CALLF MAKE_STR(V_NAME, "Video Card")
; CASE "Tech Level"
; 	CALLF MAKE_INT(V_NAME, 5)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "discover/video card/gpu/vlb/")
; ENDSELECT
; @EXIST_ComputerPart12
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @ComputerPart12(ARG, O_DATA, V_NAME)
; #FUNCTION
; #LOCALSIZE 1
; #LOCALSSIZE 1
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "Name"
; 	IF GETBIT(nVerboseItems,3)
; 		CALLF MAKE_STR(V_NAME, "3dfx Voodoo")
;     ELSE
;         CALLF MAKE_STR(V_NAME, "3DPC Mira")
;     ENDIF
; CASE "FullDesc"
; 	CALLF MAKE_STR(V_NAME, @"3DPC (3D Personal Computing) main objective was to push then workstation-only graphics onto personal desktops with the Mira, a 3D accelerator able to run the most demanding games at the time at comfortable frame rates. Gone were the times of chunky, 300x200px software rendering, as the Mira provided smooth hardware-accelerated 640x480px graphics, or about 5 times the detail.")
; CASE "Year"
; 	CALLF MAKE_INT(V_NAME, 1996)
; CASE "Research Speed"
; 	CALLF MAKE_INT(V_NAME, 6)
; CASE "Price"
; 	CALLF MAKE_INT(V_NAME, 4000)
; CASE "Part Type"
; 	CALLF MAKE_STR(V_NAME, "Video Card")
; CASE "Tech Level"
; 	CALLF MAKE_INT(V_NAME, 5)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "3dpc/video card/gpu/pci/")
; ENDSELECT
; @EXIST_ComputerPart13
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @ComputerPart13(ARG, O_DATA, V_NAME)
; #FUNCTION
; #LOCALSIZE 1
; #LOCALSSIZE 1
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "Name"
; 	IF GETBIT(nVerboseItems,3)
; 		CALLF MAKE_STR(V_NAME, "nVidia Riva 128")
;     ELSE
;         CALLF MAKE_STR(V_NAME, "iNgenunity RTA 128")
;     ENDIF
; CASE "FullDesc"
; 	CALLF MAKE_STR(V_NAME, @"iNgenunity created the Real Time Accelerator as a desperate call to redeem itself from it's failed NGA1 and NGA2 video cards. The RTA provided a wide 128-bit bus and a core running at 100 MHz, which was considered \"fast and wide\", as well as having a new type of ram called SGRAM. This allowed higher resolutions such as 800x600 compared to previous-gen competitors like the Mira, which only provided 640x480.")
; CASE "Year"
; 	CALLF MAKE_INT(V_NAME, 1997)
; CASE "Research Speed"
; 	CALLF MAKE_INT(V_NAME, 7)
; CASE "Price"
; 	CALLF MAKE_INT(V_NAME, 4000)
; CASE "Part Type"
; 	CALLF MAKE_STR(V_NAME, "Video Card")
; CASE "Tech Level"
; 	CALLF MAKE_INT(V_NAME, 5)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "ingenunity/video card/gpu/pci/")
; ENDSELECT
; @EXIST_ComputerPart14
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @ComputerPart14(ARG, O_DATA, V_NAME)
; #FUNCTION
; #LOCALSIZE 1
; #LOCALSSIZE 1
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "Name"
; 	IF GETBIT(nVerboseItems,3)
; 		CALLF MAKE_STR(V_NAME, "nVidia Riva TNT")
;     ELSE
;         CALLF MAKE_STR(V_NAME, "iNgenunity TT RTA")
;     ENDIF
; CASE "FullDesc"
; 	CALLF MAKE_STR(V_NAME, @"iNgenunity created the Real Time Accelerator as a desperate call to redeem itself from it's failed NGA1 and NGA2 video cards. The RTA provided a wide 128-bit bus and a core running at 100 MHz, which was considered \"fast and wide\", as well as having a new type of ram called SGRAM. This allowed higher resolutions such as 800x600 compared to previous-gen competitors like the Mira, which only provided 640x480.")
; CASE "Year"
; 	CALLF MAKE_INT(V_NAME, 1997)
; CASE "Research Speed"
; 	CALLF MAKE_INT(V_NAME, 11)
; CASE "Price"
; 	CALLF MAKE_INT(V_NAME, 4000)
; CASE "Part Type"
; 	CALLF MAKE_STR(V_NAME, "Video Card")
; CASE "Tech Level"
; 	CALLF MAKE_INT(V_NAME, 5)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "ingenunity/video card/gpu/pci/")
; ENDSELECT
; @EXIST_ComputerPart15
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @ComputerPart15(ARG, O_DATA, V_NAME)
; #FUNCTION
; #LOCALSIZE 1
; #LOCALSSIZE 1
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "Name"
; 	IF GETBIT(nVerboseItems,3)
; 		CALLF MAKE_STR(V_NAME, "nVidia GeForce 256")
;     ELSE
;         CALLF MAKE_STR(V_NAME, "iNgenunity 1256 GPU")
;     ENDIF
; CASE "FullDesc"
; 	CALLF MAKE_STR(V_NAME, @"Coined as \"the world's first 'GPU'\", the 1256 GPU implemented hardware-based transformation and lighting, allowing graphics to finally be fully free from the CPU, which the two were previously dependent on. The 256 in its name stems from it's four 64-bit pixel pipelines, allowing it to output 4 pixels per clock cycle. The term GPU (Graphical Processing Unit) was coined as \"a single chip processor with integrated transformation, lighting, triangle setup/clipping, and rendering engines that is capable of processing a minimum of 10 million polygons per second.\"".")
; CASE "Year"
; 	CALLF MAKE_INT(V_NAME, 1999)
; CASE "Research Speed"
; 	CALLF MAKE_INT(V_NAME, 15)
; CASE "Price"
; 	CALLF MAKE_INT(V_NAME, 4000)
; CASE "Part Type"
; 	CALLF MAKE_STR(V_NAME, "Video Card")
; CASE "Tech Level"
; 	CALLF MAKE_INT(V_NAME, 5)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "ingenunity/video card/gpu/pci/")
; ENDSELECT
; @EXIST_ComputerPart16
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @ComputerPart16(ARG, O_DATA, V_NAME)
; #FUNCTION
; #LOCALSIZE 1
; #LOCALSSIZE 1
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "Name"
; 	IF GETBIT(nVerboseItems,3)
; 		CALLF MAKE_STR(V_NAME, "nVidia GeForce2 Ultra")
;     ELSE
;         CALLF MAKE_STR(V_NAME, "iNgenunity 2800 GPU")
;     ENDIF
; CASE "FullDesc"
; 	CALLF MAKE_STR(V_NAME, @"The iNgenunity's 2 series (which was actually their 3rd generation) gave second texture mapping unit, allowing two pixels to be processed per pipeline.")
; CASE "Year"
; 	CALLF MAKE_INT(V_NAME, 2000)
; CASE "Research Speed"
; 	CALLF MAKE_INT(V_NAME, 18)
; CASE "Price"
; 	CALLF MAKE_INT(V_NAME, 4000)
; CASE "Part Type"
; 	CALLF MAKE_STR(V_NAME, "Video Card")
; CASE "Tech Level"
; 	CALLF MAKE_INT(V_NAME, 5)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "ingenunity/video card/gpu/agp/")
; ENDSELECT
; @EXIST_ComputerPart102
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @ComputerPart102(ARG, O_DATA, V_NAME)
; #FUNCTION
; #LOCALSIZE 1
; #LOCALSSIZE 1
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "Name"
; 	IF GETBIT(nVerboseItems,3)
; 		CALLF MAKE_STR(V_NAME, "nVidia GeForce RTX 2080 Ti")
;     ELSE
;         CALLF MAKE_STR(V_NAME, "iNgenunity 850 AIX")
;     ENDIF
; CASE "FullDesc"
; 	CALLF MAKE_STR(V_NAME, @"The advent of AI based applications reached a breaking-point when iNgenunity released the first AIX series of cards. The AIX series of cards allowed for real-time ray-tracing and upscaling with the inclusion of tensor cores. It was also described as \"the most significant generational upgrade to its GPUs since the first GPC cores in 2006\". The A850 was considered the most powerful variant of the AIX marketed to desktop users")
; CASE "Year"
; 	CALLF MAKE_INT(V_NAME, 2018)
; CASE "Research Speed"
; 	CALLF MAKE_INT(V_NAME, 2100)
; CASE "Price"
; 	CALLF MAKE_INT(V_NAME, 400800)
; CASE "Part Type"
; 	CALLF MAKE_STR(V_NAME, "Video Card")
; CASE "Tech Level"
; 	CALLF MAKE_INT(V_NAME, 5)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "ingenunity/video card/gpu/pcie/")
; ENDSELECT
; @EXIST_ComputerPart103
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @ComputerPart103(ARG, O_DATA, V_NAME)
; #FUNCTION
; #LOCALSIZE 1
; #LOCALSSIZE 1
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "Name"
; 	IF GETBIT(nVerboseItems,3)
; 		CALLF MAKE_STR(V_NAME, "nVidia Titan RTX")
;     ELSE
;         CALLF MAKE_STR(V_NAME, "iNgenunity 900 AIX")
;     ENDIF
; CASE "FullDesc"
; 	CALLF MAKE_STR(V_NAME, @"The advent of AI based applications reached a breaking-point when iNgenunity released the first AIX series of cards. The AIX series of cards allowed for real-time ray-tracing and upscaling with the inclusion of tensor cores. It was also described as \"the most significant generational upgrade to its GPUs since the first GPC cores in 2006\". The A900 is the top of the line gaming variant of the AIX series video cards, prioritizing the HEDT market.")
; CASE "Year"
; 	CALLF MAKE_INT(V_NAME, 2018)
; CASE "Research Speed"
; 	CALLF MAKE_INT(V_NAME, 2100)
; CASE "Price"
; 	CALLF MAKE_INT(V_NAME, 400800)
; CASE "Part Type"
; 	CALLF MAKE_STR(V_NAME, "Video Card")
; CASE "Tech Level"
; 	CALLF MAKE_INT(V_NAME, 5)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "ingenunity/video card/gpu/pcie/")
; ENDSELECT
; @EXIST_ComputerPart104
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @ComputerPart104(ARG, O_DATA, V_NAME)
; #FUNCTION
; #LOCALSIZE 1
; #LOCALSSIZE 1
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "Name"
; 	IF GETBIT(nVerboseItems,3)
; 		CALLF MAKE_STR(V_NAME, "nVidia GeForce RTX 3080")
;     ELSE
;         CALLF MAKE_STR(V_NAME, "iNgenunity α800 AIX")
;     ENDIF
; CASE "FullDesc"
; 	CALLF MAKE_STR(V_NAME, @"AIX Alpha (α) offered better GPC (General Personal Computing) and tensor cores, more VRAM, and a smaller manufacturing process, making it much more powerful than the previous gen AIX cards. The α800 uses the same core as the α900, but at a lower clock speed.")
; CASE "Year"
; 	CALLF MAKE_INT(V_NAME, 2018)
; CASE "Research Speed"
; 	CALLF MAKE_INT(V_NAME, 2760)
; CASE "Price"
; 	CALLF MAKE_INT(V_NAME, 400800)
; CASE "Part Type"
; 	CALLF MAKE_STR(V_NAME, "Video Card")
; CASE "Tech Level"
; 	CALLF MAKE_INT(V_NAME, 5)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "ingenunity/video card/gpu/pcie/")
; ENDSELECT
; @EXIST_ComputerPart100
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @ComputerPart100(ARG, O_DATA, V_NAME)
; #FUNCTION
; #LOCALSIZE 1
; #LOCALSSIZE 1
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "Name"
; 	IF GETBIT(nVerboseItems,3)
; 		CALLF MAKE_STR(V_NAME, "NVIDIA RTX PRO 6000 Blackwell")
;     ELSE
;         ;centerbase is tesla
;         CALLF MAKE_STR(V_NAME, "iNgenunity AIX Pro γ9900")
;     ENDIF
; CASE "FullDesc"
; 	CALLF MAKE_STR(V_NAME, @"2025's most powerful workstation card. AIX Gamma (γ) increased power to a outstanding 440W on it's γ900 AIX and was touted to be much more powerful compared to Beta on AI performance. It provides extreme computation power perfect for AI applications and demanding data center software.")
; CASE "Year"
; 	CALLF MAKE_INT(V_NAME, 2025)
; CASE "Research Speed"
; 	CALLF MAKE_INT(V_NAME, 4400)
; CASE "Price"
; 	CALLF MAKE_INT(V_NAME, 400800)
; CASE "Part Type"
; 	CALLF MAKE_STR(V_NAME, "Video Card")
; CASE "Tech Level"
; 	CALLF MAKE_INT(V_NAME, 5)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "ingenunity/video card/gpu/pcie/")
; ENDSELECT
; @EXIST_ComputerPart100
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @ComputerPart100(ARG, O_DATA, V_NAME)
; #FUNCTION
; #LOCALSIZE 1
; #LOCALSSIZE 1
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "Name"
; 	IF GETBIT(nVerboseItems,3)
; 		CALLF MAKE_STR(V_NAME, "Intel Core Ultra 9 285K")
;     ELSE
;         ;centerbase is tesla
;         CALLF MAKE_STR(V_NAME, "Letmein NPA 285K")
;     ENDIF
; CASE "FullDesc"
; 	CALLF MAKE_STR(V_NAME, @"2024's most powerful workstation card. AIX Gamma (γ) increased power to a outstanding 440W on it's γ900 AIX and was touted to be much more powerful compared to Beta on AI performance. It provides extreme computation power perfect for AI applications and demanding data center software.")
; CASE "Year"
; 	CALLF MAKE_INT(V_NAME, 2024)
; CASE "Research Speed"
; 	CALLF MAKE_INT(V_NAME, 4400)
; CASE "Price"
; 	CALLF MAKE_INT(V_NAME, 400800)
; CASE "Part Type"
; 	CALLF MAKE_STR(V_NAME, "Video Card")
; CASE "Tech Level"
; 	CALLF MAKE_INT(V_NAME, 5)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "ingenunity/video card/gpu/pcie/")
; ENDSELECT
