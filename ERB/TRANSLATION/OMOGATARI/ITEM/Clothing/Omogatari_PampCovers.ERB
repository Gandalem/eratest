; Modular ERB: OMOGATARI/ITEM/Clothing/Omogatari_PampCovers.ERB
;=============================================================================================================
; outter underwear (like plastic pants)
;=============================================================================================================

@EXIST_下半身下着_ずらし不可20
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし不可20(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, "plastic pants")
	CASE "FullName"
		CALLF MAKE_STR(V_NAME, "LastLeak Plastic Pants「Nightli」")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "플라스틱 팬티")
	CASE "FullDesc"
		CALLF MAKE_STR(V_NAME, @"A pair of boring-looking meant to cover what the person is actually wearing. Very crinkly however and can help stop leaks. Also suitable for cloth diapers.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「下半身下着１」")
		;旧装備処理との照会用、下半身下着１の5
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 0)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 0)
	CASE "Diaper Cover"
		CALLF MAKE_INT(V_NAME, 2)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"lastleak/")
ENDSELECT

@EXIST_下半身下着_ずらし不可21
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし不可21(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, "baby bloomers")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "베이비블루머")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「下半身下着１」")
		;旧装備処理との照会用、下半身下着１の5
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Diaper Cover"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"woc/")
ENDSELECT

@EXIST_下半身下着_ずらし不可22
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし不可22(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, "drawers")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "완만한 구조의")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「下半身下着１」")
		;旧装備処理との照会用、下半身下着１の5
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Diaper Cover"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"rw tailor/")
ENDSELECT

@EXIST_下半身下着_ずらし不可23
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし不可23(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, "frilly white diaper cover")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "러플 플라스틱 팬티")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「下半身下着１」")
		;旧装備処理との照会用、下半身下着１の5
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Diaper Cover"
		CALLF MAKE_INT(V_NAME, 3)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"lastleak/")
ENDSELECT

@EXIST_下半身下着_ずらし不可24
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし不可24(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, "teddy bear diaper cover")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "플라스틱 팬티")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「下半身下着１」")
		;旧装備処理との照会用、下半身下着１の5
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Diaper Cover"
		CALLF MAKE_INT(V_NAME, 3)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"re:dl/")
ENDSELECT

@EXIST_下半身下着_ずらし不可25
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし不可25(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, "orange rubber diaper cover")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "러버팬츠")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「下半身下着１」")
		;旧装備処理との照会用、下半身下着１の5
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Diaper Cover"
		CALLF MAKE_INT(V_NAME, 3)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"re:dl/")
ENDSELECT

@EXIST_下半身下着_ずらし不可26
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし不可26(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, "underskirt")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "꾸밈없는")
	CASE "FullDesc"
		CALLF MAKE_STR(V_NAME, @"A pair of boring-looking panties meant to cover what the person is actually wearing. Also suitable for cloth diapers.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「下半身下着１」")
		;旧装備処理との照会用、下半身下着１の5
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Diaper Cover"
		CALLF MAKE_INT(V_NAME, 3)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"woc/")
ENDSELECT

@EXIST_下半身下着_ずらし不可27
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし不可27(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, "diaper wrap")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "기저귀랩")
	CASE "FullDesc"
		CALLF MAKE_STR(V_NAME, @"A polyurethane laminate cover meant to wrap over a diaper as a newer version of plastic pants. Uses snaps to keep it in place.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「下半身下着１」")
		;旧装備処理との照会用、下半身下着１の5
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Diaper Cover"
		CALLF MAKE_INT(V_NAME, 3)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"\@GETBIT(nVerboseItems,3) ? rearz # re:dl\@/")
ENDSELECT

@EXIST_下半身下着_ずらし不可28
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし不可28(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,3)
			CALLF MAKE_STR(V_NAME, "Peepeepants")
		ELSEIF GETBIT(nVerboseItems,2)
			CALLF MAKE_STR(V_NAME, "GC FantastaPants")
		ELSE
			CALLF MAKE_STR(V_NAME, "shorts diaper cover")
		ENDIF
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "피피팬츠")
	CASE "FullDesc"
		CALLF MAKE_STR(V_NAME, @"A pair of very absorbent shorts, meant to give support at the top and bottom of a diaper. It's lining also makes it compatible for cloth diapers as well.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「下半身下着１」")
		;旧装備処理との照会用、下半身下着１の5
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Diaper Cover"
		CALLF MAKE_INT(V_NAME, 3)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"\@GETBIT(nVerboseItems,3) ? peepeepants # gensou-chan-ab\@/")
ENDSELECT

@EXIST_下半身下着_ずらし不可29
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし不可29(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, "locking diaper wrap")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "기저귀랩잠금")
	CASE "FullDesc"
		CALLF MAKE_STR(V_NAME, @"A polyurethane laminate diaper wrap modified to have a locking mechanism. Because of the lock, it makes it impossible for the wearer to go to the toilet, masturbate, or even change their diaper. For anyone besides the changer, they're unable to change the wearer's diaper as well.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「下半身下着１」")
		;旧装備処理との照会用、下半身下着１の5
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Diaper Cover"
		CALLF MAKE_INT(V_NAME, 3)
	CASE "Chastity"
		CALLF MAKE_INT(V_NAME, 39)
	CASE "Locked"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"safe virtue/")
ENDSELECT

@EXIST_下半身下着_ずらし不可30
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし不可30(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, "SecureBab")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "시큐어바브")
	CASE "FullDesc"
		CALLF MAKE_STR(V_NAME, @"A very cute diaper cover with various Touhous at the outside. The inside is lined with polyurethane laminate which is breathable but keeps leaks out. Uses snaps to keep it in place and a strap for tightness adjustment and locking.\n\nCollaboration between Safe Virtue and Gensou-chan Adult Baby.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「下半身下着１」")
		;旧装備処理との照会用、下半身下着１の5
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Diaper Cover"
		CALLF MAKE_INT(V_NAME, 3)
	CASE "Chastity"
		CALLF MAKE_INT(V_NAME, 39)
	CASE "Locked"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"safe virtue/")
ENDSELECT

@下半身下着_ずらし不可31(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,2)
			CALLF MAKE_STR(V_NAME, "PeeBlock one")
		ELSE
			CALLF MAKE_STR(V_NAME, GET_STR(-1, "下半身下着_ずらし不可", ToInt(SUBSTRING(GetDoingFunction(),20)), "Generic Name"))
		ENDIF
	CASE "FullName"
		CALLF MAKE_STR(V_NAME, "YMKA wool shorties「PeeBlock one」")
	CASE "Generic Name"
		CALLF MAKE_STR(V_NAME, "wool shorties")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "고풍스럽고 우아한")
	CASE "FullDesc"
		CALLF MAKE_STR(V_NAME, @"A hand-knit wool diaper cover in the form of shorts. When combined with a cloth diaper, you can have a robust pre-industrial solution to incontinence.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「下半身下着１」")
		;旧装備処理との照会用、下半身下着１の5
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Diaper Cover"
		CALLF MAKE_INT(V_NAME, 3)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"kappa-arm/")
ENDSELECT
