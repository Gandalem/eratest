; Modular ERB: OMOGATARI/ITEM/Clothing/Omogatari_PampCovers.ERB
;=============================================================================================================
; outter underwear (like plastic pants)
;=============================================================================================================

@EXIST_下半身下着_ずらし不可20
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし不可20(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, "plastic pants")
	CASE "FullName"
		CALLF MAKE_STR(V_NAME, "LastLeak 비닐 팬츠「Nightli」")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "플라스틱 팬티")
	CASE "FullDesc"
		CALLF MAKE_STR(V_NAME, @"속에 무엇을 입었는지 가리기 위한 수수한 덧바지. 바스락거림이 심하지만 새는 것을 어느 정도 막아 주며, 천 기저귀 위에도 입을 수 있다.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「下半身下着１」")
		;旧装備処理との照会用、下半身下着１の5
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 0)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 0)
	CASE "Diaper Cover"
		CALLF MAKE_INT(V_NAME, 2)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"lastleak/")
ENDSELECT

@EXIST_下半身下着_ずらし不可21
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし不可21(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, "baby bloomers")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "베이비블루머")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「下半身下着１」")
		;旧装備処理との照会用、下半身下着１の5
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Diaper Cover"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"woc/")
ENDSELECT

@EXIST_下半身下着_ずらし不可22
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし不可22(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, "drawers")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "완만한 구조의")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「下半身下着１」")
		;旧装備処理との照会用、下半身下着１の5
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Diaper Cover"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"rw tailor/")
ENDSELECT

@EXIST_下半身下着_ずらし不可23
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし不可23(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, "frilly white diaper cover")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "러플 플라스틱 팬티")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「下半身下着１」")
		;旧装備処理との照会用、下半身下着１の5
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Diaper Cover"
		CALLF MAKE_INT(V_NAME, 3)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"lastleak/")
ENDSELECT

@EXIST_下半身下着_ずらし不可24
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし不可24(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, "teddy bear diaper cover")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "플라스틱 팬티")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「下半身下着１」")
		;旧装備処理との照会用、下半身下着１の5
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Diaper Cover"
		CALLF MAKE_INT(V_NAME, 3)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"re:dl/")
ENDSELECT

@EXIST_下半身下着_ずらし不可25
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし不可25(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, "orange rubber diaper cover")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "러버팬츠")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「下半身下着１」")
		;旧装備処理との照会用、下半身下着１の5
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Diaper Cover"
		CALLF MAKE_INT(V_NAME, 3)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"re:dl/")
ENDSELECT

@EXIST_下半身下着_ずらし不可26
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし不可26(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, "underskirt")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "꾸밈없는")
	CASE "FullDesc"
		CALLF MAKE_STR(V_NAME, @"속에 무엇을 입었는지 가리기 위한 수수한 덧팬티. 천 기저귀 위에도 입을 수 있다.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「下半身下着１」")
		;旧装備処理との照会用、下半身下着１の5
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Diaper Cover"
		CALLF MAKE_INT(V_NAME, 3)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"woc/")
ENDSELECT

@EXIST_下半身下着_ずらし不可27
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし不可27(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, "diaper wrap")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "기저귀랩")
	CASE "FullDesc"
		CALLF MAKE_STR(V_NAME, @"비닐 팬츠를 현대적으로 다듬은 폴리우레탄 라미네이트 기저귀 커버. 스냅 단추로 단단히 고정한다.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「下半身下着１」")
		;旧装備処理との照会用、下半身下着１の5
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Diaper Cover"
		CALLF MAKE_INT(V_NAME, 3)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"\@GETBIT(nVerboseItems,3) ? rearz # re:dl\@/")
ENDSELECT

@EXIST_下半身下着_ずらし不可28
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし不可28(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,3)
			CALLF MAKE_STR(V_NAME, "Peepeepants")
		ELSEIF GETBIT(nVerboseItems,2)
			CALLF MAKE_STR(V_NAME, "GC FantastaPants")
		ELSE
			CALLF MAKE_STR(V_NAME, "shorts diaper cover")
		ENDIF
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "피피팬츠")
	CASE "FullDesc"
		CALLF MAKE_STR(V_NAME, @"기저귀의 위아래를 받쳐 주는 흡수력 높은 반바지. 안감 덕분에 천 기저귀와도 함께 쓸 수 있다.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「下半身下着１」")
		;旧装備処理との照会用、下半身下着１の5
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Diaper Cover"
		CALLF MAKE_INT(V_NAME, 3)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"\@GETBIT(nVerboseItems,3) ? peepeepants # gensou-chan-ab\@/")
ENDSELECT

@EXIST_下半身下着_ずらし不可29
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし不可29(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, "locking diaper wrap")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "기저귀랩잠금")
	CASE "FullDesc"
		CALLF MAKE_STR(V_NAME, @"잠금장치를 더한 폴리우레탄 라미네이트 기저귀 커버. 잠그면 착용자는 화장실에 갈 수도, 자위할 수도, 기저귀를 갈 수도 없다. 채워 준 사람 외에는 누구도 이 기저귀를 벗겨 줄 수 없다.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「下半身下着１」")
		;旧装備処理との照会用、下半身下着１の5
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Diaper Cover"
		CALLF MAKE_INT(V_NAME, 3)
	CASE "Chastity"
		CALLF MAKE_INT(V_NAME, 39)
	CASE "Locked"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"safe virtue/")
ENDSELECT

@EXIST_下半身下着_ずらし不可30
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし不可30(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, "SecureBab")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "시큐어바브")
	CASE "FullDesc"
		CALLF MAKE_STR(V_NAME, @"겉면에 여러 동방 캐릭터를 그려 넣은 아주 귀여운 기저귀 커버. 안쪽은 숨은 쉬면서도 샘을 막아 주는 폴리우레탄 라미네이트로 마감했다. 스냅 단추로 고정하고, 조임끈으로 크기를 맞추거나 잠글 수 있다.\n\n세이프 버추와 겐소우짱 어덜트 베이비의 협업 제품.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「下半身下着１」")
		;旧装備処理との照会用、下半身下着１の5
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Diaper Cover"
		CALLF MAKE_INT(V_NAME, 3)
	CASE "Chastity"
		CALLF MAKE_INT(V_NAME, 39)
	CASE "Locked"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"safe virtue/")
ENDSELECT

@下半身下着_ずらし不可31(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,2)
			CALLF MAKE_STR(V_NAME, "피블록 원")
		ELSE
			CALLF MAKE_STR(V_NAME, GET_STR(-1, "下半身下着_ずらし不可", ToInt(SUBSTRING(GetDoingFunction(),20)), "Generic Name"))
		ENDIF
	CASE "FullName"
		CALLF MAKE_STR(V_NAME, "YMKA 양모 쇼티「피블록 원」")
	CASE "Generic Name"
		CALLF MAKE_STR(V_NAME, "양모 쇼티")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "고풍스럽고 우아한")
	CASE "FullDesc"
		CALLF MAKE_STR(V_NAME, @"손으로 뜬 반바지형 양모 기저귀 커버. 천 기저귀와 함께 사용하면 산업화 이전 방식 그대로 튼튼한 요실금 대책이 되어 준다.")
	CASE "装備部位"
		CALLF MAKE_STR(V_NAME, "「下半身下着１」")
		;旧装備処理との照会用、下半身下着１の5
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Diaper Cover"
		CALLF MAKE_INT(V_NAME, 3)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, @"kappa-arm/")
ENDSELECT
