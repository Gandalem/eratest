; Modular ERB: OMOGATARI/ITEM/Clothing/Omogatari_Underwear.ERB
;=============================================================================================================
; Underwear (normal)
;=============================================================================================================
; @EXIST_下半身下着_ずらし可能40
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能40(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, "briefs")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "男物の")
; CASE "FullDesc"
; 	CALLF MAKE_STR(V_NAME, @"Boyish briefs, also known as tidy whities. A universal underwear choice for everyone with a working bladder. Has a slot to push a penis through for easy urinal access and sex.")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "はける"
; 	SIF !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "Unisex"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "urbworld/white/masculine/brief/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能41
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能41(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, "red briefs")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "wtf is this used for")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "はける"
; 	SIF !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "Unisex"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "urbworld/red/masculine/brief/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能42
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能42(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, "purple briefs")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "wtf is this used for")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "はける"
; 	SIF !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "Unisex"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "urbworld/purple/masculine/brief/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能43
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能43(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, "blue briefs")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "wtf is this used for")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "はける"
; 	SIF !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "Unisex"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "urbworld/masculine/brief/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能44
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能44(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, "『Up All Night』briefs")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "grey")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "はける"
; 	SIF (TALENT:ARG:184 || TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "Unisex"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "amorpan/sexy/black/feminine/brief/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能45
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能45(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, "『Let's Get a Room』briefs")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "blue")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "はける"
; 	SIF (TALENT:ARG:184 || TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "Unisex"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "beedee/sexy/white/printed/feminine/brief/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能46
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能46(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, "『Good Girl』briefs")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "white")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "はける"
; 	SIF (TALENT:ARG:184 || TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "beedee/sexy/red/feminine/brief/printed/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能47
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能47(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, "『Hole』briefs")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "pink")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "はける"
; 	SIF (TALENT:ARG:184 || TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "Unisex"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "beedee/sexy/purple/gray/feminine/brief/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能48
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能48(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, "『Feeling Lucky』briefs")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "green")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "はける"
; 	SIF (TALENT:ARG:184 || TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "Unisex"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "beedee/sexy/blue/feminine/panty/printed/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能49
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能49(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, "『See You Tonight』briefs")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "red")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "はける"
; 	SIF (TALENT:ARG:184 || TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "Unisex"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "beedee/sexy/white/feminine/brief/washable/")
; ENDSELECT

@EXIST_下半身下着_ずらし可能50
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし可能50(ARG, O_DATA, V_NAME)
#FUNCTION
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, "high waist shiny silver panties")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "어른의 매력이 넘치는")
	CASE "FullDesc"
		CALLF MAKE_STR(V_NAME, @"Shiny panties for the discerning lady who wants to be noticed. The silver is reflective and the high waist allows you to show your panties to the world.")
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "はける"
		SIF !RAND:(1 + (2 * 勝負下着判定(ARG)))
			CALLF MAKE_INT(V_NAME, 1)
	CASE "関連下着"
		CALLF MAKE_INT(V_NAME, 0)
	CASE "コレクション表示可能"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "SHIFTABLE"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, "amorpan/sexy/gray/shiny/feminine/panty/washable/")
ENDSELECT
; @EXIST_下半身下着_ずらし可能51
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能51(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, @"『I %UNICODE(0x2764)% girls』briefs")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "pink poka-dots")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "はける"
; 	SIF (TALENT:ARG:184 || TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "Unisex"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "beedee/sexy/pink/printed/feminine/brief/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能52
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能52(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, @"『Trick Me』briefs")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "red-orange")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "はける"
; 	SIF (TALENT:ARG:184 || TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "Unisex"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "beedee/sexy/white/printed/brief/washable/")
; ENDSELECT

@EXIST_下半身下着_ずらし可能53
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし可能53(ARG, O_DATA, V_NAME)
#FUNCTION
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, @"hi-cut briefs")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "평소보다 허리선이 높은 브리프. 보통 운동선수들이 입는데, 떨어지기 어렵게 해주기 때문이다.")
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "はける"
		SIF !RAND:(1 + (2 * 勝負下着判定(ARG)))
			CALLF MAKE_INT(V_NAME, 1)
	CASE "関連下着"
		CALLF MAKE_INT(V_NAME, 0)
	CASE "Unisex"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "コレクション表示可能"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, "urbworld/gray/masculine/brief/plain/washable/")
ENDSELECT

@EXIST_下半身下着_ずらし可能54
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし可能54(ARG, O_DATA, V_NAME)
#FUNCTION
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, @"sheer briefs")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "음란한")
	CASE "FullDesc"
		CALLF MAKE_STR(V_NAME, @"Partially transparent underwear, for when you really want to tease someone. These leave your privates, butt, and legs completely visible through these briefs due to the sheer fabric.")
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 0)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 0)
	CASE "はける"
		SIF !RAND:(1 + (2 * 勝負下着判定(ARG)))
			CALLF MAKE_INT(V_NAME, 1)
	CASE "関連下着"
		CALLF MAKE_INT(V_NAME, 0)
	CASE "Unisex"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "コレクション表示可能"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, "amorpan/sexy/white/sheer/feminine/panty/washable/")
ENDSELECT
; @EXIST_下半身下着_ずらし可能55
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能55(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, @"sheer nylon briefs")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "wtf is this used for")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "はける"
; 	SIF !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "Unisex"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "amorpan/sexy/white/sheer/feminine/panty/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能56
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能56(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, @"frilly black open-crotch panties")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "wtf is this used for")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "はける"
; 	SIF (TALENT:ARG:184 || TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Panties Type"
; 	CALLF MAKE_STR(V_NAME, "Open-Crotch")
; CASE "Pee Capacity"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "Poo Capacity"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "amorpan/sexy/black/panty/feminine/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能57
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能57(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, @"demin cuttoffs")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "wtf is this used for")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "はける"
; 	SIF !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; ENDSELECT

@EXIST_下半身下着_ずらし可能58
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし可能58(ARG, O_DATA, V_NAME)
#FUNCTION
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, @"latex plug panties")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "팬티 플러그")
	CASE "FullDesc"
		CALLF MAKE_STR(V_NAME, @"A lacy pair of panties combined with a thick dildo. Loosens up the wearer's vagina for easier penetration and gives them a bit of passive pleasure.")
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "はける"
		IF IS_VIRGIN(ARG)
			CALLF MAKE_INT(V_NAME, 0)
		ELSEIF (TALENT:ARG:184 || TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(1 + (2 * 勝負下着判定(ARG)))
			CALLF MAKE_INT(V_NAME, 1)
		ELSE
			CALLF MAKE_INT(V_NAME, 0)
		ENDIF
	CASE "関連下着"
		CALLF MAKE_INT(V_NAME, 0)
	CASE "コレクション表示可能"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, "efun/black/panty/sex toy/sexy/washable/")
ENDSELECT
; @EXIST_下半身下着_ずらし可能59
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能59(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, @"『Breed Me』panties")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "white")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "はける"
; 	SIF (TALENT:ARG:184 || TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "beedee/sexy/white/feminine/printed/panty/washable/")
; ENDSELECT

@EXIST_下半身下着_ずらし可能60
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし可能60(ARG, O_DATA, V_NAME)
#FUNCTION
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, @"comfy panties")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "복슬복슬한")
	CASE "FullDesc"
		CALLF MAKE_STR(V_NAME, @"Comfy wool panties for when you just want to take it easy. Just don't get so comfortable to the point you push a load inside of them (they're not designed for that).")
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "はける"
		SIF !RAND:(1 + (2 * 勝負下着判定(ARG)))
			CALLF MAKE_INT(V_NAME, 1)
	CASE "関連下着"
		CALLF MAKE_INT(V_NAME, 0)
	CASE "コレクション表示可能"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, "urbworld/purple/plain/panty/washable/")
ENDSELECT
; @EXIST_下半身下着_ずらし可能62
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能62(ARG, O_DATA, V_NAME)
; #FUNCTION
; #LOCALSIZE 1
; #LOCALSSIZE 1
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, "pink thong")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "お尻が丸出しの大胆な")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "はける"
; 	SIF (TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(3 - MIN(1,TALENT:ARG:恋慕) - 勝負下着判定(ARG))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	SELECTCASE RAND:(1 + 勝負下着判定(ARG))
; 		CASE 1
; 			CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身下着１", "ベビードール"))
; 		CASEELSE
; 			CALLF MAKE_INT(V_NAME, 0)
; 	ENDSELECT
; CASE "コレクション表示可能"
; 	SIF TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "コレクション解禁条件"
; 	CALLF MAKE_STR(V_NAME, "解禁：恋慕")
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "amorpan/sexy/pink/feminine/plain/thong/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能63
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能63(ARG, O_DATA, V_NAME)
; #FUNCTION
; #LOCALSIZE 1
; #LOCALSSIZE 1
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, "『Pretty Prettier Prettiest』thong")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "お尻が丸出しの大胆な")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "はける"
; 	SIF (TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(3 - MIN(1,TALENT:ARG:恋慕) - 勝負下着判定(ARG))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	SELECTCASE RAND:(1 + 勝負下着判定(ARG))
; 		CASE 1
; 			CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身下着１", "ベビードール"))
; 		CASEELSE
; 			CALLF MAKE_INT(V_NAME, 0)
; 	ENDSELECT
; CASE "コレクション表示可能"
; 	SIF TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "コレクション解禁条件"
; 	CALLF MAKE_STR(V_NAME, "解禁：恋慕")
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "beedee/white/feminine/printed/thong/sexy/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能64
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能64(ARG, O_DATA, V_NAME)
; #FUNCTION
; #LOCALSIZE 1
; #LOCALSSIZE 1
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, "blue thong")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "お尻が丸出しの大胆な")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "はける"
; 	SIF (TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(3 - MIN(1,TALENT:ARG:恋慕) - 勝負下着判定(ARG))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	SELECTCASE RAND:(1 + 勝負下着判定(ARG))
; 		CASE 1
; 			CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身下着１", "ベビードール"))
; 		CASEELSE
; 			CALLF MAKE_INT(V_NAME, 0)
; 	ENDSELECT
; CASE "コレクション表示可能"
; 	SIF TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "コレクション解禁条件"
; 	CALLF MAKE_STR(V_NAME, "解禁：恋慕")
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "amorpan/sexy/blue/feminine/plain/thong/sexy/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能65
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能65(ARG, O_DATA, V_NAME)
; #FUNCTION
; #LOCALSIZE 1
; #LOCALSSIZE 1
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, "flirty thong")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "お尻が丸出しの大胆な")
; CASE "FullDesc"
; 	CALLF MAKE_STR(V_NAME, @"A very thin thong who's main job is to tease.")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "はける"
; 	SIF (TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(3 - MIN(1,TALENT:ARG:恋慕) - 勝負下着判定(ARG))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	SELECTCASE RAND:(1 + 勝負下着判定(ARG))
; 		CASE 1
; 			CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身下着１", "ベビードール"))
; 		CASEELSE
; 			CALLF MAKE_INT(V_NAME, 0)
; 	ENDSELECT
; CASE "コレクション表示可能"
; 	SIF TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "コレクション解禁条件"
; 	CALLF MAKE_STR(V_NAME, "解禁：恋慕")
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "amorpan/sexy/blue/feminine/plain/thong/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能66
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能66(ARG, O_DATA, V_NAME)
; #FUNCTION
; #LOCALSIZE 1
; #LOCALSSIZE 1
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, "gray keyhole thong")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "お尻が丸出しの大胆な")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "はける"
; 	SIF (TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(3 - MIN(1,TALENT:ARG:恋慕) - 勝負下着判定(ARG))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	SELECTCASE RAND:(1 + 勝負下着判定(ARG))
; 		CASE 1
; 			CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身下着１", "ベビードール"))
; 		CASEELSE
; 			CALLF MAKE_INT(V_NAME, 0)
; 	ENDSELECT
; CASE "コレクション表示可能"
; 	SIF TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "コレクション解禁条件"
; 	CALLF MAKE_STR(V_NAME, "解禁：恋慕")
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "beedee/gray/feminine/plain/thong/sexy/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能67
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能67(ARG, O_DATA, V_NAME)
; #FUNCTION
; #LOCALSIZE 1
; #LOCALSSIZE 1
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, "『Big Cock Lover』thong")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "お尻が丸出しの大胆な")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "はける"
; 	SIF (TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(3 - MIN(1,TALENT:ARG:恋慕) - 勝負下着判定(ARG))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	SELECTCASE RAND:(1 + 勝負下着判定(ARG))
; 		CASE 1
; 			CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身下着１", "ベビードール"))
; 		CASEELSE
; 			CALLF MAKE_INT(V_NAME, 0)
; 	ENDSELECT
; CASE "コレクション表示可能"
; 	SIF TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "コレクション解禁条件"
; 	CALLF MAKE_STR(V_NAME, "解禁：恋慕")
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "beedee/black/feminine/printed/thong/sexy/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能68
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能68(ARG, O_DATA, V_NAME)
; #FUNCTION
; #LOCALSIZE 1
; #LOCALSSIZE 1
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, "rose adorned white thong")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "お尻が丸出しの大胆な")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "はける"
; 	SIF (TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(3 - MIN(1,TALENT:ARG:恋慕) - 勝負下着判定(ARG))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	SELECTCASE RAND:(1 + 勝負下着判定(ARG))
; 		CASE 1
; 			CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身下着１", "ベビードール"))
; 		CASEELSE
; 			CALLF MAKE_INT(V_NAME, 0)
; 	ENDSELECT
; CASE "コレクション表示可能"
; 	SIF TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "コレクション解禁条件"
; 	CALLF MAKE_STR(V_NAME, "解禁：恋慕")
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "amorpan/white/feminine/plain/thong/sexy/washable/")
; ENDSELECT

@EXIST_下半身下着_ずらし可能69
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし可能69(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, "Gensou-chan thong")
	CASE "Generic Name"
		CALLF MAKE_STR(V_NAME, "printed thong")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "기저귀 현상")
	CASE "FullDesc"
		CALLF MAKE_STR(V_NAME, "Nullify the burden. The Presence-Null represents a new form of underwear. It has the form of a thong without the necessity of shifting it out to go to the bathroom. The name stems from the nearly-zero feel that the Presence-Null has on the user. Have easier toilet use, elimination training, sex, and more with Presence-Null.")
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 0)
	CASE "はける"
		SIF (TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(3 - MIN(1,TALENT:ARG:恋慕) - 勝負下着判定(ARG))
			CALLF MAKE_INT(V_NAME, 1)
	CASE "関連下着"
		SELECTCASE RAND:(1 + 勝負下着判定(ARG))
			CASE 1
				CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身下着１", "ベビードール"))
			CASEELSE
				CALLF MAKE_INT(V_NAME, 0)
		ENDSELECT
	CASE "コレクション表示可能"
		SIF TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕
			CALLF MAKE_INT(V_NAME, 1)
	CASE "コレクション解禁条件"
		CALLF MAKE_STR(V_NAME, "解禁：恋慕")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, "gensou-chan-ab/multi-color/feminine/printed/thong/sexy/washable/")
ENDSELECT

@EXIST_下半身下着_ずらし可能76
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし可能76(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,2)
			CALLF MAKE_STR(V_NAME, "GC Pantoons+")
		ELSE
			CALLF MAKE_STR(V_NAME, GET_STR(-1, "下半身下着_ずらし可能", ToInt(SUBSTRING(GetDoingFunction(),20)), "Generic Name"))
		ENDIF
	CASE "Generic Name"
		CALLF MAKE_STR(V_NAME, "printed panties")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "기저귀 현상")
	CASE "FullDesc"
		CALLF MAKE_STR(V_NAME, "Make underwear fun again! Pantoons panties have a playful, all over print reminiscent of a baby diaper, based off a random Touhou character. The prints are so adorable to the point that even the most mature melt in cuteness. The + model adds extra absorbency, perfect for small leaks")
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "はける"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "関連下着"
		CALLF MAKE_INT(V_NAME, 0)
	CASE "コレクション表示可能"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, "gensou-chan-ab/multi-color/panty/printed/washable/")
ENDSELECT

@EXIST_下半身下着_ずらし可能81
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし可能81(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,2)
			CALLF MAKE_STR(V_NAME, "GC Brieftoons+")
		ELSE
			CALLF MAKE_STR(V_NAME, GET_STR(-1, "下半身下着_ずらし可能", ToInt(SUBSTRING(GetDoingFunction(),20)), "Generic Name"))
		ENDIF
	CASE "Generic Name"
		CALLF MAKE_STR(V_NAME, "printed briefs")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "기저귀 현상")
	CASE "FullDesc"
		CALLF MAKE_STR(V_NAME, "Make underwear fun again! Brieftoons panties have a playful, all over print reminiscent of a baby diaper, based off a random Touhou character. The prints are so adorable to the point that even the most mature melt in cuteness. The + model adds extra absorbency, perfect for small leaks. Brief variation of the Pantoons.")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "도대체 이거 뭐에 쓰는 거야")
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "はける"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "関連下着"
		CALLF MAKE_INT(V_NAME, 0)
	CASE "コレクション表示可能"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Unisex"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, "gensou-chan-ab/multi-color/brief/printed/washable/")
ENDSELECT
; @EXIST_下半身下着_ずらし可能78
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能78(ARG, O_DATA, V_NAME)
; #FUNCTION
; #LOCALSIZE 1
; #LOCALSSIZE 1
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, "『Oops!』thong")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "お尻が丸出しの大胆な")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "はける"
; 	SIF (TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(3 - MIN(1,TALENT:ARG:恋慕) - 勝負下着判定(ARG))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	SELECTCASE RAND:(1 + 勝負下着判定(ARG))
; 		CASE 1
; 			CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身下着１", "ベビードール"))
; 		CASEELSE
; 			CALLF MAKE_INT(V_NAME, 0)
; 	ENDSELECT
; CASE "コレクション表示可能"
; 	SIF TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "コレクション解禁条件"
; 	CALLF MAKE_STR(V_NAME, "解禁：恋慕")
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "beedee/white/thong/sexy/plain/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能79
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能79(ARG, O_DATA, V_NAME)
; #FUNCTION
; #LOCALSIZE 1
; #LOCALSSIZE 1
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, "『Be Mine』G-string")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "細い紐で腰に結わえた")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "はける"
; 	SIF (TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(3 - MIN(1,TALENT:ARG:恋慕) - 勝負下着判定(ARG))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	SELECTCASE RAND:(1 + 勝負下着判定(ARG))
; 		CASE 1
; 			CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身下着１", "ベビードール"))
; 		CASEELSE
; 			CALLF MAKE_INT(V_NAME, 0)
; 	ENDSELECT
; CASE "コレクション表示可能"
; 	SIF TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "コレクション解禁条件"
; 	CALLF MAKE_STR(V_NAME, "解禁：恋慕")
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "beedee/red/feminine/thong/sexy/printed/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能80
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能80(ARG, O_DATA, V_NAME)
; #FUNCTION
; #LOCALSIZE 1
; #LOCALSSIZE 1
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, "『Too Much Fun』G-string")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "お尻が丸出しの大胆な")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "はける"
; 	SIF (TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(3 - MIN(1,TALENT:ARG:恋慕) - 勝負下着判定(ARG))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	SELECTCASE RAND:(1 + 勝負下着判定(ARG))
; 		CASE 1
; 			CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身下着１", "ベビードール"))
; 		CASEELSE
; 			CALLF MAKE_INT(V_NAME, 0)
; 	ENDSELECT
; CASE "コレクション表示可能"
; 	SIF TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "コレクション解禁条件"
; 	CALLF MAKE_STR(V_NAME, "解禁：恋慕")
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "beedee/red/feminine/thong/sexy/printed/washable/")
; ENDSELECT

@EXIST_下半身下着_ずらし可能82
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし可能82(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, "crotchless bikini bottoms")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "엉덩이가 다 드러난 대담한")
	CASE "FullDesc"
		CALLF MAKE_STR(V_NAME, @"A bikini without the crotch covering part, leaving just the strings. Very teasing.")
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 0)
	CASE "はける"
		SIF (TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(3 - MIN(1,TALENT:ARG:恋慕) - 勝負下着判定(ARG))
			CALLF MAKE_INT(V_NAME, 1)
	CASE "関連下着"
		SELECTCASE RAND:(1 + 勝負下着判定(ARG))
			CASE 1
				CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身下着１", "ベビードール"))
			CASEELSE
				CALLF MAKE_INT(V_NAME, 0)
		ENDSELECT
	CASE "コレクション表示可能"
		SIF TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕
			CALLF MAKE_INT(V_NAME, 1)
	CASE "コレクション解禁条件"
		CALLF MAKE_STR(V_NAME, "解禁：恋慕")
	CASE "Panties Type"
		CALLF MAKE_STR(V_NAME, "Open-Crotch")
	CASE "Pee Capacity"
		CALLF MAKE_INT(V_NAME, 0)
	CASE "Poo Capacity"
		CALLF MAKE_INT(V_NAME, 0)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, "amorpan/white/feminine/bikini/sexy/plain/washable/")
ENDSELECT
; @EXIST_下半身下着_ずらし可能83
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能83(ARG, O_DATA, V_NAME)
; #FUNCTION
; #LOCALSIZE 1
; #LOCALSSIZE 1
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, "black crotchless bikini bottoms")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "お尻が丸出しの大胆な")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "はける"
; 	SIF (TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(3 - MIN(1,TALENT:ARG:恋慕) - 勝負下着判定(ARG))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	SELECTCASE RAND:(1 + 勝負下着判定(ARG))
; 		CASE 1
; 			CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身下着１", "ベビードール"))
; 		CASEELSE
; 			CALLF MAKE_INT(V_NAME, 0)
; 	ENDSELECT
; CASE "コレクション表示可能"
; 	SIF TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "コレクション解禁条件"
; 	CALLF MAKE_STR(V_NAME, "解禁：恋慕")
; CASE "Panties Type"
; 	CALLF MAKE_STR(V_NAME, "Open-Crotch")
; CASE "Pee Capacity"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "Poo Capacity"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "amorpan/black/feminine/bikini/sexy/plain/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能84
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能84(ARG, O_DATA, V_NAME)
; #FUNCTION
; #LOCALSIZE 1
; #LOCALSSIZE 1
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, "red bikini bottoms")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "お尻が丸出しの大胆な")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "はける"
; 	SIF (TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(3 - MIN(1,TALENT:ARG:恋慕) - 勝負下着判定(ARG))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	SELECTCASE RAND:(1 + 勝負下着判定(ARG))
; 		CASE 1
; 			CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身下着１", "ベビードール"))
; 		CASEELSE
; 			CALLF MAKE_INT(V_NAME, 0)
; 	ENDSELECT
; CASE "コレクション表示可能"
; 	SIF TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "コレクション解禁条件"
; 	CALLF MAKE_STR(V_NAME, "解禁：恋慕")
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "urbworld/red/feminine/bikini/plain/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能85
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能85(ARG, O_DATA, V_NAME)
; #FUNCTION
; #LOCALSIZE 1
; #LOCALSSIZE 1
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, "leopard print bikini bottoms")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "お尻が丸出しの大胆な")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "はける"
; 	SIF (TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(3 - MIN(1,TALENT:ARG:恋慕) - 勝負下着判定(ARG))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	SELECTCASE RAND:(1 + 勝負下着判定(ARG))
; 		CASE 1
; 			CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身下着１", "ベビードール"))
; 		CASEELSE
; 			CALLF MAKE_INT(V_NAME, 0)
; 	ENDSELECT
; CASE "コレクション表示可能"
; 	SIF TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "コレクション解禁条件"
; 	CALLF MAKE_STR(V_NAME, "解禁：恋慕")
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "urbworld/multi-color/feminine/bikini/printed/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能86
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能86(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, "『I woke up like this』briefs")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "grey")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "はける"
; 	SIF !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "beedee/pink/feminine/brief/printed/washable/")
; ENDSELECT

@EXIST_下半身下着_ずらし可能87
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし可能87(ARG, O_DATA, V_NAME)
#FUNCTION
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, "crotch rope")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "이미 속옷이 아니다")
	CASE "FullDesc"
		CALLF MAKE_STR(V_NAME, @"A length of rope perverted into a piece of underwear. Stiff, but arousing (for both sides).")
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 0)
	CASE "はける"
		SIF (TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(3 - MIN(1,TALENT:ARG:恋慕) - 勝負下着判定(ARG))
			CALLF MAKE_INT(V_NAME, 1)
	CASE "関連下着"
		SELECTCASE RAND:(1 + 勝負下着判定(ARG))
			CASE 1
				CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身下着１", "ベビードール"))
			CASEELSE
				CALLF MAKE_INT(V_NAME, 0)
		ENDSELECT
	CASE "コレクション表示可能"
		SIF TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕
			CALLF MAKE_INT(V_NAME, 1)
	CASE "コレクション解禁条件"
		CALLF MAKE_STR(V_NAME, "解禁：恋慕")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, "urbworld/white/thong/sexy/sex toy/printed/washable/")
ENDSELECT
; @EXIST_下半身下着_ずらし可能88
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能88(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, "hotpants")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "grey")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "はける"
; 	SIF !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "urbworld/pink/feminine/panty/plain/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能89
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能89(ARG, O_DATA, V_NAME)
; #FUNCTION
; #LOCALSIZE 1
; #LOCALSSIZE 1
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, "pink bikini bottoms")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "お尻が丸出しの大胆な")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "はける"
; 	SIF (TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(3 - MIN(1,TALENT:ARG:恋慕) - 勝負下着判定(ARG))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	SELECTCASE RAND:(1 + 勝負下着判定(ARG))
; 		CASE 1
; 			CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身下着１", "ベビードール"))
; 		CASEELSE
; 			CALLF MAKE_INT(V_NAME, 0)
; 	ENDSELECT
; CASE "コレクション表示可能"
; 	SIF TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "コレクション解禁条件"
; 	CALLF MAKE_STR(V_NAME, "解禁：恋慕")
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "urbworld/pink/feminine/bikini/plain/washable/")
; ENDSELECT

@EXIST_下半身下着_ずらし可能90
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし可能90(ARG, O_DATA, V_NAME)
#FUNCTION
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, @"double-dildo panties")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "팬티 플러그 더블")
	CASE "FullDesc"
		CALLF MAKE_STR(V_NAME, @"Two thick dildos to keep the wearer's vagina and anus gaping and full, strapped to a regular pair of simple panties.")
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "はける"
		IF IS_VIRGIN(ARG)
			CALLF MAKE_INT(V_NAME, 0)
		ELSEIF (TALENT:ARG:184 || TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(1 + (2 * 勝負下着判定(ARG)))
			CALLF MAKE_INT(V_NAME, 1)
		ENDIF
	CASE "関連下着"
		CALLF MAKE_INT(V_NAME, 0)
	CASE "コレクション表示可能"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, "efun/black/feminine/panty/sex toy/sexy/plain/washable/")
ENDSELECT

@EXIST_下半身下着_ずらし可能91
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし可能91(ARG, O_DATA, V_NAME)
#FUNCTION
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, @"buttplug panties")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "팬티 플러그 애널")
	CASE "FullDesc"
		CALLF MAKE_STR(V_NAME, @"A lacy pair of panties combined with a thick anal plug. Keeps rectums satisfied and bowels full.")
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "はける"
		IF IS_VIRGIN(ARG)
			CALLF MAKE_INT(V_NAME, 0)
		ELSEIF (TALENT:ARG:184 || TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(1 + (2 * 勝負下着判定(ARG)))
			CALLF MAKE_INT(V_NAME, 1)
		ENDIF
	CASE "関連下着"
		CALLF MAKE_INT(V_NAME, 0)
	CASE "コレクション表示可能"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, "efun/black/panty/sex toy/sexy/plain/washable/")
ENDSELECT

@EXIST_下半身下着_ずらし可能92
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし可能92(ARG, O_DATA, V_NAME)
#FUNCTION
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, @"beaded panties")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "팬티 비드")
	CASE "FullDesc"
		CALLF MAKE_STR(V_NAME, @"Beads formed to create a pair of panties. The beads are flashy and definitely give some sex appeal. The beads also shake when the user moves, announcing their presence.")
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "はける"
		IF IS_VIRGIN(ARG)
			CALLF MAKE_INT(V_NAME, 0)
		ELSEIF (TALENT:ARG:184 || TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(1 + (2 * 勝負下着判定(ARG)))
			CALLF MAKE_INT(V_NAME, 1)
		ENDIF
	CASE "関連下着"
		CALLF MAKE_INT(V_NAME, 0)
	CASE "コレクション表示可能"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, "efun/black/feminine/panty/sex toy/plain/washable/")
ENDSELECT
; @EXIST_下半身下着_ずらし可能93
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能93(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, @"『Fill Me Up』hotpants")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "wtf is this used for")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "はける"
; 	IF IS_VIRGIN(ARG)
; 		CALLF MAKE_INT(V_NAME, 0)
; 	ELSEIF (TALENT:ARG:184 || TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; 	ENDIF
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "beedee/sexy/black/feminine/panty/printed/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能94
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能94(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, @"『Baby Factory』 thong")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "wtf is this used for")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "はける"
; 	IF IS_VIRGIN(ARG)
; 		CALLF MAKE_INT(V_NAME, 0)
; 	ELSEIF (TALENT:ARG:184 || TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; 	ENDIF
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "beedee/black/feminine/thong/sexy/printed/washable/")
; ENDSELECT

@EXIST_下半身下着_ずらし可能112
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし可能112(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, GET_STR(-1, "下半身下着_ずらし可能", ToInt(SUBSTRING(GetDoingFunction(),20)), "Generic Name"))
	CASE "Generic Name"
		CALLF MAKE_STR(V_NAME, "barrier panties")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "팬티 장벽")
	CASE "FullDesc"
		CALLF MAKE_STR(V_NAME, "Barrier panties aren't like regular panties. These have the ability to open a pocket dimension that traps your waste and any external threats to the underwear. Worry no more about making it to the toilet, because you can go inside of them with no consequences. No sag, no stink, no bulk, and most importantly, you'll always stay clean.")
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "はける"
		;IF !GROUPMATCH(ARG,26) && (ARG == MASTER && !ITEM:BarrierPanties)
		IF !GROUPMATCH(ARG,26)
			CALLF MAKE_INT(V_NAME, 0)
		ELSE
			CALLF MAKE_INT(V_NAME, 1)
		ENDIF
	CASE "Panties Type"
		CALLF MAKE_STR(V_NAME, "Underwear")
	CASE "Sub Type"
		CALLF MAKE_STR(V_NAME, "Barrier")
	CASE "関連下着"
		CALLF MAKE_INT(V_NAME, 0)
	CASE "コレクション表示可能"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, "black/panty/plain/washable/")
ENDSELECT

@EXIST_下半身下着_ずらし可能169
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし可能169(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, GET_STR(-1, "下半身下着_ずらし可能", ToInt(SUBSTRING(GetDoingFunction(),20)), "Generic Name"))
	CASE "Generic Name"
		CALLF MAKE_STR(V_NAME, "loincloth")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "팬티 로인 클로스")
	CASE "FullDesc"
		CALLF MAKE_STR(V_NAME, "A simple loincloth to hide one's groin area. It's a one-piece cloth solution that cover's the wearer's genitals, butt, and a small part of their legs.")
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "はける"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Panties Type"
		CALLF MAKE_STR(V_NAME, "Underwear")
	CASE "関連下着"
		CALLF MAKE_INT(V_NAME, 0)
	CASE "コレクション表示可能"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, "rw tailor/white/panty/plain/washable/")
ENDSELECT

@EXIST_下半身下着_ずらし可能174
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし可能174(ARG, O_DATA, V_NAME)
#FUNCTION
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, "『I-Owe-You』Tag")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "이우유 태그")
	CASE "FullDesc"
		CALLF MAKE_STR(V_NAME, @"If you got this, then there's no panties that you have that matches your preferences, or the game ran out of panties for you to wear. Report this!")
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "はける"
		CALLF MAKE_INT(V_NAME, 0)
	CASE "関連下着"
		CALLF MAKE_INT(V_NAME, 0)
	CASE "Unisex"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Panties Type"
		CALLF MAKE_STR(V_NAME, "Open-Crotch")
	CASE "コレクション表示可能"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, "")
ENDSELECT

@EXIST_下半身下着_ずらし可能182
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし可能182(ARG, O_DATA, V_NAME)
#FUNCTION
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, @"piddle-retardant pants")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "팬티가 흘러내린다.")
	CASE "FullDesc"
		CALLF MAKE_STR(V_NAME, @"This pair of briefs has a layer of absorbent material at the front and back. Despite it's very slim profile it's able to hold a couple bladders of urine confidently.")
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "はける"
		CALLF MAKE_INT(V_NAME, 0)
	CASE "関連下着"
		CALLF MAKE_INT(V_NAME, 0)
	CASE "コレクション表示可能"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Panties Type"
		CALLF MAKE_STR(V_NAME, "Underwear")
	CASE "Pee Capacity"
		CALLF MAKE_INT(V_NAME, 1520)
	CASE "Poo Capacity"
		CALLF MAKE_INT(V_NAME, 1210)
	CASE "Quest Only"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, "beedee/white/feminine/briefs/washable/")
ENDSELECT
; @EXIST_下半身下着_ずらし可能183
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能183(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, @"『Sure Thing』panties")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "wtf is this used for")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "はける"
; 	SIF (TALENT:ARG:184 || TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "beedee/sexy/green/panty/feminine/printed/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能184
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能184(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, @"blue panties")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "wtf is this used for")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "はける"
; 	SIF !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "urbworld/blue/panty/plain/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能185
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能185(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, @"『I Love Sucking Cocks』panties")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "black")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "はける"
; 	SIF (TALENT:ARG:184 || TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "beedee/sexy/black/panty/feminine/printed/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能186
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能186(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, @"comfy black panties")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "wtf is this used for")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "はける"
; 	SIF !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "urbworld/black/panty/plain/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能187
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能187(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, @"『Daddy's Little Girl』panties")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "wtf is this used for")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "はける"
; 	SIF (TALENT:ARG:184 || TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "beedee/sexy/pink/panty/printed/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能188
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能188(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, @"『Blowjob Queen』trunks")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "pink with a white waistband")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "はける"
; 	SIF (TALENT:ARG:184 || TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "beedee/sexy/pink/feminine/printed/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能189
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能189(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, @"『Spank Me』panties")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "wtf is this used for")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "はける"
; 	SIF (TALENT:ARG:184 || TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "beedee/sexy/purple/feminine/printed/washable/")
; ENDSELECT
