; Modular ERB: OMOGATARI/ITEM/Clothing/Omogatari_Underwear.ERB
;=============================================================================================================
; Underwear (normal)
;=============================================================================================================
; @EXIST_下半身下着_ずらし可能40
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能40(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, "briefs")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "男物の")
; CASE "FullDesc"
; 	CALLF MAKE_STR(V_NAME, @"Boyish briefs, also known as tidy whities. A universal underwear choice for everyone with a working bladder. Has a slot to push a penis through for easy urinal access and sex.")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "はける"
; 	SIF !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "Unisex"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "urbworld/white/masculine/brief/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能41
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能41(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, "red briefs")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "wtf is this used for")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "はける"
; 	SIF !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "Unisex"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "urbworld/red/masculine/brief/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能42
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能42(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, "purple briefs")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "wtf is this used for")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "はける"
; 	SIF !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "Unisex"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "urbworld/purple/masculine/brief/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能43
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能43(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, "blue briefs")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "wtf is this used for")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "はける"
; 	SIF !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "Unisex"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "urbworld/masculine/brief/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能44
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能44(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, "『Up All Night』briefs")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "grey")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "はける"
; 	SIF (TALENT:ARG:184 || TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "Unisex"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "amorpan/sexy/black/feminine/brief/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能45
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能45(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, "『Let's Get a Room』briefs")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "blue")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "はける"
; 	SIF (TALENT:ARG:184 || TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "Unisex"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "beedee/sexy/white/printed/feminine/brief/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能46
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能46(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, "『Good Girl』briefs")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "white")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "はける"
; 	SIF (TALENT:ARG:184 || TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "beedee/sexy/red/feminine/brief/printed/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能47
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能47(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, "『Hole』briefs")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "pink")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "はける"
; 	SIF (TALENT:ARG:184 || TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "Unisex"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "beedee/sexy/purple/gray/feminine/brief/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能48
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能48(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, "『Feeling Lucky』briefs")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "green")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "はける"
; 	SIF (TALENT:ARG:184 || TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "Unisex"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "beedee/sexy/blue/feminine/panty/printed/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能49
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能49(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, "『See You Tonight』briefs")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "red")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "はける"
; 	SIF (TALENT:ARG:184 || TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "Unisex"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "beedee/sexy/white/feminine/brief/washable/")
; ENDSELECT

@EXIST_下半身下着_ずらし可能50
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし可能50(ARG, O_DATA, V_NAME)
#FUNCTION
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, "high waist shiny silver panties")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "어른의 매력이 넘치는")
	CASE "FullDesc"
		CALLF MAKE_STR(V_NAME, @"남들의 시선을 즐기는 안목 높은 숙녀를 위한 반짝이는 팬티. 은빛 천이 빛을 반사하고 허리가 높아, 팬티를 세상에 당당히 드러내기 좋다.")
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "はける"
		SIF !RAND:(1 + (2 * 勝負下着判定(ARG)))
			CALLF MAKE_INT(V_NAME, 1)
	CASE "関連下着"
		CALLF MAKE_INT(V_NAME, 0)
	CASE "コレクション表示可能"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "SHIFTABLE"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, "amorpan/sexy/gray/shiny/feminine/panty/washable/")
ENDSELECT
; @EXIST_下半身下着_ずらし可能51
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能51(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, @"『I %UNICODE(0x2764)% girls』briefs")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "pink poka-dots")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "はける"
; 	SIF (TALENT:ARG:184 || TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "Unisex"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "beedee/sexy/pink/printed/feminine/brief/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能52
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能52(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, @"『Trick Me』briefs")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "red-orange")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "はける"
; 	SIF (TALENT:ARG:184 || TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "Unisex"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "beedee/sexy/white/printed/brief/washable/")
; ENDSELECT

@EXIST_下半身下着_ずらし可能53
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし可能53(ARG, O_DATA, V_NAME)
#FUNCTION
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, @"hi-cut briefs")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "평소보다 허리선이 높은 브리프. 보통 운동선수들이 입는데, 떨어지기 어렵게 해주기 때문이다.")
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "はける"
		SIF !RAND:(1 + (2 * 勝負下着判定(ARG)))
			CALLF MAKE_INT(V_NAME, 1)
	CASE "関連下着"
		CALLF MAKE_INT(V_NAME, 0)
	CASE "Unisex"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "コレクション表示可能"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, "urbworld/gray/masculine/brief/plain/washable/")
ENDSELECT

@EXIST_下半身下着_ずらし可能54
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし可能54(ARG, O_DATA, V_NAME)
#FUNCTION
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, @"sheer briefs")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "음란한")
	CASE "FullDesc"
		CALLF MAKE_STR(V_NAME, @"마음껏 상대를 애태우고 싶을 때 입는 반투명 속옷. 얇고 비치는 천 너머로 은밀한 곳과 엉덩이, 다리까지 훤히 드러난다.")
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 0)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 0)
	CASE "はける"
		SIF !RAND:(1 + (2 * 勝負下着判定(ARG)))
			CALLF MAKE_INT(V_NAME, 1)
	CASE "関連下着"
		CALLF MAKE_INT(V_NAME, 0)
	CASE "Unisex"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "コレクション表示可能"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, "amorpan/sexy/white/sheer/feminine/panty/washable/")
ENDSELECT
; @EXIST_下半身下着_ずらし可能55
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能55(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, @"sheer nylon briefs")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "wtf is this used for")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "はける"
; 	SIF !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "Unisex"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "amorpan/sexy/white/sheer/feminine/panty/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能56
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能56(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, @"frilly black open-crotch panties")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "wtf is this used for")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "はける"
; 	SIF (TALENT:ARG:184 || TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Panties Type"
; 	CALLF MAKE_STR(V_NAME, "Open-Crotch")
; CASE "Pee Capacity"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "Poo Capacity"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "amorpan/sexy/black/panty/feminine/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能57
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能57(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, @"demin cuttoffs")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "wtf is this used for")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "はける"
; 	SIF !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; ENDSELECT

@EXIST_下半身下着_ずらし可能58
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし可能58(ARG, O_DATA, V_NAME)
#FUNCTION
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, @"latex plug panties")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "팬티 플러그")
	CASE "FullDesc"
		CALLF MAKE_STR(V_NAME, @"두꺼운 딜도가 달린 레이스 팬티. 착용자의 질을 충분히 풀어 삽입을 쉽게 하고, 은근한 쾌감도 계속 안겨 준다.")
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "はける"
		IF IS_VIRGIN(ARG)
			CALLF MAKE_INT(V_NAME, 0)
		ELSEIF (TALENT:ARG:184 || TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(1 + (2 * 勝負下着判定(ARG)))
			CALLF MAKE_INT(V_NAME, 1)
		ELSE
			CALLF MAKE_INT(V_NAME, 0)
		ENDIF
	CASE "関連下着"
		CALLF MAKE_INT(V_NAME, 0)
	CASE "コレクション表示可能"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, "efun/black/panty/sex toy/sexy/washable/")
ENDSELECT
; @EXIST_下半身下着_ずらし可能59
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能59(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, @"『Breed Me』panties")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "white")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "はける"
; 	SIF (TALENT:ARG:184 || TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "beedee/sexy/white/feminine/printed/panty/washable/")
; ENDSELECT

@EXIST_下半身下着_ずらし可能60
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし可能60(ARG, O_DATA, V_NAME)
#FUNCTION
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, @"comfy panties")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "복슬복슬한")
	CASE "FullDesc"
		CALLF MAKE_STR(V_NAME, @"마음 편히 쉬고 싶을 때 입는 포근한 양모 팬티. 너무 편해진 나머지 안에 한 무더기를 싸 버리지는 말자. 그런 용도로 만든 물건은 아니다.")
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "はける"
		SIF !RAND:(1 + (2 * 勝負下着判定(ARG)))
			CALLF MAKE_INT(V_NAME, 1)
	CASE "関連下着"
		CALLF MAKE_INT(V_NAME, 0)
	CASE "コレクション表示可能"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, "urbworld/purple/plain/panty/washable/")
ENDSELECT
; @EXIST_下半身下着_ずらし可能62
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能62(ARG, O_DATA, V_NAME)
; #FUNCTION
; #LOCALSIZE 1
; #LOCALSSIZE 1
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, "pink thong")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "お尻が丸出しの大胆な")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "はける"
; 	SIF (TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(3 - MIN(1,TALENT:ARG:恋慕) - 勝負下着判定(ARG))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	SELECTCASE RAND:(1 + 勝負下着判定(ARG))
; 		CASE 1
; 			CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身下着１", "ベビードール"))
; 		CASEELSE
; 			CALLF MAKE_INT(V_NAME, 0)
; 	ENDSELECT
; CASE "コレクション表示可能"
; 	SIF TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "コレクション解禁条件"
; 	CALLF MAKE_STR(V_NAME, "解禁：恋慕")
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "amorpan/sexy/pink/feminine/plain/thong/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能63
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能63(ARG, O_DATA, V_NAME)
; #FUNCTION
; #LOCALSIZE 1
; #LOCALSSIZE 1
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, "『Pretty Prettier Prettiest』thong")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "お尻が丸出しの大胆な")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "はける"
; 	SIF (TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(3 - MIN(1,TALENT:ARG:恋慕) - 勝負下着判定(ARG))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	SELECTCASE RAND:(1 + 勝負下着判定(ARG))
; 		CASE 1
; 			CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身下着１", "ベビードール"))
; 		CASEELSE
; 			CALLF MAKE_INT(V_NAME, 0)
; 	ENDSELECT
; CASE "コレクション表示可能"
; 	SIF TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "コレクション解禁条件"
; 	CALLF MAKE_STR(V_NAME, "解禁：恋慕")
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "beedee/white/feminine/printed/thong/sexy/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能64
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能64(ARG, O_DATA, V_NAME)
; #FUNCTION
; #LOCALSIZE 1
; #LOCALSSIZE 1
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, "blue thong")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "お尻が丸出しの大胆な")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "はける"
; 	SIF (TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(3 - MIN(1,TALENT:ARG:恋慕) - 勝負下着判定(ARG))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	SELECTCASE RAND:(1 + 勝負下着判定(ARG))
; 		CASE 1
; 			CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身下着１", "ベビードール"))
; 		CASEELSE
; 			CALLF MAKE_INT(V_NAME, 0)
; 	ENDSELECT
; CASE "コレクション表示可能"
; 	SIF TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "コレクション解禁条件"
; 	CALLF MAKE_STR(V_NAME, "解禁：恋慕")
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "amorpan/sexy/blue/feminine/plain/thong/sexy/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能65
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能65(ARG, O_DATA, V_NAME)
; #FUNCTION
; #LOCALSIZE 1
; #LOCALSSIZE 1
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, "flirty thong")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "お尻が丸出しの大胆な")
; CASE "FullDesc"
; 	CALLF MAKE_STR(V_NAME, @"A very thin thong who's main job is to tease.")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "はける"
; 	SIF (TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(3 - MIN(1,TALENT:ARG:恋慕) - 勝負下着判定(ARG))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	SELECTCASE RAND:(1 + 勝負下着判定(ARG))
; 		CASE 1
; 			CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身下着１", "ベビードール"))
; 		CASEELSE
; 			CALLF MAKE_INT(V_NAME, 0)
; 	ENDSELECT
; CASE "コレクション表示可能"
; 	SIF TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "コレクション解禁条件"
; 	CALLF MAKE_STR(V_NAME, "解禁：恋慕")
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "amorpan/sexy/blue/feminine/plain/thong/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能66
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能66(ARG, O_DATA, V_NAME)
; #FUNCTION
; #LOCALSIZE 1
; #LOCALSSIZE 1
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, "gray keyhole thong")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "お尻が丸出しの大胆な")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "はける"
; 	SIF (TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(3 - MIN(1,TALENT:ARG:恋慕) - 勝負下着判定(ARG))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	SELECTCASE RAND:(1 + 勝負下着判定(ARG))
; 		CASE 1
; 			CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身下着１", "ベビードール"))
; 		CASEELSE
; 			CALLF MAKE_INT(V_NAME, 0)
; 	ENDSELECT
; CASE "コレクション表示可能"
; 	SIF TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "コレクション解禁条件"
; 	CALLF MAKE_STR(V_NAME, "解禁：恋慕")
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "beedee/gray/feminine/plain/thong/sexy/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能67
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能67(ARG, O_DATA, V_NAME)
; #FUNCTION
; #LOCALSIZE 1
; #LOCALSSIZE 1
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, "『Big Cock Lover』thong")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "お尻が丸出しの大胆な")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "はける"
; 	SIF (TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(3 - MIN(1,TALENT:ARG:恋慕) - 勝負下着判定(ARG))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	SELECTCASE RAND:(1 + 勝負下着判定(ARG))
; 		CASE 1
; 			CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身下着１", "ベビードール"))
; 		CASEELSE
; 			CALLF MAKE_INT(V_NAME, 0)
; 	ENDSELECT
; CASE "コレクション表示可能"
; 	SIF TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "コレクション解禁条件"
; 	CALLF MAKE_STR(V_NAME, "解禁：恋慕")
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "beedee/black/feminine/printed/thong/sexy/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能68
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能68(ARG, O_DATA, V_NAME)
; #FUNCTION
; #LOCALSIZE 1
; #LOCALSSIZE 1
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, "rose adorned white thong")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "お尻が丸出しの大胆な")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "はける"
; 	SIF (TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(3 - MIN(1,TALENT:ARG:恋慕) - 勝負下着判定(ARG))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	SELECTCASE RAND:(1 + 勝負下着判定(ARG))
; 		CASE 1
; 			CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身下着１", "ベビードール"))
; 		CASEELSE
; 			CALLF MAKE_INT(V_NAME, 0)
; 	ENDSELECT
; CASE "コレクション表示可能"
; 	SIF TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "コレクション解禁条件"
; 	CALLF MAKE_STR(V_NAME, "解禁：恋慕")
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "amorpan/white/feminine/plain/thong/sexy/washable/")
; ENDSELECT

@EXIST_下半身下着_ずらし可能69
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし可能69(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, "Gensou-chan thong")
	CASE "Generic Name"
		CALLF MAKE_STR(V_NAME, "프린트 끈팬티")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "기저귀 현상")
	CASE "FullDesc"
		CALLF MAKE_STR(V_NAME, "거추장스러움을 없애다. Presence-Null은 새로운 형태의 속옷이다. 끈팬티 형태지만 화장실에 갈 때 옆으로 치울 필요가 없다. 이름처럼 착용감은 거의 느껴지지 않는다. 화장실 이용부터 배변 훈련, 성행위까지 무엇이든 한결 편하게 해 주는 Presence-Null.")
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 0)
	CASE "はける"
		SIF (TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(3 - MIN(1,TALENT:ARG:恋慕) - 勝負下着判定(ARG))
			CALLF MAKE_INT(V_NAME, 1)
	CASE "関連下着"
		SELECTCASE RAND:(1 + 勝負下着判定(ARG))
			CASE 1
				CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身下着１", "ベビードール"))
			CASEELSE
				CALLF MAKE_INT(V_NAME, 0)
		ENDSELECT
	CASE "コレクション表示可能"
		SIF TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕
			CALLF MAKE_INT(V_NAME, 1)
	CASE "コレクション解禁条件"
		CALLF MAKE_STR(V_NAME, "解禁：恋慕")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, "gensou-chan-ab/multi-color/feminine/printed/thong/sexy/washable/")
ENDSELECT

@EXIST_下半身下着_ずらし可能76
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし可能76(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,2)
			CALLF MAKE_STR(V_NAME, "GC 팬툰즈+")
		ELSE
			CALLF MAKE_STR(V_NAME, GET_STR(-1, "下半身下着_ずらし可能", ToInt(SUBSTRING(GetDoingFunction(),20)), "Generic Name"))
		ENDIF
	CASE "Generic Name"
		CALLF MAKE_STR(V_NAME, "프린트 팬티")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "기저귀 현상")
	CASE "FullDesc"
		CALLF MAKE_STR(V_NAME, "속옷을 다시 즐겁게! 팬툰즈 팬티는 무작위 동방 캐릭터를 소재로, 아기 기저귀를 닮은 장난스러운 전면 무늬를 담았다. 어른도 마음이 녹을 만큼 사랑스럽다. + 모델은 흡수력을 더해 가벼운 실수까지 받아 준다.")
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "はける"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "関連下着"
		CALLF MAKE_INT(V_NAME, 0)
	CASE "コレクション表示可能"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, "gensou-chan-ab/multi-color/panty/printed/washable/")
ENDSELECT

@EXIST_下半身下着_ずらし可能81
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし可能81(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		IF GETBIT(nVerboseItems,2)
			CALLF MAKE_STR(V_NAME, "GC 브리프툰즈+")
		ELSE
			CALLF MAKE_STR(V_NAME, GET_STR(-1, "下半身下着_ずらし可能", ToInt(SUBSTRING(GetDoingFunction(),20)), "Generic Name"))
		ENDIF
	CASE "Generic Name"
		CALLF MAKE_STR(V_NAME, "프린트 브리프")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "기저귀 현상")
	CASE "FullDesc"
		CALLF MAKE_STR(V_NAME, "속옷을 다시 즐겁게! 브리프툰즈 팬티는 무작위 동방 캐릭터를 소재로, 아기 기저귀를 닮은 장난스러운 전면 무늬를 담았다. 어른도 마음이 녹을 만큼 사랑스럽다. + 모델은 흡수력을 더해 가벼운 실수까지 받아 준다. 팬툰즈의 브리프형 제품.")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "도대체 이거 뭐에 쓰는 거야")
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "はける"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "関連下着"
		CALLF MAKE_INT(V_NAME, 0)
	CASE "コレクション表示可能"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Unisex"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, "gensou-chan-ab/multi-color/brief/printed/washable/")
ENDSELECT
; @EXIST_下半身下着_ずらし可能78
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能78(ARG, O_DATA, V_NAME)
; #FUNCTION
; #LOCALSIZE 1
; #LOCALSSIZE 1
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, "『Oops!』thong")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "お尻が丸出しの大胆な")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "はける"
; 	SIF (TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(3 - MIN(1,TALENT:ARG:恋慕) - 勝負下着判定(ARG))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	SELECTCASE RAND:(1 + 勝負下着判定(ARG))
; 		CASE 1
; 			CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身下着１", "ベビードール"))
; 		CASEELSE
; 			CALLF MAKE_INT(V_NAME, 0)
; 	ENDSELECT
; CASE "コレクション表示可能"
; 	SIF TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "コレクション解禁条件"
; 	CALLF MAKE_STR(V_NAME, "解禁：恋慕")
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "beedee/white/thong/sexy/plain/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能79
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能79(ARG, O_DATA, V_NAME)
; #FUNCTION
; #LOCALSIZE 1
; #LOCALSSIZE 1
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, "『Be Mine』G-string")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "細い紐で腰に結わえた")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "はける"
; 	SIF (TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(3 - MIN(1,TALENT:ARG:恋慕) - 勝負下着判定(ARG))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	SELECTCASE RAND:(1 + 勝負下着判定(ARG))
; 		CASE 1
; 			CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身下着１", "ベビードール"))
; 		CASEELSE
; 			CALLF MAKE_INT(V_NAME, 0)
; 	ENDSELECT
; CASE "コレクション表示可能"
; 	SIF TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "コレクション解禁条件"
; 	CALLF MAKE_STR(V_NAME, "解禁：恋慕")
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "beedee/red/feminine/thong/sexy/printed/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能80
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能80(ARG, O_DATA, V_NAME)
; #FUNCTION
; #LOCALSIZE 1
; #LOCALSSIZE 1
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, "『Too Much Fun』G-string")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "お尻が丸出しの大胆な")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "はける"
; 	SIF (TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(3 - MIN(1,TALENT:ARG:恋慕) - 勝負下着判定(ARG))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	SELECTCASE RAND:(1 + 勝負下着判定(ARG))
; 		CASE 1
; 			CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身下着１", "ベビードール"))
; 		CASEELSE
; 			CALLF MAKE_INT(V_NAME, 0)
; 	ENDSELECT
; CASE "コレクション表示可能"
; 	SIF TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "コレクション解禁条件"
; 	CALLF MAKE_STR(V_NAME, "解禁：恋慕")
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "beedee/red/feminine/thong/sexy/printed/washable/")
; ENDSELECT

@EXIST_下半身下着_ずらし可能82
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし可能82(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, "crotchless bikini bottoms")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "엉덩이가 다 드러난 대담한")
	CASE "FullDesc"
		CALLF MAKE_STR(V_NAME, @"가랑이를 가리는 천 없이 끈만 남긴 비키니. 몹시 노골적으로 상대를 애태운다.")
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 0)
	CASE "はける"
		SIF (TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(3 - MIN(1,TALENT:ARG:恋慕) - 勝負下着判定(ARG))
			CALLF MAKE_INT(V_NAME, 1)
	CASE "関連下着"
		SELECTCASE RAND:(1 + 勝負下着判定(ARG))
			CASE 1
				CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身下着１", "ベビードール"))
			CASEELSE
				CALLF MAKE_INT(V_NAME, 0)
		ENDSELECT
	CASE "コレクション表示可能"
		SIF TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕
			CALLF MAKE_INT(V_NAME, 1)
	CASE "コレクション解禁条件"
		CALLF MAKE_STR(V_NAME, "解禁：恋慕")
	CASE "Panties Type"
		CALLF MAKE_STR(V_NAME, "Open-Crotch")
	CASE "Pee Capacity"
		CALLF MAKE_INT(V_NAME, 0)
	CASE "Poo Capacity"
		CALLF MAKE_INT(V_NAME, 0)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, "amorpan/white/feminine/bikini/sexy/plain/washable/")
ENDSELECT
; @EXIST_下半身下着_ずらし可能83
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能83(ARG, O_DATA, V_NAME)
; #FUNCTION
; #LOCALSIZE 1
; #LOCALSSIZE 1
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, "black crotchless bikini bottoms")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "お尻が丸出しの大胆な")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "はける"
; 	SIF (TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(3 - MIN(1,TALENT:ARG:恋慕) - 勝負下着判定(ARG))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	SELECTCASE RAND:(1 + 勝負下着判定(ARG))
; 		CASE 1
; 			CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身下着１", "ベビードール"))
; 		CASEELSE
; 			CALLF MAKE_INT(V_NAME, 0)
; 	ENDSELECT
; CASE "コレクション表示可能"
; 	SIF TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "コレクション解禁条件"
; 	CALLF MAKE_STR(V_NAME, "解禁：恋慕")
; CASE "Panties Type"
; 	CALLF MAKE_STR(V_NAME, "Open-Crotch")
; CASE "Pee Capacity"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "Poo Capacity"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "amorpan/black/feminine/bikini/sexy/plain/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能84
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能84(ARG, O_DATA, V_NAME)
; #FUNCTION
; #LOCALSIZE 1
; #LOCALSSIZE 1
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, "red bikini bottoms")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "お尻が丸出しの大胆な")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "はける"
; 	SIF (TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(3 - MIN(1,TALENT:ARG:恋慕) - 勝負下着判定(ARG))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	SELECTCASE RAND:(1 + 勝負下着判定(ARG))
; 		CASE 1
; 			CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身下着１", "ベビードール"))
; 		CASEELSE
; 			CALLF MAKE_INT(V_NAME, 0)
; 	ENDSELECT
; CASE "コレクション表示可能"
; 	SIF TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "コレクション解禁条件"
; 	CALLF MAKE_STR(V_NAME, "解禁：恋慕")
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "urbworld/red/feminine/bikini/plain/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能85
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能85(ARG, O_DATA, V_NAME)
; #FUNCTION
; #LOCALSIZE 1
; #LOCALSSIZE 1
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, "leopard print bikini bottoms")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "お尻が丸出しの大胆な")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "はける"
; 	SIF (TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(3 - MIN(1,TALENT:ARG:恋慕) - 勝負下着判定(ARG))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	SELECTCASE RAND:(1 + 勝負下着判定(ARG))
; 		CASE 1
; 			CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身下着１", "ベビードール"))
; 		CASEELSE
; 			CALLF MAKE_INT(V_NAME, 0)
; 	ENDSELECT
; CASE "コレクション表示可能"
; 	SIF TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "コレクション解禁条件"
; 	CALLF MAKE_STR(V_NAME, "解禁：恋慕")
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "urbworld/multi-color/feminine/bikini/printed/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能86
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能86(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, "『I woke up like this』briefs")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "grey")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "はける"
; 	SIF !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "beedee/pink/feminine/brief/printed/washable/")
; ENDSELECT

@EXIST_下半身下着_ずらし可能87
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし可能87(ARG, O_DATA, V_NAME)
#FUNCTION
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, "crotch rope")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "이미 속옷이 아니다")
	CASE "FullDesc"
		CALLF MAKE_STR(V_NAME, @"밧줄 한 가닥을 음란한 속옷으로 엮었다. 뻣뻣하지만, 착용자와 바라보는 사람 모두를 달아오르게 한다.")
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 0)
	CASE "はける"
		SIF (TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(3 - MIN(1,TALENT:ARG:恋慕) - 勝負下着判定(ARG))
			CALLF MAKE_INT(V_NAME, 1)
	CASE "関連下着"
		SELECTCASE RAND:(1 + 勝負下着判定(ARG))
			CASE 1
				CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身下着１", "ベビードール"))
			CASEELSE
				CALLF MAKE_INT(V_NAME, 0)
		ENDSELECT
	CASE "コレクション表示可能"
		SIF TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕
			CALLF MAKE_INT(V_NAME, 1)
	CASE "コレクション解禁条件"
		CALLF MAKE_STR(V_NAME, "解禁：恋慕")
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, "urbworld/white/thong/sexy/sex toy/printed/washable/")
ENDSELECT
; @EXIST_下半身下着_ずらし可能88
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能88(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, "hotpants")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "grey")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "はける"
; 	SIF !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "urbworld/pink/feminine/panty/plain/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能89
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能89(ARG, O_DATA, V_NAME)
; #FUNCTION
; #LOCALSIZE 1
; #LOCALSSIZE 1
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, "pink bikini bottoms")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "お尻が丸出しの大胆な")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "はける"
; 	SIF (TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(3 - MIN(1,TALENT:ARG:恋慕) - 勝負下着判定(ARG))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	SELECTCASE RAND:(1 + 勝負下着判定(ARG))
; 		CASE 1
; 			CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身下着１", "ベビードール"))
; 		CASEELSE
; 			CALLF MAKE_INT(V_NAME, 0)
; 	ENDSELECT
; CASE "コレクション表示可能"
; 	SIF TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "コレクション解禁条件"
; 	CALLF MAKE_STR(V_NAME, "解禁：恋慕")
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "urbworld/pink/feminine/bikini/plain/washable/")
; ENDSELECT

@EXIST_下半身下着_ずらし可能90
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし可能90(ARG, O_DATA, V_NAME)
#FUNCTION
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, @"double-dildo panties")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "팬티 플러그 더블")
	CASE "FullDesc"
		CALLF MAKE_STR(V_NAME, @"평범한 팬티에 두꺼운 딜도 두 개를 단 물건. 착용자의 질과 항문을 벌어진 채 가득 채워 둔다.")
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "はける"
		IF IS_VIRGIN(ARG)
			CALLF MAKE_INT(V_NAME, 0)
		ELSEIF (TALENT:ARG:184 || TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(1 + (2 * 勝負下着判定(ARG)))
			CALLF MAKE_INT(V_NAME, 1)
		ENDIF
	CASE "関連下着"
		CALLF MAKE_INT(V_NAME, 0)
	CASE "コレクション表示可能"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, "efun/black/feminine/panty/sex toy/sexy/plain/washable/")
ENDSELECT

@EXIST_下半身下着_ずらし可能91
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし可能91(ARG, O_DATA, V_NAME)
#FUNCTION
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, @"buttplug panties")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "팬티 플러그 애널")
	CASE "FullDesc"
		CALLF MAKE_STR(V_NAME, @"두꺼운 애널 플러그를 단 레이스 팬티. 직장에는 만족감을, 장에는 묵직한 충만감을 안겨 준다.")
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "はける"
		IF IS_VIRGIN(ARG)
			CALLF MAKE_INT(V_NAME, 0)
		ELSEIF (TALENT:ARG:184 || TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(1 + (2 * 勝負下着判定(ARG)))
			CALLF MAKE_INT(V_NAME, 1)
		ENDIF
	CASE "関連下着"
		CALLF MAKE_INT(V_NAME, 0)
	CASE "コレクション表示可能"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, "efun/black/panty/sex toy/sexy/plain/washable/")
ENDSELECT

@EXIST_下半身下着_ずらし可能92
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし可能92(ARG, O_DATA, V_NAME)
#FUNCTION
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, @"beaded panties")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "팬티 비드")
	CASE "FullDesc"
		CALLF MAKE_STR(V_NAME, @"구슬을 엮어 만든 팬티. 번쩍이는 구슬이 성적인 매력을 더하고, 착용자가 움직일 때마다 흔들리며 존재감을 알린다.")
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "はける"
		IF IS_VIRGIN(ARG)
			CALLF MAKE_INT(V_NAME, 0)
		ELSEIF (TALENT:ARG:184 || TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(1 + (2 * 勝負下着判定(ARG)))
			CALLF MAKE_INT(V_NAME, 1)
		ENDIF
	CASE "関連下着"
		CALLF MAKE_INT(V_NAME, 0)
	CASE "コレクション表示可能"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, "efun/black/feminine/panty/sex toy/plain/washable/")
ENDSELECT
; @EXIST_下半身下着_ずらし可能93
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能93(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, @"『Fill Me Up』hotpants")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "wtf is this used for")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "はける"
; 	IF IS_VIRGIN(ARG)
; 		CALLF MAKE_INT(V_NAME, 0)
; 	ELSEIF (TALENT:ARG:184 || TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; 	ENDIF
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "beedee/sexy/black/feminine/panty/printed/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能94
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能94(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, @"『Baby Factory』 thong")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "wtf is this used for")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "はける"
; 	IF IS_VIRGIN(ARG)
; 		CALLF MAKE_INT(V_NAME, 0)
; 	ELSEIF (TALENT:ARG:184 || TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; 	ENDIF
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "beedee/black/feminine/thong/sexy/printed/washable/")
; ENDSELECT

@EXIST_下半身下着_ずらし可能112
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし可能112(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, GET_STR(-1, "下半身下着_ずらし可能", ToInt(SUBSTRING(GetDoingFunction(),20)), "Generic Name"))
	CASE "Generic Name"
		CALLF MAKE_STR(V_NAME, "차원 장벽 팬티")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "팬티 장벽")
	CASE "FullDesc"
		CALLF MAKE_STR(V_NAME, "차원 장벽 팬티는 평범한 팬티가 아니다. 작은 주머니 차원을 열어 배설물은 물론 속옷을 노리는 외부의 위협까지 가둔다. 화장실에 제때 갈 걱정도 없다. 그대로 볼일을 봐도 처지거나 냄새나거나 부풀지 않으며, 무엇보다 언제나 깨끗하게 지낼 수 있다.")
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "はける"
		;IF !GROUPMATCH(ARG,26) && (ARG == MASTER && !ITEM:BarrierPanties)
		IF !GROUPMATCH(ARG,26)
			CALLF MAKE_INT(V_NAME, 0)
		ELSE
			CALLF MAKE_INT(V_NAME, 1)
		ENDIF
	CASE "Panties Type"
		CALLF MAKE_STR(V_NAME, "Underwear")
	CASE "Sub Type"
		CALLF MAKE_STR(V_NAME, "Barrier")
	CASE "関連下着"
		CALLF MAKE_INT(V_NAME, 0)
	CASE "コレクション表示可能"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, "black/panty/plain/washable/")
ENDSELECT

@EXIST_下半身下着_ずらし可能169
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし可能169(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, GET_STR(-1, "下半身下着_ずらし可能", ToInt(SUBSTRING(GetDoingFunction(),20)), "Generic Name"))
	CASE "Generic Name"
		CALLF MAKE_STR(V_NAME, "샅가리개")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "팬티 로인 클로스")
	CASE "FullDesc"
		CALLF MAKE_STR(V_NAME, "가랑이를 가리는 단순한 샅가리개. 천 한 장으로 착용자의 성기와 엉덩이, 허벅지 일부를 덮는다.")
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "はける"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Panties Type"
		CALLF MAKE_STR(V_NAME, "Underwear")
	CASE "関連下着"
		CALLF MAKE_INT(V_NAME, 0)
	CASE "コレクション表示可能"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, "rw tailor/white/panty/plain/washable/")
ENDSELECT

@EXIST_下半身下着_ずらし可能174
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし可能174(ARG, O_DATA, V_NAME)
#FUNCTION
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, "『I-Owe-You』Tag")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "이우유 태그")
	CASE "FullDesc"
		CALLF MAKE_STR(V_NAME, @"이것을 받았다면 취향에 맞는 팬티가 하나도 없거나, 게임에서 입힐 팬티가 바닥난 것이다. 버그로 보고해 달라!")
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "はける"
		CALLF MAKE_INT(V_NAME, 0)
	CASE "関連下着"
		CALLF MAKE_INT(V_NAME, 0)
	CASE "Unisex"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Panties Type"
		CALLF MAKE_STR(V_NAME, "Open-Crotch")
	CASE "コレクション表示可能"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, "")
ENDSELECT

@EXIST_下半身下着_ずらし可能182
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身下着_ずらし可能182(ARG, O_DATA, V_NAME)
#FUNCTION
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
	CASE "名前"
		CALLF MAKE_STR(V_NAME, @"piddle-retardant pants")
	CASE "描写"
		CALLF MAKE_STR(V_NAME, "팬티가 흘러내린다.")
	CASE "FullDesc"
		CALLF MAKE_STR(V_NAME, @"앞뒤에 흡수층을 댄 브리프. 눈에 띄지 않을 만큼 얇지만 방광 두어 개 분량의 소변도 거뜬히 받아낸다.")
	CASE "透ける(前)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "透ける(後)"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "はける"
		CALLF MAKE_INT(V_NAME, 0)
	CASE "関連下着"
		CALLF MAKE_INT(V_NAME, 0)
	CASE "コレクション表示可能"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Panties Type"
		CALLF MAKE_STR(V_NAME, "Underwear")
	CASE "Pee Capacity"
		CALLF MAKE_INT(V_NAME, 1520)
	CASE "Poo Capacity"
		CALLF MAKE_INT(V_NAME, 1210)
	CASE "Quest Only"
		CALLF MAKE_INT(V_NAME, 1)
	CASE "Tags"
		CALLF MAKE_STR(V_NAME, "beedee/white/feminine/briefs/washable/")
ENDSELECT
; @EXIST_下半身下着_ずらし可能183
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能183(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, @"『Sure Thing』panties")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "wtf is this used for")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "はける"
; 	SIF (TALENT:ARG:184 || TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "beedee/sexy/green/panty/feminine/printed/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能184
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能184(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, @"blue panties")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "wtf is this used for")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "はける"
; 	SIF !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "urbworld/blue/panty/plain/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能185
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能185(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, @"『I Love Sucking Cocks』panties")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "black")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "はける"
; 	SIF (TALENT:ARG:184 || TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "beedee/sexy/black/panty/feminine/printed/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能186
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能186(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, @"comfy black panties")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "wtf is this used for")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "はける"
; 	SIF !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "urbworld/black/panty/plain/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能187
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能187(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, @"『Daddy's Little Girl』panties")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "wtf is this used for")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "はける"
; 	SIF (TALENT:ARG:184 || TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "beedee/sexy/pink/panty/printed/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能188
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能188(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, @"『Blowjob Queen』trunks")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "pink with a white waistband")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "はける"
; 	SIF (TALENT:ARG:184 || TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "beedee/sexy/pink/feminine/printed/washable/")
; ENDSELECT
; @EXIST_下半身下着_ずらし可能189
; #LOCALSIZE 1
; #LOCALSSIZE 1
; @下半身下着_ずらし可能189(ARG, O_DATA, V_NAME)
; #FUNCTION
; #DIMS O_DATA
; #DIMS V_NAME
; SELECTCASE O_DATA
; CASE "名前"
; 	CALLF MAKE_STR(V_NAME, @"『Spank Me』panties")
; CASE "描写"
; 	CALLF MAKE_STR(V_NAME, "wtf is this used for")
; CASE "透ける(前)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "透ける(後)"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "はける"
; 	SIF (TALENT:ARG:184 || TALENT:ARG:ヤリマン || TALENT:ARG:小悪魔 || TALENT:ARG:恋慕) && !RAND:(1 + (2 * 勝負下着判定(ARG)))
; 		CALLF MAKE_INT(V_NAME, 1)
; CASE "関連下着"
; 	CALLF MAKE_INT(V_NAME, 0)
; CASE "コレクション表示可能"
; 	CALLF MAKE_INT(V_NAME, 1)
; CASE "Tags"
; 	CALLF MAKE_STR(V_NAME, "beedee/sexy/purple/feminine/printed/washable/")
; ENDSELECT
