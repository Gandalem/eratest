;-------------------------------------------------
; custom code
; a dump of Mr Pops Alot's pee and poo-related stuff
;-------------------------------------------------

;-------------------------------------------------
; HornyFlavor
; Flavor text for if you are horny or not
; ARGS: ARG (Target, required)
;-------------------------------------------------
@HornyFlavor(ARG)
#DIM C_ID
#DIM CHARA
#DIM nResult
RESETCOLOR
VARSET LOCAL
LOCAL:3 = 0

SIF !nSex
    RETURN
SIF !SHIRAHU(ARG)
    RETURN
SIF GROUPMATCH(SELECTCOM,400,405,604,402)
    RETURN

IF !CFLAG:ARG:うふふ && !FLAG:70 && !CFLAG:ARG:今忙しくて手が離せない && (ARG != MASTER || !CFLAG:TARGET:うふふ)
    IF CFLAG:ARG:溜まってる度 >= 1000
        $Masturbate

        SETCOLOR C_PINK
        PRINTFORML %PARSE("You feel",,,ARG)% like %PARSE("you",5,,ARG)% can't go on anymore with %PARSE("your",5,,ARG)% mind racing and heart pounding like this.      
        PRINTFORM %PARSE("You look",5,,ARG)% around to see if anyone's watching before... 
        IF LOCAL:3 && ARG != MASTER
            GOTO ForcePushDown
        ENDIF
        IF GET_TARGETNUM() > 0
            IF ARG == MASTER
                PRINTFORML
                SIF TARGET != MASTER
                PRINTFORML [  0] - Proceeding to push down %CALLNAME:TARGET%.
                SIF TALENT:ARG:20 || OrgasmDeny:ARG:0 >= 2
                PRINTFORML [  1] - Holding your lust.
                PRINTFORML [998] - Quickly masturbating.
                PRINTFORML [999] - Sticking your hand in your pants.
                INPUT
                SELECTCASE RESULT  
                CASE 0
                    SELECTCOM = 999
                    IF CFLAG:TARGET:口上内抱き寄せ判定_初回
                        CALL KOJO_MESSAGE_SEND("PERMISSION", 1, TARGET)
                        SELECTCASE RESULT
                            CASE -1
                                TCVAR:TARGET:抱き寄せ初回 = 98
                                TFLAG:押し倒せない ++
                            CASE 0
                                CALL 抱き寄せ(TARGET)
                                SIF !TFLAG:押し倒せない
                                    RETURN
                            CASE 1
                                TCVAR:抱き寄せ初回 = 99
                                SETBIT CFLAG:TARGET:既成事実 , 1
                                CALL COLORMESSAGE(@"%PARSE("You receive")% %CALLNAME:TARGET%'s consent.",C_YELLOW,2)
                        ENDSELECT
                    ELSE
                        CALL 抱き寄せ(TARGET)        
                        SIF !TFLAG:押し倒せない
                            RETURN  
                    ENDIF
                CASE 998
                GOTO ForceQuickFap
                CASEELSE
                ENDSELECT
            ELSEIF (TALENT:ARG:20 || OrgasmDeny:ARG:0 >= 2 || TALENT:ARG:貞操) && RAND(100) < 75
                PRINTFORML exhibiting enough self-control to hold it.
                GOTO Leaking
            ELSE
                $ForcePushDown
                PRINTFORML proceeding to push %PARSE("you")% down.
                PRINTFORML Let them push you down?
                CALL ASK_YN()
                nResult = RESULT
                IF RESULT
                TCVAR:ARG:Attack_Power = 2*GetCombatAbility(ARG) + (ABL:ARG:指/2)
                TCVAR:MASTER:Attack_Power = 2*GetCombatAbility(MASTER) + (ABL:MASTER:指/2)
                CALL SHOW_POWERFAVOR(TCVAR:MASTER:Attack_Power,MASTER)
                CALL POWERBUFFS(MASTER)
                DRAWLINE
                CALL SHOW_POWERFAVOR(TCVAR:ARG:Attack_Power,ARG)
                CALL POWERBUFFS(ARG)
                ENDIF  
                IF TCVAR:ARG:Attack_Power >= TCVAR:MASTER:Attack_Power || !nResult
                    ;IF (!HITOKE() && INROOM(CFLAG:MASTER:現在位置) && AT_HOME(MASTER)) || CFLAG:MASTER:現在位置 == OMANEKIBEYA() || nRapeAnywhere
                    SELECTCOM = 999
                    CFLAG:ディレイ = 0
                    CFLAG:不機嫌 = 0
                    IF !UFUFU_START_TIME
                        UFUFU_START_TIME = TIME
                        UFUFU_START_DAY = DAY
                    ENDIF
                    ;CALL KOJO_EVENT(12, TARGET)
                    CALL KOJO_MESSAGE_SEND("EVENT",10,TARGET,1,0)
                    ; FOR LOCAL, 1, GET_TARGETNUM() + 1
                    ;     C_ID = TARGET:LOCAL
                    ;     CFLAG:C_ID:うふふ = 2
                    ;     IF !TCVAR:C_ID:発情 && !TCVAR:C_ID:媚薬 && !GETBIT (CFLAG:C_ID:既成事実 , 1)
                    ;         SETBIT CFLAG:C_ID:既成事実 , 1
                    ;         CALL COLORMESSAGE(@"%PARSE("You receive")% %CALLNAME:C_ID%'s consent.",C_YELLOW,2)
                    ;     ELSE
                    ;         CFLAG:C_ID:ヤラせちゃった = 1
                    ;     ENDIF
                    ; NEXT
                    CFLAG:ARG:うふふ = 2
                    IF !TCVAR:ARG:発情 && !TCVAR:ARG:媚薬 && !GETBIT (CFLAG:ARG:既成事実 , 1)
                        SETBIT CFLAG:ARG:既成事実 , 1
                        CALL COLORMESSAGE(@"%PARSE("You receive")% %CALLNAME:ARG%'s consent.",C_YELLOW,2)
                    ELSE
                        CFLAG:ARG:ヤラせちゃった = 1
                        CALL CharacterSinned(TARGET,"Rape",@"pushed down %CALLNAME:MASTER% to have sex", -3, -1)
                    ENDIF
                    CALL CheckSinsForSex(TARGET, MASTER, 0)
                    CFLAG:MASTER:うふふ = 2
                    TFLAG:102 = 3
                    TFLAG:抱きつきモード = 0
                    TFLAG:押し倒され開始時間 = TIME + DAY * 1440
                    CFLAG:ARG:恥じらい = 1
                    CFLAG:ARG:積極性 = 1
                    RETURN
                    ;ENDIF
                ENDIF
            ENDIF
        PRINTFORML Unable to convince anyone to have sex with %PARSE("you",,,ARG)%, %PARSE("you look",5,,ARG)% around before sticking %PARSE("your",5,,ARG)% hand in %PARSE("your",5,,ARG)% pants.  
        ELSE
            IF ARG != MASTER
                GOTO ForceQuickFap
            ENDIF
            PRINTFORML
            PRINTFORML [998] - Quickly masturbating.
            PRINTFORML [999] - Sticking your hand in your pants.
            INPUT
            SELECTCASE RESULT  
                CASE 998
                GOTO ForceQuickFap
                CASEELSE
            ENDSELECT
        ENDIF   

        IF ARG == MASTER
            ;do the dynamic masturbatory thingy
            CFLAG:ARG:今忙しくて手が離せない = 1
            RETURN 1
        ENDIF
        $ForceQuickFap

        IF GET_TARGETNUM() > 0
        PRINTFORML %PARSE("You ache",,,ARG)% in agony as %PARSE("you see",5,,ARG)% another person when %PARSE("you're",5,,ARG)% about to fap.
        PRINTFORMW Desperation takes over however, as %PARSE("you start",,,ARG)% masturbating.
        ENDIF
        PRINTFORML As %PARSE("you",,,ARG)% can no longer keep %PARSE("your",,,ARG)% aching body in check, %PARSE("you begin",5,,ARG)% to play with %PARSE("your",5,,ARG)% fingers...
        RESETCOLOR
        PRINTFORML
        IF SoiledDiaper(ARG)
            IF ABL:ARG:PeeAddict + ABL:ARG:PooAddict >= 10
                PRINTFORM %PARSE("Your",,,ARG)% %PeeProtectType(ARG)%'s 
                SIF SoiledDiaper(ARG,1) && ABL:ARG:PeeAddict >= 5
                    PRINTFORM drenched 
                SIF SoiledDiaper(ARG,1) && SoiledDiaper(ARG,1) && ABL:ARG:PeeAddict >= 5 && ABL:ARG:PooAddict >= 5
                    PRINTFORM and 
                SIF SoiledDiaper(ARG,2) && ABL:ARG:PooAddict >= 5
                    PRINTFORM squishy load inside its 

                IF IsPadded(ARG)
                    PRINTFORM padding 
                ELSE
                    PRINTFORM cloth 
                ENDIF

                PRINTFORM is stimulating %PARSE("your",5,,ARG)% genitals wonderfully with its warm, moist touch.
            ELSEIF ABL:ARG:PeeAddict + ABL:ARG:PooAddict >= 5
            PRINTFORM Walking around in %PARSE("your",,,ARG)% 
                SIF SoiledDiaper(ARG,1) && ABL:ARG:PeeAddict >= 3
                    PRINTFORM %FSYN("peed_in")% 
                SIF SoiledDiaper(ARG,1) && SoiledDiaper(ARG,1) && ABL:ARG:PeeAddict >= 3 && ABL:ARG:PooAddict >= 5
                    PRINTFORM and 
                SIF SoiledDiaper(ARG,2) && ABL:ARG:PooAddict >= 3
                    PRINTFORM %FSYN("pooped_in")%
                PRINTFORM %" "+PeeProtectType(ARG,1)% has %PARSE("you",5,,ARG)% feeling as aroused as %PARSE("you've",5,,ARG)% ever been. 
            ELSE
                PRINTFORM %PARSE("You aren't",,,ARG)% happy about the 
                IF SoiledDiaper(ARG,1)
                    IF IsPadded(ARG,"diaper")
                    PRINTFORM thick, damp padding between %PARSE("your",5,,ARG)% legs, 
                    ELSE
                    PRINTFORM dampness of %PARSE("your",5,,ARG)% %PeeProtectType(ARG,1)%, 
                    ENDIF
                ENDIF
                SIF SoiledDiaper(ARG,1) && SoiledDiaper(ARG,2)
                    PRINTFORM or the 
                IF SoiledDiaper(ARG,2)
                    IF IsPadded(ARG,"diaper")
                    PRINTFORM lump of %FSYN("poo:n")% resting against the rear of %PARSE("your",5,,ARG)% infantile undergarment, 
                    ELSE
                    PRINTFORM lump of %FSYN("poo:n")% resting against %PARSE("your",5,,ARG)% rear, 
                    ENDIF
                ENDIF
                PRINTFORM but even so, %PARSE("you're",5,,ARG)% feeling as aroused as %PARSE("you've",5,,ARG)% ever been. 
            ENDIF

            PRINTFORML

            IF ABL:ARG:PeeAddict >= 5 || ABL:ARG:PooAddict >= 5
            PRINTFORM %PARSE("You focus",,,ARG)% on the lovely feeling of %PARSE("your",5,,ARG)% 
            SIF SoiledDiaper(ARG,1) && ABL:ARG:PeeAddict >= 5
                PRINTFORM soaked 
            SIF SoiledDiaper(ARG,1) && SoiledDiaper(ARG,1) && ABL:ARG:PeeAddict >= 5 && ABL:ARG:PooAddict >= 5
                PRINTFORM %"and "%
            SIF SoiledDiaper(ARG,2) && ABL:ARG:PooAddict >= 5
                PRINTFORM messy
            PRINTFORM %" "+PeeProtectType(ARG,1)% 
            ELSE
            PRINTFORM %PARSE("You decide",,,ARG)% to just try and ignore the bodily waste 
            ENDIF
            PRINTFORML and prepare to rub one out before moving on.

        ENDIF
        ;for name
        ;\\@ ARG == MASTER \? %PARSE\("([^"]+)"\)% # %CALLNAME:[^\\]+\\@

        ;template
        ;\\@ ARG == MASTER \? %PARSE\("([^"]+)"\)% # [^\\]+\\@

        ;HIS_HER
        ;\\@ ARG == MASTER \? %PARSE\("([^"]+)"\)% # %HIS_HER[^\\]+\\@
        SIF HAS_PENIS(ARG)
        CALLF InstantCummies(ARG)
        PALAM:ARG:0 += PleasureRequired(ARG)

        IF HAS_VAGINA(ARG)
            PALAM:ARG:1 += PleasureRequired(ARG)
            PRINTFORMW Kneeling, %PARSE("you insert",,,ARG)% two fingers into %PARSE("your",5,,ARG)% %FSYN("slit")% and begin to massage %PARSE("your",5,,ARG)% %PRINT_CLIT(ARG)% with %PARSE("your",5,,ARG)% other hand through the fabric. 
            PRINTFORMW %PARSE("Your",5,,ARG)% %PRINT_CLIT(ARG)% rubs against the fabric, stimulating it. 
            PRINTFORMW %PARSE("You",5,,ARG)% slowly \@ ARG == MASTER ? begin # begins \@ to increase %PARSE("your",5,,ARG)% pace as %PARSE("you edge",5,,ARG)% closer and closer to orgasm. 
            IF PeeHold:ARG:2 <= -2
            CALL PEESOUNDS("hs")
            PRINTFORM Finally, %PARSE("you finish",5,,ARG)% with a gasp as \@ ARG == MASTER ? %PARSE("you")+" "+FSYN("orgasm")% # %HE_SHE(ARG)+" "+FSYN("orgasm:s")% \@, squirting a small stream of %URINE(ARG,1)% into %PARSE("your",5,,ARG)% %PeeProtectType(ARG,1)%
            ELSE
            PRINTFORM Finally, %PARSE("you finish",5,,ARG)% with a gasp as \@ ARG == MASTER ? %PARSE("you")+" "+FSYN("orgasm")% # %HE_SHE(ARG)+" "+FSYN("orgasm:s")% \@
            ENDIF   
            IF HAS_PENIS(ARG)
            PRINTFORM , %PARSE("your",5,,ARG)% %FSYN("penis")% spurts several ropes of %SEMEN(ARG)% into %PARSE("your",5,,ARG)% %PeeProtectType(ARG,1)%
            PRINTFORML .
            WAIT
            ENDIF
            LOCAL:1 = 1
        ELSE
            PRINTFORMW Kneeling, %PARSE("you grasp",,,ARG)% %PARSE("your",5,,ARG)% shaft in hand and begin to stroke. 
            PRINTFORMW The fabric rubs against %PARSE("your",5,,ARG)% %PRINT_CLIT(ARG)%, stimulating it. 
            PRINTFORMW %PARSE("You",5,,ARG)% slowly \@ ARG == MASTER ? begin # begins \@ to increase %PARSE("your",5,,ARG)% pace as %PARSE("you edge",5,,ARG)% closer and closer to orgasm. 
            PRINTFORMW Finally, %PARSE("you finish",5,,ARG)% with a gasp as %PARSE("your",5,,ARG)% %TINKO(ARG)% spurts several ropes of %SEMEN(ARG)% into %PARSE("your",5,,ARG)% %PeeProtectType(ARG,1)%.        
        ENDIF
        PRINTFORML
        PRINTFORM As %PARSE("you stand",,,ARG)% up, %PARSE("you",5,,ARG)% 
        IF SoiledDiaper(ARG,1) || SoiledDiaper(ARG,2)
        PRINTFORM shamefully \@ ARG == MASTER ? reflect # reflects \@ that touching %PARSE("yourself",5,,ARG)% through %PARSE("your",5,,ARG)% soiled 
        ELSE
        PRINTFORM \@ ARG == MASTER ? reflect # reflects \@ that touching %PARSE("yourself",5,,ARG)% through %PARSE("your",5,,ARG)% 
        ENDIF
        PRINTFORML %PeeProtectType(ARG, 2)% was actually pretty enjoyable.
        WAIT      

		EXP:ARG:自慰経験 += ABL:ARG:自慰中毒 * 2 + 1
		CALL COLORMESSAGE(@"Masturbation Exp＋{ABL:ARG:自慰中毒 * 2 + 1}",C_PINK,1)
		SELECTCASE ABL:ARG:自慰中毒
			CASE 0 TO 2
				LOCAL = 5
			CASE 3 TO 5
				LOCAL = 3
			CASEELSE
				LOCAL = 1
		ENDSELECT
        CALL FUCK(ARG, @" C, \@ LOCAL:1 ? V, #\@ desire, \@ HAS_PENIS(ARG) ? cumlove, #\@ ejac, \@ PeeHold:ARG:0 <= -2 ? piss, #\@ \@ PooHold:ARG:0 <= -2 ? shit, #\@ exposure", @"", 0, TARGET, TARGET, TARGET, @"", "ペニス", "膣", "口")
        CALL CharacterSinned(ARG,"Masturbation",@"masturbated in %HIS_HER(ARG)% %PeeProtectType(ARG,2)%", -3,-1)
        CFLAG:ARG:溜まってる度 /= 4

        FOR CHARA, 1, CHARANUM
            SIF CFLAG:MASTER:現在位置 != CFLAG:CHARA:現在位置
                CONTINUE
            SIF !SHIRAHU(CHARA)
                CONTINUE
            SIF CHARA == MASTER
                CONTINUE
            SIF CHARA == ARG
                CONTINUE

            IF ABL:CHARA:自慰中毒 >= 10 || CFLAG:CHARA:溜まってる度 >= 690
                CALLF InstantCummies(CHARA)
                PALAM:CHARA:0 += PleasureRequired(CHARA)
                SIF HAS_VAGINA(CHARA)
                PALAM:CHARA:1 += PleasureRequired(CHARA)
                PRINTFORML As %CALLNAME:CHARA% can no longer keep %HIS_HER(CHARA)% aching body in check watching %PARSE("you",,,ARG)% masturbate, %HE_SHE(CHARA)% begins to play with %HIS_HER(CHARA)% fingers...
                CALL FUCK(CHARA, @" C, desire, \@ HAS_PENIS(CHARA) ? cumlove, #\@ ejac, \@ PeeHold:CHARA:0 <= -2 ? piss, #\@ \@ PooHold:CHARA:0 <= -2 ? shit, #\@ exposure", @"", 0, ARG, ARG, ARG, @"", "ペニス", "膣", "口")
                CALL CharacterSinned(CHARA,"Masturbation",@"masturbated in %HIS_HER(ARG)% %PeeProtectType(ARG,2)%", -3,-1)
                CFLAG:CHARA:溜まってる度 /= 4
            ELSE
                IF ARG == MASTER
                    PRINTFORML %CALLNAME:CHARA% looks at %PARSE("you",,,ARG)% weirdly...
                    CALL SET_REP(CHARA,10,,1)
                    CALL GET_ANGRY(CHARA)
                ENDIF
            ENDIF
        NEXT
        
        CALL EVENT_SHOOT
    ELSEIF CFLAG:ARG:溜まってる度 >= 900
            $Leaking
            SETCOLOR C_PINK
            PRINTFORMW %PARSE("Your",,,ARG)% %FSYN("crotch")% burns with lust, %PARSE("your",5,,ARG)% heart trembles at unprecedented speeds, %PARSE("your",5,,ARG)% mind tunnel visioned on sex, %PARSE("your",5,,ARG)% %PeeProtectType(ARG,1)% soaked in %PARSE("your",5,,ARG)% fluid.
            RESETCOLOR
            IF RAND(100) > (10*OrgasmDeny:ARG:0)
                IF ARG == MASTER
                PRINTFORML Your arousal spikes, your body doing everything not to give into your sexual frustration...
                CALL ASK_M("Endure（50 ENE）",BASE:ARG:気力 >= 50, "Endure（200 ENE）",BASE:ARG:気力 >= 200, "Masturbate",1)
                SELECTCASE RESULT
                CASE 0
                $Gaman
                DOWNBASE:ARG:気力 += 50
                PRINTFORMW %PARSE("You try",,,ARG)% to endure %PARSE("your",5,,ARG)% sexual frustration.
                IF !RAND:2
                    PRINTFORMW %PARSE("Your",5,,ARG)% horniness is too much to control for %PARSE("your",5,,ARG)% poor body...
                    PRINTFORMW It's over......
                    LOCAL:3 = 1
                    IF RAND(100) <= (10*OrgasmDeny:ARG:0) && OrgasmDeny:ARG:0 >= 2
                        PRINTFORMW ...or is it...                          
                        PRINTFORMW By some miracle, %PARSE("your",,,ARG)% arousal fades as %PARSE("you're",5,,ARG)% about to touch %PARSE("your",5,,ARG)%self.
                        PRINTFORMW %PARSE("You definitely",5,,ARG)% %PARSE("need",,,ARG)% to get off now, or \@ ARG == MASTER?%PARSE("you're")%#%HE_SHE(ARG)%'s\@ doing it in \@ARG==MASTER?%PARSE("your")%#%HIS_HER(ARG)%\@ %PeeProtectType(ARG,1)%.                        
                    ELSE
                        GOTO Masturbate
                    ENDIF
                ELSE
                    PRINTFORMW %PARSE("You",5,,ARG)% arousal clears up slightly
                    PRINTFORMW But %PARSE("you definitely",5,,ARG)% %PARSE("need",,,ARG)% to find a way to have sex quickly.
                    CFLAG:ARG:溜まってる度 -= 100
                ENDIF
                CASE 1
                    PRINTFORMW %PARSE("You")% frantically %PARSE("endure")% the temptation of masturbating.
                    DOWNBASE:MASTER:気力 += 200
                CASE 2
                    GOTO Masturbate
                ENDSELECT        
                ELSE
                    GOTO Gaman
                ENDIF
            ENDIF    
            SIF PALAM:ARG:苦痛 < 4
                PALAM:ARG:苦痛 += 1            
            OrgasmDenyExp:ARG:0 += 5                
    ELSEIF CFLAG:ARG:溜まってる度 >= 800
        $Desperate
        SETCOLOR C_PINK
        SIF HAS_PENIS(ARG)
            PRINTFORML \@ARG == MASTER?%PARSE("Your")%#%CALLNAME:ARG%'s\@ %FSYN("penis")% is bulging against the front of %PARSE("your",5,,ARG)% %PeeProtectType(ARG,2)%.
        IF HAS_VAGINA(ARG)
            IF EQUIP:ARG:6
                PRINTFORMW \@ARG == MASTER?%PARSE("Your")%#%CALLNAME:ARG%'s\@ %PeeProtectType(ARG)% is starting to dampen with %PARSE("your",5,,ARG)% juices.
            ELSE
                PRINTFORMW %PARSE("Your",,,ARG)% pussy juices drip onto the floor.
            ENDIF
        ENDIF
        RESETCOLOR
        CFLAG:ARG:溜まってる度 += 50
    ELSEIF CFLAG:ARG:溜まってる度 >= 690
        $Squirming
        SETCOLOR C_PINK
        SELECTCASE RAND:3
        CASE 0
        PRINTFORMW \@ARG == MASTER?%PARSE("Your")%#%CALLNAME:ARG%'s\@ mind clouds over momentarily with lewd thoughts.
        CASE 1
        PRINTFORMW \@ARG == MASTER?%PARSE("Your")%#%CALLNAME:ARG%\@ can't seem to think straight, mind flooded with sexual cravings.
        CASE 2
        PRINTFORMW %PARSE("You put your",,,ARG)% hand on %PARSE("your",5,,ARG)% crotch, stimulating it a bit.
        ENDSELECT
        RESETCOLOR        
        CFLAG:ARG:溜まってる度 += 50
    ELSEIF CFLAG:ARG:溜まってる度 >= 500
        $Noticeable
        SETCOLOR C_PINK
        PRINTFORMW \@ARG == MASTER?%PARSE("You feel")%#%CALLNAME:ARG% feels\@ considerably aroused.
        RESETCOLOR
    ELSEIF CFLAG:ARG:溜まってる度 >= 250
    ENDIF

    ELSE
ENDIF

@InstantCummies(ARG)
#FUNCTION
TCVAR:ARG:射精快感強度 = 10000
CALLF IKISOU(ARG)


@CHASTITY_ATTEMPTED(ARG,ARG:1)
SIF !GETBIT(IN_CHASTITY(ARG),ARG:1)
    RETURN 0

SELECTCASE SELECTCOM
CASE 3
PRINTFORML You try to put your fingers in %PARSE("your",,,ARG)% %FSYN("vagina",,,ARG)%, but %PARSE("your",5,,ARG)% belt blocks it.    
CASE 703
PRINTFORML %PARSE("Your",,,ARG)% body burns with lust as %PARSE("your",5,,ARG)% hand gets dangerously close to %PARSE("your",5,,ARG)% secret place.
PRINTFORML However, as %PARSE("you try",5,,ARG)% to touch %PARSE("yourself",5,,ARG)%, the cage blocks %PARSE("your",5,,ARG)% %PeeAndPoo(HAS_PENIS(ARG), HAS_VAGINA(ARG), TINKO(ARG), FSYN("vagina",,,ARG))%.
ENDSELECT
RETURN 1

@CAGEME(ARG)
#FUNCTION
EQUIP:ARG:5 = 5
DEBUGPRINTFORML What a cute little %PeeAndPoo(HAS_PENIS(ARG), HAS_VAGINA(ARG), "peepee", "cunny")% that %CALLNAME:ARG% has. Too bad %HE_SHE(ARG)% can't use it.