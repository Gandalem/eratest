;temp info
;These are explained in AchievementList.ERB
;Also see the ERD files for names in the entry
#DIM AL_ENTRY_INTS,6
#DIM CONST AL_BUTTON_LENGTH = 27; Length of list buttons, not including the two dividing characters or image
#DIM CONST AL_BUTTON_IMAGE_SIZE = 6; Length of list image, uses double length characters, this is the linecount so it should not be touched
#DIM SAVEDATA AL_LEGACY_PROS    ;Flag to indicate that legacy achievements have been processed.

;temp vars specificity made to not do unnecessary computations.
#DIM AL_PROG,27