@ADD_CUSTOM_COM350
CALL NAS_RapeOption(TARGET)
TIME += 5
RETURN 1	

@ADD_CUSTOM_COM_ABLE350
;バイブ実行判定
SIF !TFLAG:100
	RETURN 0

SIF TARGET == MASTER
	RETURN 0

SIF CFLAG:うふふ
	RETURN 0

SIF !SHIRAHU(TARGET)
	RETURN 0

SIF !RAPE_CONDITION()
	RETURN 0
RETURN 1