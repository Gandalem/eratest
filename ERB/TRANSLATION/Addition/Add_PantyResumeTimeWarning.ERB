@PantyResumeTimeWarning
IF nPantyResumeTimeWarning && FLAG:パンツ輸送 ;addition custom code, prevent accidentally resuming time while on a panty raid
    PRINTFORMDL You might want to stash your stolen panties before letting time resume...
    CALL ASK_YN("Resume time anyway","Don't do it")
    IF !RESULT
        RETURN 1
    ELSE
        RETURN 0
    ENDIF
ELSE
    RETURN 1
ENDIF

;-------------------------------
;Custom config section
;-------------------------------
@CUSTOM_OPTION_101
PRINTFORML [101] - Warn before resuming time while carrying stolen panties：\@ nPantyResumeTimeWarning ? Yes # No \@

@CUSTOM_OPTION_RESULT_101
nPantyResumeTimeWarning = !nPantyResumeTimeWarning