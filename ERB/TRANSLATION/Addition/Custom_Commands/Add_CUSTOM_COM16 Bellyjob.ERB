@ADD_CUSTOM_COM16

TCVAR:116 = PLAYER
;奉仕快楽フラグ
TCVAR:PLAYER:108 = 1

CSTR:PLAYER:10 += @"{TARGET}/"
;TCVAR:2 射精箇所フラグ（-1=コンドーム 1=膣内 2=アナル 3=手淫 4=口淫 5=パイズリ 6=素股 7=足コキ 8=体表 9=アナル奉仕 10=道具 11=触手 12=Ａ責め 13=Ｖ責め）
TCVAR:PLAYER:2 = 8
;強度
TCVAR:PLAYER:4 = 300

CSTR:TARGET:10 += @"{PLAYER}/"
;強度
TCVAR:TARGET:4 = 300

DOWNBASE:PLAYER:体力 += 20
DOWNBASE:PLAYER:気力 += 20

SOURCE:PLAYER:快Ｃ += 40
SIF CFLAG:妊娠経過日数 >= 30
    SOURCE:PLAYER:快Ｃ += CFLAG:妊娠経過日数


TIME += 5
RETURN 1

@ADD_CUSTOM_COM_ABLE16
SIF !TFLAG:100
	RETURN 0
SIF TFLAG:102 != 2
    RETURN 0
SIF !TARGET
    RETURN 0
SIF CFLAG:うふふ == 0
    RETURN 0
SIF !HAS_PENIS(PLAYER)
	RETURN 0
SIF !CHECK_CLO("belly_visible")
    RETURN 0

RETURN 1

@ADD_CUSTOM_CAN_COM16(ARG)
SKIPDISP ARG
IF FLAG:70
	PRINTL Forced Bellyjob
ELSE
	IF !TEQUIP:縄
		PRINTL Bellyjob
		
        CALL QOL_COM_ORDER_STANDARD_CHECKS(20, QOL_COM_ORDER_SERVICE_FULL | QOL_COM_ORDER_DIRT_SOURCE_PENIS, TARGET)
		LOCAL = RESULT
		SKIPDISP 0
		RETURN LOCAL
	ENDIF
ENDIF
SKIPDISP 0
RETURN 1

@ADD_CUSTOM_MESSAGE_COM16
PRINTFORMD %ONGOING_NAME(MASTER)% rub your %TINKO_TR(PLAYER,1,,TALENT:PLAYER:双槍)% on %CALLNAME:TARGET%'s 
 
IF CFLAG:妊娠経過日数 >= 80
    PRINTFORMDL massive belly.
    PRINTFORMDW The knowledge that %HE_SHE(TARGET)% will soon give birth only makes it even more exciting.
ELSEIF CFLAG:妊娠経過日数 >= 60
    PRINTFORMDL bloated belly.
	IF GROUPMATCH(TALENT:TARGET:PREGNANCY_TYPE, PREGNANCY_EGGS, PREGNANCY_CLUTCH)
		PRINTFORMDW You can feel the %PLNOUN("outline",CFLAG:TARGET:WithChildCount)% of %HIS_HER(TARGET)% %PLNOUN("egg",CFLAG:TARGET:WithChildCount)% through the skin, exciting you further.
	ELSE
		PRINTFORMDW You can feel \@ CFLAG:TARGET:WithChildCount > 1 ? a lot of movement # your child's movements \@ through the skin, exciting you further.
	ENDIF
ELSEIF CFLAG:妊娠経過日数 >= 40
    PRINTFORMDL bulging belly.
    PRINTFORMDW Knowing that your own %PLNOUN("child",CFLAG:TARGET:WithChildCount)% %PLVERB("is",CFLAG:TARGET:WithChildCount)% in there excites you even further.
ELSEIF CFLAG:妊娠経過日数 >= 30
    PRINTFORMDL slightly bulging belly.
    PRINTFORMDW Feeling %HIM_HER(TARGET)% growing slowly fills you with a warm feeling.
ELSEIF 充填率(TARGET,1) >= 100
    PRINTFORMDL semen-stuffed belly.
ELSE
    PRINTFORMDW belly.
ENDIF
