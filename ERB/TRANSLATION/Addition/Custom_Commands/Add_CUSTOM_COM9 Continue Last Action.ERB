; Modular ERB: Addition/Custom_Commands/Add_CUSTOM_COM9 Continue Last Action.ERB

@ADD_CUSTOM_COM9
CALL ADD_TOUCH_DECREMENT
SELECTCOM = PREVCOM
PREVCOM = PREVPREVCOM
PREVPREVCOM = -1
ADD_PREVCUSTOMCOM = ADD_PREVPREVCUSTOMCOM
ADD_SELECTCUSTOMCOM = ADD_PREVCUSTOMCOM
ADD_SELECTED_SCOM = ADD_PREV_SELECTED_SCOM
ADD_PREVPREVCUSTOMCOM = -1
ADD_PREV_SELECTED_SCOM = -1
IF TFLAG:前回の特殊COM == ADD_CUSTOM_SCOM && ADD_SELECTCUSTOMCOM == 9
	PRINTFORMDL 이것은 @ADD_CUSTOM_COM9에서 무한 루프를 유발할 것이다. 보고해 달라.
	RETURN -1
ENDIF
CALL TRACHECK_TIME
JUMP CALL_COM(TARGET)

@ADD_CUSTOM_COM_ABLE9
SIF CFLAG:うふふ != 1
	RETURN 0
SIF !TFLAG:前回の特殊COM
	RETURN 0
SIF TARGET != TFLAG:前ターンのTARGET交代
	RETURN 0
IF TFLAG:前回の特殊COM == ADD_CUSTOM_SCOM
	SIF GROUPMATCH(ADD_PREV_SELECTED_SCOM, 0, 1, 5, 6, 7, 8, 9, 10, 11)
		RETURN 1
ELSEIF GROUPMATCH(TFLAG:前回の特殊COM, 9, 10, 11, 13) ;double blowjob, titjob, thighjob, alternate insertion
	RETURN 1
ENDIF
RETURN 0

@ADD_CUSTOM_MESSAGE_COM9

@ADD_CUSTOM_CAN_COM9(ARG)
RETURN 1
