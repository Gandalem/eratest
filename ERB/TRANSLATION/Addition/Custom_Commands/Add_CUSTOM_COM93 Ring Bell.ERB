; Modular ERB: Addition/Custom_Commands/Add_CUSTOM_COM93 Ring Bell.ERB

@ADD_CUSTOM_COM93
LOCAL:1 = 0
IF !CCOMRangBell
	LOCAL:1 = 1
	CCOMRangBell = 1
	PRINTFORMDL 안개 호수 부두 맨 끝 기둥 중 하나에 녹슨 오래된 종이 달려 있는 것을 본다.
	PRINTFORMDW 그것에서 똑같이 낡은 끈이 매달려 울리기를 간절히 바라고 있었다.
	SIF DAY >= 60
		PRINTFORMDW 여기에 항상 있었던 걸까?
	PRINTL
ENDIF
PRINTFORMDW 당신이 녹슨 부두 종을 울리자 호수의 수면 전체에 기분 좋은 종소리가 퍼져나간다...
PRINTL
IF CFLAG:MASTER:現在位置 == CFLAG:93:現在位置 ;Wakasagihime is already there
	LOCAL = 1
ELSEIF !ADD_RINGBELL_IS_COMING_OUT()
	LOCAL = 2
	PRINTFORMDW 하지만 아무도 오지 않았다.
ELSE
	LOCAL = 3
	PRINTFORMDW 누군가가 오고 있다.
	CFLAG:[[わかさぎ姫]]:現在位置 = CFLAG:MASTER:現在位置
	CFLAG:[[わかさぎ姫]]:遭遇位置 = CFLAG:MASTER:現在位置
	TCVAR:[[わかさぎ姫]]:なるべく動かない = 1
	CFLAG:[[わかさぎ姫]]:ディレイ = 60 ;set a delay to prevent her from moving immediately
ENDIF
CALL KOJO_MESSAGE_SEND("EVENT", 0, TARGET, LOCAL, LOCAL:1, "RINGBELL")
;want to add your own dialogue to this? check the end of this file
TIME += 1
RETURN 1

@ADD_CUSTOM_COM_ABLE93
SIF !TFLAG:100
	RETURN 0
SIF FLAG:70
	RETURN 0
SIF CFLAG:うふふ
	RETURN 0
SIF BASE:MASTER:気力 <= 0
	RETURN 0
SIF TFLAG:デート道中
	RETURN 0
;SIF CFLAG:[[わかさぎ姫]]:出禁
;	RETURN 0
SIF CFLAG:MASTER:現在位置 != GET_MAP_REPLACEMENT(P347霧の湖)
	RETURN 0
RETURN 1

@ADD_RINGBELL_IS_COMING_OUT()
#FUNCTION
;SIF ABL:[[わかさぎ姫]]:親密 < 2
;	RETURNF 0
SIF !SHIRAHU([[わかさぎ姫]])
	RETURNF 0
SIF GROUPMATCH(TIME:5, 9, 13, 15)
	RETURNF 0
SIF WIND_VELOCITY >= 3
	RETURNF 0
IF MAIN_MAP == 3
	SIF CFLAG:[[わかさぎ姫]]:現在位置 != 348
		RETURNF 0
ELSE
	SIF CFLAG:[[わかさぎ姫]]:現在位置 != SUKIMA() || !VISIT([[わかさぎ姫]]) || CFLAG:[[わかさぎ姫]]:拠点来訪 || BASE:[[わかさぎ姫]]:体力 < 1000
		RETURNF 0
ENDIF
RETURNF 1
;here's the template
