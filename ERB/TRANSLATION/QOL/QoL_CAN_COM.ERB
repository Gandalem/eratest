;CAN_COM replacements
@CAN_COM8_TR(ARG)
SKIPDISP ARG
PRINTL Spread Labia
;-------------------------------------------------
;実行できるかの判定
;-------------------------------------------------
IF FLAG:70 == 1 || CFLAG:イタズラ
ELSE
    CALL QOL_COM_ORDER_STANDARD_CHECKS(18, QOL_COM_ORDER_EXPOSURE_FULL | QOL_COM_ORDER_VAGINAL_PARTIAL, TARGET)
    LOCAL = RESULT
    SKIPDISP 0
    RETURN LOCAL
ENDIF
SKIPDISP 0
RETURN 1

@CAN_COM9_TR(ARG)
SKIPDISP ARG
SIF TEQUIP:ビデオ撮影
	PRINT Public 
SIF TEQUIP:シャワー
	PRINT Shower 
IF TEQUIP:バイブ && TEQUIP:アナルバイブ
	PRINT Double Vibrator 
ELSEIF TEQUIP:バイブ
	PRINT Vibrator 
ELSEIF TEQUIP:アナルバイブ
	PRINT Anal Vibrator 
ENDIF
PRINTL Masturbation

;難易度上昇
;公開で+10、バイブで+5、アナルバイブで+5、シャワーで＋3
V = 33
SIF TEQUIP:ビデオ撮影
	V += 10
SIF TEQUIP:シャワー
	V += 3
SIF TEQUIP:バイブ
	V += 5
SIF TEQUIP:アナルバイブ
	V += 5

CALL QOL_COM_ORDER_STANDARD_CHECKS(V, QOL_COM_ORDER_EXPOSURE_FULL, TARGET)
LOCAL = RESULT
SKIPDISP 0
RETURN LOCAL

@CAN_COM13_TR(ARG)
SKIPDISP ARG
PRINTL Spread Anus
;-------------------------------------------------
;実行できるかの判定
;-------------------------------------------------
IF FLAG:70 == 1 || CFLAG:イタズラ
ELSE
    CALL QOL_COM_ORDER_STANDARD_CHECKS(18, QOL_COM_ORDER_EXPOSURE_FULL | QOL_COM_ORDER_ANAL_PARTIAL, TARGET)
    LOCAL = RESULT
    SKIPDISP 0
    RETURN LOCAL
ENDIF
SKIPDISP 0
RETURN 1

@CAN_COM20_TR(ARG)
SKIPDISP ARG
PRINTL Kiss
;-------------------------------------------------
;実行できるかの判定
;-------------------------------------------------
IF FLAG:70 == 1 || CFLAG:イタズラ
ELSE
    CALL QOL_COM_ORDER_STANDARD_CHECKS(18, QOL_COM_ORDER_KISS_FULL, TARGET)
    LOCAL = RESULT
    SKIPDISP 0
    RETURN LOCAL
ENDIF
SKIPDISP 0
RETURN 1

@CAN_COM44_TR(ARG)
IF ARG == 1
	CALL ChkBeforeInsert(TARGET)
	IF RESULT < 0
		TFLAG:挿入不可 = RESULT * -1
	ENDIF
ENDIF
SIF TFLAG:挿入不可
	RETURN 0
SKIPDISP ARG
IF FLAG:70 == 1 || CFLAG:イタズラ
	PRINTL Vibrator Rape
ELSE
	IF TEQUIP:縄 == 0
		PRINTL Vibrator
		CALL QOL_COM_ORDER_STANDARD_CHECKS(GROUPMATCH(TALENT:処女, 1, -1) && HAS_PENIS(MASTER) ? 40 # 15, QOL_COM_ORDER_VAGINAL_FULL, TARGET)
		LOCAL = RESULT
		SKIPDISP 0
		RETURN LOCAL
	ELSE
		PRINTL Restrained Vibrator
	ENDIF
ENDIF
SKIPDISP 0
RETURN 1

@CAN_COM45_TR(ARG)
SKIPDISP ARG
IF FLAG:70 == 1 || CFLAG:イタズラ
	PRINTL Anal Vibrator Rape
ELSE
	IF TEQUIP:縄 == 0
		PRINTL Anal Vibrator
		CALL QOL_COM_ORDER_STANDARD_CHECKS(30, QOL_COM_ORDER_ANAL_FULL, TARGET)
		LOCAL = RESULT
		SKIPDISP 0
		RETURN LOCAL
	ELSE
		PRINTL Restrained Anal Vibrator
	ENDIF
ENDIF
SKIPDISP 0
RETURN 1

@CAN_COM46_TR(ARG)
SKIPDISP ARG
IF FLAG:70 == 1 || CFLAG:イタズラ
	PRINTL Anal Beads Rape
ELSE
	IF TEQUIP:縄 == 0
		PRINTL Anal Beads
		CALL QOL_COM_ORDER_STANDARD_CHECKS(32, QOL_COM_ORDER_ANAL_FULL, TARGET)
		LOCAL = RESULT
		SKIPDISP 0
		RETURN LOCAL
	ELSE
		PRINTL Restrained Anal Beads
	ENDIF
ENDIF
SKIPDISP 0
RETURN 1

@CAN_COM60_TR(ARG)
;-------------------------------------------------
;実行判定処理
;-------------------------------------------------
;NAS: Made universal
;IF ARG == 1
; 	CALL ChkBeforeInsert(TARGET)
; 	IF RESULT == -1
; 		TFLAG:挿入不可 = 1
; 	ENDIF
; ENDIF
SIF TFLAG:挿入不可
	RETURN 0
SKIPDISP ARG
IF FLAG:70 == 1 || CFLAG:イタズラ
	IF IS_STANDING_SEX(60)
		PRINTL Standing Missionary Rape
	ELSE
		PRINTL Missionary Rape
	ENDIF
ELSE
	;縄で縛られている場合は無視（縄が無い時のみ実行判定を行う）
	IF TEQUIP:縄 == 0
		IF IS_STANDING_SEX(60)
			PRINTL Standing Missionary
		ELSE
			PRINTL Missionary Position
		ENDIF
        
        CALL QOL_COM_ORDER_STANDARD_CHECKS(18, QOL_COM_ORDER_VAGINAL_FULL, TARGET)
        LOCAL = RESULT
        SKIPDISP 0
        RETURN LOCAL
	ELSE
		IF IS_STANDING_SEX(60)
			PRINTL Restrained Standing Missionary
		ELSE
			PRINTL Restrained Missionary
		ENDIF
	ENDIF
ENDIF
SKIPDISP 0
RETURN 1

@CAN_COM61_TR(ARG)
;-------------------------------------------------
;実行判定処理
;-------------------------------------------------
;NAS: Made universal
;IF ARG == 1
; 	CALL ChkBeforeInsert(TARGET)
; 	IF RESULT == -1
; 		TFLAG:挿入不可 = 1
; 	ENDIF
; ENDIF
SIF TFLAG:挿入不可
	RETURN 0
SKIPDISP ARG
IF FLAG:70 == 1 || CFLAG:イタズラ
	IF IS_STANDING_SEX(61)
		PRINTL Standing Rape From Behind
	ELSE
		PRINTL Doggy Style Rape
	ENDIF
ELSE
	;縄で縛られている場合は無視（縄が無い時のみ実行判定を行う）
	IF !TEQUIP:縄
		IF IS_STANDING_SEX(61)
			PRINTL Standing From Behind
		ELSE
			PRINTL Doggy Style
		ENDIF
        
        CALL QOL_COM_ORDER_STANDARD_CHECKS(22, QOL_COM_ORDER_VAGINAL_FULL, TARGET)
        LOCAL = RESULT
        SKIPDISP 0
        RETURN LOCAL
	ELSE
		IF IS_STANDING_SEX(61)
			PRINTL Restrained Standing From Behind
		ELSE
			PRINTL Restrained Doggy
		ENDIF
	ENDIF
ENDIF
SKIPDISP 0
RETURN 1

@CAN_COM62_TR(ARG)
SKIPDISP ARG
;-------------------------------------------------
;実行判定処理
;-------------------------------------------------
IF FLAG:70 == 1 || CFLAG:イタズラ
	IF IS_STANDING_SEX(62)
		PRINTL Standing Anal Missionary Rape
	ELSE
		PRINTL Anal Missionary Rape
	ENDIF
ELSE

	;縄で縛られている場合は無視（縄が無い時のみ実行判定を行う）
	IF !TEQUIP:縄
		IF IS_STANDING_SEX(62)
			PRINTL Standing Anal Missionary
		ELSE
			PRINTL Anal Missionary
		ENDIF
        CALL QOL_COM_ORDER_STANDARD_CHECKS(30, QOL_COM_ORDER_ANAL_FULL, TARGET)
        LOCAL = RESULT
        SKIPDISP 0
        RETURN LOCAL
	ELSE
		IF IS_STANDING_SEX(62)
			PRINTL Restrained Standing Anal Missionary
		ELSE
			PRINTL Restrained Anal Missionary
		ENDIF
	ENDIF
ENDIF
SKIPDISP 0
RETURN 1

@CAN_COM63_TR(ARG)
SKIPDISP ARG
;-------------------------------------------------
;実行判定処理
;-------------------------------------------------
IF FLAG:70 == 1 || CFLAG:イタズラ
	IF IS_STANDING_SEX(63)
		PRINTL Standing Anal Rape From Behind
	ELSE
		PRINTL Anal Doggy Rape
	ENDIF
ELSE
	;縄で縛られている場合は無視（縄が無い時のみ実行判定を行う）
	IF !TEQUIP:19
		IF IS_STANDING_SEX(63)
			PRINTL Standing Anal From Behind
		ELSE
			PRINTL Anal Doggy Style
		ENDIF
        CALL QOL_COM_ORDER_STANDARD_CHECKS(32, QOL_COM_ORDER_ANAL_FULL, TARGET)
        LOCAL = RESULT
        SKIPDISP 0
        RETURN LOCAL
	ELSE
		IF IS_STANDING_SEX(63)
			PRINTL Restrained Standing Anal From Behind
		ELSE
			PRINTL Restrained Anal Doggy
		ENDIF
	ENDIF
ENDIF
SKIPDISP 0
RETURN 1

@CAN_COM64_TR(ARG)
SKIPDISP ARG
PRINTL Reverse Rape
;-------------------------------------------------
;実行判定処理
;-------------------------------------------------
CALL QOL_COM_ORDER_STANDARD_CHECKS(15, QOL_COM_ORDER_RECEIVE_VAGINAL_FULL, TARGET)
LOCAL = RESULT
SKIPDISP 0
RETURN LOCAL

@CAN_COM65_TR(ARG)
;-------------------------------------------------
;実行判定処理
;-------------------------------------------------
;NAS: Made universal
;IF ARG == 1
; 	CALL ChkBeforeInsert(TARGET)
; 	IF RESULT == -1
; 		TFLAG:挿入不可 = 1
; 	ENDIF
; ENDIF
SIF TFLAG:挿入不可
	RETURN 0
SKIPDISP ARG
IF FLAG:70 == 1
	PRINTL Cowgirl Rape
ELSE
	PRINTL Cowgirl
    CALL QOL_COM_ORDER_STANDARD_CHECKS(35, QOL_COM_ORDER_VAGINAL_FULL, TARGET)
    LOCAL = RESULT
    SKIPDISP 0
    RETURN LOCAL
ENDIF
SKIPDISP 0
RETURN 1

@CAN_COM66_TR(ARG)
IF FLAG:70 == 1
	SKIPDISP ARG
	PRINTL Anal Cowgirl Rape
ELSE
	SKIPDISP ARG
	PRINTL Anal Cowgirl
    CALL QOL_COM_ORDER_STANDARD_CHECKS(45, QOL_COM_ORDER_ANAL_FULL, TARGET)
    LOCAL = RESULT
    SKIPDISP 0
    RETURN LOCAL
ENDIF
SKIPDISP 0
RETURN 1

@CAN_COM67_TR(ARG)
;-------------------------------------------------
;実行判定処理
;-------------------------------------------------
;NAS: Made universal
;IF ARG == 1
; 	CALL ChkBeforeInsert(TARGET)
; 	IF RESULT == -1
; 		TFLAG:挿入不可 = 1
; 	ENDIF
; ENDIF
SIF TFLAG:挿入不可
	RETURN 0
SKIPDISP ARG

IF FLAG:70 == 1
	IF IS_STANDING_SEX(67)
		PRINTL Suspended Congress Rape
	ELSE
		PRINTL Lotus Position Rape
	ENDIF
ELSE
	;縄で縛られている場合は無視（縄が無い時のみ実行判定を行う）
	IF !TEQUIP:縄
		IF IS_STANDING_SEX(67)
			PRINTL Suspended Congress
		ELSE
			PRINTL Lotus Position
		ENDIF
        CALL QOL_COM_ORDER_STANDARD_CHECKS(40, QOL_COM_ORDER_VAGINAL_FULL, TARGET)
        LOCAL = RESULT
        SKIPDISP 0
        RETURN LOCAL
	ELSE
		IF IS_STANDING_SEX(67)
			PRINTL Restrained Suspended Congress
		ELSE
			PRINTL Restrained Lotus Position
		ENDIF
	ENDIF
ENDIF
SKIPDISP 0
RETURN 1

@CAN_COM68_TR(ARG)
;-------------------------------------------------
;実行判定処理
;-------------------------------------------------
;NAS: Made universal
;IF ARG == 1
; 	CALL ChkBeforeInsert(TARGET)
; 	IF RESULT == -1
; 		TFLAG:挿入不可 = 1
; 	ENDIF
; ENDIF
SIF TFLAG:挿入不可
	RETURN 0
SKIPDISP ARG
IF FLAG:70 == 1
	IF IS_STANDING_SEX(68)
		PRINTL Reverse Suspended Congress Rape
	ELSE
		PRINTL Reverse Lotus Rape
	ENDIF
ELSE
	;縄で縛られている場合は無視（縄が無い時のみ実行判定を行う）
	IF !TEQUIP:縄
		IF IS_STANDING_SEX(68)
			PRINTL Reverse Suspended Congress
		ELSE
			PRINTL Reverse Lotus
		ENDIF
        CALL QOL_COM_ORDER_STANDARD_CHECKS(42, QOL_COM_ORDER_VAGINAL_FULL, TARGET)
        LOCAL = RESULT
        SKIPDISP 0
        RETURN LOCAL
	ELSE
		IF IS_STANDING_SEX(68)
			PRINTL Restrained Reverse Suspended Congress
		ELSE
			PRINTL Restrained Reverse Lotus
		ENDIF
	ENDIF
ENDIF
SKIPDISP 0
RETURN 1

@CAN_COM69_TR(ARG)
SKIPDISP ARG
;-------------------------------------------------
;実行判定処理
;-------------------------------------------------
IF FLAG:70 == 1
	IF IS_STANDING_SEX(69)
		PRINTL Anal Suspended Congress Rape
	ELSE
		PRINTL Anal Lotus Rape
	ENDIF
ELSE
	;縄で縛られている場合は無視（縄が無い時のみ実行判定を行う）
	IF !TEQUIP:縄
		IF IS_STANDING_SEX(69)
			PRINTL Anal Suspended Congress
		ELSE
			PRINTL Anal Lotus
		ENDIF
        CALL QOL_COM_ORDER_STANDARD_CHECKS(50, QOL_COM_ORDER_ANAL_FULL, TARGET)
        LOCAL = RESULT
        SKIPDISP 0
        RETURN LOCAL
	ELSE
		IF IS_STANDING_SEX(69)
			PRINTL Restrained Anal Suspended Congress
		ELSE
			PRINTL Restrained Anal Lotus
		ENDIF
	ENDIF
ENDIF
SKIPDISP 0
RETURN 1

@CAN_COM70_TR(ARG)
SKIPDISP ARG
;-------------------------------------------------
;実行判定処理
;-------------------------------------------------
IF FLAG:70 == 1
	IF IS_STANDING_SEX(70)
		PRINTL Anal Reverse Suspended Congress Rape
	ELSE
		PRINTL Anal Reverse Lotus Rape
	ENDIF
ELSE
	;縄で縛られている場合は無視（縄が無い時のみ実行判定を行う）
	IF !TEQUIP:縄
		IF IS_STANDING_SEX(70)
			PRINTL Anal Reverse Suspended Congress
		ELSE
			PRINTL Anal Reverse Lotus
		ENDIF
        CALL QOL_COM_ORDER_STANDARD_CHECKS(52, QOL_COM_ORDER_ANAL_FULL, TARGET)
        LOCAL = RESULT
        SKIPDISP 0
        RETURN LOCAL
	ELSE
		IF IS_STANDING_SEX(70)
			PRINTL Restrained Anal Reverse Suspended Congress
		ELSE
			PRINTL Restrained Anal Reverse Lotus
		ENDIF
	ENDIF
ENDIF
SKIPDISP 0
RETURN 1

@CAN_COM79_TR(ARG)
;-------------------------------------------------
;実行判定処理
;-------------------------------------------------
;NAS: Made universal
;IF ARG == 1
; 	CALL ChkBeforeInsert(TARGET)
; 	IF RESULT == -1
; 		TFLAG:挿入不可 = 1
; 	ENDIF
; ENDIF
SIF TFLAG:挿入不可
	RETURN 0
SKIPDISP ARG
IF FLAG:70 == 1 || CFLAG:イタズラ
	IF IS_STANDING_SEX(79)
		PRINTL Standing Inchling Rape
	ELSE
		PRINTL Inchling Rape
	ENDIF
ELSE
	;縄で縛られている場合は無視（縄が無い時のみ実行判定を行う）
	IF TEQUIP:縄 == 0
        IF IS_STANDING_SEX(79)
			PRINTL Standing Inchling Onahole
		ELSE
			PRINTL Inchling Onahole
		ENDIF
        CALL QOL_COM_ORDER_STANDARD_CHECKS(40, QOL_COM_ORDER_VAGINAL_FULL, TARGET)
        LOCAL = RESULT
        SKIPDISP 0
        RETURN LOCAL
    ELSE
        IF IS_STANDING_SEX(79)
			PRINTL Restrained Standing Inchling Onahole
		ELSE
			PRINTL Inchling Onahole
		ENDIF
	ENDIF
ENDIF
SKIPDISP 0
RETURN 1

@CAN_COM80_TR(ARG)
SKIPDISP ARG
PRINTL Handjob
;-------------------------------------------------
;実行判定処理
;-------------------------------------------------
IF FLAG:70 == 1 || CFLAG:イタズラ

ELSE
    CALL QOL_COM_ORDER_STANDARD_CHECKS(14, QOL_COM_ORDER_SERVICE_FULL | QOL_COM_ORDER_DIRT_SOURCE_PENIS, TARGET)
    LOCAL = RESULT
    SKIPDISP 0
    RETURN LOCAL
ENDIF
RETURN 1

@CAN_COM81_TR(ARG)
SKIPDISP ARG
PRINTL Fellatio
;-------------------------------------------------
;実行判定処理
;-------------------------------------------------
IF FLAG:70 == 1 || CFLAG:イタズラ

ELSE
    CALL QOL_COM_ORDER_STANDARD_CHECKS(24, QOL_COM_ORDER_SERVICE_CUM_FULL | QOL_COM_ORDER_DIRT_SOURCE_PENIS, TARGET)
    LOCAL = RESULT
    SKIPDISP 0
    RETURN LOCAL
ENDIF
RETURN 1

@CAN_COM82_TR(ARG)
SKIPDISP ARG
PRINTL Paizuri
;-------------------------------------------------
;実行判定処理
;-------------------------------------------------
IF FLAG:70 == 1 || CFLAG:イタズラ

ELSE
    CALL QOL_COM_ORDER_STANDARD_CHECKS(30, QOL_COM_ORDER_SERVICE_CUM_FULL | QOL_COM_ORDER_DIRT_SOURCE_PENIS, TARGET)
    LOCAL = RESULT
    SKIPDISP 0
    RETURN LOCAL
ENDIF
RETURN 1

@CAN_COM83_TR(ARG)
SKIPDISP ARG
PRINTL Sumata
;-------------------------------------------------
;実行判定処理
;-------------------------------------------------
IF FLAG:70 == 1

ELSE
    CALL QOL_COM_ORDER_STANDARD_CHECKS(20, QOL_COM_ORDER_SERVICE_FULL | QOL_COM_ORDER_DIRT_SOURCE_PENIS | QOL_COM_ORDER_DIRT_SOURCE_WEAKER, TARGET)
    LOCAL = RESULT
    SKIPDISP 0
    RETURN LOCAL
ENDIF
RETURN 1

@CAN_COM84_TR(ARG)
SKIPDISP ARG
PRINTL Foam Play
;-------------------------------------------------
;実行判定処理
;-------------------------------------------------
IF FLAG:70 == 1

ELSE
    CALL QOL_COM_ORDER_STANDARD_CHECKS(30, QOL_COM_ORDER_SERVICE_CUM_FULL | QOL_COM_ORDER_DIRT_SOURCE_PENIS, TARGET)
    LOCAL = RESULT
    SKIPDISP 0
    RETURN LOCAL
ENDIF
RETURN 1

@CAN_COM85_TR(ARG)
SKIPDISP ARG
PRINTL Footjob
;-------------------------------------------------
;実行判定処理
;-------------------------------------------------
IF FLAG:70 == 1 || CFLAG:イタズラ

ELSE
    CALL QOL_COM_ORDER_STANDARD_CHECKS(22, QOL_COM_ORDER_SERVICE_FULL | QOL_COM_ORDER_SADISM | QOL_COM_ORDER_DIRT_SOURCE_PENIS | QOL_COM_ORDER_DIRT_SOURCE_WEAKER, TARGET)
    LOCAL = RESULT
    SKIPDISP 0
    RETURN LOCAL
ENDIF
RETURN 1

@CAN_COM88_TR(ARG)
SKIPDISP ARG
PRINTL Vacuum Fellatio
;-------------------------------------------------
;実行判定処理
;-------------------------------------------------
IF FLAG:70 == 1 || CFLAG:イタズラ

ELSE
    CALL QOL_COM_ORDER_STANDARD_CHECKS(30, QOL_COM_ORDER_SERVICE_CUM_FULL | QOL_COM_ORDER_DIRT_SOURCE_PENIS, TARGET)
    LOCAL = RESULT
    SKIPDISP 0
    RETURN LOCAL
ENDIF
RETURN 1

@CAN_COM90_TR(ARG)
SKIPDISP ARG
PRINTL Get Anal Caress
;-------------------------------------------------
;実行判定処理
;-------------------------------------------------
CALL QOL_COM_ORDER_STANDARD_CHECKS(18, QOL_COM_ORDER_SERVICE_FULL | QOL_COM_ORDER_SADISM | QOL_COM_ORDER_DIRT_SOURCE_ANAL, TARGET)
LOCAL = RESULT
SKIPDISP 0
RETURN LOCAL
RETURN 1

@CAN_COM91_TR(ARG)
SKIPDISP ARG
PRINTL Get Analingus
;-------------------------------------------------
;実行判定処理
;-------------------------------------------------
CALL QOL_COM_ORDER_STANDARD_CHECKS(18, QOL_COM_ORDER_SERVICE_FULL | QOL_COM_ORDER_SADISM | QOL_COM_ORDER_PLAYER_SADISM | QOL_COM_ORDER_DIRT_SOURCE_ANAL, TARGET)
LOCAL = RESULT
SKIPDISP 0
RETURN LOCAL
RETURN 1

@CAN_COM92_TR(ARG)
SKIPDISP ARG
;-------------------------------------------------
;実行判定処理
;-------------------------------------------------
;縄で縛られている場合は無視（縄が無い時のみ実行判定を行う）
IF !TEQUIP:縄
	IF IS_STANDING_SEX(92)
		PRINTL Catch Standing Anal Missionary
	ELSE
		PRINTL Catch Anal Missionary
	ENDIF
    CALL QOL_COM_ORDER_STANDARD_CHECKS(30, QOL_COM_ORDER_RECEIVE_ANAL_FULL | QOL_COM_ORDER_SADISM | QOL_COM_ORDER_DIRT_SOURCE_ANAL, TARGET)
    LOCAL = RESULT
    SKIPDISP 0
    RETURN LOCAL
ENDIF
RETURN 1

@CAN_COM93_TR(ARG)
SKIPDISP ARG
;-------------------------------------------------
;実行判定処理
;-------------------------------------------------
;縄で縛られている場合は無視（縄が無い時のみ実行判定を行う）
IF !TEQUIP:縄
	IF IS_STANDING_SEX(93)
		PRINTL Get Standing Anal From Behind
	ELSE
		PRINTL Get Anal Doggy
	ENDIF
    CALL QOL_COM_ORDER_STANDARD_CHECKS(30, QOL_COM_ORDER_RECEIVE_ANAL_FULL | QOL_COM_ORDER_SADISM | QOL_COM_ORDER_DIRT_SOURCE_ANAL, TARGET)
    LOCAL = RESULT
    SKIPDISP 0
    RETURN LOCAL
ENDIF
RETURN 1

@CAN_COM94_TR(ARG)
SKIPDISP ARG
;-------------------------------------------------
;実行判定処理
;-------------------------------------------------
IF FLAG:70 == 1 || CFLAG:イタズラ
	PRINTL Give Anal Cowgirl
ELSE
    ;縄で縛られている場合は無視（縄が無い時のみ実行判定を行う）
    IF !TEQUIP:縄
        PRINTL Give Anal Cowgirl
        CALL QOL_COM_ORDER_STANDARD_CHECKS(30, QOL_COM_ORDER_RECEIVE_ANAL_FULL | QOL_COM_ORDER_SADISM | QOL_COM_ORDER_DIRT_SOURCE_ANAL, TARGET)
        LOCAL = RESULT
        SKIPDISP 0
        RETURN LOCAL
    ELSE
        PRINTL Give Anal Cowgirl
    ENDIF
ENDIF
RETURN 1

@CAN_COM99_TR(ARG)
SKIPDISP ARG
PRINTL Onaholejob
;-------------------------------------------------
;実行判定処理
;-------------------------------------------------
IF FLAG:70 == 1 || CFLAG:イタズラ

ELSE
    CALL QOL_COM_ORDER_STANDARD_CHECKS(25, QOL_COM_ORDER_SERVICE_FULL | QOL_COM_ORDER_DIRT_SOURCE_PENIS, TARGET)
    LOCAL = RESULT
    SKIPDISP 0
    RETURN LOCAL
ENDIF
RETURN 1

@CAN_COM120_TR(ARG)
SKIPDISP ARG
PRINTL Get Cunnilingus
;-------------------------------------------------
;実行判定処理
;-------------------------------------------------
CALL QOL_COM_ORDER_STANDARD_CHECKS(23, QOL_COM_ORDER_SERVICE_FULL | QOL_COM_ORDER_DIRT_SOURCE_VAGINAL, TARGET)
LOCAL = RESULT
SKIPDISP 0
RETURN LOCAL

@CAN_COM124_TR(ARG)
SKIPDISP ARG
PRINTL Give Handjob
;-------------------------------------------------
;実行判定処理
;-------------------------------------------------
IF FLAG:70 == 1 || CFLAG:イタズラ

ELSE
    CALL QOL_COM_ORDER_STANDARD_CHECKS(23, QOL_COM_ORDER_GIVE_SERVICE_FULL, TARGET)
    LOCAL = RESULT
    SKIPDISP 0
    RETURN LOCAL
ENDIF
RETURN 1

@CAN_COM125_TR(ARG)
SKIPDISP ARG
PRINTL Be Caressed
;-------------------------------------------------
;実行判定処理
;-------------------------------------------------
IF !FLAG:70
    CALL QOL_COM_ORDER_STANDARD_CHECKS(23, QOL_COM_ORDER_SERVICE_FULL | QOL_COM_ORDER_DIRT_SOURCE_VAGINAL, TARGET)
    LOCAL = RESULT
    SKIPDISP 0
    RETURN LOCAL
ENDIF
RETURN 1

@CAN_COM130_TR(ARG)
SKIPDISP ARG
IF IS_STANDING_SEX(130)
	PRINTL Catch Standing Missionary
ELSE
	PRINTL Catch Missionary Position
ENDIF
CALL QOL_COM_ORDER_STANDARD_CHECKS(15, QOL_COM_ORDER_RECEIVE_VAGINAL_FULL, TARGET)
LOCAL = RESULT
SKIPDISP 0
RETURN LOCAL

@CAN_COM131_TR(ARG)
SKIPDISP ARG
IF IS_STANDING_SEX(131)
	PRINTL Catch Standing Doggy Style
ELSE
	PRINTL Catch Doggy Style
ENDIF
CALL QOL_COM_ORDER_STANDARD_CHECKS(15, QOL_COM_ORDER_RECEIVE_VAGINAL_FULL, TARGET)
LOCAL = RESULT
SKIPDISP 0
RETURN LOCAL

@CAN_COM132_TR(ARG)
SKIPDISP ARG
PRINTL Catch Lotus Position
CALL QOL_COM_ORDER_STANDARD_CHECKS(15, QOL_COM_ORDER_RECEIVE_VAGINAL_FULL, TARGET)
LOCAL = RESULT
SKIPDISP 0
RETURN LOCAL

@CAN_COM133_TR(ARG)
SKIPDISP ARG
PRINTL Catch Reverse Lotus
CALL QOL_COM_ORDER_STANDARD_CHECKS(15, QOL_COM_ORDER_RECEIVE_VAGINAL_FULL, TARGET)
LOCAL = RESULT
SKIPDISP 0
RETURN LOCAL

@CAN_COM140_TR(ARG)
SKIPDISP ARG
PRINTL Deepthroat
;-------------------------------------------------
;実行判定処理
;-------------------------------------------------
IF FLAG:70 == 1 || CFLAG:イタズラ

ELSE
    CALL QOL_COM_ORDER_STANDARD_CHECKS(36, QOL_COM_ORDER_SERVICE_CUM_FULL | QOL_COM_ORDER_DIRT_SOURCE_PENIS, TARGET)
    LOCAL = RESULT
    SKIPDISP 0
    RETURN LOCAL
ENDIF
RETURN 1

@CAN_COM145_TR(ARG)
SKIPDISP ARG
PRINTL Anal Service
CALL QOL_COM_ORDER_STANDARD_CHECKS(23, QOL_COM_ORDER_SERVICE_FULL | QOL_COM_ORDER_DIRT_SOURCE_ANAL, TARGET)
LOCAL = RESULT
SKIPDISP 0
RETURN LOCAL

@CAN_COM150_TR(ARG)
SKIPDISP ARG
PRINTL Forced Paizuri
;-------------------------------------------------
;実行判定処理
;-------------------------------------------------
IF FLAG:70 == 1 || CFLAG:イタズラ

ELSE
    CALL QOL_COM_ORDER_STANDARD_CHECKS(36, QOL_COM_ORDER_SERVICE_CUM_FULL | QOL_COM_ORDER_DIRT_SOURCE_PENIS, TARGET)
    LOCAL = RESULT
    SKIPDISP 0
    RETURN LOCAL
ENDIF
RETURN 1

@CAN_COM192_TR(ARG)
SKIPDISP ARG
;-------------------------------------------------
;実行判定処理
;-------------------------------------------------
IF FLAG:70 == 1 || CFLAG:イタズラ

ELSE
;has unique requirements
    CALL QOL_COM_ORDER_STANDARD(TARGET)
    SIF ABL:TARGET:欲望
        CALL QOL_COM_ORDER_ELEMENT_ABL(TARGET, "欲望", 1)
    SIF ABL:TARGET:Ｖ感覚
        CALL QOL_COM_ORDER_ELEMENT_ABL(TARGET, "Ｖ感覚", 2)
    SIF ABL:TARGET:精液中毒
        CALL QOL_COM_ORDER_ELEMENT_ABL(TARGET, "精液中毒", 3)
    SIF MARK:TARGET:快楽刻印
        CALL QOL_COM_ORDER_ELEMENT_MARK(TARGET, "快楽刻印", 1)
    SIF GETPALAMLV(PALAM:TARGET:欲情,5)
        CALL QOL_COM_ORDER_ELEMENT_PALAM(TARGET, "欲情", 1, 5)
    SIF TALENT:TARGET:羞恥心 > 0
        CALL QOL_COM_ORDER_ELEMENT_TALENT(TARGET, "羞恥心", -1)
    SIF OTOKOGIRAI(TARGET)
        CALL QOL_COM_ORDER_ELEMENT_TALENT(TARGET, "性別嗜好", -12)
    SIF TALENT:TARGET:恋慕 && ASSIPLAY == 0
        CALL QOL_COM_ORDER_ELEMENT_TALENT(TARGET, "恋慕", 5)
    SIF TALENT:TARGET:愛欲 && ASSIPLAY == 0
        CALL QOL_COM_ORDER_ELEMENT_TALENT(TARGET, "愛欲", 5)
    IF CFLAG:TARGET:生解禁 == 2
        CALL QOL_COM_ORDER_ELEMENT(TARGET, "Babymaking Agreement", 5)
    ELSEIF SHIRAHU(TARGET)
        CALL QOL_COM_ORDER_ELEMENT(TARGET, "No Babymaking Agreement", -200)
    ENDIF
    SIF (TALENT:PLAYER:2 & 3) == 3
        CALL QOL_COM_ORDER_ELEMENT_TALENT(TARGET, "性別", 4, PLAYER)

    CALL QOL_COM_ORDER_CHECK(TARGET, 100)
    LOCAL = RESULT
    SKIPDISP 0
    RETURN LOCAL
    RETURN 1
ENDIF

@CAN_COM511_TR(ARG)
SKIPDISP ARG
;-------------------------------------------------
;実行判定処理
;-------------------------------------------------
CALL QOL_COM_ORDER_STANDARD_CHECKS(32, QOL_COM_ORDER_SERVICE_CUM_FULL | QOL_COM_ORDER_DIRT_SOURCE_PENIS, TARGET)
LOCAL = RESULT
SKIPDISP 0
RETURN LOCAL

;CAN_SCOM replacements
@CAN_SCOM1_TR(ARG)
SKIPDISP ARG
PRINTL Sixty Nine
;-------------------------------------------------
;実行判定処理
;-------------------------------------------------
IF FLAG:70 == 1

ELSE
    CALL QOL_COM_ORDER_STANDARD_CHECKS(33, QOL_COM_ORDER_SERVICE_CUM_FULL | QOL_COM_ORDER_DIRT_SOURCE_PENIS | QOL_COM_ORDER_DIRT_SOURCE_MOUTH, TARGET)
    LOCAL = RESULT
    SKIPDISP 0
    RETURN LOCAL
ENDIF
RETURN 1

@CAN_SCOM4_TR(ARG)
;-------------------------------------------------
;実行判定処理
;-------------------------------------------------
IF FLAG:70 == 1

ELSE
	;縄で縛られている場合は無視（縄が無い時のみ実行判定を行う）
	IF !TEQUIP:縄
		SKIPDISP ARG
		PRINTL Exposed Reverse Lotus
        
        CALL QOL_COM_ORDER_STANDARD_CHECKS(47, QOL_COM_ORDER_VAGINAL_FULL | QOL_COM_ORDER_EXPOSURE_PARTIAL, TARGET)
        LOCAL = RESULT
        SKIPDISP 0
        RETURN LOCAL
	ENDIF
ENDIF
RETURN 1

@CAN_SCOM5_TR(ARG)
SKIPDISP ARG
PRINTL Handjob & Fellatio
;-------------------------------------------------
;実行判定処理
;-------------------------------------------------
CALL QOL_COM_ORDER_STANDARD_CHECKS(24, QOL_COM_ORDER_SERVICE_CUM_FULL | QOL_COM_ORDER_DIRT_SOURCE_PENIS, TARGET)
LOCAL = RESULT
SKIPDISP 0
RETURN LOCAL

@CAN_SCOM8_TR(ARG)
SKIPDISP ARG
PRINTL 69 Paizuri
;-------------------------------------------------
;実行判定処理
;-------------------------------------------------
CALL QOL_COM_ORDER_STANDARD_CHECKS(34, QOL_COM_ORDER_SERVICE_CUM_FULL | QOL_COM_ORDER_DIRT_SOURCE_PENIS | QOL_COM_ORDER_DIRT_SOURCE_MOUTH, TARGET)
LOCAL = RESULT
SKIPDISP 0
RETURN LOCAL

@CAN_SCOM9_TR(ARG)
SKIPDISP ARG
PRINTL Double Fellatio
;-------------------------------------------------
;実行判定処理
;-------------------------------------------------
;消費体力、気力
IF FLAG:70 == 1

ELSE
    LOCAL = MASTER_POSE(SET_M,SET_P,1)
    CALL QOL_COM_ORDER_STANDARD_CHECKS_DOUBLE(34, QOL_COM_ORDER_SERVICE_CUM_FULL | QOL_COM_ORDER_DIRT_SOURCE_PENIS | QOL_COM_ORDER_DUO, TARGET, LOCAL)
    LOCAL:1 = RESULT
    SKIPDISP 0
    RETURN LOCAL:1
ENDIF
SKIPDISP 0
RETURN 1

@CAN_SCOM10_TR(ARG)
SKIPDISP ARG
PRINTL Double Sumata
;-------------------------------------------------
;実行判定処理
;-------------------------------------------------
IF FLAG:70 == 1

ELSE
    LOCAL = MASTER_POSE(SET_C,SET_P,1)
    CALL QOL_COM_ORDER_STANDARD_CHECKS_DOUBLE(25, QOL_COM_ORDER_SERVICE_FULL | QOL_COM_ORDER_DIRT_SOURCE_PENIS | QOL_COM_ORDER_DIRT_SOURCE_WEAKER | QOL_COM_ORDER_DUO, TARGET, LOCAL)
    LOCAL:1 = RESULT
    SKIPDISP 0
    RETURN LOCAL:1
ENDIF
SKIPDISP 0
RETURN 1

@CAN_SCOM11_TR(ARG)
SKIPDISP ARG
PRINTL Double Paizuri
;-------------------------------------------------
;実行判定処理
;-------------------------------------------------
IF FLAG:70 == 1

ELSE
    LOCAL = MASTER_POSE(SET_B,SET_P,1)
    CALL QOL_COM_ORDER_STANDARD_CHECKS_DOUBLE(38, QOL_COM_ORDER_SERVICE_CUM_FULL | QOL_COM_ORDER_DIRT_SOURCE_PENIS | QOL_COM_ORDER_DUO, TARGET, LOCAL)
    LOCAL:1 = RESULT
    SKIPDISP 0
    RETURN LOCAL:1
ENDIF
SKIPDISP 0
RETURN 1

@CAN_SCOM12_TR(ARG)
SKIPDISP ARG
PRINTL Paizuri & Fellatio
;-------------------------------------------------
;実行判定処理
;-------------------------------------------------
IF FLAG:70 == 1

ELSE
    CALL QOL_COM_ORDER_STANDARD_CHECKS(35, QOL_COM_ORDER_SERVICE_CUM_FULL | QOL_COM_ORDER_DIRT_SOURCE_PENIS, TARGET)
    LOCAL:1 = RESULT
    SKIPDISP 0
    RETURN LOCAL:1
ENDIF
SKIPDISP 0
RETURN 1

@CAN_SCOM13_TR(ARG)
SKIPDISP ARG
;-------------------------------------------------
;実行判定処理
;-------------------------------------------------
IF FLAG:70 == 1
	PRINTL Alternate Insertion
ELSE
	;縄で縛られている場合は無視（縄が無い時のみ実行判定を行う）
	IF TEQUIP:縄 == 0
		PRINTL Alternate Insertion

		LOCAL = MASTER_POSE(SET_V,SET_P,1)
		CALL QOL_COM_ORDER_STANDARD_CHECKS_DOUBLE(25, QOL_COM_ORDER_VAGINAL_FULL | QOL_COM_ORDER_DUO, TARGET, LOCAL)
        LOCAL:1 = RESULT
        SKIPDISP 0
        RETURN LOCAL:1
	ELSE
		PRINTL Alternate Insertion
	ENDIF
ENDIF
SKIPDISP 0
RETURN 1

@CAN_SCOM16_TR(ARG)
SKIPDISP ARG
;-------------------------------------------------
;実行判定処理
;-------------------------------------------------
IF FLAG:70 == 1
	PRINTL Alternate Anal Insertion
ELSE

	;縄で縛られている場合は無視（縄が無い時のみ実行判定を行う）
	IF TEQUIP:縄 == 0
		PRINTL Alternate Anal Insertion

		LOCAL = MASTER_POSE(SET_A,SET_P,1)
		CALL QOL_COM_ORDER_STANDARD_CHECKS_DOUBLE(25, QOL_COM_ORDER_ANAL_FULL | QOL_COM_ORDER_DUO, TARGET, LOCAL)
        LOCAL:1 = RESULT
        SKIPDISP 0
        RETURN LOCAL:1
	ELSE
		PRINTL Alternate Anal Insertion
	ENDIF
ENDIF
SKIPDISP 0
RETURN 1

@CAN_SCOM19_TR(ARG)
SKIPDISP ARG
PRINTL Nursing Handjob
;-------------------------------------------------
;実行判定処理
;-------------------------------------------------
IF !FLAG:70
    CALL QOL_COM_ORDER_STANDARD_CHECKS(32, QOL_COM_ORDER_SERVICE_FULL | QOL_COM_ORDER_DIRT_SOURCE_PENIS, TARGET)
    LOCAL:1 = RESULT
    SKIPDISP 0
    RETURN LOCAL:1
ENDIF
SKIPDISP 0
RETURN 1