; Modular ERB: QOL/QoL_UnlockNotify.ERB

@EVENTTURNEND
#LATER
FOR LOCAL,0,CHARANUM
CALL UNLOCK_COND_NOTIFY(LOCAL)
NEXT

@UNLOCK_COND_NOTIFY(ARG)
SETCOLOR C_YELLOW
SELECTCASE ARG
	CASE [[小鈴]]
		IF ABL:ARG:親密 >= 5 && !GETBIT(nUnlockCond:ARG:0, 0) ;suzu, connected to other qol with suzunaan
			PRINTFORML 이제 코스즈에게서 책 두 권을 한 번에 빌릴 수 있다!
			PRINTFORML 이제 코스즈에게서 희귀한 이상한 책 한 권을 빌릴 수 있다!
			SETBIT nUnlockCond:ARG:0, 0
		ENDIF
		IF ABL:ARG:親密 >= 10 && !GETBIT(nUnlockCond:ARG:0, 1)
			PRINTFORML 이제 코스즈에게서 세 권의 책을 한 번에 빌릴 수 있다!
			SETBIT nUnlockCond:ARG:0, 1
		ENDIF
	CASE [[霊夢]]
		IF ABL:ARG:親密 >= 5 && !GETBIT(nUnlockCond:ARG:0, 0)
			PRINTFORML 레이무와 친구가 된 후, 이제 박쿠레이 신사 정원에 로그인할 수 있다!
			PRINTFORML 레이무와 친구가 된 후, 이제 박쿠레이 신사 부지의 가판대를 열 수 있다!
			PRINTFORML 레이무와 친구가 된 후, 이제 박쿠레이 신사 부지에 함정을 설치할 수 있다!
			SETBIT nUnlockCond:ARG:0, 0
		ENDIF
	CASE [[妹紅]]
		IF ABL:ARG:親密 >= 5 && !GETBIT(nUnlockCond:ARG:0, 0) && !GETBIT(nUnlockCond_general, 0)
			PRINTFORML 목옥과 친구가 된 후에는 대나무 숲에 접속할 수 있다!
			SETBIT nUnlockCond:ARG:0, 0
			SETBIT nUnlockCond_general, 0
		ENDIF
		IF ABL:ARG:親密 >= 5 && !GETBIT(nUnlockCond:ARG:0, 1) && !GETBIT(nUnlockCond_general, 6)
			PRINTFORML 모코우와 친구가 된 후에는 대나무 숲에 함정을 설치할 수 있게 되었다!
			SETBIT nUnlockCond:ARG:0, 1
			SETBIT nUnlockCond_general, 6
		ENDIF
		IF ABL:ARG:親密 >= 5 && !GETBIT(nUnlockCond:ARG:0, 2)
			PRINTFORML 목옥과 친구가 된 후, %HE_SHE(ARG)%은 당신에게 비밀스러운 혼합 레시피를 공유했다!
			PRINTFORML [환생의 깃털]이 해금되었다!
			SETBIT nUnlockCond:ARG:0, 2
		ENDIF
	CASE [[鈴仙]]
		IF TALENT:ARG:恋慕 && !GETBIT(nUnlockCond:ARG:0, 0) && !GETBIT(nUnlockCond_general, 5)
			PRINTFORML 리센을 유혹한 후에는 이제 에이텐이의 내밀한 성소에 접근할 수 있게 되었다!
			SETBIT nUnlockCond:ARG:0, 0
			SETBIT nUnlockCond_general, 5
		ENDIF
	CASE [[てゐ]]
		IF ABL:ARG:親密 >= 5 && !GETBIT(nUnlockCond:ARG:0, 0) && !GETBIT(nUnlockCond_general, 0)
			PRINTFORML 테이와 친해진 후에는 대나무 숲에 접속할 수 있다!
			SETBIT nUnlockCond:ARG:0, 0
			SETBIT nUnlockCond_general, 0
		ENDIF
		IF TALENT:ARG:恋慕 && !GETBIT(nUnlockCond:ARG:0, 1) && !GETBIT(nUnlockCond_general, 5)
			PRINTFORML 테위에게 유혹한 후에는 에이텐이의 내밀한 성소에 접근할 수 있다!
			SETBIT nUnlockCond:ARG:0, 1
			SETBIT nUnlockCond_general, 5
		ENDIF
		IF ABL:ARG:親密 >= 5 && !GETBIT(nUnlockCond:ARG:0, 2) && !GETBIT(nUnlockCond_general, 6)
			PRINTFORML 테이와 친구가 된 후에는 대나무 숲에 함정을 설치할 수 있다!
			SETBIT nUnlockCond:ARG:0, 2
			SETBIT nUnlockCond_general, 6
		ENDIF
	CASE [[輝夜]]
		IF ABL:ARG:親密 >= 5 && !GETBIT(nUnlockCond:ARG:0, 0) && !GETBIT(nUnlockCond_general, 0)
			PRINTFORML 카구야와 친구가 된 후에는 대나무 숲에 로그인할 수 있게 되었다!
			SETBIT nUnlockCond:ARG:0, 0
			SETBIT nUnlockCond_general, 0
		ENDIF
		IF ABL:ARG:親密 >= 5 && !GETBIT(nUnlockCond:ARG:0, 1) && !GETBIT(nUnlockCond_general, 2)
			PRINTFORML 카구야와 친구가 된 후, 에이텐이의 가판대를 열 수 있게 되었다!
			SETBIT nUnlockCond:ARG:0, 1
			SETBIT nUnlockCond_general, 2
		ENDIF
		IF TALENT:ARG:恋慕 && !GETBIT(nUnlockCond:ARG:0, 2) && !GETBIT(nUnlockCond_general, 5)
			PRINTFORML 카구야를 유혹한 후, 이제 에전이의 내밀한 성소에 접근할 수 있다!
			SETBIT nUnlockCond:ARG:0, 2
			SETBIT nUnlockCond_general, 5
		ENDIF
		IF ABL:ARG:親密 >= 5 && !GETBIT(nUnlockCond:ARG:0, 3) && !GETBIT(nUnlockCond_general, 6)
			PRINTFORML 카구야와 친해지면 대나무 숲에 함정을 설치할 수 있게 되었다!
			SETBIT nUnlockCond:ARG:0, 3
			SETBIT nUnlockCond_general, 6
		ENDIF
	CASE [[永琳]]
		IF ABL:ARG:親密 >= 5 && !GETBIT(nUnlockCond:ARG:0, 0) && !GETBIT(nUnlockCond_general, 0)
			PRINTFORML 에린과 친해진 후에는 대나무 숲에 접속할 수 있다!
			SETBIT nUnlockCond:ARG:0, 0
			SETBIT nUnlockCond_general, 0
		ENDIF
		IF ABL:ARG:親密 >= 5 && !GETBIT(nUnlockCond:ARG:0, 1) && !GETBIT(nUnlockCond_general, 2)
			PRINTFORML 에이렌과 친구가 된 후에는 에이텐이의 가판대를 열 수 있다!
			SETBIT nUnlockCond:ARG:0, 1
			SETBIT nUnlockCond_general, 2
		ENDIF
		IF TALENT:ARG:恋慕 && !GETBIT(nUnlockCond:ARG:0, 2) && !GETBIT(nUnlockCond_general, 5)
			PRINTFORML 에린을 유혹한 후, 이제 에이텐이의 내밀한 성소에 접근할 수 있다!
			SETBIT nUnlockCond:ARG:0, 2
			SETBIT nUnlockCond_general, 5
		ENDIF
		IF ABL:ARG:親密 >= 5 && !GETBIT(nUnlockCond:ARG:0, 3)
			PRINTFORML 에린과 친구가 된 후, %HE_SHE(ARG)%이 당신에게 비밀스러운 혼합 레시피를 공유했다!
			PRINTFORML [Lunar Incense]와 [Peerless Patriot's Elixir]가 해금되었다!
			SETBIT nUnlockCond:ARG:0, 3
		ENDIF
		IF ABL:ARG:親密 >= 5 && !GETBIT(nUnlockCond:ARG:0, 4) && !GETBIT(nUnlockCond_general, 6)
			PRINTFORML 에린과 친구가 된 후에는 대나무 숲에 함정을 설치할 수 있게 되었다!
			SETBIT nUnlockCond:ARG:0, 4
			SETBIT nUnlockCond_general, 6
		ENDIF
	CASE [[文]]
		IF ABL:ARG:親密 >= 5 && !GETBIT(nUnlockCond:ARG:0, 0) && !GETBIT(nUnlockCond_general, 1)
			PRINTFORML 아야와 친구가 되면 요괴 산 정상에 로그인할 수 있다!
			SETBIT nUnlockCond:ARG:0, 0
			SETBIT nUnlockCond_general, 1
		ENDIF
		IF ABL:ARG:親密 >= 5 && !GETBIT(nUnlockCond:ARG:0, 1) && !GETBIT(nUnlockCond_general, 3)
			PRINTFORML 아야와 친구가 된 후에는 요괴 산 정상의 노점을 열 수 있다!
			SETBIT nUnlockCond:ARG:0, 1
			SETBIT nUnlockCond_general, 3
		ENDIF
		IF ABL:ARG:親密 >= 5 && !GETBIT(nUnlockCond:ARG:0, 2) && !GETBIT(nUnlockCond_general, 7)
			PRINTFORML 아야와 친구가 된 후에는 요괴 산 정상에 함정을 설치할 수 있게 되었다!
			SETBIT nUnlockCond:ARG:0, 2
			SETBIT nUnlockCond_general, 7
		ENDIF
	CASE [[はたて]]
		IF ABL:ARG:親密 >= 5 && !GETBIT(nUnlockCond:ARG:0, 0) && !GETBIT(nUnlockCond_general, 1)
			PRINTFORML 하타테와 친구가 된 후에는 요괴 산 정상에 접속할 수 있다!
			SETBIT nUnlockCond:ARG:0, 0
			SETBIT nUnlockCond_general, 1
		ENDIF
		IF ABL:ARG:親密 >= 5 && !GETBIT(nUnlockCond:ARG:0, 1) && !GETBIT(nUnlockCond_general, 3)
			PRINTFORML 하타테와 친구가 된 후에는 요괴 산 정상에 있는 가판대를 열 수 있다!
			SETBIT nUnlockCond:ARG:0, 1
			SETBIT nUnlockCond_general, 3
		ENDIF
		IF ABL:ARG:親密 >= 5 && !GETBIT(nUnlockCond:ARG:0, 2) && !GETBIT(nUnlockCond_general, 7)
			PRINTFORML 하타테와 친해진 후에는 요괴 산 정상에 함정을 설치할 수 있게 되었다!
			SETBIT nUnlockCond:ARG:0, 2
			SETBIT nUnlockCond_general, 7
		ENDIF
	CASE [[龍]]
		IF ABL:ARG:親密 >= 5 && !GETBIT(nUnlockCond:ARG:0, 0) && !GETBIT(nUnlockCond_general, 1)
			PRINTFORML 메구무와 친구가 된 후에는 요괴산 정상에 로그인할 수 있다!
			SETBIT nUnlockCond:ARG:0, 0
			SETBIT nUnlockCond_general, 1
		ENDIF
		IF ABL:ARG:親密 >= 5 && !GETBIT(nUnlockCond:ARG:0, 1) && !GETBIT(nUnlockCond_general, 3)
			PRINTFORML 메구무와 친해지면 요괴산 정상에 있는 노점을 열 수 있다!
			SETBIT nUnlockCond:ARG:0, 1
			SETBIT nUnlockCond_general, 3
		ENDIF
		IF ABL:ARG:親密 >= 5 && !GETBIT(nUnlockCond:ARG:0, 2) && !GETBIT(nUnlockCond_general, 7)
			PRINTFORML 메구무와 친구가 된 후에는 요괴 산 정상에 함정을 설치할 수 있다!
			SETBIT nUnlockCond:ARG:0, 2
			SETBIT nUnlockCond_general, 7
		ENDIF
	CASE [[幽香]]
		IF ABL:ARG:親密 >= 5 && !GETBIT(nUnlockCond:ARG:0, 0)
			PRINTFORML 유우카와 친구가 된 후에는 햇살의 정원에서 채집할 수 있다!
			SETBIT nUnlockCond:ARG:0, 0
		ELSEIF CFLAG:ARG:弾幕勝負勝利 && !GETBIT(nUnlockCond:ARG:0, 0)
			PRINTFORML 유우카를 물리친 후에는 태양의 정원에서 채집할 수 있게 되었다!
			SETBIT nUnlockCond:ARG:0, 0
		ENDIF
	CASE [[天子]]
		IF ABL:ARG:親密 >= 5 && !GETBIT(nUnlockCond:ARG:0, 0)
			PRINTFORML 천사와 친구가 된 후에는 하늘에서 채집할 수 있다!
			SETBIT nUnlockCond:ARG:0, 0
		ELSEIF CFLAG:ARG:弾幕勝負勝利 && !GETBIT(nUnlockCond:ARG:0, 0)
			PRINTFORML 천신을 때려눕힌 후에는 하늘에서 채집할 수 있게 되었다!
			SETBIT nUnlockCond:ARG:0, 0
		ENDIF
		IF ABL:ARG:親密 >= 5 && !GETBIT(nUnlockCond:ARG:0, 1)
			PRINTFORML 천사와 친해진 후에는 하늘의 가판대를 열 수 있게 되었다!
			SETBIT nUnlockCond:ARG:0, 1
		ENDIF
	CASE [[白蓮]]
		IF ABL:ARG:親密 >= 5 && !GETBIT(nUnlockCond:ARG:0, 0)
			PRINTFORML 백련과 친구가 된 후에는 이제 %MyouTempleName()% 사찰 부지에서 가판대를 열 수 있다!
			SETBIT nUnlockCond:ARG:0, 0
		ENDIF
	CASE [[にとり]]
		IF ABL:ARG:親密 >= 5 && !GETBIT(nUnlockCond:ARG:0, 0)
			PRINTFORML 니토리와 친구가 된 후에는 겐부 협곡의 노점을 열 수 있게 되었다!
			SETBIT nUnlockCond:ARG:0, 0
		ENDIF
	CASE [[早苗]]
		IF ABL:ARG:親密 >= 5 && !GETBIT(nUnlockCond:ARG:0, 0) && !GETBIT(nUnlockCond_general, 4)
			PRINTFORML 사나에와 친구가 된 후에는 모리야 신사 부지의 가판대를 열 수 있게 되었다!
			SETBIT nUnlockCond:ARG:0, 0
			SETBIT nUnlockCond_general, 4
		ENDIF
	CASE [[神奈子]]
		IF ABL:ARG:親密 >= 5 && !GETBIT(nUnlockCond:ARG:0, 0) && !GETBIT(nUnlockCond_general, 4)
			PRINTFORML 카나코와 친구가 된 후에는 모리야 신사 경내의 가판대를 열 수 있게 되었다!
			SETBIT nUnlockCond:ARG:0, 0
			SETBIT nUnlockCond_general, 4
		ENDIF
	CASE [[諏訪子]]
		IF ABL:ARG:親密 >= 5 && !GETBIT(nUnlockCond:ARG:0, 0) && !GETBIT(nUnlockCond_general, 4)
			PRINTFORML 스와코에게 친구가 된 후에는 모리야 신사 부지의 가판대를 열 수 있게 되었다!
			SETBIT nUnlockCond:ARG:0, 0
			SETBIT nUnlockCond_general, 4
		ENDIF
	CASE [[正邪]]
		IF TALENT:ARG:恋慕 && !GETBIT(nUnlockCond:ARG:0, 0)
			PRINTFORML 세이가를 유혹한 후에는 요괴 산기슭에 있는 그녀의 은신처에 접근할 수 있다!
			SETBIT nUnlockCond:ARG:0, 0
		ENDIF
	CASE [[百々世]]
		IF ABL:ARG:親密 >= 5 && !GETBIT(nUnlockCond:ARG:0, 0)
			PRINTFORML 모모요와 친구가 된 후에는 무지개 용 동굴에서 채집할 수 있게 되었다!
			SETBIT nUnlockCond:ARG:0, 0
		ELSEIF CFLAG:ARG:弾幕勝負勝利 && !GETBIT(nUnlockCond:ARG:0, 0)
			PRINTFORML 모요를 때리고 나면 이제 무지개 용 동굴에서 채집할 수 있게 되었다!
			SETBIT nUnlockCond:ARG:0, 0
		ENDIF
		;recipes
	CASE [[阿求]]
		IF ABL:ARG:親密 >= 5 && !GETBIT(nUnlockCond:ARG:0, 0)
			PRINTFORML 아큐우와 친구가 된 후, %HE_SHE(ARG)%이 비밀스러운 혼합 레시피를 당신과 공유했다!
			PRINTFORML [오버나잇 쿠시나다]가 해금되었다!
			SETBIT nUnlockCond:ARG:0, 0
		ENDIF
	CASE [[メディスン]]
		IF ABL:ARG:親密 >= 5 && !GETBIT(nUnlockCond:ARG:0, 0)
			PRINTFORML 메디슨과 친구가 된 후, %HE_SHE(ARG)%이 당신에게 비밀 혼합 레시피를 공유했다!
			PRINTFORML [Gassing Garden]과 [Poison Body]가 해금되었다!
			SETBIT nUnlockCond:ARG:0, 0
		ENDIF
	CASE [[魔理沙]]
		IF ABL:ARG:親密 >= 5 && !GETBIT(nUnlockCond:ARG:0, 0)
			PRINTFORML 마리사와 친구가 된 후, %HE_SHE(ARG)%이 당신에게 비밀스러운 믹싱 레시피를 공유했다!
			PRINTFORML [강력한 약물]이 해금되었다!
			SETBIT nUnlockCond:ARG:0, 0
		ENDIF
	CASE [[チルノ]]
		IF ABL:ARG:親密 >= 5 && !GETBIT(nUnlockCond:ARG:0, 0)
			PRINTFORML 서리노와 친구가 된 후, %HE_SHE(ARG)%이 당신에게 비밀 혼합 레시피를 공유했다!
			PRINTFORML [눈송이 폭탄]이 해금되었다!
			SETBIT nUnlockCond:ARG:0, 0
		ENDIF
	CASE [[布都]]
		IF ABL:ARG:親密 >= 5 && !GETBIT(nUnlockCond:ARG:0, 0)
			PRINTFORML 후토와 친구가 된 후, %HE_SHE(ARG)%은 당신에게 비밀스러운 믹싱 레시피를 공유했다!
			PRINTFORML [태의의 생석]이 해금되었다!
			SETBIT nUnlockCond:ARG:0, 0
		ENDIF
	CASE [[紫]]
		IF ABL:ARG:親密 >= 5 && !GETBIT(nUnlockCond:ARG:0, 0)
			PRINTFORML 유카리와 친구가 된 후, %HE_SHE(ARG)%이 당신에게 비밀스러운 혼합 레시피를 공유했다!
			PRINTFORML [Bewitching Bait]가 해금되었다!
			SETBIT nUnlockCond:ARG:0, 0
		ENDIF
	CASE [[パチュリー]]
		IF ABL:ARG:親密 >= 5 && !GETBIT(nUnlockCond:ARG:0, 0)
			PRINTFORML 패츄리와 친구가 된 후, %HE_SHE(ARG)%이 당신에게 비밀스러운 혼합 레시피를 공유했다!
			PRINTFORML [현자의 돌]이 잠금 해제되었다!
			SETBIT nUnlockCond:ARG:0, 0
		ENDIF
	CASE [[成美]]
		IF ABL:ARG:親密 >= 5 && !GETBIT(nUnlockCond:ARG:0, 0)
			PRINTFORML 나루미와 친해진 후, %HE_SHE(ARG)%은 당신에게 비밀스러운 믹싱 레시피를 공유했다!
			PRINTFORML [Bullet Golem]이 해금되었다!
			SETBIT nUnlockCond:ARG:0, 0
		ENDIF
	CASE [[夢美]]
		IF ABL:ARG:親密 >= 5 && !GETBIT(nUnlockCond:ARG:0, 0)
			PRINTFORML 유메미와 친해진 후, %HE_SHE(ARG)%가 비밀스러운 혼합 레시피를 당신과 공유했다!
			PRINTFORML [블루캔디]가 해금되었다!
			SETBIT nUnlockCond:ARG:0, 0
		ENDIF
	CASE [[ヘカーティア]] ;addition custom code
		IF ABL:ARG:親密 >= 5 && !GETBIT(nUnlockCond:ARG:0, 0)
			PRINTFORML 헤카티아와 친구가 된 후, %HE_SHE(ARG)%은 당신에게 비밀스러운 혼합 레시피를 공유했다!
			PRINTFORML [도플갱어 드라우트]가 해금되었다!
			SETBIT nUnlockCond:ARG:0, 0
		ENDIF
ENDSELECT
;NAS custom code
CALL NAS_UnlockNotify()
RESETCOLOR
;check if the conditions still apply after reload

@EVENTLOAD
#LATER
FOR LOCAL, 1, CHARANUM-1
SELECTCASE LOCAL
	CASE [[小鈴]]
		SIF ABL:LOCAL:親密 < 5 && GETBIT(nUnlockCond:LOCAL:0, 0)
			CLEARBIT nUnlockCond:LOCAL:0, 0
		SIF ABL:LOCAL:親密 < 10 && GETBIT(nUnlockCond:LOCAL:0, 1)
			CLEARBIT nUnlockCond:LOCAL:0, 1
	CASE [[霊夢]]
		SIF ABL:LOCAL:親密 < 5 && GETBIT(nUnlockCond:LOCAL:0, 0)
			CLEARBIT nUnlockCond:LOCAL:0, 0
	CASE [[妹紅]]
		SIF ABL:LOCAL:親密 < 5 && GETBIT(nUnlockCond:LOCAL:0, 0)
			CLEARBIT nUnlockCond:LOCAL:0, 0
		SIF ABL:LOCAL:親密 < 5 && GETBIT(nUnlockCond:LOCAL:0, 1)
			CLEARBIT nUnlockCond:LOCAL:0, 1
		SIF ABL:LOCAL:親密 < 5 && GETBIT(nUnlockCond:LOCAL:0, 2)
			CLEARBIT nUnlockCond:LOCAL:0, 2
	CASE [[鈴仙]]
		SIF !TALENT:LOCAL:恋慕 && GETBIT(nUnlockCond:LOCAL:0, 0)
			CLEARBIT nUnlockCond:LOCAL:0, 0
	CASE [[てゐ]]
		SIF ABL:LOCAL:親密 < 5 && GETBIT(nUnlockCond:LOCAL:0, 0)
			CLEARBIT nUnlockCond:LOCAL:0, 0
		SIF !TALENT:LOCAL:恋慕 && GETBIT(nUnlockCond:LOCAL:0, 1)
			CLEARBIT nUnlockCond:LOCAL:0, 1
		SIF ABL:LOCAL:親密 < 5 && GETBIT(nUnlockCond:LOCAL:0, 2)
			CLEARBIT nUnlockCond:LOCAL:0, 2
	CASE [[輝夜]]
		SIF ABL:LOCAL:親密 < 5 && GETBIT(nUnlockCond:LOCAL:0, 0)
			CLEARBIT nUnlockCond:LOCAL:0, 0
		SIF ABL:LOCAL:親密 < 5 && GETBIT(nUnlockCond:LOCAL:0, 1)
			CLEARBIT nUnlockCond:LOCAL:0, 1
		SIF !TALENT:LOCAL:恋慕 && GETBIT(nUnlockCond:LOCAL:0, 2)
			CLEARBIT nUnlockCond:LOCAL:0, 2
		SIF ABL:LOCAL:親密 < 5 && GETBIT(nUnlockCond:LOCAL:0, 3)
			CLEARBIT nUnlockCond:LOCAL:0, 3
	CASE [[永琳]]
		SIF ABL:LOCAL:親密 < 5 && GETBIT(nUnlockCond:LOCAL:0, 0)
			CLEARBIT nUnlockCond:LOCAL:0, 0
		SIF ABL:LOCAL:親密 < 5 && GETBIT(nUnlockCond:LOCAL:0, 1)
			CLEARBIT nUnlockCond:LOCAL:0, 1
		SIF !TALENT:LOCAL:恋慕 && GETBIT(nUnlockCond:LOCAL:0, 2)
			CLEARBIT nUnlockCond:LOCAL:0, 2
		SIF ABL:LOCAL:親密 < 5 && GETBIT(nUnlockCond:LOCAL:0, 3)
			CLEARBIT nUnlockCond:LOCAL:0, 3
		SIF ABL:LOCAL:親密 < 5 && GETBIT(nUnlockCond:LOCAL:0, 4)
			CLEARBIT nUnlockCond:LOCAL:0, 4
	CASE [[文]]
		SIF ABL:LOCAL:親密 < 5 && GETBIT(nUnlockCond:LOCAL:0, 0)
			CLEARBIT nUnlockCond:LOCAL:0, 0
		SIF ABL:LOCAL:親密 < 5 && GETBIT(nUnlockCond:LOCAL:0, 1)
			CLEARBIT nUnlockCond:LOCAL:0, 1
		SIF ABL:LOCAL:親密 < 5 && GETBIT(nUnlockCond:LOCAL:0, 2)
			CLEARBIT nUnlockCond:LOCAL:0, 2
	CASE [[はたて]]
		SIF ABL:LOCAL:親密 < 5 && GETBIT(nUnlockCond:LOCAL:0, 0)
			CLEARBIT nUnlockCond:LOCAL:0, 0
		SIF ABL:LOCAL:親密 < 5 && GETBIT(nUnlockCond:LOCAL:0, 1)
			CLEARBIT nUnlockCond:LOCAL:0, 1
		SIF ABL:LOCAL:親密 < 5 && GETBIT(nUnlockCond:LOCAL:0, 2)
			CLEARBIT nUnlockCond:LOCAL:0, 2
	CASE [[龍]]
		SIF ABL:LOCAL:親密 < 5 && GETBIT(nUnlockCond:LOCAL:0, 0)
			CLEARBIT nUnlockCond:LOCAL:0, 0
		SIF ABL:LOCAL:親密 < 5 && GETBIT(nUnlockCond:LOCAL:0, 1)
			CLEARBIT nUnlockCond:LOCAL:0, 1
		SIF ABL:LOCAL:親密 < 5 && GETBIT(nUnlockCond:LOCAL:0, 2)
			CLEARBIT nUnlockCond:LOCAL:0, 2
	CASE [[幽香]]
		SIF ABL:LOCAL:親密 < 5 && CFLAG:LOCAL:弾幕勝負勝利 && GETBIT(nUnlockCond:LOCAL:0, 0)
			CLEARBIT nUnlockCond:LOCAL:0, 0
	CASE [[天子]]
		SIF ABL:LOCAL:親密 < 5 && CFLAG:LOCAL:弾幕勝負勝利 && GETBIT(nUnlockCond:LOCAL:0, 0)
			CLEARBIT nUnlockCond:LOCAL:0, 0
		SIF ABL:LOCAL:親密 < 5 && GETBIT(nUnlockCond:LOCAL:0, 1)
			CLEARBIT nUnlockCond:LOCAL:0, 1
	CASE [[白蓮]]
		SIF ABL:LOCAL:親密 < 5 && GETBIT(nUnlockCond:LOCAL:0, 0)
			CLEARBIT nUnlockCond:LOCAL:0, 0
	CASE [[にとり]]
		SIF ABL:LOCAL:親密 < 5 && GETBIT(nUnlockCond:LOCAL:0, 0)
			CLEARBIT nUnlockCond:LOCAL:0, 0
	CASE [[早苗]]
		SIF ABL:LOCAL:親密 < 5 && GETBIT(nUnlockCond:LOCAL:0, 0)
			CLEARBIT nUnlockCond:LOCAL:0, 0
	CASE [[神奈子]]
		SIF ABL:LOCAL:親密 < 5 && GETBIT(nUnlockCond:LOCAL:0, 0)
			CLEARBIT nUnlockCond:LOCAL:0, 0
	CASE [[諏訪子]]
		SIF ABL:LOCAL:親密 < 5 && GETBIT(nUnlockCond:LOCAL:0, 0)
			CLEARBIT nUnlockCond:LOCAL:0, 0
	CASE [[正邪]]
		SIF !TALENT:LOCAL:恋慕 && GETBIT(nUnlockCond:LOCAL:0, 0)
			CLEARBIT nUnlockCond:LOCAL:0, 0
	CASE [[百々世]]
		SIF ABL:LOCAL:親密 < 5 && CFLAG:LOCAL:弾幕勝負勝利 && GETBIT(nUnlockCond:LOCAL:0, 0)
			CLEARBIT nUnlockCond:LOCAL:0, 0
	CASE [[阿求]]
		SIF ABL:LOCAL:親密 < 5 && GETBIT(nUnlockCond:LOCAL:0, 0)
			CLEARBIT nUnlockCond:LOCAL:0, 0
	CASE [[メディスン]]
		SIF ABL:LOCAL:親密 < 5 && GETBIT(nUnlockCond:LOCAL:0, 0)
			CLEARBIT nUnlockCond:LOCAL:0, 0
	CASE [[魔理沙]]
		SIF ABL:LOCAL:親密 < 5 && GETBIT(nUnlockCond:LOCAL:0, 0)
			CLEARBIT nUnlockCond:LOCAL:0, 0
	CASE [[チルノ]]
		SIF ABL:LOCAL:親密 < 5 && GETBIT(nUnlockCond:LOCAL:0, 0)
			CLEARBIT nUnlockCond:LOCAL:0, 0
	CASE [[布都]]
		SIF ABL:LOCAL:親密 < 5 && GETBIT(nUnlockCond:LOCAL:0, 0)
			CLEARBIT nUnlockCond:LOCAL:0, 0
	CASE [[紫]]
		SIF ABL:LOCAL:親密 < 5 && GETBIT(nUnlockCond:LOCAL:0, 0)
			CLEARBIT nUnlockCond:LOCAL:0, 0
	CASE [[パチュリー]]
		SIF ABL:LOCAL:親密 < 5 && GETBIT(nUnlockCond:LOCAL:0, 0)
			CLEARBIT nUnlockCond:LOCAL:0, 0
	CASE [[成美]]
		SIF ABL:LOCAL:親密 < 5 && GETBIT(nUnlockCond:LOCAL:0, 0)
			CLEARBIT nUnlockCond:LOCAL:0, 0
	CASE [[夢美]]
		SIF ABL:LOCAL:親密 < 5 && GETBIT(nUnlockCond:LOCAL:0, 0)
			CLEARBIT nUnlockCond:LOCAL:0, 0
ENDSELECT
NEXT
SIF ABL:[[妹紅]]:親密 < 5 && ABL:[[てゐ]]:親密 < 5 && ABL:[[輝夜]]:親密 < 5 && ABL:[[永琳]]:親密 < 5 && GETBIT(nUnlockCond_general, 0)
	CLEARBIT nUnlockCond_general, 0
SIF ABL:[[文]]:親密 < 5 && ABL:[[はたて]]:親密 < 5 && ABL:[[龍]]:親密 < 5 && GETBIT(nUnlockCond_general, 1)
	CLEARBIT nUnlockCond_general, 1
SIF ABL:[[輝夜]]:親密 < 5 && ABL:[[永琳]]:親密 < 5 && GETBIT(nUnlockCond_general, 2)
	CLEARBIT nUnlockCond_general, 2
SIF ABL:[[文]]:親密 < 5 && ABL:[[はたて]]:親密 < 5 && ABL:[[龍]]:親密 < 5 && GETBIT(nUnlockCond_general, 3)
	CLEARBIT nUnlockCond_general, 3
SIF ABL:[[早苗]]:親密 < 5 && ABL:[[神奈子]]:親密 < 5 && ABL:[[諏訪子]]:親密 < 5 && GETBIT(nUnlockCond_general, 4)
	CLEARBIT nUnlockCond_general, 4
SIF !TALENT:[[てゐ]]:恋慕 && !TALENT:[[輝夜]]:恋慕 && !TALENT:[[永琳]]:恋慕 && GETBIT(nUnlockCond_general, 5)
	CLEARBIT nUnlockCond_general, 5
SIF ABL:[[妹紅]]:親密 < 5 && ABL:[[てゐ]]:親密 < 5 && ABL:[[輝夜]]:親密 < 5 && ABL:[[永琳]]:親密 < 5 && GETBIT(nUnlockCond_general, 6)
	CLEARBIT nUnlockCond_general, 6
SIF ABL:[[文]]:親密 < 5 && ABL:[[はたて]]:親密 < 5 && ABL:[[龍]]:親密 < 5 && GETBIT(nUnlockCond_general, 7)
	CLEARBIT nUnlockCond_general, 7
