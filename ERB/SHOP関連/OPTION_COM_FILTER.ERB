; Modular ERB: OPTION_COM_FILTER.ERB
;FILTER用のCOMNAME取得関数
;とりあえず0～599の範囲で使って下さい

@GET_COMNAME(ID_COM)
#FUNCTIONS
#DIM ID_COM
;派生コマンド用
SIF INRANGE(ID_COM, 500, 599)
	RETURNF STR_TR(1000 + ID_COM)
SIF INRANGE(ID_COM, 0, 499)
	RETURNF TRAINNAME_TR(ID_COM)
THROW 不正なCOM番号が渡されました {ID_COM}

@QUICKFILTER(ID_COM)
#FUNCTION
#DIM ID_COM
#DIM CONST BIT_Ｖ系       = 1
#DIM CONST BIT_Ａ系       = 2
#DIM CONST BIT_奉仕系     = 5
#DIM CONST BIT_SM系       = 6
#DIM CONST BIT_セクハラ系 = 7
#DIM CONST BIT_家事系     = 9
#DIM CONST BIT_U     = 10
#DIM CONST BIT_ES     = 11
#DIM CONST BIT_ESPER     = 12
#DIM CONST BIT_自慰系     = 13  ;たぶんつかってない
#DIM CONST BIT_HYPNOSIS     = 14
#DIM CONST BIT_HOSTILE     = 15
SIF IS_COM_VAGINA(ID_COM)   && !CFLAG:TARGET:Ｖアンフィルタ
	RETURNF 0
SIF IS_COM_ANAL(ID_COM)     && !CFLAG:TARGET:Ａアンフィルタ
	RETURNF 0
SIF IS_COM_HOUSI(ID_COM)    && !CFLAG:TARGET:奉仕アンフィルタ
	RETURNF 0
SIF IS_COM_SM(ID_COM)       && !CFLAG:TARGET:ＳＭアンフィルタ
	RETURNF 0
SIF IS_COM_KAJI(ID_COM)     && !FLAG:家事アンフィルタ
	RETURNF 0
SIF IS_COM_SEKUHARA(ID_COM) && !CFLAG:TARGET:セクハラアンフィルタ && (!FLAG:70 || CFLAG:TARGET:344 == 1) && TARGET ;qol custom code, allow changing clothes if no target is present
	RETURNF 0
SIF IS_COM_ONANISM(ID_COM)  && !ExtraFilters:TARGET:3
	RETURNF 0
SIF IS_COM_KOJO(ID_COM) && CFLAG:TARGET:allowKojoCom ;qol custom code, inverted check
	RETURNF 0
;com custom code
SIF IS_COM_REVSERVICE(ID_COM)  && !CFLAG:TARGET:RevServiceFilter
	RETURNF 0
SIF IS_COM_REVSEX(ID_COM)  && !CFLAG:TARGET:RevSexFilter
	RETURNF 0
RETURNF 1

@フィルタ表示
;Act_COMのラインと合体
;Custom code, change filters depending on free use activation.
#DIM DYNAMIC ShowSex
IF nPagedCommandMenu
	CALL COM_FILTER_NEW_COM
	RETURN
ENDIF
SIF (TARGET != MASTER && FREE_USE_ACTIVE()) || CFLAG:うふふ || CFLAG:MASTER:イタズラ == 2
	ShowSex = 1
SELECTCASE CFLAG:オート喘ぎ
	CASE 0
		LOCALS = Auto-moan: No
	CASE 1
		LOCALS = Auto-moan: Yes
	CASE 2
		LOCALS = Auto-moan: Yes (Ahegao)
ENDSELECT
;Custom code.
PRINTFORM \@ ShowSex ? %"=" * 2% # %"=" * 6% \@의 행동은
SIF STALKED == TARGET && STALKED
	SETCOLOR C_YELLOW
PRINTBUTTON "【★】", TARGET + 4000
RESETCOLOR
;NAS sex knowledge tests
IF charaKnowsSex(MASTER) && ShowSex
	CALL PRINTBUTTON_EX("[V]",831,!CFLAG:TARGET:Ｖアンフィルタ)
	CALL STRAIGHT_BAR(1,C_GRAY,"-")
	CALL PRINTBUTTON_EX("[A]",832,!CFLAG:TARGET:Ａアンフィルタ)
	CALL STRAIGHT_BAR(1,C_GRAY,"-")
	IF nPee && TALENT:MASTER:SexKnow >= 5
		;NAS sex knowledge tests
		CALL PRINTBUTTON_EX("[U]",833,!CFLAG:TARGET:UFilter)
		CALL STRAIGHT_BAR(1,C_GRAY,"-")
	ENDIF
	;com custom code
	CALL PRINTBUTTON_EX("[Rev Sex]",-9121,!CFLAG:TARGET:RevSexFilter)
	CALL STRAIGHT_BAR(1,C_GRAY,"-")
	CALL PRINTBUTTON_EX("[SM]",836,!CFLAG:TARGET:ＳＭアンフィルタ)
	CALL STRAIGHT_BAR(1,C_GRAY,"-")
	IF ITEM:EStimBox && charaKnowsSex(MASTER)
		CALL PRINTBUTTON_EX("[E-Stim]",834,!CFLAG:TARGET:EStimFilter)
		CALL STRAIGHT_BAR(1,C_GRAY,"-")
	ENDIF
	IF TALENT:MASTER:SexKnow >= 2 && nHypno
		CALL PRINTBUTTON_EX("[Hyp]",852,!ExtraFilters:TARGET:1)
		CALL STRAIGHT_BAR(1,C_GRAY,"-")
	ENDIF
	CALL PRINTBUTTON_EX("[Service]",835,!CFLAG:TARGET:奉仕アンフィルタ)
	CALL STRAIGHT_BAR(1,C_GRAY,"-")
	;com custom code
	CALL PRINTBUTTON_EX("[Rev Ser.]",-9120,!CFLAG:TARGET:RevServiceFilter)
	CALL STRAIGHT_BAR(1,C_GRAY,"-")
ENDIF
CALL PRINTBUTTON_EX("[Masturb.]",854,!ExtraFilters:TARGET:3)
CALL STRAIGHT_BAR(1,C_GRAY,"-")
CALL PRINTBUTTON_EX("[Petting]",837,!CFLAG:TARGET:セクハラアンフィルタ)
CALL STRAIGHT_BAR(1,C_GRAY,"-")
IF nRyona
	CALL PRINTBUTTON_EX("[Hostile]",853,!ExtraFilters:TARGET:2)
	CALL STRAIGHT_BAR(1,C_GRAY,"-")
ENDIF
CALL PRINTBUTTON_EX("[Housekeep]",840,!FLAG:家事アンフィルタ)
CALL STRAIGHT_BAR(1,C_GRAY,"-")
CALL PRINTBUTTON_EX("[Dial.]",839,!CFLAG:TARGET:allowKojoCom)
CALL STRAIGHT_BAR(1,C_GRAY,"-")
;;IF charaKnowsSex(MASTER)
;;NAS sex knowledge tests
;qol custom code, moved to command buttons
;CALL PRINTBUTTON_EX("[Auto. Condom]",838,!CFLAG:自動ゴム)
;CALL STRAIGHT_BAR(1,C_GRAY,"-")
;ENDIF
IF TARGET != MASTER
	;qol custom code, moved to command buttons
	;CALL PRINTBUTTON_EX(@"[%LOCALS%]",4000,!CFLAG:オート喘ぎ)
	;custom code, commented out
	;PRINTFORM %"=" * (38 - STRLENS(LOCALS))%
ELSE
	;PRINTFORM %"=" * 40%
ENDIF
;custom code, automatically scale line for resolution
HTML_POPPRINTINGSTR
HTML_PRINT RESULTS + @"<font color='#%TO_HEX(C_GRAY)%'>%"━" * MAX((MAXWIDTH() - HTML_STRINGLEN(RESULTS))/2, 0)%</font>"
;文字列変数に保存してみた

@OPTION_COMMAND_FILTER
#LOCALSIZE 600
#DIM ID_COM
CALL LOAD_COM_FILTER(LOCAL)
DRAWLINE
FOR ID_COM ,0, 600
SIF !STRLENS(GET_COMNAME(ID_COM))
	CONTINUE
CALL PRINTBUTTON_EXC(@"%GET_COMNAME(ID_COM)%[{ID_COM,3}]", ID_COM, STRCOUNT(SAVESTR:0, @"/{ID_COM}/"))
NEXT
PRINTL 
DRAWLINE
PRINTFORML [1101] - V 필터     [1102] - A 필터     [1107] - U 필터     [1103] - 서비스 필터 
PRINTFORML [1104] - SM 필터   [1108] - ES 필터   [1105] - 애정 필터 [1106] - 가구 필터  
;com custom code
PRINTFORML [1109] - 역 서비스 필터 [1110] - 역 성 필터
PRINTFORML [1111] - 모든 필터
PRINTFORML [2000] - 하위 명령어 필터
PRINTFORML [3000] - 필터 세트 저장 [3001] - 필터 세트 불러오기
PRINTFORML [1000] - 완료
PRINTL  
PRINTFORML [9999] - 초기화
PRINTL  
INPUT
SELECTCASE RESULT
	CASE 1000
		RETURN 0
	CASE 9999
		SAVESTR:0 =
	CASE 3000
		CALL SAVE_GLOBAL_COM_FILTER
	CASE 3001
		CALL LOAD_GLOBAL_COM_FILTER
	CASE 2000
		CALL SETUP_USERCOM_FILTER
	CASE 1101
		CALL SET_FILTER_NG_VAGINA
	CASE 1102
		CALL SET_FILTER_NG_ANAL
	CASE 1103
		CALL SET_FILTER_NG_HOUSI
	CASE 1104
		CALL SET_FILTER_NG_SM
	CASE 1105
		CALL SET_FILTER_NG_SEKUHARA
	CASE 1106
		CALL SET_FILTER_NG_KAJI
	CASE 1107
		CALL SET_FILTER_NG_URETHRA
	CASE 1108
		CALL SET_FILTER_NG_ES
	CASE 1111
		CALL SET_FILTER_NG_ALL
		;com custom code
	CASE 1109
		CALL SET_FILTER_NG_REVSERVICE
	CASE 1110
		CALL SET_FILTER_NG_REVSEX
	CASEELSE
		IF STRLENS(GET_COMNAME(RESULT))
			LOCAL:RESULT = !LOCAL:RESULT
			IF LOCAL:RESULT
				CALL ADD_COM_FILTER(RESULT)
			ELSE
				CALL DEL_COM_FILTER(RESULT)
				;SAVE_COM_FILTER(LOCAL)
			ENDIF
		ENDIF
ENDSELECT
RESTART
;フィルターにNGなCOM追加する関数

@ADD_COM_FILTER(NG_ID)
#DIM NG_ID
SIF SAVESTR:0 == ""
	SAVESTR:0 = /
SIF STRCOUNT(SAVESTR:0, @"/{NG_ID}/")
	RETURN
SAVESTR:0 += @"{NG_ID}/"
;フィルターからNGなCOM削除する関数
;正規表現とかろくに触ったことないんだがこれで動くん？

@DEL_COM_FILTER(NG_ID)
#DIM NG_ID
SIF !STRCOUNT(SAVESTR:0, @"/{NG_ID}/")
	RETURN
SAVESTR:0 '= REPLACE(SAVESTR:0, @"%ESCAPE("/")%{NG_ID}%ESCAPE("/")%", ESCAPE("/"))
;REF使用COM_FILTER保存関数
;配列→文字列
;配列の要素数が600未満だと怒られる

@SAVE_COM_FILTER(NG_LIST)
#DIM REF NG_LIST
#DIM ID_COM
SAVESTR:0 = /
FOR ID_COM, 0, 600
SIF !NG_LIST:ID_COM
	CONTINUE
SIF STRCOUNT(SAVESTR:0, @"/{ID_COM}/")
	CONTINUE
SAVESTR:0 += @"{ID_COM}/"
NEXT
;#;PRINTFORML SAVE_COM_FILTER：%SAVESTR:0%
;REF使用COM_FILTER読込関数
;文字列→配列
;配列の要素数が600未満だと怒られる

@LOAD_COM_FILTER(NG_LIST)
#DIM REF NG_LIST
#DIM ID_COM
VARSET NG_LIST
IF SAVESTR:0 == ""
	SAVESTR:0 = /
ELSE
	FOR ID_COM, 0, 600
	SIF STRCOUNT(SAVESTR:0, @"/{ID_COM}/")
		NG_LIST:ID_COM = 1
	NEXT
ENDIF
;#;PRINTFORML LOAD_COM_FILTER：%SAVESTR:0%
;NG文字列をGLOBALに保存

@SAVE_GLOBAL_COM_FILTER
LOADGLOBAL
FOR LOCAL, 0, 3
PRINTFORM [{LOCAL}] - 
IF STRLENS(GLOBALS:(LOCAL * 2 + 1))
	PRINTFORML 【%GLOBALS:(LOCAL * 2 + 1)%】
ELSE
	PRINTL ----------
ENDIF
NEXT
PRINTL [100] - 반환
INPUT
DO
LOCAL = RESULT
SIF RESULT == 100
	RETURN
SIF !INRANGE(RESULT, 0, 2)
	CONTINUE
SIF STRLENS(GLOBALS:(RESULT * 2 + 1))
	PRINTFORML 【%GLOBALS:(RESULT * 2 + 1)%】는 덮어쓰여질 것이다.
PRINTL 이 명령어 필터에 이름을 입력해 주세요.
INPUTS
GLOBALS:(LOCAL * 2 + 1) = %RESULTS%
GLOBALS:(LOCAL * 2) = %SAVESTR:0%
PRINTFORMW 명령 필터【%GLOBALS:(LOCAL * 2 + 1)%】가 저장되었다.
SAVEGLOBAL
RETURN
LOOP 1
;NG文字列をGLOBALから読込

@LOAD_GLOBAL_COM_FILTER
LOADGLOBAL
FOR LOCAL, 0, 3
PRINTFORM [{LOCAL}] - 
IF STRLENS(GLOBALS:(LOCAL * 2 + 1))
	PRINTFORML 【%GLOBALS:(LOCAL * 2 + 1)%】
ELSE
	PRINTL ----------
ENDIF
NEXT
PRINTL [100] - 반환
INPUT
DO
SIF RESULT == 100
	RETURN
SIF !INRANGE(RESULT, 0, 2)
	CONTINUE
PRINTFORMW 명령 필터【%GLOBALS:(RESULT * 2 + 1)%】가 로드되었다.
SAVESTR:0 = %GLOBALS:(RESULT * 2)%
RETURN
LOOP 1
;ぶっちゃけ使わない気がする

@SETUP_USERCOM_FILTER
CALL PRINTBUTTON_EXC("Log Display[800]",             800, GETBIT(FLAG:USERCOMフィルタ, 12))
CALL PRINTBUTTON_EXC("Dirtiness[801]",             801, GETBIT(FLAG:USERCOMフィルタ,  0))
CALL PRINTBUTTON_EXC("Change Target[802]",         802, GETBIT(FLAG:USERCOMフィルタ,  1))
CALL PRINTBUTTON_EXC("Abilities[803]",             803, GETBIT(FLAG:USERCOMフィルタ,  2))
CALL PRINTBUTTON_EXC("Abilities（Master）[804]", 804, GETBIT(FLAG:USERCOMフィルタ,  3))
CALL PRINTBUTTON_EXC("Item List[805]", 805, GETBIT(FLAG:USERCOMフィルタ,  4))
SIF MAXBASE:MASTER:TSP
	CALL PRINTBUTTON_EXC("Auto-TSP Movement[995]", 995, GETBIT(FLAG:USERCOMフィルタ,  13))
;自動掃除
CALL PRINTBUTTON_EXC("Auto-Clean[996]", 996, GETBIT(FLAG:USERCOMフィルタ,  14))
CALL PRINTBUTTON_EXC("Command Filter[809]",     809, GETBIT(FLAG:USERCOMフィルタ, 11))
;com custom code (bit limit 63)
CALL PRINTBUTTON_EXC("Pilfer Panties[821]",     821, GETBIT(FLAG:USERCOMフィルタ, 60))
CALL PRINTBUTTON_EXC("Take Panties[825]",     825, GETBIT(FLAG:USERCOMフィルタ, 61))
PRINTL 
PRINTC 리턴 [0]
INPUT
SELECTCASE RESULT
	CASE 0
		RETURN
	CASE 801
		INVERTBIT FLAG:USERCOMフィルタ, 0
	CASE 802
		INVERTBIT FLAG:USERCOMフィルタ, 1
	CASE 803
		INVERTBIT FLAG:USERCOMフィルタ, 2
	CASE 804
		INVERTBIT FLAG:USERCOMフィルタ, 3
	CASE 805
		INVERTBIT FLAG:USERCOMフィルタ, 4
	CASE 809
		INVERTBIT FLAG:USERCOMフィルタ, 11
		IF GETBIT (FLAG:USERCOMフィルタ, 11)
			PRINTW 명령 필터에 다시 접근하려면 옵션으로 이동한다.
			CLEARLINE 1
		ENDIF
	CASE 800
		INVERTBIT FLAG:USERCOMフィルタ, 12
	CASE 995
		INVERTBIT FLAG:USERCOMフィルタ, 13
		;自動掃除
	CASE 996
		INVERTBIT FLAG:USERCOMフィルタ, 14
		;com custom code
	CASE 821
		INVERTBIT FLAG:USERCOMフィルタ, 60
	CASE 825
		INVERTBIT FLAG:USERCOMフィルタ, 61
ENDSELECT
CLEARLINE 3
RESTART
;V使用コマンド判定
;FLAG:1013, 1

@IS_COM_VAGINA(ID_COM)
#FUNCTION
#DIM ID_COM
SIF GROUPMATCH(ID_COM, 3, 44, 60, 61, 65, 67, 68, 71, 122, 141, 143, 185, 316, 369, 503, 504, 506, 507, 513)
	RETURNF 1
RETURNF 0

@SET_FILTER_NG_VAGINA
#DIM ID_COM
FOR ID_COM, 0, 600
;V
SIF !IS_COM_VAGINA(ID_COM)
	CONTINUE
CALL ADD_COM_FILTER(ID_COM)
NEXT
;A使用コマンド判定
;FLAG:1013, 2

@IS_COM_ANAL(ID_COM)
#FUNCTION
#DIM ID_COM
SIF GROUPMATCH(ID_COM, 4, 5, 45, 62, 63, 66, 69, 70, 314, 142, 143, 146, 147, 148, 314, 369, 514, 515, 90, 127)
	RETURNF 1
RETURNF 0

@SET_FILTER_NG_ANAL
#DIM ID_COM
FOR ID_COM, 0, 600
;A
SIF !IS_COM_ANAL(ID_COM)
	CONTINUE
CALL ADD_COM_FILTER(ID_COM)
NEXT
;奉仕コマンド判定
;FLAG:1013, 5

@IS_COM_HOUSI(ID_COM)
#FUNCTION
#DIM ID_COM
;NAS services filters commands servicing the touhou
RETURNF NAS_COM_SERVICE(ID_COM)
SIF INRANGE(ID_COM, 80, 85) || GROUPMATCH(ID_COM, 120, 121, 140, 501, 505, 508, 509, 510, 511, 512)
	RETURNF 1
SIF GROUPMATCH(ID_COM, 99) ;com custom code, modded actions: onaholejob
	RETURNF 1
RETURNF 0

@SET_FILTER_NG_HOUSI
#DIM ID_COM
FOR ID_COM, 0, 600
;HOUSI
SIF !IS_COM_HOUSI(ID_COM)
	CONTINUE
CALL ADD_COM_FILTER(ID_COM)
NEXT
;SMコマンド判定
;FLAG:1013, 6

@IS_COM_SM(ID_COM)
#FUNCTION
#DIM ID_COM
SIF INRANGE(ID_COM, 100, 119) || INRANGE(ID_COM, 140, 159)
	RETURNF 1
RETURNF 0

@SET_FILTER_NG_SM
#DIM ID_COM
FOR ID_COM, 0, 600
;SM
SIF !IS_COM_SM(ID_COM)
	CONTINUE
CALL ADD_COM_FILTER(ID_COM)
NEXT
;セクハラコマンド判定
;FLAG:1013, 7

@IS_COM_SEKUHARA(ID_COM)
#FUNCTION
#DIM ID_COM
SIF INRANGE(ID_COM, 305, 308) || INRANGE(ID_COM, 310, 316) || INRANGE(ID_COM, 560, 563) || GROUPMATCH(ID_COM, 330, 350, 352) ;NAS: Removed 429
	RETURNF 1
RETURNF 0

@SET_FILTER_NG_SEKUHARA
#DIM ID_COM
FOR ID_COM, 0, 600
;セクハラ
SIF !IS_COM_SEKUHARA(ID_COM)
	CONTINUE
CALL ADD_COM_FILTER(ID_COM)
NEXT
;家事コマンド判定
;FLAG:1013, 9

@IS_COM_KAJI(ID_COM)
#FUNCTION
#DIM ID_COM
SIF INRANGE(ID_COM, 410, 419) || GROUPMATCH(ID_COM, 403, 450)
	RETURNF 1
RETURNF 0
;kojo custom com

@IS_COM_KOJO(ID_COM)
#FUNCTION
#DIM ID_COM
SIF INRANGE(ID_COM, 270, 299)
	RETURNF 1
RETURNF 0

@SET_FILTER_NG_KAJI
#DIM ID_COM
FOR ID_COM, 0, 600
SIF !IS_COM_KAJI(ID_COM)
	CONTINUE
CALL ADD_COM_FILTER(ID_COM)
NEXT
;自慰コマンド判定
;FLAG:1013, 13

@IS_COM_ONANISM(ID_COM)
#FUNCTION
#DIM ID_COM
SIF INRANGE(ID_COM, 700, 712) || INRANGE(ID_COM, 740, 752) || GROUPMATCH(ID_COM, 451, 460, 433)
	RETURNF 1
RETURNF 0

@SET_FILTER_NG_ONANISM
#DIM ID_COM
FOR ID_COM, 0, 800
SIF !IS_COM_ONANISM(ID_COM)
	CONTINUE
CALL ADD_COM_FILTER(ID_COM)
NEXT
;ＡＬＬコマンド判定

@SET_FILTER_NG_ALL
#DIM ID_COM
FOR ID_COM, 0, 600
CALL ADD_COM_FILTER(ID_COM)
NEXT
