;FileName_催眠発動.ERB ------------------------------ Rev1.00
;催眠発動
;CALL		USER
;ARG		VOID
;RETURN		VOID
;COMMENT	
;-----------------------------------------------------------

;NAS: Completely replaced by the Esper system
@催眠発動
#DIM 催眠タイプ
#DIM 催眠判定
#DIM 催眠結果

;I made enough changes to this that I decided to make it its own function
CALL 催眠発動_TR
RETURN 0

;IF FLAG:催眠強度
;	FLAG:催眠強度 = 0
;	PRINTW あなたは広域催眠を解除した
;ELSE
	PRINTL Select the type of hypnosis.
;	SIF GET_TARGETNUM() > 1 || !TARGET
	SIF !TARGET || FLAG:70 || BASE:MASTER:TSP < 500
		SETCOLOR C_L_GRAY
	PRINTL [0] - Suppress Hostility (TSP500 - Applied immediately)
	RESETCOLOR

;	SIF GET_TARGETNUM() > 1 || TALENT:合意誤認 || !TARGET
	SIF TALENT:合意誤認 || !TARGET  || FLAG:70 || BASE:MASTER:TSP < 500
		SETCOLOR C_L_GRAY
	PRINTL [1] - Fake Consent (TSP500 - Today only)
	RESETCOLOR

;	SIF GET_TARGETNUM() > 1 || !TARGET
	SIF !TARGET || FLAG:70 || BASE:MASTER:TSP < 500
		SETCOLOR C_L_GRAY
	IF TCVAR:発情 == 1
		PRINTL [2] - Sexual Inhibition (TSP500 - Reversible)
	ELSE
		PRINTL [2] - Sexual Excitation (TSP500 - Reversible)
	ENDIF
	RESETCOLOR

	IF FLAG:催眠強度
		PRINTL [3] - Return Common Sense to Normal (No Consumption - Wide area)
	ELSE
		SIF BASE:MASTER:TSP < 1000 || FLAG:70 
			SETCOLOR C_L_GRAY
		PRINTL [3] - Common Sense Modification (TSP1000 - Wide area)
	ENDIF
	RESETCOLOR
	PRINTL [9] - Back
	
	$HYPNO_SELECT
	INPUT
;	IF (GROUPMATCH(RESULT, 0, 2) && GET_TARGETNUM() == 1) || (RESULT == 1 && TALENT:合意誤認 == 0 && GET_TARGETNUM() == 1) || (RESULT == 3 && (FLAG:催眠強度 > 0 || BASE:MASTER:TSP >= 1000))
	IF (GROUPMATCH(RESULT, 0, 2) && BASE:MASTER:TSP >= 500 && TARGET) || (RESULT == 1 && TARGET && BASE:MASTER:TSP >= 500 && TALENT:合意誤認 == 0) || (RESULT == 3 && (FLAG:催眠強度 > 0 || BASE:MASTER:TSP >= 1000))
		催眠タイプ = RESULT + 1
		
		IF RESULT == 3
			IF FLAG:催眠強度 == 0
				BASE:MASTER:TSP -= 1000
				
				LOCAL:0 = 1 + RAND:100
				LOCAL:1 = 0
				
				FOR LOCAL:2,1,GET_TARGETNUM() + 1
					LOCAL:1 += 1 + MARK:(TARGET:(LOCAL:2)):反発刻印 * 2
					REPEAT MAX(6 - GET_TARGETNUM() * 2 - GET_TARGETNUM() == 1, 1)
						LOCAL:1 += RAND:(1 + TALENT:(TARGET:(LOCAL:2)):禁断の知識 + ABL:(TARGET:(LOCAL:2)):教養)
						SIF LOCAL:2 == 1 && GET_TARGETNUM() == 3
							LOCAL:1 += RAND:(1 + TALENT:(TARGET:(LOCAL:2)):禁断の知識 + ABL:(TARGET:(LOCAL:2)):教養)
						SIF TALENT:(TARGET:(LOCAL:2)):神霊
							LOCAL:1 += 1
						;蓬莱人
						SIF TALENT:(TARGET:(LOCAL:2)):追加種族 == 4
							LOCAL:1 += 1
						SIF LOCAL:1 > 1 && CFLAG:(TARGET:(LOCAL:2)):うふふ
							LOCAL:1 -= 1
						SIF LOCAL:1 > 1 && TALENT:(TARGET:(LOCAL:2)):恋慕 && RAND:3
							LOCAL:1 -= 1
						SIF LOCAL:1 > 1 && TALENT:(TARGET:(LOCAL:2)):愛欲 && RAND:3
							LOCAL:1 -= 1
						SIF LOCAL:1 > 1 && TALENT:(TARGET:(LOCAL:2)):ヤリマン && RAND:3
							LOCAL:1 -= 1
						SIF LOCAL:1 > 1 && ABL:(TARGET:(LOCAL:2)):親密 >= 5 && RAND:3
							LOCAL:1 -= 1
						SIF LOCAL:1 > 1 && ABL:(TARGET:(LOCAL:2)):従順 >= 5 && RAND:3
							LOCAL:1 -= 1
						SIF LOCAL:1 > 1 && ABL:(TARGET:(LOCAL:2)):欲望 >= 5 && RAND:3
							LOCAL:1 -= 1
						SIF LOCAL:1 > 1 && TALENT:MASTER:禁断の知識 && RAND:3
							LOCAL:1 -= 1
					REND
				NEXT
				
				LOCAL:2 = LOCAL:0 - LOCAL:1
				
				PRINTFORM %CALLNAME:MASTER%'s Common Sense Modification Strength：
				PRINTFORM 1D100 - {LOCAL:1} = {LOCAL:0} - {LOCAL:1} = 
				SIF LOCAL:0 >= 5 && LOCAL:2 > 0
					FONTBOLD
				PRINTFORM {LOCAL:2}
				FONTREGULAR
				
				IF LOCAL:0 >= 5 && LOCAL:2 > 0
					IF LOCAL:0 > 95
						FONTBOLD
						SETCOLOR C_AQUA
						PRINTFORMW   Critical Success! （PWR＋{LOCAL:1}）
						RESETCOLOR
						FONTREGULAR
						FLAG:催眠強度 = LOCAL:0
						催眠結果 = 2
					ELSE
						PRINTW 
						FLAG:催眠強度 = LOCAL:2
						催眠結果 = 1
					ENDIF
					
					PRINTL 
					
					CALL 催眠発動の呪文(RESULT)
					
					PRINTL 
					
					IF 顔絵付きメッセージ
						FOR LOCAL,1,GET_TARGETNUM() + 1
							キャラ画像表示サイズ比 = 100
							CALL GET_BASE_RESOURCE(TARGET:LOCAL, "顔絵", , "発情")
							SIF FLAG:マルチサイズ
								デフォルトキャラ画像横幅 = SPRITEHEIGHT(RESULTS)
							CALL IMAGE_SET(RESULTS, 0, 0, キャラ画像表示サイズ比, 0)
							CALL SIMULTANEOUS_IMAGE_DISPLAY(0, 0, 1, -1)
							CALL KOJO_MESSAGE_SEND("EVENT",666,TARGET:LOCAL,催眠タイプ,催眠結果)
							IF RESULT > 0
								PRINTL 
							ELSE
								PRINTW 
							ENDIF
						NEXT
					ELSE
						FOR LOCAL,1,GET_TARGETNUM() + 1
							CALL KOJO_MESSAGE_SEND("EVENT",666,TARGET:LOCAL,催眠タイプ,催眠結果)
						NEXT
					ENDIF
					
;					PRINTW ……催眠解除。
				ELSE
					IF LOCAL:0 < 5
						FONTBOLD
						SETCOLOR 255,0,0
						PRINTW   Critical Failure! 
						RESETCOLOR
						FONTREGULAR
						
						PRINTL 
						PRINTW ...Hypnosis failed.
						
						Qol_Info_Count_Array:MASTER:Qol_Count_HypnosisFail ++ ;qol custom code
						
						FLAG:催眠強度 = 0
						催眠結果 = -2
						FOR LOCAL,1,GET_TARGETNUM() + 1
							CALL KOJO_MESSAGE_SEND("EVENT",666,TARGET:LOCAL,催眠タイプ,催眠結果)
							BASE:(TARGET:LOCAL):怒り += 200
						NEXT
					ELSE
						IF LOCAL:2 == 0
							PRINT  = 
						ELSE
							PRINT  < 
						ENDIF
						FONTBOLD
						PRINTW 0
						FONTREGULAR
						
						PRINTL 
						PRINTW ...Hypnosis failed.
						
						FLAG:催眠強度 = 0
						催眠結果 = -1
						FOR LOCAL,1,GET_TARGETNUM() + 1
							CALL KOJO_MESSAGE_SEND("EVENT",666,TARGET:LOCAL,催眠タイプ,催眠結果)
							BASE:(TARGET:LOCAL):怒り += 100
						NEXT
					ENDIF
				ENDIF
				FOR LOCAL,1,GET_TARGETNUM() + 1 ;qol custom code
					Qol_Info_Count_Array:LOCAL:Qol_Count_CommonSense ++
				NEXT
				Qol_Info_Count_Array:MASTER:Qol_Count_Hypnosis ++
				
				TFLAG:Info表示中 = 0
				RETURN 0
			ELSEIF FLAG:催眠強度
				FLAG:催眠強度 = 0
				PRINTW Mass hypnosis canceled.
			ENDIF
		ELSEIF RESULT >= 0 && RESULT <= 2
			BASE:MASTER:TSP -= 500
			催眠判定 = 0
			
			IF !CFLAG:睡眠
				;対抗
				$HYPNO_CHECK
				
				LOCAL:0 = 1 + RAND:100
				LOCAL:1 = 1 + RAND:100
				
				LOCAL:2 = 0
				LOCAL:3 = 0
				
				SIF TALENT:MASTER:禁断の知識
					LOCAL:2 += 20
				SIF CFLAG:うふふ
					LOCAL:2 += 30
				
				SIF TALENT:禁断の知識
					LOCAL:3 += 20
				SIF TALENT:淫乱
					LOCAL:3 -= 20
				SIF TALENT:服従
					LOCAL:3 -= 20
				SIF TALENT:妄信
					LOCAL:3 -= 20
				SIF TALENT:妄想の産物
					LOCAL:3 -= 100
				
				SELECTCASE TALENT:人間
					;仙人
					CASE 2
						LOCAL:3 += 30
					;天人
					CASE 3
						LOCAL:3 += 30
					;月人
					CASE 4
						LOCAL:3 += 20
					;魔界人
					CASE 5
						LOCAL:3 += 10
				ENDSELECT
				SIF TALENT:妖怪
					LOCAL:3 += 30
				SIF TALENT:妖精
					LOCAL:3 += 10
				SIF TALENT:神霊
					LOCAL:3 += 50
				SIF TALENT:幽霊
					LOCAL:3 += 30
				SIF TALENT:付喪神
					LOCAL:3 += 30
				SIF TALENT:人形
					LOCAL:3 += 30
				SELECTCASE TALENT:追加種族
					;巫女
					CASE 1
						LOCAL:3 += 20
					;蓬莱人
					CASE 4
						LOCAL:3 += 20
				ENDSELECT
				
				LOCAL:2 = MAX(0, LOCAL:2 + (50 * BASE:MASTER:体力) / MAXBASE:MASTER:体力 + (75 * BASE:MASTER:気力) / MAXBASE:MASTER:気力 - (100 * BASE:MASTER:酒気) / MAXBASE:MASTER:酒気 + ABL:MASTER:技巧 * 15 + ABL:MASTER:教養 * 15)
				LOCAL:3 = MAX(0, LOCAL:3 + MARK:反発刻印 * 30 + (50 * BASE:体力) / MAXBASE:体力 + (75 * BASE:気力) / MAXBASE:気力 - (75 * BASE:ムード) / MAXBASE:ムード + (100 * BASE:理性) / MAXBASE:理性 + (100 * BASE:怒り) / MAXBASE:怒り - (100 * BASE:酒気) / MAXBASE:酒気 - CFLAG:信頼度 / 10 - ABL:親密 * 3 - ABL:従順 * 5 - ABL:欲望 * 7 + MIN(ABL:技巧, 10) * 10 + ABL:教養 * 15)
				
				LOCAL:4 = LOCAL:0 + LOCAL:2
				LOCAL:5 = LOCAL:1 + LOCAL:3
				
				Qol_Info_Count_Array:MASTER:Qol_Count_Hypnosis ++ ;qol custom code
				PRINTFORM %CALLNAME:MASTER%'s Power：
				PRINTFORM 1D100 + {LOCAL:2} = {LOCAL:0} + {LOCAL:2} = 
				SIF LOCAL:4 > LOCAL:5 && (LOCAL:0 >= 5 || LOCAL:1 < 5)
					FONTBOLD
				PRINTFORM {LOCAL:4}
				FONTREGULAR
				IF LOCAL:0 > 95
					FONTBOLD
					SETCOLOR C_AQUA
					PRINTL   Critical Success! 
					RESETCOLOR
					FONTREGULAR
					催眠判定 += 1
				ELSEIF LOCAL:0 < 5
					FONTBOLD
					SETCOLOR 255,0,0
					PRINTL   Critical Failure! 
					Qol_Info_Count_Array:MASTER:Qol_Count_HypnosisFail ++ ;qol custom code
					RESETCOLOR
					FONTREGULAR
					催眠判定 -= 1
				ELSE
					PRINTL 
				ENDIF
				
				PRINTFORM %CALLNAME:TARGET%'s Resistance：
				PRINTFORM 1D100 + {LOCAL:3} = {LOCAL:1} + {LOCAL:3} = 
				SIF LOCAL:5 > LOCAL:4 && (LOCAL:1 >= 5 || LOCAL:0 < 5)
					FONTBOLD
				PRINTFORM {LOCAL:5}
				FONTREGULAR
				IF LOCAL:1 > 95
					FONTBOLD
					SETCOLOR C_AQUA
					PRINTL   Critical Success! 
					RESETCOLOR
					FONTREGULAR
					催眠判定 -= 1
				ELSEIF LOCAL:1 < 5
					FONTBOLD
					SETCOLOR 255,0,0
					PRINTL   Critical Failure! 
					Qol_Info_Count_Array:MASTER:Qol_Count_HypnosisFail ++ ;qol custom code
					RESETCOLOR
					FONTREGULAR
					催眠判定 += 1
				ELSE
					PRINTL 
				ENDIF
				
				IF 催眠判定 > 0
					IF 催眠判定 > 1
						PRINT Result：
						FONTBOLD
						SETCOLOR C_AQUA
						PRINTW Double Critical Success! 
						RESETCOLOR
						FONTREGULAR
						催眠判定 = 1
						催眠結果 = 3
					ELSE
						PRINT Result：
						FONTBOLD
						SETCOLOR C_AQUA
						PRINTW Critical Success! 
						RESETCOLOR
						FONTREGULAR
						催眠判定 = 0
						催眠結果 = 2
					ENDIF
				ELSEIF 催眠判定 < 0
					IF 催眠判定 < -1
						PRINT Result：
						FONTBOLD
						SETCOLOR 255,0,0
						PRINTW Double Critical Failure! 
						Qol_Info_Count_Array:MASTER:Qol_Count_HypnosisFail ++ ;qol custom code
						RESETCOLOR
						FONTREGULAR
						PRINTL 
						PRINTW ...Hypnosis failed.
						PRINTL 
						催眠結果 = -3
						CALL KOJO_MESSAGE_SEND("EVENT",666,TARGET,催眠タイプ,催眠結果)
						SIF CFLAG:2 >= 20
							CALL CHANGE_CFLAG(2,TARGET,MAX(CFLAG:2 / 20 * -1, -80) )
						SIF CFLAG:4 >= 5
							CALL CHANGE_CFLAG(4,TARGET,MAX(CFLAG:4 /  5 * -1, -40) )
						BASE:怒り += 400
					ELSE
						PRINT Result：
						FONTBOLD
						SETCOLOR 255,0,0
						PRINTW Critical Failure! 
						Qol_Info_Count_Array:MASTER:Qol_Count_HypnosisFail ++ ;qol custom code
						RESETCOLOR
						FONTREGULAR
						PRINTL 
						PRINTW ...Hypnosis failed.
						PRINTL 
						催眠結果 = -2
						CALL KOJO_MESSAGE_SEND("EVENT",666,TARGET,催眠タイプ,催眠結果)
						SIF CFLAG:2 >= 30
							CALL CHANGE_CFLAG(2,TARGET,MAX(CFLAG:2 / 30 * -1, -50) )
						SIF CFLAG:4 >= 10
							CALL CHANGE_CFLAG(4,TARGET,MAX(CFLAG:4 / 10 * -1, -20) )
						BASE:怒り += 200
					ENDIF
					
					催眠判定 = 0
					TFLAG:Info表示中 = 0
					RETURN 0
				ELSEIF LOCAL:4 > LOCAL:5
					FONTBOLD
					PRINTFORM {LOCAL:4}
					FONTREGULAR
					PRINTFORMW  > {LOCAL:5}
					催眠結果 = 1
				ELSEIF LOCAL:4 == LOCAL:5
					PRINTFORMW {LOCAL:4} = {LOCAL:5}
					PRINTL 
					GOTO HYPNO_CHECK
				ELSE
					PRINTFORM {LOCAL:4} < 
					FONTBOLD
					PRINTFORMW {LOCAL:5}
					FONTREGULAR
					PRINTL 
					PRINTW ...Hypnosis failed.
					Qol_Info_Count_Array:MASTER:Qol_Count_HypnosisFail ++ ;qol custom code
					PRINTL 
					催眠結果 = -1
					CALL KOJO_MESSAGE_SEND("EVENT",666,TARGET,催眠タイプ,催眠結果)
					SIF CFLAG:2 >= 30
						CALL CHANGE_CFLAG(2,TARGET,MAX(CFLAG:2 / 30 * -1, -50) )
					SIF CFLAG:4 >= 10
						CALL CHANGE_CFLAG(4,TARGET,MAX(CFLAG:4 / 10 * -1, -20) )
					
					催眠判定 = 0
					TFLAG:Info表示中 = 0
					RETURN 0
				ENDIF
				PRINTL 
			ENDIF
		ENDIF
		
		CALL 催眠発動の呪文(RESULT)

		IF RESULT == 0
			TALENT:反発抑制 = 1
			
			IF 催眠判定 > 0
				TALENT:機嫌 = 1
				MARK:反発刻印 = 0
			ELSE
				SIF TALENT:機嫌 < 0
					TALENT:機嫌 = 0
				SIF MARK:反発刻印 > 0
					MARK:反発刻印 -= 1
			ENDIF
			BASE:怒り = 0
			CFLAG:不機嫌 = 0
			CFLAG:ブチギレ = 0
			TCVAR:挿入拒絶 = 0
			PALAM:反感 = 0
			PALAM:不快 = 0
			PALAM:抑鬱 = 0
			Qol_Info_Count_Array:TARGET:Qol_Count_Hypnos_Hostility ++ ;qol custom code
		ELSEIF RESULT == 1
			TALENT:合意誤認 = 1
			BASE:ムード = MAX(300 + 100 * 催眠判定, BASE:ムード)
			BASE:理性 = MIN(300 - 100 * 催眠判定, BASE:理性)
			Qol_Info_Count_Array:TARGET:Qol_Count_Hypnos_Consent ++ ;qol custom code
		ELSEIF RESULT == 2
			IF TCVAR:発情 == 1
				TCVAR:発情 = 0
				BASE:ムード = MIN(200, BASE:ムード)
				BASE:理性 = MAX(400, BASE:理性)
				IF 催眠判定 > 0
					CFLAG:溜まってる度 = CFLAG:溜まってる度 / 3
					SIF PALAM:欲情 >= 30000
						PALAM:欲情 -= 30000
				ELSE
					CFLAG:溜まってる度 = CFLAG:溜まってる度 / 2
					SIF PALAM:欲情 >= 10000
						PALAM:欲情 -= 10000
				ENDIF
			ELSE
				TCVAR:発情 = 1
				BASE:ムード = MAX(MAXBASE:ムード * (2 + 催眠判定 * 2) / 4, BASE:ムード)
				BASE:理性 = MIN(250 * (2 - 催眠判定), BASE:理性)
				IF 催眠判定 > 0
					CFLAG:溜まってる度 = CFLAG:溜まってる度 * 3
					SIF PALAM:欲情 < 30000
						PALAM:欲情 += 30000
				ELSE
					CFLAG:溜まってる度 = CFLAG:溜まってる度 * 2
					SIF PALAM:欲情 < 10000
						PALAM:欲情 += 10000
				ENDIF
			ENDIF
			Qol_Info_Count_Array:TARGET:Qol_Count_Hypnos_Arouse ++ ;qol custom code
		ENDIF
		
		PRINTL 
		
		IF 顔絵付きメッセージ
			RESULTS =
			キャラ画像表示サイズ比 = 100
			IF RESULT == 0
				CALL GET_BASE_RESOURCE(TARGET, "顔絵", , "笑顔")
				SIF !RESULT
					CALL GET_BASE_RESOURCE(TARGET, "顔絵", , "通常")
			ELSEIF RESULT == 1
				CALL GET_BASE_RESOURCE(TARGET, "顔絵", , "発情")
				SIF !RESULT
					CALL GET_BASE_RESOURCE(TARGET, "顔絵", , "通常")
			ELSEIF RESULT == 2
				IF TCVAR:発情 == 1
					CALL GET_BASE_RESOURCE(TARGET, "顔絵", "裸", "発情")
					SIF !RESULT
						CALL GET_BASE_RESOURCE(TARGET, "顔絵", , "通常")
				ELSE
					CALL GET_BASE_RESOURCE(TARGET, "顔絵", , "発情")
				ENDIF
			ENDIF
			SIF FLAG:マルチサイズ
				デフォルトキャラ画像横幅 = SPRITEHEIGHT(RESULTS)
			CALL IMAGE_SET(RESULTS, 0, 0, キャラ画像表示サイズ比, 0)
			CALL SIMULTANEOUS_IMAGE_DISPLAY(0, 0, 1, -1)
		ENDIF
		CALL KOJO_MESSAGE_SEND("EVENT",666,TARGET,催眠タイプ,催眠結果)
		IF RESULT > 0
			PRINTL 
		ELSEIF 顔絵付きメッセージ
			PRINTW 
		ENDIF
		
;		PRINTW ……催眠解除。
		
		催眠判定 = 0
	ELSEIF RESULT == 9
		催眠タイプ = 0
		催眠結果 = 0
		CALL KOJO_MESSAGE_SEND("EVENT",666,TARGET,催眠タイプ,催眠結果)
	ELSE
		CLEARLINE 1
		GOTO HYPNO_SELECT
	ENDIF
;ENDIF

;FileName_催眠発動.ERB ------------------------------ Rev1.00
;催眠発動の呪文
;CALL		USER
;ARG		RESULT
;RETURN		ARG
;COMMENT	
;-----------------------------------------------------------
@催眠発動の呪文(CASTER=0,ARG)
#DIM CASTER
FONTBOLD
IF ARG == -1
	;雰囲気が伝わらない
	SETCOLOR 255,0,0
	PRINTFORMW Hypnosis Activated
	SETCOLOR 255,100,255
	PRINTFORMW Thou who art in my sight, obey me!!!
	RESETCOLOR
ELSE
	;《BLOODLINE》の魔眼能力
	; SETCOLOR 255,0,0
	; PRINTFORMW Elune-odor el elune
	; SETCOLOR 255,100,255
	; PRINTFORMW Thou who art in my sight, obey me!!!
	; RESETCOLOR
	
	CALL SET_KOJO_COLOR(CASTER)
	PRINTFORMW 「Elune-odor el elune」
	PRINTFORMW 「Thou who art in my sight, obey me!!!」
	RESETCOLOR
ENDIF
FONTREGULAR
RETURN ARG

@EVENTTURNEND
;催眠フラグOFF
FLAG:催眠強度 = 0

CVARSET TALENT, GETNUM(TALENT, "反発抑制"), 0
CVARSET TALENT, GETNUM(TALENT, "合意誤認"), 0

;-------------------------------
;Custom config section
;-------------------------------
@CUSTOM_OPTION_200
PRINTFORML [200] - Favor multiplier：\@ FLAG:好感度上昇率 ? X {FLAG:好感度上昇率} # X 1 \@

@CUSTOM_OPTION_RESULT_200
PRINTFORML Input the value, 0 = disable
CALL INPUT_MANY(@"0TO20", FLAG:好感度上昇率) ;custom code
FLAG:好感度上昇率 = LIMIT(RESULT, 0, 20)

@CUSTOM_OPTION_201
PRINTFORML [201] - Trust multiplier：\@ FLAG:信頼度上昇率 ? X {FLAG:信頼度上昇率} # X 1 \@

@CUSTOM_OPTION_RESULT_201
PRINTFORML Input the value, 0 = disable
CALL INPUT_MANY(@"0TO20", FLAG:信頼度上昇率) ;custom code
FLAG:信頼度上昇率 = LIMIT(RESULT, 0, 20)

@CUSTOM_OPTION_2
;NAS: Global options
RETURN 0
PRINTFORML [  2] - Hypnosis feature：\@ FLAG:催眠on ? On # Off \@

@CUSTOM_OPTION_RESULT_2
FLAG:催眠on = !FLAG:催眠on
