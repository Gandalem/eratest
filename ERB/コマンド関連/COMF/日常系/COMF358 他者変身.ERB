;-------------------------------------------------
;他者変身
;日常系コマンド　レベル1
;-------------------------------------------------
@COM358
#DIM NAMENUM
;-------------------------------------------------
;COM別FLAGの設定
;-------------------------------------------------

;変身先選択
NAMENUM = TARGET
IF CFLAG:TARGET:変化先 == 0
	PRINTFORML Who should %CALLNAME:NAMENUM% transform into? (Currently: Untransformed)
ELSE
	PRINTFORML Who should %CALLNAME:NAMENUM% transform into? (Currently: %CALLNAME:(CFLAG:TARGET:変化先)%)
ENDIF
CALL SHOW_CHALALIST
PRINTL 
PRINTFORML [998] - Return
PRINTL [999] - Forget it
INPUT

LOCAL = RESULT

;口上での成否判定
CALL KOJO_MESSAGE_SEND("SUCCESS",-1,TARGET,-1,-1)

SELECTCASE TFLAG:192
CASE 0, 1
	;挿入中
	IF TCVAR:PLAYER:100 == TARGET || TCVAR:PLAYER:101 == TARGET
		;体格差がある場合は経験がないとダメ
		IF 挿入不可(TARGET) == 1 && EXP:Ｖ経験 < 50 && !TALENT:PLAYER:禁断の知識
			PRINTFORML The difference in physique is too vast for this too work...
			TFLAG:193 = -1
			RETURN -1
		ENDIF
		;さらに体格差があると禁断の知識が必要
		IF 挿入不可(TARGET) == 2 && (!TALENT:PLAYER:禁断の知識 || EXP:Ｖ経験 < 50)
			PRINTFORML The difference in physique is too vast for this too work...
			TFLAG:193 = -1
			RETURN -1
		;小人
		ELSEIF 挿入不可(TARGET) == 3
			PRINTFORML The difference in physique is too vast for this too work...
			TFLAG:193 = -1
			RETURN -1
		ENDIF
	ENDIF

	;解除
	IF LOCAL == NAMENUM || LOCAL == 998
		;元が本人
		SIF !CFLAG:TARGET:変化先
			GOTO CANCEL
		CFLAG:TARGET:変化元 = CFLAG:TARGET:変化先
		CFLAG:TARGET:変化先 = 0
		PRINTFORML %CALLNAME:NAMENUM% returns to %HIS_HER(NAMENUM)% original form.
	;キャンセル
	ELSEIF !INRANGE(LOCAL,1,CHARANUM)
		$CANCEL
		PRINTL Canceled
		RETURN -1
	;変化実行
	ELSE
		;同じキャラ
		SIF CFLAG:TARGET:変化先 == LOCAL
			GOTO CANCEL
		CFLAG:TARGET:変化元 = CFLAG:TARGET:変化先
		CFLAG:NAMENUM:変化先 = LOCAL
		PRINTFORML %CALLNAME:NAMENUM% transforms into %CALLNAME:LOCAL% before your eyes!
	ENDIF

	IF TCVAR:PLAYER:100 == TARGET || TCVAR:PLAYER:101 == TARGET
		TIME += 5
	ELSE
		TFLAG:102 = 0
	ENDIF
CASEELSE
	TFLAG:193 = -1
	TFLAG:102 = 0
ENDSELECT

;-------------------------------------------------
;ソース追加処理
;-------------------------------------------------
;消費体力、気力

RETURN 0
;-------------------------------------------------
;他者変身実行判定
;-------------------------------------------------
@COM_ABLE358
SIF !TFLAG:100
	RETURN 0
;一括管理
SIF GLOBAL_COMABLE(358)
	RETURN RESULT
SIF !TARGET
	RETURN 0
;時間停止中は禁止
SIF FLAG:70 == 1
	RETURN 0
;変化持ち意外は禁止
SIF !TALENT:変化
	RETURN 0
SIF !(TALENT:恋慕 || TALENT:愛欲)
	RETURN 0

RETURN 1