; Modular ERB: COMF/日常系/COMF451 オナニー.ERB
;-------------------------------------------------
;オナニー
;日常系コマンド、レベル1
;-------------------------------------------------

@COM451
#DIM C_ID
#DIM AMT
CALL COM451_TR
RETURN
IF TARGET
	CALL Com_Mast ;com custom code
	SIF RESULT
		RETURN 1
	;Kodoku no Gourmet reference
	PRINTFORML 우리가 자위를 할 때는 방해받지 않고 자유롭게 자신을 탐닉하고 싶다. 편안함을 느끼고 싶다.
	PRINTFORMW 우리는 조용하고 안락한 곳에 혼자 있고 싶다...
	RETURN -1
ENDIF
PRINTFORML 당신의 마음속에 누가 떠오르나요?
DRAWLINE
LOCAL:9 = LINECOUNT ;custom code
DO
SIF LINECOUNT - LOCAL:9 > 0
	CLEARLINE LINECOUNT - LOCAL:9
FOR LOCAL,1,CHARANUM
IF !IS_NAMED(LOCAL) || !CFLAG:LOCAL:面識
	CONTINUE
ENDIF
PRINTFORMLC [{LOCAL, 3}] %CALLNAME:LOCAL%
NEXT
PRINTL
DRAWLINE
PRINTL [999] 아무도 상상할 필요 없어! [1000] 취소
INPUT
C_ID = RESULT
SELECTCASE C_ID
	CASE 1 TO CHARANUM-1 ;bug fix
		SIF !IS_NAMED(C_ID) || !CFLAG:C_ID:面識 ;bug fix
			CONTINUE
		CALL PRINT_FACE, C_ID, "発情", "裸", "", "照れ" ;custom code tweak - doesn't show nude version
		PRINTFORML 당신은 %CALLNAME:C_ID%를 생각하면서 자신의 성적 충동을 다스린다.
		Qol_Info_Count_Array:C_ID:Qol_Count_Fap ++ ;qol custom code
		BREAK ;custom code
	CASE 999
		PRINTFORML 당신은 %SPLIT_G("the movements of the celestial bodies:the secrets of the cosmos:the nature:the mountains, rivers, trees and grasses:the travails of life:the issues of birth, old age, illness and dying:the purpose of life")%을 생각하면서 성적인 충동을 다룬다.
		BREAK ;custom code
	CASE 1000 ;custom code
		RETURN -1
	CASEELSE
		CONTINUE ;custom code
ENDSELECT
LOOP 1
AMT = 50 + RAND:50
;custom code for onahole
IF ITEM:オナホール && HAS_PENIS(MASTER) ;this is male/futanari only command as of now, but put a check to future proof
	PRINTFORML 오나홀을 사용하고 싶으신가요?
	CALL ASK_YN
	IF !RESULT
		AMT = 100 + RAND:100
		PRINTFORML 오나홀을 사용하면 경험이 상당히 향상된다.
	ENDIF
ENDIF
;custom code for color
SETCOLOR C_CREAM
IF TALENT:MASTER:濃厚精液
	AMT *= 2
	PRINTFORMW 당신은 추가로 걸쭉한 것 {AMT}ml를 방출한다.
ELSE
	PRINTFORMW 당신은 {AMT}ml를 방출한다.
ENDIF
RESETCOLOR
SIF INRANGE(C_ID, 1, CHARANUM-1) ;qol custom code
	Qol_Info_Count_Array:C_ID:Qol_Count_FapSemen += AMT
SIF BASE:MASTER:精力 - AMT < 0 ;com custom code
	nBorrowedVig += ABS(BASE:MASTER:精力 - AMT)
BASE:MASTER:精力 -= AMT
DOWNBASE:MASTER:気力 = 100
;custom code for exp and message
EXP:MASTER:自慰経験 += 1
SIF TALENT:MASTER:濃厚精液
	PRINTFORMW 안심이 된다.
;-----------
nSexDone -- ;addition custom code
TIME += 20
RETURN 1
;待つ

@COM_ABLE451
;ショップ画面では不可
SIF !TFLAG:100
	RETURN 0
;一括管理
SIF GLOBAL_COMABLE(451)
	RETURN RESULT
;気力0では不可
SIF BASE:MASTER:気力 <= 0
	RETURN 0
;精力0では不可
;SIF BASE:MASTER:精力 <= 0 && HAS_PENIS(MASTER)
;	RETURN 0
;the has penis check has been removed as it doesn't make sense for girls not being able to fap.
;SIF !HAS_PENIS(MASTER) ;bug fix
;	RETURN 0
;ウフフ中は不可
SIF CFLAG:うふふ && !CFLAG:イタズラ ;com custom code, allow in sleep molestation
	RETURN 0
;停止中は不可
; SIF FLAG:70 == 1 ;NAS customs, masturbation is now possible in time stop
; 	RETURN 0
;私室以外では不可
;SIF CFLAG:MASTER:現在位置 != CFLAG:MASTER:初期位置 && !CFLAG:イタズラ ;com custom code, allow in sleep molestation
;	RETURN 0
SIF !nSex
	RETURN 0
SIF CFLAG:MASTER:今忙しくて手が離せない ; NAS: Is masturbating
	RETURN 0
RETURN 1
