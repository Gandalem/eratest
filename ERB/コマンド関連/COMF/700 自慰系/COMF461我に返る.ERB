@COM461
;妄想オナニーをやめる用
VARSET LOCAL

STAIN:MASTER:0 = 0
STAIN:MASTER:4 = 8

IF HAS_PENIS(MASTER)
	STAIN:MASTER:1 = 6
	STAIN:MASTER:2 = 6
	STAIN:MASTER:5 = 0
ELSEIF HAS_VAGINA(MASTER)
	STAIN:MASTER:1 = 1
	STAIN:MASTER:3 = 1
	STAIN:MASTER:5 = 1
ELSE
	STAIN:MASTER:1 = 7
	STAIN:MASTER:2 = 7
	STAIN:MASTER:3 = 7
	STAIN:MASTER:5 = 7
ENDIF

STAIN:0 = 0
STAIN:1 = 0
STAIN:2 = 2
STAIN:3 = 1
STAIN:4 = 8
STAIN:5 = 0
STAIN:6 = 1
STAIN:7 = 8
CFLAG:TARGET:同行 = 0


EXP:MASTER:14 = BEFORE_MOUSOU_EXP14
EXP:MASTER:15 = BEFORE_MOUSOU_EXP15
EXP:MASTER:16 = BEFORE_MOUSOU_EXP16
EXP:MASTER:20 = BEFORE_MOUSOU_EXP20
EXP:MASTER:21 = BEFORE_MOUSOU_EXP21
EXP:MASTER:24 = BEFORE_MOUSOU_EXP24
EXP:MASTER:25 = BEFORE_MOUSOU_EXP25
EXP:MASTER:27 = BEFORE_MOUSOU_EXP27
EXP:MASTER:28 = BEFORE_MOUSOU_EXP28
EXP:MASTER:29 = BEFORE_MOUSOU_EXP29
EXP:MASTER:53 = BEFORE_MOUSOU_EXP53

SIF BEFORE_MOUSOU_EXP29 == 0 && HAS_PENIS(MASTER)
	TALENT:MASTER:1 = 0
SIF BEFORE_MOUSOU_EXP20 == 0 && HAS_VAGINA(MASTER)
	TALENT:MASTER:0 = 1

TFLAG:妄想中 = 0
TARGET = MASTER
TFLAG:現在のTARGET = MASTER
CALL Add_Remove_Duplicates_Of_Character(CHARANUM - 1)
CFLAG:TARGET:デート中 = MAIN_MAP
CFLAG:MASTER:デート中 = MAIN_MAP

CFLAG:MASTER:現在位置 = CFLAG:MASTER:311
	FOR LOCAL, 1, CHARANUM
		SIF CFLAG:LOCAL:現在位置 == CFLAG:MASTER:311
			CFLAG:LOCAL:現在位置 = CFLAG:LOCAL:311
	NEXT
CALLF HESOYAM(MASTER)	
TIME ++
RETURN 1

;-------------------------------------------------
;461,我に返る - Delusional masturbation-related
;-------------------------------------------------

@MESSAGE_COM461_TR
;一割以下
IF BASE:MASTER:気力 <= (MAXBASE:MASTER:気力 / 10)
	PRINTFORML Exhausted, you stop playing with your delusion.
	SIF HAS_PENIS(MASTER)
		PRINTFORML Hand gripped to your %SEMEN()% covered %TINKO()%, the smell of cum stinks up the entire room...
	SIF HAS_VAGINA(MASTER)
		PRINTFORML Body covered in %FSYN("juice")%, the smell of a female permeates the entire room...
	;SIF IS_HERM(MASTER)
	;	PRINTFORML Covered in %FSYN("juice")% and %FSYN("ejac:v")%, it leaves you a merciless sight...
;ELSEIF (MAXBASE:MASTER:気力 / BASE:MASTER:気力) >= 2
;半分以下
ELSEIF BASE:MASTER:気力 <= (MAXBASE:MASTER:気力 / 2)
	PRINTFORML Satisfied, you stop playing with your delusion.
	SIF HAS_PENIS(MASTER)
		PRINTFORML Hand gripped to your %SEMEN()% covered %TINKO()%, the smell of cum lingers...
	SIF HAS_VAGINA(MASTER)
		PRINTFORML Body covered in %FSYN("juice")%, the scent of a female lingers...
	;SIF IS_HERM(MASTER)
	;	PRINTFORML %CALLNAME:MASTER%は体には至る所に精液と愛液がこびりついている……
ELSE
	PRINTFORML Having had enough, you stop playing with your delusion.
	SIF HAS_PENIS(MASTER)
		PRINTFORML Hand gripped to your %TINKO()%, a small amount of precum spits out...
	SIF HAS_VAGINA(MASTER)
		PRINTFORML A small amount of %FSYN("juice")% drips out……
	;SIF IS_HERM(MASTER)
	;	PRINTFORML %CALLNAME:MASTER%の体には僅かな我慢汁と愛液が付着している……
ENDIF
PRINTFORML You feel a little sad...

IF EX:MASTER:射精
	LOCAL = EX:MASTER:射精*10
	PRINTFORML You feel some wetness in your %PeeProtectType(MASTER)%, indicating that you %FSYN("ejac:ed")% yourself in your delusion.
	CFLAG:MASTER:2200 -= LOCAL
	IF CFLAG:MASTER:2200 <= 0	
		PRINTFORML It overflowed out of your %PeeProtectType(MASTER,1)% and leaked into the futon...
		PRINTFORML Some ropes of cum can be seen on the bed... 		
		YOGORE:(CFLAG:MASTER:現在位置) += LOCAL*10
		Qol_Info_Count_Array:MASTER:42 += 1
	ENDIF
	;TRYCALL OMORASHI_END(MASTER, 1, 0, 1)
ENDIF

IF EX:MASTER:12
	LOCAL = EX:MASTER:12*10
	PRINTFORML You feel some wetness in your %PeeProtectType(MASTER)%, indicating that you %FSYN("pee:ed")% yourself in your delusion.
	CFLAG:MASTER:2200 -= LOCAL
	IF CFLAG:MASTER:2200 <= 0	
		PRINTFORML It overflowed out of your %PeeProtectType(MASTER,1)% and leaked into the futon...
		PRINTFORML A large dark stain can be seen on the bed... 		
		YOGORE:(CFLAG:MASTER:現在位置) += LOCAL*10
		Qol_Info_Count_Array:MASTER:42 += 1
	ENDIF
	TRYCALL OMORASHI_END(MASTER, 1, 0, 1)
ENDIF

IF EX:MASTER:31
	LOCAL = EX:MASTER:31*10
	PRINTFORML A squishy feeling beneath the rear of your %PeeProtectType(MASTER)% tells you that you %FSYN("poo:ed")% in your delusion.
	CFLAG:MASTER:2202 -= LOCAL
	IF CFLAG:MASTER:2202 <= 0
		PRINTFORML It blew out of your %PeeProtectType(MASTER,1)%... staining the futon \@ GETBIT(nScat, 4) ? brown # blue\@  with your mess...   		
		YOGORE:(CFLAG:MASTER:現在位置) += LOCAL*10
		Qol_Info_Count_Array:MASTER:43 += 1
	ENDIF
	TRYCALL OMORASHI_END(MASTER, 0, 1, 1)
ENDIF

;-------------------------------------------------
;実行判定
;-------------------------------------------------
;我に返る
@COM_ABLE461
;現実帰還実行判定
SIF !TFLAG:100
	RETURN 0
SIF TFLAG:妄想中 != 1
	RETURN 0
RETURN 1