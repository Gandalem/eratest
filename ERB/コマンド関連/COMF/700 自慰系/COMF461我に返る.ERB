; Modular ERB: COMF/700 自慰系/COMF461我に返る.ERB

@COM461
;妄想オナニーをやめる用
VARSET LOCAL
STAIN:MASTER:0 = 0
STAIN:MASTER:4 = 8
IF HAS_PENIS(MASTER)
	STAIN:MASTER:1 = 6
	STAIN:MASTER:2 = 6
	STAIN:MASTER:5 = 0
ELSEIF HAS_VAGINA(MASTER)
	STAIN:MASTER:1 = 1
	STAIN:MASTER:3 = 1
	STAIN:MASTER:5 = 1
ELSE
	STAIN:MASTER:1 = 7
	STAIN:MASTER:2 = 7
	STAIN:MASTER:3 = 7
	STAIN:MASTER:5 = 7
ENDIF
STAIN:0 = 0
STAIN:1 = 0
STAIN:2 = 2
STAIN:3 = 1
STAIN:4 = 8
STAIN:5 = 0
STAIN:6 = 1
STAIN:7 = 8
CFLAG:TARGET:同行 = 0
EXP:MASTER:14 = BEFORE_MOUSOU_EXP14
EXP:MASTER:15 = BEFORE_MOUSOU_EXP15
EXP:MASTER:16 = BEFORE_MOUSOU_EXP16
EXP:MASTER:20 = BEFORE_MOUSOU_EXP20
EXP:MASTER:21 = BEFORE_MOUSOU_EXP21
EXP:MASTER:24 = BEFORE_MOUSOU_EXP24
EXP:MASTER:25 = BEFORE_MOUSOU_EXP25
EXP:MASTER:27 = BEFORE_MOUSOU_EXP27
EXP:MASTER:28 = BEFORE_MOUSOU_EXP28
EXP:MASTER:29 = BEFORE_MOUSOU_EXP29
EXP:MASTER:53 = BEFORE_MOUSOU_EXP53
SIF BEFORE_MOUSOU_EXP29 == 0 && HAS_PENIS(MASTER)
	TALENT:MASTER:1 = 0
SIF BEFORE_MOUSOU_EXP20 == 0 && HAS_VAGINA(MASTER)
	TALENT:MASTER:0 = 1
TFLAG:妄想中 = 0
TARGET = MASTER
TFLAG:現在のTARGET = MASTER
CALL Add_Remove_Duplicates_Of_Character(CHARANUM - 1)
CFLAG:TARGET:デート中 = MAIN_MAP
CFLAG:MASTER:デート中 = MAIN_MAP
CFLAG:MASTER:現在位置 = CFLAG:MASTER:311
FOR LOCAL, 1, CHARANUM
SIF CFLAG:LOCAL:現在位置 == CFLAG:MASTER:311
	CFLAG:LOCAL:現在位置 = CFLAG:LOCAL:311
NEXT
CALLF HESOYAM(MASTER)
TIME ++
RETURN 1
;-------------------------------------------------
;461,我に返る - Delusional masturbation-related
;-------------------------------------------------

@MESSAGE_COM461_TR
;一割以下
IF BASE:MASTER:気力 <= (MAXBASE:MASTER:気力 / 10)
	PRINTFORML 지쳐서 망상의산물 가지고 노는 것을 그만둔다.
	SIF HAS_PENIS(MASTER)
		PRINTFORML 손이 당신의 %SEMEN()%으로 뒤덮인 %TINKO()%을 움켜쥐자, 정액 냄새가 방 전체에 진동했다...
	SIF HAS_VAGINA(MASTER)
		PRINTFORML 몸이 %FSYN("juice")%으로 덮여 있고, 여성의 향기가 방 전체에 퍼진다...
	;SIF IS_HERM(MASTER)
	;	PRINTFORML Covered in %FSYN("juice")% and %FSYN("ejac:v")%, it leaves you a merciless sight...
	;ELSEIF (MAXBASE:MASTER:気力 / BASE:MASTER:気力) >= 2
	;半分以下
ELSEIF BASE:MASTER:気力 <= (MAXBASE:MASTER:気力 / 2)
	PRINTFORML 만족했다, 너는 망상의산물 가지고 노는 것을 그만둔다.
	SIF HAS_PENIS(MASTER)
		PRINTFORML 손이 당신의 %SEMEN()%로 덮인 %TINKO()%를 움켜쥐자, 정액 냄새가 남아 있다...
	SIF HAS_VAGINA(MASTER)
		PRINTFORML 몸이 %FSYN("juice")%으로 뒤덮여 있고, 여성의 향기가 감돈다...
	;SIF IS_HERM(MASTER)
	;	PRINTFORML %CALLNAME:MASTER%は体には至る所に精液と愛液がこびりついている……
ELSE
	PRINTFORML 충분히 가졌기에, 당신은 자신의 망상의산물과 노는 것을 그만둔다.
	SIF HAS_PENIS(MASTER)
		PRINTFORML 손이 당신의 %TINKO()%에 잡히자, 소량의 애액이 흘러나왔다...
	SIF HAS_VAGINA(MASTER)
		PRINTFORML 소량의 %FSYN("juice")%이 흘러나왔다…….
	;SIF IS_HERM(MASTER)
	;	PRINTFORML %CALLNAME:MASTER%の体には僅かな我慢汁と愛液が付着している……
ENDIF
PRINTFORML 조금 슬픈 기분이 든다...
IF EX:MASTER:射精
	LOCAL = EX:MASTER:射精*10
	PRINTFORML 당신은 자신의 망상의 산물 속에서 %PeeProtectType(MASTER)%에 어떤 축축함을 느끼며, 자신이 스스로를 %FSYN("ejac:ed")%했음을 나타낸다.
	CFLAG:MASTER:2200 -= LOCAL
	IF CFLAG:MASTER:2200 <= 0
		PRINTFORML 그것은 당신의 %PeeProtectType(MASTER,1)%에서 넘쳐 나와 이불에 새어 나왔다...
		PRINTFORML 침대 위에는 정액의 일부가 보였다... 		
		YOGORE:(CFLAG:MASTER:現在位置) += LOCAL*10
		Qol_Info_Count_Array:MASTER:42 += 1
	ENDIF
	;TRYCALL OMORASHI_END(MASTER, 1, 0, 1)
ENDIF
IF EX:MASTER:12
	LOCAL = EX:MASTER:12*10
	PRINTFORML 당신은 자신의 망상 속에서 스스로를 %FSYN("pee:ed")%했다는 것을 나타내는, %PeeProtectType(MASTER)%의 어떤 축축함을 느낀다.
	CFLAG:MASTER:2200 -= LOCAL
	IF CFLAG:MASTER:2200 <= 0
		PRINTFORML 그것은 당신의 %PeeProtectType(MASTER,1)%에서 넘쳐 나와 이불에 새어 나왔다...
		PRINTFORML 침대 위에는 크고 어두운 얼룩이 보였다... 		
		YOGORE:(CFLAG:MASTER:現在位置) += LOCAL*10
		Qol_Info_Count_Array:MASTER:42 += 1
	ENDIF
	TRYCALL OMORASHI_END(MASTER, 1, 0, 1)
ENDIF
IF EX:MASTER:31
	LOCAL = EX:MASTER:31*10
	PRINTFORML 당신의 %PeeProtectType(MASTER)% 뒤쪽 아래에서 느껴지는 축축한 감각이 당신이 망상의산물 속에서 %FSYN("poo:ed")%하고 있음을 알려준다.
	CFLAG:MASTER:2202 -= LOCAL
	IF CFLAG:MASTER:2202 <= 0
		PRINTFORML 당신의 %PeeProtectType(MASTER,1)%에서 그것이 터져 나와 이불을 얼룩지게 했다 \@ GETBIT(nScat, 4) ? 갈색 # 파란색으로 \@ 당신의 오물로...   		
		YOGORE:(CFLAG:MASTER:現在位置) += LOCAL*10
		Qol_Info_Count_Array:MASTER:43 += 1
	ENDIF
	TRYCALL OMORASHI_END(MASTER, 0, 1, 1)
ENDIF
;-------------------------------------------------
;実行判定
;-------------------------------------------------
;我に返る

@COM_ABLE461
;現実帰還実行判定
SIF !TFLAG:100
	RETURN 0
SIF TFLAG:妄想中 != 1
	RETURN 0
RETURN 1
