; Modular ERB: USERCOM_コマンド表示処理.ERB
;FileName_USERCOM.ERB ------------------------------ Rev1.00
;コマンド表示処理
;CALL		SYSTEM
;ARG		VOID
;RETURN		VOID
;COMMENT
;-----------------------------------------------------------

@SHOW_USERCOM
#DIM DYNAMIC printKojoCom
#DIM HIGHLIGHT_SCOM ;qol custom code
TFLAG:100 = !TFLAG:101
VARSET LOCAL
;前ターンのMASTERが射精した相手をLOCAL:1へセット
IF TCVAR:MASTER:112 == 1
	;V
	LOCAL:1 = MASTER_POSE(SET_V,SET_P,1)
ELSEIF TCVAR:MASTER:112 == 2
	;A
	LOCAL:1 = MASTER_POSE(SET_A,SET_P,1)
ENDIF
;NAS: Force stop redrawing
REDRAW 0
;NAS: video camera fix
SIF TFLAG:105 && !CFLAG:MASTER:うふふ
	TFLAG:105 = 0
;NAS: ufufu fix
IF CFLAG:うふふ == 1
	TFLAG:102 = 2
ELSEIF CFLAG:うふふ == 2
	TFLAG:102 = 3
ELSEIF TFLAG:102 == 3 && CFLAG:MASTER:うふふ == 2
	;既に処理済みなのでスキップ
ELSE
	TFLAG:102 = 1
ENDIF
;=============
CALL LOOK_AT
PRINTL
;REPLACED IN NAS
CALL SHOW_USERCOM_NEW_COM
IF LoG_TutorialProgress < 50
	CALL LoG_AdvanceTutorial(50)
	SIF RESULT
		RESTART
ELSEIF LoG_TutorialProgress == 60 && CFLAG:MASTER:300 == 214
	CALL LoG_AdvanceTutorial(70)
	SIF RESULT
		RESTART
ELSEIF LoG_TutorialProgress == 80
	CALL LoG_AdvanceTutorial(81)
	SIF RESULT
		RESTART
ENDIF
RETURN
CALL フィルタ表示
;コマンド表示
FOR LOCAL,0,800
SIF QOL_BLOCKED_COM_HIDDEN_SEX(LOCAL) ;qol custom code
	CONTINUE
;qol custom code
IF TARGET
	CALL KOJO_CHECK(TARGET, "COM_BLOCKED",,LOCAL)
	SIF RESULT
		CONTINUE
ENDIF
SIF LOCAL == 355 && (!FLAG:70 || CFLAG:TARGET:344 == 1)
	CONTINUE
SIF LOCAL == 200 && TFLAG:102 == 2
	PRINTL
SIF LOCAL == 300 && printKojoCom
	PRINTL
SIF LOCAL == 350 && TFLAG:102 != 2
	PRINTL
SIF LOCAL == 400 && TFLAG:102 != 2
	PRINTL
SIF GLOBAL_COMABLE2(LOCAL)
	CONTINUE
SIF GROUPMATCH(LOCAL,400,405,604)
	CONTINUE
SIF TRAINNAME:LOCAL == "" && !IS_COM_KOJO(LOCAL)
	CONTINUE
SIF LOCAL == ADD_CUSTOM_COM ;addition custom code, don't display general custom com command, handled elsewhere
	CONTINUE
RESULT = 0
;現状で対象コマンドが使用可能かどうか判定
TRYCALLFORM COM_ABLE{LOCAL}
IF RESULT
	SIF !QUICKFILTER(LOCAL)
		CONTINUE
	;qol custom code
	RESULT = 0
	CALL QOL_SCOM_CHECK(LOCAL)
	HIGHLIGHT_SCOM = RESULT
	IF HIGHLIGHT_SCOM > 0
		SETCOLOR C_HEARTPINK
		IF LOCAL == 73 && TCVAR:PLAYER:イきそう
			IF TEQUIP:TARGET:体位 == 1 && (BEDROOM(CFLAG:PLAYER:現在位置) || (BATHROOM() && ITEM:20))
				PRINTFORMC 교미 압박[{LOCAL,3}]
			ELSEIF TEQUIP:TARGET:体位 == 2
				IF CFLAG:TARGET:生理周期 == 7
					PRINTFORMC 야만적인 수정[{LOCAL,3}]
				ELSE
					PRINTFORMC 야만적인 결합[{LOCAL,3}]
				ENDIF
			ELSE
				PRINTFORMC %RESULTS%[{LOCAL,3}]
			ENDIF
		ELSEIF HIGHLIGHT_SCOM == 13
			LOCAL:10 = ADD_TOUCH_CURRENT_POSE(PLAYER, ADD_MASTER_POSE_TR(PLAYER, SET_V, SET_P))
			IF (LOCAL == 60 && LOCAL:10 == 2) || (LOCAL == 61 && LOCAL:10 == 1)
				PRINTFORMC 다른 얼굴 대면[{LOCAL,3}]
			ELSE
				PRINTFORMC %RESULTS%[{LOCAL,3}]
			ENDIF
		ELSE
			PRINTFORMC %RESULTS%[{LOCAL,3}]
		ENDIF
		RESETCOLOR
	ELSEIF (LOCAL == 42 && TEQUIP:11) || (LOCAL == 43 && TEQUIP:12) || (LOCAL == 44 && TEQUIP:13) || (LOCAL == 45 && TEQUIP:14) || (LOCAL == 46 && TEQUIP:15) || (LOCAL == 47 && TEQUIP:16) || (LOCAL == 48 && TEQUIP:17)
		;Custom code
		PRINTFORMC 해제한다 [%TRAINNAME_TR(LOCAL)%][{LOCAL,3}]
	ELSEIF LOCAL == 3 && TEQUIP:バイブ
		PRINTFORMC 바이브레이터 퍼크[{LOCAL,3}]
	ELSEIF LOCAL == 15
		IF CFLAG:MASTER:イタズラ
			PRINTFORMC V가 부드럽게 쓰다[{LOCAL,3}]
		ELSE
			PRINTFORMC 클리토리스 애무[{LOCAL,3}]
		ENDIF
		;custom code
	ELSEIF LOCAL == 19 && TEQUIP:継続キス
		PRINTFORMC 키스 중단[{LOCAL,3}]
		;--
		;com custom code
	ELSEIF LOCAL == 50 && (TEQUIP:PLAYER:コンドーム || TEQUIP:コンドーム)
		PRINTFORMC 콘돔을 제거한다[{LOCAL,3}]
	ELSEIF LOCAL == 302 && PREVCOM == 305
		PRINTFORMC Earpicking을 가져온다[{LOCAL,3}]
	ELSEIF LOCAL == 311 && CFLAG:MASTER:添い寝中
		PRINTFORMC 바디 필로우[{LOCAL,3}]
		;--
	ELSEIF LOCAL == 81 && LOCAL:1
		PRINTFORMC 클리닝 페티셔널(Cleaning Fellatio) [{LOCAL,3}]
	ELSEIF LOCAL == 82 && (TALENT:105 == -2 || TALENT:105 == -1)
		PRINTFORMC 나이즈리[{LOCAL,3}]
	ELSEIF LOCAL == 60 && IS_STANDING_SEX(LOCAL) ;addition custom code
		PRINTFORMC 스탠딩 미셔너리[{LOCAL,3}]
	ELSEIF LOCAL == 61 && IS_STANDING_SEX(LOCAL) ;addition custom code
		PRINTFORMC 뒤에서부터 박다[{LOCAL,3}]
	ELSEIF LOCAL == 62 && IS_STANDING_SEX(LOCAL) ;addition custom code
		PRINTFORMC 정상위로 서 있는 사람[{LOCAL,3}]
	ELSEIF LOCAL == 63 && IS_STANDING_SEX(LOCAL) ;addition custom code
		PRINTFORMC 뒤에서 항문으로 [{LOCAL,3}]
		;addition custom code
	ELSEIF LOCAL == 67 && IS_STANDING_SEX(LOCAL)
		PRINTFORMC 보류된 의회[{LOCAL,3}]
	ELSEIF LOCAL == 68 && IS_STANDING_SEX(LOCAL)
		PRINTFORMC 상원의 회기 중단[{LOCAL,3}]
	ELSEIF LOCAL == 69 && IS_STANDING_SEX(LOCAL)
		PRINTFORMC 항문이 중단된 의회[{LOCAL,3}]
	ELSEIF LOCAL == 70 && IS_STANDING_SEX(LOCAL)
		PRINTFORMC A는 의회에서 정지되었다[{LOCAL,3}]
	ELSEIF LOCAL == 73 && TCVAR:PLAYER:イきそう
		IF TEQUIP:TARGET:体位 == 1 && CAN_NORMAL_SEX(LOCAL) ;addition custom code
			PRINTFORMC 교미 압박[{LOCAL,3}]
		ELSEIF TEQUIP:TARGET:体位 == 2
			IF CFLAG:TARGET:生理周期 == 7
				PRINTFORMC 야만적인 수정[{LOCAL,3}]
			ELSE
				PRINTFORMC 야만적인 결합[{LOCAL,3}]
			ENDIF
		ELSE
			;Custom code
			PRINTFORMC %TRAINNAME_TR(LOCAL)%[{LOCAL,3}]
		ENDIF
	ELSEIF LOCAL == 79 && IS_STANDING_SEX(LOCAL) ;addition custom code
		PRINTFORMC 세인트 인츨링 오나홀[{LOCAL,3}]
	ELSEIF LOCAL == 92 && IS_STANDING_SEX(LOCAL) ;addition custom code
		PRINTFORMC 성인 선교사를 붙잡는다[{LOCAL,3}]
	ELSEIF LOCAL == 93 && IS_STANDING_SEX(LOCAL) ;addition custom code
		PRINTFORMC 뒤에서 성스러운 A를 잡는다[{LOCAL,3}]
	ELSEIF LOCAL == 100 && TALENT:TARGET:幼稚
		PRINTFORMC 엉덩이 때리기[{LOCAL,3}]
	ELSEIF LOCAL == 124 && TEQUIP:TARGET:オナホール ;com custom code
		PRINTFORMC 오나홀잡을 [{LOCAL,3}] 준다
	ELSEIF LOCAL == 130 && IS_STANDING_SEX(LOCAL)
		PRINTFORMC 성직자[{LOCAL,3}]를 붙잡는다
	ELSEIF LOCAL == 131 && IS_STANDING_SEX(LOCAL)
		PRINTFORMC 뒤에서 잡는다[{LOCAL,3}]
	ELSEIF LOCAL == 186 && TEQUIP:羞恥プレイ ;qol custom code
		PRINTFORMC 치정 플레이 종료[{LOCAL,3}]
	ELSEIF LOCAL == 300 && TCVAR:TARGET:302 && !TALENT:恋慕 && ABL:MASTER:教養 >= 3 && ABL:MASTER:話術技能 >= 4
		SETCOLOR C_GRAY
		PRINTFORMC 대화[{LOCAL,3}]
		RESETCOLOR
	ELSEIF LOCAL == 301 && CFLAG:TARGET:行動 == 5
		PRINTFORMC 다과를 제안한다[{LOCAL,3}]
	ELSEIF LOCAL == 301 && GETBIT(TCVAR:MASTER:特別なお茶, 0)
		PRINTFORMC 블랙티를 제공한다[{LOCAL,3}]
		;addition custom code, new banquet stuff
	ELSEIF LOCAL == 334 && (CFLAG:TARGET:350 == 49 || (!MOREDATE && TARGET == FLAG:デート相手) || (MOREDATE && IS_IN_GROUP_DATE(TARGET))) && (TALENT:TARGET:酒耐性 == -2 || CFLAG:TARGET:妊娠自覚状態 == 1)
		PRINTFORMC 접시를 내놓는다.[{LOCAL,3}]
	ELSEIF LOCAL == 304 && CFLAG:TARGET:350 == 46
		PRINTFORMC 플레이에 참여한다[{LOCAL,3}]
	ELSEIF LOCAL == 315
		IF GETBIT(TALENT:TARGET:性別,1)
			PRINTFORMC 페니스 애무[{LOCAL,3}]
		ELSEIF CFLAG:MASTER:イタズラ
			PRINTFORMC 가슴 쓰다듬기[{LOCAL,3}]
		ELSE
			PRINTFORMC 클리토리스 애무[{LOCAL,3}]
		ENDIF
	ELSEIF LOCAL == 330 ;custom code
		PRINTFORMC 치마를 뒤집는다 [{LOCAL,3}]
	ELSEIF LOCAL == 350
		IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
			LOCALS '= @"장난 시작"
		ELSEIF (CFLAG:現在位置 == 連れ込み宿 || CFLAG:現在位置 == ラブホ)&& !CFLAG:うふふ
			LOCALS '= @"더 가까이 와."
		ELSEIF CFLAG:現在位置 == 寺子屋
			LOCALS '= @"특별한 수업을 시작한다"
		ELSEIF CFLAG:現在位置 == OMANEKIBEYA() && TARGET == 43
			LOCALS '= @"연모 연습"
			;ベッドがない
			;室外
		ELSEIF !INROOM(CFLAG:PLAYER:現在位置)
			LOCALS '= @"더 가까이 다가와라"
		ELSE
			LOCALS '= @"아래로 밀어내라"
		ENDIF
		SIF GET_TARGETNUM() > 1
			LOCALS = %LOCALS%모든 것과 함께
		PRINTFORMC %LOCALS%[{LOCAL,3}]
	ELSEIF LOCAL == 351
		IF CFLAG:TARGET:同行
		ELSEIF COM_351_CONDITION(TARGET) == 1
			SETCOLOR 100, 100, 100
		ELSEIF CAN_IRAI_CHARA_GO_TOGATHER(TARGET)
			SETCOLOR C_GREEN
		ELSE
			SETCOLOR C_AQUA
		ENDIF
		;Custom code
		PRINTFORMC %TRAINNAME_TR(LOCAL)%[{LOCAL,3}]
		RESETCOLOR
	ELSEIF LOCAL == 491
		SETCOLOR 255, 179, 167
		PRINTFORMC %TRAINNAME_TR(LOCAL)%[{LOCAL,3}]
		RESETCOLOR
	ELSEIF LOCAL == 360 && BASE:MASTER:気力 <= 0
		PRINTFORMC 자비를 구하라[{LOCAL,3}]
	ELSEIF LOCAL == 366 && GROUPMATCH(IRAI_CHARA_TO_PROGRESS(TARGET), "依頼達成", "依頼失敗")
		SETCOLOR C_GREEN
		PRINTFORMC 요청 완료[{LOCAL,3}]
		RESETCOLOR
	ELSEIF LOCAL == 366 && GROUPMATCH(IRAI_CHARA_TO_PROGRESS(TARGET), "依頼未受託")
		SETCOLOR C_GREEN
		PRINTFORMC 요청을 듣는다[{LOCAL,3}]
		RESETCOLOR
	ELSEIF LOCAL == 367
		SETCOLOR C_GREEN
		PRINTFORMC %NAME_IRAI_ACT()%[{LOCAL,3}]
		RESETCOLOR
	ELSEIF LOCAL == 368
		PRINTFORMC %JOIN_IN_TR(FreeAct_Name(TARGET))%[{LOCAL,3}]
	ELSEIF LOCAL == 337
		;qol custom code
		IF QOL_INTERACTABLE_CHILDREN() == 1
			PRINTFORMC %CHILD_INTERACT_TR(子供ふれあい名称(nChildrenPresent:0), nChildrenPresent:0)%[{LOCAL,3}]
		ELSE
			PRINTFORMC %TRAINNAME_TR(LOCAL)%[{LOCAL,3}]
		ENDIF
	ELSEIF LOCAL == 417 && TIME >= 660 && TIME <= 900
		PRINTFORMC 낮잠을 잔다[{LOCAL,3}]
	ELSEIF LOCAL == 417
		PRINTFORMC 졸았다 [{LOCAL,3}]
	ELSEIF LOCAL == 420
		SELECTCASE MAIN_MAP
			CASE 0
				LOCALS '= @"루코토"
			CASE 3
				LOCALS '= @"요정 메이드"
			CASE 9
				LOCALS '= @"좀비 요정"
		ENDSELECT
		PRINTFORMC %LOCALS%[{LOCAL,3}]과 대화한다.
	ELSEIF LOCAL == 422 && CFLAG:MASTER:現在位置 == 103
		PRINTFORMC 신의 말을 듣는다[{LOCAL,3}]
	ELSEIF LOCAL == 431 && (CFLAG:MASTER:現在位置 == 24 || CFLAG:MASTER:現在位置 == 441)
		PRINTFORMC 온천에서 휴식한다[{LOCAL,3}]
	ELSEIF LOCAL == 431 && (CFLAG:MASTER:現在位置 == 大衆浴場 || CFLAG:MASTER:現在位置 == 940)
		PRINTFORMC 목욕을 한다[{LOCAL,3}]
	ELSEIF LOCAL == 431 && OPENPLACE(CFLAG:MASTER:現在位置) && ITEM:簡易プール && !BATHCHECK(CFLAG:MASTER:現在位置)
		PRINTFORMC 풀에서 휴식한다[{LOCAL,3}]
		;		ELSEIF LOCAL == 405 && CFLAG:TARGET:同行 && TARGET
		;			PRINTFORMC Date[{LOCAL,3}]
	ELSEIF LOCAL == 410 && OUTROOF(CFLAG:MASTER:現在位置) && DAY:2 == 4
		PRINTFORMC 눈 치우기[{LOCAL,3}]
	ELSEIF LOCAL == 415 && TIME >= 300 && TIME <= 540 && (CFLAG:MASTER:現在位置 == 9 || CFLAG:MASTER:現在位置 == OMANEKIBEYA()) && !FLAG:70 ;com custom code
		PRINTFORMC 아침 식사 제공[{LOCAL,3}]
	ELSEIF LOCAL == 415 && TIME >= 660 && TIME <= 900 && (CFLAG:MASTER:現在位置 == 9 || CFLAG:MASTER:現在位置 == OMANEKIBEYA()) && !FLAG:70 ;com custom code
		PRINTFORMC 점심 식사 제공[{LOCAL,3}]
	ELSEIF LOCAL == 415 && TIME >= 1020 && TIME <= 1200 && (CFLAG:MASTER:現在位置 == 9 || CFLAG:MASTER:現在位置 == OMANEKIBEYA()) && !FLAG:70 ;com custom code
		PRINTFORMC 저녁 식사를 대접한다[{LOCAL,3}]
	ELSEIF LOCAL == 415 && TIME >= 1200 && TIME <= 1440 && (CFLAG:MASTER:現在位置 == 9 || CFLAG:MASTER:現在位置 == OMANEKIBEYA()) && !FLAG:70 ;com custom code
		PRINTFORMC 저녁 식사를 준비한다[{LOCAL,3}]
	ELSEIF LOCAL == 415 && TIME >= 0 && TIME <= 180 && (CFLAG:MASTER:現在位置 == 9 || CFLAG:MASTER:現在位置 == OMANEKIBEYA()) && !FLAG:70 ;com custom code
		PRINTFORMC 저녁 식사를 대접한다[{LOCAL,3}]
	ELSEIF LOCAL == 415 && CFLAG:TARGET:行動 == 5 && !FLAG:70 ;com custom code
		PRINTFORMC 식사 시간[{LOCAL,3}]
	ELSEIF LOCAL == 443
		;custom code to translate unique coms
		PRINTFORMC %UNIQUE_COM_TR(NAME_UNIQUE_COM(TARGET))%[{LOCAL,3}]
	ELSEIF LOCAL == 448
		IF HUNTING_SPOT(CFLAG:MASTER:現在位置) && MUSHITORI_SPOT(CFLAG:MASTER:現在位置)
			IF GET_MUSHIDATA(MUSHI_TRAP, "場所") && GROUPMATCH(GET_MUSHIDATA(MUSHI_TRAP, "場所"), TRAP_POINT:1, TRAP_POINT:2, TRAP_POINT:3)
				PRINTFORMC 함정[{LOCAL,3}]을 확인한다.
			ELSE
				PRINTFORMC %TRAINNAME_TR(LOCAL)%[{LOCAL,3}]
			ENDIF
		ELSEIF GROUPMATCH(CFLAG:MASTER:現在位置, TRAP_POINT:1, TRAP_POINT:2, TRAP_POINT:3)
			PRINTFORMC 함정[{LOCAL,3}]을 확인한다.
		ELSEIF GROUPMATCH(CFLAG:MASTER:現在位置, GET_MUSHIDATA(MUSHI_TRAP, "場所"))
			PRINTFORMC 버그 트랩을 확인한다[{LOCAL,3}]
		ELSEIF MUSHITORI_SPOT(CFLAG:MASTER:現在位置)
			PRINTFORMC 버그 함을 설치한다.[{LOCAL,3}]
		ELSE
			PRINTFORMC %TRAINNAME_TR(LOCAL)%[{LOCAL,3}]
		ENDIF
	ELSEIF LOCAL == 464
		SELECTCASE CFLAG:MASTER:現在位置
			CASE 210
				IF MAIN_MAP == 8
					PRINTFORMC 케이블웨이로 이동한다.[{LOCAL,3}]
				ELSE
					PRINTFORMC Scenic Hill[{LOCAL,3}]로 이동한다.
				ENDIF
			CASE 820
				IF MAIN_MAP == 2
					PRINTFORMC 북쪽 거리[{LOCAL,3}]로 가다
				ELSE
					PRINTFORMC 플라자로 이동한다[{LOCAL,3}]
				ENDIF
			CASE 570
				PRINTFORMC 임계의 길로 이동한다[{LOCAL,3}]
			CASE 712,760 ;custom code, too long
				PRINTFORMC 블즈잉 헬의 잔해로[{LOCAL,3}]
			CASE 928,970 ;custom code, too long
				PRINTFORMC 언더그라운드 기지 센터로 이동한다[{LOCAL,3}]
			CASE 732, 770 ;addition custom code
				PRINTFORMC 산 정상으로 가기[{LOCAL,3}]
			CASE 801, 810 ;addition custom code
				PRINTFORMC 산기슭으로 가다[{LOCAL,3}]
			CASEELSE
				;Custom code
				LOCALS = %STR_TR(6000 + SPECIAL_AREAMOVE() / 10)%
				PRINTFORMC To %LOCALS%[{LOCAL,3}]
		ENDSELECT
	ELSEIF LOCAL == 482
		PRINTFORMC %CALLNAME:STALKED%를(을) 따라간다 [{LOCAL,3}]
	ELSEIF LOCAL == 626
		SIF FLAG:市場の神 == 10
			SETCOLOR 100, 250, 140
		PRINTFORMC %GIFTSHOP_NAME_TR(GIFTSHOP_NAME(CFLAG:MASTER:デート中, 1))%[{LOCAL,3}]
		RESETCOLOR
	ELSEIF LOCAL == 603 && PREVCOM == 603
		PRINTFORMC 손으로 이끌기[{LOCAL,3}]
	ELSEIF LOCAL == 698
		PRINTFORMC Go To %OMANEKI_PLACE()%[{LOCAL,3}]
		;kojo com
	ELSEIF LOCAL >= 270 && LOCAL < 300
		CALL KOJO_MESSAGE_SEND("COM", 0, TARGET, LOCAL - 270)
		PRINTFORMC %RESULTS%[{LOCAL,3}]
		printKojoCom = 1
	ELSEIF LOCAL == 606 ;addition custom code
		LOCAL:1 = 0
		FOR LOCAL:2,0,1000
		RESULT = 0
		TRYCALLFORM TSC_ABLE{LOCAL:2}()
		IF LOCAL:1 > 1
			LOCALS = %TRAINNAME_TR(606)%[{606,3}]
			BREAK
		ELSEIF RESULT == 2
			CALLFORM TSC_NAME{LOCAL:2}
			LOCALS = %RESULTS%[{606,3}]
			LOCAL:1 ++
		ENDIF
		NEXT
		PRINTFORMC %LOCALS%
		;Custom code
	ELSEIF STRLENS(TRAINNAME_TR(LOCAL))
		;custom code
		SIF LOCAL == 401 ;highlight collection
			SETCOLOR 100, 250, 140
		PRINTFORMC %TRAINNAME_TR(LOCAL)%[{LOCAL,3}]
		RESETCOLOR
	ENDIF
ENDIF
NEXT
;addition custom code
CALL Add_Custom_List_COMS
CALL ADD_TOUCH_SET_OLD_FIELDS(1)
RESULT = 0
;自動実行中なら下記表示をスキップ
SIF !TFLAG:100
	GOTO SKIP
PRINTL
;com custom code
;ATW edits
CALL PRINTBUTTON_EX(TFLAG:USERCOM非表示 ? "▽[＋][Commands]" # "▼[－][Commands]", 3002, TFLAG:USERCOM非表示)
PRINTPLAIN  == 
CALL QOL_EXTRA_COMMAND_BUTTONS() ;qol custom code
;--------------------
SIF TFLAG:USERCOM非表示
	GOTO SKIP
;SIF !GETBIT (FLAG:USERCOMフィルタ, 12)
;	PRINTC 履歴表示[800]
IF GETBIT(FLAG:瞬間移動, 1) && !CFLAG:MASTER:同行
	SETCOLOR 0x5090FF
ELSE
	SETCOLOR C_GREEN
ENDIF
RESULT = 0
CALL COM_ABLE400
SIF RESULT
	PRINTC 이동한다(400)[400]
RESULT = 0
RESETCOLOR
CALL COM_ABLE405
IF RESULT
	IF CFLAG:TARGET:同行 && TARGET
		PRINTC 날짜[405]
	ELSE
		PRINTC 나가기[405]
	ENDIF
ENDIF
RESULT = 0
IF GETBIT(FLAG:瞬間移動, 0) && !FLAG:デート相手
	SETCOLOR 0x5090FF
ELSE
	SETCOLOR C_GREEN
ENDIF
RESULT = 0
CALL COM_ABLE604
SIF RESULT
	PRINTC 탐험한다[604]
RESETCOLOR
SIF !GETBIT (FLAG:USERCOMフィルタ, 0)
	PRINTC 더러움[801]
SIF !CFLAG:添い寝中 && GET_TARGETNUM() > 1 && !GETBIT (FLAG:USERCOMフィルタ, 1)
	PRINTC 파트너 변경 [802]
SIF !GETBIT (FLAG:USERCOMフィルタ, 2)
	PRINTC 스킬[803]
SIF !GETBIT (FLAG:USERCOMフィルタ, 3)
	PRINTC 능력 (마스터)[804]
SIF !GETBIT (FLAG:USERCOMフィルタ, 4)
	PRINTC 아이템 목록[805]
;SIF !GETBIT(FLAG:USERCOMフィルタ, 11) ;com custom code, moved above
;	PRINTC Command Filter[809]
SIF FLAG:1005
	PRINTC 설정[810]
SIF TALENT:MASTER:居場所察知 && (GETBIT(TALENT:MASTER:居場所察知, 1) || !(CFLAG:うふふ || CFLAG:イタズラ || CFLAG:添い寝中)) ;qol custom code, at_home check removed
	PRINTC 위치짐작 [811]
SIF TALENT:MASTER:汚部屋察知 && AT_HOME(MASTER) && !(CFLAG:うふふ || CFLAG:イタズラ || CFLAG:添い寝中)
	PRINTC 청결 감각[812]
SIF GETBIT(TALENT:MASTER:劣情察知, 1) && !CFLAG:うふふ && !CFLAG:添い寝中 && !CFLAG:MASTER:イタズラ ;bug fix
	PRINTC 욕망 감지기[813]
SIF TALENT:MASTER:パンティーセンス && !CFLAG:うふふ && !CFLAG:添い寝中 && !CFLAG:MASTER:イタズラ ;addition custom code
	PRINTC 팬티 센스[814]
IF FLAG:70 && CFLAG:TARGET:344 == 0 && !CFLAG:TARGET:睡眠 && AT_HOME(MASTER) == 1 && TARGET > 0 && !CFLAG:うふふ && CFLAG:行動 != 5 ;qol custom code, disallow while they're working
	IF TFLAG:運搬 == 0 || TFLAG:運搬 != TARGET
		PRINTC 운반하다[815]
	ELSE
		PRINTC 내려놔라[815]
	ENDIF
ENDIF
SIF FLAG:70 && TALENT:MASTER:集合 && AT_HOME(MASTER) == 1 && !CFLAG:うふふ
	PRINTC 모인다[817]
SIF GATHERING_SPOT(CFLAG:MASTER:現在位置) != 0
	PRINTC 채집 목록[818]
;custom code
SIF FISHING_SPOT(CFLAG:MASTER:現在位置) != 0 && ABL:MASTER:教養 >= 3
	PRINTC 낚시 목록[890]
;addition custom code
IF PLACE_SENTO(CFLAG:MASTER:現在位置) == 9 && CFLAG:64:現在位置 != CFLAG:MASTER:現在位置 && !FIRSTTIME("YuugiBath", 1, 64)
	SETCOLOR 100, 250, 140
	PRINTC 유우기[891]를 호출한다.
	RESETCOLOR
ENDIF
;addition custom code, from KR version
IF CFLAG:MASTER:初期位置 == CFLAG:MASTER:現在位置 || (CFLAG:MASTER:現在位置 == CFLAG:TARGET:初期位置 && (IS_FALLEN(TARGET) || !SHIRAHU(TARGET)))
	IF !FLAG:自室施錠
		PRINTC 문을 잠근다[991]
	ELSE
		SETCOLOR 100, 250, 140
		PRINTC 문을 해제한다[991]
	ENDIF
	RESETCOLOR
ENDIF
SIF CAN_DATE(TARGET)
	PRINTC 데이트에 초대하기[992]
SIF CFLAG:MASTER:現在位置 == CFLAG:MASTER:初期位置
	PRINTC 달력[993]
SIF FLAG:パンツ輸送
	PRINTC 도난당한 팬티를 확인한다[994]
IF AT_HOME(MASTER) && GETBIT(FLAG:瞬間移動, 1)
	SETCOLOR 0x5090FF
ELSEIF !AT_HOME(MASTER) && GETBIT(FLAG:瞬間移動, 0)
	SETCOLOR 0x5090FF
ENDIF
SIF !GETBIT(FLAG:USERCOMフィルタ, 13)
	PRINTC 자동 TSP 이동[995]
RESETCOLOR
;自動掃除
IF AT_HOME(MASTER) && GETBIT(FLAG:自動掃除, 1)
	SETCOLOR 0x5090FF
ELSEIF !AT_HOME(MASTER) && GETBIT(FLAG:自動掃除, 0)
	SETCOLOR 0x5090FF
ENDIF
SIF !GETBIT(FLAG:USERCOMフィルタ, 14) & AT_HOME(MASTER) && !CFLAG:うふふ && !CFLAG:添い寝中 && !CFLAG:MASTER:イタズラ ;bug fix
	PRINTC 자동 청소[996]
RESETCOLOR
PRINTL
;custom, moved to its own line due to length
IF CAN_DATE(TARGET)
	LOCAL = QOL_RETURN_HOME_TIME_REQUIRED() ;qol custom code
	LOCALS = ({LOCAL}분)
	SIF LOCAL < 10
		SETCOLOR 100, 250, 140
	;ugly but this gets super long otherwise. perhaps move it to its own dedicated line instead?
	;PRINTFORMC %SUBSTRING(@"Invite To %GET_MAPNAME(MAIN_MAP)%", 0, GETCONFIG("PRINTCの文字数") - (5 + STRLENS(LOCALS)) )%%LOCALS%[990]
	PRINTFORMC %BL(6)%에게 초대하기 %GET_MAPNAME(MAIN_MAP)%%LOCALS%[990]
	RESETCOLOR
	PRINTL
ENDIF
IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
	SIF EQUIP:0 && TARGET && CFLAG:うふふ
		PRINTC 옷을 벗긴다[820]
	IF EQUIP:0 && TARGET && EQUIP:TARGET:下半身下着２ != 0 && GET_INT(TARGET, "下半身下着_ずらし可能", EQUIP:TARGET:下半身下着２, "コレクション表示可能") ;addition custom code, only if collectable
		IF !PANTS_ALREADY_COLLECTED()
			FONTBOLD
			SETCOLOR COLOR("PINK")
			PRINT  
		ENDIF
		SIF !GETBIT (FLAG:USERCOMフィルタ, 60) ;com custom code
			PRINTC 팬티 훔치기[821]
		RESETCOLOR
		FONTREGULAR
	ENDIF
	IF (TEQUIP:5 == 0 || EQUIP:TARGET:上半身下着１) && CFLAG:うふふ
		SIF EQUIP:0 && TARGET && EQUIP:TARGET:上半身下着１ != 0 && (EQUIP:TARGET:着物 != 0 || EQUIP:TARGET:上半身上着２ != 0 || EQUIP:TARGET:ボディースーツ == 3)
			PRINTC 상의와 하의를 제거한다[822]
	ENDIF
	IF TEQUIP:5 == 0 && CFLAG:うふふ
		SIF EQUIP:0 && TARGET && (EQUIP:TARGET:上半身下着１ || EQUIP:TARGET:上半身下着２ || EQUIP:TARGET:下半身下着１ || EQUIP:TARGET:下半身下着２ || EQUIP:TARGET:レオタード || EQUIP:TARGET:ボディースーツ) && (EQUIP:TARGET:上半身上着１ || EQUIP:TARGET:上半身上着２ || EQUIP:TARGET:ワンピース || EQUIP:TARGET:着物 || EQUIP:TARGET:外衣)
			PRINTC 속옷으로 탈의한다[823]
	ENDIF
	;addition custom code
	SIF CFLAG:うふふ || CFLAG:MASTER:イタズラ == 2
		PRINTC 옷을 벗긴다[892]
	IF (MEASUREMENTCHECK(CFLAG:TARGET:現在位置) || MEASURE_DEBUG) ;custom code
		IF CFLAG:TARGET:身体測定 != 2
			PRINTC 신체 측정 [824]
		ELSE
			PRINTC 신체 치수 측정 완료[824]
		ENDIF
	ENDIF
	SIF TFLAG:パンツ収奪 && !GETBIT (FLAG:USERCOMフィルタ, 61) ;com custom code
		PRINTC 팬티를 가져간다[825]
	IF TARGET && !CFLAG:Hypno_Orgasm_Denial ;custom code
		IF TEQUIP:焦らしモード
			PRINTC 부정 종료[826]
		ELSE
			PRINTC 부정 모드[826]
		ENDIF
	ENDIF
	IF CFLAG:うふふ
		PRINTC 장난 그만둬[999]
	ELSE
		PRINTC 시간 재개[999]
	ENDIF
ELSEIF FLAG:70 == 1 && CFLAG:TARGET:344 == 1
	PRINTC 시간 재개[999]
	IF CFLAG:うふふ
		SIF EQUIP:0 && TARGET
			PRINTC 옷 벗기 [820]
		;addition custom code
		SIF CFLAG:うふふ
			PRINTC 옷을 벗는다[892]
		IF CFLAG:うふふ == 2
			IF TFLAG:抱きつきモード
				PRINTC 떨쳐내다[850]
			ELSE
				PRINTC 빠져나간다[850]
			ENDIF
			IF !CFLAG:Hypno_Orgasm_Denial ;custom code
				IF TEQUIP:焦らしモード
					PRINTC 부정 종료[826]
				ELSE
					PRINTC 부정 모드[826]
				ENDIF
			ENDIF
		ELSE
			PRINTC 세션 종료[999]
		ENDIF
	ENDIF
ELSEIF (CFLAG:うふふ && !CFLAG:イタズラ) || FREE_USE_ACTIVE() ;custom code, enable stripping clothes when in Free Use mode
	SIF EQUIP:0 && TARGET && (!(TFLAG:102 == 3 && TFLAG:抱きつきモード) || FREE_USE_ACTIVE()) ;custom code, enable stripping clothes when in Free Use mode
		PRINTC 옷 벗기[820]
	;addition custom code
	SIF CFLAG:うふふ
		PRINTC 옷을 벗긴다[892]
	IF TARGET && !CFLAG:Hypno_Orgasm_Denial ;custom code
		IF TEQUIP:焦らしモード
			PRINTC 부정 종료[826]
		ELSE
			PRINTC 부정 모드[826]
		ENDIF
	ENDIF
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		PRINTC 시간 재개[999]
	ELSEIF CFLAG:うふふ == 2
		IF TFLAG:抱きつきモード
			PRINTC 몸을 흔들어 떨쳐내다 [850]
		ELSE
			PRINTC 슬립 아웃[850]
		ENDIF
	ELSEIF CFLAG:うふふ == 1
		PRINTC 세션 종료[999]
	ENDIF
ENDIF
IF CFLAG:イタズラ
	SIF CFLAG:イタズラ < 2 && (EXP:TARGET:睡眠姦経験 > 100 || TCVAR:睡眠薬強度 || TCVAR:睡眠深度 > 2000)
		PRINTC 옷의 단추를 푼다[830]
	;addition custom code
	SIF CFLAG:うふふ
		PRINTC 옷을 벗긴다[892]
	IF CFLAG:イタズラ < 2
		PRINTC 장난 그만둬[999]
		IF EQUIP:0 && TARGET && EQUIP:TARGET:下半身下着２ != 0 && GET_INT(TARGET, "下半身下着_ずらし可能", EQUIP:TARGET:下半身下着２, "コレクション表示可能") ;addition custom code, only if collectable ;bug fix
			IF !PANTS_ALREADY_COLLECTED()
				FONTBOLD
				SETCOLOR COLOR("PINK")
				PRINT  
			ENDIF
			SIF !GETBIT (FLAG:USERCOMフィルタ, 60) ;com custom code
				PRINTC 팬티 훔치기[821]
			RESETCOLOR
			FONTREGULAR
		ENDIF
	ENDIF
	SIF CFLAG:イタズラ > 1
		PRINTC 잠꾸러기 강간 중단[999]
ENDIF
IF CFLAG:添い寝中
	PRINTC 함께 잠드는 것을 그만둬라[999]
ENDIF
IF CHK_DATENOW(CFLAG:MASTER:デート中) && (!FLAG:70 || CFLAG:TARGET:344 == 1) && !CFLAG:MASTER:うふふ && CFLAG:MASTER:現在位置 != OMANEKIBEYA() && TFLAG:妄想中 != 1
	IF !FLAG:デート相手 && GETBIT(FLAG:瞬間移動, AT_HOME(MASTER)) && QOL_RETURN_HOME_TIME_REQUIRED() * 3 < BASE:MASTER:TSP
		SETCOLOR 0x5090FF
		PRINTFORMC 집에 돌아가기(({QOL_RETURN_HOME_TIME_REQUIRED() * 3}TSP)) [999]
		RESETCOLOR
	ELSE
		SIF QOL_RETURN_HOME_TIME_REQUIRED() < 10
			SETCOLOR 100, 250, 140
		;custom code
		PRINTFORMC \@ FLAG:デート相手 ? 종료 날짜 # 집으로 돌아가기 \@(({QOL_RETURN_HOME_TIME_REQUIRED()})분)[999]
		RESETCOLOR
	ENDIF
ENDIF
$SKIP
IF PREVCOM >= 0
	IF TFLAG:151
		STR:0 = %STR_TR(1500 + TFLAG:151)%
	ELSE
		STR:0 = %TRAINNAME_TR(PREVCOM)%
	ENDIF
	;addition custom code, custom commands
	IF TFLAG:151 == ADD_CUSTOM_SCOM
		STR:0 '= Add_Custom_SCOM_Name(ADD_PREV_SELECTED_SCOM)
	ELSEIF PREVCOM == ADD_CUSTOM_COM
		STR:0 '= Add_Custom_COM_Name(ADD_PREVCUSTOMCOM)
	ENDIF
ENDIF
SIF PREVCOM == 304 && CFLAG:TARGET:350 == 49 && TALENT:TARGET:酒耐性 != -2
	STR:0 = Share Drink
SIF PREVCOM == 304 && CFLAG:TARGET:350 == 49 && (TALENT:TARGET:酒耐性 == -2 || CFLAG:TARGET:妊娠自覚状態 == 1)
	STR:0 = Serve Dish
SIF PREVCOM == 304 && CFLAG:TARGET:350 == 51
	STR:0 = Look After
;kojo com
IF INRANGE(PREVCOM, 270, 299)
	CALL KOJO_MESSAGE_SEND("COM", 0, TARGET, PREVCOM - 270)
	STR:0 '= RESULTS
ENDIF
IF !TFLAG:USERCOM非表示
	IF PREVCOM >= 0 && PREVCOM < 300
		PRINTL 
		PRINT 마지막 훈련 명령: 
		PRINTS STR_TR(0)
		SIF TFLAG:150
			PRINT (어시스턴트)
		PRINTL >
	ELSE
		PRINTL 
		PRINTFORM <Last Command: %STR:0%>
	ENDIF
ENDIF
CALL FORCE_COMMAND() ;Custom code, force any commands set through nForceCommand.
