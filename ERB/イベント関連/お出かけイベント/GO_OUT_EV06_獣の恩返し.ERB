; Modular ERB: お出かけイベント/GO_OUT_EV06_獣の恩返し.ERB

@GO_OUT_CONDITION_EV6
IF !RAND:10
	CALL GO_OUT_EV6
ELSE
	RETURN 0
ENDIF

@GO_OUT_EV6
#DIM PAIR
#DIM LUCK
#DIM SPENT
#DIMS PREY
#DIMS PREY_TR ;custom code
SPENT = 1
;addition custom code
PREY = %SPLIT_G("狸:狐:兎:猫")%
SELECTCASE PREY
	CASE "狸"
		PREY_TR = tanuki
	CASE "狐"
		PREY_TR = fox
	CASE "兎"
		PREY_TR = rabbit
	CASE "猫"
		PREY_TR = cat
ENDSELECT
PAIR = FLAG:デート相手
LUCK = CFLAG:MASTER:今日の運勢 / 100 + TFLAG:幸運補正
PRINTFORML 가는 길에 사냥꾼의 함정에 걸린 %PREY_TR%를 발견한다.
IF (GROUPMATCH(PAIR, [[藍]], [[典]]) && PREY == "狐") || (PAIR == [[マミゾウ]] && PREY == "狸") || (GROUPMATCH(PAIR, [[橙]], [[お燐]], [[星]]) && PREY == "猫") || (GROUPMATCH(PAIR, [[鈴仙]], [[てゐ]], [[レイセン]], [[清蘭]], [[鈴瑚]]) && PREY == "兎") ;addition custom code
	PRINTFORMW %CALLNAME:PAIR%은(는) 당신을 보는 것이 마치 %HE_SHE(PAIR)%이 무언가 하고 싶은 말을 하려는 것 같다.
	PRINTFORML 선택의 여지가 없다고 느끼며, 당신은 %PREY_TR%를 함정에서 풀어준다.
	PRINTFORML 그러고 나서 %CALLNAME:PAIR%에게 감사 인사를 한 뒤 도망간다...
	PRINTFORMW 
	RETURN 1
ELSEIF (GROUPMATCH(PAIR, [[藍]], [[典]]) && PREY == "狸") || (PAIR == [[マミゾウ]] && PREY == "狐")
	PRINTFORMW %CALLNAME:PAIR%는 함정을 재빨리 흘겨본 후 걸어간다.
	PRINTFORML 당신은 %HIM_HER(PAIR)%를 뒤쫓아간다...
	PRINTFORMW 
	RETURN 1
ELSEIF PAIR ;addition custom code
	PRINTFORMW %CALLNAME:PAIR%는 %HIS_HER(TARGET)%의 눈에 동정심을 담아 붙잡힌 동물을 바라보지만 선택은 당신에게 맡긴다.
	PRINTFORML
ENDIF
PRINTFORML 아무것도 하지 않으면 머지않아 반드시 들릴 테지만...
;CALL ASK_M("Leave it alone", 1, "Help it out", 1)
CALL ASK_M("그냥 내버려 두어라", 1, "도와줘", 1, "그것을 도살해라!", 1) ;addition custom code
IF !RESULT
	PRINTFORML 당신은 그것이 안쓰럽지만, 할 수 있는 것은 없다. 다른 사냥꾼의 게임에 간섭할 자격은 없다.
	IF PAIR
		;every character feels reluctant? u wot?
		PRINTFORM %CALLNAME:PAIR%도 그것에 대해 매우 저항하는 것 같았지만 결국 당신은
	ELSE
		PRINTFORM 당신은
	ENDIF
	PRINTFORML %COND_STR(@" two", PAIR)%은 동물을 내버려 두고 서둘러 집으로 돌아가라.
	PRINTFORMW
	RETURN 1
ELSEIF RESULT == 2 ;addition custom code
	CALL Add_Hunt_Ev(LUCK, PREY_TR, PAIR)
	RETURN 1
ENDIF
PRINTFORML 당신은 불쌍한 동정심을 느끼고 그것을 자유롭게 해주기로 결정한다.
PRINTFORMW 부상을 입었지만, 약간의 신속한 의료 조치로 곧 걸을 수 있게 될 것이다.
PRINTDW .........
PRINTDW ......
PRINTDW ...
IF LUCK > 4
	PRINTFORMW 치료 후, %PREY_TR%은(는) 수풀 뒤로 사라진다.
	PRINTFORML ...그리고 입에 무언가를 물고 너에게 돌아온다.
	PRINTFORML 자기 가죽을 살린 것에 대한 보상이겠지.
	;最高級素材をランダム
	LOCAL:0 = 625, 630, 639, 640, 643, 646
	CALL GET_ITEM(ITEMNAME:(LOCAL:(RAND:6)), 1, 2)
	PRINTFORML 
	IF !(GO_OUT_FLAG_06 || PAIR || GETBIT(TFLAG:一日一回, 9)) && LUCK > 7
		GO_OUT_FLAG_06 = DAY
		SETBIT TFLAG:一日一回, 9
		;addition custom code
		LOCALS = 化け%PREY%
		SIF PREY == "兎"
			LOCALS = \@ ((GETTIME()/1000000000) % 100) % 3 == 1 && !RAND:7 ? 販売員 # イナバ \@
		;--------------------
		CALL MAKE_YUKIZURI(RANDOM_CHARANUM, LOCALS)
		CFLAG:RANDOM_CHARANUM:よく行く地域 = (MAIN_MAP == 5) ? 7 # 5
		CFLAG:RANDOM_CHARANUM:好感度 = REQUIRED_FAVOR_思慕
		CFLAG:RANDOM_CHARANUM:信頼度 = REQUIRED_TRUST_思慕
		ABL:RANDOM_CHARANUM:親密 = REQUIRED_AFFECTION_思慕
		ABL:RANDOM_CHARANUM:従順 = REQUIRED_AFFECTION_思慕
		;addition custom code
		SELECTCASE PREY
			CASE "狸"
				LOCALS = bakedanuki
			CASE "狐"
				LOCALS = bakegitsune
			CASE "兎"
				LOCALS = youkai rabbit
			CASE "猫"
				LOCALS = bakeneko
		ENDSELECT
		CSTR:RANDOM_CHARANUM:モブ子プロフィール１ = %CALLNAME:RANDOM_CHARANUM%：At %PRINT_DATE_F(DAY)%, you've saved this %LOCALS% at %GET_MAPNAME(CFLAG:MASTER:デート中)% on your way home
		BASE:RANDOM_CHARANUM:体力 -= 50
		CFLAG:RANDOM_CHARANUM:面識 = 1 ;custom code
		CALL QoL_Info_Write(RANDOM_CHARANUM, Qol_Dates_Meet) ;qol custom code
		PRINTFORML ...오 마이 갓, 잠깐만?! 이 %PREY_TR%에게 뭔가 상당히 이상한 점이 있다...!
		PRINTFORMW 정말 놀랍네! 네 눈앞에서 %PREY_TR%가 귀여운 키모노미걸로 변해버렸어!
		CALL CHARA_STATE_TR(RANDOM_CHARANUM) ;custom code
		DRAWLINE
		CALL COLORMESSAGE(@"처녀성 상실 기록: %CSTR:RANDOM_CHARANUM:処女喪失履歴%",C_PINK,1)
		PRINTFORMW 
		PRINTFORML %PREY_TR% 소녀가 자신을 %CALLNAME:RANDOM_CHARANUM%이라고 소개하며, 당신에게 여러 번 감사를 표한다.
		SIF !INRANGE(TIME, 540, 1140)
			GOTO SKIP
		PRINTFORML 그녀는 대신 당신을 집까지 따라가겠다고 말한다...
		CALL ASK_M("일단은 각자 가는 게 좋겠어", 1, "그녀가 당신을 따르게 하라", 1)
		IF RESULT
			FLAG:デート相手 = RANDOM_CHARANUM
			CFLAG:RANDOM_CHARANUM:現在位置 = CFLAG:MASTER:現在位置
			CFLAG:RANDOM_CHARANUM:同行 = 300
			CFLAG:MASTER:同行 = 300
			CFLAG:RANDOM_CHARANUM:デート中 = MAIN_MAP ;bug fix, set date flag
			CFLAG:MASTER:デート中 = MAIN_MAP ;bug fix, set date flag
			CFLAG:RANDOM_CHARANUM:眠気覚まし = 300
			CFLAG:RANDOM_CHARANUM:拠点来訪 = 1
			TARGET = RANDOM_CHARANUM ;custom code
			PRINTFORML 너는 그녀의 제안을 받아들이고 그녀를 네 집으로 데려오기로 결정한다.
			PRINTFORML 그녀는 너에게 매우 열정적인 시선을 보내고 있다...
			GOTO END_PHASE
		ENDIF
		PRINTFORML 그녀는 아직 부상을 입었기에, 당신은 일단 그녀를 떠나보내기로 결정한다.
		$SKIP
		PRINTFORMW 떠나기 전에, %CALLNAME:RANDOM_CHARANUM%이 당신에게 언제든지 \@ CFLAG:RANDOM_CHARANUM:よく行く地域 == 5 ? 마법의 숲 # 요괴 산 기슭 \@에 놀러 오라고 말한다...
		CALL COLORMESSAGE(@"당신은 %CALLNAME:RANDOM_CHARANUM%와 친구가 되었다!",C_YELLOW,2,1)
		CFLAG:RANDOM_CHARANUM:現在位置 = SUKIMA()
		CFLAG:RANDOM_CHARANUM:出禁 = 2 ;bug fix custom code
		CFLAG:RANDOM_CHARANUM:デート中 = -1 ;bug fix, disable dating flag
		GOTO END_PHASE
	ENDIF
	PRINTFORML 당신이 구한 %PREY_TR% 분들에게 배웅받으며, 당신%COND_STR(@" and %CALLNAME:PAIR%", PAIR)%은 집으로 돌아간다.
ELSE
	IF PAIR
		;addition custom code
		PRINTFORML 당신이 그것을 치료하느라 바쁠 때, %PREY_TR%은(는) 허둥지둥하며 \@ PREY == "兎" ? 당신을 찬다 # 당신에게 발톱으로 공격한다 \@.
		CALL RECOVER(MASTER, -200, "体力")
		WAIT
		PRINTFORML 게다가 상처에 모욕을 더하듯, 치료가 끝나자 %PREY_TR%는 그저 도망쳐 버렸다... 정말 배은망덕하군!
		SELECTCASE CFLAG:PAIR:性格傾向
			CASE 1
				PRINTFORMW %CALLNAME:PAIR%은(는) 걱정스러운 눈빛으로 당신을 바라본다.
			CASE 2, 3
				PRINTFORMW 당신의 비참한 상태를 보고, %CALLNAME:PAIR% \@ CFLAG:PAIR:性格傾向 == 2 ? 히스테릭하게 웃는다 # %SPLIT_G("giggles:sneers")% 경멸적으로 대한다 \@...
			CASE 4
				PRINTFORMW %CALLNAME:PAIR%의 얼굴에는 형언할 수 없이 무표정한 표정이 떠 있었다...
			CASE 5
				PRINTFORMW %CALLNAME:PAIR%는 그녀의 눈에 연민이 담긴 채 당신을 바라본다...
		ENDSELECT
		PRINTFORML 그 후, 두 사람은 아무것도 얻지 못하고 집으로 돌아간다.
	ELSE
		PRINTFORMW 네가 그것을 치료하는 대로 %PREY_TR%는 그저 달려나가 버렸다... 정말 은혜도 모르는군!
		PRINTFORML 떠날 수밖에 없게 되자, 너는 뒤에 누군가가 서 있다는 것을 깨달았다.
		PRINTFORMW 돌아보니 사냥꾼이 분노로 들끓으며 도망친 먹이를 보며 정말 격분하고 있었다...!
		PRINTFORML 
		PRINTFORML 네가 저지른 과실 때문에 머리를 맞는다!
		CALL RECOVER(MASTER, -100, "体力")
		WAIT
		PRINTFORML 게다가 너는 길게 설교도 듣는다...
		CALL RECOVER(MASTER, -300, "気力")
		WAIT
		PRINTFORML 지옥 같은 시간을 보낸 후, 당신은 힘겹게 집으로 향한다.
		SPENT = 4
	ENDIF
ENDIF
$END_PHASE
PRINTFORML {15 * SPENT}분
PRINTFORMW 
TIME += (15 * SPENT)
RETURN 1
