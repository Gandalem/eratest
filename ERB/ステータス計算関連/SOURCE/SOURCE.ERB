@SOURCE_CHECK
#DIM キスマーク反感
#DIM 欲求不満度上昇
#DIM 経験増加量
#DIM 体位保存
#DIM Ｖセックス保存
#DIM Ａセックス保存
#DIM CanUSex
#DIM PLAYER体位保存
#DIM PLAYERＶセックス保存
#DIM PLAYERＡセックス保存
#DIM PLAYER_CAN_U_SEX
#DIMS ゴム 
#DIM 中毒補正
;qol custom code, retrieve the highest index of SOURCE so we can quickly iterate over them
#DIM SOURCE_MAX
IF !SOURCE_MAX
	FOR LOCAL, VARSIZE("SOURCE")-1, -1, -1
		IF STRLENS(SOURCENAME:LOCAL)
			SOURCE_MAX = LOCAL + 1
			BREAK
		ENDIF
	NEXT
ENDIF

VARSET LOCAL
SELECTCOM = TFLAG:3
中毒補正 = 0
ゴム =
;調教SELECTCOMの時間経過
CALL TRACHECK_TIME


;NAS: Tutorial stuff
CALL TUTORIAL_START_TURN_ENTRIES
SIF (NASCheat:5)
	CALLF HESOYAM(MASTER)
;com custom code
nTrainMessageCalled = 0

;-------------------------------------------------
;身体接触の継続、解除
;-------------------------------------------------
CALL TOUCH_SUCCESSION(TARGET)

;-------------------------------------------------
;TRYCALLFORM TOUCH_SUCCESSION_{SELECTCOM}(TARGET)
;TOUCH_SUCCESSIONで行っている処理を各コマンドファイルに分散させることで、コマンド追加を容易にしたい
;-------------------------------------------------

IF !カウンター不発コマンド(SELECTCOM)
	;カウンター
	FOR LOCAL, 1, GET_TARGETNUM() + 1
		SIF TARGET:LOCAL <= 0
			CONTINUE
		CALL EVENT_COUNTER(TARGET:LOCAL,LOCAL)
		CALL EVENT_COUNTER_SOURCE(TARGET:LOCAL,LOCAL)
	NEXT
	;CALL EVENT_COUNTER_COMBINATION
ENDIF
;com custom code
; IF !GROUPMATCH(SELECTCOM, 350, 355, 400, 402, 404, 405, 420, 604, 699, 850)
	; FOR LOCAL, 1, GET_TARGETNUM() + 1
		; SIF TARGET:LOCAL <= 0
			; CONTINUE
		; IF TCVAR:(TARGET:LOCAL):カウンター行動
			; DEBUGPRINTFORML カウンター：{TARGET:LOCAL}-{TCVAR:(TARGET:LOCAL):カウンター行動}
			; TRYCALLFORM CCOMF{TCVAR:(TARGET:LOCAL):カウンター行動}(TARGET:LOCAL)
			; SIF !RESULT
				; DEBUGPRINTFORML CCOMF{TCVAR:(TARGET:LOCAL):カウンター行動}({TARGET:LOCAL})の呼び出しに失敗しました。
		; ENDIF
	; NEXT
; ENDIF

;addition custom code
FOR LOCAL,0,CHARANUM
	SIF NO:LOCAL != 0
		CONTINUE

	TEQUIP:LOCAL:50 = ADD_CHARA_WHO_TOUCHING_ACTOR(LOCAL, SET_P, SET_V)
	TEQUIP:LOCAL:51 = ADD_CHARA_WHO_TOUCHING_ACTOR(LOCAL, SET_P, SET_A)		
NEXT

FOR LOCAL,0,CHARANUM
	SIF NO:LOCAL == 0 ;addition custom code
		CONTINUE
	SIF CFLAG:LOCAL:現在位置 != CFLAG:MASTER:現在位置
		CONTINUE
	;性交フラグの設定
	;addition custom code, rewrite
	TEQUIP:LOCAL:50 = ADD_CHARA_WHO_INSERTED_IN(LOCAL, SET_V)
	TEQUIP:LOCAL:51 = ADD_CHARA_WHO_INSERTED_IN(LOCAL, SET_A)

	[SKIPSTART]
	IF TEQUIP:LOCAL:Ｖ接触部位 == 1
		TEQUIP:LOCAL:50 = MASTER
	ELSE
		TEQUIP:LOCAL:50 = -1
	ENDIF
	IF TEQUIP:LOCAL:Ａ接触部位 == 1
		TEQUIP:LOCAL:51 = MASTER
	ELSE
		TEQUIP:LOCAL:51 = -1
	ENDIF
	IF TEQUIP:LOCAL:UTouch == 1
		TEQUIP:LOCAL:55 = MASTER
	ELSE
		TEQUIP:LOCAL:55 = -1
	ENDIF	
	IF TEQUIP:LOCAL:Ｐ接触部位 == 6
		TEQUIP:PLAYER:50 = LOCAL
	ELSE
		TEQUIP:PLAYER:50 = -1
	ENDIF
	IF TEQUIP:LOCAL:Ｐ接触部位 == 7
		TEQUIP:PLAYER:51 = LOCAL
	ELSE
		TEQUIP:PLAYER:51 = -1
	ENDIF
	[SKIPEND]
NEXT

FOR LOCAL,0,CHARANUM
	SIF CFLAG:LOCAL:現在位置 != CFLAG:MASTER:現在位置
		CONTINUE
	;Ｐが入ってないなら体位リセット
	IF TEQUIP:LOCAL:50 < 0 && TEQUIP:LOCAL:51 < 0 && TEQUIP:LOCAL:55 < 0
		TEQUIP:LOCAL:体位 = 0
	;Ｖ性交中かつ前回Ｖ性交ならTEQUIPは復帰
	ELSEIF TEQUIP:LOCAL:50 >= 0 && TEQUIP:LOCAL:116
		SIF TEQUIP:LOCAL:116 != 3	;指挿入でない場合のみ適用
			TEQUIP:LOCAL:106 = TEQUIP:LOCAL:116
	;Ａ性交中かつ前回Ａ性交ならTEQUIPは復帰
	ELSEIF TEQUIP:LOCAL:51 >= 0 && TEQUIP:LOCAL:117
		SIF TEQUIP:LOCAL:117 != 3	;指挿入でない場合のみ適用
			TEQUIP:LOCAL:107 = TEQUIP:LOCAL:117
	;set u sex equips
	ELSEIF TEQUIP:LOCAL:55 >= 0 && TEQUIP:LOCAL:118
		SIF TEQUIP:LOCAL:118 != 3	;指挿入でない場合のみ適用
			TEQUIP:LOCAL:109 = TEQUIP:LOCAL:118
	ENDIF
	;ついでに勃起度の保存
	CFLAG:LOCAL:勃起度 = BASE:LOCAL:勃起
NEXT
;-------------------------------------------------
;カウンター脱衣の実処理
;-------------------------------------------------
CALL DATUI_LATE

;-------------------------------------------------
;@COMの呼び出し
;-------------------------------------------------
;TARGETごとにCOMF呼出
CALL CALL_COM(TARGET)

体位保存 = TEQUIP:体位
Ｖセックス保存 = TEQUIP:Ｖセックス
Ａセックス保存 = TEQUIP:Ａセックス
CanUSex = TEQUIP:USex
PLAYER体位保存 = TEQUIP:MASTER:体位
PLAYERＶセックス保存 = TEQUIP:PLAYER:Ｖセックス
PLAYERＡセックス保存 = TEQUIP:PLAYER:Ａセックス
PLAYER_CAN_U_SEX = TEQUIP:PLAYER:USex
TFLAG:前回のコマンド結果 = RESULT
;bug fix - when being canceled out of sex by a mob, remove favor gain (currently only underworld mobs will allow you to fuck when they're around outside of Lunatic Survival, the rest don't, adjust if necessary)
IF !FLAG:70 && CFLAG:MASTER:うふふ && WITH_MOB() && WITH_MOB() != 9 && !ITEM:正体不明のタネ && !LS_RESTRICTION("Map mob interrupt") ;nue's special item lets you stay hidden
	TFLAG:102 = 0
	TFLAG:前回のコマンド結果 = 0
ENDIF
;----------------------------
;RESULTが－1ならキャンセル判定
SIF TFLAG:前回のコマンド結果 < 0
	RETURN 0
;RESULTが0ならSOURCE計算SKIP
SIF !TFLAG:前回のコマンド結果
	GOTO SOURCE_SKIP
;口上内でソースを追加する処理
CALL KOJO_MESSAGE_SEND("EXTRASOURCE", SELECTCOM, TARGET)

IF OAZUKE(TARGET)
	PRINTFORML Being denied of what %HE_SHE(TARGET)% wanted to do makes %CALLNAME:TARGET% rather upset...
	CALL KOJO_MESSAGE_SEND("EVENT",10,TARGET,3,8) ;addition custom code
	SOURCE:反感 += 3000
	SOURCE:屈従 += 500
	SOURCE:恭順 -= 1500
ENDIF

CALL PEARL_CHK_MASTER

FOR LOCAL,0,CHARANUM
	キスマーク反感 = 0
	SIF CFLAG:LOCAL:現在位置 == SUKIMA()
		CONTINUE

	;-------------------------------------------------
	;Pee checks
	;-------------------------------------------------
	SIF !TCVAR:LOCAL:906 && ((LOCAL != MASTER && CFLAG:LOCAL:300 == CFLAG:MASTER:300) || LOCAL == MASTER)
	CALL NeedForPee(LOCAL)

	;ローター挿入
	IF CFLAG:LOCAL:ローター挿入
		SOURCE:LOCAL:快Ｖ += TIME_PROGRESS(TIME:1) * 10
		CFLAG:LOCAL:ローター挿入 = MAX(CFLAG:LOCAL:ローター挿入 - TIME_PROGRESS(TIME:1),0)
		IF CFLAG:LOCAL:ローター挿入 == 0
			PRINTFORMW The rotor %CALLNAME:LOCAL% previously inserted in your vagina runs out of power.
			IF TALENT:LOCAL:処女 < 1
				EXP:LOCAL:Ｖ経験 += 9
			ELSE
				TFLAG:2 = 1
			ENDIF
		ENDIF
	ENDIF
	IF CFLAG:LOCAL:ローターA挿入
		SOURCE:LOCAL:快Ａ += TIME_PROGRESS(TIME:1) * 10
		CFLAG:LOCAL:ローターA挿入 = MAX(CFLAG:LOCAL:ローターA挿入 - TIME_PROGRESS(TIME:1),0)
		IF CFLAG:LOCAL:ローターA挿入 == 0
			PRINTFORMW The rotor %CALLNAME:LOCAL% previously inserted in your anus runs out of power.
			EXP:LOCAL:Ａ経験 += 9
		ENDIF
	ENDIF
	IF TFLAG:キスマーク && LOCAL > 0 && !TFLAG:包帯 && !FLAG:70 && !CFLAG:LOCAL:睡眠
		IF CFLAG:LOCAL:現在位置 == CFLAG:MASTER:現在位置 && TFLAG:キスマーク != LOCAL && !TFLAG:泡風呂
			PRINTFORMW %CALLNAME:LOCAL% interrogates you about your kiss mark...
			IF TALENT:LOCAL:恋慕
				キスマーク反感 = 500
			ELSEIF TALENT:LOCAL:思慕
				キスマーク反感 = 300
			ENDIF
			キスマーク反感 = キスマーク反感 * (TALENT:LOCAL:貞操 + 2) / 2
			SOURCE:LOCAL:反感 += キスマーク反感
		ENDIF
	ENDIF
;はいてないと露出が増加
	SIF !EQUIP:LOCAL:下半身下着１ && !EQUIP:LOCAL:下半身下着２ && !TALENT:LOCAL:淫乱
		SOURCE:LOCAL:露出 += TIME_PROGRESS(TIME:1)

	SIF IsInsenceValid(LOCAL) || IsGardenValid(LOCAL)
		SOURCE:LOCAL:欲情 += TIME_PROGRESS(TIME:1) * (RAND:((ABL:LOCAL:欲望) + 1) + 3)

	;breastfeed command completely replaces nipple sucking (its's the exact copy just made to be compatible with the player)
	SIF TEQUIP:LOCAL:乳首吸い
		CALL ADD_CUSTOM_EQUIP_COM195(LOCAL)
	SIF TEQUIP:LOCAL:胸揉み
		CALL EQUIP_COM12(LOCAL)
	SIF TEQUIP:LOCAL:継続キス
		CALL EQUIP_COM19(LOCAL)
	SIF TEQUIP:LOCAL:BALL_LICKING ;addition custom code
		CALL ADD_EQUIP_CUSTOM_COM17(LOCAL)
	SIF EQUIP:LOCAL:下半身下着２
		TRYCALLFORM 下半身下着_ずらし可能{EQUIP:LOCAL:下半身下着２}_SOURCE(LOCAL)

	;装着系は後で考える
	;NAS custom code: non 0 master fix
	SIF LOCAL != MASTER && !CFLAG:LOCAL:同室
		CONTINUE
	;-------------------------------------------------
	;主人、同性、調教者の能力の処理
	;-------------------------------------------------
	CALL SOURCE_SEX_CHECK(LOCAL)
	CALL PLAYER_SKILL_CHECK(LOCAL)

	;-------------------------------------------------
	;技巧、好感度、ムード、理性のチェック
	;-------------------------------------------------
	FOR LOCAL:1,0,100
		SOURCE:LOCAL:(LOCAL:1) = SOURCE:LOCAL:(LOCAL:1) *  MASTER_FAVOR_CHECK(LOCAL,LOCAL:1) * TECHNIQUE_CHECK(LOCAL,LOCAL:1) / 10000
		SOURCE:LOCAL:(LOCAL:1) = SOURCE:LOCAL:(LOCAL:1) *  MOOD_CHECK(LOCAL,LOCAL:1) * REASON_CHECK(LOCAL,LOCAL:1) / 10000
	NEXT

	;-------------------------------------------------
	;中毒充足のチェック
	;-------------------------------------------------
	;自慰中毒
	IF EXP_UP(22,LOCAL)
		FOR LOCAL:1,0,100
			;快感,性行動,欲情,恭順,露出,屈従
			SIF (LOCAL:1 >= 0 && LOCAL:1 <= 9) || LOCAL:1 == 11 || (LOCAL:1 >= 15 && LOCAL:1 <= 18) 
				SOURCE:LOCAL:(LOCAL:1) = SOURCE:LOCAL:(LOCAL:1) * (100 + GET_REVISION(ABL:LOCAL:自慰中毒, 500, 15)) / 100
		NEXT
	ENDIF
	;-------------------------------------------------
	;バイブなど付けっぱなしアイテムのチェック
	;-------------------------------------------------
	CALL EQUIP_CHECK(LOCAL)
	;-------------------------------------------------
	;経験加算
	;-------------------------------------------------
	CALL SOURCE_EXP(LOCAL)
	;-------------------------------------------------
	;処女,童貞喪失の処理
	;-------------------------------------------------
	CALL LOST_VIRGIN(LOCAL)
	CALL 童貞キス未経験喪失(LOCAL)
	;-------------------------------------------------
	;Horniness
	;Was moved UP so ejac and orgasm can be set properly
	;-------------------------------------------------
	CALL HornyFlavor(LOCAL)		
	;-------------------------------------------------
	;快Ｃのソース
	;-------------------------------------------------
	CALL SOURCE_快Ｃ(LOCAL)
	;-------------------------------------------------
	;快Ｖのソース
	;-------------------------------------------------
	CALL SOURCE_快Ｖ(LOCAL)
	;-------------------------------------------------
	;快Ａのソース
	;-------------------------------------------------
	CALL SOURCE_快Ａ(LOCAL)
	;-------------------------------------------------
	;快Ｂのソース
	;-------------------------------------------------
	CALL SOURCE_快Ｂ(LOCAL)
	;-------------------------------------------------
	;-------------------------------------------------
	;快Ｍのソース
	;-------------------------------------------------
	CALL SOURCE_快Ｍ(LOCAL)
	;-------------------------------------------------
	;快Ｍのソース
	;-------------------------------------------------
	CALL SOURCE_UPleas(LOCAL)
	;-------------------------------------------------
	;与快Ｃのソース
	;-------------------------------------------------
	;SIF !(TALENT:PLAYER:2 & 2)
	CALL SOURCE_与快Ｃ(LOCAL)
	;-------------------------------------------------
	;与快Ｖのソース
	;-------------------------------------------------
	CALL SOURCE_与快Ｖ(LOCAL)
	;-------------------------------------------------
	;与快Ａのソース
	;-------------------------------------------------
	CALL SOURCE_与快Ａ(LOCAL)
	;-------------------------------------------------
	;与快Ｂのソース
	;-------------------------------------------------
	CALL SOURCE_与快Ｂ(LOCAL)
	;-------------------------------------------------
	;与快Ｍのソース
	;-------------------------------------------------
	CALL SOURCE_与快Ｍ(LOCAL)
	;-------------------------------------------------
	;与快Ｍのソース
	;-------------------------------------------------
	CALL SOURCE_TogetherU(LOCAL)	
	;-------------------------------------------------
NEXT

;-------------------------------------------------
;同じコマンドの連続実行による上下の処理（快楽系）
;-------------------------------------------------
IF SELECTCOM == PREVCOM && TFLAG:50 == TFLAG:151 && (SELECTCOM < 350 || SELECTCOM >= 500) && 連続実行()
	;UP:0,1,2,3は絶頂に絡むので先に処理してある
	CUP:TARGET:快Ｃ /= 2
	CUP:TARGET:快Ｖ /= 2
	CUP:TARGET:快Ａ /= 2
	CUP:TARGET:快Ｂ /= 2
	CUP:TARGET:快Ｍ /= 2
	CUP:TARGET:UPleas /= 2	
ENDIF
IF TCVAR:TARGET:無理矢理
	TIMES CUP:TARGET:快Ｃ, 0.75
	TIMES CUP:TARGET:快Ｖ, 0.75
	TIMES CUP:TARGET:快Ａ, 0.75
	TIMES CUP:TARGET:快Ｂ, 0.75
	TIMES CUP:TARGET:快Ｍ, 0.75
	TIMES CUP:TARGET:UPleas, 0.75	
ENDIF
IF TEQUIP:TARGET:焦らしモード || CFLAG:TARGET:Hypno_Orgasm_Denial ;custom code
	TIMES CUP:TARGET:快Ｃ, 0.90
	TIMES CUP:TARGET:快Ｖ, 0.90
	TIMES CUP:TARGET:快Ａ, 0.90
	TIMES CUP:TARGET:快Ｂ, 0.90
	TIMES CUP:TARGET:快Ｍ, 0.90
	TIMES CUP:TARGET:UPleas, 0.90	
ENDIF
IF TCVAR:TARGET:泥酔
	TIMES CUP:TARGET:快Ｃ, 0.70
	TIMES CUP:TARGET:快Ｖ, 0.70
	TIMES CUP:TARGET:快Ａ, 0.70
	TIMES CUP:TARGET:快Ｂ, 0.70
	TIMES CUP:TARGET:快Ｍ, 0.70
	TIMES CUP:TARGET:UPleas, 0.70	
ENDIF

FOR LOCAL,0,CHARANUM
	;装着系は後で考える
	;SIF LOCAL != MASTER && !CFLAG:LOCAL:同室
	;same room flag isn't set properly
	SIF LOCAL != MASTER && CFLAG:LOCAL:300 != CFLAG:MASTER:300
		CONTINUE
	;その他系ソース計算処理
	CALL SOURCE_CVAB_EXTRA(LOCAL)
	
	BASE:LOCAL:勃起 = BASE:LOCAL:勃起 + CUP:LOCAL:快Ｃ / 5 + CUP:LOCAL:快Ｖ / 10 + CUP:LOCAL:快Ａ / 10 + CUP:LOCAL:快Ｂ / 10
	;-------------------------------------------------
	;絶頂処理
	;-------------------------------------------------
	CALL ORGASM_ADD(LOCAL)
	SIF NOWEX:LOCAL:SextOrgasm
		CALL QoL_Info_Write(LOCAL, Qol_Dates_Sext)	
	SIF NOWEX:LOCAL:五重絶頂
		CALL QoL_Info_Write(LOCAL, Qol_Dates_Quint)
	IF NOWEX:LOCAL:SextOrgasm || NOWEX:LOCAL:五重絶頂
		IF !SaveAchievementCompleted("insightPleasure")
			; CALL ANIMATION_ROLL_TEXT( @"Achievement Unlocked：[Pleasure Insight]!", 0 ) ;addition custom code
			; ADD_ACHIEVEMENT_OBTAINFLAG:Achievement_PleasureInsight = 1
			CALL AL_NOTIFY_ACHIEVEMENT_COMPLETION("insightPleasure") ;achievement menu rework custom code

		ENDIF
	ENDIF
	SIF NOWEX:LOCAL:四重絶頂
		CALL QoL_Info_Write(LOCAL, Qol_Dates_Quad)
NEXT

;-------------------------------------------------
;射精チェック（ふたなり・オトコ）
;-------------------------------------------------
;要検討
CALL EVENT_SHOOT

FOR LOCAL,0,CHARANUM
	SIF CFLAG:LOCAL:現在位置 == SUKIMA()
		CONTINUE
	;装着系は後で考える
	IF LOCAL != MASTER && !CFLAG:LOCAL:同室
		SIF IsGardenValid(LOCAL)
			CALL SOURCE_欲情(LOCAL)
		CONTINUE
	ENDIF
	;-------------------------------------------------
	;custom code
	;-------------------------------------------------
	CALL TARGET_ORGASM_POWER(LOCAL)	
	;-------------------------------------------------
	;調教対象の噴乳チェック
	;-------------------------------------------------
	CALL TARGET_MILK_CHECK(LOCAL)
	;-------------------------------------------------
	;custom code
	;-------------------------------------------------
	CALL TARGET_PEE_CHECK(LOCAL)	
	CALL TARGET_POO_CHECK(LOCAL)

	;OAB/IBS
	IF HasOAB(LOCAL) && !RAND:40 && GETBIT(nPee,1)
		SIF LOCAL == MASTER
		PLAYSOUND "peestuffs/pee/おちびり2.m4a"
		SETCOLOR 255,100,255
		PRINTFORML 【%DamageTypes:30%】
		RESETCOLOR	
		IF LOCAL == MASTER
		PRINTFORMDW Your bladder suddenly twitches, causing you to cup your groin in desperation.
		PRINTFORMDW You need to get to a toilet! NOW!
		ELSE
		PRINTFORMDW %CALLNAME:LOCAL% suddenly feels like %HES_SHES(LOCAL)% about to pee %HIMSELF_HERSELF(LOCAL)%...  
		ENDIF
		CALL KOJO_MESSAGE_SEND("EVENT",105,TARGET,7,0)
		CALLF PEEME(LOCAL,6)
	ENDIF	
	IF HasIBS(LOCAL) && !RAND:40 && GETBIT(nScat,2)
		CALL IBSProc(LOCAL)
	ENDIF	
	IF (TALENT:LOCAL:BlortProne || NASCheat:2) && !RAND:100 && GETBIT(nScat,2) && BASE:LOCAL:Poo >= MAXBASE:LOCAL:Poo/2
		IF LOCAL == MASTER
			SELECTCASE RAND:3
			CASE 0
			PLAYSOUND "peestuffs/poo/腹痛1.m4a"
			CASE 1
			PLAYSOUND "peestuffs/poo/腹痛4.m4a"
			CASE 2
			PLAYSOUND "peestuffs/poo/腹痛5.m4a"
			ENDSELECT
		ENDIF
		SETCOLOR 255,100,255
		PRINTFORML 【%GET_TALENTNAME_TR(352,1)%】
		RESETCOLOR	
		IF LOCAL == MASTER
		PRINTFORMDW As %PARSE("you're",,,LOCAL)% doing %PARSE("your",5,,LOCAL)% thing, %PARSE("your",5,,LOCAL)% %FSYN("belly")% muscles give a push.
		ENDIF
		;CALL UNCHI_OMORASHI(LOCAL, 0, 0, 1)
		ACCIDENT_CHARA = 1
		ACCIDENT_HasScat = 1
		ACCIDENT_LINECOUNT = LINECOUNT
		CALL ACCIDENT_SHOW_BAR_RELIEVE
		PRINTFORMW By the time %PARSE("you have",,,LOCAL)% collected %PARSE("your",5,,LOCAL)% thoughts, there's a fresh load of %FSYN("poo:n")% resting between %PARSE("your",5,,LOCAL)% legs...  
		CALL OMORASHI_END(LOCAL, 0, 1, ,-1)
		CALL AfterAccidentCheck(LOCAL,0,1,1)
	ENDIF
	CALL YoujutsuSelfStudyQuestTrack(LOCAL, "CHECK")

	;-------------------------------------------------
	;睡姦からの復帰前処理
	;-------------------------------------------------
	CALL DRUNK_RECOVER_BEFORE(TARGET)
	;-------------------------------------------------
	;情愛のソース
	;-------------------------------------------------
	CALL SOURCE_情愛(LOCAL)
	;-------------------------------------------------
	;性行動のソース
	;-------------------------------------------------
	CALL SOURCE_性行動(LOCAL)
	;-------------------------------------------------
	;達成のソース
	;-------------------------------------------------
	CALL SOURCE_達成(LOCAL)
	;-------------------------------------------------
	;Custom Code
	;Terror Source
	;-------------------------------------------------
	CALL SOURCE_TERROR(LOCAL)
	CALL SOURCE_TRAUMA(LOCAL)
	CALL SOURCE_TRAUMA(LOCAL, 1)
	;-------------------------------------------------
	;痛みのソース
	;-------------------------------------------------
	CALL SOURCE_苦痛(LOCAL)
	;-------------------------------------------------
	;恐れのソース
	;-------------------------------------------------
	CALL SOURCE_恐怖(LOCAL)
	;-------------------------------------------------
	;液体のソース
	;-------------------------------------------------
	CALL SOURCE_液体(LOCAL)
	;-------------------------------------------------
	;欲情のソース
	;-------------------------------------------------
	CALL SOURCE_欲情(LOCAL)
	;-------------------------------------------------
	;恭順のソース
	;-------------------------------------------------
	CALL SOURCE_恭順(LOCAL)
	;-------------------------------------------------
	;露出のソース
	;-------------------------------------------------
	CALL SOURCE_露出(LOCAL)
	;-------------------------------------------------
	;屈従のソース
	;-------------------------------------------------
	CALL SOURCE_屈従(LOCAL)
	
	;-------------------------------------------------
	;歓楽のソース
	;-------------------------------------------------
	CALL SOURCE_歓楽(LOCAL)
	;-------------------------------------------------
	;征服のソース
	;-------------------------------------------------
	CALL SOURCE_征服(LOCAL)
	;-------------------------------------------------
	;受動のソース
	;-------------------------------------------------
	CALL SOURCE_受動(LOCAL)
	;-------------------------------------------------
	;誘惑のソース
	;-------------------------------------------------
	CALL SOURCE_誘惑(LOCAL)
	;-------------------------------------------------
	;辱めのソース
	;-------------------------------------------------
	CALL SOURCE_辱め(LOCAL)
	;-------------------------------------------------
	;挑発のソース
	;-------------------------------------------------
	CALL SOURCE_挑発(LOCAL)
	;-------------------------------------------------
	;奉仕のソース
	;-------------------------------------------------
	CALL SOURCE_奉仕(LOCAL)
	;-------------------------------------------------
	;強要のソース
	;-------------------------------------------------
	CALL SOURCE_強要(LOCAL)
	;-------------------------------------------------
	;加虐のソース
	;-------------------------------------------------
	CALL SOURCE_加虐(LOCAL)
	
	;-------------------------------------------------
	;不潔のソース
	;-------------------------------------------------
	CALL SOURCE_不潔(LOCAL)
	;-------------------------------------------------
	;鬱屈のソース
	;-------------------------------------------------
	CALL SOURCE_鬱屈(LOCAL)
	;-------------------------------------------------
	;逸脱のソース
	;-------------------------------------------------
	CALL SOURCE_逸脱(LOCAL)
	;-------------------------------------------------
	;反感のソース
	;-------------------------------------------------
	CALL SOURCE_反感(LOCAL)
	;-------------------------------------------------
	;Regression Marks
	;-------------------------------------------------
	CALL SOURCE_REGRESSION(LOCAL)
	;-------------------------------------------------
	;素質などによる上下の処理
	;-------------------------------------------------
	CALL SOURCE_EXTRA(LOCAL)
	;-------------------------------------------------
	;睡眠深度の減少処理
	;-------------------------------------------------
	CALL 睡眠深度減少(LOCAL)

	;-------------------------------------------------
	;Hypnosis
	;-------------------------------------------------
	CALL HypnoFlavor(LOCAL)

	;qol custom code, prevent issues with negative SOURCE
	FOR LOCAL:10, 0, SOURCE_MAX
		IF SOURCE:LOCAL:(LOCAL:10) < 0
			DEBUGPRINTFORML Value of SOURCE {LOCAL:10} for %CALLNAME:LOCAL% was {SOURCE:LOCAL:(LOCAL:10)}.
			SOURCE:LOCAL:(LOCAL:10) = 0
		ENDIF
	NEXT
NEXT
;-------------------------------------------------
;同じコマンドの連続実行による上下の処理
;-------------------------------------------------
IF SELECTCOM == PREVCOM && TFLAG:50 == TFLAG:151 && (SELECTCOM < 350 || SELECTCOM >= 500) && 連続実行()
	;UP:0,1,2は絶頂に絡むので先に処理してある
	UP:欲情 /= 2
	UP:恭順 /= 2
	UP:屈服 /= 2
	UP:習得 /= 2
	UP:恥情 /= 2
	UP:恐怖 /= 2
	UP:苦痛 /= 2
	UP:好意 /= 2
	UP:優越 /= 2
ENDIF

LOCAL:1 = 0
FOR LOCAL,0,CHARANUM
	SIF CFLAG:LOCAL:現在位置 == SUKIMA()
		CONTINUE
	LOCAL:1 += CUP:LOCAL:欲情 / 5
	LOCAL:1 += MAX(5,(ABL:LOCAL:マゾっ気 - 3)) * (CUP:LOCAL:苦痛 + CUP:LOCAL:苦痛) / 2
	LOCAL:1 += MAX(5,(ABL:LOCAL:マゾっ気 - 1)) * CUP:LOCAL:恥情 / 2
	LOCAL:1 = MIN(500,LOCAL:1)
	BASE:LOCAL:勃起 = BASE:LOCAL:勃起 + LOCAL:1
	;装着系は後で考える
	; SIF LOCAL > 0 && !CFLAG:LOCAL:同室
	; 	CONTINUE
	SIF LOCAL != MASTER && !IS_HERE_NAS(MASTER,LOCAL)
		CONTINUE
	;-------------------------------------------------
	;体力・気力の減少
	;-------------------------------------------------
	CALL SOURCE_DOWNBASE(LOCAL)
	;-------------------------------------------------
	;苦痛快楽経験、奉仕快楽経験のチェック
	;-------------------------------------------------
	CALL EXP_GOT_CHECK(LOCAL)
NEXT

;-------------------------------------------------
;check red thread activation
;-------------------------------------------------
CALL Add_RedThread_Proc ;addition custom code

$SOURCE_SKIP

;addition custom code, panty position
FOR LOCAL, 0, CHARANUM
	CALL SHIFT_PANTIES(LOCAL)
NEXT

;-------------------------------------------------
;調教口上
;-------------------------------------------------
;CALL KOJO_MESSAGE_COM
IF !nSkipKojo ;com custom code
	TFLAG:TRAIN_MESSAGE_CALLED = 0
	TFLAG:SKIPASSIKOJO = 0
	CALL KOJO_MESSAGE_SEND("COMMAND",-1,TARGET,-1,-1)
	SIF !TFLAG:SKIPASSIKOJO
		CALL SEND_ASSISTANT_KOJO
ENDIF
CALL MOREDATES_SOUVENIR() ;Custom code, handle Souvenir Shop on group dates.
nSkipKojo = 0 ;com custom code
;bugfix custom code, in some cases, such as COM337 (child interaction), a TARGET can be set that is not present. We reset it here right after the KOJO dialogue is triggered.
SIF !IS_HERE(TARGET)
	TARGET = 0

;挿絵　正面系　ゴムありの射精時、体位もなくなるのはどうしてだ
IF (Ｖセックス保存 != -1 || CanUSex != -1) && GROUPMATCH(体位保存,1,2,3,4,5) && !FLAG:挿入アニメフィルタ & アニメフィルタ_挿入
	SIF FLAG:画像表示 && FLAG:挿絵表示
		PRINTL
	SELECTCASE 体位保存
		CASE 1 ;image tweak custom code, standing missionary
			IF (!BEDROOM(CFLAG:PLAYER:現在位置) && !(BATHROOM() && ITEM:20)) || TFLAG:193
				CALL PRINT_IMAGE_ANIMATION_正常位("正常位_stand_挿入")
			ELSE
				CALL PRINT_IMAGE_ANIMATION_正常位("正常位_挿入")
			ENDIF
		CASE 1,3,4
			CALL PRINT_IMAGE_ANIMATION_正常位("正常位_挿入")
		CASE 2,5
			CALL PRINT_IMAGE_ANIMATION_後背位("後背位_挿入")
	ENDSELECT
ENDIF

IF (PLAYERＶセックス保存 != -1 || PLAYER_CAN_U_SEX != -1) && GROUPMATCH(PLAYER体位保存,1,2,3,4,5) && !FLAG:挿入アニメフィルタ & アニメフィルタ_挿入
	SIF FLAG:画像表示 && FLAG:挿絵表示
		PRINTL
	SELECTCASE PLAYER体位保存
		CASE 1 ;image tweak custom code, standing missionary
			IF (!BEDROOM(CFLAG:PLAYER:現在位置) && !(BATHROOM() && ITEM:20)) || TFLAG:193
				CALL PRINT_IMAGE_ANIMATION_正常位("正常位_stand_挿入")
			ELSE
				CALL PRINT_IMAGE_ANIMATION_正常位("正常位_挿入")
			ENDIF
		CASE 1,3,4
			CALL PRINT_IMAGE_ANIMATION_正常位("正常位_挿入")
		CASE 2,5
			CALL PRINT_IMAGE_ANIMATION_後背位("後背位_挿入")
	ENDSELECT
ENDIF

IF Ａセックス保存 != -1 && GROUPMATCH(体位保存,1,2,3,4,5) && !FLAG:挿入アニメフィルタ & アニメフィルタ_挿入
	SIF FLAG:画像表示 && FLAG:挿絵表示
		PRINTL
	SELECTCASE 体位保存
		CASE 1,3,4
			CALL PRINT_IMAGE_ANIMATION_正常位("Ａ正常位_挿入")
		CASE 2,5
			CALL PRINT_IMAGE_ANIMATION_後背位("Ａ後背位_挿入")
	ENDSELECT
ENDIF
IF PLAYERＡセックス保存 != -1 && GROUPMATCH(PLAYER体位保存,1,2,3,4,5) && !FLAG:挿入アニメフィルタ & アニメフィルタ_挿入
	SIF FLAG:画像表示 && FLAG:挿絵表示
		PRINTL
	SELECTCASE PLAYER体位保存
		CASE 1,3,4
			CALL PRINT_IMAGE_ANIMATION_正常位("Ａ正常位_挿入" )
		CASE 2,5
			CALL PRINT_IMAGE_ANIMATION_後背位("Ａ後背位_挿入" )
	ENDSELECT
ENDIF

SIF FLAG:風チラCT > 0
	FLAG:風チラCT -= 1
SIF FLAG:風チラ発生
	CALL WINDY_PANCHIRA

;カウンター
IF TARGET
	LOCAL:4 = TARGET
	DRAWLINE
	FOR LOCAL, 1, GET_TARGETNUM() + 1
		SIF TCVAR:(TARGET:LOCAL):30 > 1
			CONTINUE
			
		;com custom code
		CALL CCOM_DIRECT29(TARGET:LOCAL)
		SIF RESULT
			CONTINUE
		;---------------------------------
			
		SIF TCVAR:(TARGET:LOCAL):20 && TFLAG:193 != 99 && !CFLAG:TARGET:添い寝中
			CALL KOJO_MESSAGE_SEND("COUNTER",2,TARGET:LOCAL,0,0)
	NEXT
	TARGET = LOCAL:4
ENDIF

;-------------------------------------------------
;睡姦からの復帰処理
;-------------------------------------------------
CALL DRUNK_RECOVER(TARGET)

;-------------------------------------------------
;EXP取得表示
;-------------------------------------------------
LOCAL:2 = 0
LOCAL:3 = 0
;---------------
LOCAL:9 = 0 ;addition custom, put last here
LOCAL:10 = 0
;---------------
FOR LOCAL,0,CHARANUM
	SIF CFLAG:LOCAL:現在位置 != CFLAG:MASTER:現在位置
		CONTINUE
	SIF TCVAR:LOCAL:表示カット
		CONTINUE
	;Hole loosening
	HoleStretch:LOCAL:0 -= (EXP_UP(1, LOCAL)*3)
	HoleStretch:LOCAL:1 -= (EXP_UP(2, LOCAL)*3)
	HoleStretch:LOCAL:2 -= (EXP_UP(30, LOCAL)*3)

	LOCAL:11 = 0
	FOR LOCAL:1,0,171
		IF EXP_UP(LOCAL:1,LOCAL)
			経験増加量 = EXP_UP(LOCAL:1,LOCAL)
			LOCAL:2 ++
			LOCAL:3 ++
			SIF LOCAL:2 == 1
				PRINTL
			IF LOCAL:1 == FLAG:加護 && FLAG:加護 && LOCAL == MASTER
				EXP:MASTER:(LOCAL:1) ++
				経験増加量 ++
			ENDIF
			;Suzunaan addition custom code, demon book effect: during sex or molestation it will give bonus to selected experiences for all participants (if they're going to receive that exp)
			IF ITEM:GETNUM(ITEM,  "妖魔本") > 0 && GROUPMATCH(LOCAL:1, GETNUM(EXP, "Ｃ経験"), GETNUM(EXP, "Ｖ経験"), GETNUM(EXP, "Ａ経験"), GETNUM(EXP, "Ｂ経験"), GETNUM(EXP, "Ｍ経験"), GETNUM(EXP, "精液経験"), GETNUM(EXP, "精飲経験"), GETNUM(EXP, "Ｖ性交経験"), GETNUM(EXP, "Ａ性交経験"), GETNUM(EXP, "自慰経験"), GETNUM(EXP, "調教自慰経験"), GETNUM(EXP, "手淫経験"), GETNUM(EXP, "口淫経験"), GETNUM(EXP, "パイズリ経験"), GETNUM(EXP, "キス経験"), GETNUM(EXP, "道具使用経験"), GETNUM(EXP, "挿入経験"), GETNUM(EXP, "奉仕快楽経験"), GETNUM(EXP, "苦痛快楽経験"), GETNUM(EXP, "嗜虐快楽経験"), GETNUM(EXP, "露出快楽経験"), GETNUM(EXP, "Ｖ拡張経験"), GETNUM(EXP, "Ａ拡張経験"), GETNUM(EXP, "緊縛経験"), GETNUM(EXP, "子宮口開発経験"))
				SIF !(LOCAL:9++)
					CALL COLORMESSAGE("Demon Book Bonus!",C_ORANGE,1,1)
				EXP:LOCAL:(LOCAL:1) ++
				経験増加量 ++
			ENDIF

			;NAS Potential
			LOCAL:12 = EXPPotential:LOCAL:(LOCAL:1)
			SIF HasBionicPart(LOCAL,61)
				LOCAL:12 += 2
			IF (LOCAL:12)
				CALL COLORMESSAGE(@"Potential: %EXP_TR(LOCAL:1)% +{(LOCAL:12)} (%CALLNAME:LOCAL%)",C_AQUA,1,1)
				;add exp to the counter
				SIF EXPPotential:LOCAL:(LOCAL:1)
					EXPPotentialCounter:LOCAL:(LOCAL:1) += 経験増加量

				;if the 25*potential level multiplier is better than flat points
				IF (経験増加量*(100+(25*(LOCAL:12)))/100) > 経験増加量+(LOCAL:12)
					;reset pre multiplied exp, then give the exp
					EXP:LOCAL:(LOCAL:1) -= 経験増加量
					EXP:LOCAL:(LOCAL:1) += (経験増加量*(100+(25*(LOCAL:12)))/100)
					経験増加量 = (経験増加量*(100+(25*(LOCAL:12)))/100)
				ELSE
					EXP:LOCAL:(LOCAL:1) += (LOCAL:12)
					経験増加量 += (LOCAL:12)
				ENDIF
			ENDIF

			;NAS Age Bonus
			IF GETBIT(NASFeature,2)
				SIF TALENT:LOCAL:150
					GOTO ThreeYoBonus
				SELECTCASE CharaAge:LOCAL:0
					CASE 20 TO 25
						$YABonus
						SIF !(LOCAL:11++)
							CALL COLORMESSAGE(@"Age: Young Adult Bonus! (%CALLNAME:LOCAL%)",C_GREEN,1,1)
						EXP:LOCAL:(LOCAL:1) ++
						経験増加量 ++
					CASE 13 TO 19
						$TeenYoBonus
						SIF !(LOCAL:11++)
							CALL COLORMESSAGE(@"Age: Teenager Bonus! (%CALLNAME:LOCAL%)",C_GREEN,1,1)
						EXP:LOCAL:(LOCAL:1) ++
						経験増加量 ++
					CASE 10 TO 13
						$TenYoBonus
						SIF !(LOCAL:11++)
							CALL COLORMESSAGE(@"Age: Pre-Teen Bonus! (%CALLNAME:LOCAL%)",C_GREEN,1,1)
						EXP:LOCAL:(LOCAL:1) += 2
						経験増加量 += 2			
					CASE 7 TO 10
						$SevenYoBonus
						SIF !(LOCAL:11++)
							CALL COLORMESSAGE(@"Age: Child Bonus! (%CALLNAME:LOCAL%)",C_GREEN,1,1)
						EXP:LOCAL:(LOCAL:1) += 3
						経験増加量 += 3
					CASE 3 TO 7
						$ThreeYoBonus
						SIF !(LOCAL:11++)
							CALL COLORMESSAGE(@"Age: Toddler Bonus! (%CALLNAME:LOCAL%)",C_GREEN,1,1)
						EXP:LOCAL:(LOCAL:1) += 4
						経験増加量 += 4
				ENDSELECT
			ENDIF

			IF (SoiledSelf:MASTER:0 && ABL:MASTER:PeeAddict < 10) || (SoiledSelf:MASTER:1 && ABL:MASTER:PooAddict < 10)
				IF !(LOCAL:10++)
					PRINTL
					CALL COLORMESSAGE("Peed Pants Debuff...",C_RED,1,1)
				ENDIF
				経験増加量 --
			ENDIF
			;qol custom code
			PRINTFORM %EXP_TR(LOCAL:1)% +{経験増加量}(%CALLNAME:LOCAL%)
			;#; PRINTFORM  (EXPID: {LOCAL:1})
			CALL Qol_SkillProgressBar(LOCAL, LOCAL:1)
			PRINTL
			;------------------------------------
			;あなたは無自覚経験を得ないように
			IF LOCAL:1 >= 0 && LOCAL:1 <= 3 && LOCAL && (FLAG:70 || CFLAG:イタズラ)
				EXP:LOCAL:((LOCAL:1) + 60) += 1
			ENDIF			;qol custom code

			;NAS Potential check
			IF EXPPotentialCounter:LOCAL:(LOCAL:1) >= PotentialResetCounter
				;over 300 pre multiplier EXP gained. we decrease the potential counter
				LOCAL:13 = 0
				WHILE EXPPotentialCounter:LOCAL:(LOCAL:1) >= PotentialResetCounter
					LOCAL:13--
					EXPPotentialCounter:LOCAL:(LOCAL:1) = MAX(0,EXPPotentialCounter:LOCAL:(LOCAL:1)-PotentialResetCounter)
				WEND
				CALL EXPPotentialChangeMsg(LOCAL,LOCAL:1,LOCAL:13)
			ENDIF
		ENDIF
		
	NEXT
	
	;custom code for non-CSV exp
	;Mr Pops Alot
	CALL ShowEnduranceChange(LOCAL)
	CALL ShowYoujutsuChange(LOCAL,1)
	LOCAL:3 += RESULT
	;==================================

	LOCAL:2 = 0
NEXT
SIF LOCAL:3
	WAIT
;-------------------------------------------------
;ABLUP
;-------------------------------------------------
IF !FLAG:ABLUP省略
	FOR LOCAL,0,CHARANUM
		SIF CFLAG:LOCAL:現在位置 != CFLAG:MASTER:現在位置
			CONTINUE
		SIF TCVAR:LOCAL:表示カット
			CONTINUE
		CALL SOURCE_ABLUP(LOCAL)
	NEXT
ENDIF
;-------------------------------------------------
;触手の射精時の口上
;-------------------------------------------------
;DEBUG	CALL KOJO_MESSAGE_PALAMCNG_E
CALL KOJO_MESSAGE_SEND("PALAM",5,TARGET,-1,-1)
;-------------------------------------------------
;奴隷の絶頂時の口上
;-------------------------------------------------
;CALL MESSAGE_PALAMCNG_B2

CALL KOJO_MESSAGE_SEND("PALAM",2,TARGET,-1,-1)
;挿絵　挿入∨絶頂
IF ((NOWEX:Ｖ絶頂 && Ｖセックス保存 != -1) || (NOWEX:UOrgasm && CanUSex != -1)) && GROUPMATCH(体位保存,1,2,3,4,5)
	IF !GETBIT(TCVAR:50,1)
		SELECTCASE 体位保存
			CASE 1 ;image tweak custom code, standing missionary
				IF (!BEDROOM(CFLAG:PLAYER:現在位置) && !(BATHROOM() && ITEM:20)) || TFLAG:193
					CALL PRINT_IMAGE_ANIMATION_正常位("正常位_stand_絶頂")
				ELSE
					CALL PRINT_IMAGE_ANIMATION_正常位("正常位_絶頂")
				ENDIF
			CASE 1,3,4
				CALL PRINT_IMAGE_ANIMATION_正常位("正常位_絶頂")
			CASE 2,5
				CALL PRINT_IMAGE_ANIMATION_後背位("後背位_絶頂")
		ENDSELECT
	ENDIF
ENDIF
IF NOWEX:Ａ絶頂 && Ａセックス保存 != -1 && GROUPMATCH(体位保存,1,2,3,4,5)
	IF !GETBIT(TCVAR:50,2)
		SELECTCASE 体位保存
			CASE 1,3,4
				CALL PRINT_IMAGE_ANIMATION_正常位("Ａ正常位_絶頂")
			CASE 2,5
				CALL PRINT_IMAGE_ANIMATION_後背位("Ａ後背位_絶頂")
		ENDSELECT
	ENDIF
ENDIF

;挿絵　PALYER挿入∨絶頂
IF ((NOWEX:MASTER:Ｖ絶頂 && PLAYERＶセックス保存 != -1) || (NOWEX:MASTER:UOrgasm && PLAYER_CAN_U_SEX != -1)) && GROUPMATCH(PLAYER体位保存,1,2,3,4,5)
	IF NOWEX:TARGET:射精 == 0
		SELECTCASE PLAYER体位保存
			CASE 1 ;image tweak custom code, standing missionary
				IF (!BEDROOM(CFLAG:PLAYER:現在位置) && !(BATHROOM() && ITEM:20)) || TFLAG:193
					CALL PRINT_IMAGE_ANIMATION_正常位("正常位_stand_絶頂")
				ELSE
					CALL PRINT_IMAGE_ANIMATION_正常位("正常位_絶頂")
				ENDIF
			CASE 1,3,4
				CALL PRINT_IMAGE_ANIMATION_正常位("正常位_絶頂")
			CASE 2,5
				CALL PRINT_IMAGE_ANIMATION_後背位("後背位_絶頂")
		ENDSELECT
	ENDIF
ENDIF
IF NOWEX:MASTER:Ａ絶頂 && PLAYERＡセックス保存 != -1 && GROUPMATCH(PLAYER体位保存,1,2,3,4,5)
	IF NOWEX:TARGET:射精 == 0
		SELECTCASE PLAYER体位保存
			CASE 1,3,4
				CALL PRINT_IMAGE_ANIMATION_正常位("Ａ正常位_絶頂")
			CASE 2,5
				CALL PRINT_IMAGE_ANIMATION_後背位("Ａ後背位_絶頂")
		ENDSELECT
	ENDIF
ENDIF
;DEBUG	CALL KOJO_MESSAGE_PALAMCNG_B


;-------------------------------------------------
;奴隷の焦らし時の口上
;-------------------------------------------------
IF (EX:TARGET:Ｃ寸止め + EX:TARGET:Ｖ寸止め + EX:TARGET:Ａ寸止め + EX:TARGET:Ｍ寸止め + EX:TARGET:Ｂ寸止め) > 0 && (TEQUIP:TARGET:焦らしモード || CFLAG:TARGET:Hypno_Orgasm_Denial) ;custom code
	CALL KOJO_MESSAGE_SEND("PALAM",6,TARGET,-1,-1)
	;ウフフ時系装備リセット
	FOR LOCAL,10, 80
		SELECTCASE LOCAL
			CASE 18 TO 20, 25 TO 40, 49 TO 51, 55, 82
				CONTINUE
		ENDSELECT
		TEQUIP:TARGET:LOCAL = 0
	NEXT
ENDIF
;-------------------------------------------------
;調教者の射精時の口上
;-------------------------------------------------
CALL 射精絶頂表記(MASTER)
;NAS: check all local
FOR LOCAL, 1, GET_TARGETNUM() + 1
	LOCAL:1 = TARGET:LOCAL
	SIF !LOCAL:1
		BREAK
	CALL 射精絶頂表記(LOCAL:1)
NEXT

;SIF TARGET != MASTER
;CALL 射精絶頂表記(TARGET)
CALL MESSAGE_PALAMCNG_B2

;DEBUG	CALL KOJO_MESSAGE_PALAMCNG_A
CALL KOJO_MESSAGE_SEND("PALAM",1,TARGET,-1,-1)

;pullout promise patch, handle logic
FOR LOCAL, 1, GET_TARGETNUM() + 1
	SIF TARGET:LOCAL < 1
		CONTINUE
	;口上出力用のフラグがないと飛ばす
	SIF !TCVAR:(TARGET:LOCAL):50
		CONTINUE
	SIF !TCVAR:(TARGET:LOCAL):外に出すから
		CONTINUE

	IF GETBIT(TCVAR:(TARGET:LOCAL):50,1) && !nPulledOut
		;todo - might make the failure state a condition for some kind of achievement that gives some kind of bonus related to semen and impregnation
		;custom - end sex here (which is near the end of processing) as it resets a lot of stuff like positions
		TCVAR:(TARGET:LOCAL):外に出すから = 0
		CALL ENDUFUFU_ALL
		TFLAG:106 = 0
		CALL GET_ANGRY(TARGET:LOCAL)
	ELSEIF GETBIT(TCVAR:(TARGET:LOCAL):50,8) && nPulledOut
		Qol_Info_Count_Array:(TARGET:LOCAL):Qol_Count_PulloutSex ++
	ENDIF
NEXT

;挿絵　挿入射精　正常位系列
IF GETBIT(TCVAR:50,1) && (TEQUIP:Ｖセックス != -1 || TEQUIP:USex != -1) && !TCVAR:PLAYER:コンドーム && GROUPMATCH(体位保存,1,2,3,4,5)
	SIF FLAG:画像表示 && FLAG:挿絵表示
		PRINTL
	SELECTCASE 体位保存
		CASE 1 ;image tweak custom code, standing missionary
			IF (!BEDROOM(CFLAG:PLAYER:現在位置) && !(BATHROOM() && ITEM:20)) || TFLAG:193
				CALL PRINT_IMAGE_ANIMATION_正常位("正常位_stand_射精")
			ELSE
				CALL PRINT_IMAGE_ANIMATION_正常位("正常位_射精")
			ENDIF
		CASE 1,3,4
			CALL PRINT_IMAGE_ANIMATION_正常位("正常位_射精")
		CASE 2,5
			CALL PRINT_IMAGE_ANIMATION_後背位("後背位_射精")
	ENDSELECT
ENDIF
IF GETBIT(TCVAR:50,2) && TEQUIP:Ａセックス != -1 && !TCVAR:PLAYER:コンドーム && GROUPMATCH(体位保存,1,2,3,4,5)
	SIF FLAG:画像表示 && FLAG:挿絵表示
		PRINTL
	SELECTCASE 体位保存
		CASE 1,3,4
			CALL PRINT_IMAGE_ANIMATION_正常位("Ａ正常位_射精")
		CASE 2,5
			CALL PRINT_IMAGE_ANIMATION_後背位("Ａ後背位_射精")
	ENDSELECT
ENDIF

;挿絵　挿入射精
;相手がコンドーム付けることあるのかな？
IF NOWEX:TARGET:射精 > 0 && (PLAYERＶセックス保存 != -1 || PLAYER_CAN_U_SEX != -1) && !TCVAR:TARGET:コンドーム && GROUPMATCH(PLAYER体位保存,1,2,3,4,5)
	SIF FLAG:画像表示 && FLAG:挿絵表示
		PRINTL
	SELECTCASE PLAYER体位保存
		CASE 1 ;image tweak custom code, standing missionary
			IF (!BEDROOM(CFLAG:PLAYER:現在位置) && !(BATHROOM() && ITEM:20)) || TFLAG:193
				CALL PRINT_IMAGE_ANIMATION_正常位("正常位_stand_射精")
			ELSE
				CALL PRINT_IMAGE_ANIMATION_正常位("正常位_射精")
			ENDIF
		CASE 1,3,4
			CALL PRINT_IMAGE_ANIMATION_正常位("正常位_射精")
		CASE 2,5
			CALL PRINT_IMAGE_ANIMATION_後背位("後背位_射精", 1)
	ENDSELECT
ENDIF
IF NOWEX:TARGET:射精 > 0 && PLAYERＡセックス保存 != -1 && !TCVAR:TARGET:コンドーム && GROUPMATCH(PLAYER体位保存,1,2,3,4,5)
	SIF FLAG:画像表示 && FLAG:挿絵表示
		PRINTL
	SELECTCASE PLAYER体位保存
		CASE 1,3,4
			CALL PRINT_IMAGE_ANIMATION_正常位("Ａ正常位_射精")
		CASE 2,5
			CALL PRINT_IMAGE_ANIMATION_後背位("Ａ後背位_射精", 1)
	ENDSELECT
ENDIF

;-------------------------------------------------
;調教後の口上
;-------------------------------------------------
;DEBUG	CALL KOJO_MESSAGE_PALAMCNG_C
CALL KOJO_MESSAGE_SEND("PALAM",3,TARGET,-1,-1)
;-------------------------------------------------
;パラメータ変動による口上
;-------------------------------------------------
;DEBUG	CALL KOJO_MESSAGE_PALAMCNG_D
CALL KOJO_MESSAGE_SEND("PALAM",4,TARGET,-1,-1)
;-------------------------------------------------
;刻印取得のチェック(イタズラ中は禁止)
;-------------------------------------------------
IF !CFLAG:TARGET:イタズラ && !TCVAR:MASTER:906
	CALL MARK_GOT_CHECK(TARGET)

CALL HMF_HeartfeltApology(TARGET) ;addition custom code

;-------------------------------------------------
;刻印取得による口上(イタズラ中は禁止)
;-------------------------------------------------
;DEBUG	CALL KOJO_MESSAGE_MARKCNG
	CALL KOJO_MESSAGE_SEND("MARK",-1,TARGET,-1,-1)
;-------------------------------------------------
;噴乳メッセージ
;-------------------------------------------------
SIF NOWEX:噴乳
	CALL TARGET_MILK_MESSAGE(TARGET)
;-------------------------------------------------
;従順上昇による[抵抗]の消滅チェック(イタズラ中は禁止)
;-------------------------------------------------
	CALL JUJUN_UP_CHECK
ENDIF
RESETCOLOR
;-------------------------------------------------
;調教ソースの表示
;-------------------------------------------------
CALL SHOW_SOURCE
;-------------------------------------------------
;数値変動の表示
;-------------------------------------------------
SETCOLOR C_YELLOW
;相性
;R = NO:PLAYER
R = MASTER ;vehicle fix.
SIF !GROUPMATCH(RELATION:R, 0,100)
	PRINTFORML ＜Affinity: x{RELATION:R/100}.%TOSTR(RELATION:R%100,"00")%＞
SIF TCVAR:TARGET:好きにして
	PRINTFORML ＜Being Played With＞

SIF SELECTCOM == 21
	PRINTFORML ＜[Do Nothing] increased your pleasure＞
SIF SELECTCOM == 22
	PRINTFORML ＜[Be Seduced] increased Dominance＞

SIF TCVAR:コマンドＣ補正 && SOURCE:快Ｃ
	PRINTFORML ＜Command C Pleasure Adjustment：{ABS(TCVAR:コマンドＣ補正)}％ \@ TCVAR:コマンドＣ補正 > 0 ? Up # Down\@＞
SIF TCVAR:コマンドＢ補正 && SOURCE:快Ｂ
	PRINTFORML ＜Command B Pleasure Adjustment：{ABS(TCVAR:コマンドＢ補正)}％ \@ TCVAR:コマンドＢ補正 > 0 ? Up # Down\@＞
SIF TCVAR:コマンドＶ補正 && SOURCE:快Ｖ
	PRINTFORML ＜Command V Pleasure Adjustment：{ABS(TCVAR:コマンドＶ補正)}％ \@ TCVAR:コマンドＶ補正 > 0 ? Up # Down\@＞
SIF TCVAR:コマンドＡ補正 && SOURCE:快Ａ
	PRINTFORML ＜Command A Pleasure Adjustment：{ABS(TCVAR:コマンドＡ補正)}％ \@ TCVAR:コマンドＡ補正 > 0 ? Up # Down\@＞
SIF TCVAR:コマンドＭ補正 && SOURCE:快Ｍ
	PRINTFORML ＜Command M Pleasure Adjustment：{ABS(TCVAR:コマンドＭ補正)}％ \@ TCVAR:コマンドＭ補正 > 0 ? Up # Down\@＞
SIF TCVAR:COMMAND_U_CORRECTION && SOURCE:UPleas
	PRINTFORML ＜Command U Pleasure Adjustment：{ABS(TCVAR:COMMAND_U_CORRECTION)}％ \@ TCVAR:COMMAND_U_CORRECTION > 0 ? Up # Down\@＞	
IF SELECTCOM == PREVCOM && TFLAG:50 == TFLAG:151 && (SELECTCOM < 350 || SELECTCOM >= 500) && 連続実行()
	PRINTL ＜Continuous execution of the same command＞
	BASE:Hypno += 15
ENDIF

;気力０
IF IsDead:TARGET:0
	SETCOLOR C_D_RED
	PRINTL ★Dead★
ELSEIF BASE:1 <= 0
	PRINTL ★0 ENE★
ENDIF
RESETCOLOR
LOCAL:1 = 0
FOR LOCAL,0,CHARANUM
	SIF CFLAG:LOCAL:現在位置 == SUKIMA()
		CONTINUE
	IF DOWNBASE:LOCAL:体力
		IF DOWNBASE:LOCAL:体力 > 0
			SETCOLOR C_YELLOW
			SIF DOWNBASE:LOCAL:体力 > 150
				SETCOLOR C_RED
			PRINTFORM STA-{DOWNBASE:LOCAL:体力} 
		ELSE
			SETCOLOR C_GREEN
			PRINTFORM STA+{-DOWNBASE:LOCAL:体力}
		ENDIF
		LOCAL:1 ++
	ENDIF
	IF DOWNBASE:LOCAL:気力
		IF DOWNBASE:LOCAL:気力 > 0
			SETCOLOR C_YELLOW
			SIF DOWNBASE:LOCAL:気力 > 150
				SETCOLOR C_RED
			PRINTFORM ENE-{DOWNBASE:LOCAL:気力} 
		ELSE
			SETCOLOR C_GREEN
			PRINTFORM ENE+{-DOWNBASE:LOCAL:気力}
		ENDIF
		LOCAL:1 ++
	ENDIF
	RESETCOLOR
	SIF LOCAL:1
		PRINTFORML (%CALLNAME:LOCAL%)
	LOCAL:1 = 0
NEXT

;NAS: Tsp Translation
IF FLAG:70 && TIME_PROGRESS(TIME:1)
	V:7 = TIME_PROGRESS(TIME:1)
	TIME -= V:7
	TIME:1 = TIME + DAY * 1440
	; TIME:1 -= V:7
	; TIME = TIME:1 - ((DAY*1440))
	BASE:MASTER:TSP -= (V:7*3)
ENDIF

SIF TIME_PROGRESS(TIME:1) > 0 && SELECTCOM != 400 && SELECTCOM != 402 && SELECTCOM != 604 && !CFLAG:MASTER:うふふ
	CALL COLORMESSAGE(@"+%PRINT_PLUR("minute", TIME_PROGRESS(TIME:1))%",0x5090FF, 1, 0) ;custom code
SIF nRemberTsp - BASE:MASTER:TSP > 0 ;qol custom code
	CALL COLORMESSAGE(@"TSP-{nRemberTsp - BASE:MASTER:TSP}",0x5090FF, 1, 0)
SIF TIME_PROGRESS(TIME:1) >= 40
	TCVAR:TARGET:なるべく動かない = 1
PRINTL 

;-------------------------------------------------
;パラメータの上昇＆表示（DOWNもここで）
;-------------------------------------------------
VARSET LOCAL
FOR LOCAL:1,0,CHARANUM
	SIF TCVAR:表示カット
		CONTINUE
	LOCAL:3 = 0
	FOR LOCAL,0,100
		SIF PALAM_TR(LOCAL) == ""
			CONTINUE
		SIF CUP:(LOCAL:1):LOCAL - CDOWN:(LOCAL:1):LOCAL == 0 && !QoL_ThresholdCheck(LOCAL, LOCAL:1)
			CONTINUE
		IF !LOCAL:3 && CFLAG:(LOCAL:1):現在位置 == CFLAG:MASTER:現在位置
			PRINTL
			PRINTFORML %CALLNAME:(LOCAL:1)%
		ENDIF
		LOCAL:3 ++
		LOCAL:2 = CUP:(LOCAL:1):LOCAL - CDOWN:(LOCAL:1):LOCAL
		IF CFLAG:(LOCAL:1):現在位置 == CFLAG:MASTER:現在位置
			PRINTFORM %PALAM_TR(PALAMNAME:LOCAL),12,LEFT% {PALAM:(LOCAL:1):LOCAL,10,LEFT} + {CUP:(LOCAL:1):LOCAL,10,LEFT} =   {PALAM:(LOCAL:1):LOCAL + CUP:(LOCAL:1):LOCAL,10,LEFT}
			; SELECTCASE LOCAL
			; CASE 10
			; 	IF SOURCE:LOCAL > 0 && LOCAL:1 != MASTER
			; 		LOCAL:4 = LewdMarkThreshold2:(MIN(MARK:(LOCAL:1):不埒刻印+1,5))
			; 		PRINTFORM  \@ MARK:不埒刻印 >= 5 ? (At max lewd) # ({(CUP:(LOCAL:1):LOCAL+CUP:12:LOCAL)*100/LOCAL:4}\% to lewd mark {MARK:(LOCAL:1):不埒刻印+1})\@
			; 	ENDIF	
			; CASE 15
			; 	IF SOURCE:LOCAL > 0 && LOCAL:1 != MASTER
			; 		LOCAL:4 = PainMarkThreshold:(MIN(MARK:(LOCAL:1):苦痛刻印+1,5))
			; 		PRINTFORM  \@ MARK:苦痛刻印 >= 5 ? (At max hate) # ({CUP:(LOCAL:1):LOCAL*100/LOCAL:4}\% to pain mark {MARK:(LOCAL:1):苦痛刻印+1})\@
			; 	ENDIF	
			; CASE 30
			; 	IF SOURCE:LOCAL > 0 && LOCAL:1 != MASTER
			; 		LOCAL:4 = HateMarkThreshold:(MIN(MARK:(LOCAL:1):反発刻印+1,5))
			; 		PRINTFORM  \@ MARK:反発刻印 >= 5 ? (At max hate) # ({CUP:(LOCAL:1):LOCAL*100/LOCAL:4}\% to hate mark {MARK:(LOCAL:1):反発刻印+1})\@
			; 	ENDIF		
			; CASE 33
			; 	IF SOURCE:LOCAL > 0 && LOCAL:1 != MASTER
			; 		LOCAL:4 = RegressionMarkThreshold:(MIN(MARK:(LOCAL:1):RegressionMark+1,5))
			; 		PRINTFORM  \@ MARK:RegressionMark >= 5 ? (At max hate) # ({CUP:(LOCAL:1):LOCAL*100/LOCAL:4}\% to regression mark {MARK:(LOCAL:1):RegressionMark+1})\@
			; 	ENDIF
			; ENDSELECT
			CALL QoL_MarkGainThreshold(LOCAL, LOCAL:1) ;qol custom code
			PRINTL
		ENDIF
		PALAM:(LOCAL:1):LOCAL += CUP:(LOCAL:1):LOCAL - CDOWN:(LOCAL:1):LOCAL
		CUP:(LOCAL:1):LOCAL = 0
		CDOWN:(LOCAL:1):LOCAL = 0
	NEXT
NEXT
PRINTL

IF GETBIT(TCVAR:50,1) && (TEQUIP:Ｖセックス != -1 || TEQUIP:USex != -1) && !TCVAR:PLAYER:コンドーム && GROUPMATCH(体位保存,1,2,3,4,5)
	SELECTCASE 体位保存
		CASE 1 ;image tweak custom code, standing missionary
			IF (!BEDROOM(CFLAG:PLAYER:現在位置) && !(BATHROOM() && ITEM:20)) || TFLAG:193
				CALL PRINT_IMAGE_ANIMATION_射精後("正常位_stand_中出し")
			ELSE
				CALL PRINT_IMAGE_ANIMATION_射精後("正常位_中出し")
			ENDIF
		CASE 1,3,4
			CALL PRINT_IMAGE_ANIMATION_射精後("正常位_中出し")
		CASE 2,5
			CALL PRINT_IMAGE_ANIMATION_射精後("後背位_中出し")
	ENDSELECT
	IF FLAG:画像表示 && FLAG:挿絵表示
		WAIT
		PRINTL
	ENDIF
ENDIF

IF NOWEX:TARGET:射精 > 0 && (PLAYERＶセックス保存 != -1 || PLAYER_CAN_U_SEX != -1) && !TCVAR:TARGET:コンドーム && GROUPMATCH(PLAYER体位保存,1,2,3,4,5)
	SELECTCASE PLAYER体位保存
		CASE 1 ;image tweak custom code, standing missionary
			IF (!BEDROOM(CFLAG:PLAYER:現在位置) && !(BATHROOM() && ITEM:20)) || TFLAG:193
				CALL PRINT_IMAGE_ANIMATION_射精後("正常位_stand_中出し")
			ELSE
				CALL PRINT_IMAGE_ANIMATION_射精後("正常位_中出し")
			ENDIF
		CASE 1,3,4
			CALL PRINT_IMAGE_ANIMATION_射精後("正常位_中出し")
		CASE 2,5
			CALL PRINT_IMAGE_ANIMATION_射精後("後背位_中出し", 1)
	ENDSELECT
	IF FLAG:画像表示 && FLAG:挿絵表示
		WAIT
		PRINTL
	ENDIF
ENDIF

IF GETBIT(TCVAR:50,2) && TEQUIP:Ａセックス != -1 && !TCVAR:PLAYER:コンドーム && GROUPMATCH(体位保存,1,2,3,4,5)
	SELECTCASE 体位保存
		CASE 1,3,4
			CALL PRINT_IMAGE_ANIMATION_射精後("Ａ正常位_中出し")
		CASE 2,5
			CALL PRINT_IMAGE_ANIMATION_射精後("Ａ後背位_中出し")
	ENDSELECT
	IF FLAG:画像表示 && FLAG:挿絵表示
		WAIT
		PRINTL
	ENDIF
ENDIF

IF NOWEX:TARGET:射精 > 0 && PLAYERＡセックス保存 != -1 && !TCVAR:TARGET:コンドーム && GROUPMATCH(PLAYER体位保存,1,2,3,4,5)
	SELECTCASE PLAYER体位保存
		CASE 1,3,4
			CALL PRINT_IMAGE_ANIMATION_射精後("Ａ正常位_中出し")
		CASE 2,5
			CALL PRINT_IMAGE_ANIMATION_射精後("Ａ後背位_中出し", 1)
	ENDSELECT
	IF FLAG:画像表示 && FLAG:挿絵表示
		WAIT
		PRINTL
	ENDIF
ENDIF

DRAWLINE
;-------------------------------------------------
;欲求不満度上昇
;-------------------------------------------------
IF TCVAR:TARGET:欲求不満度管理 < GETPALAMLV(PALAM:欲情,15) && TARGET != MASTER && nPassiveHorny
	欲求不満度上昇 = (GETPALAMLV(PALAM:欲情,15) - TCVAR:TARGET:欲求不満度管理) * 30
	IF TCVAR:TARGET:媚薬
		欲求不満度上昇 = 欲求不満度上昇 * (30 + TCVAR:TARGET:発情 * 20 + TCVAR:TARGET:媚薬 * 20) / 30
	ELSEIF TCVAR:TARGET:発情 || TFLAG:仙香玉兎
		欲求不満度上昇 = 欲求不満度上昇 * (30 + TCVAR:TARGET:発情 * 20 + TFLAG:仙香玉兎 * 10) / 30
	;Lust mode custom code
	ELSEIF FLAG:甲斐性無 && !FLAG:LUSTMODE
		;なかなか手を出さない相手を見るとやきもきするよね
		欲求不満度上昇 = 欲求不満度上昇 * MAX((1000 - CFLAG:TARGET:溜まってる度) / 5 - 110, 30) / 30
	ENDIF
	SIF TALENT:TARGET:行きずり == 1
		欲求不満度上昇 *= 3
	欲求不満度上昇 = 欲求不満素質補正(TARGET, 欲求不満度上昇)
	CFLAG:TARGET:溜まってる度 = LIMIT(欲求不満度上昇 + CFLAG:TARGET:溜まってる度, 0, 1000)
	CFLAG:MASTER:溜まってる度 = LIMIT(欲求不満度上昇 + CFLAG:MASTER:溜まってる度, 0, 1000)
	TCVAR:TARGET:欲求不満度管理 = GETPALAMLV(PALAM:欲情,15)
ENDIF

;cancel out insertions from condom sex
IF SEX_DISENGAGE
	CALL 挿入解除(SEX_DISENGAGE-1)
	SEX_DISENGAGE = 0
ENDIF
nPulledOut = 0

;-------------------------------------------------
;好感度上昇
;-------------------------------------------------
CALL FAVOR_CALC(TARGET)

;-------------------------------------------------
;信頼度上昇（ウフフ以外＆上昇対象コマンド実行時）
;-------------------------------------------------
SIF (TFLAG:98 && !CFLAG:TARGET:うふふ) && !CFLAG:TARGET:睡眠
	CALL TRUST_CALC(TARGET)

;-------------------------------------------------
;泡風呂タイムアップ処理
;-------------------------------------------------
SIF TFLAG:泡風呂 && (DAY * 1440 + TIME >= TFLAG:泡風呂)
	CALL 泡風呂終了判定("タイムアップ")

;-------------------------------------------------
;潜伏率の増減処理
;-------------------------------------------------
SIF GETBIT(FLAG:潜伏,0)
	CALL 潜伏率増減処理
	
;-------------------------------------------------
;睡眠深度0による覚醒処理
;-------------------------------------------------
SIF CFLAG:MASTER:イタズラ == 2 && (TCVAR:TARGET:睡眠深度 <= 0 || !睡眠時間(TARGET))
	CALL 睡眠姦発覚(TARGET)

;-------------------------------------------------
;抱きつきモードの解除
;-------------------------------------------------
IF TFLAG:102 == 3 && TFLAG:抱きつきモード
	TFLAG:抱きつき解放ポイント --
	IF TFLAG:抱きつき解放ポイント <= 0
		PRINTFORML Satisfied, %CALLNAME% lets go of your body...
		CALL 押し倒され終了
		TFLAG:抱きつきモード = 0
		TCVAR:抱きつき禁止時間 = 90 + MAX(10 - ABL:欲望, 0) * 3 - MAX((CFLAG:積極性 + CFLAG:恥じらい) * 5, 30)
	ENDIF
ENDIF

;-------------------------------------------------
;変数のリセット
;-------------------------------------------------
CVARSET TCVAR, GETNUM(TCVAR, "奉仕フラグ"), 0
CVARSET TCVAR, GETNUM(TCVAR, "射精ゲージ増加量"), 0
CVARSET TCVAR, GETNUM(TCVAR, "泥酔姦バレ"), 0
CVARSET TCVAR, GETNUM(TCVAR, "料理評価値"), 0
;-------------------------------------------------
;調教ソースの表示（DOWNもここで）
;-------------------------------------------------
@SHOW_SOURCE
SIF TCVAR:表示カット
	RETURN 0
;custom code replace
CALL SOURCE_TR(TARGET)
RETURN
;--------------------

;PRINTL 　
SIF SOURCE:快Ｃ > 0
	PRINTFORM 快Ｃ({SOURCE:快Ｃ})
SIF SOURCE:快Ｖ > 0
	PRINTFORM 快Ｖ({SOURCE:快Ｖ})
SIF SOURCE:快Ａ > 0
	PRINTFORM 快Ａ({SOURCE:快Ａ})
SIF SOURCE:快Ｂ > 0
	PRINTFORM 快Ｂ({SOURCE:快Ｂ})
SIF SOURCE:快Ｍ > 0
	PRINTFORM 快Ｍ({SOURCE:快Ｍ})
SIF SOURCE:情愛 > 0
	PRINTFORM 情愛({SOURCE:情愛})
SIF SOURCE:性行動 > 0
	PRINTFORM 性行動({SOURCE:性行動})
SIF SOURCE:達成 > 0
	PRINTFORM 達成感({SOURCE:達成})
SIF SOURCE:苦痛 > 0
	PRINTFORM 痛み({SOURCE:苦痛})
SIF SOURCE:恐怖 > 0
	PRINTFORM 恐れ({SOURCE:恐怖})
SIF SOURCE:液体 > 0
	PRINTFORM 液体({SOURCE:液体})
SIF SOURCE:欲情 > 0
	PRINTFORM 欲情({SOURCE:欲情})
SIF SOURCE:恭順 > 0
	PRINTFORM 恭順({SOURCE:恭順})
SIF SOURCE:露出 > 0
	PRINTFORM 露出({SOURCE:露出})
SIF SOURCE:屈従 > 0
	PRINTFORM 屈服({SOURCE:屈従})
SIF SOURCE:歓楽 > 0
	PRINTFORM 歓楽({SOURCE:歓楽})
SIF SOURCE:征服 > 0
	PRINTFORM 征服({SOURCE:征服})
SIF SOURCE:強要 > 0
	PRINTFORM 征服({SOURCE:強要})
SIF SOURCE:受動 > 0
	PRINTFORM 受動({SOURCE:受動})
SIF SOURCE:不潔 > 0
	PRINTFORM 不潔({SOURCE:不潔})
SIF SOURCE:鬱屈 > 0
	PRINTFORM 鬱屈({SOURCE:鬱屈})
SIF SOURCE:逸脱 > 0
	PRINTFORM 逸脱({SOURCE:逸脱})
SIF SOURCE:反感 > 0
	PRINTFORM 反発({SOURCE:反感})
PRINTL 　

@OAZUKE(ARG)
#FUNCTION
SIF CFLAG:ARG:うふふ != 2
	RETURNF 0
SELECTCASE TCVAR:ARG:20
	CASE 41
	;正常位V
		SIF SELECTCOM != 320
			RETURNF 1
	CASE 43
	;バックV
		SIF SELECTCOM != 322
			RETURNF 1
	CASE 44
	;バックA
		SIF SELECTCOM != 323
			RETURNF 1
ENDSELECT
RETURNF 0