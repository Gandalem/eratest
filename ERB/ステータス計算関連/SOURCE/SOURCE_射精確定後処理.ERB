; Modular ERB: SOURCE/SOURCE_射精確定後処理.ERB
;射精確定後の処理

@EJAC_CHECK_AFTER(キャラ登録番号)
#DIM キャラ登録番号
#DIM 射精回数
#DIM 過剰射精
VARSET 射精回数
VARSET 過剰射精
;キャラ登録番号 射精する人
IF !TCVAR:キャラ登録番号:イきそう
	IF BASE:キャラ登録番号:射精 > MAXBASE:キャラ登録番号:射精
		IF BASE:キャラ登録番号:勃起 < 1000
			BASE:キャラ登録番号:射精 = MAXBASE:キャラ登録番号:射精 - 1
		ELSE
			TCVAR:キャラ登録番号:イきそう = 1
		ENDIF
	ENDIF
	RETURN 0
ELSEIF (TEQUIP:キャラ登録番号:焦らしモード || CFLAG:キャラ登録番号:Hypno_Orgasm_Denial) && キャラ登録番号 != MASTER ;custom code
	TCVAR:キャラ登録番号:焦らし回数 += 2
	RETURN 0
ENDIF
BASE:キャラ登録番号:精力 = MAX(0, BASE:キャラ登録番号:精力) ;bug fix custom code, failsafe
;射精(絶頂)回数
LOCAL = MAX(MAXBASE:キャラ登録番号:射精,1); bug fix
射精回数 = MIN(BASE:キャラ登録番号:射精 / LOCAL, (100 + BASE:キャラ登録番号:精力) / 100)
;5x nerf
IF TALENT:キャラ登録番号:AbnormalCumProduction || !GETBIT(NASFeature,7)
	NOWEX:キャラ登録番号:射精量 = MAX(BASE:キャラ登録番号:射精 * 10 / (ABL:キャラ登録番号:Ｃ感覚 / 2 + 10) / 100,5)
ELSE
	NOWEX:キャラ登録番号:射精量 = MAX(BASE:キャラ登録番号:射精 * 10 / (ABL:キャラ登録番号:Ｃ感覚 / 2 + 10) / 500,5)
ENDIF
;addition custom code, revert to old style amount calculation (canceled in favor of new ejaculation volume calculation)
;NOWEX:キャラ登録番号:射精量 = BASE:キャラ登録番号:射精 * (BASE:キャラ登録番号:精力 + 1000 ) / ((MAXBASE:キャラ登録番号:精力 + 1000 ) * 100)
SIF NOWEX:キャラ登録番号:射精量 > 400
	NOWEX:キャラ登録番号:射精量 = 300 + (NOWEX:キャラ登録番号:射精量 - 300) * 7 / 10
SIF nKillerOrgasm ;cum a shit ton when killer orgasm is enabled
	NOWEX:キャラ登録番号:射精量 = 100000
IF NOWEX:キャラ登録番号:射精量 > BASE:キャラ登録番号:精力
	NOWEX:キャラ登録番号:射精量 = (NOWEX:キャラ登録番号:射精量 - BASE:キャラ登録番号:精力) / 5 + BASE:キャラ登録番号:精力
	過剰射精 = NOWEX:キャラ登録番号:射精量 - BASE:キャラ登録番号:精力
ENDIF
IF 過剰射精
	IF TALENT:キャラ登録番号:絶倫 ;addition custom code
	ELSEIF TFLAG:ポイズンボディ
		DOWNBASE:キャラ登録番号:体力 += 過剰射精 * 10
	ELSE
		DOWNBASE:キャラ登録番号:体力 += 過剰射精 * 4
	ENDIF
ENDIF
;addition custom code
SIF NO:キャラ登録番号 == MASTER && BASE:キャラ登録番号:精力 >= NOWEX:キャラ登録番号:射精量
	nVigCheck += NOWEX:キャラ登録番号:射精量
IF TALENT:キャラ登録番号:AbnormalCumProduction || !GETBIT(NASFeature,7)
	BASE:キャラ登録番号:精力 = MAX(0, BASE:キャラ登録番号:精力 - (NOWEX:キャラ登録番号:射精量))
ELSE
	BASE:キャラ登録番号:精力 = MAX(0, BASE:キャラ登録番号:精力 - (NOWEX:キャラ登録番号:射精量*10))
ENDIF
TCVAR:キャラ登録番号:イきそう = 0
STAIN:キャラ登録番号:Ｐ |= 4
;BASEの処理
BASE:キャラ登録番号:射精 = 0
CSTR:キャラ登録番号:射精後処理用 = %CSTR:キャラ登録番号:射精判定用%
TCVAR:キャラ登録番号:射精した箇所フラグ = TCVAR:キャラ登録番号:射精箇所
;異常経験、射精経験、精液経験
;女性器がある場合？
SIF !EXP:キャラ登録番号:射精経験 && TALENT:キャラ登録番号:性別 & 1
	EXP:キャラ登録番号:異常経験 += 1
EXP:キャラ登録番号:絶頂経験 ++
EXP:キャラ登録番号:射精経験 += 射精回数
NOWEX:キャラ登録番号:射精 += 射精回数
EX:キャラ登録番号:射精 += 射精回数
TCVAR:キャラ登録番号:焦らし回数 = 0
TEQUIP:キャラ登録番号:焦らしモード = 0
SIF AT_HOME(キャラ登録番号)
	YOGORE:(CFLAG:キャラ登録番号:現在位置) += 射精回数 * 50
IF TALENT:キャラ登録番号:絶倫 ;addition custom code
ELSEIF TFLAG:ポイズンボディ
	DOWNBASE:キャラ登録番号:体力 += LIMIT(NOWEX:キャラ登録番号:射精量 / 4, 30, 300)
	DOWNBASE:キャラ登録番号:気力 += LIMIT(NOWEX:キャラ登録番号:射精量 / 2, 40, 400)
ELSE
	DOWNBASE:キャラ登録番号:体力 += LIMIT(NOWEX:キャラ登録番号:射精量 / 8, 10, 100)
	DOWNBASE:キャラ登録番号:気力 += LIMIT(NOWEX:キャラ登録番号:射精量 / 4, 20, 200)
ENDIF
;MASTERでなければBASEやSOURCE等の処理を入れる
SIF キャラ登録番号 == MASTER
	RETURN 0
IF EXP:キャラ登録番号:射精経験 < EXPLV:1
	SOURCE:キャラ登録番号:露出 += 5000 * (射精回数 + 1) / 2
	SOURCE:キャラ登録番号:屈従 += 2500 * (射精回数 + 1) / 2
	SOURCE:キャラ登録番号:不潔 += 200 * (射精回数 + 1) / 2
ELSEIF EXP:キャラ登録番号:射精経験 < EXPLV:2
	SOURCE:キャラ登録番号:露出 += 2500 * (射精回数 + 1) / 2
	SOURCE:キャラ登録番号:屈従 += 2000 * (射精回数 + 1) / 2
	SOURCE:キャラ登録番号:不潔 += 150 * (射精回数 + 1) / 2
ELSEIF EXP:キャラ登録番号:射精経験 < EXPLV:3
	SOURCE:キャラ登録番号:露出 += 1000 * (射精回数 + 1) / 2
	SOURCE:キャラ登録番号:屈従 += 800 * (射精回数 + 1) / 2
	SOURCE:キャラ登録番号:不潔 += 100 * (射精回数 + 1) / 2
ELSEIF EXP:キャラ登録番号:射精経験 < EXPLV:4
	SOURCE:キャラ登録番号:露出 += 800 * (射精回数 + 1) / 2
	SOURCE:キャラ登録番号:屈従 += 600 * (射精回数 + 1) / 2
	SOURCE:キャラ登録番号:不潔 += 50 * (射精回数 + 1) / 2
ELSEIF EXP:キャラ登録番号:射精経験 < EXPLV:5
	SOURCE:キャラ登録番号:露出 += 400 * (射精回数 + 1) / 2
	SOURCE:キャラ登録番号:屈従 += 400 * (射精回数 + 1) / 2
	SOURCE:キャラ登録番号:不潔 += 10 * (射精回数 + 1) / 2
ELSE
	SOURCE:キャラ登録番号:露出 += 200 * (射精回数 + 1) / 2
	SOURCE:キャラ登録番号:屈従 += 250 * (射精回数 + 1) / 2
ENDIF
;-------------------------------------------------
;調教対象の射精チェック
;-------------------------------------------------

@TARGET_EJAC_CHECK(キャラ登録番号)
#DIM キャラ登録番号
;男性器がなければ帰る
SIF !HAS_PENIS(キャラ登録番号)
	RETURN 0
LOCAL = (CUP:キャラ登録番号:快Ｃ + CUP:キャラ登録番号:快Ｖ + CUP:キャラ登録番号:快Ａ + CUP:キャラ登録番号:快Ｂ) / 4
;自制心
SIF TALENT:キャラ登録番号:自制心
	LOCAL /= 2
;快感に素直
SIF TALENT:キャラ登録番号:快感応答 > 0
	TIMES LOCAL , 1.20
;快感の否定（淫乱、娼婦で無効）
SIF TALENT:キャラ登録番号:快感応答 < 0 && !TALENT:キャラ登録番号:淫乱 && ABL:キャラ登録番号:欲望 < 20 ;qol custom code, counteract denies pleasure
	TIMES LOCAL , 0.80
;媚薬
SIF (TCVAR:キャラ登録番号:媚薬 || IsInsenceValid(キャラ登録番号)) && キャラ登録番号 != MASTER
	LOCAL *= 2
;仙香玉兎
SIF IsInsenceValid(キャラ登録番号)
	LOCAL *= 2
;利尿剤
SIF TCVAR:キャラ登録番号:利尿剤
	LOCAL /= 2
;睡眠中
SIF CFLAG:キャラ登録番号:睡眠
	LOCAL /= 2
BASE:キャラ登録番号:射精 += LOCAL
;SIF BASE:キャラ登録番号:射精 > MAXBASE:キャラ登録番号:射精
;	BASE:キャラ登録番号:射精 = MAXBASE:キャラ登録番号:射精 - 1
;射精処理のキャラ別ターミナル

@SAMEN_SHOOT(射精したキャラ番号)
#DIM 射精したキャラ番号
#DIM 射精されたキャラ番号
FOR 射精されたキャラ番号, 0, CHARANUM
SIF SAMEN_DIRECTION2(射精したキャラ番号, 射精されたキャラ番号)
	CALL SAMEN_SHOOT2(射精したキャラ番号, 射精されたキャラ番号)
NEXT
;部位別処理

@SAMEN_SHOOT2(SENDER, RECEIVER)
#DIM SENDER
#DIM RECEIVER
#DIM 無理矢理反感
#DIM 無理矢理恐怖
#DIM 信頼マイナス
#DIM 好感度マイナス
#DIM 欲求不満解消係数
#DIM 中毒ボーナス
#DIM モブ子用
#DIM DYNAMIC ALSO_ANAL ;addition custom code, a quick hack set to true during DP
CALL TWKR_ORIG_SAMEN(RECEIVER, SENDER) ;com custom code, ejaculation size (from KR version)
中毒ボーナス = 0
IF SENDER == MASTER && CFLAG:RECEIVER:うふふ == 2
	SOURCE:RECEIVER:達成 += NOWEX:SENDER:射精 * 150
	SOURCE:RECEIVER:征服 += NOWEX:SENDER:射精 * 150
	SIF !(NOWEX:RECEIVER:Ｃ絶頂 + NOWEX:RECEIVER:Ｖ絶頂 + NOWEX:RECEIVER:Ａ絶頂 + NOWEX:RECEIVER:Ｂ絶頂 + NOWEX:RECEIVER:Ｍ絶頂)
		SOURCE:RECEIVER:征服 += NOWEX:SENDER:射精 * 100
ENDIF
IF SENDER == MASTER && TALENT:RECEIVER:行きずり == 1
	モブ子用 = MIN(NOWEX:SENDER:射精,3)
	SELECTCASE TCVAR:SENDER:射精した箇所フラグ
		CASE 1
			SIF ABL:RECEIVER:膣 < モブ子用
				CALL ChangeABL(RECEIVER, "膣", 1)
		CASE 2
			SIF ABL:RECEIVER:アナル < モブ子用
				CALL ChangeABL(RECEIVER, "アナル", 1)
		CASE 3
			SIF ABL:RECEIVER:指 < モブ子用
				CALL ChangeABL(RECEIVER, "指", 1)
		CASE 4
			SIF ABL:RECEIVER:舌 < モブ子用
				CALL ChangeABL(RECEIVER, "舌", 1)
		CASE 5 ;bugfix custom code
			SIF ABL:RECEIVER:胸 < モブ子用
				CALL ChangeABL(RECEIVER, "胸", 1)
		CASE 50 ;addition custom code
			SIF ABL:RECEIVER:膣 < モブ子用
				CALL ChangeABL(RECEIVER, "膣", 1)
			SIF ABL:RECEIVER:アナル < モブ子用
				CALL ChangeABL(RECEIVER, "アナル", 1)
	ENDSELECT
ENDIF
IF TEQUIP:SENDER:コンドーム == 1
	;ゴムを外す
	TEQUIP:SENDER:コンドーム = 0
	IF RECEIVER
		SELECTCASE TCVAR:SENDER:射精した箇所フラグ
			CASE 1
				CFLAG:RECEIVER:溜まってる度 += ABL:RECEIVER:膣射中毒 * NOWEX:SENDER:射精 * 15
			CASE 2
				CFLAG:RECEIVER:溜まってる度 += ABL:RECEIVER:肛射中毒 * NOWEX:SENDER:射精 * 15
			CASE 50 ;addition custom code
				CFLAG:RECEIVER:溜まってる度 += (ABL:RECEIVER:膣射中毒 + ABL:RECEIVER:肛射中毒) * NOWEX:SENDER:射精 * 15
		ENDSELECT
	ENDIF
	;接触のリセット
	;CALL 挿入解除(RECEIVER) ;custom code, comment out because this part creates a ton of problems, it messes up the detection and proper dialogue calls are not executed
	TFLAG:ゴム内 += NOWEX:SENDER:射精量 ;com custom code
	IF CFLAG:自動ゴム && ITEM:コンドーム ;&& SENDER == MASTER ;com custom code
		ITEM:コンドーム --
		TEQUIP:SENDER:コンドーム = 1
	ELSE
		;send a flag to cancel all insertions to emulate the function above, but after all the stuff is processed, wrap it up at source.erb
		SEX_DISENGAGE = RECEIVER + 1
	ENDIF
ELSE
	;TCVAR:12 射精箇所フラグ（1=膣内 2=アナル 3=手淫 4=口淫 5=パイズリ 6=素股 7=足コキ 8=体表 9=アナル奉仕 10=道具 11=触手）
	IF EQUIP:SENDER:下半身下着２ && !PANTIES_SHIFT_ASIDE(SENDER) ;NAS: waste ejac in undies if not shifted
		TCVAR:SENDER:射精した箇所フラグ = 63
	ENDIF
	SIF TEQUIP:SENDER:オナホール ;com custom code, set to onahole ejaculation if equipped
		TCVAR:SENDER:射精した箇所フラグ = 10
	;pullout promise patch
	;note - the original was broken as player doesn't receive 外に出すから and doesn't need to (and it checked pc for some reason), this part is processed first, then the SOURCE_射精確定前処理.ERB part, then the orgasm description
	;custom edit - modified from chance-based to player input
	IF GROUPMATCH(TCVAR:SENDER:射精した箇所フラグ, 1, 50) && !TCVAR:RECEIVER:ピル && TCVAR:RECEIVER:外に出すから == 1 ;if on pill, just let it happen harmlessly
		LOCAL = LIMIT(MULTIPLY(RAND(7000, 10001), 100 - LIMIT(ABL:技巧 + ABL:膣 - GETEXPLV(EXP:MASTER:GETNUM(EXP, "挿入経験"), 12) - ABL:PLAYER:技巧 - TALENT:PLAYER:自制心*3, 0, 10)*10) - (TALENT:PLAYER:絶倫 || EXP:PLAYER:Ｖ性交経験 <= 100)*3000, 1500, 10000)
		REDRAW 1
		PRINTFORML  [0] 빼!
		PRINTFORML  [1] 포기해
		TINPUT LOCAL, 100, 1, "Time Out!"
		;nakadashi happens
		IF RESULT
			SIF RESULT == 1
				PRINTFORML 당신은 압도적인 욕정에 굴복하고 약속을 잊는다...
		ELSE
			;pull out, set to outside
			TCVAR:SENDER:射精した箇所フラグ = 8
			nPulledOut = 1
			SEX_DISENGAGE = RECEIVER + 1
		ENDIF
	ENDIF
	;--------------------------------
	SELECTCASE TCVAR:SENDER:射精した箇所フラグ
		;膣内
		CASE 1
			$VAGINAL_EJAC
			EXP:RECEIVER:精液経験 += NOWEX:SENDER:射精 * 2
			EXP:RECEIVER:膣射経験 += NOWEX:SENDER:射精 * 2
			STAIN:RECEIVER:Ｖ |= 4
			STAIN:RECEIVER:膣内 |= 4
			TCVAR:RECEIVER:Vに中出しされたフラグ = 2
			TCVAR:RECEIVER:Vに中出ししたキャラの番号 = SENDER
			IF FLAG:時間停止 == 1
				CFLAG:RECEIVER:膣内射精 = 2
			ELSE
				CFLAG:RECEIVER:膣内射精 = 1
			ENDIF
			NOWEX:RECEIVER:膣内精液 = NOWEX:SENDER:射精量
			EX:RECEIVER:膣内精液 += NOWEX:RECEIVER:膣内精液
			欲求不満解消係数 = 800 / MIN(100 + ABL:RECEIVER:膣射中毒 * 10,200)
			SIF NOWEX:RECEIVER:膣内精液 > 0
				CFLAG:RECEIVER:溜まってる度 -= SQRT(NOWEX:RECEIVER:膣内精液) * 欲求不満解消係数
			CFLAG:RECEIVER:累計膣内精液 += NOWEX:RECEIVER:膣内精液
			CALL POISONBODY(RECEIVER, SENDER, 1)
			;SIF !TCVAR:RECEIVER:ピル
			;PRINTFORML %CALLNAME:SENDER%が%CALLNAME:RECEIVER%に{NOWEX:RECEIVER:膣内精液}出した
			;#; PRINTFORML CALLING SEMEN SHOOT 3: Amount: {NOWEX:RECEIVER:膣内精液}, Sender: %NAME:SENDER%, Receiver %NAME:RECEIVER%
			CALL SAMEN_SHOOT3(NOWEX:RECEIVER:膣内精液 , SENDER, RECEIVER)
			IF SHIRAHU(RECEIVER)
				TCVAR:RECEIVER:種付けられ自覚 = 1
			ENDIF
			IF SHIRAHU(SENDER) && SENDER != MASTER
				TCVAR:SENDER:種付け自覚 = 1
			ENDIF
			IF TCVAR:RECEIVER:無理矢理
				IF TALENT:RECEIVER:行きずり == 1
					CSTR:RECEIVER:モブ子プロフィール２ = %REPLACE(@"%CSTR:RECEIVER:モブ子プロフィール２%","され今はもう大人しい","され、膣内射精された")%
					SIF !STRCOUNT(CSTR:RECEIVER:モブ子プロフィール２, "膣内射精された")
						CSTR:RECEIVER:モブ子プロフィール２ = %REPLACE(@"%CSTR:RECEIVER:モブ子プロフィール２%","処女を奪われた","処女を奪われた挙句、膣内射精された")%
					IF EX:RECEIVER:膣内精液 >= 300 && !STRCOUNT(CSTR:RECEIVER:モブ子プロフィール２, "たっぷりと")
						CSTR:RECEIVER:モブ子プロフィール２ = %REPLACE(@"%CSTR:RECEIVER:モブ子プロフィール２%","膣内射精された","たっぷりと精液を注ぎ込まれた")%
					ENDIF
					IF モブ子初期Ｖ性交経験 + 10 <= EXP:RECEIVER:Ｖ性交経験
						IF !STRCOUNT(CSTR:RECEIVER:モブ子プロフィール２, "ハメ倒されて")
							CSTR:RECEIVER:モブ子プロフィール２ = %REPLACE(@"%CSTR:RECEIVER:モブ子プロフィール２%","膣内射精された", "ハメ倒されて膣内射精された")%
						ENDIF
						IF モブ子初期Ｖ性交経験 + 15 <= EXP:RECEIVER:Ｖ性交経験 && !STRCOUNT(CSTR:RECEIVER:モブ子プロフィール２, "散々")
							CSTR:RECEIVER:モブ子プロフィール２ = %REPLACE(@"%CSTR:RECEIVER:モブ子プロフィール２%","ハメ倒されて", "散々ハメ倒されて")%
						ENDIF
					ENDIF
					IF MARK:RECEIVER:快楽刻印 && !STRCOUNT(CSTR:RECEIVER:モブ子プロフィール２, "イかされ")
						CSTR:RECEIVER:モブ子プロフィール２ = %REPLACE(@"%CSTR:RECEIVER:モブ子プロフィール２%","ハメ倒されて", "ハメ倒されてイかされ")%
					ENDIF
					IF MARK:RECEIVER:快楽刻印 && !STRCOUNT(CSTR:RECEIVER:モブ子プロフィール２, "イキ狂わされ")
						CSTR:RECEIVER:モブ子プロフィール２ = %REPLACE(@"%CSTR:RECEIVER:モブ子プロフィール２%","イかされ", "イキ狂わされ")%
					ENDIF
				ENDIF
				SELECTCASE ABL:RECEIVER:膣射中毒
					CASE IS < 3
						無理矢理反感 = 100000
						好感度マイナス = NOWEX:SENDER:射精 * RAND:50 + NOWEX:SENDER:射精 * RAND:50
						信頼マイナス = NOWEX:SENDER:射精 * RAND:20 + NOWEX:SENDER:射精 * RAND:20
						無理矢理恐怖 = NOWEX:RECEIVER:膣内精液 * 5
					CASE 3 TO 5
						無理矢理反感 = 50000
						好感度マイナス = NOWEX:SENDER:射精 * RAND:30 + NOWEX:SENDER:射精 * RAND:30
						信頼マイナス = NOWEX:SENDER:射精 * RAND:15 + NOWEX:SENDER:射精 * RAND:15
						無理矢理恐怖 = NOWEX:RECEIVER:膣内精液 * 3
					CASEELSE
						無理矢理反感 = 10000
						好感度マイナス = NOWEX:SENDER:射精 * RAND:10 + NOWEX:SENDER:射精 * RAND:10
						信頼マイナス = NOWEX:SENDER:射精 * RAND:10 + NOWEX:SENDER:射精 * RAND:10
						無理矢理恐怖 = NOWEX:RECEIVER:膣内精液
				ENDSELECT
				IF CFLAG:RECEIVER:生理周期 == 7
					無理矢理反感 *= 2
					無理矢理恐怖 *= 2
					好感度マイナス *= 2
					信頼マイナス *= 2
				ENDIF
				IF CFLAG:RECEIVER:無理矢理膣射 > 1
					無理矢理反感 *= 2
					無理矢理恐怖 *= 2
					好感度マイナス *= 2
					信頼マイナス *= 2
				ENDIF
				SOURCE:RECEIVER:反感 += 無理矢理反感
				IF CFLAG:RECEIVER:無理矢理膣射 > 1
					CFLAG:RECEIVER:無理矢理膣射 = 3
				ELSE
					CFLAG:RECEIVER:無理矢理膣射 = 1
				ENDIF
				;custom code, rape expansion
				SETBIT FALL_RAPE:RECEIVER:0, 3
				無理矢理恐怖 *= 100 ; force marks
				SOURCE:RECEIVER:Terror += 無理矢理恐怖 ;bugfix
				EXP:RECEIVER:FallenRape ++
				TCVAR:RECEIVER:TCVAR_CUMIN_RAPE = 1
				;;#; PRINTFORML SAMEN_SHOOT2 FEAR GET {無理矢理恐怖}
				;==============================
				TFLAG:好感度管理 -= 好感度マイナス
				CALL CHANGE_CFLAG(4,RECEIVER,信頼マイナス * (-1))
			ELSEIF TCVAR:RECEIVER:外に出すから && GROUPMATCH(TCVAR:PLAYER:コンドーム,0,-1) && !TCVAR:ピル ;pullout promise patch + custom - do not apply if protection was used
				;nas custom: fake condoms will get you caught, but fake pills won't
				無理矢理反感 = 5000
				無理矢理恐怖 = NOWEX:RECEIVER:膣内精液
				好感度マイナス = NOWEX:SENDER:射精 * RAND:10 + NOWEX:SENDER:射精 * RAND:10
				信頼マイナス = NOWEX:SENDER:射精 * RAND:10 + NOWEX:SENDER:射精 * RAND:10
				;危険日
				IF CFLAG:RECEIVER:生理周期 == 7
					無理矢理反感 *= 10
					無理矢理恐怖 *= 10
					好感度マイナス *= 3
					信頼マイナス *= 3
					;排卵日
				ELSEIF CFLAG:RECEIVER:生理周期 == 8
					無理矢理反感 *= 5
					無理矢理恐怖 *= 5
					好感度マイナス *= 2
					信頼マイナス *= 2
				ELSE
					無理矢理反感 *= 5
					無理矢理恐怖 *= 5
					好感度マイナス *= 1
					信頼マイナス *= 1
				ENDIF
				PRINTFORML %CALLNAME:RECEIVER%는 당신이 그녀와 생식 없이 관계를 가지게 해준 후에 사정하겠다고 약속을 어겼기 때문에 분노로 들끓는다.
				PRINTW 
				SOURCE:RECEIVER:反感 += 無理矢理反感
				TFLAG:好感度管理 -= 好感度マイナス
				CFLAG:RECEIVER:PullOutPromiseDenied = 1 ;custom, deny the option if you didn't hold your promise
				TCVAR:RECEIVER:挿入拒絶 = 1 ;in case you try to do it again on the same day after calming them down
				CALL CHANGE_CFLAG(4,RECEIVER,信頼マイナス * (-1))
			ENDIF
			SIF ALSO_ANAL ;addition custom code
				GOTO ANAL_EJAC
			;アナル
		CASE 2
			$ANAL_EJAC
			EXP:RECEIVER:精液経験 += NOWEX:SENDER:射精 * 2
			EXP:RECEIVER:肛射経験 += NOWEX:SENDER:射精 * 2
			STAIN:RECEIVER:Ａ |= 4
			STAIN:RECEIVER:腸内 |= 4
			;中出し表示用
			TCVAR:RECEIVER:Aに中出しされたフラグ = 2
			TCVAR:RECEIVER:Aに中出ししたキャラの番号 = SENDER
			IF FLAG:時間停止 == 1
				CFLAG:RECEIVER:アナル射精 = 2
			ELSE
				CFLAG:RECEIVER:アナル射精 = 1
			ENDIF
			NOWEX:RECEIVER:アナル内精液 = NOWEX:SENDER:射精量
			EX:RECEIVER:アナル内精液 += NOWEX:SENDER:射精量
			欲求不満解消係数 = 800 / MIN(100 + ABL:RECEIVER:肛射中毒 * 10,200)
			SIF GETBIT(nScat, 2)
				BASE:RECEIVER:Poo += NOWEX:SENDER:射精量
			CFLAG:RECEIVER:溜まってる度 -= SQRT(NOWEX:RECEIVER:アナル内精液) * 欲求不満解消係数
			CFLAG:RECEIVER:累計アナル精液 += NOWEX:SENDER:射精量
			CALL POISONBODY(RECEIVER, SENDER, 2)
			SIF ALSO_ANAL ;addition custom code, restore value
				NOWEX:SENDER:射精量 *= 2
			;手淫
		CASE 3
			EXP:RECEIVER:精液経験 += 1 + NOWEX:SENDER:射精
			STAIN:RECEIVER:手 |= 4
			IF FLAG:時間停止 == 1
				CFLAG:RECEIVER:手に精液 = 2
			ELSE
				CFLAG:RECEIVER:手に精液 = 1
			ENDIF
			IF ABL:RECEIVER:精液中毒 > 2 && ABL:RECEIVER:技巧 > 2
				EXP:RECEIVER:精飲経験 ++
				CFLAG:RECEIVER:累計精飲 += NOWEX:SENDER:射精量 / 2
				CFLAG:RECEIVER:溜まってる度 -= NOWEX:SENDER:射精量 * ABL:RECEIVER:精液中毒 / 10 * (TALENT:RECEIVER:精愛味覚 + 1)
				CALL POISONBODY(RECEIVER, SENDER, 31)
			ELSE
				CALL POISONBODY(RECEIVER, SENDER, 3)
			ENDIF
			;接触のリセット
			TEQUIP:SENDER:指接触部位 = 0
			;口淫
		CASE 4
			EXP:RECEIVER:精液経験 += NOWEX:SENDER:射精 * 3
			STAIN:RECEIVER:口 |= 4
			IF FLAG:時間停止 == 1
				CFLAG:RECEIVER:口に精液 = 2
			ELSE
				CFLAG:RECEIVER:口に精液 = 1
			ENDIF
			;イラマチオ
			欲求不満解消係数 = 800 / MIN(100 + ABL:RECEIVER:精液中毒 * 10,200)
			IF TALENT:RECEIVER:精愛味覚
				SOURCE:RECEIVER:快Ｍ += NOWEX:SENDER:射精 * 10
				欲求不満解消係数 += 1
			ENDIF
			IF SELECTCOM == 140
				EXP:RECEIVER:精飲経験 += NOWEX:SENDER:射精 * 3
				CFLAG:RECEIVER:累計精飲 += NOWEX:SENDER:射精量
			ELSEIF RAND:(MIN(60, EXP:RECEIVER:精飲経験 + 1)) > 10 + RAND:30
				EXP:RECEIVER:精飲経験 += NOWEX:SENDER:射精 * 3
				CFLAG:RECEIVER:累計精飲 += NOWEX:SENDER:射精量
			ELSE
				EXP:RECEIVER:精飲経験 += NOWEX:SENDER:射精
				CFLAG:RECEIVER:累計精飲 += NOWEX:SENDER:射精量 / 3
				欲求不満解消係数 /= 2
			ENDIF
			CALL POISONBODY(RECEIVER, SENDER, 4)
			CFLAG:RECEIVER:溜まってる度 -= SQRT(NOWEX:SENDER:射精量) * 欲求不満解消係数 / 4
			;接触のリセット
			TEQUIP:SENDER:口接触部位 = 0
			;パイズリ
		CASE 5
			EXP:RECEIVER:精液経験 += NOWEX:SENDER:射精 * 3
			CFLAG:RECEIVER:累計ぶっかけ += NOWEX:SENDER:射精量
			CALL POISONBODY(RECEIVER, SENDER, 5)
			STAIN:RECEIVER:5 |= 4
			IF FLAG:時間停止 == 1
				CFLAG:RECEIVER:顔に精液 = 2
			ELSE
				CFLAG:RECEIVER:顔に精液 = 1
			ENDIF
			;TFLAG:特殊COM = 12 に該当するものがCSVに書いてない
			;(1=シックスナイン 2=岩清水 3=Gスポット刺激 4=乱れ牡丹 5=手淫フェラ 6= 7= 8= シックスナインパイズリ 13=交互挿入 )
			;TFLAGS.txtと書いてあることが違う（TFLAG:12 逆レイプ継続フラグ（VA共通））
			;	→嫌な予感
			SIF TFLAG:特殊COM == 12
				STAIN:RECEIVER:口 |= 4
			;接触のリセット
			TEQUIP:SENDER:Ｂ接触部位 = 0
			;素股
		CASE 6
			EXP:RECEIVER:精液経験 += NOWEX:SENDER:射精
			CFLAG:RECEIVER:累計ぶっかけ += NOWEX:SENDER:射精量
			CALL POISONBODY(RECEIVER, SENDER, 6)
			STAIN:RECEIVER:Ｖ |= 4
			;接触のリセット
			TEQUIP:SENDER:Ｃ接触部位 = 0
			;足コキ
		CASE 7
			EXP:RECEIVER:精液経験 += NOWEX:SENDER:射精
			CFLAG:RECEIVER:累計ぶっかけ += NOWEX:SENDER:射精量
			CALL POISONBODY(RECEIVER, SENDER, 7)
			;体表
		CASE 8
			EXP:RECEIVER:精液経験 += NOWEX:SENDER:射精 * 2
			CFLAG:RECEIVER:累計ぶっかけ += NOWEX:SENDER:射精量
			CALL POISONBODY(RECEIVER, SENDER, 8)
			SIF nPulledOut && TCVAR:RECEIVER:外に出すから == 1
				Qol_Info_Count_Array:RECEIVER:Qol_Count_EjacOut += NOWEX:SENDER:射精量
			;脇コキ, custom code
		CASE 14
			EXP:RECEIVER:精液経験 += NOWEX:SENDER:射精
			STAIN:RECEIVER:脇 |= 4
			IF FLAG:時間停止 == 1
				CFLAG:RECEIVER:脇に精液 = 2
			ELSE
				CFLAG:RECEIVER:脇に精液 = 1
			ENDIF
			CFLAG:RECEIVER:累計ぶっかけ += NOWEX:SENDER:射精量
			CALL POISONBODY(RECEIVER, SENDER, 9)
		CASE 50 ;addition custom code
			ALSO_ANAL = 1
			GOTO VAGINAL_EJAC
			;U sex
		CASE 21
			EXP:RECEIVER:精液経験 += NOWEX:SENDER:射精 * 2
			EXP:RECEIVER:UEjac += NOWEX:SENDER:射精 * 2
			STAIN:RECEIVER:Ｕ |= 4
			;STAIN:RECEIVER:腸内 |= 4
			;中出し表示用
			TCVAR:RECEIVER:UCreampie = 2
			TCVAR:RECEIVER:UCreampieingChara = SENDER
			IF FLAG:時間停止 == 1
				CFLAG:RECEIVER:UEjac = 2
			ELSE
				CFLAG:RECEIVER:UEjac = 1
			ENDIF
			NOWEX:RECEIVER:UCreampie = NOWEX:SENDER:射精量
			EX:RECEIVER:UCreampie += NOWEX:SENDER:射精量
			SIF GETBIT(nPee, 1)
				BASE:RECEIVER:尿意 += NOWEX:SENDER:射精量
			欲求不満解消係数 = 1000 / MIN(100 + ABL:RECEIVER:肛射中毒 * 10,200)
			CFLAG:RECEIVER:溜まってる度 -= SQRT(NOWEX:RECEIVER:UCreampie) * 欲求不満解消係数
			CFLAG:RECEIVER:UCreampieTotal += NOWEX:SENDER:射精量
			CALL POISONBODY(RECEIVER, SENDER, 9)
			;NAS: In Underwear/Diaper
		CASE 63
			STAIN:SENDER:Ｖ |= 4
			STAIN:SENDER:膣内 |= 4
			CALLF MessUndies(SENDER, "liquid", 3, NOWEX:SENDER:射精量)
			;todo: possible self impreg if futa???
			SIF IS_FUTA(SENDER)
				CALL SAMEN_SHOOT3(NOWEX:RECEIVER:膣内精液/10, SENDER, SENDER)
	ENDSELECT
	SIF SHIRAHU(RECEIVER)
		CALL ADDICTION_SOURCE(RECEIVER, SENDER)
ENDIF
CFLAG:RECEIVER:溜まってる度 = LIMIT(CFLAG:RECEIVER:溜まってる度, 0, 1000)
CFLAG:SENDER:溜まってる度 = MAX(CFLAG:SENDER:溜まってる度 - NOWEX:SENDER:射精 * 50, 0)
SIF SENDER == MASTER
	nSexDone ++ ;addition custom code
;射精された精子を子宮内へ送る処理

@SAMEN_SHOOT3(射精した精液量, 射精したキャラ, 射精されたキャラ)
#DIM 射精した精液量
#DIM 射精されたキャラ
#DIM 減算補正値
#DIM 加算値
;使われてない……
#DIM 射精したキャラ
;73,挿入子宮口責め
;後ろから上書きされてるけどこれ意味有る？
IF SELECTCOM == 73
	減算補正値 = 100
ELSE
	SELECTCASE TEQUIP:射精されたキャラ:子宮
		CASE 0
			減算補正値 = 150
		CASE 1
			減算補正値 = 120
		CASE 2
			減算補正値 = 100
	ENDSELECT
ENDIF
;だいしゅきホールド
SIF TCVAR:射精されたキャラ:カウンター行動の派生 == 2 && TCVAR:射精されたキャラ:カウンター行動 == 500
	減算補正値 -= 30
;加算値 = 射精した精液量 - 減算補正値
加算値 = 射精した精液量
IF !TALENT:射精されたキャラ:行きずり
	SELECTCASE EXP:射精されたキャラ:子宮口開発経験
		CASE IS >= 300
		CASE IS > 100
			加算値 = 加算値 * 2 / 3
		CASEELSE
			加算値 /= 2
	ENDSELECT
ENDIF
;計算結果が負なら加算しない
IF 加算値 < 0
	RETURN 0
ENDIF
;SIF 射精されたキャラ == MASTER
;	TCVAR:射精したキャラ:種付け量 += 加算値
nInsemination:射精されたキャラ:射精したキャラ += 加算値
EX:射精されたキャラ:子宮内精液 += 加算値
;通常のコマンド外であなたがＶに出した時の処理を一括でやる関数
;基本的にオナバレックス用

@ADD_SAMEN_V(ARG, 射精した精液量,無自覚, SENDER)
#DIM 射精した精液量
#DIM 無自覚
#DIM SENDER
射精した精液量 = MIN(BASE:SENDER:精力, 射精した精液量)
CALL SAMEN_SHOOT3(射精した精液量, SENDER, ARG)
SIF BASE:SENDER:精力 >= 射精した精液量 && 射精した精液量 > 0
	nVigCheck += 射精した精液量 ;addition custom code
BASE:SENDER:精力 -= 射精した精液量
SIF TALENT:SENDER:絶倫 ;addition custom code
	射精した精液量 *= 2
EX:ARG:膣内精液 += 射精した精液量
CFLAG:ARG:累計膣内精液 += 射精した精液量
SIF !無自覚
	TCVAR:ARG:種付けられ自覚 = 1
STAIN:ARG:Ｖ |= 4
STAIN:ARG:膣内 |= 4
STAIN:SENDER:Ｐ |= 1
STAIN:SENDER:Ｐ |= 4
nSexDone ++ ;addition custom code
;#; PRINTFORML ejaculated {射精した精液量}ml
;通常のコマンド外であなたがＡに出した時の処理を一括でやる関数
;基本的にオナバレックス用

@ADD_SAMEN_A(ARG, 射精した精液量, SENDER)
#DIM 射精した精液量
#DIM SENDER
射精した精液量 = MIN(BASE:SENDER:精力, 射精した精液量)
SIF BASE:SENDER:精力 >= 射精した精液量 && 射精した精液量 > 0
	nVigCheck += 射精した精液量 ;addition custom code
BASE:SENDER:精力 -= 射精した精液量
SIF TALENT:SENDER:絶倫 ;addition custom code
	射精した精液量 *= 2
EX:ARG:アナル内精液 += 射精した精液量
CFLAG:ARG:累計アナル精液 += 射精した精液量
STAIN:ARG:Ａ |= 4
STAIN:ARG:腸内 |= 4
STAIN:SENDER:Ｐ |= 8
STAIN:SENDER:Ｐ |= 4
nSexDone ++ ;addition custom code
;custom code

@ADD_SEMEN_M(ARG, 射精した精液量, SENDER)
#DIM 射精した精液量
#DIM SENDER
射精した精液量 = MIN(BASE:SENDER:精力, 射精した精液量)
SIF BASE:SENDER:精力 >= 射精した精液量 && 射精した精液量 > 0
	nVigCheck += 射精した精液量 ;addition custom code
BASE:SENDER:精力 -= 射精した精液量
CFLAG:ARG:口に精液 += 射精した精液量
EXP:ARG:精飲経験 += 射精した精液量 * 3
CFLAG:ARG:累計精飲 += 射精した精液量
STAIN:ARG:口 |= 4
;STAIN:ARG:腸内 |= 4
;STAIN:SENDER:Ｐ |= 8
STAIN:SENDER:Ｐ |= 4
nSexDone ++ ;addition custom code

@ADD_SEMEN_U(ARG, 射精した精液量, SENDER)
#DIM 射精した精液量
#DIM SENDER
射精した精液量 = MIN(BASE:SENDER:精力, 射精した精液量)
SIF BASE:SENDER:精力 >= 射精した精液量 && 射精した精液量 > 0
	nVigCheck += 射精した精液量 ;addition custom code
BASE:SENDER:精力 -= 射精した精液量
EX:ARG:UCreampie += 射精した精液量
CFLAG:ARG:UCreampieTotal += 射精した精液量
;STAIN:ARG:Ｍ |= 4
;STAIN:ARG:腸内 |= 4
;STAIN:SENDER:Ｐ |= 8
STAIN:SENDER:Ｐ |= 4
nSexDone ++ ;addition custom code

@ADDICTION_SOURCE(RECEIVER, SENDER)
#DIM RECEIVER
#DIM SENDER
#DIM AMOUNT
#DIMS REVISED_FLAG
#DIM REVISION
#DIM 中毒レベル
SELECTCASE TCVAR:SENDER:射精した箇所フラグ
	CASE 1
		中毒レベル = ABL:RECEIVER:膣射中毒
		REVISED_FLAG = 膣内射精補正
	CASE 2
		中毒レベル = ABL:RECEIVER:肛射中毒
		REVISED_FLAG = 肛内射精補正
	CASE 4
		中毒レベル = ABL:RECEIVER:精液中毒
		REVISED_FLAG = 口内射精補正
	CASE 50 ;addition custom code, dp, only add to v for now
		中毒レベル = ABL:RECEIVER:膣射中毒
		REVISED_FLAG = 膣内射精補正
	CASEELSE
		RETURN
ENDSELECT
TCVAR:RECEIVER:REVISED_FLAG = NOWEX:SENDER:射精量 * (10 + 中毒レベル)
IF TALENT:SENDER:濃厚精液
	REVISION = GET_REVISION(TCVAR:RECEIVER:REVISED_FLAG, 500, 1000)
ELSE
	REVISION = GET_REVISION(TCVAR:RECEIVER:REVISED_FLAG, 500, 2000)
ENDIF
;同時絶頂の場合増加
SIF SENDER == MASTER && SYNCED_ORGASM(RECEIVER)
	REVISION = REVISION * 5 / 4
SOURCE:RECEIVER:情愛 += REVISION * 2
SOURCE:RECEIVER:欲情 += REVISION * 2
SOURCE:RECEIVER:恭順 += REVISION
SOURCE:RECEIVER:屈従 += REVISION
TCVAR:RECEIVER:REVISED_FLAG = REVISION

@ADDICTION_SHOW(ARG, 補正名, 対応中毒)
#DIMS 対応中毒
#DIMS 補正名
#DIM REVISION
LOCAL = FINDELEMENT(ABLNAME, 対応中毒,,,1) ;potential bug fix
REVISION = TCVAR:ARG:補正名
SIF REVISION
	PRINTFORML %CALLNAME:ARG%(%ABL_TR(LOCAL)% Lv{ABL:ARG:対応中毒}): 호의+{REVISION * 2} 욕정+{REVISION * 2} 복종+{REVISION} 공순+{REVISION}
IF TALENT:ARG:行きずり == 1 && ABL:ARG:対応中毒 < REVISION / 150 && ABL:ARG:対応中毒 < 3
	ABL:ARG:対応中毒 ++
	CALL COLORMESSAGE(@"%CALLNAME:ARG%의 %ABL_TR(LOCAL)%가 {ABL:ARG:対応中毒}으로 증가했다.",C_YELLOW,2)
ENDIF

@AUTO_SKIN_EQUIP
SIF !CFLAG:TARGET:自動ゴム
	RETURN
SIF !ITEM:コンドーム
	RETURN
SIF TEQUIP:MASTER:コンドーム
	RETURN
ITEM:コンドーム --
TEQUIP:MASTER:コンドーム = 1
PRINTFORML 당신은 콘돔({ITEM:コンドーム} 남음)을 착용한다.
;===========================================
;ポイズンボディの効果を規定 MASTER限定
;POINTは射精箇所
;===========================================

@POISONBODY(RECEIVER, SENDER, POINT)
#DIM RECEIVER
#DIM SENDER
#DIM POINT
#DIM ゲージ増加
ゲージ増加 = 0
IF TFLAG:ポイズンボディ && SENDER == MASTER
	SELECTCASE POINT
		;膣内。粘膜吸収1.5倍。一番効果が出る
		CASE 1
			ゲージ増加 += NOWEX:SENDER:射精量 * 3 / 2
			;アナル。粘膜吸収1.0倍。
		CASE 2
			ゲージ増加 += NOWEX:SENDER:射精量
			;手淫。経皮0.2倍。一応効果はある程度
		CASE 3
			ゲージ増加 += NOWEX:SENDER:射精量 / 5
			;手淫→舐め取り。粘膜0.5倍
		CASE 31
			ゲージ増加 += NOWEX:SENDER:射精量 / 2
			;口。粘膜吸収1.0倍。飲みこぼしなどで減免はしない
		CASE 4
			ゲージ増加 += NOWEX:SENDER:射精量
			;胸。経皮0.5倍。手よりは多い
		CASE 5
			ゲージ増加 += NOWEX:SENDER:射精量 / 2
			;素股。粘膜？0.75倍
		CASE 6
			ゲージ増加 += NOWEX:SENDER:射精量 * 3 / 4
			;足。経皮0.2倍。一応効果はある程度
		CASE 7
			ゲージ増加 += NOWEX:SENDER:射精量 / 5
			;体表。経皮0.1倍。
		CASE 8
			ゲージ増加 += NOWEX:SENDER:射精量 / 10
	ENDSELECT
	;濃厚精液の場合は２倍に補正
	SIF TALENT:SENDER:濃厚精液
		ゲージ増加 *= 2
ENDIF
TCVAR:RECEIVER:ポイズンゲージ += ゲージ増加
