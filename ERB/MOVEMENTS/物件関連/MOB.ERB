; Modular ERB: 物件関連/MOB.ERB
;-------------------------------------------------
;モブのいるマップを返す
;ARG=MAPID
;-------------------------------------------------

@GET_EXISTMOB(ARG)
#FUNCTION
SIF GROUPMATCH(ARG, 3, 4, 9, 10)
	RETURNF 1
;-------------------------------------------------
;お出かけ時のモブ判定
;ARG=場所ID
;-------------------------------------------------

@DATE_HITOGOMI(ARG)
#FUNCTION
SIF !ARG
	ARG = CFLAG:MASTER:現在位置
IF IN_HOME(ARG)
	RETURNF 0
ELSE
	SIF ODEKAKEMAP_SETTING(ARG, "モブ")
		RETURNF 1
ENDIF
RETURNF 0
;-------------------------------------------------
;MASTERが在宅中かつモブと同じ位置にいるならMAIN_MAPを返す
;-------------------------------------------------

@WITH_MOB()
#FUNCTION
SIF AT_HOME(MASTER) && ROOMDATA:(CFLAG:MASTER:現在位置 % 100) & 場所_モブ
	RETURNF MAIN_MAP
;-------------------------------------------------
;モブの与える汚れ値（掃除値）
;ARG=MAPID
;-------------------------------------------------

@MOB_YOGORE(ARG)
#FUNCTION
SELECTCASE ARG
	CASE 3
		RETURNF -3
	CASE 4
		SIF !GROUPMATCH(TIME:2,1,6,7)
			RETURNF 1
	CASE 9
		SIF !GROUPMATCH(TIME:2,1,6,7)
			RETURNF 2
	CASE 10
		;玉兎はプラスマイナスゼロ
ENDSELECT
;-------------------------------------------------
;モブの基本名
;ARG=MAPID
;-------------------------------------------------

@MOB_NAME(ARG)
#FUNCTIONS
SIF !ARG
	ARG = MAIN_MAP
SELECTCASE ARG
	CASE 3
		LOCALS '= @"홉고블린"
	CASE 4
		LOCALS '= @"요괴 토끼"
	CASE 9
		LOCALS = %ANIMALNAME()%
	CASE 10
		LOCALS '= @"달토끼"
ENDSELECT
RETURNF LOCALS
;-------------------------------------------------
;お出かけ中のモブ名
;ARG=場所ID
;-------------------------------------------------

@DATE_HITOGOMI_SYUBETU(ARG)
#FUNCTIONS
VARSET LOCALS
SIF !ARG
	ARG = CFLAG:PLAYER:現在位置
SELECTCASE GET_MAPID(ARG)
	;モブの種類
	;月
	CASE 10
		IF ARG == 月の都
			LOCALS += "월인"
		ELSE
			LOCALS += "달토끼"
		ENDIF
		;地底（街道と温泉では鬼も出てくる）
	CASE 9
		IF ARG != 地霊殿 && !RAND:3
			LOCALS += "오니"
		ELSE
			LOCALS += "Youkai"
		ENDIF
		;妖怪の山山頂（守矢神社境内のみ希に人間）
	CASE 8
		IF ARG == 守矢神社境内 && !RAND:5
			LOCALS += "인간"
		ELSE
			LOCALS += "Youkai"
		ENDIF
		;妖怪の山山麓
	CASE 7
		;三途の川～冥界（死神、妖怪、希に人間）
	CASE 6
		IF !RAND:5
			LOCALS += "인간"
		ELSEIF !RAND:3
			LOCALS += "Youkai"
		ELSE
			LOCALS += "사신"
		ENDIF
		;魔法の森
	CASE 5
		;迷いの竹林（妖怪ウサギ、希に人間(患者)）
	CASE 4
		IF RAND:5
			LOCALS += "요괴 토끼"
		ELSE
			LOCALS += "인간"
		ENDIF
		;紅魔館（お出かけ中のモブは全員少女なのでホフさんはお休み）
	CASE 3
		LOCALS += "요정 하녀"
		;人間の里（基本人間、希に妖怪）
	CASE 2
		IF ARG == 寺子屋 ;addition custom code, when in school
			LOCALS += "어린아이"
		ELSEIF RAND:5
			LOCALS += "인간"
		ELSE
			LOCALS += "Youkai"
		ENDIF
		;命蓮寺（人間と妖怪）
	CASE 1
		IF !RAND:2
			LOCALS += "인간"
		ELSE
			LOCALS += "Youkai"
		ENDIF
		;博麗神社
	CASE 0
ENDSELECT
LOCALS '= @"어린 %LOCALS% 소녀"
RETURNF LOCALS
;-------------------------------------------------
;まかないイベント処理
;-------------------------------------------------

@MAKANAI
#DIM 気力回復量
#DIM 体力回復量
VARSET LOCAL
IF DISH_NAME == "" && !TFLAG:まかない && TIME:2 == 3
	;食事できない人がいるかをチェック
	FOR LOCAL, 0, CHARANUM
	SIF TCVAR:LOCAL:空腹時刻 - TIME:1 > 0 && CFLAG:LOCAL:現在位置 == CFLAG:MASTER:現在位置
		LOCAL:1 ++
	NEXT
	IF !LOCAL:1 && !RAND:5
		TFLAG:まかない = 1
		PRINTFORML %CAPITALIZE(ARTICLE(MOB_NAME()))%이 음식을 제공했다.
		PRINTFORML 먹을까?
		CALL ASK_YN
		IF RESULT
			PRINTFORML 당신은 정중하게 거절한다.
		ELSE
			SELECTCASE RAND:5
				CASE 0, 1
					PRINTFORML 괜찮았다.
					体力回復量 = 100
					気力回復量 = 300
				CASE 2, 3
					PRINTFORML 꽤 맛있다.
					体力回復量 = 150
					気力回復量 = 400
				CASE 4
					PRINTFORML 이거 맛있다!
					体力回復量 = 300
					気力回復量 = 500
			ENDSELECT
			PRINTFORMW 당신%COND_STR("과 일행",GET_TARGETNUM())%은 간식을 먹었다.
			TIME += 15
			FOR LOCAL, 0, CHARANUM
			IF CFLAG:LOCAL:現在位置 == CFLAG:MASTER:現在位置
				CALL RECOVER(LOCAL, 体力回復量, "体力")
				CALL RECOVER(LOCAL, 気力回復量, "気力")
				CALL 満腹度上昇(LOCAL, "軽食")
			ENDIF
			NEXT
		ENDIF
	ENDIF
ENDIF
;＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝
;紅魔館のモブ設定（ホフゴブリン）
;＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝
;-------------------------------------------------
;モブのいる場所
;-------------------------------------------------

@MOB_PLACE_3
#DIM ROOM
FOR LOCAL, MINROOM(), MAXROOM
ROOM = LOCAL % 100
ROOMDATA:ROOM &= ~場所_モブ
SELECTCASE TIME:2
	CASE 2
		SIF GROUPMATCH(LOCAL, P302庭, P306裏庭, P307東テラス, P308西テラス)
			ROOMDATA:ROOM |= 場所_モブ
	CASE 3
		SIF GROUPMATCH(LOCAL, P313厨房, P314食堂, P315応接間)
			ROOMDATA:ROOM |= 場所_モブ
	CASE 4
		SIF GROUPMATCH(LOCAL, P309広間, P319二階廊下)
			ROOMDATA:ROOM |= 場所_モブ
	CASE 5
		SIF GROUPMATCH(LOCAL, P324大バルコニー, P327小バルコニー)
			ROOMDATA:ROOM |= 場所_モブ
	CASE 6
		SIF GROUPMATCH(LOCAL, P306裏庭, P328地下廊下, P329倉庫, P330使用人部屋)
			ROOMDATA:ROOM |= 場所_モブ
ENDSELECT
NEXT
;-------------------------------------------------
;モブのメッセージ
;-------------------------------------------------

@MOB_MESSAGE_3
IF FLAG:時間停止
	PRINTFORML 호그블린들이 얼어붙었다...
ELSEIF CFLAG:MASTER:現在位置 == P329倉庫
	PRINTFORML 호그밥린이 고양이를 돌보고 있다...
ELSE
	PRINTFORML 호그블린들이 일하고 있다...
ENDIF
;-------------------------------------------------
;モブのイベント
;-------------------------------------------------

@MOB_TURNENDEVENT_3
IF !FLAG:時間停止
	SIF LS_RESTRICTION("Map mob interrupt") ;Custom code, restrict interrupting sex during Lunatic Survival.
		RETURN 0
	CALL Add_SeedOfUnknownForm("mobsex") ;addition custom code - nue's special item lets you stay hidden
	SIF RESULT
		RETURN 0
	IF CFLAG:MASTER:うふふ
		PRINTFORML 홉고블린들은 %SPLIT_G("이곳은 공공장소이므로 그런 행동은 허용되지 않는다고 친절히 알려 주었다:공공장소에서는 그런 행동을 삼가 달라고 정중히 부탁했다")%.
		PRINTFORML 너는 그것에 대해 기분이 나쁘다.
		BASE:TARGET:ムード = 0
		CALL ENDUFUFU_ALL
	ELSEIF CFLAG:MASTER:現在位置 == P313厨房
		CALL MAKANAI
	ENDIF
ENDIF
;＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝
;永遠亭のモブ設定（イナバ）
;＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝
;-------------------------------------------------
;モブのいる場所
;-------------------------------------------------

@MOB_PLACE_4
#DIM ROOM
FOR LOCAL, MINROOM(), MAXROOM
ROOM = LOCAL % 100
ROOMDATA:ROOM &= ~場所_モブ
SELECTCASE TIME:2
	CASE 1 TO 7
		SIF GROUPMATCH(LOCAL, P407廊下, P408中廊下, P409縁側, P421庭, P422裏庭, P431兎の洞穴)
			ROOMDATA:ROOM |= 場所_モブ
ENDSELECT
NEXT
;-------------------------------------------------
;モブのメッセージ
;-------------------------------------------------

@MOB_MESSAGE_4
IF FLAG:時間停止
	PRINTFORML 토끼들이 얼어버렸다...
ELSEIF GROUPMATCH(TIME:2, 1, 6, 7)
	PRINTFORML 토끼들은 자고 있다...
ELSEIF !DAY:3 % 10
	IF !TALENT:MASTER:性別 & 2
		PRINTFORML 토끼들은 발정 때문에 안절부절못한다.
	ELSE
		PRINTFORML 발정 중인 토끼들에게서 강렬한 시선을 느낀다...
	ENDIF
ELSE
	SELECTCASE RAND:5
		CASE 0
			PRINTFORML 토끼들은 %SPLIT_G("목적 없이 이리저리 돌아다닌다:주변을 어슬렁거린다")%...
		CASE 1
			PRINTFORML 토끼들이 놀고 있다...
		CASE 2
			PRINTFORML 토끼들이 무언가에 대해 이야기하고 있다...
		CASE 3
			PRINTFORML 토끼들은 %SPLIT_G("한데 옹기종기 모여 있다:서로 이리저리 부딪치고 있다")%...
		CASE 4
			PRINTFORML 토끼들이 서로 쌓이고 있다...
	ENDSELECT
ENDIF
;-------------------------------------------------
;モブのイベント
;-------------------------------------------------

@MOB_TURNENDEVENT_4
IF !FLAG:時間停止
	CALL Add_SeedOfUnknownForm("mobsex") ;addition custom code - nue's special item lets you stay hidden
	SIF RESULT
		RETURN 0
	IF CFLAG:MASTER:うふふ
		PRINTFORML 당신과 파트너를 보고 토끼들이 소란을 피우며 더 많은 구경꾼이 모이자 비명을 지른다.
		PRINTFORML 기분이 완전히 망가진다...
		BASE:TARGET:ムード = 0
		CALL ENDUFUFU_ALL
	ENDIF
ENDIF
;＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝
;地霊殿のモブ設定（ペット＆怨霊＆鬼）
;＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝
;-------------------------------------------------
;モブのいる場所
;-------------------------------------------------

@MOB_PLACE_9
#DIM ROOM
FOR LOCAL, MINROOM(), MAXROOM
ROOM = LOCAL % 100
ROOMDATA:ROOM &= ~場所_モブ
SIF GROUPMATCH(LOCAL, P904大広間, P905一階廊下, P913食堂, P914二階廊下, P915バルコニー, P923裏庭, P926中庭)
	ROOMDATA:ROOM |= 場所_モブ
SIF GROUPMATCH(LOCAL, P931大通り, P932裏通り, P933酒場, P938鬼の溜まり場)
	ROOMDATA:ROOM |= 場所_モブ
SIF GROUPMATCH(LOCAL, P928灼熱地獄跡, P929地獄の人工太陽, P930旧血の池地獄)
	ROOMDATA:ROOM |= 場所_モブ
NEXT
;-------------------------------------------------
;モブのメッセージ
;-------------------------------------------------

@MOB_MESSAGE_9
IF INRANGE(CFLAG:MASTER:現在位置, P928灼熱地獄跡, P930旧血の池地獄)
	IF FLAG:時間停止
		PRINTFORML 복수심에 찬 영혼들이 얼어붙었다...
	ELSE
		IF CFLAG:うふふ
			PRINTFORML 복수심에 찬 영혼들이 분위기를 망치고 있다...
		ELSEIF !RAND:2
			PRINTFORML 원한을 품은 영혼들은 %SPLIT_G("목적 없이 떠돈다:주변을 배회한다")%...
		ELSE
			PRINTFORML 복수심에 찬 영혼들은 %SPLIT_G("공중에 머문다:주변을 둥둥 떠다닌다:앞뒤로 천천히 떠다닌다")%...
		ENDIF
	ENDIF
ELSEIF INRANGE(CFLAG:MASTER:現在位置, P931大通り, P939路地裏)
	IF FLAG:時間停止
		PRINTFORML 오니들이 얼어붙었다...
	ELSEIF GROUPMATCH(TIME:2, 1, 6, 7)
		PRINTFORML 오니들은 잠들어 있다...
	ELSEIF CFLAG:MASTER:うふふ ;addition custom code
		CALL Add_Mob_Oni_Message
	ELSE
		SELECTCASE RAND:6
			CASE 0
				PRINTFORML 오니들이 술판을 벌이고 있다...
			CASE 1
				PRINTFORML 오니들이 함께 어울리고 있다...
			CASE 2
				PRINTFORML 오니들이 싸우고 있다...
			CASE 3
				PRINTFORML 오니들은 %SPLIT_G("목적 없이 돌아다닌다:주변을 어슬렁거린다")%...
			CASE 4
				PRINTFORML 오니들은 %SPLIT_G("떠들썩하게 소란을 피운다:광란의 분위기를 만든다:시끄럽게 떠든다")%...
			CASE 5
				PRINTFORML 오니 한 마리가 길을 가로질러 쓰러져 있다... 성가시군.
		ENDSELECT
	ENDIF
ELSE
	IF FLAG:時間停止
		PRINTFORML %ANIMALNAME()%은(는) 얼어버렸다...
	ELSEIF GROUPMATCH(TIME:2, 1, 6, 7)
		PRINTFORML %ANIMALNAME()%는 잠들어 있다...
	ELSEIF CFLAG:MASTER:現在位置 == P923裏庭
		IF !RAND:2
			PRINTFORML 물속에서 %ANIMALNAME()% 한 마리가 %SPLIT_G("잠깐 몸을 담근다:느긋하게 목욕한다")%...
		ELSE
			PRINTFORML %ANIMALNAME()%은(는) 물속을 헤엄친다...
		ENDIF
	ELSE
		IF !RAND:2
			PRINTFORML %ANIMALNAME()% 한 마리가 %SPLIT_G("목적 없이 돌아다닌다:주변을 어슬렁거린다")%...
		ELSE
			PRINTFORML %ANIMALNAME()%가 길을 막고 쓰러져 있다... 성가시네.
		ENDIF
	ENDIF
ENDIF
;-------------------------------------------------
;ペットの名前
;-------------------------------------------------

@ANIMALNAME()
#FUNCTIONS
SELECTCASE TIME:1 % 25
	;小型哺乳類
	CASE 0
		LOCALS '= @"개"
	CASE 1
		LOCALS '= @"고양이"
	CASE 2
		LOCALS '= @"코알라"
	CASE 3
		LOCALS '= @"고릴라"
	CASE 4
		LOCALS '= @"저지 데블"
	CASE 5
		LOCALS '= @"아르마딜로"
	CASE 6
		LOCALS '= @"멧돼지 새끼"
	CASE 7
		LOCALS '= @"카피바라"
		;大型哺乳類
	CASE 8
		LOCALS '= @"개미핥기"
	CASE 9
		LOCALS '= @"말"
	CASE 10
		LOCALS '= @"소"
	CASE 11
		LOCALS '= @"양"
	CASE 12
		LOCALS '= @"알파카"
	CASE 13
		LOCALS '= @"물개"
		;超大型哺乳類
	CASE 14
		LOCALS '= @"코끼리"
	CASE 15
		LOCALS '= @"기린"
		;鳥類
	CASE 16
		LOCALS '= @"닭"
	CASE 17
		LOCALS '= @"펭귄"
	CASE 18
		LOCALS '= @"넓적부리황새"
	CASE 19
		;LOCALS = ゾンビフェアリー
		LOCALS '= @"공작"
	CASE 20
		LOCALS '= @"타조"
		;爬虫類
	CASE 21
		LOCALS '= @"악어"
	CASE 22
		LOCALS '= @"코모도왕도마뱀"
	CASE 23
		LOCALS '= @"버마비단뱀"
	CASE 24
		LOCALS '= @"갈라파고스땅거북"
ENDSELECT
RETURNF LOCALS
;＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝
;月都のモブ設定（玉兎）
;＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝

@MOB_PLACE_10
#DIM ROOM
FOR LOCAL, MINROOM(), MAXROOM
ROOM = LOCAL % 100
ROOMDATA:ROOM &= ~場所_モブ
SELECTCASE TIME:2
	CASE 2
		SIF GROUPMATCH(LOCAL, P1003玉兎街, P1004広場, P1014桃林, P1020綿月亭・門, P1023門番詰め所, P1025玉兎詰め所)
			ROOMDATA:ROOM |= 場所_モブ
	CASE 3
		SIF GROUPMATCH(LOCAL, P1003玉兎街, P1004広場, P1020綿月亭・門, P1024玉兎食堂)
			ROOMDATA:ROOM |= 場所_モブ
	CASE 4
		SIF GROUPMATCH(LOCAL, P1003玉兎街, P1004広場, P1014桃林, P1020綿月亭・門, P1023門番詰め所, P1025玉兎詰め所)
			ROOMDATA:ROOM |= 場所_モブ
	CASE 5
		SIF GROUPMATCH(LOCAL, P1003玉兎街, P1004広場, P1020綿月亭・門, P1024玉兎食堂)
			ROOMDATA:ROOM |= 場所_モブ
	CASE 6
		SIF GROUPMATCH(LOCAL, P1003玉兎街, P1004広場, P1020綿月亭・門, P1023門番詰め所, P1024玉兎食堂, P1025玉兎詰め所)
			ROOMDATA:ROOM |= 場所_モブ
	CASEELSE
		SIF GROUPMATCH(LOCAL, P1020綿月亭・門)
			ROOMDATA:ROOM |= 場所_モブ
ENDSELECT
NEXT
;-------------------------------------------------
;モブのメッセージ
;-------------------------------------------------

@MOB_MESSAGE_10
IF FLAG:時間停止
	IF GROUPMATCH(CFLAG:MASTER:現在位置, P1020綿月亭・門, P1023門番詰め所)
		PRINTFORML 문지기들이 얼어붙었다...
	ELSE
		PRINTFORML 달 토끼들은 얼어있다...
	ENDIF
ELSE
	SELECTCASE CFLAG:MASTER:現在位置
		CASE P1003玉兎街, P1004広場
			PRINTFORML 그곳은 달 토끼들로 가득하다...
		CASE P1014桃林
			PRINTFORML 달 토끼들은 훈련하고 있다.
		CASE P1020綿月亭・門
			PRINTFORML 문지기들이 경비를 서고 있다...
		CASE P1023門番詰め所
			PRINTFORML 문지기들이 쉬고 있다...
		CASE P1024玉兎食堂
			PRINTFORML 달토끼들이 식사 중이다...
		CASE P1025玉兎詰め所
			PRINTFORML 달 토끼들이 쉬고 있다...
	ENDSELECT
ENDIF
;-------------------------------------------------
;モブのイベント
;-------------------------------------------------

@MOB_TURNENDEVENT_10
IF !FLAG:時間停止
	CALL Add_SeedOfUnknownForm("mobsex") ;addition custom code - nue's special item lets you stay hidden
	SIF RESULT
		RETURN 0
	IF CFLAG:MASTER:うふふ
		IF GROUPMATCH(CFLAG:MASTER:現在位置, P1020綿月亭・門, P1023門番詰め所)
			PRINTFORML 문지기들이 소란을 피우며 무슨 일인지 확인하러 달려왔다.
		ELSE
			PRINTFORML 당신과 파트너를 본 달 토끼들이 소란을 일으키며 더 많은 구경꾼이 모이자 비명을 지른다.
		ENDIF
		PRINTFORML 그것은 분위기를 완전히 망친다.
		BASE:TARGET:ムード = 0
		CALL ENDUFUFU_ALL
	ENDIF
ENDIF
