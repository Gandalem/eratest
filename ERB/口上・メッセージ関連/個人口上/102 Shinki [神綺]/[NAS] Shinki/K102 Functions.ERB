@K102_CoreMoments(ARG)
#FUNCTIONS
SELECTCASE ARG
CASE 1
    ;diaper lover
    RESULTS:1 '= @"Let %CALLNAME:102% have an accident"
    RESULTS:2 '= @"A gentle warmth between the legs can remind %HIM_HER(102)% of something %HE_SHE(102)% wore a lot..."
    RESULT = C_PINK
    RETURNF @"Shinki's Bell"
CASE 2
    ;pregnancy
    RESULTS:1 '= @"Attempt to have raw sex in an unsafe day"
    RESULTS:2 '= @"While others may worry about conceiving a child, %HE_SHE(102)% need not worry because %HE_SHE(102)% won't think anyone would try to knock %HIM_HER(102)% up..."
    RESULT = C_PINK
    RETURNF @"Joys of Pregnancy"
CASE 3
    ;sexual liberation
    RESULTS:1 '= @"Consciously have an orgasm"
    RESULTS:2 '= @"A climax can kickstart a journey of desire..."
    RESULT = C_PINK
    RETURNF @"Deviation Reunion"
CASE 3
    ;
    RESULTS:1 '= @"Send %CALLNAME:102% to therapy"
    RESULTS:2 '= @"Surely someone can fix a mind messed-up as hers..."
    RESULT = C_PINK
    RETURNF @"\"I Can Fix Her\""
CASE 4
    ;diaper lover
    RESULTS:1 '= @"%CALLNAME:102%'s had too many accidents to shrug off now. Time to get the padding out."
    RESULTS:2 '= @"Bringing %HIM_HER(102)% to too many accidents can force %HIM_HER(102)% back to something %HE_SHE(102)% wore a lot..."
    RESULT = C_PINK
    RETURNF @"Stinky Shinki"
ENDSELECT

@K102_EventDebugMenu
#DIMS DISP_NAME = "Daily Events", "Punishments", "Commands", "Special Events", "Path A"
#DIM PAGE
#DIM EDM_LINECOUNT
VARSET PAGE
CALL EasyImage(@"EV908","OMOGATARI/Menus/EV908.png")

CUSTOMDRAWLINE ━
EDM_LINECOUNT = LINECOUNT
DO
    CLEARLINE LINECOUNT-EDM_LINECOUNT
    TRYCALLFORM K102_EventDebugMenu_Page_{PAGE}
    SETCOLOR C_GRAY
    CUSTOMDRAWLINE ━
    RESETCOLOR
    FOR LOCAL, 0, VARSIZE("DISP_NAME")
        SIF !STRLENS(DISP_NAME:LOCAL)
            CONTINUE
        SIF PAGE == LOCAL
            SETCOLOR 0x00FFFF
        ;exception for player character
        PRINTBUTTON DISP_NAME:LOCAL, LOCAL + 8000
        RESETCOLOR
        PRINTFORM 　　
    NEXT
    PRINTL
    CUSTOMDRAWLINE ━
    SIF !InTrain
    PRINTFORML [900] - Continue normally
    PRINTFORML [999] - Get me out of here!!
    INPUT
    SELECTCASE RESULT
    CASE 8000 TO VARSIZE("DISP_NAME")+8000
        PAGE = RESULT-8000
    CASE 900
        RETURN 900
    CASE 999
        RETURN 0
    CASEELSE
        CALLFORM K102_EventDebugMenu_Input_{PAGE}(RESULT)
        RETURN 1 
    ENDSELECT
LOOP 1

@K102_EventDebugMenu_Page_0
PRINTFORML [000] - Introduction
PRINTFORML [001] - Increased Responsibilities
PRINTFORML [002] - Spiked Cookies
PRINTFORML [003] - Random Diaper Check
PRINTFORML [004] - Hypnotic Ball Sucking
PRINTFORML [005] - Before-bed Enema
PRINTFORML [006] - Hypnotic Analinglus 
PRINTFORML [007] - Truth of the Potty Monster
PRINTFORML [008] - Hypnotic Diaper Feast
PRINTFORML [009] - Unworthy of Undies

@K102_EventDebugMenu_Input_0(nInput)
#DIM nInput
CALL DAILY_EV_908(nInput)
;===========================================================

@K102_EventDebugMenu_Page_1
PRINTFORML [000] - Punishment: 50 Slaps
PRINTFORML [001] - Punishment: NureBachi
PRINTFORML [002] - Punishment: Potty Bondage

@K102_EventDebugMenu_Input_1(nInput)
#DIM nInput
K102_PlayerPunishedFor '= "debug"
SELECTCASE nInput
CASE 0
    CALL K102_Punishment_50Slaps
CASE 1
    CALL K102_Punishment_KeepWetDiaper
CASE 2
    CALL K102_Punishment_PottyBondage
ENDSELECT
;===========================================================

@K102_EventDebugMenu_Page_2
PRINTFORML [000] - Get Breastfeeding
PRINTFORML [001] - Give Breastfeeding

@K102_EventDebugMenu_Input_2(nInput)
#DIM nInput
SELECTCASE RESULT
CASE 0
CALL K102_BreastFeed
ENDSELECT
;===========================================================

@K102_EventDebugMenu_Page_3
PRINTFORML [000] - OyaTsumi Send
PRINTFORML [001] - OyaTsumi 1
PRINTFORML [002] - OyaTsumi 2
PRINTFORML [003] - OyaTsumi 3
PRINTFORML [004] - OyaTsumi 4

@K102_EventDebugMenu_Input_3(nInput)
#DIM nInput
SELECTCASE nInput
CASE 0
    CALL K102_MysteryDiaper_Send
CASEELSE
    TRYCALLFORM K102_MysteryDiaper_Guilt_{nInput}
ENDSELECT

@K102_EventDebugMenu_Page_4
PRINTFORML [000] - %K102_ChapterNames(200000)%
PRINTFORML [001] - %K102_ChapterNames(200001)%
PRINTFORML [002] - %K102_ChapterNames(201001)%
PRINTFORML [003] - %K102_ChapterNames(201002)%
PRINTL
PRINTFORML [500] - Eirin Incon Chip Removal

@K102_EventDebugMenu_Input_4(nInput)
#DIM nInput
SELECTCASE nInput
CASE 500
    CALLF TP(72)
    CALL K102_EirinVisitToRemoveInconChip
CASEELSE
    TRYCALLFORM M_KOJO_EVENT_K102_シナリオ_200000(nInput)
ENDSELECT

@K102_PampPick(ARG)
#FUNCTION
#DIM PampPick
#DIM PampStage
IF nOmutsu
    FOR PampStage,0,4
	    SELECTCASE PampStage
        CASE 0
            ;ko-fantasta
            PampPick = 134
        CASE 1
            ;fantastabab
            PampPick = 103
        CASE 2
            ;dai-diaper
            PampPick = 104
        CASE 3
            ;fantastabab+
            PampPick = 171
        CASE 4
            ;dai-diaper+
            PampPick = 178
        ENDSELECT
        CALLF DiapeStats(ARG,PampPick,1,1)
        IF (MAXBASE:ARG:4+1000) > RESULT:1 || (MAXBASE:ARG:Poo+1000) > RESULT:2
            CONTINUE
        ENDIF
        BREAK
    NEXT
    RETURNF PampPick
ELSE
	RETURNF 161
ENDIF

@K102_DickRate(ARG)
IF !HAS_PENIS(ARG)
    CALL PRINT_GP("W", @"%CALLNAME:102% looks at %PARSE("your",,,ARG)% strap-on with curiosity.")
    CALL PRINT_GPC("W", @"「So this is what they call a \"strap-on\", right?」", 102)
    CALL PRINT_GPC("W", @"「It looks kind-of big compared to my darling.」", 102)
    CALL PRINT_GP("W", @"%HE_SHE(102)% squishes %PARSE("your",,,ARG)% strap-on, then strokes it with %HIS_HER(102)% hands.")
    CALL PRINT_GP("W", @"Gently tilting %HIS_HER(102)% head, %HE_SHE(102)% continues to stroke it, waiting for a reaction.")
    CALL PRINT_GPC("W", @"「Hmmnh. Just as I expected. Doesn't seem to allow you to \"feel\" my strokes.」", 102)
    CALL PRINT_GPC("W", @"「Wouldn't it feel weird to feel absolutely nothing during sex, eh?」", 102)
    RETURN
ENDIF

SELECTCASE TALENT:ARG:250
CASE 1
    CALL PRINT_GP("W", @"%CALLNAME:102% looks at %PARSE("your",,,ARG)% micropenis with stary eyes.")
    CALL PRINT_GPC("W", @"「Awwww. Look at how cute your peepee is. It's like a baby's peepee.」", 102)
    CALL PRINT_GPC("W", @"「I betcha that Shanghai could gulp it down with her mouth.」", 102)
ENDSELECT

;useful for determining what diaper she will put you in
@K102_DynamicDiaper(ARG, ARGS)
#FUNCTION
#DIM changingTo
SELECTCASE ARGS
CASE "diaper"
	changingTo = 134
    CALLF DiapeStats(0,changingTo, 1, 1)
	IF (MAXBASE:ARG:4+1000) > RESULT:1 || (MAXBASE:ARG:Poo+1000) > RESULT:2
		changingTo = 103
        CALLF DiapeStats(0,changingTo, 1, 1)
		IF (MAXBASE:ARG:4+1000) > RESULT:1 || (MAXBASE:ARG:Poo+1000) > RESULT:2
			changingTo = 104
            CALLF DiapeStats(0,changingTo, 1, 1)
			IF (MAXBASE:ARG:4+1000) > RESULT:1 || (MAXBASE:ARG:Poo+1000) > RESULT:2
				changingTo = 171
			ENDIF
		ENDIF
	ENDIF
CASE "pull-ups"
    changingTo = 135
    CALLF DiapeStats(0,changingTo, 1, 1)
    IF MAXBASE:ARG:4 > RESULT:1 || MAXBASE:MASTER:Poo > RESULT:2
        changingTo = 74
        CALLF DiapeStats(0,changingTo, 1, 1)
        IF MAXBASE:MASTER:4 > RESULT:1 || MAXBASE:MASTER:Poo > RESULT:2
            changingTo = 164
            CALLF DiapeStats(0,changingTo, 1, 1)
        ENDIF
    ENDIF
CASEELSE
    changingTo = 161
ENDSELECT
RETURNF changingTo

;filter for the diaper stuff
@K102_ABDLMode
#FUNCTION
;ABDL Filter ON
SIF K102_UPDATE:5 == 2
    RETURNF 0
;ABDL Filter No Diapers
SIF K102_UPDATE:5 == 1
    RETURNF 1
;Diaper filter
SIF nOmutsu
    RETURNF 2
RETURNF 0

;retard checks to see if weapon is invalid
@K102_WeaponIsInvalid(ARG)
#FUNCTION
SELECTCASE K102_UPDATE:9
    CASE IS >= 600000000000000000
        IF !DT_CELL_GET("WeaponInventory",K102_UPDATE:9,"id",1)
            RETURNF 1
        ENDIF
    CASE IS < 0
        RETURNF 1
    CASE IS > VAR_TEMP_MAX
        SIF !STRLENS(GET_STR(102, "WeaponPreset", K102_UPDATE:9,"Name"))
            RETURNF 1
    CASE 0 TO VAR_TEMP_MAX
        SIF !STRLENS(GET_STR(102, "Weapon", K102_UPDATE:9,"名前"))
            RETURNF 1
ENDSELECT


;default gun for Mommy Shinki
@K102_GetGun
#FUNCTION
IF K102_WeaponIsInvalid(K102_UPDATE:9)
    K102_UPDATE:9 = K102_DefaultGun
ENDIF
RETURNF K102_UPDATE:9

; @K102_MASTER(ARG)
; #FUNCTIONS
; ;She's mad at you, or you're lovers
; SIF ARG || TALENT:102:恋人
;     RETURNF @"%CALLNAME:MASTER%"
; IF !IS_CHILD(MASTER)
; ;aka-chan
;     RETURNF @"musuko-san"
;     ;RETURNF @"%CALLNAME:MASTER%-san"
; ELSE
;     RETURNF CHAN_KUN(MASTER)
; ENDIF


@K102_MISCLICK(MISCLICK_TYPE)
#DIM MISCLICK_TYPE

IF FIRSTTIME("ShinkiMisclick")
    CALL PRINT_GPC("W", @"「Ahh!」", 102)
    CALL PRINT_GP("W", @"%CALLNAME:102% jumps out and looks at you with scorn.")
    CALL PRINT_GPC("W", @"「What is wrong with you?!」", 102)
    CALL PRINT_GPC("W", @"「You just randomly \@ MISCLICK_TYPE == 310 ? groped my ass # lifted my dress\@ like a pervert!」", 102)
    CALL PRINT_GP("W", @"You try to explain that it was an accident, but %CALLNAME:102% just rolls %HIS_HER(TARGET)% eyes.")
    CALL PRINT_GPC("W", @"「Are you serious, %CALLNAME:MASTER%? Really?」", 102)
    LOCALS '= "「You were trying to "
    SELECTCASE PREVCOM
        CASE 300
            LOCALS += "talk to me"
        CASE 302
            LOCALS += "rub my arms"
        CASE 301
            LOCALS += "serve me \@ K102_TeaSucks ? your piss # tea\@"
    ENDSELECT
    CALL PRINT_GPC("W", @"「But you then just \"accidentally\" \@ MISCLICK_TYPE == 310 ? started groping my bottom # showed everyone my %PeeProtectType(102,4)%\@ instead?」", 102)
    PRINTL
    CALL PRINT_GP("W", @"You apologize and plead for forgiveness, trying to make %HIM_HER(102)% forget.")
    CALL PRINT_GP("W", @"Surprisingly, %HE_SHE(102)% relents.")
    CALL PRINT_GPC("W", @"「Okay. I understand it was just a Meta World disturbance.」", 102)
    CALL PRINT_GPC("W", @"「In the Meta World, that's called a misclick, and thats when you choose an option that you did not want to pick.」", 102)
    CALL PRINT_GPC("W", @"「Just.... just don't do it again.」", 102)
ELSEIF FIRSTTIME("ShinkiMisclick2")
    ;second time
    CALL PRINT_GPC("W", @"「You \"accidentally\" \@ MISCLICK_TYPE ? grabbed my %COND_STR("diapered ",IsPadded(102))%ass # showed my %COND_STR("diapered ",IsPadded(102))%crotch\@ off to the world AGAIN?」", 102)
    IF CAN_ACCIDENT(MASTER,1) || CAN_ACCIDENT(MASTER,2)
        CALL PRINT_GPC("W", @"「Next time, I'm going to \"accidentally\" make you have an accident!」", 102)
    ELSE
        CALL PRINT_GPC("W", @"「Next time, I'm going to \"accidentally\" make you learn your lesson!」", 102)
    ENDIF
ELSE
    ;third time, she backs the promise
    CALL PRINT_GP("W", @"%CALLNAME:102% jumps out and %HE_SHE(102)% glares at you.")
    IF CAN_ACCIDENT(MASTER,1) && CAN_ACCIDENT(MASTER,2)
        CALL PROCESS_YOUJUTSU_COMMAND(102,MASTER,18,35)
    ELSEIF CAN_ACCIDENT(MASTER,2)
        CALL PROCESS_YOUJUTSU_COMMAND(102,MASTER,18,24)
    ELSEIF CAN_ACCIDENT(MASTER,1)
        CALL PROCESS_YOUJUTSU_COMMAND(102,MASTER,18,14)
    ELSE
        SELECTCASE MISCLICK_TYPE
        CASE 310
        K102_PlayerPunishedFor '= "misclickGrope"
        CASEELSE
        K102_PlayerPunishedFor '= "misclickSkirt"
        ENDSELECT
        CALL K102_Punishment
        RETURN 1
    ENDIF
    SIF CAN_ACCIDENT(MASTER,2)
    CALL PRINT_GP("L", @"You can feel your %PeeProtectType(MASTER,2)% bulging out as a mushy mess enters your %PeeProtectType(MASTER,2)%, the smell filling the air of both yours and %CALLNAME:102%'s noses.")
    SIF CAN_ACCIDENT(MASTER,1)
    CALL PRINT_GP("L", @"\@ CAN_ACCIDENT(MASTER, 1) ? At the same time, you can feel your # You can feel your \@ bladder involuntarily releasing a torrent of pee into your %PeeProtectType(MASTER,2)%, absolutely soaking it.")
    CALL OMORASHI_END(MASTER, CAN_ACCIDENT(MASTER,1), CAN_ACCIDENT(MASTER,2))
    CALL PRINT_GPC("W", @"「That's what you get for \"accidentally\" \@ MISCLICK_TYPE == 310 ? grabbing my ass # lifting my dress off \@ to the world.」", 102)
    CALL PRINT_GPC("W", @"「And to just rub it in...」", 102)
    CALL PRINT_GP("W", @"%CALLNAME:102% \"accidentally\" lifts up your skirt, revealing your soiled %PeeProtectType(MASTER)%.")
    CALL PRINT_GP("W", @"%HIS_HER(102,1)% hands \"accidentally\" begins to grope the back of your soiled %PeeProtectType(MASTER,2)%, making you blush beet red.")
    CALL PRINT_GPC("W", @"「I think that's enough of a punishment for my for my little %PRINT_MALE("boy", MASTER)%.」", 102)
ENDIF
SOURCE:102:反感 = 0
CUP:102:反感 = 0


@K102_CatchesYouDelayingPunishment(ARGS)
PRINTFORMDW %CALLNAME:102% glares at you, a frown forming on %HIS_HER(102)% face as %HIS_HER(102)% eyes turn red.
IF CFLAG:102:300 != CFLAG:MASTER:300
    ;teleports to you
    CFLAG:102:300 = CFLAG:MASTER:300
    PRINTFORMDW %HE_SHE(102,1)% teleports in front of you, and before you know it, you're grabbed and stuck tightly against %HIS_HER(102)%
ELSE
    PRINTFORMDW Before you know it, you're grabbed and stuck tightly against %HIS_HER(102)%.
ENDIF
PRINTFORMDW %HE_SHE(102,1)% has %HIS_HER(102)% arm around your neck.
PRINTFORMDW You can't even move. It's hard to breathe.
IF ARGS == "date"
    PRINTFORMDL %CALLNAME:(CFLAG:MASTER:デート中)% freaks out and runs away
    PRINTFORMDW You had to end the date early...
    CALL DATE_EVENT(CFLAG:MASTER:デート中)
    CALL SET_DATE(99,CFLAG:MASTER:デート中)
ENDIF
CALL PRINT_GPC("W", @"「%ToUpper(CALLNAME:MASTER)%!」", 102, C_RED)
SELECTCASE ARGS
CASE "date"
    CALL PRINT_GPC("W", @"「You thought you could date someone to avoid your punishment, huh♪」", 102, C_RED)
CASEELSE
    CALL PRINT_GPC("W", @"「You thought you could escape your punishment forever huh?」", 102, C_RED)
ENDSELECT
CALL PRINT_GPC("W", @"「The God of Makai never forgets....」", 102, C_RED)
PRINTFORMDW Eventually, %HE_SHE(102)% calms down, but %HES_SHES(102)% not content on letting you go just yet.
PRINTL
CALL K102_Punishment
RETURN 1

;K102_PlayerPunishedFor '= "misclickGrope"

@K102_CALLNAME()
#FUNCTIONS
IF SaveIsEZB
    RETURNF "The Otherworldy God"
ELSE
    RETURNF CALLNAME:102
ENDIF


@K102_FirstTimePottyPantsWarn(ARG,ARG:1=-1)
SELECTCASE K102_PlayerShitSelfWithDoll
CASE 0
CASEELSE
CALL K102_TooManyPottyPants(0)
ENDSELECT

;if you pee/poop yourself at least 4 times in 3 days with the mini shinki, she'll forcefully diaper you if you aren't padded
;or if you have an accident in front of at least 3 people and none of them are carers
;doesn't trigger if you're diapered
@K102_TooManyPottyPants(ARG)
#DIM DYNAMIC HasPoopedSelf
HasPoopedSelf = GETBIT(PottyChartStatus:ARG:(DAY % 7),1)
IF HasPoopedSelf == 2
    CALL PRINT_GPC("W", @"「Ugh! I can't trust you to be in pull-ups if you just keep pooping in them.」", 102)
    CALL PRINT_GPC("W", @"「These pull-ups are made for those who have the occasional flood or bowel oopsie.」", 102)
    CALL PRINT_GP("W", @"Shinki moves to go behind you.")
    CALL PRINT_GPC("W", @"「Not for...」", 102)
    CALL PRINT_GP("W", @"Shinki sniffs as she places her hand at the back of your messy pull-up.")
    CALL PRINT_GPC("W", @"「...someone who uses them as a full on toilet♥.」", 102)
ELSE
    CALL PRINT_GPC("W", @"「Aggh! I can't believe you did this again!」", 102)
    IF HasPoopedSelf
    CALL PRINT_GPC("W", @"「You've pooped your pants!!!」", 102)
    CALL PRINT_GPC("W", @"「Look how brown the back is.」", 102)
    ELSE
    CALL PRINT_GPC("W", @"「You've wet your pants!!!」", 102)
    ENDIF
    CALL PRINT_GP("W", @"Shinki glares at you in a slight disgust, until she suddenly becomes inspired.")
ENDIF
PRINTL
CALL PRINT_GPC("W", @"「Well, since you want to go potty right in your pants....」", 102)
CALL PRINT_GPC("W", @"「I have something that'll probably be great for you.♥」", 102)
CALL PRINT_GPC("W", @"「Get on the ground, I need to get these dirty pants off you」", 102)
CALL PRINT_GP("W", @"You sit down on the ground, ignoring your mess, and lie back.")
CALL PRINT_GPC("W", @"「You know, don't you? Lying on your back like a real baby.」", 102)
PRINTL
CALL PRINT_GP("W", @"She summons a pack of baby wipes and shows it off to you.")
CALL PRINT_GPC("W", @"「I got some wipes to clean your filthy ass off.」", 102)
CALL PRINT_GP("W", @"She puts the baby wipes down and summons a bottle full of powder.")
CALL PRINT_GPC("W", @"「Some fresh baby powder to keep you fresh and clean.」", 102)
IF HasPoopedSelf
CALL PRINT_GPC("W", @"「And the nicest, bestest diaper that'll keep all of your messes contained.」", 102)
CALL PRINT_GP("W", @"She touches the diaper with her hands, each touch making a plastic rustle.")
CALL PRINT_GPC("W", @"「Low at how crinkly it is.」", 102)
ELSE
CALL PRINT_GPC("W", @"「And the nicest, bestest pull-up so you can pee wherever you want.」", 102)
CALL PRINT_GPC("W", @"「If you keep doing what you're doing, you might even get an upgrade.」", 102)
CALL PRINT_GP("W", @"An upgrade to what?")
PRINTL
CALL PRINT_GPC("W", @"「Well... imagine these, but with tapes...」", 102)
CALL PRINT_GP("W", @"Uhhhhh. It doesn't really cross your mind")
CALL PRINT_GPC("W", @"「and crinkly plastic...」", 102)
CALL PRINT_GP("W", @"Well there's a bunch of things with loud crinkly plastic.")
CALL PRINT_GPC("W", @"「and the oh so cute designs...」", 102)
PRINTL
CALL PRINT_GP("W", @"Oh.")
CALL PRINT_GP("W", @"Diapers. Tape-on diapers.")
CALL PRINT_GP("W", @"You blush profusely, the realization filling you with humiliation.")
CALL PRINT_GPC("W", @"「You're such a baby. You don't need to be all embarrassed about it.」", 102)
CALL PRINT_GPC("W", @"「It'll be over quick.」", 102)
ENDIF


CALL PRINT_GPC("W", @"「You go in your diapies, okay.」", 102)
CALL PRINT_GPC("W", @"「When they get stinky or begin to leak, just let me know and I'll give you a change.」", 102)



@M_KOJO_CHECK_K102_SNOWMAN(ARGS,ARG,ARG:1)
#DIM SNOWMAN_TYPE
LOCAL = 30

$Retry
IF !FA17_OBJ:(ARG % 100)
    SNOWMAN_TYPE = RAND:8
ELSE
    SNOWMAN_TYPE = FA17_TYPE:(ARG % 100)
ENDIF
SELECTCASE SNOWMAN_TYPE
CASE 0
    SIF K102_UPDATE:5 == 2
        GOTO Retry
    RESULTS = snow bottle
    RESULTS:1 = filled with water
    LOCAL = 60
CASE 1
    SIF K102_UPDATE:5
        GOTO Retry
    RESULTS = snow diaper
    RESULTS:1 = that's full and leaking
    LOCAL = 60		
CASE 2
    SIF K102_UPDATE:5 == 2
        GOTO Retry
    RESULTS = ice pacifier
    RESULTS:1 = that doesn't seem to melt
    LOCAL = 40
CASE 3
    RESULTS = snow Yumeko
    RESULTS:1 = doing little snow angles herself
    LOCAL = 70
CASE 4
    RESULTS = snow Alice
    RESULTS:1 = making her own snow dolls
    LOCAL = 50
CASE 5
    RESULTS = ice devil
    RESULTS:1 = justaposing the icy atmosphere
    LOCAL = 40
CASE 6
    RESULTS = small icy ship
    RESULTS:1 = in the style of big sailboats 
    LOCAL = 90
CASE 7
    RESULTS = icy Mini-Shinki
    RESULTS:1 = doing her part in the snowy chaos
    LOCAL = 120
ENDSELECT

RETURN LOCAL, SNOWMAN_TYPE

@K102_Akachan()
IF FIRSTTIME("K102_Akachan",,102)
    PRINTFORMDW Akachan?
    PRINTFORMW 「Baby, %CALLNAME:MASTER%. Akachan means baby.」
ENDIF