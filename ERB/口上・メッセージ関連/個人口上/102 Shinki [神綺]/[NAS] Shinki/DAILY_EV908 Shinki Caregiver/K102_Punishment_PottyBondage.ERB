;Shinki ties you down to a potty chair and binds your arms as punishment
;then she gives you enemas
@K102_Punishment_PottyBondage
#DIM PlayerPottiedBeforeHand
#DIM MAGIC_CANDO,4
MAGIC_CANDO:1 = GETBIT(TALENT:MASTER:ESP,21)
MAGIC_CANDO:2 = GETBIT(TALENT:MASTER:ESP,18)
MAGIC_CANDO:3 = GETBIT(TALENT:MASTER:ESP,19)

CALL PRINT_GPC("L", @"「As punishment for your naughty behavior today I've decided on putting you in timeout to help you learn not to ignore doing what you're told, my sweet little %CALLNAME:MASTER%!」", 102)
CALL PRINT_GPC("W", @"「%K102_WhyAmIGettingPunished()%, so now you're going to timeout as a reminder.」", 102)
PRINTL
CALL PRINT_GPC("L", @"「Now now, there's no use trying to delay the inevitable, put your hands up where I can see them well.」", 102)
CALL PRINT_GP("L", @"%HE_SHE(102,1)% looks at you sternly, waiting for your action.")


{
CALL ASK_M(
    @"I'm so sorry %PRINT_MALE("daddy:mommy",102)%. I promise to be better next time.", 1,
    "No! You can't do this to me!", 1,
    "[Run] Timeout? Nah, I rather walk free.", 1,
    COND_STR(@"Put me in timeout. I've been a bad %PRINT_MALE("boy",MASTER)%.",K102_PlayerConfessesAboutLikingDiapers || GETBIT(TALENT:MASTER:OmoGuardian,2)), K102_PlayerConfessesAboutLikingDiapers || GETBIT(TALENT:MASTER:OmoGuardian,2),
    @"\@ MAGIC_CANDO:0 ? [Youjutsu] Cast『Skip』#\@", MAGIC_CANDO:0,
    @"\@ MAGIC_CANDO:1 ? Cast『Dorima』#\@", MAGIC_CANDO:1,
    @"\@ MAGIC_CANDO:2 ? Cast『Coprodyne』#\@", MAGIC_CANDO:2,
)
}
SELECTCASE RESULT
CASE 0
    CALL PRINT_GPC("L", @"「I understand, %CALLNAME:MASTER%, but your actions have consequences, and you can't undo what already happened.」", 102)
    CALL PRINT_GPC("L", @"「Well, other than Meta World intervention, but let's just forget about that for now.」", 102)
    CALL PRINT_GP("L", @"You place your hands up high.")
    CALL PRINT_GPC("L", @"「Good %PRINT_MALE("boy",MASTER)%. Now face your back against me.」", 102)
    CALL PRINT_GP("W", @"And you turn your back against %CALLNAME:102%, who quickly grabs your hands and starts binding them with rope.")
CASE 1
    CALL PRINT_GPC("L", @"「Yes, I can. Now let me do this real quick.」", 102)
    $CastFail2
    CALL PRINT_GP("L", @"%HE_SHE(102,1)% turns you around, grabs your hands, and starts binding them together.")
    CALL PRINT_GP("W", @"You continue to complain, but its futile.")
CASE 2
    CALL PRINT_GP("W", @"You run off, attempting to run away from %HIM_HER(102)%.")
    CALL PRINT_GP("W", @"In a flash, you're teleported to right back where you started.")
    CALL PRINT_GPC("L", @"「%CALLNAME:MASTER%. This isn't funny. You're just making yourself look like a fool.」", 102)
    CALL PRINT_GP("W", @"You try to run away from %CALLNAME:102% again, but %HE_SHE(102)% just keeps teleporting you back.")
    CALL PRINT_GP("W", @"Frustrated with your antics, %HE_SHE(102)% grabs your hand and binds them together with rope.")
CASE 3
    CALL PRINT_GP("W", @"You turn your back against %CALLNAME:102%, who gently grabs your hands and starts binding them with rope.")
    CALL PRINT_GPC("L", @"「You really get to the point quickly, huh?」", 102)
CASE 4
    CALL PRINT_GPC("W", @"「Skip!」", MASTER)
    CALL PRINT_GP("W", @"You teleport around the area.")
    SETCOLOR C_RED
    FONTITALIC
    PRINTFORMDL
    PRINTFORMW What in the hell?
    FONTREGULAR
    PRINTFORMDL
    CALL PRINT_GP("W", @"%HES_SHES(102,1)% confused for a bit, trying to look for you")
    CALL PRINT_GPC("W", @"「Skip!」", MASTER)
    CALL PRINT_GP("W", @"You teleport around the area again.")
    SETCOLOR C_RED
    FONTITALIC
    PRINTFORMDL
    PRINTFORMW 「%CALLNAME:MASTER%. This isn't funny. You're just making yourself look like a fool.」
    FONTREGULAR
    PRINTFORMDL
    CALL PRINT_GP("W", @"This continues for a bit, but %CALLNAME:102% gets fed up with your game very quickly.")
    CALL PRINT_GPC("W", @"「Cast Reunion from %NAME:MASTER%, ID {MASTER}, to %NAME:102%, ID {102}.」", 102, C_RED)
    CALL PRINT_GP("W", @"You're teleported back to %CALLNAME:102% unexpectedly, allowing %HIM_HER(102)% to grab you tight.")
    CALL PRINT_GPC("W", @"「Su... S-Skip!」", MASTER)
    CALL PRINT_GP("W", @"You teleport yourself free from %HIS_HER(102)% grasp.")
    SETCOLOR C_RED
    FONTITALIC
    PRINTFORMDL
    PRINTFORMW What a stubborn %COND_STR("son of a ", IS_MALE(MASTER))%bitch.
    PRINTFORMW 「Cast Reunion from %NAME:MASTER%, ID {MASTER}, to %NAME:102%, ID {102}.」
    PRINTFORMW 「Makajama...」
    FONTREGULAR
    PRINTFORMDL
    CALL PRINT_GP("W", @"As your ability to talk, and therefore cast magic, is sealed away, you try to run, attempting to escape the inevitable.")
    CALL PRINT_GP("W", @"This however, does not last for long.")
    CALL PRINT_GP("W", @"Frustrated with your antics, %HE_SHE(102)% grabs your hand and binds them together with rope.")
CASE 5
    CALL K102_Punishment_NotNowMommy
    RETURN
CASE 6
    CALL PROCESS_YOUJUTSU_COMMAND(MASTER,102,18,24)
    IF RESULT != 9530
        SELECTCASE RAND:3
        CASE 0
        ;win
        CALL PRINT_GPC("L", @"「I need to poop so bad... Did you-」", 102)
        CALL PRINT_GP("L", @"As %HIM_HER(102)% squats down, %HE_SHE(102)% can't help but make a grunt and start pushing.")
        CALL PRINT_GPC("L", @"「%MOAN("m")%...」", 102)
        CALL PRINT_GP("L", @"You can hear a loud fart and the mess sloshing around %HIS_HER(102)% %PeeProtectType(102,2)% as %HE_SHE(102)% packs it with %HIS_HER(102)% mess.")
        CALL PRINT_GP("W", @"By the time that %HE_SHE(102)% finishes pooping, %HIS_HER(102)% %PeeProtectType(102,2)% has visually doubled in size, reeking with a barnyard scent.")
        CALL OMORASHI_END(102,0,1)
        PRINTL
        CALL PRINT_GPC("L", @"「You casted 『%NAS_ESP_NAME%』 didn't you?」", 102)
        CALL PRINT_GPC("L", @"「I made such a mess. I'm sorry for trying to put you in timeout.」", 102)
        CALL PRINT_GPC("L", @"「You may be victorious for now, but you won't get away with this later.」", 102)
        K102_PlayerPunishAutoClear = 0
        K102_AwaitingFurtherPunishment = 1
        K102_PlayerPunishedFor '= "delayingPunishment"
        RETURN
        CASE 1
            CALL PROCESS_YOUJUTSU_COMMAND(102,102,14,18)
            CLEARLINE 1
            CALLF POOME(102,0)
            IF CAN_ACCIDENT(MASTER,2)
                CALL PRINT_GPC("L", @"「Just what do you think you're doing?」", 102)
                CALLF POOME(MASTER)
                CALL CAUSE_OMORASHI(MASTER,0,1)
                CALL PRINT_GP("L", @"In your humiliation, %HE_SHE(102,1)% turns around, grabs your hands, and binds them together with rope.")
                CALL PRINT_GPC("L", @"「You thought you could try escaping by making me have an accident, huh?」", 102)
                CALL PRINT_GPC("L", @"「Look who's shitting %HIMSELF_HERSELF(MASTER)% now.」", 102)
            ELSE
                GOTO CastFail
            ENDIF
        CASE 2
            CALL PROCESS_YOUJUTSU_COMMAND(102,102,18,28)
            GOTO CastFail
        ENDSELECT
    ELSE
        $CastFail
        CALL PRINT_GP("W", @"Why isn't it working?")
        CALL PRINT_GPC("L", @"「Nice try, %CALLNAME:MASTER%.」", 102)
        GOTO CastFail2
    ENDIF
ENDSELECT
PRINTL
;skip abdl content when its off
SIF !CAN_ACCIDENT(MASTER,1) && !CAN_ACCIDENT(MASTER,2)
    GOTO NonABDLModeSkip
;Shinki removes your underwear
CALL PRINT_GPC("L", @"「Now, let me change you out of your underwear real quick? We can't start off with a stinky tush, you know.」", 102)
CALL PRINT_GP("L", @"%CALLNAME:102% brings out a changing pad with some kind of strap and lays it down on the floor.")
CALL PRINT_GPC("L", @"「Lie down for me real quick. This won't take long.」", 102)
CALL PRINT_GP("L", @"You lower yourself onto a sitting position, before lying your back down on the changing pad.")
CALL PRINT_GP("L", @"%CALLNAME:102% also gently squats down.")

IF PooBarStage(102) >= 2 && GETBIT(nScat,2)
    PRINTL
    CALL PRINT_GP("L", @"As %HIM_HER(102)% squats down, %HE_SHE(102)% can't help but make a grunt and start pushing.")
    CALL PRINT_GPC("L", @"「%MOAN("m")%...」", 102)
    CALL PRINT_GP("L", @"You can hear a fart and the mess sloshing around %HIS_HER(102)% %PeeProtectType(102,2)%.")
    CALL PRINT_GPC("L", @"「Oh... sorry about that, %CALLNAME:MASTER%.」", 102)
    CALL PRINT_GPC("L", @"「I guess I'll need a change too.」", 102)
    CALL OMORASHI_END(102,0,1)
ENDIF
CALL PRINT_GP("L", @"%CALLNAME:102% spreads your legs using %HIS_HER(102)% hands, allowing for easy access to your \@ K102_ABDLMode() ? diaper area # crotch\@.")
PRINTL
CALL MESSAGE_UNDRESSING_BOTTOM_NAS(MASTER,102,2)
IF EQUIP:MASTER:6
    CALL MESSAGE_UNDERWEAR_UNDRESS_REMOVE_PANTY_NAS(MASTER,102)
    PRINTL
    IF SoiledDiaper(MASTER,2,2,0) >= 500
        CALL PRINT_GPC("W", @"「Oh dear, %CALLNAME:MASTER%. You really packed those \@ IsPadded(MASTER) ? pamps # pants\@.」", 102)
        CALL PRINT_GPC("W", @"「I guess you really do need your diapers.」", 102)
    ELSEIF SoiledDiaper(MASTER,1,2,0) >= 500 
        CALL PRINT_GPC("W", @"「Wow, you peed quite a bit.」", 102)
        CALL PRINT_GPC("W", @"「I guess you really do need your diapers.」", 102)
    ENDIF
ENDIF
CALL PRINT_GP("L", @"%HE_SHE(102,1)% grabs a few wet wipes and wipes down your crotch, cleaning up any mess that got onto it.")
SIF EQUIP:MASTER:6
CALL MESSAGE_UNDERWEAR_UNDRESS_POCKET_PANTY_NAS(MASTER,102)
CALLF PANTY_REWORK_CHANGE(MASTER,0)

;She puts an enema up your ass if scat is on.
;Gensou-chan Dai-barase: Very strong fact-acting enema. Like KyuuKyuu-Ana, but also causes hyperscats.
;Gensou-chan KyuuKyuu-Ana: KyuuKyuu-Un but anally injected.
;Urbworld Saline Enema: Basic stuff.
IF CAN_ACCIDENT(MASTER,2)
    PRINTL
    CALL PRINT_GPC("W", @"「Stay still for me, okay.」", 102)
    IF !K102_TimesWentToTimeOut
        CALL PRINT_GP("L", @"As you lay down, you begin to notice something entering your anus.")
        CALL PRINT_GP("L", @"You look up and see that %HES_SHES(102)% inserting some kind of bottle full of fluid up your ass.")
        {
        CALL ASK_M(
            @"...", 1,
            "What are you doing to me?!", 1,
            @"\@ ITEM:9 ? Hey, I have one of those too. #\@", ITEM:9,
        )
        }
        SELECTCASE RESULT
        CASE 0
            CALL PRINT_GP("L", @"You feel the liquid sloshing throughout your bowels.")
        CASE 1
            CALL PRINT_GPC("W", @"「Relax. It's just the regular Urbworld stuff. Keeps you calm, nothing too special.」", 102)
            CALL PRINT_GP("L", @"Before you can say anything back, you feel the liquid sloshing throughout the insides of your stomach.")
                CALL PRINT_GP("L", @"Urbworld stuff? What does that mean?")
        CASE 2
            CALL PRINT_GPC("W", @"「You have one of those anal injectors too? Well that spoils the surprise.」", 102)
            CALL PRINT_GP("W", @"You correct %CALLNAME:102%, telling %HIM_HER(102)% that it's an enema.")
            CALL PRINT_GP("L", @"You feel the liquid sloshing throughout your bowels as you interact with %HIM_HER(102)%.")
        ENDSELECT
        DrugStats:MASTER:EnemaStrength  = 30
        DrugStats:MASTER:EnemaTime  = 90
    ELSE
        CALL PRINT_GP("L", @"You lie down on your back, noticing %CALLNAME:102% pull out something.")
        SELECTCASE RAND:3
        CASE 0
            CALL PRINT_GP("L", @"You see a small enema bottle with the markings \"SALINE ENEMA\".")
            CALL PRINT_GPC("W", @"「Relax. It's just the regular Urbworld stuff. Keeps you calm, nothing too special.」", 102)
            DrugStats:MASTER:EnemaStrength  = 30
            DrugStats:MASTER:EnemaTime  = 90
        CASE 1
            CALL PRINT_GP("L", @"You see a small enema bottle with the markings \"KyuuKyuu-Ana\".")
            CALL PRINT_GP("L", @"%HE_SHE(102,1)% feigns concern as you look at what %HES_SHES(102)% holding.")
            CALL PRINT_GPC("W", @"「Oh, well I guess you saw the label.」", 102)
            CALL PRINT_GPC("W", @"「Its KyuuKyuu-Un, but in a version that allows you to inject anally.」", 102)
            CALL PRINT_GPC("W", @"「It'll make you go quicky.」", 102)
            CALL PRINT_GP("L", @"You have a bad feeling when %CALLNAME:102% said that it'll make you \"go quicky\".")
            DrugStats:MASTER:EnemaStrength  = 45
            DrugStats:MASTER:EnemaTime  = 120
        CASE 2
            CALL PRINT_GP("L", @"You see a small enema bottle with the markings \"Gensou-chan Dai-barase\".")
            CALL PRINT_GPC("W", @"「Here is some Dai-barase.」", 102)
            CALL PRINT_GPC("W", @"「Makes KyuuKyuu-Ana look like a cup of coffee in comparison.」", 102)
            CALL PRINT_GPC("W", @"「Once this is in you, you'll only have a couple of minutes before your bowels shoot out everything inside. Ufufu~」", 102)
            DrugStats:MASTER:EnemaStrength  = 90
            DrugStats:MASTER:EnemaTime  = 180
        ENDSELECT
        CALL PRINT_GP("L", @"You look up and see that %HES_SHES(102)% inserting some kind of bottle full of fluid up your ass.")
        CALL PRINT_GP("L", @"You feel the liquid sloshing throughout your bowels.")
    ENDIF
    ;CALL PRINT_GPC("W", @"「Kyuu-Ana」", 102)
    ;K102_PlayerUsedKyuuKyuu
ELSEIF CAN_ACCIDENT(MASTER,1)
    ;wetting
    PRINTL
    CALL PRINT_GPC("W", @"「Stay still for me, okay.」", 102)
    IF !K102_TimesWentToTimeOut
        CALL PRINT_GP("L", @"As you lay down, you begin to notice the feeling of a nipple in your mouth.")
        CALL PRINT_GP("L", @"You look up and see that %HES_SHES(102)% trying to make you drink something from a baby bottle.")
        {
        CALL ASK_M(
            @"...", 1,
            "What are you doing to me?!", 1 
        )
        }
        SELECTCASE RESULT
        CASE 0
            CALL PRINT_GP("L", @"You feel the liquid sloshing throughout your mouth.")
        CASE 1
            CALL PRINT_GPC("W", @"「Relax. It's just the regular Urbworld stuff. Keeps you calm, nothing too special.」", 102)
            CALL PRINT_GP("L", @"Before you can say anything back, you feel the liquid sloshing throughout your mouth.")
            CALL PRINT_GP("L", @"Urbworld stuff? What does that mean?")
        ENDSELECT
        TCVAR:MASTER:利尿剤  = 210
    ELSE
        CALL PRINT_GP("L", @"You lie down on your back, noticing %CALLNAME:102% pull out something.")
        SELECTCASE RAND:3
        CASE 0
            CALL PRINT_GP("L", @"You see a bottle with the markings \"DIURETIC\".")
            CALL PRINT_GPC("W", @"「Relax. It's just the regular Urbworld stuff. Keeps you calm, nothing too special.」", 102)
            DrugStats:MASTER:EnemaStrength  = 30
            DrugStats:MASTER:EnemaTime  = 90
        CASE 1
            CALL PRINT_GP("L", @"You see a bottle with the markings \"DIURETIC\".")
            CALL PRINT_GPC("W", @"「Relax. It's just the regular Urbworld stuff. Keeps you calm, nothing too special.」", 102)
            DrugStats:MASTER:EnemaStrength  = 30
            DrugStats:MASTER:EnemaTime  = 90
        CASE 2
            CALL PRINT_GP("L", @"You see a baby bottle with the markings \"KyuuKyuu-Nyo\".")
            CALL PRINT_GPC("W", @"「Its KyuuKyuu-Nyo, your favorite.」", 102)
            CALL PRINT_GP("L", @"Before you can say anything back, you feel the liquid sloshing throughout your mouth.")
            CALL PRINT_GPC("W", @"「Drink up.」", 102)
            DrugStats:MASTER:KyuuKyuuNyoTime = 210
        ENDSELECT
        CALL PRINT_GP("L", @"You look up and see that %HES_SHES(102)% inserting some kind of bottle full of fluid up your ass.")
        CALL PRINT_GP("L", @"You feel the liquid sloshing throughout your bowels.")
    ENDIF
ENDIF

PRINTL
CALL PRINT_GP("W", @"%CALLNAME:102% thinks for a bit, before saying what %HES_SHES(102)% going to do further.")

SIF !K102_TimesWentToTimeOut
    GOTO ChangeToUndies

;RNG on what underwear to wear


SELECTCASE RAND(100)
CASE IS < 10
;NO UNDIES
$ChangeToOpen
CALL PRINT_GPC("W", @"「You know what, I feel merciful today. I'll let you go in the potty like a real person. That means no underwear.」", 102)
CASE IS <= 40
$ChangeToUndies
;undies
IF K102_ABDLMode() != 2
    CALL PRINT_GPC("W", @"「A new clean pair of underwear for my big %PRINT_MALE("boy", MASTER)%. Don't ruin it.」", 102)
ELSE
    CALL PRINT_GPC("W", @"「Normally, I would put you in a diaper, but today, I want you to see how you go in big %PRINT_MALE("boy", MASTER)% underwear.」", 102)
ENDIF
CALL DiaperChangeDesc(MASTER, 19, 102, -1)
CASE IS <= 60
;pull-up
IF K102_ABDLMode() != 2
    GOTO ChangeToUndies
ENDIF
CALL PRINT_GPC("W", @"「You know what, you look like a little potty trainer.」", 102)
CALL PRINT_GPC("W", @"「And to look like the real potty trainer that you are, you need some pull-ups.」", 102)
CALL DiaperChangeDesc(MASTER, K102_DynamicDiaper(MASTER, "pull-ups"), 102)
CASEELSE
;diaper
IF K102_ABDLMode() != 2
    GOTO ChangeToUndies
ENDIF
CALL PRINT_GPC("W", @"「Time to give you your protection. You'll need it.」", 102)
CALL DiaperChangeDesc(MASTER, K102_DynamicDiaper(MASTER, "diaper"), 102)
ENDSELECT
CALL PRINT_GP("L", @"%CALLNAME:102% deconstructs the changing pad, watching as it turns into nothingness.")
PRINTL
$NonABDLModeSkip
IF K102_ABDLMode() == 2
;potty
    CALL PRINT_GP("L", @"%CALLNAME:102% brings out a potty chair and a spreader bar and places them down on the floor.")
    CALL PRINT_GP("L", @"%HE_SHE(102,1)% pushes you forward to the potty and lowers you down.")
    CALL PRINT_GP("L", @"The potty is clearly undersized compared to you, causing you to squat on the chair rather than sit on it.")
    CALL PRINT_GP("W", @"%HE_SHE(102,1)% straps the spreader bar to your ankles, forcing your legs wide.")
    PRINTL
    CALL PRINT_GPC("W", @"「Don't worry about the potty, it's there so that any gifts you make can be directly deposited.」", 102)
ELSE
;chair
    CALL PRINT_GP("L", @"%CALLNAME:102% brings out a wooden chair and a spreader bar and places them down on the floor.")
    CALL PRINT_GP("L", @"%HE_SHE(102,1)% pushes you forward to the chair and lowers you down.")
    CALL PRINT_GP("W", @"%HE_SHE(102,1)% straps the spreader bar to your ankles, forcing your legs wide.")
    CALL PRINT_GP("W", @"%HE_SHE(102,1)% then moves your arms up and binds each of your arms to the back of the chair.")
    PRINTL
    CALL PRINT_GPC("L", @"「This chair is rather comfy, I hope you like it.」", 102)
    SIF CAN_ACCIDENT(MASTER,1) || CAN_ACCIDENT(MASTER,2)
    CALL PRINT_GPC("W", @"「It'll be a shame if you were to make a mess...」", 102)
ENDIF
SELECTCASE RAND:10
CASE 0
LOCALS:2 '= "3 hours"
K102_PunishPottyTimeLeft = 180
CASE 1, 2
LOCALS:2 '= "4 hours"
K102_PunishPottyTimeLeft = 240
CASE 3, 4, 5
LOCALS:2 '= "5 hours"
K102_PunishPottyTimeLeft = 300
CASE 6, 7, 8
LOCALS:2 '= "6 hours"
K102_PunishPottyTimeLeft = 360
CASE 9
LOCALS:2 '= "8 hours"
K102_PunishPottyTimeLeft = 480
ENDSELECT
;first time
IF !K102_TimesWentToTimeOut
    LOCALS:2 '= "3 and a half hours, since its your first time"
    K102_PunishPottyTimeLeft = 210
ENDIF

CALL PRINT_GPC("W", @"「I'll let you go after %LOCALS:2%.」", 102)
CALL PRINT_GPC("W", @"「I'll come back to check on you from time to time, okay.」", 102)
CALL PRINT_GP("L", @"%CALLNAME:102% teasingly kisses you on the cheek, before leaving.")
FORCEWAIT
K102_TimesWentToTimeOut++
CALLF ChangeRestraint(MASTER,"RestrainArm",2)
CALLF ChangeRestraint(MASTER,"RestrainLeg",1)
; DrugStats:MASTER:KyuuKyuuNyoTime = 210
; DrugStats:MASTER:KyuuKyuuUnTime  = 210
VARSET K102_PunishPottyExcreted
CALL EAT(MASTER,1000)
RETURN 1




@K102_Punishment_PottyBondage_DiaperCheck(releasePlayer=0)
#DIM DYNAMIC releasePlayer
;add potty stats
K102_PunishPottyExcreted:2 += K102_PunishPottyExcreted:0
K102_PunishPottyExcreted:3 += K102_PunishPottyExcreted:1

CALL PRINT_GP("L", @"%CALLNAME:102% approaches your bound state with a naughty face.")
IF PeeBarStage(MASTER) >= 7 || PooBarStage(MASTER) >= 7
    ;wetting at the start of check
    CALL PRINT_GPC("L", @"「How are you doing-」", 102)
    CALL PRINT_GP("L", @"Your desperation blocks out everything except the feeling of your %PeeAndPoo(PeeBarStage(MASTER) >= 7,PooBarStage(MASTER) >= 7,"bladder","bowels")% about to break.")
    CALL PRINT_GP("W", @"And its pushing out everything right now.")
    PRINTL
    IF !EQUIP:MASTER:6
    ;undressed
        IF K102_ABDLMode() == 2
            ;potty chair
            SIF CAN_ACCIDENT(MASTER,2)
                CALL PRINT_GP("L", @"You can feel your anus pushing out a large log as it drops into the potty bowl, the smell filling the air of both yours and %CALLNAME:102%'s noses.")
            SIF CAN_ACCIDENT(MASTER,1)
                CALL PRINT_GP("L", @"\@ CAN_ACCIDENT(MASTER, 1) ? At the same time, you can feel your # You can feel your \@ bladder involuntarily releasing a torrent of pee into your potty, filling it up.")
        ELSE
            SIF CAN_ACCIDENT(MASTER,2)
                CALL PRINT_GP("L", @"You can feel your anus pushing out a large log as it badly stains into your body and the chair as you sit on it, the smell filling the air of both yours and %CALLNAME:102%'s noses.")
            SIF CAN_ACCIDENT(MASTER,1)
                CALL PRINT_GP("L", @"\@ CAN_ACCIDENT(MASTER, 1) ? At the same time, you can feel your # You can feel your \@ bladder involuntarily releasing a torrent of pee onto the chair, absolutely soaking it and yourself as the pee drips down and pools onto the floor.")
        ENDIF
    ELSE
        ;dressed
        SIF CAN_ACCIDENT(MASTER,2)
        CALL PRINT_GP("L", @"You can feel your %PeeProtectType(MASTER,2)% bulging out as a mushy mess enters your %PeeProtectType(MASTER,2)%, the smell filling the air of both yours and %CALLNAME:102%'s noses.")
        SIF CAN_ACCIDENT(MASTER,1)
        CALL PRINT_GP("L", @"\@ CAN_ACCIDENT(MASTER, 1) ? At the same time, you can feel your # You can feel your \@ bladder involuntarily releasing a torrent of pee into your %PeeProtectType(MASTER,2)%, absolutely soaking it.")
    ENDIF
    CALL PRINT_GP("L", @"Your breathing loosens up, and your senses clear, %CALLNAME:102%'s voice audible.")
    CALL PRINT_GPC("L", @"「Perrrfect timing. Sounds like you really had to go.」", 102)
    CALL PRINT_GP("L", @"Your face turns beet red as you look down and see that you had an accident involuntarily....")
    CALL PRINT_GP("W", @"Right in front of %HIM_HER(102)%.")
    CALL OMORASHI_END(MASTER, CAN_ACCIDENT(MASTER,1), CAN_ACCIDENT(MASTER,2))
ELSE
    IF K102_PunishPottyExcreted:1 >= 200
        CALL PRINT_GP("L", @"As %HE_SHE(102)% walks towards you, a strong odor causes %HIM_HER(102)% to slightly recoil.")
        CALL PRINT_GPC("L", @"「Oh-... it stinks. It smells a lot like poop.」", 102)
        CALL PRINT_GPC("L", @"「Did my little go in %HIS_HER(MASTER)% potty?」", 102)
        CALL PRINT_GP("L", @"Your face turns beet red as you look down and see that you pooped in your potty involuntarily.")
        IF releasePlayer
            CALL PRINT_GPC("L", @"「Don't be so embarrassed, your timeout is over.」", 102)
            CALL PRINT_GPC("L", @"「I just need to change you out.」", 102)
        ENDIF
    ELSEIF K102_PunishPottyExcreted:0 >= 200
        CALL PRINT_GP("L", @"As %HE_SHE(102)% walks towards you, a telltale smell causes %HIM_HER(102)% to slightly smile.")
        IF releasePlayer
            CALL PRINT_GPC("L", @"「Seems like my little one went pee-pee in %HIS_HER(MASTER)% potty one last time.」", 102)
            CALL PRINT_GPC("L", @"「Good thing your timeout is over.」", 102)
        ELSE
            CALL PRINT_GPC("L", @"「It-... smells very urine'y.」", 102)
            CALL PRINT_GPC("L", @"「Did my little go in %HIS_HER(MASTER)% potty?」", 102)
        ENDIF
        CALL PRINT_GP("L", @"Your face turns beet red as you look down and see that you used your potty involuntarily.")
    ELSE
        CALL PRINT_GP("L", @"%HE_SHE(102,1)% walks towards you and lowers down to your level.")
        IF releasePlayer
            CALL PRINT_GPC("L", @"「Timeout's over. Let me check your underwear real quick」", 102)
            CALL PRINT_GP("L", @"You leave a sigh of relief.")
        ELSE
            CALL PRINT_GPC("L", @"「How are you doing %CALLNAME:MASTER%? You feel comfortable?」", 102)
            CALL PRINT_GP("L", @"This position doesn't feel very comfortable for you, but you shrug it off for now.")
        ENDIF
    ENDIF
ENDIF
PRINTW
CALL PRINT_GPC("L", @"「Stand up for %PRINT_MALE("daddy:mommy",102)% %CALLNAME:102%, okay.」", 102)
CALL PRINT_GP("W", @"%HE_SHE(102,1)% gently grabs you by your armpits and lifts you up to standing level.")

IF K102_PunishPottyExcreted:1 >= 200 || K102_PunishPottyExcreted:0 >= 200
    CALL PRINT_GP("L", @"%CALLNAME:102% squats down to your potty and removes the soiled bowl, placing it aside.")
    CALL PRINT_GPC("L", @"「Thank you for your gift. It'll be great use to me.」", 102)
    CALL PRINT_GP("W", @"You blush in humiliation.")
    SIF !releasePlayer
    CALL PRINT_GP("L", @"%CALLNAME:102% places a new bowl in the potty and stands up back to your level.")
    K102_PunishPottyExcreted:0 = 0
    K102_PunishPottyExcreted:1 = 0
ENDIF

IF SoiledDiaper(MASTER,2)
    CALL PRINT_GP("W", @"%CALLNAME:102% looks at the back of your %PeeProtectType(MASTER,2)%, completely astonished by the amount of %CS_JellyOrPoo()% stained all over.")
    CALL PRINT_GPC("L", @"「Wow. Those enemas did a number to your \@ IsPadded(MASTER) ? diaper # panties\@.」", 102)
    CALL PRINT_GPC("L", @"「And the smell... It smells so good...」", 102)
    CALL PRINT_GPC("W", @"「I guess you don't deserve any more big %PRINT_MALE("boy",MASTER)% panties for now.」", 102)
    PRINTL
    CALL PRINT_GPC("L", @"「I hope you're enjoying it, because you aren't getting a change anytime soon.」", 102)
    SELECTCASE ABL:MASTER:PooAddict
    CASE IS < 3
        CALL PRINT_GP("L", @"Bound and messy, all you can do is turn pale at the pathetic state of your \@ IsPadded(MASTER) ? diaper # underwear\@.")
    CASE IS < 5
        CALL PRINT_GP("L", @"You find yourself blushing hard, but in a less mortifying way.")
    CASE IS < 8
        CALL PRINT_GP("L", @"You blush, but deep down, you start to realize that you wanted this.")
        CALL PRINT_GP("L", @"You feel like pooping yourself due to being bound is starting to feel exciting.")
    CASE IS < 10
        CALL PRINT_GP("L", @"Despite your affection to messy accidents, you can't help but blush a bit.")
        CALL PRINT_GP("W", @"An uncontrollable accident like this feels a bit weird to you, but its going to hook you in sooner or later.")
    CASEELSE
        CALL PRINT_GP("W", @"You can't help but say that you wanted this.")
        CALL PRINT_GP("W", @"To be bound to a toilet that you can't use normally as an accident rapidly approaches...")
        CALL PRINT_GP("W", @"To find yourself having no choice but to fill your \@ IsPadded(MASTER) ? diaper # underwear\@...")
        CALL PRINT_GP("W", @"And then having to sit on the potty, with \@ IsPadded(MASTER) ? a diaper # pants\@ full of your stinky accident...")
        CALL PRINT_GP("W", @"It feels truly blissful.")
    ENDSELECT
ELSEIF SoiledDiaper(MASTER,1)
    CALL PRINT_GP("W", @"%CALLNAME:102% looks at the front of your %PeeProtectType(MASTER,2)%, impressed by the big yellow pee stain.")
    CALL PRINT_GPC("L", @"「Seems like my %PRINT_MALE("boy",MASTER)% made an uh-oh, because there's a pee stain at the front!」", 102)
    PRINTL
    CALL PRINT_GPC("L", @"「I hope you're enjoying it, because you aren't getting a change anytime soon.」", 102)
    SELECTCASE ABL:MASTER:PeeAddict
    CASE IS < 3
        CALL PRINT_GP("L", @"Bound and wet, all you can do is turn pale at the pathetic state of your \@ IsPadded(MASTER) ? diaper # underwear\@.")
    CASE IS < 5
        CALL PRINT_GP("L", @"You find yourself blushing hard, but in a less mortifying way.")
    CASE IS < 8
        CALL PRINT_GP("L", @"You blush, but deep down, you start to realize that you wanted this.")
        CALL PRINT_GP("L", @"You feel like peeing yourself due to being bound is starting to feel exciting.")
    CASE IS < 10
        CALL PRINT_GP("L", @"Despite your obsession with peeing yourself, you can't help but blush a bit.")
        CALL PRINT_GP("W", @"An uncontrollable accident like this feels a bit weird to you, but its going to hook you in sooner or later.")
    CASEELSE
        CALL PRINT_GP("W", @"You can't help but say that you wanted this.")
        CALL PRINT_GP("W", @"To be bound to a toilet that you can't use normally as an accident rapidly approaches...")
        CALL PRINT_GP("W", @"To find yourself having no choice but to just pee...")
        IF IsPadded(MASTER)
            CALL PRINT_GP("W", @"Not filling your potty, but your diaper instead...")
        ELSE
            CALL PRINT_GP("W", @"Filling your potty in such a naughty way...")
        ENDIF
        CALL PRINT_GP("W", @"It feels truly blissful.")
    ENDSELECT
ELSE
    ;shinki gives you enemas
    CALL PRINT_GP("W", @"%CALLNAME:102% pats the front and back of your %PeeProtectType(MASTER,2)%, looking for any soiling.")
    CALL PRINT_GP("W", @"When %HE_SHE(102)% can't find any, a disappointed look grows on %HIS_HER(102)% face.")
    IF releasePlayer
        CALL PRINT_GPC("L", @"「Well, at least you held it in.」", 102)
    ELSE
        CALL PRINT_GPC("L", @"「It's dry?」", 102)
        CALL PRINT_GPC("L", @"「No no.... We can't have that.」", 102)
    ENDIF
ENDIF
PRINTL

IF releasePlayer
    $releaseThePlayer
    CALL K102_Punishment_PottyBondage_ReleasePlayer
    RETURN
ENDIF

;random fun stuff
$RedoFun
SELECTCASE RAND:5
CASE 0
    SIF TCVAR:MASTER:LiquidIntake && DrugStats:MASTER:KyuuKyuuNyoTime
        GOTO RedoFun
    CALL PRINT_GPC("L", @"「Here, have a KyuuKyuu on me. You look like you're thirsty.」", 102)
    CALL PRINT_GP("L", @"Without any warning, %CALLNAME:102% pushes a bottle of KyuuKyuu on your mouth and lifts it up.")
    CALL PRINT_GP("L", @"As the liquid on the bottle flows, you begin to drink, eventually drinking the entire bottle.")
        DrugStats:MASTER:KyuuKyuuNyoTime = 210
        DrugStats:MASTER:KyuuKyuuUnTime  = 210
    CALL DRINK(MASTER,500)
CASE 1
    SIF PooBarStage(MASTER) < 2 && PeeBarStage(MASTER) < 2
        GOTO RedoFun
    CALL PRINT_GPC("L", @"「Aren't you bursting for relief? Why delay the inevitable?」", 102)
    CALL PRINT_GPC("W", @"「Just let go, it'll feel really good.」", 102)
    IF PooBarStage(MASTER) >= 2
        CALL PRINT_GP("L", @"You find yourself hunching over, your stomach hurting more badly than before.")
        CALL PRINT_GP("L", @"Its almost like %HE_SHE(102)% has a trigger for your bowels.")
    ELSEIF PooBarStage(MASTER) >= 2
    ENDIF
    CALL ASK_YN(@"Give up and \@ EQUIP:MASTER:6 ?  soil yourself # use the potty \@", "Keep holding it")
    SELECTCASE RESULT
    CASE 0
        SIF CAN_ACCIDENT(MASTER,2)
            CALLF Poome(MASTER)
        SIF CAN_ACCIDENT(MASTER,1)
            CALLF Peeme(MASTER)
        CALL PRINT_GP("L", @"You decide to end your suffering and let go of your %PeeAndPoo(PeeBarStage(MASTER) >= 2, PooBarStage(MASTER) >= 2, "bladder", "bowels")%.")
        CALL PRINT_GPC("L", @"「Letting go? Let me help you out」", 102)
        CALL PRINT_GPC("L", @"「Double Trouble...」", 102)

        IF !EQUIP:MASTER:6
        ;undressed
            IF K102_ABDLMode() == 2
                ;potty chair
                SIF CAN_ACCIDENT(MASTER,2)
                    CALL PRINT_GP("L", @"You can feel your anus pushing out a large log as it drops into the potty bowl, the smell filling the air of both yours and %CALLNAME:102%'s noses.")
                SIF CAN_ACCIDENT(MASTER,1)
                    CALL PRINT_GP("L", @"\@ CAN_ACCIDENT(MASTER, 1) ? At the same time, you can feel your # You can feel your \@ bladder involuntarily releasing a torrent of pee into your potty, filling it up.")
            ELSE
                SIF CAN_ACCIDENT(MASTER,2)
                    CALL PRINT_GP("L", @"You can feel your anus pushing out a large log as it badly stains into your body and the chair as you sit on it, the smell filling the air of both yours and %CALLNAME:102%'s noses.")
                SIF CAN_ACCIDENT(MASTER,1)
                    CALL PRINT_GP("L", @"\@ CAN_ACCIDENT(MASTER, 1) ? At the same time, you can feel your # You can feel your \@ bladder involuntarily releasing a torrent of pee onto the chair, absolutely soaking it and yourself as the pee drips down and pools onto the floor.")
            ENDIF
        ELSE
            ;dressed
            SIF CAN_ACCIDENT(MASTER,2)
            CALL PRINT_GP("L", @"You can feel your %PeeProtectType(MASTER,2)% bulging out as a mushy mess enters your %PeeProtectType(MASTER,2)%, the smell filling the air of both yours and %CALLNAME:102%'s noses.")
            SIF CAN_ACCIDENT(MASTER,1)
            CALL PRINT_GP("L", @"\@ CAN_ACCIDENT(MASTER, 1) ? At the same time, you can feel your # You can feel your \@ bladder involuntarily releasing a torrent of pee into your %PeeProtectType(MASTER,2)%, absolutely soaking it.")
        ENDIF
        CALL PRINT_GPC("L", @"「Perrrfect. Sounds like you really \@ EQUIP:MASTER:6 ? soiled that %PeeProtectType(MASTER,1)% well # had to go\@.」", 102)
        CALL OMORASHI_END(MASTER, CAN_ACCIDENT(MASTER,1), CAN_ACCIDENT(MASTER,2))
    CASE 1
        CALL PRINT_GP("L", @"You decide to keep holding on desperately.")
        CALL PRINT_GPC("L", @"「Okay, but don't cry to me when you end up with a \@ CAN_ACCIDENT(MASTER,2) ? log # pool\@ \@ EQUIP:MASTER:6 ? in your %PeeProtectType(MASTER,2)% # on the floor\@」", 102)
    ENDSELECT
CASE 2
    CALL PRINT_GP("L", @"%CALLNAME:102% grabs a bowl of water and places it on a stand next the chair")
    CALL PRINT_GP("L", @"%HE_SHE(102,1)% then moves your bound hands into the bowl of water.")
    CALL PRINT_GPC("W", @"「Don't move your hands okay. It's calming water.」", 102)
    PRINTL
    CALL PRINT_GP("W", @"You feel like you need to go more urgently with your hand in the water.")
    CALLF PEEME(MASTER,PeeBarStage(MASTER)+1)
    CALLF POOME(MASTER,PooBarStage(MASTER)+1)
CASE 3
    CALL PRINT_GP("L", @"%CALLNAME:102% grabs a wand vibrator and places it against your crotch.")
    CALL PRINT_GPC("W", @"「Did you know that placing vibrators against genitals makes them have to pee?」", 102)
    CALL PRINT_GP("L", @"%CALLNAME:102% smiles naughtily.")
    CALL PRINT_GPC("W", @"「I hope it feels good on you.」", 102)
    PRINTL
    CALL PRINT_GP("L", @"%HE_SHE(102)% turns on the vibrator and lets the vibrations hit your crotch.")
    CALL PRINT_GP("L", @"It feels good, unsurprisingly. You do in fact have to pee a bit more urgently though.")
    CALL PRINT_GP("W", @"%HE_SHE(102)% then increases the power of the vibrator gently, causing it to feel a bit more pleasurable over time.")
    PRINTL
    CALL PRINT_GP("L", @"Then, you suddenly become heated and realize that you're on the edge of orgasm.")
    CALL PRINT_GP("L", @"You try to block the feeling away, but it feels too good to resist.")
    CALL PRINT_GP("L", @"Without being able to fully pull yourself away from the vibrator, you orgasm.")
    CALL PRINT_GP("W", @"Your back curves as you moan in orgasm, the massager now overstimulating your orgasming body.")
    SIF HAS_PENIS(MASTER)
    CALLF InstantCummies(MASTER)
    PALAM:MASTER:0 += PleasureRequired(MASTER)
    CALL EVENT_SHOOT
    PRINTL
    CALL PRINT_GP("L", @"As you breath in the afterglow, %CALLNAME:102% turns off the vibrator and pulls it away from you.")
    CALL PRINT_GPC("L", @"「It seemed like you enjoyed it a lot, but having a vibrator making you cum during your timeout isn't really a punishment per say.」", 102)
    CALL PRINT_GPC("L", @"「Some say its a reward even...」", 102)
    CALL PRINT_GPC("L", @"「But as long as you keep making presents, its all fun and games❤」", 102)
    ;CALL PRINT_GPC("W", @"「But its all in the name of making gifts for me after all❤」", 102)
    CALL PRINT_GP("W", @"Putting away the vibrator, %HE_SHE(102)% walks away from you.")
CASE 4
    ;free
    CALL PRINT_GPC("L", @"「I had an idea on what I could do to you, but I forgot.」", 102)
    CALL PRINT_GPC("L", @"「Instead of letting you suffer, I'm just going to let you take a break. Take a rest and get comfortable.」", 102)
    SIF K102_ABDLMode() == 2
    CALL PRINT_GPC("L", @"「And make sure to use your potty. There's no need to hold it in.」", 102)
ENDSELECT

@K102_Punishment_PottyBondage_ReleasePlayer
CALL PRINT_GP("W", @"%CALLNAME:102% lowers down to %HIS_HER(102)% legs and unties your legs from the spreader bar.")
CALL PRINT_GP("W", @"Grabbing the spreader bar, she moves it aside and moves up to you.")

IF SoiledDiaper(MASTER)
    IF IsPadded(MASTER, "diaper")
        CALL PRINT_GP("W", @"%CALLNAME:102% gently holds the back of your diaper on one end and begins to remove the tapes one by one until all of them are loose.")
        CALL PRINT_GP("W", @"The diaper is then moved down to the floor and lifted away from you.")
    ELSEIF EQUIP:MASTER:6
        CALL PRINT_GP("W", @"%CALLNAME:102% gently holds both sides of your %PeeProtectType(MASTER,2)% and pulls them down, letting you lift up your legs.")
        CALL PRINT_GP("W", @"Afterwards, %HE_SHE(102)% moves your %PeeProtectType(MASTER,2)% to the side.")
    ENDIF
    CALL PRINT_GPC("L", @"「And these will go to good use.」", 102)
    CALL PRINT_GP("W", @"%HE_SHE(102,1)% wipes your crotch down with some wet wipes, making sure you're clean.")
    IF (CAN_ACCIDENT(MASTER,1) || CAN_ACCIDENT(MASTER,2)) && nOmutsu
        PRINTL
        CALL PRINT_GP("W", @"%HE_SHE(102,1)% pulls out a thick printed diaper and opens it, lifting it against your crotch.")
        CALL PRINT_GP("W", @"Using your body as support, %HE_SHE(102)% tapes the diaper to the front, making sure its wrapped tight.")
        CALL PRINT_GP("W", @"Giving your diaper a slight tug to verify a good fit, she begins to move back.")
        CALL PRINT_GPC("L", @"「And a new diapie for you, since the big baby couldn't hold %HIMSELF_HERSELF(MASTER)% during %HIS_HER(102)% timeout.」", 102)
        CALL LUCID_EV27_DIAPERCHANGE(MASTER)
    ENDIF
ENDIF
PRINTL
CALL PRINT_GP("W", @"%CALLNAME:102% moves to your back and begins to cut the rope binding your arms.")
CALL PRINT_GPC("L", @"「Stay still for me.」", 102)
CALL PRINT_GP("W", @"You feel your arms adjust back to the sides of your body as %HE_SHE(102)% cuts the rope.")
CALL PRINT_GPC("L", @"「I hope you learned your lesson.」", 102)
CALL PRINT_GP("W", @"The chair and the restraints on the floor disappear, leaving you free, almost like it never happened.")

CALLF ChangeRestraint(MASTER,"RestrainArm",0)
CALLF ChangeRestraint(MASTER,"RestrainLeg",0)
CALL K102_ClearPunishment