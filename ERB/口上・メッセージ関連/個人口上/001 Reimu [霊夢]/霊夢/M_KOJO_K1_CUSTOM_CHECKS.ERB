;custom code
@M_KOJO_CHECK_K1_PANTIES(ARGS,ARG,ARG:1)
CUSTOM_PANTY_EXISTS:0 = 1
CUSTOM_PANTY_NAME:0 = ofuda
CUSTOM_PANTY_DESCRIPTION:0 = that sticks neatly to her pussy
CUSTOM_PANTY_DESCRIPTION_ORDER:0 = 1
SETBIT CUSTOM_PANTY_OPTIONS:0, 0 ;only covers front
SETBIT CUSTOM_PANTY_OPTIONS:0, 3 ;acts as pasties
SETBIT CUSTOM_PANTY_OPTIONS:0, 11 ;nothing on back
CUSTOM_PANTY_MATCHING_BRA:0 = 25 ;reimu custom bra #1, ofuda
IF IS_FEMALE(TARGET) && (ABL:露出癖 >= 10 || (ABL:露出癖 >= 5 && !RAND:3)) ;worn more frequently with high exposure
    CUSTOM_PANTY_DAILY_JUDGMENT:0 = 1
ELSEIF ABL:露出癖 < 5
    CUSTOM_PANTY_COLLECTION_UNLOCK_TEXT:0 = 解禁：露出癖5
ENDIF
CUSTOM_PANTY_COLLECTION_DISPLAY:0 = 1

CUSTOM_BRA_EXISTS:0 = 1
CUSTOM_BRA_NAME:0 = ofuda
SIF (ABL:露出癖 >= 10 || (ABL:露出癖 >= 5 && !RAND:3)) ;worn more frequently with high exposure
    CUSTOM_BRA_DAILY_JUDGMENT:0 = 1
CUSTOM_BRA_FABRIC_THICKNESS:0 = 0

CUSTOM_PANTY_EXISTS:1 = 1
CUSTOM_PANTY_NAME:1 = crimson slasher panties
CUSTOM_PANTY_DESCRIPTION:1 = youkai-banishing
;covers back and front
SETBIT CUSTOM_PANTY_OPTIONS:1, 0
SETBIT CUSTOM_PANTY_OPTIONS:1, 1
SETBIT CUSTOM_PANTY_OPTIONS:1, 2 ;shiftable
SETBIT CUSTOM_PANTY_OPTIONS:1, 12 ;tied with strings

CUSTOM_PANTY_MATCHING_BRA:1 = 2 ;worn with sarashi
SIF ABL:戦闘能力 >= 6 ;requires maxed combat (generally gained on first defeat)
    CUSTOM_PANTY_DAILY_JUDGMENT:1 = !RAND:3 ;rarely worn

;becomes known to the player if love/lust or requirement met
IF TALENT:恋慕 || TALENT:愛欲 || ABL:戦闘能力 >= 6
    CUSTOM_PANTY_COLLECTION_DISPLAY:1 = 1
    SIF ABL:戦闘能力 < 6
        CUSTOM_PANTY_COLLECTION_UNLOCK_TEXT:1 = Req: Reimu Ex Combat
ENDIF

RETURN 1

;determines if a specific event/command has timestop dialogue
;overrides CFLAG:時間停止口上有
@M_KOJO_K1_TIMESTOP_ALLOWED(ARGS, ARG, ARGS:1)
SELECTCASE ARGS
    CASE "COM"
        SIF GROUPMATCH(ARG, 43)
            RETURN -1
    CASE "SCOM"
    CASE "CUSTOM_COM"
    CASE "CUSTOM_SCOM"
    CASE "EVENT"
    CASE "PALAM"
    CASE "COUNTER"
    CASE "COM_ASSISTANT"
    CASE "SCOM_ASSISTANT"
    CASE "CUSTOM_COM_ASSISTANT"
    CASE "CUSTOM_SCOM_ASSISTANT"
ENDSELECT
RETURN 0
