;※※※※※※※※※※※※※※※※※※※※※※※※※
;日常系コマンド
;※※※※※※※※※※※※※※※※※※※※※※※※※
;==================================================
;300,会話
;==================================================
@M_KOJO_MESSAGE_COM_K50_300
;コマンド共通メッセージ(TRAIN_MESSAGE) 
;削除すると「～話をした。」とかが消えます
;口上メイン呼出
CALL M_KOJO_MESSAGE_COM_K50_300_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_300_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	IF TFLAG:193 == -3 ;custom code, hate mark 3
		RETURN
	ENDIF
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;マイナスになってもいいさと考えるんだ
	;IF !CFLAG:1502 && CFLAG:信頼度 >= 500 && ABL:教養 > 2 && MONEY >= 300000
	;IF !CFLAG:1502 && CFLAG:信頼度 >= 500 && ABL:教養 > 2
	;	TRYCALLFORM M_KOJO_MESSAGE_COM_K50_進学
	;	RETURN 1
	;ENDIF
	
	IF 家庭菜園可(CFLAG:MASTER:現在位置) && !GETBIT (TCVAR:350,1)
		SETBIT TCVAR:350,1
		;SELECTCASE PLANT_TYPE_0
		SELECTCASE PLANT_TYPE
			CASE 151
				PRINTFORMW 「Ah, cabbage farm.」
				PRINTFORML 「Planting so many things in the garden%UNICODE(0x2661)%」
				PRINTFORMW 「Onii-sama, are you preparing for a baby?」
				IF CFLAG:生解禁 <= 2 && TALENT:恋人
					IF CFLAG:16:信頼度 < 500
						PRINTFORML 「But Onee-sama says we can't.」
					ELSE
						PRINTFORML 
						PRINTFORML 「Okay,」
						PRINTFORMW 「because %CALLNAME:TARGET% also want Onii-sama's baby.」
						PRINTFORMW 「Ufufu! Come on, Onii-sama! Try to catch %CALLNAME:TARGET% in the cabbage field!」
						CFLAG:生解禁 = 2
					ENDIF
				ELSE
					PRINTDATAL
						DATALIST
							DATAFORM 「It's a bit too early for %CALLNAME:TARGET%, maybe.」
							;ばっちゃに語呂を合わせぱっちぇ呼称
							DATAFORM 「Patchie says it's impossible before %CALLNAME:TARGET% has her sekihan(red rice, celebration for a girl's first period).」
						ENDLIST
						DATALIST
							DATAFORM 「Onii-sama, not yet.」
							DATAFORM 「No doing it by force, okay? Ufufu♪」
						ENDLIST
						DATALIST
							DATAFORM 「You want to, don't you? O-ni-i-sa-ma?」
							DATAFORM 「%CALLNAME:TARGET% can see it in your eyes, after all.」
							DATAFORM 「Ufufu, Nooooot yet.%UNICODE(0x2661)%」
						ENDLIST
					ENDDATA
				ENDIF
			CASE 163
				PRINTFORML 「Hmmm, Was Onee-sama green when she was young too?」
			CASE 164
				PRINTFORML 「Watermelon-san～ Watermelon-san～ Grow biiiig and strong, okay? Watermelon-san～」
				PRINTFORML 「Let's eat watermelon with everyone!」
				PRINTFORMDW %CALLNAME:TARGET% laughs.
			CASE 177
				PRINTFORML 「Let's make wine with these grapes.」
				PRINTFORML 「%CALLNAME:TARGET% wants to do the stomping, okay?」
				PRINTFORML 「Ufufu, It's so fun♪」
				PRINTFORMD %CALLNAME:TARGET% holds up her skirt slightly, 
				
				IF TALENT:MASTER:マゾ
					PRINTFORMDL and as she does so, her eyes drift towards your crotch, her tiny feet stomping about.
				ELSE
					PRINTFORMDL stomping about on a pantomime grape treading spree.
				ENDIF
			CASE 187
				PRINTFORML 「Yuri～♪ Yuriyuri～♪」 (Yuri is the Japanese word for lily)
				PRINTFORML 「Onii-sama, did you know, did you know? You can eat the root of the lily!」
				SIF TALENT:MASTER:性別 == 3
					PRINTFORMDL %CALLNAME:TARGET% looks at your crotch somewhat weirdly.
			CASE 188
				IF TCVAR:媚薬
					PRINTFORML 「Onii-sama, did you know did you know,」
					PRINTFORML 「if you squeeze a lemon on a carrot, it turns into an ashprodeeshiak. But what is an ashprodeeshiak anyway?」
				ELSE
					PRINTFORML 「Carrot-san♪ Raddish-san♪ Shiitake-san♪ Lappa-san♪ Lotus root-san who is full of holes♪」
					PRINTFORML 「And bittercur-san who is full of vines♪」
				ENDIF
		ENDSELECT
	ENDIF
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	
	IF TFLAG:193 == 1
		PRINTFORMDW %CALLNAME:TARGET% nods her head in agreement as you speak.
		RETURN 1
	ELSEIF TFLAG:193 == -1
		PRINTFORMDW %CALLNAME:TARGET% shakes her head a little at your words.
		RETURN 1
	ENDIF
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;ここに口上書いて下さい

ENDIF
RETURN 1
;==================================================
;301,お茶を淹れる
;==================================================
@M_KOJO_MESSAGE_COM_K50_301
CALL M_KOJO_MESSAGE_COM_K50_301_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_301_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	
	IF TFLAG:193 == -1
		PRINTFORMW 「Oww!」
		PRINTFORMDW It seems like it was a bit too hot for %CALLNAME:TARGET%.
		PRINTFORMW 「%CALLNAME:TARGET%'s tongue hurts...」
		RETURN 1
	ENDIF
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;302,スキンシップ
;==================================================
@M_KOJO_MESSAGE_COM_K50_302
CALL M_KOJO_MESSAGE_COM_K50_302_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_302_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	SELECTCASE CFLAG:300
		CASE 13,24,39
			PRINTFORML 「Huggie huggie huggie in the bath♪」
			PRINTFORML 「Hum hum hum huuuum♪」
	ENDSELECT
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:10
			PRINTFORMDW %CALLNAME:TARGET% takes your hand and spins in a graceful circle.
			PRINTFORML 「Onii-sama!」
			PRINTFORMDW %CALLNAME:TARGET%'s intense stare takes you by surprise.
			PRINTFORMDW Before you realize, %CALLNAME:TARGET%'s arms are already wrapped around your waist...
			PRINTFORML 「Fufu～」
			PRINTFORMDW %CALLNAME:TARGET%'s graceful and elegant movement makes your heart skip a beat.
			PRINTFORML 
			PRINTFORML 「Onee-sama always plays the lady in our dances, and %CALLNAME:TARGET% always plays the gentleman.」
			PRINTFORMW 「It's unfair that Onee-sama always get to play the lady.」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;303,謝る
;==================================================
@M_KOJO_MESSAGE_COM_K50_303
CALL M_KOJO_MESSAGE_COM_K50_303_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_303_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	IF TALENT:TARGET:2 & 2
		PRINTFORMW 「Eh? Did you just say you'll do anything?」
		PRINTFORML 「Onii-sama?」
		PRINTFORML 「You just said you'll do anything, right? Ufufu%UNICODE(0x2661)%」
		CFLAG:MASTER:うふふ = 2
		CFLAG:うふふ = 2
		RETURN 1
	ENDIF
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;304,仕事を手伝う
;==================================================
@M_KOJO_MESSAGE_COM_K50_304
CALL M_KOJO_MESSAGE_COM_K50_304_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_304_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;ちんこついてる
	PRINTFORML 「Ah, This cat is so cute!」
	IF TALENT:TARGET:2 & 2
		PRINTFORMW 「But %CALLNAME:TARGET% wants to %SPLIT_G("pet:give some milk to")% Onii-sama's『cat』too♪」
		PRINTFORMDW She backs you up against the corner of the room and pushes you down.
		CFLAG:MASTER:うふふ = 2
		CFLAG:うふふ = 2
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;305,膝枕してもらう
;==================================================
@M_KOJO_MESSAGE_COM_K50_305
CALL M_KOJO_MESSAGE_COM_K50_305_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_305_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;306,お腹を撫でる
;==================================================
@M_KOJO_MESSAGE_COM_K50_306
CALL M_KOJO_MESSAGE_COM_K50_306_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_306_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;307,頬をつまむ
;==================================================
@M_KOJO_MESSAGE_COM_K50_307
CALL M_KOJO_MESSAGE_COM_K50_307_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_307_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;308,頬をつつく
;==================================================
@M_KOJO_MESSAGE_COM_K50_308
CALL M_KOJO_MESSAGE_COM_K50_308_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_308_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	;IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	PRINTFORML 「Funyuuu!」
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	;ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
ENDIF
RETURN 1
;==================================================
;309,頭を撫でる
;==================================================
@M_KOJO_MESSAGE_COM_K50_309
CALL M_KOJO_MESSAGE_COM_K50_309_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_309_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	IF FIRSTTIME(SELECTCOM)
		PRINTFORML 「Onii-sama! Flan is a lady, so don't treat Flan like a kid, okay?!」
		PRINTFORMDW Flan gets mad at you...
	;恋慕 後期
	ELSEIF TALENT:恋慕
		IF !RAND:3
			PRINTFORML 「Hauuuuu...」
			PRINTFORMDW %CALLNAME:TARGET%'s wings twitches slightly...
		ELSEIF !RAND:2
			PRINTFORML 「Onii-sama...」
		ELSE
			PRINTFORMW 「%CALLNAME:TARGET% got pet by Onii-sama...」
			PRINTFORMW 「against her will...」
			PRINTFORMW 「You meanieee……%UNICODE(0x2661)%」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			PRINTFORML 「Stop petting %CALLNAME:TARGET%'s head all of a sudden!」
		ELSEIF !RAND:2
			PRINTFORML 「Onii-sama...」
			PRINTFORML 「You can't... You shouldn't...」
			PRINTFORMDW %CALLNAME:TARGET% starts crying...
		ELSE
			PRINTFORML 「Onii-sama, no headpats for you...」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			PRINTFORML 「Hiyaaa!」
			PRINTFORMDW %CALLNAME:TARGET% gracefully blocks your hand, striking a charismatic pose.
		ELSEIF !RAND:2
			PRINTFORML 「Don't... Tarako is going to come now...」
		ELSE
			PRINTFORML 「No... Don't...」
		ENDIF
		RETURN 1
	ENDIF
ENDIF
RETURN 1
;==================================================
;310,尻を撫でる
;==================================================
@M_KOJO_MESSAGE_COM_K50_310
CALL M_KOJO_MESSAGE_COM_K50_310_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_310_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;311,抱き付く
;==================================================
@M_KOJO_MESSAGE_COM_K50_311
CALL M_KOJO_MESSAGE_COM_K50_311_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_311_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	PRINTFORML 「Heheheー♪ Huggie huggieー♪」
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;312,キスする
;==================================================
@M_KOJO_MESSAGE_COM_K50_312
CALL M_KOJO_MESSAGE_COM_K50_312_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_312_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	IF FIRSTTIME()
		PRINTFORML 「Onii-sama.」
		PRINTFORMDW %CALLNAME:TARGET% looks at you with a serious expression, taking you aback.
		PRINTFORMW 「If you kiss someone, you might make a baby.」
		PRINTFORMW 「You shouldn't do that so lightly, okay?」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			PRINTFORML 「Onii-sama, %CALLNAME:TARGET%'s weewee is asking for a kiss%UNICODE(0x2661)%.」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;妊娠中
	IF TALENT:妊娠
	;	PRINTFORML 「」
		RETURN 1
	ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			PRINTFORML 「Fuwaaa...%UNICODE(0x2661)%」
			PRINTFORMW 「Onii-sama... %CALLNAME:TARGET% might have a baby now...%UNICODE(0x2661)%」
		;ELSEIF !RAND:2
		ELSE
			PRINTFORMW 「%CALLNAME:TARGET% will become %CALLNAME:TARGET% mama now...%UNICODE(0x2661)%」
		ENDIF
		RETURN 1
	;思慕 中期
	;ELSEIF TALENT:思慕
	;	IF !RAND:3
	;		;PRINTFORML 「」
	;	ELSEIF !RAND:2
	;		;PRINTFORML 「」
	;	ELSE
	;		;PRINTFORML 「」
	;	ENDIF
	;	RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			PRINTFORML 「Onii-samaaa... No kisses for you...」
		ELSEIF !RAND:2
			PRINTFORMW 「%CALLNAME:TARGET% will become %CALLNAME:TARGET% mama now... Hic Hic...」
		ELSE
			PRINTFORMW 「We'll have a baby... we'll have a baby because of the kiss... Hic Hic...」
		ENDIF
		RETURN 1
	ENDIF
ENDIF
RETURN 1
;==================================================
;313,胸愛撫
;==================================================
@M_KOJO_MESSAGE_COM_K50_313
CALL M_KOJO_MESSAGE_COM_K50_313_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_313_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	IF TIME > 360 && TIME > 1020
		PRINTFORML 「Onii-sama. The sun will see us doing it. It's not good, okay?」
		PRINTFORMDW %CALLNAME:TARGET% says that you should only do perverted things at night.
		PRINTFORML 
		PRINTFORML 「Fuweeee, you can't...」
		PRINTFORML 「You can't...」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;314,アナル愛撫
;==================================================
@M_KOJO_MESSAGE_COM_K50_314
;CALL M_KOJO_MESSAGE_COM_K50_314_1
CALL M_KOJO_MESSAGE_COM_K50_313_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_314_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;315,クリ愛撫
;==================================================
@M_KOJO_MESSAGE_COM_K50_315
;CALL M_KOJO_MESSAGE_COM_K50_315_1
CALL M_KOJO_MESSAGE_COM_K50_313_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_315_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;316,指挿入れ
;==================================================
@M_KOJO_MESSAGE_COM_K50_316
;CALL M_KOJO_MESSAGE_COM_K50_316_1
CALL M_KOJO_MESSAGE_COM_K50_313_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_316_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;317,Ｖ腰を振る
;==================================================
@M_KOJO_MESSAGE_COM_K50_317
CALL M_KOJO_MESSAGE_COM_K50_317_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_317_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;318,Ａ腰を振る
;==================================================
;@M_KOJO_MESSAGE_COM_K50_318
;CALL TRAIN_MESSAGE
;CALL M_KOJO_MESSAGE_COM_K50_318_1
;RETURN RESULT

;@M_KOJO_MESSAGE_COM_K50_318_1
;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
;LOCAL = 0
;-------------------------------------------------
;IF LOCAL

;ENDIF
;RETURN 1
;==================================================
;330,スカートめくり
;==================================================
@M_KOJO_MESSAGE_COM_K50_330
CALL M_KOJO_MESSAGE_COM_K50_330_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_330_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	IF FIRSTTIME(SELECTCOM)
	;幼女のマジ泣き。可愛げを出さないように
		PRINTFORMW 「Eh...」
		PRINTFORMDW %CALLNAME:TARGET%'s expression starts crumbling bit by bit as tears well up in her eyes...
		PRINTFORMW 「Uuuu... Eeeeehhhhh...」
		PRINTFORML 「Sob... Ueeeehhhhーー!」
		PRINTFORMW 「Onii-sama... sob... you... sob... big... meanie... uwaaaaaaーー!!」
		PRINTFORMW 「Aaaaaaaーー!! Ahhhhhhーーー!!!」
	;犯される生娘のように真剣に
	ELSE
		PRINTFORMW 「Stop!」
		PRINTFORMW 「Onii-sama stop!」
		PRINTFORMW 「Stop Onii-sama stoooop!」
		PRINTFORMW 「Stop it...please...」
		PRINTFORMDW Large teardrops flow down %CALLNAME:TARGET%'s cheek...
		RETURN 1
	ENDIF
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;331,お茶を淹れる（薬入り）
;==================================================
@M_KOJO_MESSAGE_COM_K50_331
CALL M_KOJO_MESSAGE_COM_K50_331_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_331_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	IF FIRSTTIME(SELECTCOM)
		PRINTFORML 「This tastes just like %CALLNAME:15%'s tea.」
		PRINTFORML 「You did a good job, Onii-sama.」
		PRINTFORML 「Good %PRINT_MALE("boy", MASTER)%, good %PRINT_MALE("boy", MASTER)%.」
	ENDIF
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;350,押し倒す（時姦時イタズラする
;TFLAG:194 (0=成功 1=人目がある　2=野外は嫌でござりまする　3=多人数プレイは勘弁　4=合意があってもムードが足りぬ）
;TCVAR:抱き寄せ初回　1=普通に合意ゲット　2=先にキスぐらいしろよ　3=媚薬or発情には勝てなかったよお…　4=悔しい…刻印さえなければﾋﾞｸﾋﾞｸﾝ
;5=強引に迫られると断れぬ(素質臆病持ち専用) 6=まんざらでもない　7=駄目　8=論外
;==================================================
@M_KOJO_MESSAGE_COM_K50_350
CALL M_KOJO_MESSAGE_COM_K50_350_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_350_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	SIF TFLAG:194 || TFLAG:押し倒せない  ;custom code - failure check
		RETURN 1
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;351,連れ出す
;==================================================
@M_KOJO_MESSAGE_COM_K50_351
;連れ出しにはTRAIN_MESSAGEが無い
CALL M_KOJO_MESSAGE_COM_K50_351_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_351_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	IF FIRSTTIME()
		PRINTFORML 「What's wrong, Onii-sama?」
		PRINTFORML 「...could it be...」
		PRINTFORML 「You're scared of going to the toilet alone?」
	ENDIF
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;352,告白する
;==================================================
@M_KOJO_MESSAGE_COM_K50_352
CALL M_KOJO_MESSAGE_COM_K50_352_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_352_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;353,イタズラする
;==================================================
@M_KOJO_MESSAGE_COM_K50_353
CALL M_KOJO_MESSAGE_COM_K50_353_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_353_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;354,添い寝する（相手は起きて布団に招く感じ）開始側
;==================================================
@M_KOJO_MESSAGE_COM_K50_354
CALL M_KOJO_MESSAGE_COM_K50_354_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_354_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;355,時止め（地の文の改変程度に留めるべき
;==================================================
@M_KOJO_MESSAGE_COM_K50_355
CALL M_KOJO_MESSAGE_COM_K50_355_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_355_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;360,身を任せる
;==================================================
@M_KOJO_MESSAGE_COM_K50_360
CALL M_KOJO_MESSAGE_COM_K50_360_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_360_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;363,読み聞かせ（[幼稚]持ち）
;==================================================
@M_KOJO_MESSAGE_COM_K50_363
CALL M_KOJO_MESSAGE_COM_K50_363_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_363_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;364,豆まき(萃香、勇儀、スカーレット姉妹用
;==================================================
@M_KOJO_MESSAGE_COM_K50_364
CALL M_KOJO_MESSAGE_COM_K50_364_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_364_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;365,守矢神社へ移動(早苗、神奈子、諏訪子用
;==================================================
@M_KOJO_MESSAGE_COM_K50_365
CALL M_KOJO_MESSAGE_COM_K50_365_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_365_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;402,就寝（添い寝時）
;==================================================
@M_KOJO_MESSAGE_COM_K50_402
;就寝にTRAIN_MESSAGEは無い
CALL M_KOJO_MESSAGE_COM_K50_402_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_402_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;403,休憩
;==================================================
@M_KOJO_MESSAGE_COM_K50_403
CALL M_KOJO_MESSAGE_COM_K50_403_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_403_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;405,出掛ける(同行時デートに出掛ける
;==================================================
@M_KOJO_MESSAGE_COM_K50_405
;出掛けるにTRAIN_MESSAGEは無い
CALL M_KOJO_MESSAGE_COM_K50_405_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_405_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;410,掃除
;==================================================
@M_KOJO_MESSAGE_COM_K50_410
CALL M_KOJO_MESSAGE_COM_K50_410_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_410_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;411,戦闘訓練
;==================================================
@M_KOJO_MESSAGE_COM_K50_411
CALL M_KOJO_MESSAGE_COM_K50_411_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_411_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;412,勉強
;==================================================
@M_KOJO_MESSAGE_COM_K50_412
CALL M_KOJO_MESSAGE_COM_K50_412_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_412_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;413,料理
;==================================================
@M_KOJO_MESSAGE_COM_K50_413
;料理にTRAIN_MESSAGEは無い
CALL M_KOJO_MESSAGE_COM_K50_413_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_413_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;初めて
	;記入チェック
	LOCAL:1 = 1
	;-------------------------------------------------
	IF LOCAL:1 && FIRSTTIME(SELECTCOM)
		;吸血鬼の性知識
		PRINTFORML 「Ow!」
		PRINTFORMDW While preparing the food, %CALLNAME:TARGET% accidentally cut her finger.
		PRINTFORML 
		PRINTFORMDW As you lick the blood away, %CALLNAME:TARGET% seemed to panic.
		PRINTFORML 「O-Onii-sama?」
		PRINTFORMW 「%CALLNAME:TARGET% is a vampire, so umm, blood sucking is like reproduction.」
		PRINTFORMW 「Oh no what should I do. %CALLNAME:TARGET% isn't even married yet, but Onii-sama already reproduced with %CALLNAME:TARGET%.」
		PRINTFORML 「Not even Onee-sama did this to %CALLNAME:TARGET% before...」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;414,食事
;==================================================
@M_KOJO_MESSAGE_COM_K50_414
CALL M_KOJO_MESSAGE_COM_K50_414_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_414_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	IF TASTE_REACTION("辛い")
		PRINTFORMDL %CALLNAME:TARGET% breaks out in cold sweat at the sight of the scarlet curry...
		PRINTFORMW 「U-umm, Onii-sama.」
		PRINTFORMW 「It looks really spicy.」
		PRINTFORMW 「This isn't tarako, right?」
		PRINTFORMDW You reassure her this isn't tarako as %CALLNAME:TARGET% shakily puts a spoonful into her mouth...
		PRINTFORML Nom!
		PRINTFORML 「Ahhh, spicy!!」
		;時間停止イラマチオのススメ (I recommend time stop irrumatio here)
		PRINTFORMDL %CALLNAME:TARGET% spits out her tongue, panting profusely...
		RETURN 1
	ENDIF
	
	IF TASTE_REACTION("わさび")
;		PRINTFORMDW You give the pregnant %CALLNAME:TARGET% something sour to sate her appetite...
		PRINTFORML 「Nnnnggg～～!」
		PRINTFORML 「Wasabi-san is hitting the inside of %CALLNAME:TARGET%'s nooooose!」
		RETURN 1
	ENDIF
	;妊娠中
	IF TALENT:妊娠
		IF TASTE_REACTION("すっぱい")
			PRINTFORMDW You give the pregnant %CALLNAME:TARGET% something sour to sate her appetite...
			PRINTFORML 「Did you put in %CALLNAME:TARGET%'s favorite flavoring?」
			PRINTFORML 「Ahh! It's so sour and delicious!♪」
			SIF TALENT:処女 == -1
				PRINTFORMDL Although she isn't aware of her pregnancy, her body naturally starts craving citrusy foods.
			PRINTFORML %CALLNAME:TARGET%'s face fills with happiness as she eats your homemade food...
		ENDIF
		RETURN 1
	ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;415,食事をふるまう
;==================================================
@M_KOJO_MESSAGE_COM_K50_415
CALL M_KOJO_MESSAGE_COM_K50_415_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_415_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	IF TASTE_REACTION("辛い")
		PRINTFORMDL %CALLNAME:TARGET% breaks out in cold sweat at the sight of the scarlet curry...
		PRINTFORMW 「U-umm, Onii-sama.」
		PRINTFORMW 「It looks really spicy.」
		PRINTFORMW 「This isn't tarako, right?」
		PRINTFORMDW You reassure her this isn't tarako as %CALLNAME:TARGET% shakily puts a spoonful into her mouth...
		PRINTFORML Nom!
		PRINTFORML 「Ahhh, spicy!!」
		;時間停止イラマチオのススメ (I recommend time stop irrumatio here)
		PRINTFORMDL %CALLNAME:TARGET% spits out her tongue, panting profusely...
		RETURN 1
	ENDIF
	
	IF TASTE_REACTION("わさび")
		IF TALENT:恋人
			PRINTFORML （This taste...）
			PRINTFORMDW %CALLNAME:TARGET%, getting inspiration from your flavoring choice, whispers into your ear...
			PRINTFORML 「Okay♪ When we're alone, let's be sweet%UNICODE(0x2661)% to each other lots and lots.」
		ELSE
			PRINTFORMW 「This is really sweet!」
		ENDIF
		RETURN 1
	ENDIF
	IF TASTE_REACTION("わさび")
;		PRINTFORMDW You give the pregnant %CALLNAME:TARGET% something sour to sate her appetite...
		PRINTFORML 「Nnnnggg～～!」
		PRINTFORML 「Wasabi-san is hitting the inside of %CALLNAME:TARGET%'s nooooose!」
		RETURN 1
	ENDIF
	;妊娠中
	IF TALENT:妊娠
		IF TASTE_REACTION("すっぱい")
			PRINTFORMDW You give the pregnant %CALLNAME:TARGET% something sour to sate her appetite...
			PRINTFORML 「Did you put %CALLNAME:TARGET%'s favorite flavoring?」
			PRINTFORML 「Ahh! It's so sour and delicious!♪」
			SIF TALENT:処女 == -1
				PRINTFORMDL Although she isn't aware of her pregnancy, her body naturally starts craving citrusy foods.
			PRINTFORML %CALLNAME:TARGET%'s face fills with happiness as she eats your homemade food...
		ENDIF
		RETURN 1
	ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;416,演奏する(凍結中？
;==================================================
@M_KOJO_MESSAGE_COM_K50_416
CALL M_KOJO_MESSAGE_COM_K50_416_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_416_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;417,昼寝をする
;==================================================
@M_KOJO_MESSAGE_COM_K50_417
CALL M_KOJO_MESSAGE_COM_K50_417_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_417_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;421,願掛けする
;==================================================
@M_KOJO_MESSAGE_COM_K50_421
CALL M_KOJO_MESSAGE_COM_K50_421_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_421_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;431,お風呂でくつろぐ
;==================================================
@M_KOJO_MESSAGE_COM_K50_431
CALL M_KOJO_MESSAGE_COM_K50_431_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_431_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	IF TALENT:機嫌 == -1
		;IF !RAND:3
			PRINTFORMDL %CALLNAME:TARGET% sinks into the bath, blowing bubbles underwater with her tiny mouth...
		;ELSEIF TALENT:機嫌 == -1 && !RAND:2
			;躾がなっていない事と幼い事は別物故に破棄
		;	;PRINTFORMDL %CALLNAME:TARGET%は湯船を掴み足をばたつかせている…
		;何もしない分岐
		;ELSE
		;ENDIF
		RETURN 1
	ENDIF
	
	IF !RAND:10
		PRINTFORML 「Onii-sama, Onii-sama! Look! It's a duck!」
		PRINTFORML 「Quack～!!」
		PRINTFORMDW %CALLNAME:TARGET% runs after the duck, quacking as she chases...
	ELSEIF !RAND:9
		PRINTFORMDW %CALLNAME:TARGET% rolls her towel into a ball and watches it sink, again and again...
		PRINTFORML 「Glub glub glub glub～!」
	ELSEIF !RAND:8
		PRINTFORMW 「Onii-sama Onii-sama! Did you know did you know? When %CALLNAME:TARGET% goes underwater,」
		PRINTFORML 「%CALLNAME:TARGET% multiplies!」
		PRINTFORMW 『%CALLNAME:TARGET% multiplies!』
		PRINTFORML 「When %CALLNAME:TARGET% washes her hair, there's someone to watch %CALLNAME:TARGET%'s back!」
		PRINTFORMW 『When %CALLNAME:TARGET% washes her hair, there's someone to watch %CALLNAME:TARGET%'s back!』
		PRINTFORML 「Cool, rightー?」
		PRINTFORMW 『Cool, rightー?』
	ELSEIF !RAND:7
		
	ELSE
	ENDIF
	
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;440,待つ
;==================================================
@M_KOJO_MESSAGE_COM_K50_440
CALL M_KOJO_MESSAGE_COM_K50_440_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_440_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	IF !RAND:3
		PRINTFORML 「Throw it in the pot, and boil itー♪ Is it cooked? Let's find outー」
		PRINTFORML 「Nom nom nom!」
		PRINTFORML 「It's not cooked yet.」
		PRINTFORMDW %CALLNAME:TARGET% sings a nursery rhyme as she sits beside you, bored.
	ELSEIF !RAND:2
		PRINTFORML 「Hey, Onii-sama?」
		PRINTFORMW 「...」
		PRINTFORML 
		PRINTFORMW 「You shouldn't let a lady get bored, okay?」
		PRINTFORML 「Nnnnmmph!」
	ELSE
		PRINTFORML 「Heeeey, heeeey, Onii-sama.」
		PRINTFORMW 「Let's play?」
		PRINTFORMW %CALLNAME:TARGET% starts tugging at your arm persistently.
	ENDIF
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;441,授業を受ける(慧音専用？
;==================================================
@M_KOJO_MESSAGE_COM_K50_441
CALL M_KOJO_MESSAGE_COM_K50_441_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_441_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	;IF CFLAG:うふふ == 2
	;	;ちんこついてる
	;	IF TALENT:TARGET:2 & 2
	;		PRINTFORML 「せんせーい！バナナが入りました～！」
	;		PRINTFORMDW %CALLNAME:TARGET%のバナナに貫かれながら%CALLNAME:MASTER%は獣のような声を上げている…
	;	;ちんこついてない
	;	ELSE
	;		PRINTFORML 「せんせーい！バナナはおやつに入りますか？」
	;		PRINTFORMDW %CALLNAME:TARGET%はここぞとばかりに質問した
	;	ENDIF
	;	RETURN 1
	;ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;442,伐採する
;==================================================
@M_KOJO_MESSAGE_COM_K50_442
CALL M_KOJO_MESSAGE_COM_K50_442_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_442_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1

;※※※※※※※※※※※※※※※※※※※※※※※※※
;デート系コマンド
;※※※※※※※※※※※※※※※※※※※※※※※※※
[SKIPSTART]
;現状未実装
;==================================================
;600,デート会話
;==================================================
@M_KOJO_MESSAGE_COM_K50_600
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_600_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_600_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1

;==================================================
;601,デートスキンシップ
;==================================================
@M_KOJO_MESSAGE_COM_K50_601
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_601_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_601_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1

;==================================================
;602,デートキス
;==================================================
@M_KOJO_MESSAGE_COM_K50_602
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_602_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_602_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
[SKIPEND]
;==================================================
;603,手を繋ぐ
;==================================================
@M_KOJO_MESSAGE_COM_K50_603
CALL M_KOJO_MESSAGE_COM_K50_603_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_603_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1

;==================================================
;604,散策する
;==================================================
@M_KOJO_MESSAGE_COM_K50_604
;散策するにはTRAIN_MESSAGEが無い
CALL M_KOJO_MESSAGE_COM_K50_604_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_604_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;605,寄り道する
;==================================================
@M_KOJO_MESSAGE_COM_K50_605
;寄り道するにはTRAIN_MESSAGEが無い
CALL M_KOJO_MESSAGE_COM_K50_605_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_605_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	IF !RAND:5
		PRINTFORMDW %CALLNAME:TARGET% pulls your sleeve while fidgeting uncomfortably, trying to tell you something.
		PRINTFORMDW %CALLNAME:TARGET% looks up at you with watery eyes...
		PRINTFORMW 「... I need to pee.」
	ELSEIF !RAND:4
	ELSEIF !RAND:3
		PRINTFORMDW Losing all sense of sanity, you drag %CALLNAME:TARGET% into an empty snack shop before shoving a long, black stick of wheat candy down her throat!
		MONEY:0 -= 20
	ELSEIF !RAND:2
		IF !RAND:2
			PRINTFORMDW You buy a stick of cotton candy for the good girl %CALLNAME:TARGET%.
			MONEY:0 -= 500
		ELSE
			PRINTFORMDW You buy a lollipop for the good girl %CALLNAME:TARGET%.
			MONEY:0 -= 500
		ENDIF
	ELSE
		PRINTFORMDW Shimacchau-ojisan watches the two of you with a loving gaze. (A hard-to-explain cultural joke)
	ENDIF
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1

;==================================================
;610,食事に誘う
;==================================================
@M_KOJO_MESSAGE_COM_K50_610
CALL M_KOJO_MESSAGE_COM_K50_610_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_610_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1

;==================================================
;613,お金稼ぎする
;==================================================
@M_KOJO_MESSAGE_COM_K50_613
CALL M_KOJO_MESSAGE_COM_K50_613_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_613_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;614,食材の買い出し
;==================================================
@M_KOJO_MESSAGE_COM_K50_614
CALL M_KOJO_MESSAGE_COM_K50_614_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_614_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;615,お弁当を食べる
;==================================================
@M_KOJO_MESSAGE_COM_K50_615
CALL M_KOJO_MESSAGE_COM_K50_615_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_615_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	IF !TALENT:処女 && !RAND:(ABL:料理技能 + 1)
		PRINTFORMW 「Onii-sama, Onii-sama, will %CALLNAME:TARGET% become a good bride?」
		PRINTFORMW 「%CALLNAME:TARGET% can't be a bride anymore? Even you if you say so, Onii-sama,」
		PRINTFORMW 「that's for Onee-sama to decide.」
	ELSE
	PRINTDATAL
		DATALIST
			DATAFORM 「O-bentooo o-bentooo what a happy dayー♪」
			DATAFORM 「Following after the turtle that I helpedー♪」 (Parody of the nursery rhyme Urashimatarou)
		ENDLIST
		DATALIST
			DATAFORM 「Onii-sama Onii-sama! Will %CALLNAME:TARGET% become a good bride? Will she?」
			DATAFORM 「%CALLNAME:TARGET% will, right? Yeah, Flan will try her best!」
		ENDLIST
		DATALIST
			DATAFORM 「Onii-sama, did you know? If you fry an egg without flipping, it's called sunny-side-up.」
			DATAFORM 「Flan doesn't like the sun. That's why it's a moon-side-up, okay?」
			DATAFORM Flan's hair bounces up and down as she skips.
		ENDLIST
	ENDDATA
	ENDIF
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;617,甘味処に誘う
;==================================================
@M_KOJO_MESSAGE_COM_K50_617
CALL M_KOJO_MESSAGE_COM_K50_617_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_617_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	PRINTFORML 「Onii-sama! That was delicious!」
	PRINTFORML 「It was worth the Charisma!」
	PRINTFORML 「%UNICODE(0x2661)%」
	PRINTFORMDW %CALLNAME:TARGET%'s wings flutter happily as she enjoys the deserts.
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;618,花屋
;==================================================
@M_KOJO_MESSAGE_COM_K50_618
CALL M_KOJO_MESSAGE_COM_K50_618_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_618_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	PRINTFORML 「綺麗」
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;620,鈴奈庵
;==================================================
@M_KOJO_MESSAGE_COM_K50_620
CALL M_KOJO_MESSAGE_COM_K50_620_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_620_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	IF TALENT:妊娠
		PRINTFORML 「Onii-sama. My favorite book is Tamapyo Club(kurabu). Lovey-dovey(rabu-rabu).」
	ELSE
		PRINTFORMDL %CALLNAME:TARGET% seems to be incredibly excited...
	ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;436,読書 custom code, edited
;==================================================
@M_KOJO_MESSAGE_COM_K50_436
CALL M_KOJO_MESSAGE_COM_K50_436_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_436_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	IF EQUIP:アクセサリ == 1
		PRINTFORMDL Reading while wearing glasses seems to have completely changed %CALLNAME:TARGET%'s aura,
		PRINTFORMDW her innocent gestures draws out the normally hidden elegant side of her.
		PRINTFORML 「...Is something the matter, Onii-sama?」
		IF TALENT:TARGET:2 & 2
			PRINTFORMDW %CALLNAME:TARGET% closes the bad girl's picture book...
		ELSEIF ABL:教養 < 3
			PRINTFORMDW %CALLNAME:TARGET% closes the good girl's picture book...
		ENDIF
	ELSE
		PRINTFORMDW You have no idea when %CALLNAME:TARGET% started wearing glasses.
		EQUIP:アクセサリ = 1
	ENDIF
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;698,部屋に入る
;==================================================
@M_KOJO_MESSAGE_COM_K50_698
CALL M_KOJO_MESSAGE_COM_K50_698_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_698_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;699,外に出る
;==================================================
@M_KOJO_MESSAGE_COM_K50_699
CALL M_KOJO_MESSAGE_COM_K50_699_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_699_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1

;※※※※※※※※※※※※※※※※※※※※※※※※※
;派生コマンド
;※※※※※※※※※※※※※※※※※※※※※※※※※
;==================================================
;60,耳かき
;==================================================
@M_KOJO_MESSAGE_SCOM_K50_60
CALL M_KOJO_MESSAGE_SCOM_K50_60_1
RETURN RESULT
@M_KOJO_MESSAGE_SCOM_K50_60_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;61,抱き枕
;==================================================
@M_KOJO_MESSAGE_SCOM_K50_61
CALL M_KOJO_MESSAGE_SCOM_K50_61_1
RETURN RESULT
@M_KOJO_MESSAGE_SCOM_K50_61_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		;IF !RAND:3
			;PRINTFORML 「」
		;ELSEIF !RAND:2
			;PRINTFORML 「」
		;ELSE
			;PRINTFORML 「」
		;ENDIF
		PRINTFORML 「Nnngyuuu...」
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;62,デコちゅー
;==================================================
@M_KOJO_MESSAGE_SCOM_K50_62
CALL M_KOJO_MESSAGE_SCOM_K50_62_1
RETURN RESULT
@M_KOJO_MESSAGE_SCOM_K50_62_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;63,もっとキスする
;==================================================
@M_KOJO_MESSAGE_SCOM_K50_63
CALL M_KOJO_MESSAGE_SCOM_K50_63_1
RETURN RESULT
@M_KOJO_MESSAGE_SCOM_K50_63_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	IF TALENT:妊娠
	;	PRINTFORML 「」
		RETURN 1
	ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	PRINTFORML （Wow, Onii-sama really wants to make %CALLNAME:TARGET% pregnant...）
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;80,手を引く
;==================================================
@M_KOJO_MESSAGE_SCOM_K50_80
CALL M_KOJO_MESSAGE_SCOM_K50_80_1
RETURN RESULT
@M_KOJO_MESSAGE_SCOM_K50_80_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:16:恋慕
		IF !RAND:3
			PRINTFORML 「%CALLNAME:TARGET% doesn't want to make Onee-sama sad...」
			PRINTFORMDW %CALLNAME:TARGET% tells you as she looks up at you with teary eyes.
		ELSEIF !RAND:2
			PRINTFORML 「No, not now, Onii-sama!」
		ELSE
			PRINTFORML 「Onii-sama, no...」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:16:思慕
		IF !RAND:3
			PRINTFORML 「Onee-sama will be jealous if she sees this.」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			PRINTFORML 「Wait! Onii-sama! Leaving %CALLNAME:TARGET% behind is against the law!」
		ELSEIF !RAND:2
			PRINTFORML 「Onii-sama! Wait! Wait!」
		ELSE
			PRINTFORML 「Don't pull so hard... Onii-sama...」
			PRINTFORMW 「Onii-sama, you'll be sued if you're caught...」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1