; Modular ERB: 個人口上/026 Yukari [紫]/紫/M_KOJO_K26_絶頂.ERB
;~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
;分岐作りに有用そうな変数などのメモ
;~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
;NOWEX:Ｃ絶頂 NOWEX:Ｖ絶頂 NOWEX:Ｂ絶頂 NOWEX:Ａ絶頂 NOWEX:Ｍ絶頂
;各部位の絶頂強度。まず0~3が割り当てられ、多重絶頂により最大16倍される。つまり単部位なら最大3、5重絶頂で最大48
;NOWEX:TotalEX
;多重絶頂補正を受ける前の各部位絶頂強度の合計値。1~15。このテンプレにおける分岐の基準。
;TEQUIP:50
;Ｖに誰の竿が入ってるか。TEQUIP:50 = MASTERであなたが挿入中となる。
;TEQUIP:51
;Ａに誰の竿が入ってるか。TEQUIP:51 = MASTERであなたが挿入中となる。
;NOWEX:射精
;射精量、最大3。たとえばIF NOWEX:MASTER:射精 > 1だと、あなたが大量射精以上した場合となる。
;SYNCED_ORGASM(TARGET)
;相手とあなたが同時に絶頂した場合真となる。絶頂の部位は問わない。
;-------------------------------------------------
;絶頂時の口上
;-------------------------------------------------

@M_KOJO_MESSAGE_PALAMCNG_B_K26
#DIM TotalEX
;-------------------------------------------------
;噴乳
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL && NOWEX:噴乳
	CALL M_KOJO_COLOR_K26
	IF FLAG:70
		PRINTFORMDW 유카리는 시간이 멈춘 채로 절정에 도달한다.
	ELSE
		CALL HPH_PRINT,@"「흐으응ㅎ프」","W"
		CALL HPH_PRINT,@"「내 가슴에서 나오는 중이야」","W"
	ENDIF
ENDIF
;-------------------------------------------------
;射精した
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL && NOWEX:射精
	PRINTFORMW 
ENDIF
;-------------------------------------------------
;放尿
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL && NOWEX:放尿
	PRINTFORMW 
ENDIF
;-------------------------------------------------
;絶頂
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
;合計絶頂数で分岐
;これだけではどの部位が絶頂したかは区別されないため、書き分けたければ任意で分岐を加筆されたし。
;-------------------------------------------------
;五重絶頂
;記入チェック（=0, 非表示、1, 表示）
LOCAL:1 = 1
;-------------------------------------------------
IF NOWEX:五重絶頂
	CALL M_KOJO_COLOR_K26
	IF FLAG:70
		PRINTFORMDW 유카리는 시간이 멈춘 채로 절정에 도달한다.
	ELSE
		IF RAND:3 == 0
			CALL HPH_PRINT,@"「후웅흐헵 느릿느릿한 후웅흐헵 조금은」","W"
			PRINTFORMDW 유카리는 거부할 수 없는 황홀경 속에서 형언할 수 없고 매혹적인 비명을 지르며 경련한다...
		ELSEIF RAND:2 == 0
			CALL HPH_PRINT,@"「우호오혼ㅎㅍ 슈 고오오ㅎㄷㅎㅍ」","W"
			PRINTFORMDW 유카리는 거부할 수 없는 황홀경 속에서 형언할 수 없고 매혹적인 비명을 지르며 경련한다...
		ELSE
			CALL HPH_PRINT,@"「흐으응ㅎㅍ 오오 눈후으은ㅎㅍ 나아하아안ㅎㅍ」","W"
			PRINTFORMDW 유카리는 거부할 수 없는 황홀경 속에서 형언할 수 없고 매혹적인 비명을 지르며 경련한다...
		ENDIF
	ENDIF
	RETURN 1
ENDIF
;-------------------------------------------------
;四重絶頂
;記入チェック（=0, 非表示、1, 表示）
LOCAL:1 = 1
;-------------------------------------------------
IF NOWEX:四重絶頂
	CALL M_KOJO_COLOR_K26
	;部位を指定せず簡易的に書く場合、[SKIPSTART]&[SKIPEND]のコメントアウトを外して、[SKIPSTART]とこのコメントの間に書いてください
	IF FLAG:70
		PRINTFORMDW 유카리는 시간이 멈춘 채로 절정에 도달한다.
	ELSEIF RAND:5 == 0
		CALL HPH_PRINT,@"「오오호응흐프 잉흐응흐프」","W"
	ELSEIF RAND:4 == 0
		CALL HPH_PRINT,@"「흐으읍 흐응흐으읍」","W"
	ELSEIF RAND:3 == 0
		CALL HPH_PRINT,@"「후우우웅」","W"
	ELSEIF RAND:2 == 0
		CALL HPH_PRINT,@"「아아앙흐햣ㅁ으음ㄴ하응햣」","W"
	ELSE
		CALL HPH_PRINT,@"「나의 황홀경」","W"
	ENDIF
ENDIF
;-------------------------------------------------
;三重絶頂
;記入チェック（=0, 非表示、1, 表示）
LOCAL:1 = 1
;-------------------------------------------------
IF NOWEX:三重絶頂
	CALL M_KOJO_COLOR_K26
	;部位を指定せず簡易的に書く場合、[SKIPSTART]&[SKIPEND]のコメントアウトを外して、[SKIPSTART]とこのコメントの間に書いてください
	IF FLAG:70
		PRINTFORMDW 유카리는 시간이 멈춘 채로 절정에 도달한다.
	ELSEIF RAND:5 == 0
		CALL HPH_PRINT,@"「흐으우호응흠ㅁㅇㅎㅍ 오오은ㅎㄴㅍ 오오응ㅎㅍ」","W"
	ELSEIF RAND:4 == 0
		CALL HPH_PRINT,@"「아아앙흐으응흐으응」","W"
	ELSEIF RAND:3 == 0
		CALL HPH_PRINT,@"「흠흠흐으음호오응흐음」","W"
	ELSEIF RAND:2 == 0
		CALL HPH_PRINT,@"「오옹흐프 오옹흐프 우웅흐프」","W"
	ELSE
		CALL HPH_PRINT,@"「음ㅁ흠흐으아앙ㅎㅍ」","W"
	ENDIF
ENDIF
;-------------------------------------------------
;二重絶頂
;記入チェック（=0, 非表示、1, 表示）
LOCAL:1 = 1
;-------------------------------------------------
IF NOWEX:二重絶頂
	CALL M_KOJO_COLOR_K26
	;部位を指定せず簡易的に書く場合、[SKIPSTART]&[SKIPEND]のコメントアウトを外して、[SKIPSTART]とこのコメントの間に書いてください
	IF FLAG:70
		PRINTFORMDW 유카리는 시간이 멈춘 채로 절정에 도달한다.
	ELSEIF RAND:5 == 0
		CALL HPH_PRINT,@"「흐으응후우, 안 되겠어」","W"
	ELSEIF RAND:4 == 0
		CALL HPH_PRINT,@"「노오온흐프 아하아하프」","W"
	ELSEIF RAND:3 == 0
		CALL HPH_PRINT,@"「오오오온ㅎㅍㅁㅇㅎㅍ」","W"
	ELSEIF RAND:2 == 0
		CALL HPH_PRINT,@"「나아응흐프 민흐프 우운흐프」","W"
	ELSE
		CALL HPH_PRINT,@"「우으응흐햣 감당할 수가 없어으윽흐햣」","W"
	ENDIF
ENDIF
IF NOWEX:Ｃ絶頂
	CALL M_KOJO_COLOR_K26
	;時姦中
	IF FLAG:70
		PRINTFORMDW 유카리는 시간이 멈춘 채로 절정에 도달한다.
	ELSEIF RAND:5 == 0
		CALL HPH_PRINT,@"「음응흐흑」","W"
	ELSEIF RAND:4 == 0
		CALL HPH_PRINT,@"「못 참겠어(HPH)」","W"
	ELSEIF RAND:3 == 0
		CALL HPH_PRINT,@"「하아안흐프」","W"
	ELSEIF RAND:2 == 0
		CALL HPH_PRINT,@"「너무 민감하네」","W"
	ELSE
		CALL HPH_PRINT,@"「노오호프 스톱 이트호프」","W"
	ENDIF
ENDIF
IF NOWEX:Ｖ絶頂
	IF FLAG:70
		PRINTFORMDW 유카리는 시간이 멈춘 채로 절정에 도달한다.
	ELSEIF TEQUIP:50 == MASTER && TEQUIP:MASTER:コンドーム == 0
		IF RAND:5 == 0
			CALL HPH_PRINT,@"「날 거친 좆이 너무 좋아」","W"
		ELSEIF RAND:4 == 0
			CALL HPH_PRINT,@"「흐으응, 날 위해 이걸 주세요」","W"
		ELSEIF RAND:3 == 0
			CALL HPH_PRINT,@"「으응흐흠 흐음 흐음 흐음」","W"
		ELSEIF RAND:2 == 0
			CALL HPH_PRINT,@"「날것의 섹스 HPH MnnhHPH 더 HPH」","W"
		ELSE
			CALL HPH_PRINT,@"「푸아하안HPH 응응HPH」","W"
			CALL HPH_PRINT,@"「...날것으로 하는 게 너무 굉장해...HPH」","W"
		ENDIF
	ELSEIF TEQUIP:50 == MASTER
		IF RAND:5 == 0
			CALL HPH_PRINT,@"「음... 더 이상은 안 돼」","W"
		ELSEIF RAND:4 == 0
			CALL HPH_PRINT,@"「NNMHPH MNNNNHNGHPH S-느려져요HPH」","W"
		ELSEIF RAND:3 == 0
			CALL HPH_PRINT,@"「NhoohnHPH OoonhnghHPH 더 많이 줘 dickHPH」","W"
		ELSEIF RAND:2 == 0
			CALL HPH_PRINT,@"「흐음 흐으음 음음」","W"
		ELSE
			CALL HPH_PRINT,@"「안흐프냐아앙ㅎ프하」","W"
		ENDIF
	ELSE
		IF RAND:5 == 0
			CALL HPH_PRINT,@"「바로 거기 HPH NmmnhHPHHPHHPH」","W"
		ELSEIF RAND:4 == 0
			CALL HPH_PRINT,@"「더 이상은 더 이상 더 이상」","W"
		ELSEIF RAND:3 == 0
			CALL HPH_PRINT,@"「흐으음... 오오옹... 그래, 음음...」","W"
		ELSEIF RAND:2 == 0
			CALL HPH_PRINT,@"「음... 아아... 음...」","W"
		ELSE
			CALL HPH_PRINT,@"「응흐흠 흐음흐음흐음」","W"
		ENDIF
	ENDIF
ENDIF
IF NOWEX:Ａ絶頂
	;時姦中
	IF FLAG:70
		PRINTFORMDW 유카리는 시간이 멈춘 채로 절정에 도달한다.
	ELSEIF RAND:5 == 0
		CALL HPH_PRINT,@"「흐읍」","W"
	ELSEIF RAND:4 == 0
		CALL HPH_PRINT,@"「내 엉덩이가 약점이라고 했잖아HPH」","W"
	ELSEIF RAND:3 == 0
		CALL HPH_PRINT,@"「내 항문은 방어력이 없어, 알잖아 HPH 부드럽게 다뤄줘 HPH」","W"
	ELSEIF RAND:2 == 0
		CALL HPH_PRINT,@"「음냐아아 여기 없네에에!히팍」","W"
	ELSE
		CALL HPH_PRINT,@"「더, 계속해봐!HPH 나 좀 더 놀려줘 HPH」","W"
	ENDIF
ENDIF
IF NOWEX:Ｂ絶頂
	;時姦中
	IF FLAG:70
		PRINTFORMDW 유카리는 시간이 멈춘 채로 절정에 도달한다.
	ELSEIF RAND:5 == 0
		CALL HPH_PRINT,@"「응흐흑 흐으윽」","W"
	ELSEIF RAND:4 == 0
		CALL HPH_PRINT,@"「흥, 내 가슴 괴롭지 마라 흥」","W"
	ELSEIF RAND:3 == 0
		CALL HPH_PRINT,@"「나의 보봇은 오고 있어잉ㅎㅍ」","W"
	ELSEIF RAND:2 == 0
		CALL HPH_PRINT,@"「흐응음 흐으으 나는 가슴에서 나올 거야」","W"
	ELSE
		CALL HPH_PRINT,@"「더 많은 HPH가 내 가슴을 더 망쳐줘 HPH」","W"
	ENDIF
ENDIF
IF NOWEX:Ｍ絶頂
	;時姦中
	IF FLAG:70
		PRINTFORMDW 유카리는 시간이 멈춘 채로 절정에 도달한다.
	ELSEIF RAND:3 == 0
		CALL HPH_PRINT,@"「므응흐헵」","W"
	ELSEIF RAND:2 == 0
		CALL HPH_PRINT,@"「흐읍, 입으로만 왔어」","W"
	ELSE
		CALL HPH_PRINT,@"「내 입을 더 탐해줘HPH」","W"
	ENDIF
ENDIF
;-------------------------------------------------
;奴隷の睡眠絶頂時の口上
;-------------------------------------------------

@M_KOJO_MESSAGE_PALAMCNG_B2_K26
;-------------------------------------------------
;睡眠噴乳
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL && NOWEX:噴乳
	PRINTFORMW 
ENDIF
;-------------------------------------------------
;睡眠射精した
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL && NOWEX:射精
	PRINTFORMW 
ENDIF
;-------------------------------------------------
;睡眠放尿
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL && NOWEX:放尿
	PRINTFORMW 
ENDIF
;-------------------------------------------------
;睡眠絶頂
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
;合計絶頂数で分岐
;これだけではどの部位が絶頂したかは区別されないため、書き分けたければ任意で分岐を加筆されたし。
IF LOCAL
	SELECTCASE NOWEX:TotalEX
		;強
		CASE 5 TO 15
			PRINTFORMW 
			;中
		CASE 2 TO 4
			PRINTFORMW 
			;弱
		CASE 1
			PRINTFORMW 
	ENDSELECT
ENDIF
;-------------------------------------------------
;あなたが射精したときに関する口上の表示
;一時的にTARGETを口上主に設定している
;-------------------------------------------------
;==================================================
;Optional: Determines if the standard description should be skipped.
;Return 0 if description and KOJO should be shown, 1 if description should be hidden and KOJO shown, 2 if description should be shown and KOJO hidden, and 3 if both description and KOJO should be hidden.
;==================================================

@M_KOJO_MESSAGE_PALAMCNG_MESSAGECHECK_A_K26_00
RETURN 0

@M_KOJO_MESSAGE_PALAMCNG_A_K26_00(ARG)
;-------------------------------------------------
;TARGETの手による射精で、射精対象が非TARGET
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	CALL M_KOJO_COLOR_K26
	IF RAND:3 == 0
		PRINTFORMW 「오호? 벌써 터지려고 하는 거야?」
		RETURN 1
	ELSEIF RAND:2 == 0
		CALL HPH_PRINT,@"「그래, 네 얼굴에 그 비참한 표정을 하고 쏴버려라... HPH」","W"
		RETURN 1
	ELSE
		CALL HPH_PRINT,@"「내 손가락이 정말 그렇게 멋지게 느껴졌을까」","W"
		RETURN 1
	ENDIF
ENDIF
RETURN 1
;==================================================
;Optional: Determines if the standard description should be skipped.
;Return 0 if description and KOJO should be shown, 1 if description should be hidden and KOJO shown, 2 if description should be shown and KOJO hidden, and 3 if both description and KOJO should be hidden.
;==================================================

@M_KOJO_MESSAGE_PALAMCNG_MESSAGECHECK_A_K26_0
RETURN 0

@M_KOJO_MESSAGE_PALAMCNG_A_K26_0(ARG)
;-------------------------------------------------
;コンドーム
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	CALL M_KOJO_COLOR_K26
	IF TALENT:MASTER:濃厚精液
		IF RAND:3 == 0
			CALL HPH_PRINT,@"「흐으응, 콘돔을 끼고 있어도 이렇게 많이 사정하다니... 흐읍」","W"
			RETURN 1
		ELSEIF RAND:2 == 0
			CALL HPH_PRINT,@"「마치 고무로 나를 임신시킬 수 있을 것만 같다」","W"
			RETURN 1
		ELSE
			CALL HPH_PRINT,@"「MnhHPH 당신이 고무를 그렇게 많은 뜨거운 정액으로 채우는 걸 보는 게 너무 흥분돼 HPH」","W"
			RETURN 1
		ENDIF
	ELSE
		IF RAND:3 == 0
			PRINTFORMW 「벌써 터질 거야?」
			RETURN 1
		ELSEIF RAND:2 == 0
			CALL HPH_PRINT,@"「정말 빨랐네, 고무 HPH를 쓰고 있긴 했지만.」","W"
			RETURN 1
		ELSE
			CALL HPH_PRINT,@"「음... 좀 빨랐지 않아?」","W"
			RETURN 1
		ENDIF
	ENDIF
ENDIF
RETURN 1
;==================================================
;Optional: Determines if the standard description should be skipped.
;Return 0 if description and KOJO should be shown, 1 if description should be hidden and KOJO shown, 2 if description should be shown and KOJO hidden, and 3 if both description and KOJO should be hidden.
;==================================================

@M_KOJO_MESSAGE_PALAMCNG_MESSAGECHECK_A_K26_23
RETURN 0

@M_KOJO_MESSAGE_PALAMCNG_A_K26_23(ARG)
;-------------------------------------------------
;イラマチオ
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	CALL M_KOJO_COLOR_K26
	IF RAND:3 == 0
		CALL HPH_PRINT,@"「음... 흐으... 하흡」","W"
		RETURN 1
	ELSEIF RAND:2 == 0
		PRINTFORMW 「흐으음... 음흠...」
		RETURN 1
	ELSE
		CALL HPH_PRINT,@"「헤이... 기침, 기침... 너무 많이 토했네」","W"
		RETURN 1
	ENDIF
ENDIF
RETURN 1
;==================================================
;Optional: Determines if the standard description should be skipped.
;Return 0 if description and KOJO should be shown, 1 if description should be hidden and KOJO shown, 2 if description should be shown and KOJO hidden, and 3 if both description and KOJO should be hidden.
;==================================================

@M_KOJO_MESSAGE_PALAMCNG_MESSAGECHECK_A_K26_22
RETURN 0

@M_KOJO_MESSAGE_PALAMCNG_A_K26_22(ARG)
;-------------------------------------------------
;シックスナイン
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	CALL M_KOJO_COLOR_K26
	;同時射精
	IF NOWEX:射精 && NOWEX:MASTER:射精
		PRINTFORMW
		RETURN 1
		;TARGETだけ射精
	ELSEIF NOWEX:射精
		;PLAYERについてる
		IF GETBIT(TALENT:PLAYER:性別,1)
			PRINTFORMW
			RETURN 1
			;付いてない
		ELSE
			PRINTFORMW
			RETURN 1
		ENDIF
		;PLAYERだけ射精
	ELSE
		;TARGETについてる
		IF GETBIT(TALENT:性別,1)
			PRINTFORMW
			RETURN 1
			;付いてない
		ELSE
			IF RAND:3 == 0
				CALL HPH_PRINT,@"「음 음 맛있다」","W"
				RETURN 1
			ELSEIF RAND:2 == 0
				CALL HPH_PRINT,@"「이게 훨씬 빨리 끝낼 수 있을 것 같군」","W"
				RETURN 1
			ELSE
				CALL HPH_PRINT,@"「음...흠」","W"
				CALL HPH_PRINT,@"「너는 내가 하기 전에 사정하는 정말 즐거움 탐욕쟁이구나 HPH」","W"
				RETURN 1
			ENDIF
		ENDIF
	ENDIF
ENDIF
RETURN 1
;==================================================
;Optional: Determines if the standard description should be skipped.
;Return 0 if description and KOJO should be shown, 1 if description should be hidden and KOJO shown, 2 if description should be shown and KOJO hidden, and 3 if both description and KOJO should be hidden.
;==================================================

@M_KOJO_MESSAGE_PALAMCNG_MESSAGECHECK_A_K26_26
RETURN 0

@M_KOJO_MESSAGE_PALAMCNG_A_K26_26(ARG)
;-------------------------------------------------
;授乳プレイ
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	CALL M_KOJO_COLOR_K26
	IF RAND:3 == 0
		CALL HPH_PRINT,@"「후훗, 너는 그냥 큰 아기잖아, 안 그래HPH」","W"
		RETURN 1
	ELSEIF RAND:2 == 0
		CALL HPH_PRINT,@"「네가 내 가슴에서 마시면서 그렇게 치정스럽게 절정에 이를 수 있다니 믿을 수가 없어」","W"
		RETURN 1
	ELSE
		CALL HPH_PRINT,@"「오호? 벌써 뿅뿅하고 있네」","W"
		RETURN 1
	ENDIF
ENDIF
RETURN 1
;==================================================
;Optional: Determines if the standard description should be skipped.
;Return 0 if description and KOJO should be shown, 1 if description should be hidden and KOJO shown, 2 if description should be shown and KOJO hidden, and 3 if both description and KOJO should be hidden.
;==================================================

@M_KOJO_MESSAGE_PALAMCNG_MESSAGECHECK_A_K26_21
RETURN 0

@M_KOJO_MESSAGE_PALAMCNG_A_K26_21(ARG)
;-------------------------------------------------
;パイズリフェラ
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	CALL M_KOJO_COLOR_K26
	;時姦中
	IF FLAG:70
		PRINTFORMDW 네 것을 발사하며 유카리의 입안을 정액으로 가득 채우고 그녀는 얼어붙은 시간 속에서 무력하게 앉아 있었다.
	ELSEIF TALENT:MASTER:濃厚精液
		IF RAND:3 == 0
			CALL HPH_PRINT,@"「응음흠흐응」","W"
			CALL HPH_PRINT,@"「농후정액이네, 정말 맛있다HPH」","W"
			RETURN 1
		ELSEIF RAND:2 == 0
			CALL HPH_PRINT,@"「...음흠 음흠」","W"
			CALL HPH_PRINT,@"「동시에 가슴과 입으로 애무를 받는 것이 이렇게 기분 좋을 줄 몰랐다HPH」","W"
			PRINTFORMDW 유카리의 입에서 정액 줄기가 넘쳐 가슴을 물들인다...
			RETURN 1
		ELSE
			CALL HPH_PRINT,@"「흐으읍 흐으읍」","W"
			CALL HPH_PRINT,@"「정말 진하네, 내 가슴이 방금 저렇게 기분 좋았나 봐, 음흠흠」","W"
			RETURN 1
		ENDIF
	ELSE
		IF RAND:3 == 0
			CALL HPH_PRINT,@"「음흠」","W"
			CALL HPH_PRINT,@"「제발, 마실 걸 더 주세요HPH」","W"
			RETURN 1
		ELSEIF RAND:2 == 0
			PRINTFORMW 「이런 식으로 가슴과 혀를 사용하는 법을 배우니 정말 무언가를 성취한 기분이야~」
			RETURN 1
		ELSE
			CALL HPH_PRINT,@"「핥는다 음음음음」","W"
			CALL HPH_PRINT,@"「나의 가슴 일은 칭찬할 만했고, 나의 혀 놀림도 나쁘지 않았지, 그렇지 않아 HPH」","W"
			RETURN 1
		ENDIF
	ENDIF
ENDIF
RETURN 1
;==================================================
;Optional: Determines if the standard description should be skipped.
;Return 0 if description and KOJO should be shown, 1 if description should be hidden and KOJO shown, 2 if description should be shown and KOJO hidden, and 3 if both description and KOJO should be hidden.
;==================================================

@M_KOJO_MESSAGE_PALAMCNG_MESSAGECHECK_A_K26_20
RETURN 0

@M_KOJO_MESSAGE_PALAMCNG_A_K26_20(ARG)
;-------------------------------------------------
;手淫フェラ
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	CALL M_KOJO_COLOR_K26
	;時姦中
	IF FLAG:70
		PRINTFORMDW 네 것을 발사하며 유카리의 입안을 정액으로 가득 채우고 그녀는 얼어붙은 시간 속에서 무력하게 앉아 있었다.
	ELSE
		IF RAND:3 == 0
			CALL HPH_PRINT,@"「음...」","W"
			CALL HPH_PRINT,@"「더 짜버릴까아」","W"
			RETURN 1
		ELSEIF RAND:2 == 0
			CALL HPH_PRINT,@"「흐음, 내 손과 입은 당신의 정액 냄새로 뒤덮여있어」","W"
			RETURN 1
		ELSE
			CALL HPH_PRINT,@"「흠흠흠」","W"
			RETURN 1
		ENDIF
	ENDIF
ENDIF
RETURN 1
;==================================================
;Optional: Determines if the standard description should be skipped.
;Return 0 if description and KOJO should be shown, 1 if description should be hidden and KOJO shown, 2 if description should be shown and KOJO hidden, and 3 if both description and KOJO should be hidden.
;==================================================

@M_KOJO_MESSAGE_PALAMCNG_MESSAGECHECK_A_K26_9
RETURN 0

@M_KOJO_MESSAGE_PALAMCNG_A_K26_9(ARG)
;-------------------------------------------------
;アナル奉仕
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	PRINTFORMW 
	RETURN 1
ENDIF
RETURN 1
;==================================================
;Optional: Determines if the standard description should be skipped.
;Return 0 if description and KOJO should be shown, 1 if description should be hidden and KOJO shown, 2 if description should be shown and KOJO hidden, and 3 if both description and KOJO should be hidden.
;==================================================

@M_KOJO_MESSAGE_PALAMCNG_MESSAGECHECK_A_K26_8
RETURN 0

@M_KOJO_MESSAGE_PALAMCNG_A_K26_8(ARG)
;-------------------------------------------------
;泡踊り
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	CALL M_KOJO_COLOR_K26
	;同時射精
	IF NOWEX:射精 && NOWEX:MASTER:射精
		PRINTFORMW
		RETURN 1
		;TARGETだけ射精
	ELSEIF NOWEX:射精
		;PLAYERについてる
		IF GETBIT(TALENT:PLAYER:性別,1)
			PRINTFORMW
			RETURN 1
			;付いてない
		ELSE
			PRINTFORMW
			RETURN 1
		ENDIF
		;PLAYERだけ射精
	ELSE
		;TARGETについてる
		IF GETBIT(TALENT:性別,1)
			PRINTFORMW
			RETURN 1
			;付いてない
		ELSE
			IF RAND:3 == 0
				CALL HPH_PRINT,@"「LookHPH」","W"
				PRINTFORMW 「내가 너를 여기까지 데려왔는데, 얼마나 한심한 거야~」
				RETURN 1
			ELSEIF RAND:2 == 0
				CALL HPH_PRINT,@"「맞아, 나조차도 느껴질 정도야HPH」","W"
				RETURN 1
			ELSE
				CALL HPH_PRINT,@"「이거 HPH에서 사정하다니 믿을 수가 없어」","W"
				RETURN 1
			ENDIF
		ENDIF
	ENDIF
ENDIF
RETURN 1
;==================================================
;Optional: Determines if the standard description should be skipped.
;Return 0 if description and KOJO should be shown, 1 if description should be hidden and KOJO shown, 2 if description should be shown and KOJO hidden, and 3 if both description and KOJO should be hidden.
;==================================================

@M_KOJO_MESSAGE_PALAMCNG_MESSAGECHECK_A_K26_7
RETURN 0

@M_KOJO_MESSAGE_PALAMCNG_A_K26_7(ARG)
;-------------------------------------------------
;足コキ
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	CALL M_KOJO_COLOR_K26
	IF RAND:3 == 0
		CALL HPH_PRINT,@"「여기서 내 발에 밟히고 보기 싫은 네 정액을 나에게 주렴」","W"
		RETURN 1
	ELSEIF RAND:2 == 0
		CALL HPH_PRINT,@"「%CALLNAME:MASTER%, 너 완전 마조구나, 그렇지?HPH」","W"
		RETURN 1
	ELSE
		CALL HPH_PRINT,@"「어떻게 내 다리 전체에 그 한심한 것들을 뿌릴 수 있었지?」","W"
		RETURN 1
	ENDIF
ENDIF
RETURN 1
;==================================================
;Optional: Determines if the standard description should be skipped.
;Return 0 if description and KOJO should be shown, 1 if description should be hidden and KOJO shown, 2 if description should be shown and KOJO hidden, and 3 if both description and KOJO should be hidden.
;==================================================

@M_KOJO_MESSAGE_PALAMCNG_MESSAGECHECK_A_K26_6
RETURN 0

@M_KOJO_MESSAGE_PALAMCNG_A_K26_6(ARG)
;-------------------------------------------------
;素股
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	CALL M_KOJO_COLOR_K26
	CALL HPH_PRINT,@"「흠, 모든 것을 쏴 버리자 HPH」","W"
ENDIF
RETURN 1
;==================================================
;Optional: Determines if the standard description should be skipped.
;Return 0 if description and KOJO should be shown, 1 if description should be hidden and KOJO shown, 2 if description should be shown and KOJO hidden, and 3 if both description and KOJO should be hidden.
;==================================================

@M_KOJO_MESSAGE_PALAMCNG_MESSAGECHECK_A_K26_5
RETURN 0

@M_KOJO_MESSAGE_PALAMCNG_A_K26_5(ARG)
;-------------------------------------------------
;パイズリ
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	CALL M_KOJO_COLOR_K26
	IF RAND:3 == 0
		CALL HPH_PRINT,@"「음냐옹」","W"
		CALL HPH_PRINT,@"「진심으로, 네 그 남자 같은 악취로 나를 뒤덮을 거야?」","W"
		RETURN 1
	ELSEIF RAND:2 == 0
		CALL HPH_PRINT,@"「음, 그 악취가 너무 강해서 거의 중독적일 정도야HPH」","W"
		RETURN 1
	ELSE
		CALL HPH_PRINT,@"「흠흠 여기 온다 HPH」","W"
		RETURN 1
	ENDIF
ENDIF
RETURN 1
;==================================================
;Optional: Determines if the standard description should be skipped.
;Return 0 if description and KOJO should be shown, 1 if description should be hidden and KOJO shown, 2 if description should be shown and KOJO hidden, and 3 if both description and KOJO should be hidden.
;==================================================

@M_KOJO_MESSAGE_PALAMCNG_MESSAGECHECK_A_K26_4
RETURN 0

@M_KOJO_MESSAGE_PALAMCNG_A_K26_4(ARG)
;-------------------------------------------------
;フェラチオ
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	CALL M_KOJO_COLOR_K26
	;時姦中
	IF FLAG:70
		PRINTFORMDW 네 것을 발사하며 유카리의 입안을 정액으로 가득 채우고 그녀는 얼어붙은 시간 속에서 무력하게 앉아 있었다.
	ELSEIF TALENT:MASTER:濃厚精液
		IF RAND:3 == 0
			CALL HPH_PRINT,@"「흠흠흠흡」","W"
			CALL HPH_PRINT,@"「참 진한 남성 맛이군...HPH」","W"
			RETURN 1
		ELSEIF RAND:2 == 0
			CALL HPH_PRINT,@"「음ㅁ흠흐응흥ㅎ프」","W"
			CALL HPH_PRINT,@"「내 입은 네 정액으로 완전히 가득 차 있어」","W"
			PRINTFORMDW 유카리의 입에서 정액이 넘쳐 끈적한 줄을 따라 흘러내린다...
			RETURN 1
		ELSE
			CALL HPH_PRINT,@"「음흠흠 음흠흠」","W"
			CALL HPH_PRINT,@"「너무 진해, 다음엔 정상적으로 빼줘」","W"
			RETURN 1
		ENDIF
	ELSE
		IF RAND:3 == 0
			CALL HPH_PRINT,@"「흠흠」","W"
			CALL HPH_PRINT,@"「더로는 부족해, 마실 걸 더 줘 HPH」","W"
			RETURN 1
		ELSEIF RAND:2 == 0
			CALL HPH_PRINT,@"「내 혀가 당신의 만족을 주었다고 생각한다」","W"
			RETURN 1
		ELSE
			CALL HPH_PRINT,@"「음흠 이거 나쁜 습관이 될지도 몰라, 알잖아HPH」","W"
			RETURN 1
		ENDIF
	ENDIF
ENDIF
RETURN 1
;==================================================
;Optional: Determines if the standard description should be skipped.
;Return 0 if description and KOJO should be shown, 1 if description should be hidden and KOJO shown, 2 if description should be shown and KOJO hidden, and 3 if both description and KOJO should be hidden.
;==================================================

@M_KOJO_MESSAGE_PALAMCNG_MESSAGECHECK_A_K26_3
RETURN 0

@M_KOJO_MESSAGE_PALAMCNG_A_K26_3(ARG)
;-------------------------------------------------
;手淫
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	CALL M_KOJO_COLOR_K26
	;時姦中
	IF FLAG:70
		PRINTFORMDW 네 것이 분출하며 유카리의 손가락을 정액으로 뒤덮는다. 그녀는 얼어붙은 시간 속에 무력하게 앉아 있다.
	ELSE
		IF RAND:3 == 0
			CALL HPH_PRINT,@"「음...」","W"
			CALL HPH_PRINT,@"「정말 믿을 수 없을 정도로 활기찬 성이군... 저런 것에서 나올 줄은 몰랐어.」","W"
			RETURN 1
		ELSEIF RAND:2 == 0
			CALL HPH_PRINT,@"「후후~ 내 손가락이 정말 그렇게 멋지게 느껴졌나 'HPH'」","W"
			RETURN 1
		ELSE
			PRINTFORMW 「오호?」
			CALL HPH_PRINT,@"「이제 내 손은 정액처럼 지독해지겠군... 내가 뭘 해야 하나 HPH」","W"
			RETURN 1
		ENDIF
	ENDIF
ENDIF
RETURN 1
;==================================================
;Optional: Determines if the standard description should be skipped.
;Return 0 if description and KOJO should be shown, 1 if description should be hidden and KOJO shown, 2 if description should be shown and KOJO hidden, and 3 if both description and KOJO should be hidden.
;==================================================

@M_KOJO_MESSAGE_PALAMCNG_MESSAGECHECK_A_K26_2
RETURN 0

@M_KOJO_MESSAGE_PALAMCNG_A_K26_2(ARG)
;-------------------------------------------------
;アナル
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	CALL M_KOJO_COLOR_K26
	;時姦中
	IF FLAG:70
		PRINTFORMDW 당신은 얼어붙은 시간 속에 무력하게 앉아 있는 유카리의 항문을 정액으로 가득 채운다.
	ELSE
		IF RAND:3 == 0
			CALL HPH_PRINT,@"「음...」","W"
			CALL HPH_PRINT,@"「네 유전자가 나의 항문에서 고동치고 있어 HPH」","W"
			RETURN 1
		ELSEIF RAND:2 == 0
			CALL HPH_PRINT,@"「내 엉덩이에 그렇게 많이 쌌어」","W"
			RETURN 1
		ELSE
			CALL HPH_PRINT,@"「흐읍 네가 전부 내 항문에 부었어HPH」","W"
			RETURN 1
		ENDIF
	ENDIF
ENDIF
RETURN 1
;==================================================
;Optional: Determines if the standard description should be skipped.
;Return 0 if description and KOJO should be shown, 1 if description should be hidden and KOJO shown, 2 if description should be shown and KOJO hidden, and 3 if both description and KOJO should be hidden.
;==================================================

@M_KOJO_MESSAGE_PALAMCNG_MESSAGECHECK_A_K26_1
RETURN 0

@M_KOJO_MESSAGE_PALAMCNG_A_K26_1(ARG)
;-------------------------------------------------
;膣内
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	CALL M_KOJO_COLOR_K26
	IF TALENT:MASTER:濃厚精液
		IF RAND:3 == 0
			CALL HPH_PRINT,@"「흐음흠, 하아음하우」","W"
			RETURN 1
		ELSEIF RAND:2 == 0
			CALL HPH_PRINT,@"「이렇게 내보내면 나는 확실히 임신할 거야HPH」","W"
			RETURN 1
		ELSE
			CALL HPH_PRINT,@"「네 정액이 내 질을 가득 채우는 게 느껴져, 전부 HPH에」","W"
			RETURN 1
		ENDIF
	ELSE
		IF RAND:3 == 0
			CALL HPH_PRINT,@"「네 유전자로 표시될 거야HPH」","W"
			RETURN 1
		ELSEIF RAND:2 == 0
			CALL HPH_PRINT,@"「네 성기도 기분이 좋기를 바라, %CALLNAME:MASTER%HPH」","W"
			RETURN 1
		ELSE
			CALL HPH_PRINT,@"「당신 내 질에 정말 많이 사정했지, 안 그래HPH」","W"
			RETURN 1
		ENDIF
	ENDIF
ENDIF
RETURN 1
;-------------------------------------------------
;眠姦射精に関する口上の表示
;一時的にTARGETを口上主に設定している
;-------------------------------------------------
;==================================================
;Optional: Determines if the standard description should be skipped.
;Return 0 if description and KOJO should be shown, 1 if description should be hidden and KOJO shown, 2 if description should be shown and KOJO hidden, and 3 if both description and KOJO should be hidden.
;==================================================

@M_KOJO_MESSAGE_PALAMCNG_MESSAGECHECK_A2_K26_0
RETURN 0

@M_KOJO_MESSAGE_PALAMCNG_A2_K26_0(ARG)
;-------------------------------------------------
;コンドーム
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	PRINTFORMW 
	RETURN 1
ENDIF
RETURN 1
;==================================================
;Optional: Determines if the standard description should be skipped.
;Return 0 if description and KOJO should be shown, 1 if description should be hidden and KOJO shown, 2 if description should be shown and KOJO hidden, and 3 if both description and KOJO should be hidden.
;==================================================

@M_KOJO_MESSAGE_PALAMCNG_MESSAGECHECK_A2_K26_23
RETURN 0

@M_KOJO_MESSAGE_PALAMCNG_A2_K26_23(ARG)
;-------------------------------------------------
;イラマチオ
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	PRINTFORMW 
	RETURN 1
ENDIF
RETURN 1
;==================================================
;Optional: Determines if the standard description should be skipped.
;Return 0 if description and KOJO should be shown, 1 if description should be hidden and KOJO shown, 2 if description should be shown and KOJO hidden, and 3 if both description and KOJO should be hidden.
;==================================================

@M_KOJO_MESSAGE_PALAMCNG_MESSAGECHECK_A2_K26_7
RETURN 0

@M_KOJO_MESSAGE_PALAMCNG_A2_K26_7(ARG)
;-------------------------------------------------
;足コキ
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	PRINTFORMW 
	RETURN 1
ENDIF
RETURN 1
;==================================================
;Optional: Determines if the standard description should be skipped.
;Return 0 if description and KOJO should be shown, 1 if description should be hidden and KOJO shown, 2 if description should be shown and KOJO hidden, and 3 if both description and KOJO should be hidden.
;==================================================

@M_KOJO_MESSAGE_PALAMCNG_MESSAGECHECK_A2_K26_5
RETURN 0

@M_KOJO_MESSAGE_PALAMCNG_A2_K26_5(ARG)
;-------------------------------------------------
;パイズリ
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	PRINTFORMW 
	RETURN 1
ENDIF
RETURN 1
;==================================================
;Optional: Determines if the standard description should be skipped.
;Return 0 if description and KOJO should be shown, 1 if description should be hidden and KOJO shown, 2 if description should be shown and KOJO hidden, and 3 if both description and KOJO should be hidden.
;==================================================

@M_KOJO_MESSAGE_PALAMCNG_MESSAGECHECK_A2_K26_4
RETURN 0

@M_KOJO_MESSAGE_PALAMCNG_A2_K26_4(ARG)
;-------------------------------------------------
;フェラチオ
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	PRINTFORMW 
	RETURN 1
ENDIF
RETURN 1
;==================================================
;Optional: Determines if the standard description should be skipped.
;Return 0 if description and KOJO should be shown, 1 if description should be hidden and KOJO shown, 2 if description should be shown and KOJO hidden, and 3 if both description and KOJO should be hidden.
;==================================================

@M_KOJO_MESSAGE_PALAMCNG_MESSAGECHECK_A2_K26_3
RETURN 0

@M_KOJO_MESSAGE_PALAMCNG_A2_K26_3(ARG)
;-------------------------------------------------
;手淫
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	PRINTFORMW 
	RETURN 1
ENDIF
RETURN 1
;==================================================
;Optional: Determines if the standard description should be skipped.
;Return 0 if description and KOJO should be shown, 1 if description should be hidden and KOJO shown, 2 if description should be shown and KOJO hidden, and 3 if both description and KOJO should be hidden.
;==================================================

@M_KOJO_MESSAGE_PALAMCNG_MESSAGECHECK_A2_K26_2
RETURN 0

@M_KOJO_MESSAGE_PALAMCNG_A2_K26_2(ARG)
;-------------------------------------------------
;アナル
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	PRINTFORMW 
	RETURN 1
ENDIF
RETURN 1
;==================================================
;Optional: Determines if the standard description should be skipped.
;Return 0 if description and KOJO should be shown, 1 if description should be hidden and KOJO shown, 2 if description should be shown and KOJO hidden, and 3 if both description and KOJO should be hidden.
;==================================================

@M_KOJO_MESSAGE_PALAMCNG_MESSAGECHECK_A2_K26_1
RETURN 0

@M_KOJO_MESSAGE_PALAMCNG_A2_K26_1(ARG)
;-------------------------------------------------
;膣内
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	PRINTFORMW 
	RETURN 1
ENDIF
RETURN 1

@M_KOJO_MESSAGE_PALAMCNG_F_K26(ARG)
;-------------------------------------------------
;焦らし
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	PRINTFORMW 
	RETURN 1
ENDIF
RETURN 1
