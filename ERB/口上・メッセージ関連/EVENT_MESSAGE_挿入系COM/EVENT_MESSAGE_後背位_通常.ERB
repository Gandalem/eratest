
@MESSAGE_COM61通常
CALL MESSAGE_COM61通常_TR
RETURN
;背面立位
IF TFLAG:193
	;抜かず
	IF TCVAR:100 == PLAYER && TARGET == TFLAG:103
		IF NOWEX:MASTER:射精
			PRINTFORM You grab %CALLNAME:TARGET%'s \@ OPENPLACE(CFLAG:MASTER:現在位置) == 2 ? arms # hips \@ 
			PRINTFORML and start thrusting furiously while pouring your semen into %HIS_HER(TARGET)% vagina...
		ELSEIF TCVAR:102
			PRINTFORM You finish cumming inside %CALLNAME:TARGET%, 
			PRINTFORML and start thrusting again before your %TINKO(PLAYER)% becomes soft...
		ELSEIF TEQUIP:体位２ == 2
			;調教者についてる
			IF TALENT:PLAYER:2 & 2
				PRINTFORML %CALLNAME:TARGET%の膣内の感触を味わいながら、再び腰を動かし始めた…
			;ペニスバンドを使用している場合
			ELSE
				PRINTFORML %CALLNAME:TARGET%の膣内から抜かずに、再び腰を動かし始めた…
			ENDIF
		ELSE
			IF TEQUIP:体位２ == 2
				PRINTFORML %CALLNAME:TARGET%の膣内から%TINKO(PLAYER)%を抜き去ることなく再び腰を動かし始めた…
			;対面立位から
			ELSE
				PRINTFORML 一旦%CALLNAME:TARGET%から%TINKO(PLAYER)%を抜いて壁に手を付かせ
				PRINTFORML 後ろから挿入し再び腰を動かし始めた…
			ENDIF
		ENDIF
	ELSE
		IF TALENT:処女 == 1
			PRINTFORML \@ OPENPLACE(CFLAG:MASTER:現在位置) == 2 ? 後ろから両手を掴んだ # 壁に手を付かせた \@%CALLNAME:TARGET%におしりを突き出させ
			PRINTFORML 男を知らない\@ TALENT:体型 < 0 ? 幼いワレメ # 秘裂 \@を%TINKO(PLAYER)%で貫いた…
		ELSEIF MASTER_POSE(SET_V,SET_P,1) && MASTER_POSE(SET_V,SET_P,1) != TARGET
			PRINTFORML %CALLNAME:(MASTER_POSE(SET_V,SET_P,1))%から%TINKO(PLAYER)%を抜くと同時に
			PRINTFORML %CALLNAME:TARGET%に挿入した…
		ELSE
			IF TEQUIP:0 & 2
				PRINTFORM %SHOW_BOTTOM(TARGET,2)%をずらした
			ELSEIF TEQUIP:0 & 1
				PRINTFORM %SHOW_BOTTOM(TARGET,1)%を捲り上げた
			ENDIF
			PRINTFORM %CALLNAME:TARGET%
			PRINTFORM \@ OPENPLACE(CFLAG:MASTER:現在位置) == 2 ? に突き出させた両手を掴み # の両手を壁に付かせて \@、後ろから
			;潤滑LV4以上
			SIF PALAM:潤滑 >= PALAMLV:4
				PRINT 濡れそぼった
			;小柄体型
			IF TALENT:体型 < 0
				PRINT 幼いワレメ
			ELSE
				PRINT 秘裂
			ENDIF
			PRINTFORML の最奥まで突き入れた…
		ENDIF
	ENDIF
ELSE
;抜かず
	IF TCVAR:100 == PLAYER && TARGET == TFLAG:103
		IF NOWEX:MASTER:射精
			PRINTFORM You make %CALLNAME:TARGET% get down on all fours, and grab %HIS_HER(TARGET)% hips, 
			PRINTFORML and start thrusting furiously while pouring semen into %HIS_HER(TARGET)% vagina...
		ELSEIF TCVAR:102
			PRINTFORM You finish cumming inside %CALLNAME:TARGET%, 
			PRINTFORML and start thrusting again before your %TINKO(PLAYER)% becomes soft...
		ELSEIF TEQUIP:体位２ == 2
			;調教者についてる
			IF TALENT:PLAYER:2 & 2
				PRINTFORML %CALLNAME:TARGET%の膣内の感触を味わいながら、再び腰を動かし始めた…
			;ペニスバンドを使用している場合
			ELSE
				PRINTFORML %CALLNAME:TARGET%の膣内から抜かずに、再び腰を動かし始めた…
			ENDIF
		ELSE
			PRINTFORML While inserted in %CALLNAME:TARGET%, you push %HIM_HER(TARGET)% down on all fours and continue thrusting...
		ENDIF
	ELSE
		IF TALENT:処女 == 1
			PRINTFORML 四つん這いにさせた%CALLNAME:TARGET%のおしりを高くあげさせ
			PRINTFORML 男を知らない\@ TALENT:体型 < 0 ? 幼いワレメ # 秘裂 \@を%TINKO(PLAYER)%で貫いた…
		ELSEIF MASTER_POSE(SET_V,SET_P,1) && MASTER_POSE(SET_V,SET_P,1) != TARGET
			PRINTFORML %CALLNAME:(MASTER_POSE(SET_V,SET_P,1))%から%TINKO(PLAYER)%を抜くと同時に
			PRINTFORML %CALLNAME:TARGET%に挿入した…
		ELSE
			PRINTFORM You push %CALLNAME:TARGET% down on all fours
			IF TEQUIP:0 & 2
				PRINTFORM , moved %HIS_HER(TARGET)% %TOLOWER(SHOW_BOTTOM(TARGET,2))% aside,
			ELSEIF TEQUIP:0 & 1
				PRINTFORM , flipped up %HIS_HER(TARGET)% %TOLOWER(SHOW_BOTTOM(TARGET,1))%,
			ENDIF
			PRINTFORM  and pushed all the way into %CALLNAME:TARGET%'s 
			;潤滑LV4以上
			SIF PALAM:潤滑 >= PALAMLV:4
				PRINT dripping wet 
			;小柄体型
			IF TALENT:体型 < 0
				PRINTFORM %COND_STR(@"and ", PALAM:潤滑 >= PALAMLV:4)%immature slit
			ELSE
				PRINT slit
			ENDIF
			PRINTFORML  from behind...
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;322,相手主導後背位
;-------------------------------------------------
@MESSAGE_COM322
IF TALENT:処女 == 1
	PRINTFORML %CALLNAME:TARGET% aligns the head of your %TINKO(MASTER)% with %HIS_HER(TARGET)% virginal \@ TALENT:体型 < 0 ? young slit # slit \@ and begins pushing it in.
	PRINTFORML It slowly spreads %HIS_HER(TARGET)% lips open and pushes further inside, before tearing %HIS_HER(TARGET)% maidenhood.
		SIF TALENT:恋慕
			PRINTFORML %CALLNAME:TARGET% bears the pain as %HE_SHE(TARGET)% clings to you...
ELSE
	PRINTFORML You hang over %CALLNAME:TARGET% as you push your glans into %HIS_HER(TARGET)% twitching, greedy hole...
	SELECTCASE ABL:TARGET:膣
		CASE 0
			PRINTFORML You thrust your %TINKO()% into %HIS_HER(TARGET)% tight and narrow opening, wrenching it open and reaching deep inside...
		CASE 1
			PRINTFORML You bury your %TINKO()% in %CALLNAME:TARGET%'s still slightly stiff insides...
		CASE 2,3
			PRINTFORML You pound your %TINKO()% into %CALLNAME:TARGET%'s tight insides, reaching deep inside...
		CASEELSE
			PRINTFORML %CALLNAME:TARGET%'s warm insides melt around your %TINKO()%, inviting you, letting you push deep inside without force...
	ENDSELECT
	PRINTFORML Wrapped up in %CALLNAME:TARGET%'s warm insides, your %TINKO()% twitches excitedly knowing it's where it belongs...
ENDIF