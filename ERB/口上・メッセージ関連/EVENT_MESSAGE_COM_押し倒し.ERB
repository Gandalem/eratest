;-------------------------------------------------
;350,押し倒す
;-------------------------------------------------
@MESSAGE_COM350
IF FLAG:70 == 1
	IF GET_TARGETNUM() > 1
		PRINTFORML You decide to do some mischief with %CALLNAME:TARGET% and the others while time is stopped...
	ELSE
		PRINTFORML You decide to do some mischief with %CALLNAME:TARGET% while time is stopped...
	ENDIF
;custom uncomment, translated
ELSEIF TFLAG:194 == 2
	PRINTFORML However, %CALLNAME:TARGET% turns you down.
	PRINTFORML %HE_SHE(TARGET, 1)% must be still uncomfortable with the idea of doing it outside...
ELSEIF TFLAG:194  == 100
	;口上内特殊判定の場合地の文非表示
ELSEIF TFLAG:194 == 1
	PRINTL It's easy to be seen from here...
	IF nRapeAnywhere && ABL:TARGET:露出癖 + TALENT:TARGET:羞恥心 < 6
	PRINTFORML ... %CALLNAME:TARGET% turns %PARSE("you")% down.
	PRINTFORML %HE_SHE(TARGET, 1)% must be still uncomfortable with the idea of doing it outside...
	ELSEIF nRapeAnywhere
		PRINTL ...but you do it anyway
	ENDIF
;custom uncomment, translated
ELSEIF TFLAG:194 == 3
	PRINTFORML You attempt to push down %CALLNAME:TARGET% with the others.
	PRINTFORML However, some of them still oppose the idea of having an orgy...
ELSEIF TFLAG:194 == 4
	PRINTFORML You try to embrace %CALLNAME:TARGET%, but %HE_SHE(TARGET)% isn't in the mood and refuses. 
;ELSEIF TFLAG:194 == 5
;	PRINTL 素面の目撃者がいるのに酔姦するわけにいかない
ELSEIF TFLAG:194 == 6
	;NAS: Religion
	;GET_INT(15, "Religion", 4, "Max Rank For Sex")
	PRINTFORML However, %CALLNAME:TARGET% turns you down.
	PRINTFORML %HIS_HER(TARGET,1)% position in %TOSTR_RELIGION(TALENT:TARGET:Religion)% causes %HIM_HER(TARGET)% to decline.
ELSEIF TFLAG:194 == 7
	;NAS: Religion
	PRINTFORML However, %CALLNAME:TARGET% turns you down.
	PRINTFORML %HIS_HER(TARGET,1)% religion forces %HIM_HER(TARGET)% to decline (%TOSTR_RELIGION(TALENT:TARGET:Religion)%: %TOSTR_PRECEPT_RANKING("Physical Love", GET_INT(TARGET, "Religion", TALENT:TARGET:Religion, "Physical Love"), 0)%).
ELSEIF TCVAR:TARGET:泥酔 == 1
	PRINTFORML You decide to mess with the drunk %TNAME(TARGET,,1)%...
	PRINTFORML %CALLNAME:TARGET% is drunk, and bereft of %HIS_HER(TARGET)% common sense...
ELSEIF GET_TARGETNUM() > 1
	PRINTFORML After making sure there is no one around, you embrace %CALLNAME:TARGET% and the others...
ELSEIF (CFLAG:TARGET:現在位置 == 連れ込み宿 || CFLAG:TARGET:現在位置 == ラブホ || CFLAG:TARGET:現在位置 == OMANEKIBEYA()) && CFLAG:TARGET:うふふ != 2
	PRINTFORML The two of you alone together, you embrace %CALLNAME:TARGET%'s shoulders...
ELSEIF CFLAG:現在位置 == 寺子屋
	PRINTFORML 「I guess the persevering %CALLNAME:PLAYER% deserves a reward...」
	;Normally hardcoded to be Keine, but the case is for everyone, this is minimum-effort.
	PRINTFORML With just the two of you in the classroom, %CALLNAME:TARGET%-sensei begins %HIS_HER(TARGET)% special class...
ELSEIF !TFLAG:194 && TCVAR:TARGET:抱き寄せ初回 == 10
	PRINTFORML You thrust a winning ticket in %CALLNAME:TARGET%'s face and ask them to undress.
	PRINTFORML %CALLNAME:TARGET% doesn't hide %HIS_HER(TARGET)% anger, but undresses either way...
ELSEIF TCVAR:TARGET:抱き寄せ初回 == 99
	;口上内特殊判定の場合地の文非表示
ELSE
	PRINTFORML After making sure there is no one around, you embrace %CALLNAME:TARGET%...
	
	;custom code
	IF TCVAR:抱き寄せ初回 >= 6
		SIF TCVAR:抱き寄せ初回 < 10
			PRINTFORM ...But 
		SELECTCASE TCVAR:抱き寄せ初回
			;1-5 are letting you do it, there's
			CASE 6
				PRINTFORMW %HE_SHE(TARGET)% politely declines, hinting that %HE_SHE(TARGET)%'d be willing if %HE_SHE(TARGET)% had a bit more confidence in you...
			CASE 7
				PRINTFORMW %HE_SHE(TARGET)% turns you down right away, saying that there's no way %HE_SHE(TARGET)%'d agree to that...
			CASE 8
				PRINTFORM %HE_SHE(TARGET)% violently resists your unwanted advances, scolding you with all %HIS_HER(TARGET)% might
				SIF TALENT:度胸 < 0
					PRINTFORM .\n%HIS_HER(TARGET,1)% voice shakes and you can tell that you almost traumatized %HIS_HER(TARGET)%
				PRINTW ...
			CASE 9 ;not used yet
				PRINTFORMW %HE_SHE(TARGET)% is nervous, agreeing on the condition that you will not go all the way since %HE_SHE(TARGET)% isn't ready yet...
			CASE 10
				PRINTFORMW %HE_SHE(TARGET,1)% stares at you in disbelief, looking both disgusted and disappointed, but still goes along with your demands...
			CASE 20
				PRINTFORMW %HE_SHE(TARGET,1)% glares daggers at you, but having no say in it %HE_SHE(TARGET)% submits herself to the victor...
		ENDSELECT
		PRINTFORML
	ENDIF
ENDIF
WAIT
IF nRapeAnywhere && !INROOM(CFLAG:PLAYER:現在位置)
;TCVAR:抱き寄せ初回　1=普通に合意ゲット　2=先にキスぐらいしろよ　3=媚薬or発情には勝てなかったよお…　4=悔しい…刻印さえなければﾋﾞｸﾋﾞｸﾝ　5=強引に迫られると断れぬ
;6=まんざらでもない　7=駄目　8=論外　9=挿入禁止 10= 守矢くじデート相手
SETCOLOR C_YELLOW
PRINTFORML ...in a public space...
RESETCOLOR
WAIT
ENDIF
IF TFLAG:194 == 1
	SETCOLOR C_RED
	PRINTFORML ...with potential witnesses...
	RESETCOLOR
	WAIT
ENDIF		
IF !TFLAG:70 && !TFLAG:194 || nRapeAnywhere && TFLAG:194 == 1
	SELECTCASE TCVAR:TARGET:抱き寄せ初回
		CASE 99
			;口上内特殊判定の場合地の文非表示
		CASE 98
			;口上内特殊判定の場合地の文非表示
		CASE 2
			PRINTFORML %CALLNAME:TARGET% agrees but seems a bit dissatisfied.
			PRINTFORML ...Right, you haven't even kissed after a date yet.
		CASE 3
			PRINTFORML %CALLNAME:TARGET% can't control how %HIS_HER(TARGET)% body aches, and yields it to you...
			CALL ASK_DIARY(@"With %CALLNAME:TARGET% being unable to control the tingling in %HIS_HER(TARGET)% loins, it was an easy task to compel %HIM_HER(TARGET)% into having sex.")
		CASE 4
			PRINTFORML %CALLNAME:TARGET% finds %HIMSELF_HERSELF(TARGET)% accepting being swept up by you...
			SIF CFLAG:体目当て
				CALL COLORMESSAGE(@"Thanks to the Stone of Mirada I was able to do H-stuff with a girl I liked!（%CALLNAME:MASTER%, resident at %GET_MAPNAME(MAIN_MAP)%）",C_YELLOW,2,2)
			CALL ASK_DIARY(@"With %CALLNAME:TARGET% being offered a proposal %HE_SHE(TARGET)% couldn't refuse, it was an easy task to compel %HIM_HER(TARGET)% into having sex.")
			
			;IF MARK:時姦刻印 >= 3 && !ADD_ACHIEVEMENT_OBTAINFLAG:Achievement_TSPInsight
			IF MARK:時姦刻印 >= 3 && !SaveAchievementCompleted("insightTime")
				;CALL ANIMATION_ROLL_TEXT( @"Achievement Unlocked：[TSP Insight]!", 0 ) ;addition custom code
				;ADD_ACHIEVEMENT_OBTAINFLAG:Achievement_TSPInsight = 1
				CALL AL_NOTIFY_ACHIEVEMENT_COMPLETION("insightTime") ;achievement menu rework custom code
			ENDIF
		CASE 5
			PRINTFORML After being given a light push, %CALLNAME:TARGET% can't refuse you...
			CALL ASK_DIARY(@"%CALLNAME:TARGET% was so timid, there was no way %HE_SHE(TARGET)% could say no to having sex.")
		CASE 6
			PRINTFORML %CALLNAME:TARGET% declines...
			PRINTFORML But, %HE_SHE(TARGET)% seems like %HE_SHE(TARGET)% wouldn't mind doing it sometime in the future.
		CASE 7
			IF TALENT:TARGET:処女 == 0
				PRINTFORML %CALLNAME:TARGET% doesn't take it too seriously and refuses...
			ELSE
				PRINTFORML %CALLNAME:TARGET% refuses...
			ENDIF
			SIF CFLAG:体目当て
				CALL COLORMESSAGE(@"Although I used it, I was still refused. It doesn't monopolize lust at all.（%CALLNAME:MASTER%, resident at %GET_MAPNAME(MAIN_MAP)%）",C_YELLOW,2,2)
		CASE 8
			IF TALENT:TARGET:度胸 < 0 || OTOKOGIRAI(TARGET)
				PRINTFORML %CALLNAME:TARGET% screams and pushes you away.
			ELSE
				PRINTFORML %CALLNAME:TARGET% immediately takes a step back from you.
			ENDIF
			SIF CFLAG:体目当て
				CALL COLORMESSAGE(@"Although I used it, I was still refused. It doesn't monopolize lust at all.（%CALLNAME:MASTER%, resident at %GET_MAPNAME(MAIN_MAP)%）",C_YELLOW,2,2)
		CASE 9
			PRINTFORML %BREAKENG(@"Unable to bear the tingling of %HIS_HER(TARGET)% loins, %CALLNAME:TARGET% ends up giving %HIMSELF_HERSELF(TARGET)% to you on the condition that there would be no penetration...")%
		CASE 1
			IF TALENT:恋慕 && TALENT:恋人
				PRINTFORML You and %CALLNAME:TARGET% hug each other...
			ELSEIF TALENT:恋慕 && TALENT:度胸 < 0
				PRINTFORML %CALLNAME:TARGET% looks as if %HE_SHE(TARGET)%'s ready as %HE_SHE(TARGET)% stares at you.
			ELSEIF TALENT:調合知識
				PRINTFORML %CALLNAME:TARGET% seems worked up...
			ELSEIF TALENT:SexKnow == -1
				PRINTFORML %CALLNAME:TARGET% doesn't seem to understand what it means...
			ELSEIF TALENT:度胸 < 0
				PRINTFORML %CALLNAME:TARGET% doesn't look completely ready...
			ELSE
				PRINTFORML %CALLNAME:TARGET% has a resolute look in %HIS_HER(TARGET)% eyes as %HE_SHE(TARGET)% stares at you.
			ENDIF
			IF TALENT:処女 == -1 || TALENT:処女 == 1
				PRINTFORM %CALLNAME:TARGET% 
				IF TARGET == 57 && !TALENT:恋慕
					PRINTFORM alternates between a Saru, Kodomo, and Okina mask
				ELSE
					PRINTFORM seems caught between anxiety and anticipation
				ENDIF
				PRINTFORML ...
			ENDIF
			CALL ASK_DIARY(@"Received proper consent to having sex for the first time with %CALLNAME:TARGET%.")
		CASE 20
			PRINTFORML %CALLNAME:TARGET% resists at first, but submits to the hug.
			IF TALENT:TARGET:度胸 > 0 || TALENT:態度 > 0 || TALENT:プライド > 0
				PRINTFORM %HE_SHE(TARGET,1)% bites %HIS_HER(TARGET)% lip in humiliation
			ELSE
				PRINTFORM Tears flow down %HIS_HER(TARGET)% cheeks
			ENDIF
			PRINTFORML ...
			;NAS: TCVAR 793 used to determine if the rape was from Danmaku or pushdown, also puts a firsttime check.
			IF FIRSTTIME("RapeExpansionPushDown",,TARGET)
			IF TCVAR:MASTER:793
			CALL ASK_DIARY(@"%CALLNAME:TARGET% was no match for my close quarters combat skills, and I was able to do anything to %HIS_HER(TARGET)% body...")
			ELSE
			CALL ASK_DIARY(@"After owning %HIM_HER(TARGET)% in a duel, it was an easy task to coerce %CALLNAME:TARGET% into letting me own %HIS_HER(TARGET)% body as well...")
			ENDIF
			ENDIF
		ENDSELECT
ENDIF

IF !FLAG:70 && HAS_PENIS(MASTER) && !TFLAG:194 && !TFLAG:押し倒せない ;custom code
	IF TALENT:恋人
		CALL COLORMESSAGE(@"Feeling the warmth of %CALLNAME:TARGET%'s body so closely puts you at full mast!",C_PINK,1)
		BASE:MASTER:勃起 = MAXBASE:MASTER:勃起
	ELSEIF CFLAG:溜まってる度 >= 900
		CALL COLORMESSAGE(@"%CALLNAME:TARGET%'s begging for sex is enough to make %PARSE("your")% %FSYN("penis")% flood with anticipation.",C_PINK,1)
		BASE:MASTER:勃起 = MAXBASE:MASTER:勃起
	ELSEIF TCVAR:TARGET:抱き寄せ初回 == 20
		CALL COLORMESSAGE(@"Thinking that you can now do whatever you want with %CALLNAME:TARGET% regardless of what %HE_SHE(TARGET)% thinks makes you harden...",C_PINK,1)
		BASE:MASTER:勃起 = MAX(BASE:MASTER:勃起, 1000)
	ELSEIF CFLAG:溜まってる度 >= 500
		CALL COLORMESSAGE(@"Seeing %CALLNAME:TARGET%'s lust-filled eyes makes you unable to contain your excitement.",C_PINK,1)
		BASE:MASTER:勃起 = MAX(BASE:MASTER:勃起, 1000)
	ENDIF
ENDIF