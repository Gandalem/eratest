
;-------------------------------------------
;触手の射精時のテキスト
;-------------------------------------------
@PALAM_MESSAGE_E
RESETCOLOR
;触手が射精
SIF TFLAG:15 == 1
	PRINTFORML The tentacle coiling around %CALLNAME:TARGET% discharge sticky fluids...
;触手が大量射精
SIF TFLAG:15 == 2
	PRINTFORML The tentacle coiling around %CALLNAME:TARGET% sprays out massive quantities of sticky fluids...
;テキストを読むためのウエイト
;表示があるときのみ
SIF TFLAG:15
	WAIT
CALL SET_KOJO_COLOR()

;-------------------------------------------
;奴隷の焦らしのテキスト
;-------------------------------------------
@PALAM_MESSAGE_F
RESETCOLOR
IF CFLAG:Hypno_Orgasm_Denial ;custom code
	PRINTFORMW %CALLNAME:TARGET% is on the verge of orgasm, but the hypnosis won't let %HIM_HER(TARGET)% cum.
ELSE
	PRINTFORMW Just as %CALLNAME:TARGET% approaches %HIS_HER(TARGET)% peak, you stop the caress.
ENDIF
SIF !FLAG:70
	PRINTFORMW %CALLNAME:TARGET% moans in frustration, wiggling %HIS_HER(TARGET)% body trying to get that final push...
CALL SET_KOJO_COLOR()

;-------------------------------------------
;奴隷の絶頂時のテキスト
;-------------------------------------------
@PALAM_MESSAGE_B
RESETCOLOR

CALL TARGET_ORGASM_POWER(TARGET)
LOCAL =  RESULT
LOCAL:1 = RESULT
;NAS: replaced with function above
;;custom code
;LOCAL:1 = MIN(NOWEX:Ｃ絶頂, 1) + MIN(NOWEX:Ｖ絶頂, 1) + MIN(NOWEX:Ａ絶頂, 1) + MIN(NOWEX:Ｂ絶頂, 1) + MIN(NOWEX:Ｍ絶頂, 1) + MIN(NOWEX:射精, 1)
SIF !(LOCAL + NOWEX:放尿)
	RETURN 0
	
IF SHIRAHU(TARGET) && !Qol_Info_Dates_Array:TARGET:Qol_Dates_Orgasm ;qol custom code
	PRINTFORMDW %CALLNAME:TARGET% feels the pressure building inside %HIM_HER(TARGET)%, %HIS_HER(TARGET)% body tightening as %HE_SHE(TARGET)% gets closer to %HIS_HER(TARGET)% first climax.
	PRINTFORML
ENDIF

;絶頂
IF FLAG:70 == 1 ;bugfixes, account for TARGET not having female genitals
	PRINTFORM %CALLNAME:TARGET%'s 
	SIF NOWEX:噴乳
		PRINTFORM %CHECK_PLNOUN(OPPAI_DESCRIPTION_TR(TARGET), "shoot")% \@ NOWEX:噴乳 > 1 ? rivers of # some\@ milk out of %HIS_HER(TARGET)% %NIPPLE_DESCRIPTION(TARGET)%
	;custom code, rewritten for readability
	IF LOCAL >= 5 && HAS_VAGINA(TARGET)
		IF NOWEX:噴乳
			IF NOWEX:射精
				PRINTFORM , %HIS_HER(TARGET)% 
			ELSE
				PRINTFORM  as %HIS_HER(TARGET)% 
			ENDIF
		ENDIF
		PRINTFORM pussy gushes transparent liquid
	ENDIF
	IF NOWEX:射精
		IF NOWEX:噴乳 || LOCAL >= 5
			PRINTFORM , and %HIS_HER(TARGET)% 
		ELSE
			PRINTFORM  
		ENDIF
		PRINTFORM %TINKO(TARGET)% starts firing %SAMEN_DESCRIPTION(TARGET,1)%
		SIF TEQUIP:TARGET:オナホール
			PRINTFORM  inside the %FSYN("onahole")%
	ENDIF
	;Custom code
	SIF !(NOWEX:噴乳 || LOCAL >= 5 || NOWEX:射精)
		PRINT body trembles
	PRINTFORM .
	CALL PRINT_BREAK,,"dc"
	PRINTFORM %HIS_HER(TARGET,1)% 

	;custom code, rewritten for simplicity
	;Ignoring the "no ejaculation" thing
	LOCALS =
	SIF (NOWEX:射精 || NOWEX:Ｃ絶頂) && HAS_PENIS(TARGET)
		LOCALS += @"%TINKO(TARGET)%、"
	SIF NOWEX:Ｃ絶頂 && !HAS_PENIS(TARGET)
		LOCALS += @"clitoris、"
	SIF NOWEX:Ｖ絶頂
		LOCALS += @"vagina、"
	SIF NOWEX:Ａ絶頂
		LOCALS += @"anus、"
	SIF NOWEX:Ｍ絶頂
		LOCALS += @"lips、"
	SIF NOWEX:Ｂ絶頂
		LOCALS += @"%NIPPLE_DESCRIPTION(TARGET,0)%、"
	SIF NOWEX:UOrgasm
		LOCALS += @"urethra、"
	PRINTFORM %LIST_IN_STRING(LOCALS)%
	
	;if multiple orgasms, or lips or nipples (alone)
	PRINTFORM  tremble%COND_STR(@"s", !(LOCAL:1 > 1 || (NOWEX:Ｍ絶頂 && LOCAL:1 == 1) || (NOWEX:Ｂ絶頂 && LOCAL:1 == 1)))% in orgasm as time is stopped...
	CALL PRINT_BREAK,,"dc"

	SIF NOWEX:Ｖ絶頂 && TEQUIP:50 == PLAYER
		PRINTFORML As %HIS_HER(TARGET)% pussy trembles in orgasm it clamps down hard on your %TINKO(PLAYER)%...
	SIF NOWEX:Ａ絶頂 && TEQUIP:51 == PLAYER
		PRINTFORML As %HIS_HER(TARGET)% anus trembles in orgasm it clamps down hard on your %TINKO(PLAYER)%...

	;絶頂とおもらし
	;NAS: Now corresponds to the right traits
	IF LOCAL >= 3 && PeeHold:TARGET:2 < 0
		PRINTFORML The relief causes %CALLNAME:TARGET%'s urethra to relax and %HE_SHE(TARGET)% lets out urine...
	ELSEIF LOCAL >= 3 && GETBIT(nPee,0) && NOWEX:放尿 > 0
		PRINTFORML Unable to withstand the pleasure, %CALLNAME:TARGET% urinates %HIMSELF_HERSELF(TARGET)% silly...
	ENDIF

	;NAS: Scats
	IF LOCAL >= 3 && GETBIT(nScat,1) && NOWEX:Defecation > 0
		PRINTFORML Unable to withstand the pleasure, %CALLNAME:TARGET% defecates %HIMSELF_HERSELF(TARGET)% silly...
	ENDIF

	;絶頂と噴乳
	IF LOCAL >= 3 && NOWEX:噴乳 && TEQUIP:乳首吸い
		PRINTFORML You suck on %CALLNAME:TARGET%'s %NIPPLE_DESCRIPTION(TARGET,1)%, milk overflowing as you swallow...
	ELSEIF NOWEX:噴乳 && TEQUIP:乳首吸い
		PRINTFORML You suck on %CALLNAME:TARGET%'s %NIPPLE_DESCRIPTION(TARGET,1)%, lapping up the milk that comes out...
	ENDIF
	
	;addition custom code
	CALL Add_Milking(TARGET, NOWEX:噴乳)
ELSEIF CFLAG:MASTER:イタズラ == 2
	PRINTFORM The sleeping %CALLNAME:TARGET%'s 
	SIF NOWEX:噴乳
		PRINTFORM %CHECK_PLNOUN(OPPAI_DESCRIPTION_TR(TARGET), "shoot")% \@ NOWEX:噴乳 > 1 ? rivers of # some\@ milk out of %HIS_HER(TARGET)% %NIPPLE_DESCRIPTION(TARGET)%

	;custom code, rewritten for readability
	IF LOCAL >= 5 && HAS_VAGINA(TARGET)
		IF NOWEX:噴乳
			IF NOWEX:射精
				PRINTFORM , %HIS_HER(TARGET)% 
			ELSE
				PRINTFORM  as %HIS_HER(TARGET)% 
			ENDIF
		ENDIF
		PRINTFORM pussy gushes transparent liquid
	ENDIF
	IF NOWEX:射精
		IF NOWEX:噴乳 || LOCAL >= 5
			PRINTFORM , and %HIS_HER(TARGET)% 
		ELSE
			PRINTFORM  
		ENDIF
		PRINTFORM %TINKO(TARGET)% starts firing %SAMEN_DESCRIPTION(TARGET,1)%
		SIF TEQUIP:TARGET:オナホール
			PRINTFORM  inside the %FSYN("onahole")%
	ENDIF
	;Custom code
	SIF !(NOWEX:噴乳 || (LOCAL >= 5 && HAS_VAGINA(TARGET)) || NOWEX:射精)
		PRINT body trembles
	PRINTFORM  in %HIS_HER(TARGET)% sleep...
	CALL PRINT_BREAK,,"dc"

	SIF NOWEX:Ｖ絶頂 && TEQUIP:50 == PLAYER
		PRINTFORML %BREAKENG(@"As %HIS_HER(TARGET)% pussy trembles in orgasm it clamps down hard on your %TINKO(PLAYER)%, as if trying to milk the %SAMEN_DESCRIPTION(PLAYER)% out of it...")%
	SIF NOWEX:Ａ絶頂 && TEQUIP:51 == PLAYER
		PRINTFORML As %HIS_HER(TARGET)% anus trembles in orgasm it clamps down hard on your %TINKO(PLAYER)%, squeezing it...

	;絶頂と噴乳とおもらし 
	CALL PrintMilkAndOmorashi(TARGET) 
ELSEIF  LOCAL:1 > 0
	PRINTFORM %CALLNAME:TARGET%'s 
	;Custom code
	LOCALS = 
	SIF TCVAR:無理矢理
		LOCALS += "eyes are full of tears、"
	SIF NOWEX:噴乳
		LOCALS += @"%COND_STR(@"%HIS_HER(TARGET)% ", TCVAR:無理矢理)%%CHECK_PLNOUN(OPPAI_DESCRIPTION_TR(TARGET), "squirt")% \@ NOWEX:噴乳 > 1 ? rivers of # some\@ milk out of %HIS_HER(TARGET)% %NIPPLE_DESCRIPTION(TARGET)%、"
	SIF LOCAL >= 5 && HAS_VAGINA(TARGET)
		LOCALS += @"%COND_STR(@"%HIS_HER(TARGET)% ", TCVAR:無理矢理||NOWEX:噴乳)%pussy gushes transparent liquid、"
	SIF NOWEX:射精
		LOCALS += @"%COND_STR(@"%HIS_HER(TARGET)% ", TCVAR:無理矢理||NOWEX:噴乳||LOCAL >= 5)%%TINKO(TARGET)% spurts %SAMEN_DESCRIPTION(TARGET,1)% intensely%COND_STR(@" inside the %FSYN("onahole")%", TEQUIP:TARGET:オナホール)%\、"
	SIF LOCAL < 5
		LOCALS += @"%COND_STR(@"%HIS_HER(TARGET)% ", TCVAR:無理矢理||NOWEX:噴乳||LOCAL >= 5||NOWEX:射精)%back curves in orgasm、"
	PRINTFORM %LIST_IN_STRING(LOCALS)%
	PRINTFORM , pleasure filling %HIS_HER(TARGET)% entire body as %HE_SHE(TARGET)% cums%COND_STR(@" together with you", SYNCED_ORGASM(TARGET))%.
	CALL PRINT_BREAK,,"dc"

	IF NOWEX:射精
		CALL Com_Target_Condom ;com custom code
		SIF RESULT
			GOTO SKIP
		IF TEQUIP:PLAYER:Ｖセックス == TARGET
			PRINTFORML %CALLNAME% fires %HIS_HER(TARGET)% %SAMEN_DESCRIPTION(TARGET,1)% into your vagina...
		ELSEIF TEQUIP:PLAYER:Ａセックス == TARGET
			PRINTFORML %CALLNAME% fires %HIS_HER(TARGET)% %SAMEN_DESCRIPTION(TARGET,1)% into your anus...
		ENDIF
	ENDIF
	$SKIP
	;絶頂と噴乳とおもらし
	CALL PrintMilkAndOmorashi(TARGET)
ENDIF

;qol custom code
SIF LOCAL:1 > 0
	CALL QoL_Info_Write(TARGET, Qol_Dates_Orgasm)
IF NOWEX:射精 && !TCVAR:コンドーム
	IF TEQUIP:PLAYER:Ｖセックス == TARGET
		Qol_Info_Count_Array:TARGET:Qol_Count_EjacV += EXP_UP(GETNUM(EXP, "射精経験"), TARGET)
	ELSEIF TEQUIP:PLAYER:Ａセックス == TARGET
		Qol_Info_Count_Array:TARGET:Qol_Count_EjacA += EXP_UP(GETNUM(EXP, "射精経験"), TARGET)
	ELSEIF EXP_UP(GETNUM(EXP, "精飲経験"), PLAYER) ;check player's semen drinking, might be faulty
		Qol_Info_Count_Array:TARGET:Qol_Count_EjacM += EXP_UP(GETNUM(EXP, "射精経験"), TARGET)
	ENDIF
ENDIF
;------------------

;テキストを読むためのウエイト
;表示があるときのみ
IF LOCAL + NOWEX:放尿 > 0
	WAIT
	CALL SET_KOJO_COLOR(TARGET) ;custom code
ENDIF

;-------------------------------------------
;調教者の射精時のテキスト
;-------------------------------------------
@PALAM_MESSAGE_A
VARSET LOCAL
RESETCOLOR

;LOCAL = NOWEX:MASTER:Ｃ絶頂 + NOWEX:MASTER:Ｖ絶頂 + NOWEX:MASTER:Ａ絶頂 + NOWEX:MASTER:Ｂ絶頂 + NOWEX:MASTER:Ｍ絶頂
CALL TARGET_ORGASM_POWER(MASTER,1)
LOCAL = RESULT
IF TCVAR:カウンター行動 == 29 ;com custom stuff
	TCVAR:カウンター行動の派生 = 2
	CALL KOJO_MESSAGE_SEND("COUNTER",2,TARGET,0,0)
	PRINTFORML
ENDIF

IF LOCAL >= 1
	PRINTFORM You
	IF NOWEX:MASTER:SextOrgasm || NOWEX:MASTER:五重絶頂 || NOWEX:MASTER:四重絶頂
		PRINTFORML  cry in drowning ecstasy as the intense pleasure wracks your body.
	ELSE
		PRINTFORM r 
		LOCAL:1 = 0
		;Custom code
		LOCALS = 
		LOCAL:10 = 0
		IF NOWEX:MASTER:Ｃ絶頂
			IF HAS_PENIS(MASTER)
				LOCALS += "penis、"
			ELSE
				LOCALS += "clit、"
			ENDIF
			LOCAL:1 ++
		ENDIF
		IF NOWEX:MASTER:Ｖ絶頂
			SIF LOCAL:1
				PRINT 
			LOCALS += "vagina、"
			LOCAL:1 ++
		ENDIF
		IF NOWEX:MASTER:Ａ絶頂
			SIF LOCAL:1
				PRINT 
			LOCALS += "anus、"
			LOCAL:1 ++
		ENDIF
		IF NOWEX:MASTER:Ｂ絶頂
			SIF LOCAL:1
				PRINT 
			IF TALENT:MASTER:性別 & 1
				LOCALS += "chest、"
			ELSE
				LOCALS += "nipples、"
				LOCAL:10 = 1
			ENDIF
			LOCAL:1 ++
		ENDIF
		IF NOWEX:MASTER:Ｍ絶頂
			SIF LOCAL:1
				PRINT 
			LOCALS += "lips、"
			LOCAL:10 = 1
			LOCAL:1 ++
		ENDIF
		IF NOWEX:MASTER:UOrgasm
			SIF LOCAL:1
				PRINT 
			LOCALS += "urethra、"
			LOCAL:1 ++
		ENDIF
		PRINTFORM %LIST_IN_STRING(LOCALS)%
		IF LOCAL >= 5 && LOCAL:1 >= 3
			PRINTFORML all come at once, the sensation making you blank out...
		ELSE
			PRINTFORML reach%COND_STR(@"es", LOCAL:1 <= 1 && !LOCAL:10)% orgasm, making your body quiver in pleasure...
		ENDIF
	ENDIF
ENDIF
CALL TARGET_ORGASM_POWER(MASTER)
LOCAL = RESULT ; omo shit

;TFLAG:1 射精箇所 (ビット0=コンドーム 1=膣内 2=アナル 3=手淫 4=口淫 5=パイズリ 6=素股 7=足コキ 8=体表 9=アナル奉仕
;20=手淫フェラ 21=パイズリフェラ22=シックスナイン 24=子宮口 25=疑似 26=授乳プレイ

;com custom code 搾乳手コキ
CALL CCOM_DIRECT_EJAC29(TARGET)
SIF RESULT
	RETURN 1
;------------------------------------

;コンドーム
IF TCVAR:PLAYER:コンドーム == 1
	IF FLAG:70 == 1
		PRINTFORML Your %TINKO(PLAYER)% fills the condom with%COND_STR(@" large quantities of", NOWEX:MASTER:射精 > 1)% %SAMEN_DESCRIPTION(PLAYER)%...
		IF GETBIT(TFLAG:1,1)
			SIF ABL:TARGET:膣射中毒
				PRINTFORML %CALLNAME:(TARGET)%'s vagina aches with frustration as it expected to be filled...
		ELSEIF GETBIT(TFLAG:1,2)
			SIF ABL:TARGET:肛射中毒
				PRINTFORML %CALLNAME:(TARGET)%'s anus aches with frustration as it expected to be filled...
		ENDIF
	ELSEIF CFLAG:MASTER:イタズラ == 2
		PRINTFORML Your %TINKO(PLAYER)% fills the condom with%COND_STR(@" large quantities of", NOWEX:MASTER:射精 > 1)% %SAMEN_DESCRIPTION(PLAYER)%...
		PRINTFORM You pull out your %TINKO(PLAYER)% and leave the 
		SELECTCASE NOWEX:MASTER:射精
			CASE IS > 2
				PRINT condom bursting with semen
			CASE 2
				PRINT rather full condom
			;Custom code
			CASEELSE
				PRINT condom
		ENDSELECT
		PRINTFORML  on the side of the %BED_FUTON()%...
	ELSE
		PRINTFORML Your %TINKO_TR(PLAYER)% shoots out%COND_STR(@" large quantities of", NOWEX:MASTER:射精 > 1)% %SAMEN_DESCRIPTION(PLAYER)% into the condom.
		PRINTFORM You pull out your penis and show the 
		SELECTCASE NOWEX:MASTER:射精
			CASE IS > 2
				PRINT condom bursting with semen
			CASE 2
				PRINT rather full condom
			;Custom code
			CASEELSE
				PRINT condom
		ENDSELECT
		PRINTFORML  to %CALLNAME:TARGET%...
		;custom code, during reverse rape
		IF CFLAG:うふふ == 2 && CFLAG:溜まってる度 >= 500
			PRINTFORML %CALLNAME:(TARGET)% smiles at you lasciviously as %HE_SHE(TARGET)% takes the filled condom off your %TINKO(MASTER)%...
			SIF GETBIT(TFLAG:1,1) && ABL:膣射中毒
				PRINTFORML %CALLNAME:(TARGET)%'s vagina aches with frustration as it expected to be filled...
			SIF GETBIT(TFLAG:1,2) && ABL:肛射中毒
				PRINTFORML %CALLNAME:(TARGET)%'s vagina aches with frustration as it expected to be filled...
		;膣内射精
		ELSEIF GETBIT(TFLAG:1,1)
			IF (TALENT:TARGET:思慕 || TALENT:TARGET:恋慕 || TALENT:TARGET:淫乱 || TALENT:TARGET:服従) && CFLAG:TARGET:生解禁 != 2
				PRINTFORML %CALLNAME:(TARGET)% is relieved to see the proof...
			ELSEIF CFLAG:生解禁 != 2 && TALENT:TARGET:SexKnow > -1
				PRINTFORML %CALLNAME:(TARGET)% seems relieved that you used contraception...
			ENDIF
			SIF ABL:膣射中毒
				PRINTFORML %CALLNAME:(TARGET)%'s vagina aches with frustration as it expected to be filled...
		ELSEIF GETBIT(TFLAG:1,2)
			SIF (TALENT:TARGET:思慕 || TALENT:TARGET:恋慕 || TALENT:TARGET:淫乱 || TALENT:TARGET:服従)
				PRINTFORML %CALLNAME:(TARGET)% is relieved to see the proof...
			SIF ABL:肛射中毒
				PRINTFORML %CALLNAME:(TARGET)%'s anus aches with frustration as it expected to be filled...
		ENDIF
		CALL QoL_Info_Write(TARGET, Qol_Dates_Semen, "condom") ;qol custom code
	ENDIF
	IF CFLAG:TARGET:自動ゴム
		IF TEQUIP:MASTER:コンドーム
			PRINTFORML You put on another condom. ({ITEM:コンドーム} Left)
		ELSE
			PRINTFORML You try putting on another condom but you've run out.
		ENDIF
	ENDIF
	WAIT
	;custom code, fix for not applying kojo color after condom
	;RETURN
;外に出すから
ELSEIF (GETBIT(TFLAG:1,1) || GETBIT(TFLAG:1,8)) && TCVAR:外に出すから == 1 ;pullout promise patch
	;relevant to females only, so no pronouns
	IF nPulledOut ;came outside
		IF nPulledOut == 1 ;custom, only describe the pulling out part once
			SELECTCASE RAND:2
			CASE 0
				PRINTFORM Remembering what you have to do in the midst of action, you pull your %TINKO(PLAYER)% out at the last second and shoot \@ NOWEX:MASTER:射精 >1 ? a huge load of%" "% # \@%SAMEN_DESCRIPTION(PLAYER)%, 
			CASE 1
				PRINTFORM Just as you are about to ejaculate, you pull your %TINKO(PLAYER)% out of her %FSYN("vagina",,TARGET)% and release \@ NOWEX:MASTER:射精 >1 ? a large amount of%" "% # \@%SAMEN_DESCRIPTION(PLAYER)%, 
			ENDSELECT
		ELSE
			PRINTFORM Your %TINKO(PLAYER)% continues to shoot out %SAMEN_DESCRIPTION(PLAYER)%, 
		ENDIF
		nPulledOut++

		SELECTCASE ADD_CURRENT_POSE(TARGET) ;position
		CASE 1 ;missionary
			SELECTCASE RAND:3
			CASE 0
				;note - good place to mention pubes
				PRINTFORM %SPLIT_G("covering:spraying all over:landing all over")% %CALLNAME:TARGET%'s smooth %SPLIT_G("crotch:belly")%...
			CASE 1
				PRINTFORM causing it to %SPLIT_G("cover:spray all over:land all over")% %CALLNAME:TARGET%'s face and chest...
			CASE 2
				PRINTFORM which smears %CALLNAME:TARGET%'s underbelly and lets it %FSYN("leak")% down along her %FSYN("slit")%...
			ENDSELECT
		CASE 2 ;doggy
			SELECTCASE RAND:2
			CASE 0
				PRINTFORM soaking %CALLNAME:TARGET%'s %FSYN("tremble:ing")% buttocks in white...
			CASE 1
				PRINTFORM leaving a puddle of milky sticky goo in the crevices of %CALLNAME:TARGET%'s back...
			CASE 2
				PRINTFORM spurting down on the crack of %CALLNAME:TARGET%'s %FSYN("ass")% as it slowly flows down over her anus and vulva...
			ENDSELECT
		CASE 3 ;cowgirl
			SELECTCASE RAND:2
			CASE 0
				PRINTFORM coating %CALLNAME:TARGET%'s nethers with white stickiness...
			CASE 1
				PRINTFORM which flies high in the air and lands on %CALLNAME:TARGET%'s skin as it oozes down between her %FSYN("chest_describe",,TARGET)%...
			ENDSELECT
		CASE 4 ;lotus
			SELECTCASE RAND:2
			CASE 0
				PRINTFORM staining %CALLNAME:TARGET%'s %FSYN("chest_describe",,TARGET)% and stomach white...
			CASE 1
				PRINTFORM which splashes all over %CALLNAME:TARGET%'s face and in her hair...
			ENDSELECT
		CASE 5 ;rev lotus
			;custom - moved the case from cowgirl here because it makes more sense in terms of position
			PRINTFORM drawing an arc as it lands on %CALLNAME:TARGET%'s ass and back, soiling them...
		CASE 6 ;custom, inchling onahole
			PRINTFORM covering %CALLNAME:TARGET%'s tiny frame completely...
		CASEELSE ;custom, changed from rev lotus as a failsafe
			PRINTFORM thick ropes of which make an arc as they fly through the air...
		ENDSELECT
		CALL PRINT_BREAK
	ELSE ;came inside
		SELECTCASE RAND:4
		CASE 0
			PRINTFORML %BREAKENG(@"Too caught up in pounding your hips away and losing yourself in overwhelming pleasure, you end up unwittingly ejaculating inside %CALLNAME:(TARGET)%'s %FSYN("vagina",,TARGET)%...")%
		CASE 1
			PRINTFORML %BREAKENG(@"Just a few more strokes, you thought, a little thrust of your hips is all you need, but ultimately it proved to be too much as what came out of your %TINKO(PLAYER)% wasn't just precum, but your virile seed going straight into %CALLNAME:(TARGET)%'s unprotected %FSYN("vagina",,TARGET)%...")%
		CASE 2
			PRINTFORML %BREAKENG(@"Just as you realize you're on the verge of ejaculation, your %TINKO(PLAYER)% throbs and rages inside %CALLNAME:(TARGET)%'s %FSYN("vagina",,TARGET)%, gushing out its baby batter...")%
		CASE 3
			PRINTFORML %BREAKENG(@"You desperately try to hold back your ejaculation, but all too late and in vain, as the impregnation juice pours directly into %CALLNAME:(TARGET)%'s %FSYN("vagina",,TARGET)% in abundance...")%
		ENDSELECT
		
		PRINTFORM The \@ NOWEX:MASTER:射精 >1 ? vast quantity of%" "%#\@%SAMEN_DESCRIPTION(PLAYER)% you end up releasing 
		IF ESTRUS_CYCLE(TARGET) == 150
			PRINTFORML relentlessly ravages her womb on her most fertile of days...
		ELSEIF ESTRUS_CYCLE(TARGET) == 100
			PRINTFORML makes its way through her canal, targeting her defenseless eggs...
		ELSE
			PRINTFORML invades her womanhood, soaking and swirling inside...
		ENDIF
	ENDIF
;個別処理
;子宮口責め
ELSEIF GETBIT(TFLAG:1,24)
	IF NOWEX:TARGET:Ｖ絶頂
		;---------------------------------
		;時間停止
		;---------------------------------
		IF FLAG:70 == 1
			;---------------------------------
			;時間停止・鬼畜
			;---------------------------------
			IF EXP:MASTER:時姦経験 >= EXP:TARGET:愛情経験 + 50
				;---------------------------------
				;小柄体型
				;---------------------------------
				IF TALENT:TARGET:体型 == -2
					IF EXP:TARGET:子宮口開発経験 >=300
						PRINTFORML %CALLNAME:(TARGET)% has an intense orgasm as %HIS_HER(TARGET)% womb gets penetrated.
						PRINTFORML The walls, folds, and the uterus itself lick you all at once, as if milking you of %SAMEN_DESCRIPTION(PLAYER)%.
						PRINTFORML The %SAMEN_DESCRIPTION(PLAYER,1)% covers every nook and cranny of the womb, making %CALLNAME:(TARGET)% quiver as %HIS_HER(TARGET)% vagina clamps down.
						PRINTFORMW As %CALLNAME:(TARGET)% is frozen in time, you fire \@ NOWEX:MASTER:射精 > 1 ? so much %SAMEN_DESCRIPTION(PLAYER,1)% into %HIS_HER(TARGET)% womb that a noticeable bulge appears on %HIS_HER(TARGET)% abdomen # %SAMEN_DESCRIPTION(PLAYER,1)% into %HIS_HER(TARGET)% womb \@...
					ELSE
						PRINTFORML %CALLNAME:(TARGET)% reaches orgasm as you poke %HIS_HER(TARGET)% cervix.
						PRINTFORML You immediately press your %TINKO(PLAYER)% hard against the mouth of %HIS_HER(TARGET)% womb and begin firing semen.
						PRINTFORML %CALLNAME:(TARGET)%'s small womb gets filled with %SAMEN_DESCRIPTION(PLAYER,1)% in no time.
						PRINTFORMW %HIS_HER(TARGET,1)% smooth opening overflows with your %SAMEN_DESCRIPTION(PLAYER)%...
					ENDIF
				;---------------------------------
				;幼児体型
				;---------------------------------
				ELSEIF TALENT:TARGET:体型 == -1
					PRINTFORML %CALLNAME:(TARGET)% reaches orgasm as you poke %HIS_HER(TARGET)% cervix.
					PRINTFORML As %HIS_HER(TARGET)% vagina clamps down hard, it feels as though %HIS_HER(TARGET)% uterus is licking you, pushing you over the edge.
					PRINTFORML The childlike %CALLNAME:(TARGET)%'s tight insides are filled with your %TINKO(PLAYER)%, and as the glans pushes against the cervix it begins firing its %SAMEN_DESCRIPTION(PLAYER,1)%...
					PRINTFORMW %HIS_HER(TARGET,1)% tiny womb can't hold everything and %SAMEN_DESCRIPTION(PLAYER)% begins pouring out from %HIS_HER(TARGET)% smooth opening...
				;---------------------------------
				;それ以外
				;---------------------------------
				ELSEIF EXP:TARGET:子宮口開発経験 >= 300
					PRINTFORML %CALLNAME:TARGET%'s vagina clamps down hard as %HE_SHE(TARGET)% reaches orgasm despite %HIS_HER(TARGET)% cervix having been wrenched open, squeezing your %TINKO(PLAYER)%.
					PRINTFORML Stimulated by the pleasure, your push your %TINKO(PLAYER)% hard against %HIS_HER(TARGET)% most inner depths, treating it like nothing more than a hole to fill as you stab at %HIS_HER(TARGET)% inner wall...
					PRINTFORMW As time is stopped you pour your %SAMEN_DESCRIPTION(PLAYER,1)% into %CALLNAME:(TARGET)%'s interior, completely ignoring whatever %HE_SHE(TARGET)% might think...
					SIF NOWEX:MASTER:射精 > 1
						PRINTFORMW You fire massive quantities of %SAMEN_DESCRIPTION(PLAYER)% into %HIM_HER(TARGET)%, painting %HIS_HER(TARGET)% womb...
				ELSE
					PRINTFORML As you poke and prod %CALLNAME:(TARGET)%'s uterus %HE_SHE(TARGET)% starts convulsing and clamps down hard.
					PRINTFORML It stops you from pushing further in, but not from grinding your %TINKO(PLAYER)% hard against %HIS_HER(TARGET)% cervix and you begin to cum.
					PRINTFORMW Your %SAMEN_DESCRIPTION(PLAYER,1)% invades %HIS_HER(TARGET)% insides...
					SIF NOWEX:MASTER:射精 > 1
						PRINTFORMW You get drunk on a sense of conquest as your %SAMEN_DESCRIPTION(PLAYER)% begins pouring into %HIM_HER(TARGET)%...
				ENDIF
			;---------------------------------
			;時間停止・通常
			;---------------------------------
			ELSEIF EXP:TARGET:子宮口開発経験 >= 50
				PRINTFORML %CALLNAME:(TARGET)%'s insides reach orgasm and begin to clamp down on your %TINKO(PLAYER)% while time is stopped.
				PRINTFORMW You push hard against %HIS_HER(TARGET)% cervix and begin firing your %SAMEN_DESCRIPTION(PLAYER,1)% into %HIS_HER(TARGET)% womb...
			ELSE
				PRINTFORML %CALLNAME:(TARGET)%'s insides begin clamping down and convulsing as it reaches orgasm.
				PRINTFORML You press your glans hard against %HIS_HER(TARGET)% cervix, only managing to pry it open a tiny bit.
				PRINTFORMW Your %TINKO(PLAYER)% begins pouring %SAMEN_DESCRIPTION(PLAYER)% right into %HIS_HER(TARGET)% womb...
			ENDIF
		;---------------------------------
		;睡眠姦
		;---------------------------------
		ELSEIF CFLAG:MASTER:イタズラ == 2
			;---------------------------------
			;睡眠姦・鬼畜
			;---------------------------------
			IF EXP:TARGET:睡眠姦経験 >= EXP:TARGET:愛情経験 + 50
				;---------------------------------
				;小柄体型
				;---------------------------------
				IF TALENT:TARGET:体型 == -2
					IF EXP:TARGET:子宮口開発経験 >=300
						PRINTFORML %CALLNAME:(TARGET)% has an intense orgasm as %HIS_HER(TARGET)% womb gets penetrated.
						PRINTFORML The walls, folds, and the uterus itself lick you all at once, as if milking you of %SAMEN_DESCRIPTION(PLAYER)%.
						PRINTFORML The %SAMEN_DESCRIPTION(PLAYER,1)% covers every nook and cranny of the womb, making %CALLNAME:(TARGET)% quiver as %HIS_HER(TARGET)% vagina clamps down.
						PRINTFORMW As you lay next to %CALLNAME:(TARGET)%, you fire \@ NOWEX:MASTER:射精 > 1 ? so much %SAMEN_DESCRIPTION(PLAYER,1)% into %HIS_HER(TARGET)% womb that a noticeable bulge appears on %HIS_HER(TARGET)% abdomen # %SAMEN_DESCRIPTION(PLAYER,1)% into %HIS_HER(TARGET)% womb \@...
					ELSE
						PRINTFORML %CALLNAME:(TARGET)% reaches orgasm as you poke %HIS_HER(TARGET)% cervix.
						PRINTFORML You immediately press your %TINKO(PLAYER)% hard against the mouth of %HIS_HER(TARGET)% womb and begin firing semen.
						PRINTFORML %CALLNAME:(TARGET)%'s small womb gets filled with %SAMEN_DESCRIPTION(PLAYER,1)% in no time.
						PRINTFORMW %HIS_HER(TARGET,1)% smooth opening overflows with your %SAMEN_DESCRIPTION(PLAYER)%...
					ENDIF
				;---------------------------------
				;幼児体型
				;---------------------------------
				ELSEIF TALENT:TARGET:体型 == -1
					PRINTFORML %CALLNAME:(TARGET)% reaches orgasm as you poke %HIS_HER(TARGET)% cervix.
					PRINTFORML As %HIS_HER(TARGET)% vagina clamps down hard, it feels as though %HIS_HER(TARGET)% uterus is licking you, pushing you over the edge.
					PRINTFORML The childlike %CALLNAME:(TARGET)%'s tight insides are filled with your %TINKO(PLAYER)%, and as the glans pushes against the cervix it begins firing its %SAMEN_DESCRIPTION(PLAYER,1)%...
					PRINTFORMW %HIS_HER(TARGET,1)% tiny womb can't hold everything and %SAMEN_DESCRIPTION(PLAYER)% begins pouring out from %HIS_HER(TARGET)% smooth opening...
				;---------------------------------
				;それ以外
				;---------------------------------
				ELSEIF EXP:TARGET:子宮口開発経験 >= 300
					PRINTFORML %CALLNAME:TARGET%'s vagina clamps down hard as %HE_SHE(TARGET)% reaches orgasm despite %HIS_HER(TARGET)% cervix having been wrenched open, squeezing your %TINKO(PLAYER)%.
					PRINTFORML Stimulated by the pleasure, your push your %TINKO(PLAYER)% hard against %HIS_HER(TARGET)% most inner depths, treating it like nothing more than a hole to fill as you stab at %HIS_HER(TARGET)% inner wall...
					PRINTFORMW As you lay next to %HIM_HER(TARGET)% you pour your %SAMEN_DESCRIPTION(PLAYER,1)% into %CALLNAME:(TARGET)%'s interior, completely ignoring whatever %HE_SHE(TARGET)% might think...
					SIF NOWEX:MASTER:射精 > 1
						PRINTFORMW You fire massive quantities of %SAMEN_DESCRIPTION(PLAYER)% into %HIM_HER(TARGET)%, painting %HIS_HER(TARGET)% womb...
				ELSE
					PRINTFORML As you poke and prod %CALLNAME:(TARGET)%'s uterus %HE_SHE(TARGET)% starts convulsing and clamps down hard.
					PRINTFORML It stops you from pushing further in, but not from grinding your %TINKO(PLAYER)% hard against %HIS_HER(TARGET)% cervix and you begin to cum.
					PRINTFORMW Your %SAMEN_DESCRIPTION(PLAYER,1)% invades %HIS_HER(TARGET)% insides...
					SIF NOWEX:MASTER:射精 > 1
						PRINTFORMW You get drunk on a sense of conquest as your %SAMEN_DESCRIPTION(PLAYER)% begins pouring into %HIM_HER(TARGET)%...
				ENDIF
			;---------------------------------
			;睡眠姦・通常
			;---------------------------------
			ELSEIF EXP:TARGET:子宮口開発経験 >= 50
				PRINTFORM %CALLNAME:(TARGET)%'s insides reach orgasm and begin to clamp down on your %TINKO(PLAYER)% while %HE_SHE(TARGET)% sleeps.
				PRINTFORMW You push hard against %HIS_HER(TARGET)% cervix and begin firing your %SAMEN_DESCRIPTION(PLAYER,1)% into %HIS_HER(TARGET)% womb...
			ELSE
				PRINTFORML %CALLNAME:(TARGET)%'s insides begin clamping down and convulsing as it reaches orgasm.
				PRINTFORML You press your glans hard against %HIS_HER(TARGET)% cervix, only managing to pry it open a tiny bit.
				PRINTFORMW Your %TINKO(PLAYER)% begins pouring %SAMEN_DESCRIPTION(PLAYER)% right into %HIS_HER(TARGET)% womb...
			ENDIF
		;---------------------------------
		;シラフ
		;---------------------------------
		ELSE
			IF EXP:TARGET:子宮口開発経験 >= 300
				IF TEQUIP:TARGET:体位２ == 1
					PRINTFORML You thrust into %CALLNAME:(TARGET)% will all your strength, as if hungering for %HIS_HER(TARGET)% womb...
					PRINTFORML As the hot %SAMEN_DESCRIPTION(PLAYER)% pours into %HIS_HER(TARGET)% womb, %HE_SHE(TARGET)% quivers as %HE_SHE(TARGET)% reaches orgasm.
					PRINTFORML %CALLNAME:(TARGET)%'s waist begins convulsing hard and %HE_SHE(TARGET)% clings desperately to you.
					PRINTFORML %HIS_HER(TARGET,1)% toes curls from the pleasure piercing %HIS_HER(TARGET)% body as %HE_SHE(TARGET)% willingly welcomes your %SAMEN_DESCRIPTION(PLAYER)%...
					PRINTFORMW You hold %CALLNAME:(TARGET)% close, glued to %HIM_HER(TARGET)% as you pour your %SAMEN_DESCRIPTION(PLAYER)% into %HIM_HER(TARGET)%...
				ELSE
					PRINTFORML %CALLNAME:TARGET% moans and a bit of saliva escapes the corner of %HIS_HER(TARGET)% mouth as %HE_SHE(TARGET)% reaches orgasm.
					PRINTFORML The convulsions and craps mixed with the pressure pushes you over the edge as your %TINKO(PLAYER)% is pushed against %HIS_HER(TARGET)% cervix.
					PRINTFORMW You pour your %SAMEN_DESCRIPTION(PLAYER,1)% directly into %CALLNAME:(TARGET)%'s womb...
					SIF TALENT:TARGET:動物耳 && TEQUIP:TARGET:体位２ == 2
						PRINTFORMW Being bred like an animal fills %CALLNAME:(TARGET)% with instinctual pleasure...
				ENDIF
			ELSE
				PRINTFORML %CALLNAME:TARGET%'s entire body quivers as %HE_SHE(TARGET)% approaches %HIS_HER(TARGET)% peak.
				PRINTFORM You press your %TINKO(PLAYER)% against the convulsing %CALLNAME:TARGET%'s cervix, 
				PRINTFORMW firing load after load of %SAMEN_DESCRIPTION(PLAYER,1)% into %HIS_HER(TARGET)% womb...
			ENDIF
			IF TCVAR:TARGET:無理矢理
				PRINTFORML Despite being unwilling, %CALLNAME:(TARGET)%'s face melts as %HIS_HER(TARGET)% womb gets filled, before coming back to %HIS_HER(TARGET)% senses and trembling with fear...
			ELSE
				IF CFLAG:TARGET:生解禁 < 2 && 妊娠可否(TARGET) && TALENT:TARGET:SexKnow > -1
					PRINTFORMW %HIS_HER(TARGET,1)% head clouded with pleasure, the thought of potentially getting inseminated enters %CALLNAME:TARGET%'s mind...
				ELSE
					PRINTFORMW As %CALLNAME:(TARGET)%'s womb gets filled and %HE_SHE(TARGET)% reaches climax, %HE_SHE(TARGET)% feels a happiness only women can...
				ENDIF
			ENDIF
		ENDIF
	ELSE
		;---------------------------------
		;時間停止
		;---------------------------------
		IF FLAG:70 == 1
			;---------------------------------
				;時間停止・鬼畜
			;---------------------------------
			IF EXP:MASTER:時姦経験 >= EXP:TARGET:愛情経験 + 50
				;---------------------------------
				;小柄体型
				;---------------------------------
				IF TALENT:TARGET:体型 == -2
					IF EXP:TARGET:子宮口開発経験 >=300
						PRINTFORML You push against %CALLNAME:(TARGET)%'s womb and begin ejaculating.
						PRINTFORML The %TINKO(PLAYER)% blocks the %SAMEN_DESCRIPTION(PLAYER)% from going anywhere else by pushing hard against the cervix, and %CALLNAME:(TARGET)%'s young womb gets completely flooded.
						PRINTFORMW As %CALLNAME:(TARGET)% is frozen in time, you fire \@ NOWEX:MASTER:射精 > 1 ? so much %SAMEN_DESCRIPTION(PLAYER,1)% into %HIS_HER(TARGET)% womb that a noticeable bulge appears on %HIS_HER(TARGET)% abdomen # %SAMEN_DESCRIPTION(PLAYER,1)% into %HIS_HER(TARGET)% womb \@...
					ELSE
						PRINTFORML You push the tip of your %TINKO(PLAYER)% hard against %CALLNAME:(TARGET)%'s cervix, causing it to buckle inwards as you ejaculate.
						PRINTFORML %CALLNAME:(TARGET)%'s small and young uterus becomes filled with %SAMEN_DESCRIPTION(PLAYER,1)% instantly.
						PRINTFORMW %HIS_HER(TARGET,1)% smooth opening overflows with your %SAMEN_DESCRIPTION(PLAYER)%...
					ENDIF
				;---------------------------------
				;幼児体型
				;---------------------------------
				ELSEIF TALENT:TARGET:体型 == -1
					PRINTFORML The childlike %CALLNAME:(TARGET)%'s tight insides are filled with your %TINKO(PLAYER)%, and as the glans pushes against the cervix it begins firing its %SAMEN_DESCRIPTION(PLAYER,1)%...
					PRINTFORMW %HIS_HER(TARGET,1)% tiny womb can't hold everything and %SAMEN_DESCRIPTION(PLAYER)% begins pouring out from %HIS_HER(TARGET)% smooth opening...
				;---------------------------------
				;それ以外
				;---------------------------------
				ELSEIF EXP:TARGET:子宮口開発経験 >= 300
					PRINTFORML Your push your %TINKO(PLAYER)% hard against %HIS_HER(TARGET)% most inner depths, treating it like nothing more than a hole to fill as you stab at %HIS_HER(TARGET)% inner wall...
					PRINTFORMW As time is stopped you pour your %SAMEN_DESCRIPTION(PLAYER,1)% into %CALLNAME:(TARGET)%'s interior, completely ignoring whatever %HE_SHE(TARGET)% might think...
				ELSE
					PRINTFORML %CALLNAME:(TARGET)% clamps down hard, stopping you from pushing further in, but not from grinding your %TINKO(PLAYER)% hard against %HIS_HER(TARGET)% cervix as you begin to cum.
					PRINTFORMW Your %SAMEN_DESCRIPTION(PLAYER,1)% invades %HIS_HER(TARGET)%%COND_STR(@" ovulating", CFLAG:TARGET:生理周期 == 7)% insides...
				ENDIF
				SIF NOWEX:MASTER:射精 > 1
					PRINTFORMW You get drunk on a sense of conquest as your %SAMEN_DESCRIPTION(PLAYER)% begins pouring into %HIM_HER(TARGET)%...
			;---------------------------------
				;時間停止・通常
			;---------------------------------
			ELSEIF EXP:TARGET:子宮口開発経験 >= 50
				PRINTFORML You stab your %TINKO(PLAYER)% into %CALLNAME:(TARGET)%'s deepest insides while time is stopped.
				PRINTFORMW You grind against %HIS_HER(TARGET)% cervix and pour %SAMEN_DESCRIPTION(PLAYER,1)% into %HIS_HER(TARGET)% womb...
			ELSE
				PRINTFORML You press your glans hard against %HIS_HER(TARGET)% cervix, only managing to pry it open a tiny bit.
				PRINTFORMW Your %TINKO(PLAYER)% begins pouring %SAMEN_DESCRIPTION(PLAYER)% right into %HIS_HER(TARGET)% womb...
			ENDIF
		;---------------------------------
		;睡眠姦
		;---------------------------------
		ELSEIF CFLAG:MASTER:イタズラ == 2
			;---------------------------------
			;睡眠姦・鬼畜
			;---------------------------------
			IF EXP:TARGET:睡眠姦経験 >= EXP:TARGET:愛情経験 + 50
				;---------------------------------
				;小柄体型
				;---------------------------------
				IF TALENT:TARGET:体型 == -2
					IF EXP:TARGET:子宮口開発経験 >=300
						PRINTFORML You push against %CALLNAME:(TARGET)%'s womb and begin ejaculating.
						PRINTFORML The %TINKO(PLAYER)% blocks the %SAMEN_DESCRIPTION(PLAYER)% from going anywhere else by pushing hard against the cervix, and %CALLNAME:(TARGET)%'s young womb gets completely flooded.
						PRINTFORMW As you lay next to %CALLNAME:(TARGET)%, you fire \@ NOWEX:MASTER:射精 > 1 ? so much %SAMEN_DESCRIPTION(PLAYER,1)% into %HIS_HER(TARGET)% womb that a noticeable bulge appears on %HIS_HER(TARGET)% abdomen # %SAMEN_DESCRIPTION(PLAYER,1)% into %HIS_HER(TARGET)% womb \@...
					ELSE
						PRINTFORML You push the tip of your %TINKO(PLAYER)% hard against %CALLNAME:(TARGET)%'s cervix, causing it to buckle inwards as you ejaculate.
						PRINTFORML %CALLNAME:(TARGET)%'s small and young uterus becomes filled with %SAMEN_DESCRIPTION(PLAYER,1)% instantly.
						PRINTFORMW %HIS_HER(TARGET,1)% smooth opening overflows with your %SAMEN_DESCRIPTION(PLAYER)%...
					ENDIF
				;---------------------------------
				;幼児体型
				;---------------------------------
				ELSEIF TALENT:TARGET:体型 == -1
					PRINTFORML The childlike %CALLNAME:(TARGET)%'s tight insides are filled with your %TINKO(PLAYER)%, and as the glans pushes against the cervix it begins firing its %SAMEN_DESCRIPTION(PLAYER,1)%...
					PRINTFORMW %HIS_HER(TARGET,1)% tiny womb can't hold everything and %SAMEN_DESCRIPTION(PLAYER)% begins pouring out from %HIS_HER(TARGET)% smooth opening...
				;---------------------------------
				;それ以外
				;---------------------------------
				ELSEIF EXP:TARGET:子宮口開発経験 >= 300
					PRINTFORML Your push your %TINKO(PLAYER)% hard against %HIS_HER(TARGET)% most inner depths, treating it like nothing more than a hole to fill as you stab at %HIS_HER(TARGET)% inner wall...
					PRINTFORMW As you lay next to %HIM_HER(TARGET)% you pour your %SAMEN_DESCRIPTION(PLAYER,1)% into %CALLNAME:(TARGET)%'s interior, completely ignoring whatever %HE_SHE(TARGET)% might think...
				ELSE
					PRINTFORML %CALLNAME:(TARGET)% clamps down hard, stopping you from pushing further in, but not from grinding your %TINKO(PLAYER)% hard against %HIS_HER(TARGET)% cervix as you begin to cum.
					PRINTFORMW Your %SAMEN_DESCRIPTION(PLAYER,1)% invades %HIS_HER(TARGET)%%COND_STR(@" ovulating", CFLAG:TARGET:生理周期 == 7)% insides...
				ENDIF
				SIF NOWEX:MASTER:射精 > 1
					PRINTFORMW You get drunk on a sense of conquest as your %SAMEN_DESCRIPTION(PLAYER)% begins pouring into %HIM_HER(TARGET)%...
			;---------------------------------
				;睡眠姦・通常
			;---------------------------------
			ELSEIF EXP:TARGET:子宮口開発経験 >= 50
				PRINTFORML You stab your %TINKO(PLAYER)% into the sleeping %CALLNAME:(TARGET)%'s deepest insides.
				PRINTFORMW You grind against %HIS_HER(TARGET)% cervix and pour %SAMEN_DESCRIPTION(PLAYER,1)% into %HIS_HER(TARGET)% womb...
			ELSE
				PRINTFORML You press your glans hard against %HIS_HER(TARGET)% cervix, only managing to pry it open a tiny bit.
				PRINTFORMW Your %TINKO(PLAYER)% begins pouring %SAMEN_DESCRIPTION(PLAYER)% right into %HIS_HER(TARGET)% womb...
			ENDIF
		;---------------------------------
		;シラフ
		;---------------------------------
		ELSE
			IF EXP:TARGET:子宮口開発経験 >= 300
				IF TEQUIP:TARGET:体位２ == 1
					PRINTFORML You thrust into %CALLNAME:(TARGET)% will all your strength, as if hungering for %HIS_HER(TARGET)% womb...
					PRINTFORML %HIS_HER(TARGET,1)% toes curls from the pleasure piercing %HIS_HER(TARGET)% body as %HE_SHE(TARGET)% willingly welcomes your %SAMEN_DESCRIPTION(PLAYER)%...
					PRINTFORMW You hold %CALLNAME:(TARGET)% close, glued to %HIM_HER(TARGET)% as you pour your %SAMEN_DESCRIPTION(PLAYER)% into %HIM_HER(TARGET)%...
				ELSE
					PRINTFORML You can't hold back anymore, and you're forced you over the edge as your %TINKO(PLAYER)% is pushed against %HIS_HER(TARGET)% cervix.
					PRINTFORMW You pour your %SAMEN_DESCRIPTION(PLAYER,1)% directly into %CALLNAME:(TARGET)%'s womb...
				ENDIF
			ELSE
				PRINTFORM You push your %TINKO(PLAYER)% against %CALLNAME:(TARGET)%'s cervix, 
				PRINTFORMW pouring your %SAMEN_DESCRIPTION(PLAYER,1)% inside %HIS_HER(TARGET)% uterus...
			ENDIF
			IF TCVAR:TARGET:無理矢理
				PRINTFORML %CALLNAME:(TARGET)% trembles with fear as %HE_SHE(TARGET)% feels %HIS_HER(TARGET)% womb get flooded with semen...
			ELSEIF TALENT:TARGET:恋慕
				PRINTFORMW As %CALLNAME:TARGET%'s womb gets flooded %HE_SHE(TARGET)% feels a happiness only women can...
			ELSEIF CFLAG:TARGET:生解禁 < 2 && 妊娠可否(TARGET) && TALENT:TARGET:SexKnow > -1
				PRINTFORMW The thought of potentially getting inseminated enters %CALLNAME:TARGET%'s mind...
			ENDIF
		ENDIF
	ENDIF
	CALL POISONBODY_MESSAGE(24)
	IF SHIRAHU(TARGET)
		CALL QoL_Info_Write(TARGET, Qol_Dates_Semen) ;qol custom code
		CALL QoL_Info_Write(TARGET, Qol_Dates_Creampie) ;qol custom code
	ENDIF
;シックスナイン
ELSEIF GETBIT(TFLAG:1,22)
	IF NOWEX:TARGET:Ｃ絶頂 || NOWEX:TARGET:Ｍ絶頂
		IF FLAG:70 == 1
			IF NOWEX:TARGET:Ｃ絶頂 && NOWEX:TARGET:Ｍ絶頂
				PRINTFORML %CALLNAME:(TARGET)%'s \@ GETBIT(TALENT:TARGET:性別,1) ? %TINKO((TARGET))% # clitoris \@ and mouth tremble as %HE_SHE(TARGET)% reaches climax.
			ELSEIF NOWEX:TARGET:Ｃ絶頂 && !NOWEX:TARGET:Ｍ絶頂
				PRINTFORML %CALLNAME:(TARGET)%'s \@ GETBIT(TALENT:TARGET:性別,1) ? %TINKO((TARGET))% # clitoris \@ trembles as %HE_SHE(TARGET)% reaches climax.
			ELSEIF !NOWEX:TARGET:Ｃ絶頂 && NOWEX:TARGET:Ｍ絶頂
				PRINTFORML %CALLNAME:(TARGET)%'s mouth trembles as %HE_SHE(TARGET)% reaches climax.
			ENDIF
			IF NOWEX:TARGET:射精 && NOWEX:MASTER:射精
				PRINTFORMW You and %CALLNAME:(TARGET)% ejaculate into each other's mouths while time is stopped.
			ELSEIF NOWEX:TARGET:射精
				IF HAS_PENIS(MASTER)
					PRINTFORML %CALLNAME:(TARGET)%'s %TINKO((TARGET))% twitches and jumps in your mouth as %HE_SHE(TARGET)% cums while time is stopped.
					PRINTFORMW The %SAMEN_DESCRIPTION((TARGET),1)% fills your mouth as your %TINKO(PLAYER)% fills %HIS_HERS(TARGET)%...
				ELSE
					PRINTFORMW %CALLNAME:(TARGET)%'s %TINKO((TARGET))% twitches and jumps in your mouth as %HE_SHE(TARGET)% cums while time is stopped...
				ENDIF
			ELSE
				IF HAS_PENIS(TARGET)
					PRINTFORM You keep time stopped while sucking on %CALLNAME:(TARGET)%'s penis 
				ELSE
					PRINTFORM You keep time stopped while licking %CALLNAME:(TARGET)%'s slit 
				ENDIF
				PRINTFORML as you ejaculate into %HIS_HER(TARGET)% mouth...
				PRINTFORMW You suck on the twitching part while pushing your %TINKO(PLAYER)% into the back of %HIS_HER(TARGET)% throat and begin pouring in your %SAMEN_DESCRIPTION(PLAYER,1)%...
			ENDIF
		ELSE
			IF NOWEX:TARGET:Ｃ絶頂 && NOWEX:TARGET:Ｍ絶頂
				PRINTFORML %CALLNAME:(TARGET)%'s \@ GETBIT(TALENT:TARGET:性別,1) ? %TINKO((TARGET))% # clitoris \@ and mouth tremble as %HE_SHE(TARGET)% reaches climax.
			ELSEIF NOWEX:TARGET:Ｃ絶頂 && !NOWEX:TARGET:Ｍ絶頂
				PRINTFORML %CALLNAME:(TARGET)%'s \@ GETBIT(TALENT:TARGET:性別,1) ? %TINKO((TARGET))% # clitoris \@ trembles as %HE_SHE(TARGET)% reaches climax.
			ELSEIF !NOWEX:TARGET:Ｃ絶頂 && NOWEX:TARGET:Ｍ絶頂
				PRINTFORML %CALLNAME:(TARGET)%'s mouth trembles as %HE_SHE(TARGET)% reaches climax.
			ENDIF
			IF NOWEX:TARGET:射精 && NOWEX:MASTER:射精
				PRINTFORML You and %CALLNAME:(TARGET)% ejaculate into each other's mouths simultaneously...
				PRINTFORMW You two take the other's penis in your mouths to the base, ensuring the other one feels good...
			ELSEIF NOWEX:TARGET:射精
				IF HAS_PENIS(MASTER)
					PRINTFORML %CALLNAME:(TARGET)% ejaculates before you do...
					PRINTFORMW The %SAMEN_DESCRIPTION((TARGET),1)% fills your mouth as your %TINKO(PLAYER)% fills %HIS_HERS(TARGET)%...
				ELSE
					PRINTFORMW %CALLNAME:(TARGET)% ejaculates inside your mouth...
				ENDIF
			ELSE
				IF HAS_PENIS(TARGET)
					PRINTFORM You suck on %CALLNAME:(TARGET)%'s penis 
				ELSE
					PRINTFORM You lick %CALLNAME:(TARGET)%'s slit 
				ENDIF
				PRINTFORML as you ejaculate into %HIS_HER(TARGET)% mouth...
				PRINTFORMW %HIS_HER(TARGET,1)% lower half and mouth twitch from the stimulation and %HE_SHE(TARGET)% slurps while sucking down the %SAMEN_DESCRIPTION(PLAYER)%...
			ENDIF
		ENDIF
	ELSE
		IF FLAG:70 == 1
			IF NOWEX:TARGET:射精 && NOWEX:MASTER:射精
				PRINTFORMW You and %CALLNAME:(TARGET)% ejaculate into each other's mouths while time is stopped.
			ELSEIF NOWEX:TARGET:射精
				IF HAS_PENIS(TARGET)
					PRINTFORML %CALLNAME:(TARGET)%'s %TINKO((TARGET))% twitches and jumps in your mouth as %HE_SHE(TARGET)% cums while time is stopped.
					PRINTFORMW The %SAMEN_DESCRIPTION((TARGET),1)% fills your mouth as your %TINKO(PLAYER)% fills %HIS_HERS(TARGET)%...
				ELSE
					PRINTFORMW %CALLNAME:(TARGET)%'s %TINKO((TARGET))% twitches and jumps in your mouth as %HE_SHE(TARGET)% cums while time is stopped...
				ENDIF
			ELSE
				IF HAS_PENIS(TARGET)
					PRINTFORM You keep time stopped while sucking on %CALLNAME:(TARGET)%'s penis 
				ELSE
					PRINTFORM You keep time stopped while licking %CALLNAME:(TARGET)%'s slit 
				ENDIF
				PRINTFORML as you ejaculate into %HIS_HER(TARGET)% mouth...
				PRINTFORMW You suck on the twitching part while pushing your %TINKO(PLAYER)% into the back of %HIS_HER(TARGET)% throat and begin pouring in your %SAMEN_DESCRIPTION(PLAYER,1)%...
			ENDIF
		ELSE
			IF NOWEX:TARGET:射精 && NOWEX:MASTER:射精
				PRINTFORML You and %CALLNAME:(TARGET)% ejaculate into each other's mouths simultaneously...
				PRINTFORMW You two take the other's penis in your mouths to the base, ensuring the other one feels good...
			ELSEIF NOWEX:TARGET:射精
				IF HAS_PENIS(MASTER)
					PRINTFORML %CALLNAME:(TARGET)% ejaculates before you do...
					PRINTFORMW The %SAMEN_DESCRIPTION((TARGET),1)% fills your mouth as your %TINKO(PLAYER)% fills %HIS_HERS(TARGET)%...
				ELSE
					PRINTFORMW %CALLNAME:(TARGET)% ejaculates inside your mouth...
				ENDIF
			ELSE
				IF HAS_PENIS(TARGET)
					PRINTFORM You suck on %CALLNAME:(TARGET)%'s penis 
				ELSE
					PRINTFORM You lick %CALLNAME:(TARGET)%'s slit 
				ENDIF
				PRINTFORML as you ejaculate into %HIS_HER(TARGET)% mouth...
				PRINTFORMW %HIS_HER(TARGET,1)% lower half twitches from the stimulation and %HE_SHE(TARGET)% slurps while sucking down the %SAMEN_DESCRIPTION(PLAYER)%...
			ENDIF
		ENDIF
	ENDIF
	CALL POISONBODY_MESSAGE(22)
	SIF SHIRAHU(TARGET)
		CALL QoL_Info_Write(TARGET, Qol_Dates_Semen, "69") ;qol custom code
;授乳プレイ
ELSEIF GETBIT(TFLAG:1,26)
	PRINTFORML You press your mouth harder against %CALLNAME:TARGET%'s %NIPPLE_DESCRIPTION(TARGET)% as you start to cum, %TNAME(TARGET)% working your %TINKO(PLAYER)% even faster.
	
	IF ABL:TARGET:精液中毒 >= 5 && ABL:TARGET:技巧 > 3
		PRINTFORML There is a look of pure ecstasy on %CALLNAME:TARGET%'s face as %HE_SHE(TARGET)% \@ NOWEX:PLAYER:射精 > 1 ? completely covers %HIM_HER(TARGET)%self in# showers %HIM_HER(TARGET)%self with \@ your %SAMEN_DESCRIPTION(PLAYER)%...
	ELSEIF NOWEX:PLAYER:射精 == 1
		PRINTFORML %CALLNAME:TARGET% showers %HIM_HER(TARGET)%self in your %SAMEN_DESCRIPTION(PLAYER)%...
	ELSE
		PRINTFORML %CALLNAME:(TARGET)%'s hand and face get \@ NOWEX:PLAYER:射精 > 1 ? completely covered in # showered by \@ your %SAMEN_DESCRIPTION(PLAYER)%...
	ENDIF 
	PRINTFORM %CALLNAME:(TARGET)% 
	IF NOWEX:TARGET:Ｂ絶頂
		PRINTFORM reaches climax from %HIS_HER(TARGET)% nipples at the same time, working you with loving caress until your %TINKO(PLAYER)% stops twitching...
	ELSE
		PRINTFORM continues working you, lovingly caressing your %TINKO(PLAYER)% until it stops twitching...
	ENDIF
	;噴乳は地の文に任せた
	PRINTFORMW 
	CALL POISONBODY_MESSAGE(26)
	SIF SHIRAHU(TARGET)
		CALL QoL_Info_Write(TARGET, Qol_Dates_Semen) ;qol custom code
;パイズリフェラ
ELSEIF GETBIT(TFLAG:1,21)
	IF NOWEX:TARGET:Ｂ絶頂 || NOWEX:TARGET:Ｍ絶頂
		IF FLAG:70 == 1
			PRINTFORML You use %CALLNAME:(TARGET)%'s %OPPAI_DESCRIPTION_TR((TARGET))% to squeeze your %TINKO(PLAYER)% and you feel your %SAMEN_DESCRIPTION(PLAYER,1)% rising...
			IF NOWEX:TARGET:Ｂ絶頂 && !NOWEX:TARGET:Ｍ絶頂
				PRINTFORM \@ TALENT:TARGET:バストサイズ >= 0 ? You wrap %HIS_HER(TARGET)% %OPPAI_DESCRIPTION_TR((TARGET))% around your %TINKO(PLAYER)%  # You rub your %TINKO(PLAYER)% against %HIS_HER(TARGET)% %OPPAI_DESCRIPTION_TR((TARGET))% \@
			ELSEIF !NOWEX:TARGET:Ｂ絶頂 && NOWEX:TARGET:Ｍ絶頂
				PRINTFORM You hold the glans of your %TINKO(PLAYER)% in %HIS_HER(TARGET)% mouth
			ELSE
				PRINTFORM \@ TALENT:TARGET:バストサイズ >= 0 ? You wrap %HIS_HER(TARGET)% %OPPAI_DESCRIPTION_TR((TARGET))% around your %TINKO(PLAYER)%  # You rub your %TINKO(PLAYER)% against %HIS_HER(TARGET)% %OPPAI_DESCRIPTION_TR((TARGET))% \@ while 
				PRINTFORM holding the glans of your %TINKO(PLAYER)% in %HIS_HER(TARGET)% mouth
			ENDIF
			PRINTFORML  as you cum.
		ELSE
			PRINTFORML %CALLNAME:(TARGET)%'s %OPPAI_DESCRIPTION_TR((TARGET))% squeezes your %TINKO(PLAYER)% and you feel your %SAMEN_DESCRIPTION(PLAYER,1)% rising...
			IF NOWEX:TARGET:Ｂ絶頂 && !NOWEX:TARGET:Ｍ絶頂
				PRINTFORM \@ TALENT:TARGET:バストサイズ >= 0 ? %HE_SHE(TARGET,1)% wraps %HIS_HER(TARGET)% %OPPAI_DESCRIPTION_TR((TARGET))% around your %TINKO(PLAYER)%  # %HE_SHE(TARGET,1)% rubs your %TINKO(PLAYER)% against %HIS_HER(TARGET)% %OPPAI_DESCRIPTION_TR((TARGET))% \@
			ELSEIF !NOWEX:TARGET:Ｂ絶頂 && NOWEX:TARGET:Ｍ絶頂
				PRINTFORM %HE_SHE(TARGET,1)% holds the glans of your %TINKO(PLAYER)% in %HIS_HER(TARGET)% mouth
			ELSE
				PRINTFORM \@ TALENT:TARGET:バストサイズ >= 0 ? %HE_SHE(TARGET,1)% wraps %HIS_HER(TARGET)% %OPPAI_DESCRIPTION_TR((TARGET))% around your %TINKO(PLAYER)%  # %HE_SHE(TARGET,1)% rubs your %TINKO(PLAYER)% against %HIS_HER(TARGET)% %OPPAI_DESCRIPTION_TR((TARGET))% \@ while 
				PRINTFORM while holding the glans of your %TINKO(PLAYER)% in %HIS_HER(TARGET)% mouth
			ENDIF
			PRINTFORML , entranced as you ejaculate...
		ENDIF
	ELSE
		IF FLAG:70 == 1
			PRINTFORMW You use %CALLNAME:(TARGET)%'s %OPPAI_DESCRIPTION_TR((TARGET))% to squeeze your %TINKO(PLAYER)% and you feel your %SAMEN_DESCRIPTION(PLAYER,1)% rising and you fire all over %CALLNAME:(TARGET)%...
		ELSE
			PRINTFORMW %CALLNAME:(TARGET)% sandwiches your %TINKO(PLAYER)% between %HIS_HER(TARGET)% %OPPAI_DESCRIPTION_TR((TARGET))% and you feel your %SAMEN_DESCRIPTION(PLAYER,1)% rising and you fire all over %CALLNAME:(TARGET)%...
		ENDIF
	ENDIF
	CALL POISONBODY_MESSAGE(21)
	SIF SHIRAHU(TARGET)
		CALL QoL_Info_Write(TARGET, Qol_Dates_Semen) ;qol custom code
;手淫フェラ
ELSEIF GETBIT(TFLAG:1,20)
	IF NOWEX:TARGET:Ｍ絶頂
		PRINTFORML %CALLNAME:(TARGET)%'s mouth twitches with pleasure as you begin firing %SAMEN_DESCRIPTION(PLAYER,1)% into %HIS_HER(TARGET)% throat...
	ELSE
		PRINTFORML You fire your %SAMEN_DESCRIPTION(PLAYER,1)% into %CALLNAME:(TARGET)%'s throat...
	ENDIF
	;精飲
	IF EXP_UP(15,TARGET) == NOWEX:PLAYER:射精 * 3
		PRINTFORML %CALLNAME:(TARGET)% holds the glans firmly in %HIS_HER(TARGET)% mouth, trying not to choke on the force of the ejaculation.
		PRINTFORMW %HE_SHE(TARGET,1)% uses %HIS_HER(TARGET)% other hand to stroke the length, as if trying to squeeze out every last bit...
		CALL POISONBODY_MESSAGE(20)
		;RETURN 1
	ELSE
		IF TCVAR:TARGET:精飲経験 == 0
			PRINTFORML %CALLNAME:(TARGET)% is surprised as the %TINKO(PLAYER)% jumps out of %HIS_HER(TARGET)% mouth.
			PRINTFORMW %HIS_HER(TARGET,1)% face gets covered in %SAMEN_DESCRIPTION(PLAYER)% - soiling it for the first time - and a bit landing in %HIS_HER(TARGET)% mouth gives %HIM_HER(TARGET)% the first taste of semen.
		ELSE
			PRINTFORML %CALLNAME:(TARGET)% pushes %HIS_HER(TARGET)% tongue against the head, prodding the urethra.
			PRINTFORMW %HE_SHE(TARGET,1)% uses %HIS_HER(TARGET)% other hand to stroke the length, as if trying to squeeze out every last bit...
		ENDIF
			CALL POISONBODY_MESSAGE(3)
		;RETURN 2
	ENDIF
	SIF SHIRAHU(TARGET)
		CALL QoL_Info_Write(TARGET, Qol_Dates_Semen) ;qol custom code
;部位別処理
;膣内
ELSEIF TCVAR:PLAYER:12 == 1
	IF TFLAG:50 == 13 && TARGET != TFLAG:152
		;custom code, TFLAG:152 is the other partner in duo actions
		PRINTFORM You fire %SAMEN_DESCRIPTION(PLAYER,1)% into %CALLNAME:(TFLAG:152)%'s insides, before pulling out your %TINKO(PLAYER)% and inserting it into %CALLNAME:TARGET% while still ejaculating.
		CALL PRINT_BREAK
		PRINTFORML You stay inside %CALLNAME:(TARGET)% without pulling out, pumping in every last drop...
		SIF NOWEX:TARGET:Ｖ絶頂
			PRINTFORML As semen and pleasure gets poured into %HIM_HER(TARGET)% %HIS_HER(TARGET)% entire body shakes as %HE_SHE(TARGET)% reaches climax.
	;時間停止or睡眠姦
	ELSEIF FLAG:70 || CFLAG:MASTER:イタズラ == 2
		IF TFLAG:50 == 7
			IF (FLAG:70 && EXP:MASTER:時姦経験 >= EXP:TARGET:愛情経験 + 50) || (CFLAG:MASTER:イタズラ == 2 && EXP:TARGET:睡眠姦経験 >= EXP:TARGET:愛情経験 + 50)
				IF EXP:TARGET:子宮口開発経験 >= 50
					PRINTFORML You don't resist as you feel an orgasm well up in your %TINKO(PLAYER)% and you drive it deep inside...
					PRINTFORMW \@ FLAG:70 ? As time is stopped %CALLNAME:TARGET% is completely unaware as you  # You hold the sleeping %CALLNAME:(TARGET)% close,\@ press hard against %HIS_HER(TARGET)% uterus, and begin pouring in your %SAMEN_DESCRIPTION(PLAYER,1)%...
				ELSE
					PRINTFORML \@ FLAG:70 ? As time is stopped %CALLNAME:TARGET% is completely unaware as you  # You hold the sleeping %CALLNAME:(TARGET)% close and\@ push deep inside %HIS_HER(TARGET)% vagina...
					PRINTFORMW You unload all your %SAMEN_DESCRIPTION(PLAYER,1)% into %HIS_HER(TARGET)% uterus without any hesitation...
				ENDIF
			ELSEIF EXP:TARGET:子宮口開発経験 >= 50
				PRINTFORML \@ FLAG:70 ? As time is stopped %CALLNAME:TARGET% is completely unaware as you  # You hold the sleeping %CALLNAME:(TARGET)% close and\@ push inside %HIS_HER(TARGET)% vagina...
				PRINTFORMW You grind against %HIS_HER(TARGET)% cervix and pour %SAMEN_DESCRIPTION(PLAYER,1)% into %HIS_HER(TARGET)% womb...
			ELSE
				PRINTFORML \@ FLAG:70 ? As time is stopped %CALLNAME:TARGET% is completely unaware as you  # You hold the sleeping %CALLNAME:(TARGET)% close and\@ push against the back of %HIS_HER(TARGET)% vagina...
				PRINTFORMW You pour your %SAMEN_DESCRIPTION(PLAYER,1)% into %HIS_HER(TARGET)% uterus...
			ENDIF
			SIF NOWEX:TARGET:Ｖ絶頂
				PRINTFORMW Despite \@ FLAG:70 ? time being stopped # being asleep \@, %CALLNAME:TARGET% reaches orgasm and %HIS_HER(TARGET)% vagina clamps down even more, as if hungry for your %SAMEN_DESCRIPTION(PLAYER,1)%...
		ELSE
			IF (FLAG:70 && EXP:MASTER:時姦経験 >= EXP:TARGET:愛情経験 + 50) || (CFLAG:MASTER:イタズラ == 2 && EXP:TARGET:睡眠姦経験 >= EXP:TARGET:愛情経験 + 50)
				IF TALENT:TARGET:体型 == -2
					SIF NOWEX:TARGET:Ｖ絶頂
						PRINTFORML %CALLNAME:(TARGET)%'s body reaches climax as %HIS_HER(TARGET)% tiny womb gets gouged out...
					IF EXP:TARGET:子宮口開発経験 >= 50
						PRINTFORML You drive your %TINKO(PLAYER)% forcibly into %CALLNAME:(TARGET)%'s tiny body, pushing all the way to the base, as you begin pouring in your %SAMEN_DESCRIPTION(PLAYER,1)%...
						PRINTFORML %HIS_HER(TARGET,1)% small body gets overfilled with your %SAMEN_DESCRIPTION(PLAYER)%, and it flows down from %HIS_HER(TARGET)% prematurely developed womb...
						PRINTFORMW You enjoy the pressure the immature genitalia offers, and make sure you pump in every last drop...
					ELSE
						PRINTFORML You drive your %TINKO(PLAYER)% forcibly into %CALLNAME:(TARGET)%'s tiny body, pushing all the way to the base, as you begin pouring in your %SAMEN_DESCRIPTION(PLAYER,1)%...
						PRINTFORMW %HIS_HER(TARGET,1)% small body gets overfilled with your %SAMEN_DESCRIPTION(PLAYER)%, and it flows out of %HIM_HER(TARGET)%...
					ENDIF
				ELSEIF EXP:TARGET:子宮口開発経験 >= 50
					SIF NOWEX:TARGET:Ｖ絶頂
						PRINTFORML %CALLNAME:(TARGET)%'s body reaches climax and %HE_SHE(TARGET)% begins clamping down hard, as if trying to milk semen from you.
					PRINTFORML You unload all your %SAMEN_DESCRIPTION(PLAYER,1)% into the back of \@NOWEX:TARGET:Ｖ絶頂?%HIS_HER(TARGET)%#%CALLNAME:(TARGET)%'s\@ vagina without any hesitation...
					PRINTFORML Your %TINKO(PLAYER)% unloads the %SAMEN_DESCRIPTION(PLAYER)% into %HIS_HER(TARGET)% vagina cum cumtoilet, and it eagerly sucks up the %SAMEN_DESCRIPTION(PLAYER)%, pressing hard on the twitching %TINKO(PLAYER)%...
					PRINTFORMW You decide to use this『Hole It Feels Good To Cum Inside』to sate your sexual hunger...
				ELSE
					SIF NOWEX:TARGET:Ｖ絶頂
						PRINTFORML %CALLNAME:(TARGET)% climaxes, and %HIS_HER(TARGET)% vagina wriggles and clenches around your %TINKO(PLAYER)%.
					IF EX:TARGET:処女喪失チェック
						PRINTFORML You ejaculate relentlessly into \@NOWEX:TARGET:Ｖ絶頂?the#%CALLNAME:(TARGET)%'s\@ vagina, marking and polluting it with your semen to your heart's content, robbing it of whatever purity it still had.
					ELSE
						PRINTFORML You don't hesitate to use \@NOWEX:TARGET:Ｖ絶頂?%HIS_HER(TARGET)%#%CALLNAME:(TARGET)%'s\@ vagina as a meet onahole and you cum inside it.
					ENDIF
					PRINTFORML You decide to use%COND_STR(@" the sleeping", !FLAG:70)% %CALLNAME:TARGET%'s vagina in place of an onahole and cum inside without a care in the world%COND_STR(@" while time is stopped", FLAG:70)%...
				ENDIF
			ELSEIF TALENT:TARGET:体型 == -2
				SIF NOWEX:TARGET:Ｖ絶頂
					PRINTFORML %CALLNAME:(TARGET)%'s body reaches climax as %HIS_HER(TARGET)% tiny womb gets gouged out...
				PRINTFORML \@ FLAG:70 ? As time is stopped %CALLNAME:TARGET% is completely unaware as   # You hold the sleeping %CALLNAME:(TARGET)% close and \@ begin ejaculating into %HIS_HER(TARGET)% vagina.
				PRINTFORMW %HIS_HER(TARGET,1)% tiny body is filled with your %SAMEN_DESCRIPTION(PLAYER,1)% in no time, and it begins spilling out...
			ELSEIF EXP:TARGET:子宮口開発経験 >= 50
				SIF NOWEX:TARGET:Ｖ絶頂
					PRINTFORML %CALLNAME:(TARGET)%'s body reaches climax as you batter against %HIS_HER(TARGET)% uterus...
				PRINTFORML You begin ejaculating into the back of \@NOWEX:TARGET:Ｖ絶頂?%HIS_HER(TARGET)%#%CALLNAME:(TARGET)%'s\@ vagina.
				PRINTFORMW %COND_STR(@"the sleeping ", !FLAG:70)%%CALLNAME:(TARGET)%'s uterus clamps down on your glans, as if trying to milk your %SAMEN_DESCRIPTION(PLAYER)%%COND_STR(@" while time is stopped", FLAG:70)%...
			ELSE
				SIF NOWEX:TARGET:Ｖ絶頂
					PRINTFORML You notice %CALLNAME:(TARGET)% reaching climax as %HIS_HER(TARGET)% vagina clamps down hard on your %TINKO(PLAYER)%.
				PRINTFORML \@ NOWEX:TARGET:Ｖ絶頂 ? You cum too and # You \@ begin ejaculating into \@NOWEX:TARGET:Ｖ絶頂?%HIM_HER(TARGET)%#%CALLNAME:(TARGET)%\@.
				PRINTFORMW \@ FLAG:70 ? Being frozen in time,  # The sleeping \@ %CALLNAME:(TARGET)% can't put up any resistance as %HIS_HER(TARGET)% womb gets basted with %SAMEN_DESCRIPTION(PLAYER)%...
			ENDIF
		ENDIF
	;通常
	ELSE
		IF NOWEX:TARGET:Ｖ絶頂
			PRINTFORML %CALLNAME:(TARGET)%'s entire body quivers, and %HE_SHE(TARGET)% lets out a shrill moan as %HE_SHE(TARGET)% reaches %HIS_HER(TARGET)% peak.
			PRINTFORML %HIS_HER(TARGET,1)% vagina clamps down on your %TINKO(PLAYER)%, massaging and squeezing it with full force, trying to milk every drop of semen from it.
		ENDIF
		IF TFLAG:50 == 7
			PRINTFORML You push your %TINKO(PLAYER)% into the back of %CALLNAME:(TARGET)% vagina.
			PRINTFORMW You pour your %SAMEN_DESCRIPTION(PLAYER,1)% right into %HIS_HER(TARGET)% womb%COND_STR(@" despite it being a dangerous day", CFLAG:TARGET:900 == 7)%...
			SIF EXP:TARGET:子宮口開発経験 >= 300
				PRINTFORMW Having memorized the taste of your %SAMEN_DESCRIPTION(PLAYER)%, the cervix opens wide, drinking greedily of the %SAMEN_DESCRIPTION(PLAYER,1)%...
		ELSE
			PRINTFORML You fire your %SAMEN_DESCRIPTION(PLAYER,1)% into %CALLNAME:(TARGET)%'s insides...
			IF CFLAG:TARGET:900 == 7
				PRINTFORMW The ovulating folds wriggle and worm, trying to get your semen as deep inside as possible...
			ELSE
				PRINTFORMW %CALLNAME:(TARGET)% twitches and pulsates rhythmically around the %TINKO(PLAYER)% as it continues to ejaculate...
			ENDIF
		ENDIF
		SIF TALENT:TARGET:動物耳 && TEQUIP:TARGET:体位 == 2 && NOWEX:TARGET:Ｖ絶頂 ;doggy mating
			PRINTFORML %CALLNAME:(TARGET)%'s female instinct awakens and %HE_SHE(TARGET)% pushes %HIS_HER(TARGET)% %HIP((TARGET))% against you, not wanting even a drop of semen to go to waste...
		IF ABL:TARGET:膣射中毒 > 3
			SIF NOWEX:TARGET:Ｖ絶頂
				PRINTFORML %CALLNAME:(TARGET)% reaches climax as semen pours into %HIM_HER(TARGET)%, and %HE_SHE(TARGET)% drools in ecstasy...
			PRINTFORML %BREAKENG(@"The sensation of sperm flooding %CALLNAME:(TARGET)%'s vagina causes %HIM_HER(TARGET)% to let out a moan as %HIS_HER(TARGET)% face melts just from the pleasure of being ejaculated inside of alone.")%
		ELSEIF TCVAR:TARGET:無理矢理
			PRINTFORML %BREAKENG(@"\@ NOWEX:TARGET:Ｖ絶頂 ? For a moment, %CALLNAME:(TARGET)%'s face is absorbed in pleasure as %HE_SHE(TARGET)% reaches climax, before %HE_SHE(TARGET)% # %CALLNAME:(TARGET)% \@ lets out a weak, muffled scream as %HE_SHE(TARGET)% realizes you've ejaculated inside %HIM_HER(TARGET)%...")%
		ENDIF
		SIF NOWEX:MASTER:射精 > 1
			PRINTFORML You keep moving your waist while continuing to shoot your %SAMEN_DESCRIPTION(PLAYER)%, completely immersed in pleasure...
	ENDIF
	CALL POISONBODY_MESSAGE(1)
	IF SHIRAHU(TARGET)
		CALL QoL_Info_Write(TARGET, Qol_Dates_Semen) ;qol custom code
		CALL QoL_Info_Write(TARGET, Qol_Dates_Creampie, "vag") ;qol custom code
	ENDIF
;アナル
ELSEIF GETBIT(TFLAG:1,2)
	IF NOWEX:TARGET:Ａ絶頂
		PRINTFORML %CALLNAME:(TARGET)% clamps down almost painfully hard as %HE_SHE(TARGET)% reaches climax.
	ENDIF
	PRINTFORM \@ NOWEX:TARGET:Ａ絶頂 ? At the same time, you # You \@ start ejaculating inside the intestines of 
	SIF FLAG:70 || CFLAG:MASTER:イタズラ == 2
		PRINTFORM %COND_STR(@"the sleeping ", !FLAG:70)%
	PRINTFORML %CALLNAME:(TARGET)%%COND_STR(@" while time is frozen", FLAG:70)%.
	PRINTFORMW %HIS_HER(TARGET,1)% intestines tremble as they overflow with %SAMEN_DESCRIPTION(PLAYER)%, tightening around your %TINKO(PLAYER)%...
	IF !FLAG:70 && CFLAG:MASTER:イタズラ != 2
		IF ABL:TARGET:肛射中毒 > 3 && !FLAG:70
			PRINTFORML As semen floods %CALLNAME:TARGET%, pleasure ripples through %HIS_HER(TARGET)% body, causing %HIS_HER(TARGET)% face to melt and anus to tighten.
			PRINTFORML %BREAKENG(@"The sensation of sperm flooding %CALLNAME:(TARGET)%'s anus causes %HIM_HER(TARGET)% to let out a moan as %HIS_HER(TARGET)% face melts just from the pleasure of being ejaculated inside of alone.")%
		ELSEIF TCVAR:TARGET:無理矢理 && !FLAG:70
			PRINTFORML %BREAKENG(@"\@ NOWEX:TARGET:Ａ絶頂 ? For a moment, %CALLNAME:(TARGET)%'s face is absorbed in pleasure as %HE_SHE(TARGET)% reaches climax, before %HE_SHE(TARGET)% # %CALLNAME:(TARGET)% \@ lets out a weak, muffled scream as %HE_SHE(TARGET)% realizes you've ejaculated inside %HIM_HER(TARGET)%, your %SAMEN_DESCRIPTION(PLAYER)% climbing %HIS_HER(TARGET)% intestines...")%
		ENDIF
	ENDIF
	CALL POISONBODY_MESSAGE(2)
	IF SHIRAHU(TARGET)
		CALL QoL_Info_Write(TARGET, Qol_Dates_Semen) ;qol custom code
		CALL QoL_Info_Write(TARGET, Qol_Dates_ACreampie) ;qol custom code
	ENDIF
;手淫
ELSEIF GETBIT(TFLAG:1,3)
	IF FLAG:70 || CFLAG:MASTER:イタズラ == 2
		PRINTFORMW You push your %TINKO(PLAYER)% into %CALLNAME:(TARGET)%'s hand and use it to masturbate, before rubbing your glans and ejaculating...
	ELSE
		IF CFLAG:うふふ == 2
			PRINTFORML As you begin to cum, %CALLNAME:(TARGET)% starts stroking your %TINKO(PLAYER)% with even more force.
			PRINTFORML %HE_SHE(TARGET,1)% watches contently as you \@ NOWEX:PLAYER:射精 > 1 ? fire massive quantities of # shoot out \@ %SAMEN_DESCRIPTION(PLAYER)%.
			PRINTFORMW %HE_SHE(TARGET,1)% keeps caressing your %TINKO(PLAYER)% until it stops twitching...
		ELSE
			IF ABL:TARGET:精液中毒 >= 5 && ABL:TARGET:技巧 > 3
				PRINTFORML %CALLNAME:(TARGET)%'s tongue lolls out as if in a trance as %HE_SHE(TARGET)% showers %HIMSELF_HERSELF(TARGET)% in your %SAMEN_DESCRIPTION(PLAYER,1)%...
			ELSEIF NOWEX:PLAYER:射精 == 1
				PRINTFORMW You fire your %SAMEN_DESCRIPTION(PLAYER)% onto %CALLNAME:(TARGET)%...
			ELSE
				PRINTFORMW You splatter your %SAMEN_DESCRIPTION(PLAYER)% across %CALLNAME:(TARGET)%, covering %HIS_HER(TARGET)% arms and face with large quantities...
			ENDIF
		ENDIF
	ENDIF
	CALL POISONBODY_MESSAGE(3)
	SIF SHIRAHU(TARGET)
		CALL QoL_Info_Write(TARGET, Qol_Dates_Semen, "hj") ;qol custom code
;フェラチオ
ELSEIF GETBIT(TFLAG:1,4)
	IF NOWEX:TARGET:Ｍ絶頂
		IF FLAG:70 || CFLAG:MASTER:イタズラ == 2
			PRINTFORML You rub your %TINKO(PLAYER)% against the roof of %CALLNAME:TARGET%'s mouth, and then %HIS_HER(TARGET)% lips begin to quiver.
		ELSE
			PRINTFORML %CALLNAME:(TARGET)% seems to be completely enraptured by having your %TINKO(PLAYER)% in %HIS_HER(TARGET)% mouth.
			PRINTFORML Soon after, %HIS_HER(TARGET)% shoulders begin to shake and %HIS_HER(TARGET)% mouth clamps down as %HE_SHE(TARGET)% reaches climax.
		ENDIF
	ENDIF
	IF FLAG:70 || CFLAG:MASTER:イタズラ == 2
		SELECTCASE RAND:2
			CASE 0
				PRINTFORMW You thrust your %TINKO(PLAYER)% into %CALLNAME:(TARGET)%'s throat and ejaculate...
			CASE 1
				PRINTFORMW You rub the glans of your %TINKO(PLAYER)% against %CALLNAME:(TARGET)%'s tongue, before coating it in %SAMEN_DESCRIPTION(PLAYER,1)%...
		ENDSELECT
	ELSE
		IF CFLAG:うふふ == 2
			PRINTFORML You ejaculate inside %CALLNAME:(TARGET)%'s mouth.
			PRINTFORMW %HE_SHE(TARGET,1)% holds the %SAMEN_DESCRIPTION(PLAYER)% in %HIS_HER(TARGET)% mouth while using %HIS_HER(TARGET)% tongue to caress the glans, milking you of semen...
		ELSEIF SELECTCOM == 140
			PRINTFORML You push your %TINKO(PLAYER)% into the back of %CALLNAME:(TARGET)%'s throat and start to ejaculate.
			PRINTFORMW You fire your %SAMEN_DESCRIPTION(PLAYER,1)% right into %HIS_HER(TARGET)% stomach...
		ELSE
			PRINTFORMW You ejaculate inside %CALLNAME:(TARGET)%'s mouth.
			;精飲
			IF EXP_UP(15,TARGET) == NOWEX:PLAYER:射精 * 3
				IF NOWEX:PLAYER:射精 == 1
					PRINTFORML %CALLNAME:(TARGET)% uses %HIS_HER(TARGET)% lips to stimulate the glans, filling %HIS_HER(TARGET)% mouth with %SAMEN_DESCRIPTION(PLAYER)%.
					PRINTFORMW Once all the %SAMEN_DESCRIPTION(PLAYER,1)% is inside %HIS_HER(TARGET)% mouth, %HE_SHE(TARGET)% gulps it all down.
				ELSE
					PRINTFORMW %CALLNAME:(TARGET)% holds the glans in %HIS_HER(TARGET)% mouth as it fires load after load of %SAMEN_DESCRIPTION(PLAYER)%, gulping it down...
				ENDIF
				;RETURN 1
			ELSE
				IF TCVAR:TARGET:精飲経験 == 0
					PRINTFORML %CALLNAME:(TARGET)% is surprised as the %TINKO(PLAYER)% jumps out of %HIS_HER(TARGET)% mouth.
					PRINTFORMW %HIS_HER(TARGET,1)% face gets covered in %SAMEN_DESCRIPTION(PLAYER)% - soiling it for the first time - and a bit landing in %HIS_HER(TARGET)% mouth gives %HIM_HER(TARGET)% the first taste of semen.
				ELSEIF TCVAR:TARGET:精飲経験 < 20
					PRINTFORML After you finish, %PNAME(TARGET)% spits out the %SAMEN_DESCRIPTION(PLAYER)% from %HIS_HER(TARGET)% mouth.
					PRINTFORMW It seems %HE_SHE(TARGET)% still hasn't gotten used to the taste of your %SAMEN_DESCRIPTION(PLAYER)%...
				ELSE
					IF NOWEX:PLAYER:射精 == 1
						PRINTFORML %CALLNAME:(TARGET)% uses %HIS_HER(TARGET)% lips to stimulate the glans, filling %HIS_HER(TARGET)% mouth with %SAMEN_DESCRIPTION(PLAYER)%.
						PRINTFORMW %HE_SHE(TARGET,1)% spits out the %SAMEN_DESCRIPTION(PLAYER)% in %HIS_HER(TARGET)% hands before showing it to you...
					ELSE
						PRINTFORML %CALLNAME:(TARGET)% sticks %HIS_HER(TARGET)% tongue out and uses it to prod the urethra while it continues to fire %SAMEN_DESCRIPTION(PLAYER,1)%.
						PRINTFORMW When %HE_SHE(TARGET)% opens %HIS_HER(TARGET)% mouth, it overflows with %SAMEN_DESCRIPTION(PLAYER)% and spills down...
					ENDIF
				ENDIF
				;RETURN 2
			ENDIF
		ENDIF
	ENDIF
	CALL POISONBODY_MESSAGE(4)
	SIF SHIRAHU(TARGET)
		CALL QoL_Info_Write(TARGET, Qol_Dates_Semen, "bj") ;qol custom code
;パイズリ
ELSEIF GETBIT(TFLAG:1,5)
	PRINTFORM Your 
	SIF FLAG:70 || CFLAG:MASTER:イタズラ == 2
		PRINTFORM 
	PRINTFORM 
	IF TALENT:バストサイズ > 0
		PRINTFORMW %TINKO(PLAYER)% fires its %SAMEN_DESCRIPTION(PLAYER,1)% lodged between%COND_STR(@" the sleeping", CFLAG:MASTER:イタズラ == 2)% %CALLNAME:(TARGET)%'s %OPPAI_DESCRIPTION_TR(TARGET)%%COND_STR(@" while time is stopped", FLAG:70)%...
	ELSEIF TALENT:バストサイズ < 0
		PRINTFORMW %TINKO(PLAYER)% fires its %SAMEN_DESCRIPTION(PLAYER,1)% while rubbing against%COND_STR(@" the sleeping", CFLAG:MASTER:イタズラ == 2)% %CALLNAME:(TARGET)%'s %OPPAI_DESCRIPTION_TR(TARGET)%%COND_STR(@" while time is stopped", FLAG:70)%...
	ELSE
		PRINTFORMW %TINKO(PLAYER)% fires its %SAMEN_DESCRIPTION(PLAYER,1)% lodged between%COND_STR(@" the sleeping", CFLAG:MASTER:イタズラ == 2)% %CALLNAME:(TARGET)%'s %OPPAI_DESCRIPTION_TR(TARGET)%%COND_STR(@" while time is stopped", FLAG:70)%...
	ENDIF
	IF NOWEX:PLAYER:射精 == 1
		IF TALENT:バストサイズ > 0
			PRINTFORMW A small pool of %SAMEN_DESCRIPTION(PLAYER,1)% is formed in the cleavage of %CALLNAME:(TARGET)%'s %OPPAI_DESCRIPTION_TR((TARGET))%...
		ELSE
			PRINTFORMW %CALLNAME:(TARGET)%'s upper body - from %HIS_HER(TARGET)% %OPPAI_DESCRIPTION_TR((TARGET))% to %HIS_HER(TARGET)% face - gets covered in %SAMEN_DESCRIPTION(PLAYER,1)%...
		ENDIF
	ELSE
		PRINTFORMW You fire %SAMEN_DESCRIPTION(PLAYER)% all over %CALLNAME:(TARGET)%, coating everything from %HIS_HER(TARGET)% chest to %HIS_HER(TARGET)% face in a thick layer...
	ENDIF
	IF NOWEX:TARGET:Ｂ絶頂
		IF FLAG:70 || CFLAG:MASTER:イタズラ == 2
			PRINTFORMW %CALLNAME:TARGET%'s %OPPAI_DESCRIPTION_TR((TARGET))% start quivering, revealing %HIS_HER(TARGET)% orgasm.
		ELSE
			PRINTFORML %CALLNAME:TARGET%'s %OPPAI_DESCRIPTION_TR((TARGET))% suddenly push down harder on your %TINKO(PLAYER)% as %CALLNAME:(TARGET)% reaches orgasm.
		ENDIF
	ENDIF
	CALL POISONBODY_MESSAGE(5)
	SIF SHIRAHU(TARGET)
		CALL QoL_Info_Write(TARGET, Qol_Dates_Semen, "tj") ;qol custom code
;疑似 
ELSEIF GETBIT(TFLAG:1,25) 
	PRINTFORM %CALLNAME:PLAYER%は 
	IF FLAG:70 
		PRINTFORM 時間を止めた 
	ELSEIF CFLAG:MASTER:イタズラ == 2 
		PRINT 眠っている 
	ENDIF 
	PRINTFORML %CALLNAME:TARGET%の膣口に擦りつけていたペニスを性器のすぐ上に押し付けて射精した… 
	PRINTFORMW %CALLNAME:TARGET%の下腹が%SAMEN_DESCRIPTION(PLAYER)%で汚されていく… 
	CALL POISONBODY_MESSAGE(25)
	SIF SHIRAHU(TARGET)
		CALL QoL_Info_Write(TARGET, Qol_Dates_Semen) ;qol custom code
;素股
ELSEIF GETBIT(TFLAG:1,6)
	IF !GETBIT(TALENT:性別,1) && NOWEX:TARGET:Ｃ絶頂
		IF FLAG:70 || CFLAG:MASTER:イタズラ == 2
			PRINTFORML %CALLNAME:TARGET%'s clitoris twitches as it rubs against your %TINKO(PLAYER)%, and %HE_SHE(TARGET)% reaches orgasm.
		ELSE
			PRINTFORML %CALLNAME:(TARGET)%'s body starts trembling as you rub against %HIS_HER(TARGET)% clitoris.
		ENDIF
	ENDIF
	IF FLAG:70 || CFLAG:MASTER:イタズラ == 2
		PRINTFORMW You rub your %TINKO(PLAYER)% against%COND_STR(@" the sleeping", !FLAG:70)% %CALLNAME:(TARGET)%'s genitals as you ejaculate%FLAG:80?" while time is stopped"#""%...
	ELSE
		PRINTFORMW You rub against %CALLNAME:TARGET%'s genitals as you ejaculate...
	ENDIF
	CALL POISONBODY_MESSAGE(6)
	SIF SHIRAHU(TARGET)
		CALL QoL_Info_Write(TARGET, Qol_Dates_Semen, "thigh") ;qol custom code
;足コキ
ELSEIF GETBIT(TFLAG:1,7)
	IF FLAG:70 || CFLAG:MASTER:イタズラ == 2
		PRINTFORMW You rub your %TINKO(PLAYER)% against%COND_STR(@" the sleeping", !FLAG:70)% %CALLNAME:(TARGET)%'s feet as you ejaculate%FLAG:80?" while time is stopped"#""%...
	ELSE
		PRINTFORM %CALLNAME:(TARGET)% 
		SIF TALENT:TARGET:サド
			PRINTFORM stares at you with a cold gaze as %HE_SHE(TARGET)% 
		PRINTFORMW rubs %HIS_HER(TARGET)% feet against your %TINKO(PLAYER)% as it fires \@ NOWEX:MASTER:射精 == 2 ? massive amounts of hot # hot \@ %SAMEN_DESCRIPTION(PLAYER)% over %HIS_HER(TARGET)% feet...
	ENDIF
	CALL POISONBODY_MESSAGE(7)
	SIF SHIRAHU(TARGET)
		CALL QoL_Info_Write(TARGET, Qol_Dates_Semen, "foot") ;qol custom code
;泡踊り
ELSEIF GETBIT(TFLAG:1,8)
	IF NOWEX:TARGET:射精 && NOWEX:MASTER:射精
		PRINTFORML You and %CALLNAME:(TARGET)% press hard against each other as you begin ejaculating...
		PRINTFORMW You two shudder as your penises rub together, both reveling in pleasure as you fire load after load...
	ELSEIF NOWEX:TARGET:射精
		PRINTFORMW %CALLNAME:(TARGET)% fires load after load of %SAMEN_DESCRIPTION(TARGET)% from %HIS_HER(TARGET)% %TINKO(TARGET)% while rubbing against you...
	ELSE
		PRINTFORMW %CALLNAME:(TARGET)% rubs against your body as you fire load after load of %SAMEN_DESCRIPTION(PLAYER,1)%...
	ENDIF
	CALL POISONBODY_MESSAGE(8)
	SIF SHIRAHU(TARGET)
		CALL QoL_Info_Write(TARGET, Qol_Dates_Semen, "foam") ;qol custom code
;アナル奉仕
ELSEIF GETBIT(TFLAG:1,9)
	PRINTFORMW You bend your head down and you begin ejaculating large quantities %SAMEN_DESCRIPTION(PLAYER)%, covering your face...
	SIF SHIRAHU(TARGET)
		CALL QoL_Info_Write(TARGET, Qol_Dates_Semen) ;qol custom code
;Ａ責め
ELSEIF GETBIT(TFLAG:1,12)
	PRINTFORM Your entire waist goes numb from the pleasure, as your penis 
	IF NOWEX:MASTER:射精 >= 2
		PRINTFORML starts firing massive loads of %SAMEN_DESCRIPTION(PLAYER)%...
		PRINTFORMW You keep firing %SAMEN_DESCRIPTION(PLAYER)% over and over, the pleasure washes over you as it agonizes you...
	ELSE
		PRINTFORMW begins leaking %SAMEN_DESCRIPTION(PLAYER)% like a faucet...
	ENDIF
	SIF SHIRAHU(TARGET)
		CALL QoL_Info_Write(TARGET, Qol_Dates_Semen) ;qol custom code
;Ｖ責め
ELSEIF GETBIT(TFLAG:1,13)
	IF NOWEX:MASTER:射精 >= 2
		PRINTFORML Your entire waist goes numb from the pleasure, as your penis begins firing large amounts of %SAMEN_DESCRIPTION(PLAYER)% without stopping...
		PRINTFORMW The pleasure of a female combines the that of ejaculation, filling your head with nothing but white...
	ELSE
		PRINTFORMW Your entire waist goes numb from the pleasure, as your penis begins leaking %SAMEN_DESCRIPTION(PLAYER)% like a faucet...
	ENDIF
	SIF SHIRAHU(TARGET)
		CALL QoL_Info_Write(TARGET, Qol_Dates_Semen) ;qol custom code
;足コキ
ELSEIF GETBIT(TFLAG:1,14)
	IF FLAG:70 || CFLAG:MASTER:イタズラ == 2
		PRINTFORMW You rub your %TINKO(PLAYER)% against \@ FLAG:70 ? # the sleeping%" "% \@%CALLNAME:(TARGET)%'s armpit as you ejaculate%FLAG:80?" while time is stopped"#""%...
	ELSE
		PRINTFORM %CALLNAME:(TARGET)% 
		SIF TALENT:TARGET:サド
			PRINTFORM stares at you with a cold gaze as %HE_SHE(TARGET)% 
		PRINTFORMW rubs %HIS_HER(TARGET)% armpit against your %TINKO(PLAYER)% as it fires \@ NOWEX:MASTER:射精 == 2 ? massive amounts of hot # hot \@ %SAMEN_DESCRIPTION(PLAYER)% into %HIS_HER(TARGET)% armpit...
	ENDIF
	CALL POISONBODY_MESSAGE(9)
	SIF SHIRAHU(TARGET)
		CALL QoL_Info_Write(TARGET, Qol_Dates_Semen, "armpit") ;qol custom code
ENDIF

IF CFLAG:TARGET:うふふ == 2
	SIF !(NOWEX:TARGET:Ｃ絶頂 + NOWEX:TARGET:Ｖ絶頂 + NOWEX:TARGET:Ａ絶頂 + NOWEX:TARGET:Ｂ絶頂 + NOWEX:TARGET:Ｍ絶頂)
		PRINTFORML After %CALLNAME:(TARGET)%'s climax ends and %HIS_HER(TARGET)% muscles give in, %HE_SHE(TARGET)% stares at you with a grin...
	IF GROUPMATCH(TCVAR:PLAYER:射精した箇所フラグ,1,2)
		CALL SHIBORITORI(MASTER,(TARGET))
	ELSEIF GROUPMATCH(TCVAR:TARGET:射精した箇所フラグ,1,2,50) ;addition custom code
		CALL SHIBORITORI((TARGET),MASTER)
	ENDIF
ENDIF

;custom code
;NAS: Replaced
CALL ZECCHOU_OMORASHI(MASTER)
		
globalLineSave = LINECOUNT
CALL SET_KOJO_COLOR(TARGET) ;custom code

@PrintMilkAndOmorashi(ARG, OMORASHI)
#DIM OMORASHI
#DIM IKIOI = 0, 1, 1, 2, 2, 3, 3, 3, 4
#DIM BOOST
#DIMS BED
;絶頂と噴乳とおもらし

;NAS: return new function
CALL ZECCHOU_OMORASHI(ARG)
RETURN

SIF ARG >= 3
	PRINTS CALLNAME:TARGET
IF ARG >= 5 && TEQUIP:50 > 0
	PRINTL 's meaty folds drip a sticky liquid, opening and closing as if panting...
ELSEIF ARG >= 5
	PRINTFORML  comes repeatedly, draining %HIS_HER(TARGET)% body of strength...
ELSEIF ARG >= 3
	PRINTFORML  \@CFLAG:MASTER:イタズラ == 2 ?collapses on the bed, breathing heavily #breathes heavily \@ while taking in the orgasm...
ENDIF

IF (ARG >= 7 && TCVAR:利尿剤 && TALENT:おもらし癖) || (ARG >= 9 && NOWEX:噴乳)
	PRINTFORM %HE_SHE(TARGET,1)% trembles and convulses
ELSEIF (ARG >= 7 && OMORASHI) || (ARG >= 7 && NOWEX:噴乳)
	PRINTFORM %HE_SHE(TARGET,1)% convulses
ELSEIF (ARG >= 5 && OMORASHI) || (ARG >= 5 && NOWEX:噴乳)
	PRINTFORM %HE_SHE(TARGET,1)% trembles
ELSEIF (ARG >= 3 && OMORASHI) || (ARG >= 3 && NOWEX:噴乳)
	PRINTFORM %HE_SHE(TARGET,1)% twitches
ELSEIF (ARG >= 1 && OMORASHI) || NOWEX:噴乳
	PRINTFORM %CALLNAME:TARGET% gasps
ENDIF
IF NOWEX:噴乳
	IF ARG >= 9
		LOCALS = , %HIS_HER(TARGET)% %NIPPLE_DESCRIPTION(TARGET)% vigorously spurting milk, sending it flying
	ELSEIF ARG >= 7
		LOCALS = , %HIS_HER(TARGET)% %NIPPLE_DESCRIPTION(TARGET)% shooting milk in steady bursts
	ELSEIF ARG >= 5
		LOCALS = , %HIS_HER(TARGET)% %NIPPLE_DESCRIPTION(TARGET)% gushing milk in gouts
	ELSEIF ARG >= 3
		LOCALS = , %HIS_HER(TARGET)% %NIPPLE_DESCRIPTION(TARGET)% squirting milk
	ELSE
		LOCALS = , milk dribbling from %HIS_HER(TARGET)% %NIPPLE_DESCRIPTION(TARGET)%
	ENDIF
	IF OMORASHI
		PRINTFORM %LOCALS%, while 
	ELSE
		PRINTFORML %LOCALS%...
	ENDIF
ELSEIF (ARG >= 1 && OMORASHI) ;custom code
	PRINTFORM , 
ENDIF
IF OMORASHI
	BOOST = TCVAR:利尿剤 && TALENT:おもらし癖
	IF BEDROOM(CFLAG:現在位置)
		BED = %BED_FUTON()%
		IF (CFLAG:現在位置 == CFLAG:MASTER:初期位置) && (CFLAG:初期位置 != CFLAG:現在位置)
			LOCALS = your %BED%
		ELSEIF CFLAG:現在位置 / 100 == MAIN_MAP && PRIVATEROOM:(CFLAG:現在位置 % 100)
			LOCALS = \@ (PRIVATEROOM:(CFLAG:現在位置 % 100)) == TARGET ? %HIS_HER(TARGET)% # %CALLNAME:(PRIVATEROOM:(CFLAG:現在位置 % 100))%'s \@ %BED%
		ELSEIF CFLAG:現在位置 == OMANEKIBEYA()
			LOCALS = %HIS_HER(TARGET)% %BED%
		ELSE
			LOCALS = the %BED%
		ENDIF
	ELSE
		LOCALS = the %\@ OUTROOF(CFLAG:現在位置) ? ground # floor \@%
	ENDIF
	SELECTCASE IKIOI:(MIN(ARG, 7) + BOOST)
		CASE 4
			PRINTFORM peeing %HIMSELF_HERSELF(TARGET)% without end.<br>
			IF BEDROOM(CFLAG:現在位置)
				PRINTFORM %TNAME(TARGET,1,,1)% urine soaks into %LOCALS%, getting it utterly drenched...
			ELSE
				PRINTFORM An excessively large puddle forms on %LOCALS%...
			ENDIF
		CASE 3
			IF BOOST
				PRINTFORM urine \@ NOWEX:噴乳 ? gushes # gushing \@ out of %HIS_HER(TARGET)% urethra like a water fountain.<br>
			ELSE
				PRINTFORM %SPLIT_G(@"a stream of urine \@ NOWEX:噴乳 ? squirts # squirting \@ out of %HIS_HER(TARGET)% urethral opening with great force:a vigorous current of urine \@ NOWEX:噴乳 ? gushes # gushing \@ out of %CALLNAME:TARGET%'s urethra")%, %SPLIT_G("forming:drawing")% a beautiful arc.<br>
			ENDIF
			IF BEDROOM(CFLAG:現在位置)
				PRINTFORM %TNAME(TARGET,1)% makes a large, soaked stain on %LOCALS%...
			ELSE
				PRINTFORM A large puddle forms on %LOCALS%...
			ENDIF
		CASE 2
			IF BOOST
				PRINTFORM a large portion of urine \@ NOWEX:噴乳 ? leaks # leaking \@ loudly out of %HIS_HER(TARGET)% urethra with no sign of stopping.<br>
			ELSE
				PRINTFORM urine \@ NOWEX:噴乳 ? oozes # oozing \@ out of %HIS_HER(TARGET)% urethra, wetting %HIS_HER(TARGET)% lower body.<br>
			ENDIF
			IF BEDROOM(CFLAG:現在位置)
				PRINTFORM %TNAME(TARGET,1)% makes a clearly visible stain on %LOCALS%...
			ELSE
				PRINTFORM The liquid keeps dripping down, making a puddle on %LOCALS%...
			ENDIF
		CASE 1
			IF BOOST
				PRINTFORM a bit of urine \@ NOWEX:噴乳 ? oozes # oozing \@ out of %HIS_HER(TARGET)% slit and trickles down %HIS_HER(TARGET)% thigh.<br>
			ELSE
				PRINTFORM a small trickle of urine \@ NOWEX:噴乳 ? spills # spilling \@ out of %HIS_HER(TARGET)% slit.<br>
			ENDIF
			IF BEDROOM(CFLAG:現在位置)
				PRINTFORM %TNAME(TARGET,1)% makes a small stain on %LOCALS% as it drips down...
			ELSE
				PRINTFORM The small droplets continue dripping on %LOCALS%...
			ENDIF
	ENDSELECT
	;this can get pretty long?
	CALL PRINT_BREAK
ENDIF
IF TEQUIP:乳首吸い && NOWEX:噴乳
	SELECTCASE ARG
		CASE IS >= 9
			LOCALS = %HIS_HER(TARGET)% deluge contained by your lips as you gulp loudly, swallowing all the milk...
		CASE IS >= 7
			LOCALS = drinking deeply from %HIS_HER(TARGET)% milk, just a tiny bit spilling over...
		CASE IS >= 5
			LOCALS = drinking %HIS_HER(TARGET)% overflowing milk...
		CASE IS >= 3
			LOCALS = drinking %HIS_HER(TARGET)% milk...
		CASEELSE
			LOCALS = licking the milk oozing out...
	ENDSELECT
	PRINTFORML You suck on %CALLNAME:TARGET%'s %NIPPLE_DESCRIPTION(TARGET)%, %LOCALS%
ENDIF
;addition custom code
CALL Add_Milking(TARGET, NOWEX:噴乳)

@POISONBODY_MESSAGE(ARG)
SIF !TFLAG:ポイズンボディ
	RETURN 

SELECTCASE ARG
	CASE 3, 5, 6, 7, 8, 9, 25, 26;体外, armpit added
		LOCALS '= @"The semen seems to wrap around %CALLNAME:(TARGET)%'s extremities, glowing faintly as it soaks into the skin"
	CASE 4, 20, 21, 22;口
		LOCALS '= @"The semen's aphrodisiacal component soaks into the back of %CALLNAME:(TARGET)%'s throat"
	CASE 1, ;膣
		LOCALS '= @"Unloaded into %CALLNAME:(TARGET)%'s vagina, the semen's aphrodisiacal component soaks into her core"
	CASE 2;アナル
		LOCALS '= @"Unloaded into %CALLNAME:(TARGET)%'s rectum, the semen's aphrodisiacal component soaks into %HIS_HER(TARGET)% core"
	CASE 24;子宮口
		LOCALS '= @"Unloaded into %CALLNAME:(TARGET)%'s womb, the semen's aphrodisiacal component stirs her female instincts"
ENDSELECT
CALL COLORMESSAGE(@"%LOCALS%...", C_PINK, 1)
